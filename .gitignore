node_modules/
.tmp/

# ML model artifacts — generated on Railway, never committed
*.pkl
python-api/ml/artifacts/*.json
temporary screenshots/
screenshot.mjs
serve.mjs
ohzi.io_.png
package-lock.json

# Internal AI agent config — not for public deployment
.claude/

# MCP server config — contains local creds (PATs, tokens, DSNs); never commit
.mcp.json

# Private business documents
web context/

# Test/scratch files
index-v3-test.html

# RETIRED 2026-05-13 — archival pages, no public surface
# symbol-gallery.html — SVG symbol reference (retired; was a Stage 4 reference)
# architecture.html — platform self-overview (retired; admin-only context)
# Kept on disk for direct-URL archival; not in nav, not in any active validator scope

# Backup files (already disallowed in robots.txt but keep out of repo)
*.backup.html
*.backup2.html

# Supabase CLI temp files
supabase/.temp/
supabase/.branches/

# Edge function secrets — never commit
.env
.env.local
.env.azure
.env.roboflow
.env.mcp
infra/mcp/.env.mcp
supabase/functions/.env
supabase/.env.local

# Test environment migration staging area (originals are restored to migrations/)
supabase/migrations_archived/

# Release gate per-machine state
.last-gate-pass

# Platform Guardian — generated on every run, no need to track history
platform_health.json
platform_baseline.json
lessons_report.json
autofix_report.json
*_report.json

# Leftover temp/scratch files
lps_tmp.json
*.tmp.json
CLAUDE (1).md
__pycache__/
*.pyc
*.pyo

# Playwright runtime artifacts — re-generated on each `npx playwright test` run.
# The committed test specs live under tests/*.spec.ts; everything below is
# disposable per-run output (traces, screenshots, videos, json report).
test-results/
playwright-report/
playwright/.cache/
playwright-report.json

# Memento local memory cache — health export (contains personal usage patterns)
memento_health.json

# Playwright MCP runtime output — contains REAL-USER PII (uploaded resumes:
# *-jsonresume.json / *-resume.docx/pdf), console logs, and ad-hoc screenshots.
# Never commit; these are disposable per-session MCP artifacts.
.playwright-mcp/

# Ad-hoc debug/critique screenshots dropped at repo root during MCP sweeps.
crit-*.png
resume-0*.png
resume-projects-awards-live.png
resume-reorder-controls.png
memento-panel-*.png

# Playwright MCP scratch snapshots (repo-root)
pm-snapshot.md
pm-detail.md
pm-*.png
*-snapshot.md
*-state.png
