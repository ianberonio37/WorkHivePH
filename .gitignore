node_modules/
.tmp/
temporary screenshots/
screenshot.mjs
serve.mjs
ohzi.io_.png
package-lock.json

# Internal AI agent config — not for public deployment
.claude/

# Private business documents
web context/

# Test/scratch files
index-v3-test.html

# Internal developer reference tools — not user-facing, no nav/auth
# symbol-gallery.html — SVG symbol reference for diagram builders
# architecture.html — platform architecture diagram (internal only)
# These are kept in repo for dev reference but excluded from nav-hub

# Backup files (already disallowed in robots.txt but keep out of repo)
*.backup.html
*.backup2.html

# Supabase CLI temp files
supabase/.temp/

# Leftover temp/scratch files
lps_tmp.json
*.tmp.json
CLAUDE (1).md
