node_modules/
.tmp/
.tmp_*

# ML model artifacts — generated on Railway, never committed
*.pkl
python-api/ml/artifacts/*.json
temporary screenshots/
screenshot.mjs
serve.mjs
ohzi.io_.png
package-lock.json

# Internal AI agent config — not for public deployment
.claude/

# MCP server config — contains local creds (PATs, tokens, DSNs); never commit
.mcp.json

# Private business documents
web context/

# Test/scratch files
index-v3-test.html

# RETIRED 2026-05-13 — archival pages, no public surface
# symbol-gallery.html — SVG symbol reference (retired; was a Stage 4 reference)
# architecture.html — platform self-overview (retired; admin-only context)
# Kept on disk for direct-URL archival; not in nav, not in any active validator scope

# Backup files (already disallowed in robots.txt but keep out of repo)
*.backup.html
*.backup2.html

# Supabase CLI temp files
supabase/.temp/
supabase/.branches/

# Edge function secrets — never commit
.env
.env.local
.env.azure
.env.roboflow
.env.mcp
infra/mcp/.env.mcp
supabase/functions/.env
supabase/.env.local

# Social-media publishing credentials (tokens/app-passwords Ian pastes once) —
# real brand-account secrets, NEVER commit. Template lives in social_accounts.env.example.
social_accounts.env
.social_publish_log.jsonl

# Test environment migration staging area (originals are restored to migrations/)
supabase/migrations_archived/

# Release gate per-machine state
.last-gate-pass

# Companion Dev Tool (companion_dev.py) per-machine run state
# (the substrate MANIFEST is tracked like substrate_manifest.json; these are not)
.last-companion-gate-pass
companion_dev_runs.jsonl

# Platform Guardian — generated on every run, no need to track history
platform_health.json
platform_baseline.json
lessons_report.json
autofix_report.json
*_report.json

# Leftover temp/scratch files
lps_tmp.json
*.tmp.json
CLAUDE (1).md
__pycache__/
*.pyc
*.pyo

# Playwright runtime artifacts — re-generated on each `npx playwright test` run.
# The committed test specs live under tests/*.spec.ts; everything below is
# disposable per-run output (traces, screenshots, videos, json report).
test-results/
playwright-report/
playwright/.cache/
playwright-report.json

# Memento local memory cache — health export (contains personal usage patterns)
memento_health.json

# Playwright MCP runtime output — contains REAL-USER PII (uploaded resumes:
# *-jsonresume.json / *-resume.docx/pdf), console logs, and ad-hoc screenshots.
# Never commit; these are disposable per-session MCP artifacts.
.playwright-mcp/

# Ad-hoc debug/critique screenshots dropped at repo root during MCP sweeps.
crit-*.png
resume-0*.png
resume-projects-awards-live.png
resume-reorder-controls.png
memento-panel-*.png
vm_*.png
walkthrough_*.png
wh_skillmatrix_*.png
companion-dev-tool-cockpit.png
tester-7layer-panel.png

# Playwright MCP scratch snapshots (repo-root)
pm-snapshot.md
pm-detail.md
pm-*.png
*-snapshot.md
*-state.png

# Content Grounding Gate runtime (mirror companion convention)
.last-content-gate-pass
content_dev_runs.jsonl
.momentum_drive
.momentum_allow_stop
.momentum_stop_state.json

# Native/sovereign AI local models + generated voice samples (large binaries, not committed)
models/piper/
wh_voice_*.wav
cand_*.wav
final_*.wav

# Night Crawler local watchlist (the .example is committed)
night_crawler_watch.txt

# ── Emoji/palette session (2026-07-19): transient backups + verification artifacts — never commit
*.pre-emoji.bak
.emoji_bak/
.palette_bak/
.leftover_bak/
.hexvar_bak/
.baseline_bak/
.fullgate.out
marketplace-emoji-cards.png
marketplace-emoji-top.png
analytics-emoji-first.png
analytics-palette.png
ai-quality-palette.png
hive-palette.png
index-emoji-check.png
index-sectors.png

# temp gate output + local data-backup artifacts (Arc S)
.fastgate.out
.fullgate.out
backups/
