{
  "breaches": 17,
  "_note": "8->9 2026-07-23: community.html crossed budget (html 152.1KB>152, inline_script 108.2KB>108, both ~0.1-0.2KB over) from the Y1b OFFLINE POST-QUEUE feature (a brownout post queues to IndexedDB + optimistically renders + drains on reconnect, deduped by a client-settable id). community was EXACTLY at budget (152/108) before, so a real field-resilience feature tips it. Lean effort was done FIRST: ~1KB of explanatory comments trimmed, the two optimistic-render branches deduplicated into one _queueOfflinePost() helper, and a dead _pendingSync marker removed \u2014 the residual ~0.2KB is irreducible feature code, not bloat. Same DOCUMENTED-feature-growth class as the integrations CMMS-fix (6->7) and marketplace contact-UI (7->8) breaches below. Prior 7->8: marketplace.html inline_script 112.3KB pre-existing breach. The 9 breaches: analytics/asset-hub/hive/index/inventory/logbook (large daily-driver pages) + integrations (CMMS fix) + marketplace (contact UI) + community (Y1b offline post-queue). Residual overages are documented feature/fix code on the platform's heaviest pages, not new bloat. || 9->10 2026-07-24: marketplace.html crossed its override (html 180.3KB>170, inline_script 122.7KB>112) during the marketplace deepwalk arc. VERIFIED PRE-EXISTING AT HEAD: reverting this change set to HEAD and re-running still reports 10, so the breach came from the arc-s EARLIER, already-deployed commits (MK1/2/4/7/8/9/10 fixes) and the baseline was simply not updated then. Lean effort was done FIRST on this change set: the two longest new comment blocks in openPostSheet and the guest stats card were compressed, and the Certified-badge rule repeated on 3 surfaces was extracted to window.whCertBadgeEarned in utils.js rather than inlined a third time. The residual is real feature logic (anon post disclosure, earned-badge guards, trust-signal render fixes). The structural fix remains PLATFORM_ROADMAP F.5, already named in this page-s override _reason: extract the person-card + related-listings / bridge modules out of the inline script. || 10->11 2026-07-28: pm-scheduler.html crossed budget (html 155.1KB>150, inline_script 103.7KB>100) from the PM Scheduler deepwalk arc. It sat at 148.8KB against a 150 budget BEFORE this arc - one of the tightest margins on the platform - so the arc's real fixes tipped it: the shared whFreqFromDays interval mapping (an RCM interval was being rounded to a RARER frequency, which is the direction that leaves equipment un-inspected), the pm_assets insert/update/delete guards, and the completion scope-parent guard. Raised rather than trimmed because the added bytes are the guards themselves plus the comments recording WHY each exists, and this platform has no build step so nothing can strip comments at deploy. FLAGGED AS DEBT, not absolved: pm-scheduler and asset-hub are both now well over and are the two best candidates for an inline-script extraction pass. || 11->12 2026-07-28: project-manager.html crossed (html 175.5KB>175) during the PROJECT MANAGER deepwalk arc. It sat at 172KB against a 175 budget before the arc. The added bytes are REAL code paths, not decoration: the supervisor-only budget READ and WRITE now route through RPCs (get_project_budget / set_project_budget) instead of a plain column, openDetail enumerates its columns because a select('*') would 42501 against the revoked column, and the CPM and Earned Value panes gained honest 'could not compute' branches that used to render an absence as a verdict. TRIMMED FIRST rather than reaching for this: six long comments were condensed to point at migrations 023/024/026/030 where the same reasoning is held verbatim, taking the page 176.4 -> 175.5KB. The remaining 0.5KB is the code itself. FLAGGED AS DEBT, not absolved: project-manager, pm-scheduler and asset-hub are now the three best candidates for an inline-script extraction pass, and this platform has no build step so nothing strips comments at deploy. || 13->17 2026-08-20: alert-hub, dayplanner, report-sender and resume crossed budget during the page-testbank walk. VERIFIED NEW, not inherited: each page was re-measured at HEAD with this gate's own measure() and all four were WITHIN budget there (alert-hub inline 71.2, dayplanner 69.4, report-sender 55.9, resume html 138.2/inline 118.7), so the growth is this release's. Deltas: alert-hub +16.4KB, dayplanner +20.9KB inline, report-sender +26.0KB inline, resume +11.3KB inline. WHAT the bytes are, measured not claimed: roughly half of every added line is comment (alert-hub 119 of 225 added lines, dayplanner 150 of 354, report-sender 205 of 400, resume 92 of 216) and the comments record WHY each guard exists - the offline refusal on the one action that cannot be undone, the edge function's own reason no longer being thrown away, the contact that vanished locally while the server never heard, the 44px touch floor measured at 36px. This platform has no build step, so nothing strips comments at deploy and they ship over the wire. NOT ABSOLVED, FLAGGED AS DEBT: report-sender is only 1.9KB over and is the cheapest to reclaim; the structural fix for all four is the same inline-script extraction already named for pm-scheduler, asset-hub and project-manager. Seven pages now carry that debt, which makes extraction a roadmap item rather than a per-page trim.",
  "scope_growth": {
    "marketplace-seller.html": {
      "surface": "svc",
      "min_functions": 10,
      "reason": "the whole service-hailing PROVIDER CONSOLE landed on this page this arc - registration, the open-hail feed, accept/advance/cancel, the wallet, GCash top-ups, push opt-in, the live-location publisher and the job-list poll. The page went 80KB -> 118KB total. LEAN EFFORT WAS DONE FIRST and is verifiable, not claimed: whRequireOnline and whLivePoll were both extracted to utils.js (removing a duplicated guard from BOTH marketplace pages and the poll's three timer disciplines from this one), and the comment that then duplicated utils.js was cut to a pointer - 88.5 -> 87.1KB. The residual 1.1KB is feature code. DEBT, NOT ABSOLVED: this page now hosts two products (the listing seller AND the provider console) and is the best candidate for the split Ian and I considered - tracked as its own arc rather than rushed at the tail of this one."
    }
  },
  "_scope_growth_doc": "Every raise above was justified in PROSE and unverifiable. An entry here is CHECKED on every run: the gate re-counts `function <surface>*` definitions in the named page and reds if the claimed surface is gone. An allowance may not outlive its reason."
}