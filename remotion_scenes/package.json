{
  "name": "workhive-remotion-scenes",
  "version": "1.0.0",
  "private": true,
  "description": "Branded, data-driven background scenes for the WorkHive video pipeline (prototype). Renders to .tmp/scene_clips so the existing video_assembler.py can use it in the scene_clip slot.",
  "scripts": {
    "studio": "remotion studio",
    "render": "remotion render WorkHiveOEEScene out/workhive-oee-scene.mp4 --codec=h264"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.4.0"
  }
}
