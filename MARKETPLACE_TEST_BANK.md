# Marketplace Test Bank

_Generated by `tools/build_test_bank.py --doc`. The derived skeleton comes from the four guard functions; the authored cells are the scenarios no generator describes._

**298 obligations** · 99 already locked by an existing gate · 198 asserted by the bank · 1 owed

## Authored cells — the scenarios

| Cell | Lane | Layers | Runner / gate | Status |
|---|---|---|---|---|
| `TB-A345-architecture-quality` | sql | S1-ui · S3-data | — | owed |
| `TB-ANNC-announcement-supervisor` | sql | S4-db · S8-gates | TB-ANNC-announcement-supervisor.sql | banked |
| `TB-BIRTH-privileged-birth-refused` | sql | S4-db · S8-gates | TB-BIRTH-privileged-birth-refused.sql | banked |
| `TB-CAP-pdf-job-chunk-ceiling` | sql | S4-db · S8-gates | TB-CAP-pdf-job-chunk-ceiling.sql | banked |
| `TB-CO-change-order-terms` | sql | S4-db · S8-gates | TB-CO-change-order-terms.sql | banked |
| `TB-DAILY-pdf-user-cap` | sql | S4-db · S8-gates | TB-DAILY-pdf-user-cap.sql | banked |
| `TB-F1-affordances-are-named` | sql | S1-ui · S8-gates | — | covered |
| `TB-FIELD-nonstatus-edits-and-hive-party` | sql | S4-db · S8-gates | TB-FIELD-nonstatus-edits-and-hive-party.sql | banked |
| `TB-FWD-forward-only-status` | sql | S4-db · S8-gates | TB-FWD-forward-only-status.sql | banked |
| `TB-I1-anon-gating` | sql | S4-db · S1-ui | TB-I1-anon-gating.sql | banked |
| `TB-I2-admin-bypass-only-for-non-parties` | sql | S4-db · S8-gates | TB-I2-admin-bypass-only-for-non-parties.sql | banked |
| `TB-I4-client-validates-before-writing` | sql | S1-ui · S8-gates | — | covered |
| `TB-I5-audit-outlives-the-action` | sql | S4-db · S8-gates | TB-I5-audit-outlives-the-action.sql | banked |
| `TB-IMG-inline-image-cap` | sql | S4-db · S8-gates | TB-IMG-inline-image-cap.sql | banked |
| `TB-J2-inquiry-roundtrip` | journey | S1-ui · S3-data · S4-db | — | banked |
| `TB-L4-service-views-party-scope` | sql | S4-db · S8-gates | truth-view-read-isolation | banked |
| `TB-LESSONS-supervisor-only` | sql | S4-db · S8-gates | TB-LESSONS-supervisor-only.sql | banked |
| `TB-MINT-topup-party-routes-and-mint-once` | sql | S4-db · S8-gates | TB-MINT-topup-party-routes-and-mint-once.sql | banked |
| `TB-MK1-trust-signal-integrity` | journey | S1-ui · S3-data · S4-db | — | banked |
| `TB-MK10-ranking-transparency` | journey | S1-ui | — | banked |
| `TB-MK11-error-remedy-actionability` | sql | S1-ui · S8-gates | tools/validate_error_remedy_actionable.py | covered |
| `TB-MK12-post-action-coherence` | sql | S1-ui · S8-gates | tools/validate_post_action_coherence.py | covered |
| `TB-MK13-reachable-capability` | sql | S1-ui · S8-gates | tools/validate_reachable_capability.py | covered |
| `TB-MK4-lifecycle-state-reachability` | sql | S4-db · S1-ui · S8-gates | lifecycle-state-reachability | banked |
| `TB-MK7-jsonld-truth` | sql | S1-ui · S8-gates | jsonld-truth | banked |
| `TB-MK8-payment-disclosure` | sql | S1-ui · S8-gates | payment-disclosure | banked |
| `TB-MK9-response-sla-honesty` | sql | S1-ui · S4-db | TB-MK9-response-sla-honesty.sql | banked |
| `TB-MR-metamorphic-relations` | sql | S3-data · S4-db | TB-MR-metamorphic-relations.sql | banked |
| `TB-PLOG-progress-log-record` | sql | S4-db · S8-gates | TB-PLOG-progress-log-record.sql | banked |
| `TB-QUOTA-ai-reports-both-modes` | sql | S4-db · S8-gates | TB-QUOTA-ai-reports-both-modes.sql | banked |
| `TB-QUOTA-community-cumulative` | sql | S4-db · S8-gates | TB-QUOTA-community-cumulative.sql | banked |
| `TB-QUOTA-invtx-cumulative` | sql | S4-db · S8-gates | TB-QUOTA-invtx-cumulative.sql | banked |
| `TB-QUOTA-logbook-cumulative` | sql | S4-db · S8-gates | TB-QUOTA-logbook-cumulative.sql | banked |
| `TB-QUOTA-pm-cumulative` | sql | S4-db · S8-gates | TB-QUOTA-pm-cumulative.sql | banked |
| `TB-RATE-ai-feedback-daily` | sql | S4-db · S8-gates | TB-RATE-ai-feedback-daily.sql | banked |
| `TB-RATE-community-post` | sql | S4-db · S8-gates | TB-RATE-community-post.sql | banked |
| `TB-RATE-community-reply` | sql | S4-db · S8-gates | TB-RATE-community-reply.sql | banked |
| `TB-RATE-listing-daily` | sql | S4-db · S8-gates | TB-RATE-listing-daily.sql | banked |
| `TB-RATE-logbook-per-user` | sql | S4-db · S8-gates | TB-RATE-logbook-per-user.sql | banked |
| `TB-RATE-platform-feedback-identity-bucket` | sql | S4-db · S8-gates | TB-RATE-platform-feedback-identity-bucket.sql | banked |
| `TB-RATE-review-daily` | sql | S4-db · S8-gates | TB-RATE-review-daily.sql | banked |
| `TB-S2-pwa-offline-hail-degraded` | journey | S2-pwa · S1-ui | offline-write-guard | banked |
| `TB-S5-edge-push-audience-selection` | sql | S5-edge · S4-db | TB-S5-edge-push-audience-selection.sql | banked |
| `TB-S5-edge-push-delivery-roundtrip` | journey | S5-edge · S4-db | — | banked |
| `TB-S6-realtime-map-datapath-publisher-x-watcher` | sql | S6-realtime · S4-db | TB-S6-realtime-map-datapath-publisher-x-watcher.sql | banked |
| `TB-S6-realtime-map-ui-publisher-x-watcher` | journey | S6-realtime · S1-ui · S4-db | tools/validate_marketplace_bank_journey.py | banked |
| `TB-S7-ai-triage-eval-oracle` | sql | S7-ai · S5-edge | service-triage-eval | banked |
| `TB-S8-gates-surface-honesty` | sql | S8-gates · S1-ui | gate-panel-honesty | banked |
| `TB-S9-knowledge-writeback-roundtrip` | sql | S9-knowledge · S4-db | TB-S9-knowledge-writeback-roundtrip.sql | banked |
| `TB-SELLER-trust-columns` | sql | S4-db · S8-gates | TB-SELLER-trust-columns.sql | banked |
| `TB-SJ01-responder-answers-the-hail` | journey | S1-ui · S4-db · S3-data | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SJ06-the-quoter-side` | sql | S4-db · S3-data | TB-SJ06-the-quoter-side.sql | banked |
| `TB-SJ06-ui-the-quoter-composes-and-the-client-compares` | journey | S1-ui · S4-db · S3-data | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SJ07-ui-the-watcher-sees-the-job-advance` | journey | S1-ui · S4-db | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SJ09-10-cancellation-tells-the-other-party` | sql | S4-db · S5-edge | TB-SJ09-10-cancellation-tells-the-other-party.sql | banked |
| `TB-SJ09-ui-the-open-page-learns-of-the-cancellation` | journey | S1-ui · S6-realtime · S4-db | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SJ10-ui-the-client-learns-the-provider-cancelled` | journey | S1-ui · S4-db | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SJ28-ui-a-waiting-provider-sees-the-hail-arrive` | journey | S1-ui · S5-edge · S4-db | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SJ33-ui-presence-counts-a-provider-who-just-came-online` | journey | S1-ui · S4-db | tools/validate_marketplace_bank_journey.py | banked |
| `TB-SNEAK-replay-and-session-switch` | sql | S4-db | TB-SNEAK-replay-and-session-switch.sql | banked |
| `TB-SREVIEW-service-review-integrity` | sql | S4-db · S8-gates | TB-SREVIEW-service-review-integrity.sql | banked |
| `TB-STATE-inducers-empty-filtered0-edge` | sql | S3-data · S4-db | TB-STATE-inducers-empty-filtered0-edge.sql | banked |
| `TB-STATE-journey-error-and-degraded` | journey | S1-ui · S2-pwa | — | covered |
| `TB-TRUST-provider-cannot-self-verify` | sql | S4-db · S8-gates | TB-TRUST-provider-cannot-self-verify.sql | banked |

### What each one proves

**`TB-A345-architecture-quality`**
- (none yet - deliberately not banked; a weak cell would inflate the UFAI board with an assertion that proves nothing)

_Why:_ Recorded so the last three unmeasured UFAI cells are a stated disposition with evidence rather than a silent gap. 'Covered-by-nature' is the smallest, last-resort bucket and is claimed only after the build attempt genuinely fails ([[feedback_build_structure_to_make_it_liveable]]).

**`TB-ANNC-announcement-supervisor`**
- a community post with category='announcement' may only be created by a SUPERVISOR of its hive: a worker is refused (42501), a supervisor is allowed - the write that separates 'supervisor-gated announcements' from 'no announcements'
- an ordinary post (general/safety/technical/marketplace) is anyone's to write, so the guard only fires on the announcement category
- a single insert never trips this table's rate-limit or daily-quota triggers, so what refuses the worker's announcement is unambiguously this guard

_Why:_ guard_community_announcement, found unscored 2026-07-31 (ARC 13 / F, roadmap S14).

**`TB-BIRTH-privileged-birth-refused`**
- no row may be BORN in a privileged state: a request cannot start `accepted` or already matched, a top-up cannot start `verified`, an order cannot start `released`
- each refusal is paired with the legitimate creation, so a guard that refused everything would fail
- the credit ledger is read back: a refused born-verified top-up must mint NOTHING
- the attribution case asserts WHICH LAYER refuses (`guard`), pinning that a BEFORE trigger runs ahead of RLS WITH CHECK — if the guard's pin were removed the value would become `rls`

_Why:_ Found 2026-07-30 by the guard mutation score, not by reading. Every one of the bank's 247 cells is derived from the authorised-TRANSITION set, so all of them are UPDATEs and none had ever INSERTed a row as a raw client; each guard's `TG_OP = 'INSERT'` branch had zero cells. Deleting those birth rules one at a time left `birth_status_unchecked`, `born_matched_allowed` and `refusal_removed` (top-ups AND orders) alive with no cell objecting. The top-up case is the sharp end: `verified` is what mints credit, so a client able to insert an already-verified top-up never needs to defeat the verification path at all. Reachability was checked against pg_policies BEFORE authoring - `status` appears in no INSERT policy, so these rules are the guard's alone.

**`TB-CAP-pdf-job-chunk-ceiling`**
- the boundary from BOTH sides: exactly 200 chunks is allowed and 201 is refused. Testing only 201 would pass a guard written >= 200, which would reject a legitimate 200-chunk document
- the sqlstate is asserted (54000, the code this guard chose) and not merely the refusal - the first run of this cell was blocked at 23514 by pdf_jobs_target_table_check, which without the code would have read as "the cap rejects a legitimate 200-chunk job"
- a non-array payload counts as ZERO chunks and passes, which is deliberate - pinned so a future rewrite cannot start rejecting ordinary jobs

_Why:_ Fourth and mildest of the guards no registered gate names (2026-07-30 ranking of all 27 un-scored guards). A resource bound, not money or tenancy: its failure mode is a stalled embedding queue. Included for completeness because a ceiling nobody tests quietly becomes a suggestion.

**`TB-CO-change-order-terms`**
- a raised change order's commercial terms are immutable to a user: cost_impact_php, title, scope_change, co_number and schedule_impact_days are all refused - a contract amendment whose terms can be quietly rewritten is not a record
- a change order cannot be DELETED by a user (cancel it instead, so it stays on the amendment trail)
- the guard's own backend branch (auth.uid() null) still allows a seeder/service write, so the positive isolates THIS trigger rather than depending on another's verdict
- no generic operator bleeds onto this guard - it shares no variable names with the status machines and calls neither is_marketplace_admin() nor a system-write GUC, so it scores against its own five column/DELETE operators alone

_Why:_ guard_change_order_terms_immutable, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). A change order is a contract amendment (fix 8059cc4); the lifecycle fields are owned by wh_guard_supervisor_approval, this guard owns the immutable terms.

**`TB-DAILY-pdf-user-cap`**
- the SHARED per-user daily row cap (check_daily_row_cap, uploaded_by, 10/day on pdf_jobs) refuses the 11th (10 planted today) with 54000 - one uploader cannot flood the ingest queue
- a different uploader is unaffected (per user, not a freeze); hive_id NULL skips the per-hive branch and chunks_json NULL keeps the size cap out, so the daily cap is the sole refuser

_Why:_ check_daily_row_cap, the shared TG_ARGV daily cap on ~20 tables, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Scored on pdf_jobs (lowest caps, no count sibling).

**`TB-F1-affordances-are-named`**
- an icon-only control is not a control until it has a name - a signifier nobody can read is decoration

_Why:_ F1_completeness maps to the D1 affordances dim, recorded unmeasured on three of the four marketplace pages. ANTI-DUPLICATION: two registered gates already assert it platform-wide, so the bank records the class as covered and NAMES them rather than building a third detector.

_Note:_ My own quick probe reported 3 unnamed controls on marketplace.html. All three were FALSE: it stripped `${...}` template text, which is where those buttons' visible labels come from. That was the fourth instrument false-positive of the session, and the reason the answer here is 'name the gate that already measures this' rather than 'write a fourth detector of my own'.

**`TB-FIELD-nonstatus-edits-and-hive-party`**
- the rules that hold when status is UNCHANGED - the branch a transition-shaped suite cannot enter: matching may not be reassigned by a direct write, ownership may not be transferred
- an admin who is a party ONLY through hive membership is still a party, so the admin bypass does not apply and an illegal transition on their own hive's job is refused
- the permission that branch exists to grant is asserted too: an active hive member may act for the hive's provider profile (accepted -> en_route)
- the stranger case records the LAYER (`rls-filtered`), because a USING clause hides the row and the guard never speaks - crediting the guard with that refusal would keep reading green if its rule were deleted
- the cancel window is asserted from OUTSIDE its boundary: a SETTLED (done and paid) job cannot be cancelled by the client - the case the state_list_widened mutant demanded, which 109 cells missed because the derived grid enumerates neither that transition nor that illegal origin
- the ownership case asserts the LAYER, not the refusal: strip the guard's rule and RLS still rejects the row, so `blocked` would stay green - only `guard` vs `rls` can tell the difference
- the chain's ORDER, not just its membership: en_route -> in_progress skips on_site, so a provider would be recorded as working without having arrived. The grid proves each hop is PERMITTED; nothing proved the hops cannot be SHORT-CIRCUITED, and a mutation widening that clause survived 121 cells

_Why:_ Found 2026-07-30 by the guard mutation score. `reassignment_allowed`, `ownership_transfer_allowed`, `stranger_field_edit_allowed` and `hive_provider_branch_removed` all survived across 107 cells on guard_service_request_status - the four rules that live in the `status unchanged` branch. The hive one is a self-deal hole rather than a field rule: v_is_party is satisfied EITHER by owning the provider (TB-I2 covers that) OR by active membership of the hive that owns it (nothing covered that), so dropping the hive branch would let an admin whose own hive is doing the work compute as a non-party moderator and take the bypass. The operator rewrites the FIRST occurrence, which is the copy inside v_is_party used only by the admin bypass - so an ADMIN party-via-hive is the only caller who can observe the mutant, which is why no ordinary-caller cell could ever have killed it.

**`TB-FWD-forward-only-status`**
- anomaly_signals cannot regress out of a terminal state (resolved/expired -> active is refused), but a terminal->terminal move and any forward move are allowed - forward-only, not frozen
- shift_plans rank cannot go backward (published -> draft is refused) while a forward move (published -> archived) is allowed
- neither guard checks auth.uid(), so the ratchet applies to EVERY caller including the backend - no other trigger's verdict is involved

_Why:_ Two forward-only status guards, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Pure ratchets, no auth branch.

**`TB-I1-anon-gating`**
- OPEN: an unauthenticated visitor CAN read the active catalogue - a marketplace nobody can price has nobody to join it
- CLOSED: anon reads 0 live positions, 0 top-ups (the GCash reference ties a phone number to a payment), 0 service requests
- CLOSED: anon cannot create a job
- OBSERVED: the service provider directory refuses anon today - correct for the current product, recorded so a change is a decision rather than a drift

_Why:_ The derived matrix generates an `anon` authority-negative for EVERY guarded transition and the SQL runner maps `anon` to no probe identity, so it skipped every one. The partition with the largest blast radius - an unauthenticated stranger on the public internet - was enumerated as an obligation and never executed. UFAI I1 was recorded pct:null for the same surface.

_Note:_ Two instrument lessons are baked in. (1) A single 42501 aborts a postgres transaction, so an unguarded sequence of anon reads stops at the first denial and the rest silently never run - each read is now its own plpgsql exception block. (2) Emitting from inside one requires RAISE NOTICE, which psql writes to STDERR, so the runner now parses RESULT lines from both streams; parsing stdout alone made a fully-passing probe look empty.

**`TB-I2-admin-bypass-only-for-non-parties`**
- a platform admin who is a PARTY to the row falls through to the ordinary party rules
- a platform admin who is NOT a party keeps every moderation power
- the ledger is read back: a blocked self-verify must mint NO credit, a legitimate one exactly one
- THE ROW-VERSION HOLE (2 live exploits, mig 20260730000006): the party gate reads coalesce(OLD.seller_name, NEW.seller_name) while the consequence lands on NEW, so ONE statement passed the gate on the old values and collected on the new ones - an admin claimed a stranger SALE (admin_total_sales=1, real seller 0) and took over a stranger LISTING (published as their own). The trust counter is read back, because a blocked claim that still bumped it would be the worst outcome and the status assertion could not see it
- THE PIN, COLUMN BY COLUMN: migs 005/006 pin several fields per guard and only seller_name had a cell. An 8th mutation wave deleted the remaining columns one at a time and nothing objected, so price, hive_id (orders AND listings) and gcash_ref each get one. A pin over N columns is only asserted for the columns someone thought to test - the every-disjunct-needs-a-negative rule applied to a CONJUNCTION.

_Why:_ mig 20260730000003. Found 2026-07-30: teaching the SQL runner the `anon` partition made `admin` the next unexecuted partition, and minting an admin identity to run it surfaced the bypass. A probe proved a provider-admin firing completed->settled on its OWN job (rows=1) - the client's 'I paid' confirmation, which fires trg_mint_settlement_commission. The same unqualified is_marketplace_admin() early-return stood in all FOUR status guards, five weeks after mig 20260729000003 fixed exactly this in guard_service_review.

_Note:_ TEETH VERIFIED by restoring the vulnerable guard inside a rolled-back transaction: the self-deal came back as ALLOWED, so the `blocked` assertions bite. The same technique showed that service_requests moderation returned 0 rows BEFORE and AFTER the fix - its RLS policy has no admin clause, so a non-party admin's row is filtered away and that bypass's only reachable effect was the self-deal. The other three tables DO have an admin RLS clause, which is why moderation is asserted as `works` there and deliberately not asserted at all on service_requests.

**`TB-I4-client-validates-before-writing`**
- a handler that reads a user-typed field and writes it must validate first - the DB CHECK is the backstop, not the first line

_Why:_ I4_client_validation was recorded unmeasured on marketplace-admin and marketplace-seller-profile. ANTI-DUPLICATION: a registered gate already asserts it across all 42 pages, so the bank names it rather than rebuilding it.

_Note:_ Paired deliberately with the SQL lane's refusal cells: this one holds the CLIENT half (validate before writing) while the transition cells hold the SERVER half (the guard refuses regardless). Neither substitutes for the other - a client check is a courtesy, the guard is the enforcement.

**`TB-I5-audit-outlives-the-action`** — mover:P-provider × reverser:P-client
- THE TRAIL SURVIVES: a cancelled job keeps all 4 of its state events - a job that was cancelled still happened
- ATTRIBUTED: the events carry actor_uid; the birth event is the only 'system' one (auth.uid() is null on the service-role insert), which is correct rather than anonymous
- the PROVIDER's two moves are recorded as the provider's and the CLIENT's cancel as the client's - a trail that says 'system' for everything is a log, not an audit
- THE CLAIM DOES NOT SURVIVE: 0 commission rows for a job that never completed
- VISIBILITY CONTROL: the same query CAN see a planted ledger row, so the 0 above is a real absence and not an RLS artifact

_Why:_ UFAI I5 was recorded pct:null on marketplace.html - unmeasured by the UFAI walk - and the platform has already been burned by its opposite twin, where an audit row, a ledger row and a knowledge row all OUTLIVED a reversed action ([[feedback_records_that_outlive_the_action]]).

_Note:_ The visibility control exists because of a mistake made earlier in this same arc: the S9 idempotence count ran under an identity RLS hid the rows from, returned 0, and would have read green either way. Any assertion of ABSENCE now has to prove it could have seen a presence.

**`TB-IMG-inline-image-cap`**
- an inline base64 photo over the 1.5 MB cap is refused (54000) on logbook, while an ordinary small photo is allowed - a resource bound tested from both sides so an off-by-one cannot reject real photos
- the guard has no auth branch, so the row is planted as postgres (which also bypasses logbook's quota/rate-limit triggers), isolating this cap as the sole refuser

_Why:_ check_inline_image_size (logbook + inventory_items), found unscored 2026-07-31 (ARC 13 / F, roadmap S14). A resource bound - failure is a heavy row, not a stolen one.

**`TB-J2-inquiry-roundtrip`**
- MK3 PII staging: no phone/email pattern in the DOM before contact is initiated - verified on the live listing grid
- attribution HOLDS: an inquiry inserted with the caller's own buyer_name lands (buyer_name='Pablo Aguilar', seller_name='Romeo Beltran', status='pending')
- attribution FORGE refused: the same insert with buyer_name='Ricardo Morales' is rejected 42501 by mkt_inq_insert CHECK (buyer_name IN (SELECT auth_worker_names()))
- zero pollution: probe rows swept, 0 remain

_Why:_ The J2 state note read 'End-to-end round trip NOT yet walked' while the board showed W:done - the gap that motivated the whole test-bank arc.

_Note:_ Neither marketplace_listings nor marketplace_inquiries carries auth_uid; attribution is enforced on the free-text buyer_name via auth_worker_names(). The planned assertion ('auth_uid pinned') was wrong and checking the schema first saved a false finding.

**`TB-L4-service-views-party-scope`** — owner:P-provider × stranger:P-any-member
- the OWNER sees their own GCash top-up filing - without this the stranger's 0 is meaningless, since a view nobody can read isolates perfectly and serves no one
- a signed-in STRANGER reads 0 top-ups: the gcash_ref ties a real phone number to a real payment
- a stranger reads 0 credit-ledger movements
- the public catalog IS readable - a marketplace with no rate card is broken, not secured

_Why:_ The per-page bughunt scoreboard reported 3 GAP pages: five service views appear in page footprints but carry NO hive_id column, so the cross-hive read-isolation probe could not reach them. 'No hive column' is exactly the shape that slips past a hive-shaped gate. validate_truth_view_read_isolation now carries a PARTY_SCOPED family with a stranger probe, and the scoreboard went 3 GAP -> 0 GAP.

**`TB-LESSONS-supervisor-only`**
- lessons-learned appears on the SIGNED project report, so a non-supervisor worker cannot change it (42501), while a supervisor of the hive can - the write that separates 'supervisor-gated' from 'frozen'
- keyed on meta->>'lessons_learned' so an ordinary project edit that does not touch it passes through untouched

_Why:_ guard_lessons_learned_is_supervisor, found unscored 2026-07-31 (ARC 13 / F, roadmap S14).

**`TB-MINT-topup-party-routes-and-mint-once`**
- party-ness on the money guard is a THREE-branch disjunction and each branch is its own authorisation path: the payer route (TB-I2), the provider-account route and the consumer-wallet route are asserted separately, because an admin who owns the DESTINATION mints into an account they control
- the mint fires ONCE: re-saving an already-verified top-up is accepted but adds no second ledger row, which only a ledger COUNT can show since the status column is identical either way
- the moderation half is asserted in the same run: verifying a top-up the admin is a party to in no way still works, and mints exactly one entry
- WHO verified it is recorded: the guard stamps verified_by/verified_at and nothing asserted the stamp existed, so an operator deleting it left every cell green while the money path kept minting credit and lost the accountability for it
- THE REDIRECT, a live exploit closed by mig 20260730000005: the party gate reads coalesce(old.account_id,...) while the mint inserts new.account_id, so ONE statement (change the destination to an account you own AND verify) passed the gate on the old values and credited the new ones - probed ALLOWED with 500 credits into the admin own account before the fix
- VALUE forgery as distinct from DESTINATION forgery: an admin may not inflate a pending top-up (500 -> 50000) and verify it in one statement. Found by an operator written against my OWN migration - dropping the amount column from the pinned intake facts left nothing objecting, and a new rule with no cell behind it is the same gap as any other
- the RECEIPT is pinned too: gcash_ref is the evidence a verification is justified by, and nothing asserted it until an 8th wave deleted it from the pin

_Why:_ Found 2026-07-30 by a FOURTH wave of mutation operators written to re-challenge a 100% rather than bank it - the previous 100% on this tool had been fabricated by a broken injection, so an unchallenged score was not trusted. mint_on_any_prior_status, party_provider_account_branch_removed and party_consumer_account_branch_removed all survived the guard s 4 existing cells. The shipped guard is CORRECT in all three cases: these were test gaps, so the fix is a cell, not a migration. 2026-07-30 addendum: probing what coalesce(old.X,new.X) MEANS on an UPDATE found a live money exploit - an admin who is a party to NOTHING could redirect a stranger s top-up to their own provider account and verify it in one statement. Closed by mig 20260730000005 (the intake facts are immutable for any real caller; the backend and system-write paths are untouched) and locked here plus by the intake_immutability_removed operator.

**`TB-MK1-trust-signal-integrity`**
- every rendered star rating has a canonical backing row-count: rating_avg IS NOT NULL => rating_count > 0
- MEASURED 0 violations across 13 sellers (Bryan Garcia 5.00/2, Wilfredo Malabanan 4.75/4, Pablo Aguilar 4.67/3 ...)
- the query is written to CATCH the seed defect, not to confirm health: `where rating_avg is not null and coalesce(rating_count,0)=0`

_Why:_ MK1 seed observation 2026-07-24: a listing showed '3.9 stars, 1 completed' beside 'No reviews yet' - a trust signal standing on nothing. This cell is the standing proof that it cannot return.

**`TB-MK10-ranking-transparency`**
- a ranked list discloses WHY these listings in this order: ordering language present + 1 sort control exposed to the buyer

_Why:_ MK10: EU P2B Reg. 2019/1150 Art.5 requires ranking disclosure for marketplaces; good practice everywhere.

**`TB-MK11-error-remedy-actionability`**
- an error must not propose a remedy the system knows cannot work - a catch that offers 'try again' on a failure retrying cannot fix

_Why:_ The class was born from the walk: the same shape appeared four independent times. ANTI-DUPLICATION: a registered gate already asserts this exact obligation, so the bank does NOT rebuild it - the cell records the class as covered and NAMES the owning gate, quoting that gate's own printed result.

**`TB-MK12-post-action-coherence`**
- a success message must not outrun the page: a handler that writes and claims success must also re-derive the surfaces it invalidated

_Why:_ Saving a marketplace search toasted 'Search saved.' while the counter beside it still read 0 until a full reload. ANTI-DUPLICATION: a registered gate already asserts this exact obligation, so the bank does NOT rebuild it - the cell records the class as covered and NAMES the owning gate, quoting that gate's own printed result.

**`TB-MK13-reachable-capability`**
- an empty state must not promise what nothing can produce - a table read and listed by a page but written by none is unreachable from the UI

_Why:_ Both admin consoles said 'No open disputes' for a queue no buyer can ever file into: the dispute flow left with the payment rail. ANTI-DUPLICATION: a registered gate already asserts this exact obligation, so the bank does NOT rebuild it - the cell records the class as covered and NAMES the owning gate, quoting that gate's own printed result.

**`TB-MK4-lifecycle-state-reachability`**
- every state in a status CHECK has a SURFACE - something renders it, so a row in it is not a blank
- every state has a PRODUCER - the derived guard matrix names it a legal destination, or a DB function assigns it, or a page writes it
- the producer test unions three signals because none alone is right: the matrix misses system writes (accepted via the GUC bypass, expired via the cron sweep), a payload-only page test misses a state passed as an ARGUMENT (svcAdvance(id, next)), and a text match on the literal counts READS as writes
- FORWARD-ONLY: the one known gap is named in a baseline and still prints; a NEW unreachable state FAILs

_Why:_ MK4 was unasserted. Building it found marketplace_listings.sold: fully dressed - a blue chip, a border rule, a schema.org/SoldOut mapping, and a tier calculation that counts sold listings - with nothing anywhere that writes it. The seller's sales tier therefore counts a number that is always zero.

_Note:_ The detector produced a FALSE PASS on that very finding TWICE - first by matching any function mentioning the literal (get_marketplace_price_comps READS status IN ('published','sold')), then by matching `status = 'x'` anywhere (recompute_seller_sales_and_tier COUNTS ... WHERE status='sold'). Only `SET status =` and `status :=` are unambiguous writes. A false PASS on a reachability gate is worse than a false FAIL: it retires the question.

**`TB-MK7-jsonld-truth`**
- a runtime JSON-LD builder BAILS when its subject does not exist - a Person schema for a page whose body says 'Seller not found' is a claim the page itself contradicts
- an aggregateRating is gated on a review COUNT, not on the average alone: an average can be non-null with zero reviews, which is exactly how a star rating ends up standing on nothing
- a truncated itemListElement never ships alongside a numberOfItems that claims the full total

_Why:_ MK7 was the roadmap's own open question - 'injectJsonLd on seller-profile - does it emit AggregateRating?'. It does, correctly gated, and nothing locked that. The registered seo-technical gate reads JSON-LD WRITTEN INTO the HTML and cannot see structured data BUILT AT RUNTIME from database values - which is where the claims with something at stake live.

_Note:_ No defect found: both guards were already right. The cell exists so they stay right - a fabricated AggregateRating is both a trust lie to the buyer and a structured-data violation Google penalises.

**`TB-MK8-payment-disclosure`**
- the money step states that WorkHive NEVER HOLDS the payment
- it states the consequence - the platform therefore CANNOT REVERSE it
- it gives a concrete precaution (inspect first, meet in a public or business place)
- no buyer-facing copy promises buyer/purchase protection, a money-back guarantee, held funds, or escrow - promises PAYMENTS_ENABLED=false makes false
- a warning living only in an HTML COMMENT does not count: a reassurance nobody reads reassures nobody
- the whole gate SKIPS if PAYMENTS_ENABLED flips to true - these invariants describe a different product then, and must be re-derived rather than silently kept

_Why:_ MK8 was unasserted. This marketplace is contact-only with no escrow, so 100% of the money risk sits at one step: the buyer taking a stranger off-platform. RA 11967 (the PH Internet Transactions Act) requires consumer education on the red flags in internet transactions, and silence lets a buyer assume protection by default - the default assumption everywhere else online is that the platform holds the money.

_Note:_ No defect found: the warning was added during the 2026-07-24 deepwalk and reads well. Nothing locked it, so a future copy edit could have dropped it silently. marketplace_orders DOES carry an escrow-shaped vocabulary (pending_payment, escrow_hold, released, refunded) for a flow that is switched OFF - fine as schema, and the gate deliberately searches PROSE, not status strings, so the two never get confused.

**`TB-MK9-response-sla-honesty`**
- the rendered 'Responds in ~Nh' equals a recomputation from the inquiry history
- the rendered 'N% reply rate' equals replied/total from the same history
- both match the independent arithmetic (3.0h, 0.75), not merely themselves
- a seller with inquiries but NO replies keeps BOTH columns NULL - never an invented 0%

_Why:_ MK9 was recorded owed as VACUOUS: 0 sellers carried a response_rate and marketplace_inquiries was empty, so there was no rendered claim to diff against source truth. That was the right call for a green over an empty denominator - and the wrong place to stop. Checked before assuming: a living producer DOES exist (trg_update_seller_response_stats recomputes both columns from the inquiry history), and the NULLs are the page's honest empty state, not a dead counter. So the probe MANUFACTURES the denominator instead of waiting for one.

_Note:_ The recomputation is done independently inside the probe, then pinned to fixed arithmetic (1h+2h+6h)/3 = 3.0 and 3/4 = 0.75 - so a producer that agrees with itself while being wrong still fails. TEETH VERIFIED against the plausible bug this exists to catch: replacing the producer with one that COALESCEs the empty state to 0 made both empty-state assertions report 'NO 0.0' / 'NO 0.00'. A trust signal that lies when a seller is brand new is worse than no signal, and it is the direction a naive COALESCE breaks first.

**`TB-MR-metamorphic-relations`**
- MR1 filter subset: a filtered listing set is a SUBSET of the unfiltered one, for category and for price independently - and the filter demonstrably kept some rows and removed others
- MR2 permission monotonicity: a member sees a STRICT subset of what a supervisor sees on the same hive, and the supervisor's set is non-empty
- MR3 order-independent credit: verifying two top-ups in either order reaches the same balance, and both orderings actually minted

_Why:_ The oracle mix was 194 `refusal` of 247 cells and exactly ONE `eval`. That was not an accident of effort: wherever no exact expected value could be written down - ranking, filtering, permission-visibility - the surface was skipped or graded by rubric. A metamorphic relation is a necessary property that MUST involve MULTIPLE EXECUTIONS, so it needs no right answer, only a relation the right answers obey. The published canonical example is our exact shape: a filtered search 'should return a subset of the previous results'.

_Note:_ EVERY MR CARRIES ITS OWN NON-VACUITY CHECK, because these relations are the easiest in the bank to satisfy trivially: an empty filter result is a subset of anything, a system that shows nobody anything is monotonic, and 0 = 0 is order-independent. So MR1 asserts the filter kept AND removed rows, MR2 asserts the supervisor sees something AND that the subset is STRICT, MR3 asserts both orderings minted. My first MR2 fixture gave both requests a hive_id, which made the two visible sets IDENTICAL - subset held by equality while proving no permission boundary existed. Rebuilt so one request is visible to the supervisor only.

**`TB-PLOG-progress-log-record`**
- bind_progress_log_submitter binds identity server-side: a worker who CLAIMS reported_by = the supervisor's name has it overwritten to his own (bind_reported_by=self), auth_uid is pinned to the caller (bind_auth=self), and a non-member cannot file at all - the name is the caller's, not the caller's choice
- guard_progress_log_is_a_record makes a filed report immutable: pct_complete, hours_worked, blockers and notes are all refused on UPDATE - a report you can quietly rewrite is not a record
- acknowledging is a SUPERVISOR act (a worker acking a report he did not write is refused), and NOT of your OWN report (the supervisor acking his own report is refused) - segregation of duties, reviewing your own work is a signature not a review
- the legitimate act survives: a supervisor acknowledging SOMEONE ELSE's report is allowed (sup_acks_other), separating 'a record with a review gate' from 'nothing can be acknowledged'

_Why:_ Two guards on project_progress_logs, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Six of seven project tables lack an auth_uid, so the not-own-report check is a name comparison (PJK1's open half); this cell locks the guards as they stand.

**`TB-QUOTA-ai-reports-both-modes`**
- BOTH modes of a two-mode guard: enforce_blocking=true refuses at cap with 54000 (the code that means QUOTA, asserted rather than merely "something refused"), and enforce_blocking=false ALLOWS the write and LOGS the overrun to automation_log
- the WARN-ONLY log is read BACK, because that mode fails SILENTLY - if the log INSERT ever stops, quota overruns simply stop being recorded and nothing goes red
- non-vacuous in both directions: under the cap the write works AND nothing is logged, so a guard that cried wolf would fail even though the warn-only assertion alone would pass

_Why:_ One of FOUR guards no registered gate names, found 2026-07-30 by ranking all 27 un-scored guards. It is the only one of the four that WRITES, and its failure mode is a COST leak - an invoice, not a corrupted row. It is live and attached but had NEVER fired: 3 hive_quotas rows all enforcing at cap 5000 against 16 ai_reports rows, so the cap sat three orders of magnitude from the data and neither branch had executed. The probe plants a cap of 2 instead of approaching 5000.

**`TB-QUOTA-community-cumulative`**
- the cumulative per-hive community-posts quota refuses at/over cap in enforce mode (54000) and in warn-only mode allows the write while recording the overrun to automation_log (read back)
- THREE-WAY isolated: yesterday-rows separate cumulative from any daily/recent window; a different author per test insert keeps the per-author 30s rate limit at zero; the 200/100 daily cap is far above the test rows
- non-vacuity: below the cap the write works and nothing is logged

_Why:_ check_hive_quota_community, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Same two-branch shape as the other hive quotas; scored by the four §12 quota operators, no new ones.

**`TB-QUOTA-invtx-cumulative`**
- the cumulative per-hive inventory-transactions quota refuses at/over cap in enforce mode (54000) and in warn-only mode allows the write while recording the overrun to automation_log (read back)
- isolated from check_daily_row_cap by yesterday-rows (cumulative>today); inventory_transactions has no per-author recent-window rate limit, so no author trick is needed
- non-vacuity: below the cap the write works and nothing is logged

_Why:_ check_hive_quota_inv_tx, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Direct mirror of TB-QUOTA-logbook; scored by the four §12 quota operators, no new ones.

**`TB-QUOTA-logbook-cumulative`**
- the cumulative per-hive logbook quota refuses at/over cap in enforce mode (54000), and in warn-only mode ALLOWS the write while recording the overrun to automation_log (read BACK, because a silent warn is no warn)
- isolated from the sibling check_logbook_rate_limit by dating the fixture rows YESTERDAY (cumulative>today) and pinning the per-user cap high, so only the cumulative quota can trip
- non-vacuity: below the cap the write works and nothing is logged (no false alarm)

_Why:_ check_hive_quota_logbook, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Same two-branch shape as check_hive_quota_ai_reports (§12); scored by the four existing §12 quota operators, no new ones.

**`TB-QUOTA-pm-cumulative`**
- the cumulative per-hive pm-completions quota refuses at/over cap in enforce mode (54000) and in warn-only mode allows the write while recording the overrun (read back)
- isolated from check_daily_row_cap by dating the seed rows' completed_at yesterday (the daily cap counts by completed_at; the quota is cumulative and counts all rows); no per-author rate limit on this table
- non-vacuity: below the cap the write works and nothing is logged

_Why:_ check_hive_quota_pm_completions, the FIFTH and last hive quota, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Scored by the four §12 quota operators, no new ones.

**`TB-RATE-ai-feedback-daily`**
- the per-user 200/day AI-reply-feedback cap refuses the 201st (200 planted at the cap) - one account cannot flood feedback
- the vetted backend path (auth_uid NULL) bypasses the cap; the guard is the sole trigger on the table so it is the sole refuser

_Why:_ enforce_ai_reply_feedback_daily_limit, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Per-user daily cap, threshold 200.

**`TB-RATE-community-post`**
- the short-window anti-flood refuses a 4th post by the same author in the same hive within 30s while the first three succeed - tested from both sides
- it is PER AUTHOR, not a freeze: a different author is unaffected
- isolated from the cumulative community quota (cap pinned to 1000) and check_daily_row_cap (200/hive, well under), so the rate limit is the sole refuser at the 4th

_Why:_ community_post_rate_limit, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Recent-window rate-limit exemplar; same shape as community_reply_rate_limit.

**`TB-RATE-community-reply`**
- the reply anti-flood refuses a 6th reply by the same author in the same hive within 15s while the first five succeed
- per author, not a freeze: a different author is unaffected
- isolated from check_daily_row_cap (well under its cap); no cumulative quota on replies

_Why:_ community_reply_rate_limit, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Reply-side mirror of community_post_rate_limit (15s window, threshold 5).

**`TB-RATE-listing-daily`**
- the per-hive 24h listing anti-spam refuses the 21st listing (20 planted at the cap) while a listing in a different hive is unaffected - per hive, not a freeze
- a fresh hive isolates it from prior listings; born-'draft' satisfies the birth-status guard so this rate limit is the sole refuser at the 21st

_Why:_ check_listing_rate, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Recent-window rate limit, threshold 20.

**`TB-RATE-logbook-per-user`**
- the per-user/day logbook rate limit refuses the write that exceeds the cap (54000), while the write AT the cap is allowed - the abuse stop tested from both sides
- ISOLATED from the sibling check_hive_quota_logbook (which reads the same max_rows_logbook) by pinning the hive cap to 1,000,000 so only the per-user rule can trip, and the cap is computed from the caller's LIVE same-day count so the cell controls its own state rather than assuming an empty day

_Why:_ check_logbook_rate_limit (per-user rule), found unscored 2026-07-31 (ARC 13 / F, roadmap S14). First of the count-based ceilings; establishes the cap-isolation + determinism method for the rest.

**`TB-RATE-platform-feedback-identity-bucket`**
- BOTH bounds: the per-identity 5/hour holds under a stable name (6th refused 23P01), AND the global anonymous ceiling of 20/hour holds under 25 DIFFERENT names - so renaming no longer evades it
- the ceiling is keyed on auth_uid IS NULL, not on the coalesced identity, because a bound the client can move by changing a string is not a bound
- bound 2 is asserted BEHAVIOURALLY, without reading the table: the first version measured its own headroom with a COUNT and got 20 when the truth was 15, because the anon role cannot SELECT unpublished feedback while the guard (SECURITY DEFINER) counts every row. A fixture blind to the state it reasons about computes a confident wrong number
- both bounds share ERRCODE 23P01 deliberately, so the client toast needed no change - and the code is asserted rather than merely "something refused"

_Why:_ Second of the four guards no registered gate names (2026-07-30 ranking of all 27 un-scored guards). The evasion was PROBED first: six submissions under six worker_names were all accepted. Ian chose the global-ceiling fix over bucketing all anonymous users together, because the latter would throttle legitimate anonymous feedback collectively at 5/hour. mig 20260730000008 adds 20/hour on auth_uid IS NULL; signed-in users are untouched, their bucket already being an id they cannot forge.

**`TB-RATE-review-daily`**
- the per-reviewer 10/day review cap refuses the 11th (10 planted today) - one reviewer cannot flood reviews
- a different reviewer_name is unaffected (per reviewer, not a freeze); LISTING reviews (request_id null) so guard_service_review returns immediately and the daily cap is the sole refuser

_Why:_ enforce_marketplace_review_daily_cap, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Per-reviewer daily cap, threshold 10.

**`TB-S2-pwa-offline-hail-degraded`** — actor:P-client
- offline, the hail produces a VISIBLE message naming the offline state
- the message says NOTHING WAS SENT - 'try again later' would leave a half-request in doubt
- the guard runs BEFORE field validation, so an offline person is told about the network, not about an address they cannot submit
- ZERO service_requests rows were created, measured against a baseline taken at the same moment
- STATIC COMPANION (gate offline-write-guard): all 17 user-triggered writes across both service pages check connectivity before writing

_Why:_ Building this cell found the real defect: svcRequireOnline existed and was wired into 2 of 7 client writes, and the seller page guarded 1 of 10. degraded-state-central passed the whole time because both pages DID adopt the offline banner - a passive warning that does not stop a button. 14 write paths fixed.

_Note:_ This surface deliberately does NOT queue: a hail and an accept are races, and a queued accept would claim a job someone else already took. Refusing honestly is the correct degraded posture here, so the cell asserts refusal rather than a queue round-trip - the original cell text assumed the Y1b queue and was wrong about the product.

**`TB-S5-edge-push-audience-selection`** — subscriber:P-provider × trigger:system
- the ELIGIBLE provider (online, category match, in radius, not the client) IS enqueued
- an OFFLINE provider is not paged
- a provider of the WRONG TRADE is not paged
- a provider ~96km out is not paged (radius 5000m honoured)
- the CLIENT'S OWN provider profile is never paged with their own hail
- the audience is EXACTLY 1 - not 'at least the right one'
- re-writing 'broadcasting' onto an already-broadcasting row does NOT enqueue a second push
- the URGENT branch is REACHABLE: a 'critical' hail titles the push differently from a normal one - it used to test urgency='emergency', a value the CHECK forbids, so a production-down job pushed with the same words as a whenever-convenient one and nothing ever failed (mig 20260729000017)

_Why:_ Anti-duplication check against outbox-delivery showed its L7 probe hand-builds the payload (`where availability='online' limit 1`), so fanout_broadcast_push's own audience query had never been executed by any gate. A push interrupts a person: who receives it is as much an invariant as whether it arrives.

**`TB-S5-edge-push-delivery-roundtrip`** — subscriber:P-provider × trigger:system
- the enable-alerts path reaches an ACTIVE worker instead of awaiting a promise that never settles
- a push delivered the way FCM delivers one renders a notification carrying its payload
- the notification's title and body match what was sent

_Why:_ Building the runtime tier found a live defect: svcEnablePush() awaited navigator.serviceWorker.ready while NOTHING on marketplace-seller.html called register(). Measured getRegistrations()=0, controller=false, ready never settled - so 'Enable job alerts' hung forever after the user granted permission, with no toast, error or log. The catch block covered every failure that THROWS; ready does not throw.

_Note:_ The cell was previously recorded owed with 'nothing proves a real subscription RENDERS the notification'. It was buildable the whole time - it needed three context settings (serviceWorkers:'allow', permissions, channel:'chromium') and the last one was the difference between 'un-probeable ceiling' and a passing test.

**`TB-S6-realtime-map-datapath-publisher-x-watcher`** — publisher:P-provider × watcher:P-client
- PUBLISHER: the matched provider writes live_location under their own identity (as watchPosition does)
- WATCHER: the CLIENT reads it through v_service_job_tracking -> live_lat 16.4100
- MOVEMENT: the provider publishes again and the client sees the NEW position -> 16.4200 (the path carries movement, not a static render)
- PRIVACY: a third identity with no active job reads 0 rows - live_location is never ambient

_Why:_ Ian 2026-07-29: 'why is it we have no journeys e2e for using the realtime map x personas'. J29 had been walked with P-client-supervisor + P-client-worker - two WATCHERS, zero PUBLISHERS - satisfying the '>=2 personas' count while never exercising the stream. The rule needed a ROLE-PAIR, not a count.

_Note:_ This proves the DATA path at SQL altitude (cheapest honest). The UI half - the marker visibly moving on the MapLibre canvas via the realtime subscription - remains owed and needs the two-browser-context harness that tools/ does not yet have.

**`TB-S6-realtime-map-ui-publisher-x-watcher`** — publisher:P-provider × watcher:P-client
- the client's active job renders a real 'Track provider' control (driven by role+name, not a selector)
- the tracker hands the map a provider position at all - so a later 'no movement' result cannot be vacuous
- the PROVIDER's own page publishes via watchPosition from its own session (the run logs which path fired)
- the WATCHER's map repaints at the NEW coordinate within two 10s poll cycles, with no reload
- a provider marker was actually placed on the watcher's map
- TEETH: WH_TB_FREEZE=1 parks the publisher and the watcher assertion goes RED - proven, not assumed

_Note:_ The tracker POLLS v_service_job_tracking every 10s (marketplace.html ~3584); it is not a Realtime channel. The cell asserts the repaint, which is the user-visible claim, whichever mechanism carries it.

**`TB-S7-ai-triage-eval-oracle`** — actor:P-client
- RUBRIC, never exact-match: the grade is 'can the product apply this', never 'did it say Plumbing'
- category, when offered, exists in the LIVE service_catalog - an out-of-vocabulary answer finds no <option>, sets nothing, and reports nothing
- urgency and mode, when offered, are inside the service_requests CHECK vocabularies
- an ABSENT field is legitimate: 'no clear match' is an honest answer and must not be graded as failure
- a rate-limited or unreachable chain is a SKIP, never a PASS - a green there would mean the feature is down
- the vocabularies are read from the DATABASE each run, so a new catalog category is graded against without editing the gate

_Why:_ S7 was the last untested layer and the only one that cannot use an exact-match oracle. The invariant is the one that actually bites: marketplace.html applies a suggestion by finding the matching <option>, so 'Plumbing Services' or 'urgent' silently sets nothing and the person watches a form that does not move.

_Note:_ The same prompt returned Mechanical/critical/instant on one call and 'no opinion' on the next - which is precisely why the oracle is a rubric. Also: the platform's 45s supabase fetch timeout (utils.js) already bounds this call, so the 'Thinking...' state cannot hang; no per-page timeout was needed.

**`TB-S8-gates-surface-honesty`** — actor:P-founder
- a gate row's visible text is the SHORT id-derived name, never the engineering label
- the full label survives as the row's tooltip - shortening is not hiding
- no gate row prints v.label as visible chrome (the G2 regression, locked)
- the failing-gate list truncates at 12 and DISCLOSES '+N more'
- the regressions list truncates at 6 and DISCLOSES the remainder - FOUND BROKEN and fixed by this cell

_Why:_ Everything on this platform is watched by a gate; the surface that DISPLAYS the gates was watched by nothing. Building the cell found that the regressions block - the highest-severity signal on the page, a gate that was passing and now is not - rendered regs.slice(0, 6) with no overflow line, so everything past the sixth vanished and the short list read as the whole list.

_Note:_ The gate reddened twice on CORRECT page code while its own detector was wrong: first a regex that spelled out the title-attribute quoting, then a .find() that anchored on the CSS rule instead of the render site. Both were fixed in the detector, never by bending the page ([[feedback_teach_the_gate_not_bend_the_code]]).

**`TB-S9-knowledge-writeback-roundtrip`** — actor:P-provider × reader:P-client
- WRITER: the matched PROVIDER completes the job under their own identity
- READER: the CLIENT reads the resulting logbook entry through RLS - a row the client cannot see is not knowledge
- ASSET LINKAGE: the [PUMP-207] scope prefix lands in logbook.machine, tying the entry to the physical asset
- ATTRIBUTION: the outside provider is NAMED in the entry, readable months later without re-opening the marketplace
- the entry is filed Closed, not left open on the client's board
- TENANT: a supervisor of a different hive reads 0 rows - the writeback crosses an org boundary and must not leak
- the re-fire is ACCEPTED (so the trigger really ran) and still yields exactly 1 entry - a duplicated maintenance record would corrupt every MTBF/MTTR computed from it

_Why:_ S9 was the only layer with zero cells, and it carries the arc's differentiator: an outside provider's work becoming the hive's own maintenance history. The writeback had one proof from the build and none standing.

_Note:_ Counting the idempotence under the PROVIDER's JWT first returned 0 - the provider is not a hive member, so RLS hid every logbook row. The assertion was measuring RLS, not duplication, and would have read green either way.

**`TB-SELLER-trust-columns`**
- a non-admin seller cannot self-forge any trust column - kyb_verified, cert_verified, tier, total_sales - by UPDATE, and cannot be BORN verified at INSERT; every refusal is 42501 from the guard, isolated from RLS so the trigger is what refuses
- the legitimate write survives (messenger_username edit), separating 'protects the trust columns' from 'rejects sellers' - a guard that pinned the whole row would pass every negative while breaking the product
- the admin self-manage path is asserted ON PURPOSE (admin_self_kyb=ALLOWED): a marketplace admin who is also a seller MAY set their own trust columns. Ian decided this 2026-07-31 after it was raised as the mig-003 self-deal class; banking it LOCKS the decision, so a later party-guard on the admin bypass turns this cell red and forces a re-decision instead of silent drift
- the admin moderator job survives too (admin_verify_stranger=works): the bypass that permits self-management also permits verifying others, which is the point of an admin

_Why:_ Seller-side sibling of guard_service_provider_writes (TB-TRUST). Found unscored 2026-07-31 while extending the mutation harness across the 23 guards no gate names (ARC 13 / F). The non-admin refusals are the security guarantee; the admin_self_kyb=ALLOWED assertion encodes Ian's 2026-07-31 decision to leave admin self-management intentional. See [[feedback_admin_bypass_before_party_check_is_selfdeal]] and roadmap S14.

**`TB-SJ01-responder-answers-the-hail`** — hailer:P-client-supervisor × responder:P-freelancer-provider
- the client sees their own hail on their page
- the RESPONDER is offered an Accept control for THAT request and presses it - the act none of the five hail journeys had ever walked
- the DB is the first witness: status accepted AND matched to this provider
- the CLIENT's chip then reads 'Provider accepted' with no reload - the handoff completes at the SCREEN, not just in the database
- the provider starts ONLINE and free, because accept_service_request flips them to on_job as part of accepting - starting on_job would test an unreachable world
- SJ-J27: accepting also LANDS the job on the provider's dayplan (land_accepted_job_on_dayplan) - asserted here because it is a side-effect of this exact act, and because J27's CLIENT half has nothing further to walk: schedule_items is owner-only RLS (auth_uid = auth.uid()) and marketplace.html reads it ZERO times, so a provider's calendar is deliberately invisible to the client

_Why:_ Five journeys - SJ-J01/J02/J22/J24/J30 - were each walked from the HAILER's side only. They differ in how the hail originates (instant, quote, from an alert, recurring, consumer) and share ONE unwalked act: a provider ANSWERING it. So 'a provider accepts' was assumed by all five and proven by none.

_Note:_ One cell, five journeys credited, and the shared shape stated in covers_journeys so the breadth is auditable rather than implied. What each journey still owes separately is its own ORIGINATION - the alert-to-hail jump, the recurring schedule, the consumer entry - and those are walked on the client side already. ROOT CAUSE FOUND, not widened away: an INSTANT insert is normalised to `requested` regardless of the supplied status, so the hail was silently absent from every provider feed and the failure read as a missing Accept control. Every hail cell now patches the status and ASSERTS it stuck rather than trusting the write.

**`TB-SJ06-the-quoter-side`** — quoter:P-freelancer-provider × selector:P-client-supervisor
- THE QUOTER: a provider can send their own price - the side nobody had ever walked
- a RIVAL cannot post a quote under a competitor's identity (the insert names provider_id explicitly, so only the policy stands between them)
- the CLIENT cannot mint a quote and hold a provider to it
- the client SEES the quote through the exact shape their page reads (request_id + kind=quote + status=pending)
- the client sees the PRICE, not a masked row - a comparison surface that cannot show the number is worse than none
- a RIVAL reads 0: a provider who can see a competitor's price undercuts it by a peso

_Why:_ The ONLY one of the 14 role-pair gaps with no evidence at any altitude. SJ-J06-quote-select was walked as two CLIENTS - a quote-selection journey where nobody sends a quote has no content. Every other flagged journey had its missing half's data path banked by some cell built this session; this one had nothing.

_Note:_ No defect found - the RLS posture was already right in all four directions. The cell exists because a quote is a provider's price attached to their name, and 'nobody has checked' is not the same as 'nobody can forge one'.

**`TB-SJ06-ui-the-quoter-composes-and-the-client-compares`** — quoter:P-freelancer-provider × selector:P-client-supervisor
- THE QUOTER: a provider TYPES a price on their own page and sends it - the act the journey is named for and the one nobody had walked
- an offer exists at that exact price, pending, owned by one of that login's OWN provider profiles
- THE CLIENT opens their quotes and SEES THE PRICE - the number is the whole point, so 'a quote arrived' is not enough

_Why:_ SJ-J06 was walked as two CLIENTS - a quote-SELECTION journey with no quote in it. The SQL cell proved a provider CAN quote and that rivals and clients cannot forge one; this proves a person can actually compose it and another person can compare it.

_Note:_ The first run failed on MY assertion, not the product: submit_service_quote picks which provider profile files the quote via my_service_provider_ids() ordered by `verified desc, created_at`, and one seeded login owns TWO profiles - so naming a single id fails on correct behaviour. The cell now picks a login owning exactly ONE profile and asserts against that login's own ids. Removing the ambiguity beat guessing which the RPC would choose.

**`TB-SJ07-ui-the-watcher-sees-the-job-advance`** — mover:P-freelancer-provider × watcher:P-client-supervisor
- the client's status chip advances from 'Provider accepted' to 'Provider on the way' with no reload, after the provider advances the job from their OWN session
- FOUND IT FAILING FIRST: marketplace.html had exactly one interval (the tracker map) and loadClientServices() ran only after the client's own writes

_Why:_ The mirror of the seller-page defect, on the client side and worse: the tracker map DOES poll, so on a job being watched the MAP MOVED while the label beside it still read 'Provider accepted'. A screen that disagrees with ITSELF is worse than one merely stale - the person cannot tell which half to believe.

_Note:_ Fixing this cost three further defects, each caught by a cell in this same file and none of them visible from reading the code: (1) the poll hammered a dead network while offline and repainted the pane as an error every 15s; (2) it repainted the pane out from under an OPEN tracker map, destroying it; (3) whLivePoll evaluated its condition only at arm time, so a condition that became false never stopped the timer. The helper now re-checks every tick and yields to an open tracker, which owns the refresh while it is up.

**`TB-SJ09-10-cancellation-tells-the-other-party`** — canceller:P-client-supervisor × notified:P-freelancer-provider
- a client cancelling an EN ROUTE job notifies the matched provider - the case where the harm is a real trip already underway
- the message is ACTIONABLE: it says 'stand down - do not travel', not just 'Job cancelled'
- the canceller is not paged about their own action
- a provider cancelling notifies the CLIENT, and NAMES which provider - 'someone cancelled' is not something a person can act on
- re-writing the same cancelled status enqueues NO second notice: a duplicate cancellation push implies a second job
- a hail cancelled while still BROADCASTING pages nobody - it was never anyone's job
- VISIBILITY CONTROL: the broadcast push for that same row IS still present, so the 0 above is a real absence

_Why:_ Found by the role-pair rule, and the missing half was missing from the PRODUCT. SJ-J09 and SJ-J10 were both walk-complete having been walked only from the canceller's side; a search of every function touching the cancelled states returned exactly two - the availability sync and the status guard. NOTHING TOLD THE OTHER PARTY. A client could cancel while the provider was en route: their availability flipped back to online, the job vanished from their list, and they kept driving to a site for work that no longer existed. Wired in mig 20260729000018 onto the push rail that already carries 'New job nearby'.

_Note:_ enqueue_service_push_uids is a NEW NAME, not an overload of enqueue_service_push - a second PostgREST signature would 203 the endpoint for every caller ([[feedback_rpc_overload_pgrst203_kills_endpoint]]). Two probe bugs were caught while writing it: `reset role` does NOT clear request.jwt.claims, so the service-role inserts still arrived with a live auth.uid() and the guard refused a system-set birth state; and the last assertion matched ANY push carrying the scope text, catching the broadcast fan-out's own correct 'New job nearby' and calling it a failure.

**`TB-SJ09-ui-the-open-page-learns-of-the-cancellation`** — canceller:P-client-supervisor × notified:P-freelancer-provider
- the provider's OPEN seller page stops showing a job the client cancelled, with no reload
- walked in two contexts and two real sessions: the provider watching, the client cancelling from their own
- FOUND IT FAILING FIRST: the page held the dead job for the full 40s window before the fix

_Why:_ The UI half of the cancellation gap. The SQL cell proved the provider is TOLD; this one asked whether the screen they are actually looking at agrees. It did not: marketplace-seller.html had NO interval and NO realtime subscription, so once loaded the job list was frozen. The push said 'stand down - do not travel' while the page still listed the job as live.

_Note:_ Fixed with a POLL, not a subscription, and deliberately: the client's tracker already polls every 10s, so this matches a cadence the platform already pays for instead of adding a realtime channel with its own cleanup contract. It runs only while a job is live, pauses on a hidden tab, and clears on beforeunload - the same discipline as svcGeoSync, because a timer that outlives its reason is the leak that discipline exists to prevent.

**`TB-SJ10-ui-the-client-learns-the-provider-cancelled`** — canceller:P-freelancer-provider × notified:P-client-supervisor
- the client is watching an ACCEPTED job, expecting someone to arrive
- the provider cancels from their OWN session
- the client's page stops saying 'Provider accepted' with no reload - they are the stranded party here, waiting for a person who is no longer on the way, and the screen is what they have

_Why:_ The mirror of SJ-J09, and the direction that had never been walked: J10 was complete from the CANCELLER's side, so nobody had checked that the person left waiting finds out. The SQL cell proves they are notified; this proves the page they are actually looking at agrees.

**`TB-SJ28-ui-a-waiting-provider-sees-the-hail-arrive`** — trigger:P-client-supervisor × subscriber:P-freelancer-provider
- a provider who is ONLINE and holding NO job sees a new hail appear on the page they already have open
- the state is set up deliberately: availability online, every active job cleared - the exact state the platform's promise to a provider is ABOUT
- FOUND IT FAILING FIRST, and the cause was MY OWN EARLIER FIX: the refresh armed only while jobs.some(SVC_ACTIVE)

_Why:_ The worst of the three frozen-page findings. My first poll fix armed the refresh only when the provider ALREADY had an active job, which froze the page in exactly the state the product exists to serve: online, waiting, and told nothing when work arrived. Refreshing only once you already have work is the opposite of a job feed.

_Note:_ The poll now arms while the provider is AVAILABLE (a hail can land) OR has a live job (its state can change under them). Worth noting how it was found: not by reading the code I had just written - I read it and thought it was right - but by walking the state I had not considered. A fix verified only in the case that motivated it is a fix with an untested half.

**`TB-SJ33-ui-presence-counts-a-provider-who-just-came-online`** — publisher:P-freelancer-provider × viewer:P-consumer
- PUBLISHER: the provider goes online through their OWN session, not an admin write - the publisher side of this pair is a person flipping their availability
- they are taken OFFLINE first, so 'came online' is a real transition and not a coincidence of whatever the seed left
- v_service_area_presence counts them: total rises by exactly 1
- VIEWER: the rendered count EQUALS the view's total - a liquidity hint that overstates is worse than no hint, because it tells someone to wait for help that is not there
- the provider's original availability is restored afterwards - this walk flips a real seeded row

_Why:_ SJ-J33 was walked as P-consumer + P-client-worker - two VIEWERS. The publisher, whose availability flip is the only thing that changes the number, was never in it.

_Note:_ No poll was added here, deliberately. svcLoadPresence runs once and the design says why: the hint is ADDITIVE and 'silence is golden' - it renders nothing rather than a discouraging '0 providers' the client can do nothing about. Adding a third timer would have been inventing a requirement the design explicitly rejected; the assertable claim is that the number is TRUE when shown, not that it live-updates.

**`TB-SNEAK-replay-and-session-switch`**
- replaying an authorised transition leaves the status unchanged AND journals exactly once
- switching the JWT mid-transaction refuses the next statement - identity is read per statement
- the real provider still works afterwards, so the refusal is about identity, not a poisoned row

_Why:_ The two sneak paths that are neither a one-UPDATE cell nor already locked by a gate. Both were about to be written off as covered-by-nature ('a same-status update changes nothing'; 'the guards call auth.uid() inline'). Both statements are true and neither is evidence - they describe code I read, not behaviour I observed.

_Note:_ The replay assertion is the SIDE EFFECT, not the status: trg_journal_service_request appends to service_job_events, and a replay that journals twice writes a history saying the provider set off for the job twice. Nothing errors; the timeline just becomes fiction.

**`TB-SREVIEW-service-review-integrity`**
- a SERVICE review is refused until the job is completed/settled, and refused if it points at a request that does not exist - a review of a job that never happened is the rot the verified_purchase badge exists to prevent
- the DIRECTION must match the reviewer's side: only the client posts client_to_provider, only the matched provider posts provider_to_client, and a stranger (no party) is refused outright
- verified_purchase and reviewer_auth_uid are pinned server-side (verified_pinned=true, reviewer_pinned=self) no matter what the client sends - forged provenance is impossible
- the legitimate reviews on BOTH sides survive (client->provider and provider->client), separating 'protects review integrity' from 'rejects reviews'
- this is the ONE trust guard that already party-guards its admin bypass (is_marketplace_admin AND NOT v_is_party), so an admin reviewing their OWN job does not get the moderator pass - the mig-003 rule, already applied here

_Why:_ guard_service_review, found unscored 2026-07-31 (ARC 13 / F, roadmap S14). Correct but unwatched. Unlike the seller/provider trust guards it already applies the admin-not-a-party self-deal rule.

**`TB-STATE-inducers-empty-filtered0-edge`**
- empty (a real tenant owning nothing), filtered0 (real rows, no match) and a GHOST tenant all return the same 0 rows - so a count is never a diagnosis
- the tenant-existence check DOES separate empty from ghost, which is the check the read-battery lacked when it invented six page defects
- filtered0 is non-vacuous: the unfiltered set had rows to filter
- edge boundaries hold: price=0 (the CHECK's own minimum) persists, and title is capped at 120 - exactly the number #post-title's maxlength uses

_Why:_ `state` was None on 212 of 247 cells because the inducers the original plan required (§7b) were never built - and that plan named the cost itself: 'a state axis with no induction mechanism is decoration'. Two axes (viewport, lang) were retired today for exactly that reason; this is the other half of the answer, inducing the states that CAN be induced at SQL altitude instead of removing them.

_Note:_ `error` and `degraded` are deliberately NOT here: a route abort / 401 / 5xx and an offline write queue are browser facts belonging to the journey lane, and claiming them at SQL altitude would break the cheapest-honest-altitude rule in the expensive direction. THE EDGE ASSERTION WAS CORRECTED MID-BUILD: it first demanded a 200-char title survive, which returned 120 and looked like silent truncation of user content (the edge fn slices at 200, the DB trigger caps at 120). Unreachable - #post-title carries maxlength=120 matching the DB, and the AI writes only category and description, never the title. So both ENDS agree and only the middle layer is slack. The assertion now locks the AGREEMENT at 120, which reddens if either end drifts; demanding 200 would have asked the product to abandon a deliberate cap.

**`TB-STATE-journey-error-and-degraded`**
- error: a failed read shows an error affordance, not a fake empty state or a stuck skeleton
- error: the first-run CTA is ABSENT, because 'be the first to sell' on a failed query reads as 'your listings are gone'
- degraded: offline refuses the write AND says so, including that nothing was sent

_Why:_ The state axis was induced at SQL altitude for empty/filtered0/edge; `error` and `degraded` are browser facts and were deliberately excluded there rather than claimed. This closes the axis across both altitudes - the cheapest honest altitude for each state, not one altitude for all.

_Note:_ The product was RIGHT and my test was wrong twice before it passed: a 2.5s sleep measured the page's RETRY budget (aborts climb 12->20->32, the error lands at ~8s) so it must POLL; and body.innerText matched the error copy and the CTA from other sections, so it must assert inside #listing-grid. Fifth suspected finding this session that dissolved on inspection.

**`TB-TRUST-provider-cannot-self-verify`**
- a provider cannot award themselves the trust badge, on either branch: not by UPDATE, not at INSERT, and not by touching verified_at alone - a badge with a forged date has forged provenance even if the boolean is untouched
- a provider cannot set on_job, which is DISPATCH state - appearing busy dodges the matcher and appearing free while working oversubscribes them
- both legitimate writes survive: an ordinary profile edit AND the offline/online toggle, which is the one availability control a provider is supposed to have. A guard that pinned the whole column would pass the four negatives while breaking the product
- the trust column is read BACK with the identity dropped: a refused self-verify that nevertheless left verified=true would be the worst outcome and every status assertion would still read blocked
- the born-ON_JOB case sets verified=false so the BADGE rule cannot answer first: the guard tests new.verified before new.availability, so a born-verified-AND-on_job row never reaches the availability clause and that mutation SURVIVED until the case was aimed properly
- the badge-GRANTING path is asserted as a permission, not leaned on as a fixture: the first version planted a verified provider in setup, which killed the backend-branch mutation by BREAKING SETUP - a weaker kill the evidence-quality ratchet went red for, correctly

_Why:_ Third of the four guards no registered gate names (2026-07-30 ranking of all 27 un-scored guards), and the one that protects TRUST. The guard is CORRECT - it was unwatched, so deleting any of its four rules would have gone unnoticed. Same family the platform was already burned by: a forgeable trust counter and trust signals standing on nothing.

## Still owed — stated, not hidden

- **`TB-A345-architecture-quality`** — NOT covered-by-nature by default - a real attempt was made, and re-checking it CORRECTED IT TWICE. A4 was hunted as a STALE-HIVE defect: marketplace.html captures const HIVE_ID = whHiveId() at load and stamps it on every hail, which would file work into the wrong hive if a switch could happen while the page is open. (1) The earlier note here claimed every wh_active_hive_id write lives on hive.html. FALSE - index.html:2953 writes it too, during the sign-in hive bootstrap. The CONCLUSION survived (index -> marketplace is a full page load, so the const is read fresh) but the premise was wrong, and a gate built on it would have enforced something untrue. (2) index.html ALSO reads whHiveId() into a const HIVE_ID (~3802), which looks like the same stale-capture shape and is not: that capture is FUNCTION-scoped inside the ops-home renderer so it is re-read per call, and the write is deliberately followed by _initDashboard(...) to re-render against the new hive - verified by reading both sites, not by trusting the comment. So the distinction is SCOPE, not filename. A4 IS NO LONGER MERELY OBSERVED: tools/validate_hive_capture_invariant.py (registered as hive-capture-invariant) now asserts the writer allowlist AND that no capturing marketplace page writes the key, with a self-test that injects a writer and requires a red. Still OWED because A3 (configurability) reduces to the D9 knobs, which are Ian to set, and A5 (extensibility) is DEMONSTRATED rather than assertable - this arc s own denominator grows by itself when a guard changes, which is the claim A5 makes. Deliberately NOT banked: the remaining two thirds would be a weak cell inflating the UFAI board, and the A4 third is owned by a platform gate rather than a bank cell, which is the same disposition the thin layers use.
