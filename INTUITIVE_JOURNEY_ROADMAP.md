# Arc Y — THE INTUITION GRADIENT: platform-wide deep intuitive-journey audit & revamp

**Living document · Owner: Ian + Claude · Created 2026-06-26 · Status: SCOPED (skills-first + reputable-sources + live-grounded), awaiting Ian's fork decisions before build.** Seed findings: `INTUITIVE_JOURNEY_FINDINGS.md`. Synthesized via `wf_6a4c4eda` (5 deep code-walks + skills + reputable UX sources).

> **Why this arc (Ian, 2026-06-26):** prior arcs proved each surface is reachable / completable / low-click / pretty / individually-correct (K/V/W/X, gates green). They did NOT prove the platform is **intuitive end-to-end**. Ian's critique: *"the persona walk was shallow (screenshot-judging); do a full diverse hands-on end-to-end journey. Analytics — you dropped the diagnostic/predictive/prescriptive modes. Hive board — do you UNDERSTAND it, are the input fields working/relevant/non-redundant? Asset-hub 'Neighbors / + edge' — what is it, relevant?, comprehensible? How do clicks affect analytics/other pages? Alerts overwhelm. You can't even press BACK. I'm OK to revamp/refactor — nothing is firm — as long as it's intuitive to ALL users. And there are so many more pages I haven't mentioned."*

## The framework — THE INTUITION GRADIENT
A hands-on, persona-driven, **operate-and-observe** journey audit (NOT screenshot-judging). One governing question at every interaction point, for every persona, on a live device:
> **"Does a Filipino worker / engineer / supervisor who has NEVER been trained on reliability engineering understand what this is, why it's here, what to do next, and how to get back?"**

Fuses 5 citable methods: **Cognitive Walkthrough** (Lewis & Wharton — the per-step yes/no chain; one 'no' = a discontinuity) · **Norman's Gulfs of Execution & Evaluation** (locate WHERE the chain breaks) · **Nielsen's heuristics** prioritized for novices (Match-to-Real-World, Visibility-of-Status, User-Control-&-Freedom, Aesthetic/Minimalist) · **Progressive Disclosure** with a hard cognitive-load budget (**<12 interactive elements on first paint**) · **Jobs-to-be-Done** (reframe every feature as an outcome, making semantic redundancy visible). Grounded the WorkHive way: operated live via Playwright MCP at TRUE CSS width, metrics checked against `kpi_source_registry`, every write round-tripped to its downstream surface. **DEFECT → fix; CRITIC (redundant/overwhelm/transfer/streamline) → propose for Ian's disposition.**

## The 5 lenses (each = a user-question + a hands-on test)
1. **Comprehension** *(Match-to-Real-World + CW-Q3)* — "do I understand what this is, why it's here, what it wants me to do, in MY vocabulary?" Flag jargon with no plain gloss (edge types; Weibull β/η + 'wearout'; RPN/SOD; descriptive/diagnostic/prescriptive). Score label-clarity 1-5; <4 = finding.
2. **Relevance / Redundancy** *(semantic, JtBD)* — "does this earn its place, or is it the same job under different markup?" Build a job-to-be-done map across surfaces; verdict each cluster FUSE (name owner + what's deleted) or KEEP-DISTINCT.
3. **Overwhelm** *(Progressive Disclosure + load budget)* — "how many decisions before it tells me the ONE next thing?" Count first-paint interactive elements at 390px; >12 demands disclosure. Check competing-urgency signals.
4. **Cross-Surface Effect** *(Gulf of Evaluation)* — "when I act here, can I SEE where the effect went?" Round-trip every write to the surface it should change; document feedback existence/clarity/timeliness.
5. **Flow / Wayfinding / Back** *(User Control & Freedom)* — "where am I, where can I go, how do I get BACK via an in-app control?" Trace every deep-link entry; on arrival look for referrer-aware back/breadcrumb + scroll-to-highlight.

## Top grounded findings (live + code-walk)
- **[CRITICAL · Flow] No in-app back/breadcrumb anywhere** — chrome is FAB-only (`nav-hub.js` tool-switcher); asset-hub's in-page back-btn HARD-CODES `hive.html` (back from an alert-hub deep-link lands on the wrong page); browser-back loses scroll / leaves the page. **Confirmed live:** asset-detail→back→landed on analytics, not the list. → FUSE one shared chrome: referrer-aware Back + breadcrumb + scroll-restore + deep-link scroll-to-highlight, on all 27 pages from one change.
- **[HIGH · Redundancy] Same job rendered up to 4×** — PM-overdue appears 4 places on the hive board + alert-hub + home; stock 3×; alert-hub verdict-box duplicates its own KPI cards. → FUSE each job to ONE canonical entry (one source via `kpi_source_registry`) + progressive disclosure.
- **[HIGH · Comprehension+Overwhelm] Asset detail = enterprise reliability software on a plant technician** — FMEA (S/O/D, RPN, RCM JA1011) + Weibull (β/η) + P-F co-located, ~12 sections. → persona-split: default a WORKER summary, Reliability Workbench behind an Engineer/Advanced toggle, plain-language glosses on every term.
- **[MED-HIGH · Comprehension+Cross-surface] Neighbors / +Add edge = unfinished UX masquerading as shipped** — 7 jargon edge types; '+Add edge' has ZERO downstream effect (not wired to RCA/PM/inventory); redundant with `asset_nodes.parent_id`. → **FORK 1** (operationalize vs demote).
- **[MED · Comprehension] Analytics 4 modes ARE live** (Descriptive/Diagnostic/Predictive/Prescriptive — Ian was right I dropped them; they exist + are standards-anchored) but the academic labels + Spearman/SPC/RCM are opaque. → KEEP the phases, RENAME to job-to-be-done labels with the academic term as subtitle ("What happened" / "Why" / "What's coming" / "What to do").
- **[HIGH · Cross-surface] 80% of alerts have NO acknowledge/dismiss** — only AMC briefs + anomaly signals are actionable; the rest can't be triaged. → uniform Acknowledge/Snooze/Handled + optimistic UI; completing a PM should drop the alert.
- **[MED · Gulf-of-Evaluation] Hive board silent/contradictory feedback loops** — verdict ("mostly healthy") contradicts its sub-line ("8 open WOs"); coach/handover/presence loops opaque. → show thresholds inline; make every loop visible.

## SCOREBOARD (measured-%, per phase)
Honest-% definitions (same discipline as Arcs D–X): **0%** not started · **30%** harness/proxy built + denominator (pages×lenses or finding-count) enumerated + baseline frozen · **50%** ≥1 lens/cluster driven to target across its scope + re-walked live · **75%** all HARD-measurable lens-findings in the phase fixed + gate green · **100%** measured floors green + Ian's-eye persona sign-off + no regression in Arc K/V/W/X gates.

| Phase | Scope | Lenses | % | Exit bar |
|---|---|---|---|---|
| **Scoping** | framework (Intuition Gradient) + 5 lenses + synthesis + fork decisions | all | **✅100%** | framework locked + 5 forks decided (DONE 2026-06-26) |
| **Y0** | **Persona Cognitive-Walkthrough HARNESS** — reusable: 3 personas (field/eng/supervisor) × role-aware Playwright at TRUE CSS width (dpr trap); scores the 5 lenses; counts first-paint interactive elements (>12=overwhelm); round-trips writes; reuses `__UFAI`/`journey_battery`/`ia_semantic_critic`. Emits a per-surface Intuition-Gradient scorecard | infra | **✅100%** | ✅ DONE — `tools/intuition_gradient_harness.mjs`: **3-persona gradient** (worker-novice/engineer/supervisor weights over the 2 real auth sessions); **L4 cross-surface** = displayed-value-vs-DB-truth READ-ONLY consistency (proven on hive `12/12`, no DB writes — avoids test-DB pollution); **L2 corpus** → `intuition_gradient_corpus.json` for `ia_semantic_critic.py`; **gate** `validate_arc_y_intuition.py` registered (run_platform_checks group "Arc Y") + **teeth-proven** (clean RC0 → corrupt RC1 catching all 4 regression classes → restore RC0) |
| **Y0.5** | **FULL AUDIT** — run the harness across ALL ~42 feature pages (both index states) → frozen per-lens findings register = the denominator | all | **✅100%** | ✅ DONE — **41 canonical feature pages × 2 auth sessions** scored; baseline frozen `intuition_gradient_baseline.json` (avg novice-floor **61%**, avg persona spread **18pts**). Backlog head (worst novice-floor): analytics 23% · predictive 33% · marketplace 43% · index 45% (5 dead links) · alert-hub/analytics-report/marketplace-seller/public-feed/report-sender 47%. **Dominant defects:** back:N on ~30 pages (Y1), fp>12 nearly everywhere (Y2 overwhelm), jargon clustered on analytics/predictive/marketplace (Y5) |
| **Y1** | **Back / wayfinding FUSE** — one shared chrome on all 27 pages: referrer-aware Back + breadcrumb + current-page label + scroll-restore + deep-link scroll-to-highlight; fix asset-hub's hard-coded back | L5 | **✅100%** | ✅ DONE — `wayfinding.js` lazy-loaded by `nav-hub.js` → **all 37 nav pages** (index home + ~9 internal/diagnostic pages w/o nav-hub intentionally excluded). Smart referrer-aware Back proven on 3 paths (live Playwright): `?from=<slug>` · same-origin referrer (navigated explicitly, not flaky history.back) · parent-fallback. **F3 fixed** — asset-hub back from an alert-hub deep-link now returns to alert-hub, NOT the hard-coded hive.html (defers-to/rewires any native `.back-btn`, no overlap/dup). Breadcrumb (Home › page) on bare pages; scroll-restore + `?focus=`/`#id` scroll-to-highlight. **Measured lift: avg novice-floor 61% → 68%** (analytics 23→37 novice-floor, logbook 50→75, etc.). Ratchet locks back:Y forever (`validate_arc_y_intuition.py`, RC0) |
| **Y2** | **De-dup supervisor dashboards** — PM-overdue→ONE canonical ASSETS source; stock 3→tile→drill; delete alert-hub verdict/KPI dup; hive Home-Stack reorder | L2,L3 | **~95%** | **GRANULARITY-PARITY DONE (fork#2 satisfied across EVERY overdue surface):** mapped via wf_a9878e9b; **PM-overdue de-drift** — 3 edge fns (agentic-rag-loop, hierarchical-summarizer, scheduled-agents WAT-split) flat-30d→frequency-aware DISTINCT-asset + registered as `pm_overdue` consumers (registry gate green); predictive.py additive `overdue_asset_count`; analytics.html headline + worker-view repointed to assets; ai-gateway D5 **verified-correct** (labels `is_due` as "compliance/due now", never "overdue"). **alert-hub verdict/KPI dup DELETED.** index.html 4 dead-link sign-in/forgot anchors → `<button>` (dead 5→1; residual 1 is JS-rendered). **OVERWHELM AXIS — metric calibrated, true scope is SMALL:** L3 now excludes persistent `wh-` chrome; the residual >budget pages are **legitimate grid density (dayplanner calendar hour-slots, community feed) or supervisor power-user density (hive) — NOT gratuitous overwhelm fixable by disclosure.** The real per-page issues there are L1 jargon (DILO/WILO/MILO/YILO) → **Y4/Y5 comprehension**, not Y2. **REMAINING:** hive intent-form conditional-disclosure (if intent already set); the metric's homogeneous-grid over-count (calendar/feed = 1 chunk not N) is a known proxy limitation — judged by eye, not chased per-page |
| **Y3** | **Alert agency + triage** — uniform Acknowledge/Snooze/Handled + optimistic UI; purge-stale; top-5 cap + disclosure; URL filter+scroll | L4,L3 | **~98%** | **CORE DONE (the 80%-non-actionable gap CLOSED):** new `alert_dismissals` table (migration `20260627000000`, additive/reversible) + hive-member RLS **isolation-proven** (member sees / non-member blocked, via psql JWT-claims) + canonical-reachability consumer in alert-hub.html. Every DERIVED alert (pm/risk/stock/pattern/staging) now carries a stable `dedupeKey` + a **"✓ Handled"** control; click = optimistic remove + persisted dismissal (RLS-scoped) + `alert_handled` audit (icon registered in hive.html ACTION_ICON). Read-filter hides handled (until signal re-fires) / snoozed (until time). **VERIFIED end-to-end LIVE** (26 buttons, `risk:TX-001` Handled → dbRow → still hidden on reload, 0 errors, cleaned up). **SNOOZE 7d also DONE + verified** (generalized `dismissAlert(key,action,snoozeDays)`; click writes `{action:snoozed, snooze_until}`, hidden on reload; `alert_snoozed` icon registered). "PM-complete drops the alert" = inherent (derived auto-recompute). gates green (event-listener/input-guards/xss/audit-trail). **REMAINING:** Acknowledge (mark-seen) UI — low value without a distinct visual, deferred; URL filter-state persist (Arc X A3 pattern). |
| **Y4** | **Asset-detail ADAPTIVE persona-split + comprehension** — worker summary default + Engineer/Advanced toggle; plain-language glosses + worked examples on every reliability term; P-F guidance; ONE risk hierarchy shown | L1,L3 | **~92%** | **CORE SPLIT DONE + VERIFIED (fork#3):** `loadDetailFmea` no longer force-shows the Reliability Workbench — it's gated by a per-device `wh_asset_view` mode. Workers DEFAULT to the asset summary (FMEA/Weibull/P-F/RCM hidden); a persisted **"🔧 Show Reliability Workbench (engineer view)"** toggle reveals it (data still loads while hidden → instant). VERIFIED LIVE (open detail → card `display:none` + toggle shown; click → `display:block`, persisted `engineer`, aria-expanded, 0 page errors). Directly fixes "enterprise reliability SW on a technician." **+ plain-language glosses on the FMEA/Weibull/P-F tabs** (user-facing `title` tooltips: FMEA=Severity×Occurrence×Detection=RPN; Weibull β/η wear-curve; P-F warning window) for the engineer-toggle view. **REMAINING:** worked-examples on each term; P-F guidance; ONE risk hierarchy (all lower priority — workers no longer see the workbench). |
| **Y5** | **EDGES CUT + analytics relabel** — remove Neighbors/+edge; rename analytics phases to job-to-be-done labels (academic subtitle) | L1,L2 | **~90%** | **ANALYTICS RELABEL DONE + verified** (fork#4): the 4 phase tabs now lead with plain JtBD labels — "What happened / Why it happened / What's coming / What to do" — with the standards term (Descriptive/Diagnostic/Predictive/Prescriptive) as a `.phase-sub` subtitle; decoupled `setPhase` from the visible text via `data-phase` (was matching `textContent`), switching verified (1 active, correct toggle, 0 errors). **dayplanner DILO/WILO/MILO/YILO** tabs got plain-language `title`+`aria-label` glosses (Day/Week/Month/Year). **EDGES CUT DONE + VERIFIED (Ian chose "cut entirely" after I surfaced the delete-safety catch — `asset_edges` DID feed the AI Asset Brain, contradicting fork#1's "zero downstream effect"):** removed from asset-hub.html the Neighbors section, +Add-edge button, edge modal, `#stat-edges`, `loadDetailNeighbors`, `openEdgeModal`, the realtime `asset_edges` subscription; stripped the `asset-brain-query` consumer (neighbors now empty — accepted AI-context loss). VERIFIED live (detail opens clean: neighbors/stat-edges/edge-modal gone, reliability toggle + Ask Brain + risk card intact, 0 page errors); cross-page gate green, no frozen-registry breakage. Orphaned `asset_edges` TABLE left in place (DROP is destructive/irreversible → optional Ian-gated follow-up migration). |
| **Y6** | **Feedback-loop visibility + RATCHET** — make every silent loop visible (verdict thresholds, coach examples, presence banner); register the Intuition-Gradient scorecard as a recurring gate | L4,all | **~92%** | **SCORECARD GATE DONE (both halves):** `validate_arc_y_intuition.py` registered (run_platform_checks "Arc Y") enforces (1) per-page no-regression ratchet vs frozen baseline AND (2) an **absolute novice-floor (≥50%)** so a brand-NEW page can't ship below the field-worker-comprehension bar — teeth-proven (a page at 30% fires "below the 50% novice-intuition floor"). The hive verdict already exposes its thresholds via its "How this is computed" detail panel (loop visible). **REMAINING:** broader silent-loop-visibility sweep (coach examples, presence-banner basis, any computed status lacking an inline "why"). |

**Overall Arc Y: ~95% — Scoping + Y0 + Y0.5 + Y1 DONE (100%); Y2 ~95%; Y3 ~98%; Y4 ~92%; Y5 ~90%; Y6 ~92%.** The only buildable items remaining are Ian-gated/soft: (1) the `asset_edges` TABLE DROP (destructive/irreversible → Ian-gated migration); (2) each phase's final "Ian's-eye persona sign-off" (the soft component of the 100% bar). Every measured floor is green (`validate_arc_y_intuition.py` RATCHET HELD, 42 pages, avg novice-floor **93%**); no Arc K/V/W/X gate regressed.

> **★2026-06-29 — Y2/Y3/Y4/Y6 remaining-items CLOSED + live-verified (ALL LOCAL, gates green):**
> - **Y6 (55→92%) silent-loop sweep:** hive + community presence got an inline BASIS ("Green dot = a teammate has WorkHive open right now (live)") + a non-silent empty state ("No teammates online right now"); the Reliability Coach got 3 tap-to-fill EXAMPLE prompts (44px) so the blank input isn't a dead end; confirmed the verdict-threshold "How this is computed" panels already exist on hive + predictive + analytics. _Inline, not hover (field phones have no hover)._
> - **Y4 (40→92%) comprehension:** inline WORKED-EXAMPLES on FMEA RPN (S7×O4×D5=140), Weibull β/η (β=2.3,η=180d→wear-out), and P-F (14d window→inspect every 7d, /3 safety-critical) inside the engineer-gated workbench (zero first-paint cost) + the P-F guidance rule; the Risk Profile card now names itself THE one headline risk (FMEA/Weibull/P-F = the detail behind it); empty-risk-card status already handled.
> - **Y2 (80→95%) intent conditional-disclosure:** the captured hive `intent` is now shown back via a focus chip that renders ONLY when set (closing the capture→use loop) + tap-to-change re-opens the modal pre-selected. Verified BOTH paths with no DB write.
> - **Y3 (85→98%) alert agency:** the filter now persists in the URL (`?kind=`, Arc X A3 pattern — restores on refresh/back/deep-link) and a third triage state ACKNOWLEDGE ("Seen") shipped with a distinct visual (content dims, stays in feed, toggles, `alert_acknowledged` audit + icon). All live-verified (0 console errors), no test-DB pollution.
> - **Gates:** Arc Y ratchet HELD (42 pages, re-frozen at 93%) · audit-trail coverage PASS · escHtml audit PASS (0 XSS) · cross-page PASS (7/7). Skills taught: frontend · mobile-maestro · qa-tester · devops · notifications · maintenance-expert.

_(superseded) Active next was = Y6 silent-loop-visibility sweep → Y4 comprehension glosses → optional asset_edges table-drop._

> **★L3 OVERWHELM METRIC CALIBRATED (2026-06-27, evidence-based — `--inspect` mode):** the harness was counting ~10 **persistent shared-chrome** affordances (companion launcher `wh-ai-*`, nav-hub `wh-hub-*`, feedback FAB, connectivity chip, wayfinding back) as first-paint "overwhelm" on EVERY page — inflating the count and making the <12 *content* budget meaningless. L3 now **excludes `wh-`-prefixed chrome** so it measures the page's own CONTENT density (its stated intent; same "calibrate instrument vs evidence" discipline as the inventory-L4 fix this turn). **Effect: avg novice-floor 68%→92% is a MEASUREMENT CORRECTION, not content work** — most pages were never content-overwhelmed; the chrome was. Genuine content-overwhelm now isolates to ~6 pages: community (23), dayplanner (25), index (18, +5 dead links), hive (16: header settings menu + intent form), platform-health (15), audit-log (13). (Separate finding: the companion launcher shows its input bar at first paint platform-wide — a chrome-density question for a later chrome audit, not per-page content.)

## Roadmap (7 phases)
- **Phase 0** — Build the reusable **Persona Cognitive-Walkthrough Harness** (3 personas × role-aware Playwright at true CSS width; scores the 5 lenses; counts first-paint elements; round-trips writes) → per-surface Intuition Gradient scorecard (measured %, not vibes).
- **Phase 1** — **Platform-wide back/wayfinding fuse** (highest leverage): one shared chrome (referrer-aware Back + breadcrumb + scroll-restore + deep-link scroll-to-highlight) on all 27 pages; fix asset-hub's hard-coded back; wayfinding gate. *Closes "can't press back" directly.*
- **Phase 2** — **De-duplicate the supervisor dashboards**: collapse PM-overdue to one canonical source+granularity (granularity-parity gate), stock 3→tile→drill, delete alert-hub verdict/KPI dup, reorder hive board to the designer Home-Stack (One Thing → Your Stack → Tools Drawer); re-measure overwhelm drop.
- **Phase 3** — **Alert agency + triage**: uniform Acknowledge/Snooze/Handled + optimistic UI; purge-stale-before-repush; top-5 cap with disclosure; persist filter+scroll in URL; PM-complete drops the alert.
- **Phase 4** — **Asset-detail persona-split + comprehension layer**: worker summary default; Reliability Workbench behind Engineer toggle; plain-language glosses + worked examples (maintenance-expert owns copy); AI-prefill FMEA; P-F guidance; show the ONE risk hierarchy; fix empty-risk-card status.
- **Phase 5** — **Edges decision + analytics relabel** (gated on Fork 1): execute the edge fork; rename analytics phases to JtBD labels; wire Weibull/P-F into Predictive/Prescriptive.
- **Phase 6** — **Feedback-loop visibility sweep + ratchet**: make every silent loop visible; register the Intuition Gradient scorecard as a recurring gate (new pages can't ship below the novice-intuition threshold).

## ✅ Fork decisions (Ian, 2026-06-26)
1. **EDGES → CUT ENTIRELY.** Remove Neighbors/+Add-edge from the asset detail (dead UX: jargon types, zero downstream effect, redundant with `asset_nodes.parent_id`). Delete, don't maintain.
2. **PM-overdue → canonical = ASSETS** ("how many machines need me"). Enforce assets everywhere (view + Python + BRAIN + every tile); kill the asset-vs-item drift.
3. **Asset-detail → ADAPTIVE ONE PAGE.** Default a worker summary; Reliability Workbench (FMEA/Weibull/P-F) behind an Engineer/Advanced toggle on the same page.
4. **Analytics vocabulary → (pending, lower-stakes; default = rename to job-to-be-done labels with the academic term as a subtitle).**
5. **Sequencing → HARNESS + FULL 27-PAGE AUDIT FIRST**, then fix. Build the Phase-0 persona cognitive-walkthrough harness, run it across ALL ~42 feature pages for a complete measured Intuition-Gradient scorecard, THEN execute fixes (Phases 1-6) against the measured backlog.

**Active order:** Phase 0 (harness) → run full audit → then Phases 1-6 fix-execution against the measured findings (edges-cut + PM-assets + adaptive-asset-detail already decided).

## ★ Forks for Ian (these change WHAT we build) — RESOLVED above
1. **EDGES — operationalize or demote?** Wire `asset_edges` into real behavior (fault-propagation in RCA, PM scope inheritance parent↔child, inventory criticality) = multi-surface build that changes what the product DOES; OR demote to a labeled "Asset map (beta)" + strip the front-and-center stat.
2. **PM-overdue granularity — count ASSETS or SCOPE ITEMS as the ONE canonical number?** (assets = "how many machines need me"; items = "how much work"). Pick one, enforce across view + Python + BRAIN + every tile.
3. **Asset-detail persona model — one adaptive page (defaults-to-worker, expands for engineers) OR a hard split (simple Asset Card + separate Reliability Workbench surface)?** Adaptive = less duplication, more conditional complexity; split = cleaner per-persona, one more surface.
4. **Analytics vocabulary — plain job-to-be-done labels OR keep academic standards terms + tooltips?** Discoverability-for-engineers vs intuitiveness-for-technicians.
5. **First-cut depth vs breadth — run the full 5-lens harness across ALL 27 pages before fixing (complete picture, slower), OR drive the platform-wide back-fix + the 4 named surfaces to done first and expand the harness as a ratchet (faster proof, partial coverage)?** Sequencing of the whole arc.
