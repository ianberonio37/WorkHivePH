---
name: skill-qa-tester
type: skill
source: skill:qa-tester
source_sha: d715a8444430e998
last_verified: 2026-07-13
supersedes: null
---
## skill · qa-tester

Tests flows, responsiveness, accessibility, and edge cases. Triggers on "test", "QA", "bug", "broken", "check the site", "accessibility", "edge case".

**Sections:** QA Tester Agent · Double-submit is a P7 bug class — a bare `addEventListener('click', writeHandler)` fires the write TWICE (2026-07-21, bug-hunt P7) · Refresh-mid-submit → retry duplicates a non-idempotent write (the button-lock does NOT catch it) (2026-07-22, DEEPWALK D2) · A user-input write MUST validate before the write — a post-write `if(error)` shows a raw PostgREST error (2026-07-21, bug-hunt P4) · A `<input type=number min step>` in a modal that submits via a BUTTON (not a native `<form>`) NEVER has its min/step enforced — test 2.5, 1e-9, scientific notation (2026-07-22, DEEPWALK D5) · A 0-result SEARCH must show the search "no-results", NOT the first-run "empty-state" CTA — server-filtered views collapse them (2026-07-22, DEEPWALK D3) · When a NEW gate's first pass flags many findings, TRIAGE for false positives before acting (2026-07-21) · A single-identity live sweep is BLIND to per-persona breakage — walk a DIVERSE roster headless (2026-07-01, FB2 Browser-CI) · Three honesty/contract checks from the Dayplanner/Growth PDDA (2026-07-12) · Classify a secret leak by CREDENTIAL SHAPE, not the bare word (2026-07-01, FB2) · A "renders empty / broken" verdict is only as good as your ROW SELECTOR — read the render fn first (2026-07-06, deep-walk dim-9) · A view-switcher page has ≥2 STATES — walk EACH, and an i18n page ≥2 LANGUAGES — walk BOTH (2026-07-15, home dashboard) · A fold-only walk is BLIND — expand EVERY collapsible + Show toggle and reach the TRUE bottom (2026-07-11, Hive Board PDDA) · Removing a redundant tile that's a parity anchor — keep it as a HIDDEN data cell, don't delete or re-home (2026-07-11, Hive Board PDDA) · "Completable" ≠ "effortless" — measure INTERACTION COST, not just pass/fail (2026-06-24, Arc V EFFORTLESS) · A "live journey" = a real SIGNED-IN browser walk, not HTTP codes — and a health check judges status<500, not r.ok (2026-07-18, prod live journey) · A live invoke catches what every static gate misses — and the edge runtime caches the compiled fn (2026-06-19, Arc E) · Value-at-the-glass has FOUR tiers — a hermetic oracle is green on STALE source; prove the live API serves it too (2026-06-23, Arc Q) · A passing tier can ROT — re-run the DOM sweep as a ratchet, not once (2026-06-23, Arc Q) · "It doesn't have to be live — is there another way to check it?" — VERIFIED, not just live (2026-06-19, Ian, Arc E) · Your Responsibilities · CLS (layout-shift) is a PASSIVE-load test — measure it before any interaction (2026-06-23, Arc L L1) · Auto-learned (2026-07-12 — PM Scheduler PDDA) · Lessons from the Grounded UFAI Sweep (2026-06-07) · Coverage-metric honesty — mine the denominator from the live registry (2026-06-17) · A new feature silently regresses the gates it was never registered in — re-run the full sweeps after ANY build (2026-06-21) · Triaging a WALL of forward-ratchet gate FAILs (a stale baseline ≠ 34 new bugs) — 2026-06-21 · The pre-push gate surfaces cascade fails in WAVES — and your own source-fix seeds the next wave (2026-07-21) · Lessons from Production (April 2026 — Report Sender build) · Missing Supabase CDN crashes ALL JS silently

(Deep source: `skill:qa-tester` — retrieve this TOC to know WHICH section to read.)
