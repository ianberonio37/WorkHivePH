---
name: skill-devops
type: skill
source: skill:devops
source_sha: f40a22be340caf30
last_verified: 2026-07-13
supersedes: null
---
## skill · devops

Handles deploy, CDN, staging, server config, and monitoring. Triggers on "deploy", "ship it", "server", "CDN", "staging", "cron", "hosting".

**Sections:** DevOps Agent · Your Responsibilities · Lessons from Production (April 2026 — Report Sender build) · Every new edge function requires a config.toml entry · Every new edge function that touches `hive_id` requires a CALLER GATE (2026-07-20) · Resend requires domain verification before using a custom `from` address · Always run platform guardian before committing · Service worker caches old HTML after deploy · Lessons from Production (June 2026 — Node toolchain on this machine) · npm `.cmd`/`.ps1` bin shims break because the project path contains `&` and spaces · `imageio_ffmpeg.get_ffmpeg_exe()` hangs → the whole video pipeline stalls · How to Operate · This Platform's Context · Deploy Checklist · Edge Function CORS — Always Use getCorsHeaders(req), Never Static Origin · deploy-functions.ps1 Must Cover Every Edge Function · Supabase Edge Functions — Separate Deploy Step · AI Provider Chain — TPM Limits Cause 413, Not 429 · Windows Deploy Workaround — Folder Name Contains `&` · Windows — PowerShell Execution Policy · Docker Not Required for Supabase Edge Function Deployment · Supabase Secrets — Must Redeploy After `secrets set` · Netlify CDN Cache — Users Need Hard Refresh After Deploy · Diagnosing Edge Function 500 Errors — curl Is the Only Reliable Method · API Validation After Every Edge Function Deploy · Context Compaction Resume — Edge Function May Be Committed But Not Deployed · Post-Deploy Propagation Delay · `ERR_NAME_NOT_RESOLVED` on Supabase Edge Functions — DNS Hiccup, Not Code · Large File Patching — Write-then-run Python Script · Social Auto-Publisher — paste-once credentials + auto/assist split (2026-07-01)

(Deep source: `skill:devops` — retrieve this TOC to know WHICH section to read.)
