"""
WorkHive Platform Guardian — Master Orchestrator
=================================================
Phase 1: Run every validator, check readiness, compare to baseline.

Usage:
  python run_platform_checks.py             # full run
  python run_platform_checks.py --fast      # skip live API calls (Layer 3)
  python run_platform_checks.py --gate-only # readiness gate only (no validators)

Output:
  platform_health.json   — machine-readable report (feeds future visual dashboard)
  platform_baseline.json — saved when all checks pass (used for regression detection)

Exit codes:
  0 = all pass (safe to deploy / start next feature)
  1 = one or more validators failed
  2 = regression detected (was passing, now failing)
  3 = readiness gate blocked

Loops (Phase 1 implements 1 + 3; Phases 2-4 add the rest):
  Loop 0: Observation    — baseline snapshot comparison
  Loop 1: Retrospection  — run all validators, classify failures
  Loop 3: Readiness Gate — git/deployment/API status
  Loop 2: Self-Learning  — (future: auto-update skill files)
  Loop 4: Improvement    — (future: web search, backlog)

Crash-prevention checks added 2026-04-28 (from production Safari iOS crash):
  validate_mobile.py     +2  — will-change:filter mobile override (FAIL),
                               body{animation} prefers-reduced-motion override (FAIL)
  validate_performance.py +1 — body{animation} animationend safety guard (FAIL),
                               index.html added to LIVE_PAGES scope

Deployment config + live endpoint coverage added 2026-04-29 (from analytics 500):
  validate_edge_config.py    — every supabase/functions/ dir must have config.toml
                               entry with explicit verify_jwt (catches silent JWT default)
  validate_analytics_live.py — calls deployed analytics-orchestrator for all 4 phases
                               (skip_if_fast=True; catches what static checks miss)
"""
import subprocess, sys, os, json, time, datetime
import urllib.request

PYTHON = sys.executable
FAST     = "--fast" in sys.argv
GATE     = "--gate-only" in sys.argv
AUTOFIX  = "--autofix" in sys.argv

BASELINE_FILE = "platform_baseline.json"
HEALTH_FILE   = "platform_health.json"

# ── Colour helpers (Windows-safe ANSI) ────────────────────────────────────────
def green(s):  return f"\033[92m{s}\033[0m"
def red(s):    return f"\033[91m{s}\033[0m"
def yellow(s): return f"\033[93m{s}\033[0m"
def cyan(s):   return f"\033[96m{s}\033[0m"
def bold(s):   return f"\033[1m{s}\033[0m"

# ── Unicode output (Windows UTF-8 fix) ────────────────────────────────────────
if sys.platform == "win32":
    import io
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8", errors="replace")
    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8", errors="replace")

# ── Validator registry ─────────────────────────────────────────────────────────
# Each entry: (id, script, label, group, report_json)
VALIDATORS = [
    # ── Engineering Calc Suite ────────────────────────────────────────────────
    # run_all_checks.py runs 3 layers internally and produces its own reports.
    # We call it with --fast here; the full integration test (Layer 3) is separate.
    {
        "id":      "anon-boundaries",
        "script":  "tools/prove_anon_boundaries.py",
        "args":    [],
        "label":   "ANON identity boundaries asked over the REAL gateway with the publishable key and NO session - and it was never being run (registered 2026-08-31). The script existed, passed 3/3, and was referenced by nothing: not this file, not another tool. It asks what a signed-in probe structurally cannot: verify_identity_boundaries answers the BE-ufai-I questions as a signed-in NON-ADMIN, which is a different threat model from a stranger holding only the publishable key. Its three checks are the ones that matter for a public API surface: BOLA_OBJECT - an unpublished listing that EXISTS, fetched by id as anon, returns HTTP 200 with 0 rows (an empty 200 is PostgREST's legible RLS refusal, and the row is real, so the zero is a refusal rather than an empty table); JWT_NOT_BODY - an anon INSERT claiming a seller_name in the BODY is refused 401 by the policy rather than trusted, which is the identity-from-the-JWT-not-the-payload rule that the worker_profiles and logbook recipes enforce on the DB side; TENANT_BOUNDARY - every unpublished listing across ALL hives asked for in ONE anon query returns 0 of 7, so the hidden set is invisible AS A SET, not merely one row at a time. ★FOUND BY AUDITING ORPHANS, and it is the same class as companion-delivery registered directly above and the 96 psql recipes nothing re-ran: 629 gate-shaped scripts exist, 96 are absent from this file, and after removing the ones invoked indirectly by a validator wrapper, SIX were referenced by nothing at all. Unregistered, unrun and skipped are indistinguishable from green when viewed from outside, which is why each had to be found by asking a different question rather than by reading the board.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "companion-delivery",
        "script":  "tools/companion_delivery_gate.py",
        "args":    [],
        "label":   "T85: the companion's persona choice must actually be DELIVERED, and the gate that proves it was never being run (registered 2026-08-31). tools/companion_delivery_gate.py has existed and PASSES standalone - persona_resolution current=0 baseline=0 - but it appeared ZERO times in this file, so the board never executed it. T85 sat at `locking` 90% naming `companion-delivery` in its artifacts, and validate_trajectory_registry let that stand because rule 5 requires a `locking` row to NAME a gate without checking the name RESOLVES. So the registry asserted a lock, the script existed, the script passed - and nothing connected the three. ★THIS IS THE SAME DEFECT CLASS FOUND TWICE MORE TODAY, which is why it is worth a long label: 96 psql recipes carried a `replay` line and nothing ever re-ran them (fixed by the psql-probe-suite gate), and 16 of 20 locked trajectories rest on gates reading SKIP because full boards were dying on KeyError: 'args'. A lock nothing runs locks nothing, and the three failure modes are indistinguishable from the outside - unregistered, unrun, and skipped all look exactly like green. The gate itself is the pre-existing companion delivery check (persona resolution against its baseline); registering it is the whole fix, and it makes T85's claim true rather than aspirational.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "boundary-refusal-ratchet",
        "script":  "tools/census_boundary_not_emptiness.py",
        "args":    ["--check"],
        "label":   "T-arc boundary_not_emptiness, held as a forward-only line (2026-08-31). 34 bank rows carry ONE oracle - 'a permission boundary reads as not-visible-with-this-session, never as nothing-here' - and no gate covered it: empty-state-discrimination is about search-0 vs first-run, failed-read-is-not-empty is about a read that ERRORED, auth-boundary is about whether the server refuses. This is the third thing: the read SUCCEEDED, the server lawfully returned nothing, and the question is whether the page says 'you cannot see this' or 'there is nothing here'. ★WHY IT IS HARD AND WHY MOST SURFACES FAIL IT: RLS refuses by FILTERING, not raising - a USING clause returns zero rows and no error, so a foreign-hive read and a genuinely empty hive are byte-identical at the client unless the page makes a separate membership check and has somewhere to say so. MEASURED across 42 root surfaces: 26 own an absence state and 10 of those carry NO refusal copy at all. ★THAT NUMBER READ 25 FIRST AND THE INSTRUMENT WAS WRONG, TWICE OVER, WHICH IS WORTH MORE THAN THE COUNT: the vocabulary was narrower than the product's ('needs a hive'; 'Create or join a Hive' where the regex knew only 'join or create'; 'does not have access' where it knew only 'do not'), AND an edit wrote literal BACKSPACE bytes (0x08) where \b word-boundaries were intended, so the join/create clause demanded a backspace character in page text and could never fire. It went 25 -> 24 -> 22 -> 10 with no page changing. A census whose vocabulary is narrower than the product's measures its own wording and invents backlog - here, 2.5x the real one. The scenario is ordinary - a worker removed from a hive keeps wh_active_hive_id in localStorage, their next read is lawfully filtered to zero, and pm-scheduler answers 'No completions recorded yet.' ★THE ONE EXCEPTION IS THE FIX PATTERN, ALREADY IN THIS REPO: marketplace.html:1222-1226 keeps _rateLimited (429 + the wait), _authExpired (401, 'the remedy is signing in, not retrying') and _accessDenied ('a 403 is a REFUSED ROW with a perfectly good session, and signing in again cannot fix it'), reset per load so a since-granted permission stops reading as denied. So the work for the other 25 is to propagate an idiom this codebase already reasoned about. ★AND EVEN THAT EXCEPTION ONLY COVERS THE LOUD HALF: _accessDenied fires on a 403 the server announces; the silent case - RLS filtering, 200 with zero rows - is invisible to any HTTP status and needs the membership read. ★A RATCHET, NOT A FAIL, ON PURPOSE: 25 of 26 fail today, and a gate that is red the day it ships is one people learn to scroll past; this repo's ordering is fix first then enforce (retry_path did the same). The count may FALL, never rise - a NEW surface shipping an empty state with no way to say a refusal happened fails here by name. Comments are stripped before counting, because the first pass credited inventory.html with four refusal states that were every one a source comment or the toast 'Part removed from inventory.' Teeth proven by dropping a surface from the baseline: the check names it and fails.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "psql-probe-suite",
        "script":  "tools/validate_psql_probe_suite.py",
        "args":    [],
        "label":   "Re-executes EVERY psql recipe in tools/psql_probes/ - the 99 database invariants the page banks stand on. WHY IT EXISTS: 96 bank rows carry evidence kind `psql`, each naming a recipe file in its `replay` line, and until 2026-08-31 NOTHING RAN THEM. They were proven once at bank time and never again. The bank's freshness machinery expires such a row when its declared deps change, which is right for a claim ABOUT A FILE and says nothing about an invariant living in the DATABASE, where a migration, a policy edit or a dropped grant falsifies a recipe while every file it depends on sits untouched. The gap was not theoretical: public-feed__public_identity_only.sql is a REGRESSION LOCK written the same day, because an anonymous visitor could read every public post author's internal auth_uid (measured: 15 of 15 posts, 7 authors, 2 hives) - and a lock nothing executes locks nothing. ★IT EARNED ITS KEEP ON ITS FIRST FULL RUN: three of 99 recipes no longer held, and all three were INSTRUMENT faults rather than product regressions, which is exactly the split this gate exists to force someone to make. (1) assistant__feedback_attributable compared a before-count taken as postgres against an after-count taken as `authenticated`, whose SELECT policy shows only their own rows - postgres saw 1, the user saw 0 - so a perfectly good insert read as a refusal; it now counts the INSERT itself with RETURNING. (2) logbook__pm_mirror_idempotent assumed `completed_at + 1 day` was free, but its fixture worker had completions on CONSECUTIVE days, so the dedup index refused it exactly as designed and the probe called that a broken invariant; the free day is now computed from that pair's latest completion. (3) registry__ghost_retired asserted `marketplace_orders` holds zero rows - a PROXY for 'this relation is inert to clients' that our own test-data-seeder falsified by writing six lifecycle rows; the count leg is gone, the security legs (anon sees 0, INSERT refused by RLS, view is security_invoker) remain, and the genuinely unresolved half - marketplace-admin.html:1026 UPDATEs the table, so 'retired' is drifting - is recorded for Ian instead of quietly re-greened. ★RETRY-ONCE, REPORTED AS A FLAKE: the recipes take row locks and several SET ROLE, so 99 back-to-back reproduces this repo's known flake-under-load class - assistant__citations_resolve_lawfully failed inside the suite once and then passed three times standalone. A failing recipe is retried exactly once and a flake is PRINTED as a flake, never folded into the pass: a false RED sends someone hunting a defect that is not there and teaches the team to re-run reds until they turn green. Sequential on purpose, ~25s for all 99. No residue by construction - every mutating recipe wraps its teeth in BEGIN/ROLLBACK and most assert their own restoration.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "trust-claim-backed",
        "script":  "tools/validate_trust_claim_backed.py",
        "args":    [],
        "label":   "T70: a verification badge requires the thing it verifies. The seller profile is where a buyer decides whether to trust a stranger with money, so every pixel that reads as an endorsement is a claim the PLATFORM makes on that stranger's behalf - and the recorded incident shows what an unenforced one costs: the 'Certified' chip used to render on the cert_verified flag ALONE across three surfaces, and three sellers were found with cert_verified true, certifications NULL and cert_verified_at NULL, a violet chip asserting an admin had verified trade credentials THAT DID NOT EXIST. CENSUSED EVERY TRUST CLAIM ON THE PROFILE and the page is in good order - each is computed from data rather than decoration: Top rated (ratingAvg >= 4.5 AND ratingCount >= 5), Quick reply (avgResponseHours < 4 AND replyCount >= 5), Tier (stated thresholds - silver 11 sales, gold 51 - plus 'set by WorkHive from completed sales, not self-assigned'), ID Verified (gated on kyb_verified), Certified (routed through whCertBadgeEarned on all three surfaces). ★ONE LOOKED LIKE THE ORIGINAL BUG AND IS NOT: the certifications SECTION reads cert_verified on its own, but the whole block sits inside `if (certSection && certs.length)`, so a non-empty list is guaranteed by construction - read before accusing. THE ASSERTION guards the fixed class rather than a present defect: no buyer-facing surface may present a certification claim from cert_verified alone; it must call the central predicate or guard on the certifications list in the same breath. ★MODERATION SURFACES ARE EXCLUDED BY NAME, and not for convenience: marketplace-admin, founder-console and platform-actions are where verification is GRANTED, so they must see the raw flag including the exact 'set but no certifications on file' state this gate keeps off a buyer's screen - blinding the moderator to what they moderate would invert the goal. Teeth proven by reverting the profile badge to the flag alone, which resurrects the original incident's shape.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
        "severity": "blocker",
    },
    {
        "id":      "ai-render-provenance",
        "script":  "tools/validate_ai_render_provenance.py",
        "args":    [],
        "label":   "T87/AI6: the RENDER half of machine authorship. validate_ai_write_provenance proves the WRITE half at 6/6 - every AI write into a human-read domain table declares machine authorship ON THE ROW - and that is necessary but not sufficient, because a row can carry its provenance faithfully while the page throws it away on the way to the screen. ★THAT IS EXACTLY WHAT WAS HAPPENING: logbook's 'past repairs on this machine' panel, the one a technician reads BEFORE OPENING A MACHINE, queried fault_knowledge selecting only problem/root_cause/action/knowledge/worker_name/created_at and rendered each row attributed BY NAME. fault_knowledge is a table the AI also writes into (visual-defect-capture), so a machine-captured observation would have appeared as a repair a named colleague performed. The write was accountable; the READ discarded the accountability. Fixed: the select now fetches source and ai_model, and the render appends 'AI-captured (<model>)' beside the name - verified live with a seeded row, one badge among five, the four human entries untouched, probe removed and residue re-counted 0. ★THE BADGE TESTS source LIKE 'ai_%', NOT != 'manual', because that is the contract the DATABASE enforces - CHECK ((source ~~ 'ai\\_%') = (ai_model IS NOT NULL)) over a closed vocabulary of manual / ai_visual_capture / cmms_import - so the badge cannot drift from the guarantee, and cmms_import (an import, not a machine author) is correctly left unmarked. ★THREE INSTRUMENT CORRECTIONS, ALL CAUGHT BY THE TEETH TEST: blanking comments with plain spaces preserved byte offsets but destroyed LINE numbers, so it first reported the finding at an Escape-key handler; a .select('id') after an .update() was mistaken for a display read when it is an optimistic-concurrency no-op detector; and a 600-char scan window ended before logbook's own .select() because the explanatory comment above it is ~700 chars - the gate silently stopped seeing the query it was built for and reported PASS with the fix reverted. A window sized to today's code narrows every time someone documents something.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
        "severity": "blocker",
    },
    {
        "id":      "glossary-registry",
        "script":  "tools/validate_glossary_registry.py",
        "args":    [],
        "label":   "T177: the platform must teach its own vocabulary. WorkHive puts domain acronyms straight onto the glass - MTBF beside a number, OEE on a card, SPI in a project summary. For a reliability engineer that is correct and efficient; for the audience this platform explicitly courts - new graduates, workers coming off paper - an unexplained acronym is a number they can neither act on nor argue with, and 'I did not understand the screen' is indistinguishable from 'the screen was wrong'. THE RULE: every registered term appearing in an app page's visible text must be EXPLAINED somewhere reachable - expanded in place, or taught in the learn cluster. Both count, because a guide is a real answer and so is a phrase in the sentence itself. MEASURED 2026-08-26: thirteen terms on the glass and ELEVEN were already handled well - MTBF, MTTR, OEE, RCM, FMEA, RPN, CPM, LOTO, SOP and P-F each have a learn guide spelling them out, which says the platform's teaching habit is sound. ★THE ONE THAT WAS NOT: project-manager showed supervisors 'EVM: SPI 0.87, CPI 1.02, status ... (BAC ₱..., EV ₱...)' - SIX acronyms in a single line - and nothing anywhere on the platform explained one of them; earned value was the only vocabulary the product used without teaching. Fixed at FIRST USE in that line ('Earned value (EVM): schedule performance SPI ..., cost performance CPI ..., ... (budget at completion ₱..., earned value ₱...)'), because a glossary a reader has to go and find is a glossary they do not read. ★assistant.html IS EXCLUDED WITH ITS REASON: it carries a capability CATALOG describing every other page to the model ('...daily progress, EV/SPI/CPI, and sign-off'), which is model-facing rather than UI and double-counts vocabulary belonging to the pages it names - pages this gate scans on their own. Flagging it would demand a glossary inside a system prompt. Teeth proven by reverting the fix, which reproduces the exact pre-fix finding.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
        "severity": "blocker",
    },
    {
        "id":      "toast-is-readable",
        "script":  "tools/validate_toast_is_readable.py",
        "args":    [],
        "label":   "T182: a message shown too briefly was never shown. A toast is how this platform says 'saved', 'not saved', 'you cannot do that yet' - the entire receipt for most actions - and a receipt that vanishes before it can be read is WORSE than none, because the action looks silent so the person repeats it. Not hypothetical here: the recorded incident had nine REFUSALS displayed for zero milliseconds, i.e. never displayed at all, on exactly the paths where the user most needed to know why they were stopped. MEASURED 2026-08-26 and the class is EXTINCT: 937 showToast calls, 906 on their page's default and 31 with an explicit duration, all between 4000 and 7000ms; every one of the 27 per-page showToast implementations holds its toast 2200-3500ms; nothing anywhere is under 1500ms. TWO ASSERTIONS, guarding an absence rather than fixing a present defect: no call passes an explicit duration below the floor, and no implementation holds its message for less. ★THE FLOOR IS 1500ms AND DELIBERATELY LOW - this gate does not litigate whether 3000 beats 3500, a judgement 27 pages have already made sensibly and which a gate would only turn into enforced taste; it catches what is objectively broken, a message a human cannot finish reading. ★THE CENSUS PARSES ARGUMENTS RATHER THAN SCANNING A WINDOW, and that is the lesson: a first version looked for the nearest ', <number>)' after each call and reported ten short toasts, EVERY ONE FALSE - it had matched slice(0, 24), setTimeout(..., 60) and other numerics belonging to different calls entirely, including a '24ms toast' in utils.js that does not exist. Only balanced-paren splitting knows which arguments belong to which call. Teeth proven on both assertions.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "form-control-named",
        "script":  "tools/validate_form_control_named.py",
        "args":    [],
        "label":   "T180: every form control says what it is. A control with no accessible name is a box a screen-reader user is asked to fill in without being told what goes in it - and on this platform that was not theoretical: the unnamed ones were a PM frequency selector, a CMMS column mapper, a scope-item include checkbox and its estimated-hours field, each sitting in a LIST of near-identical rows where 'combo box' announced twelve times running is indistinguishable from noise. Fixed six, and every label names the ROW rather than the field, because these are heard out of visual context: 'Frequency for <task text>', 'Source column for <field label>', 'Include scope item <title>', 'Estimated hours for <title>', plus two validator-catalog filters sitting beside a search box that already had one. Verified in the LIVE DOM on all four pages (0 unnamed, 0 pageerrors), which also covers the dynamically-rendered controls a static pass cannot reach. ★THE CENSUS WAS WRONG THREE TIMES AND THE CORRECTIONS ARE THE REAL CONTENT: 41 -> 18 -> 8 -> 6, every step removing FALSE accusations rather than real defects. (1) Checking only label[for=id] missed the WRAPPING form - '<label>Safety-critical <input type=checkbox></label>' - which needs no `for` and is how every checkbox and radio here is labelled; that alone was 23 wrong accusations including all six of hive.html's intent radios. (2) Reading raw source matched <input> and <select> written inside HTML, CSS and JS COMMENTS - prose DESCRIBING controls, like logbook's 'A filter <select> may not be squeezed narrower than the option it is showing' - so 62 of the 311 'controls' were sentences. (3) Two survivors were labelled by TEMPLATE INTERPOLATION: resume.html builds `const _alab = aria-label=\"...\"` and injects ${_alab} into the tag, an attribute that is real at runtime and invisible to a static reader. The gate therefore SKIPS any control whose attributes carry an unresolved ${...} and REPORTS the count, because a static oracle must decline what it cannot know rather than guess. ★PLACEHOLDER IS NOT ACCEPTED AS A NAME: it vanishes the moment the field is focused, which is exactly when the reminder is needed.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "icon-meaning-registry",
        "script":  "tools/validate_icon_meaning_registry.py",
        "args":    [],
        "label":   "T179: one glyph, one meaning. An icon-only control teaches by repetition - a worker learns that a small x makes something go away, and after the tenth time they stop reading and just tap. That learned reflex is the whole value of an icon and the whole danger, because the moment the same glyph does something ELSE, the reflex fires anyway. CENSUSED every icon-only button across the app pages; five glyphs in use, two carrying trouble. ✕ meant Close, Remove, AND 'Delete resume <title>' - the same mark for 'dismiss this panel' and for 'permanently destroy the resume you spent an hour building'. Fixed by adopting the platform's OWN declared delete glyph (audit-log.html:790 maps edit '✎', delete '🗑'), so the resume delete is a wastebasket and ✕ is left meaning dismiss. × appeared on three project-manager buttons carrying title='Remove' and NO aria-label: ★THE TITLE DOES NOT SAVE IT, because the accessible-name order is aria-labelledby, aria-label, CONTENT, then title - content wins, a screen reader announces 'multiplication sign' and never reaches the title, and on touch there is no hover at all. Each now names what it removes ('Remove scope item <title>', 'Remove link <label>', 'Remove <name> from this project'), since three identical 'Remove' buttons in one list are their own kind of unusable. Also found and named a bare ← in logbook's multi-step entry form, sitting directly beside Save Entry where the difference between going back and saving is the user's whole entry. TWO ASSERTIONS: every icon-only button has an accessible name beyond its glyph, and no glyph carries both a DISMISS meaning and a DESTROY meaning. ★TWO SCOPING DECISIONS, both stated rather than silent: 'Remove' is NOT treated as destroy - taking a link off a list is undoable list-editing, and lumping it with delete would force a second glyph for something users correctly read as dismissal; and icon-ONLY means nothing but the glyph, after an earlier 'at most 3 other characters' allowance flagged six perfectly good icon+label buttons ('⬇ CSV', '⬇ PDF', '🟢 Low') whose visible text already IS their accessible name. Teeth proven on both assertions, the clash half being a true resurrection against the pre-fix ✕.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "inventory-txn-actor-attribution",
        "script":  "tools/validate_inventory_txn_actor_attribution.py",
        "args":    [],
        "label":   "Cluster 3 / critic T11: the inventory LEDGER attributes a part use to the ACTOR, not the item owner. Walked live (reproduced x2): inventory_transactions.worker_name was the item OWNER because inventory_deduct read v_worker FROM inventory_items - so Bryan pulling a part off an item Leandro registered logged 'Leandro used 1' on the audit trail a supervisor reads to answer who-took-this. Fixed (mig 20260902000002): the RPC resolves the actor from hive_members by auth.uid() in the item's hive (the XP/exam JWT->name mapping), owner-name only as last-resort for a system write. Verified live in a rolled-back tx: Bryan deducting a Leandro-owned item logs worker_name='Bryan Garcia'. Gate reads the installed prosrc; skips if the DB is down.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "toast-queue",
        "script":  "tools/validate_toast_queue.py",
        "args":    [],
        "label":   "C7 (critic deepwalk T9): RECEIPT TOASTS QUEUE instead of stomping. Walked live: a logbook save fires 'Entry saved' + '+15 XP' + a badge back-to-back and the single toast slot REPLACED each with the next in ~0ms (clearTimeout + overwrite) - the worker read only the last receipt; the XP receipt vanished unread on every save. Fixed: showToast enqueues while one is showing (cap 4, same-message de-dup so a repeating error cannot build a backlog) and the hide timer chains the next. Proven live: the exact three-receipt burst showed sequentially at 0/2.8/5.6s, each full-length. Three static legs with per-leg resurrection teeth. Platform note: 25 other pages define their own showToast - the sweep to a shared queued primitive is the follow-on; this locks the WALKED surface.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "hive-name-reconciler-wired",
        "script":  "tools/validate_hive_name_reconciler_wired.py",
        "args":    [],
        "label":   "C11 (critic deepwalk): STALE IDENTITY CACHES cured platform-wide, not per-page. Walked receipt: wh_hive_name renders on ~24 pages and never followed wh_active_hive_id - Bryan (Baguio) saw 'Lucena Pharmaceutical' chrome over his own correctly-scoped data after a shared-device divergence. The cure EXISTED (whReconcileHiveName: read v_hives_truth, correct cache, repaint) but only hive.html called it - built-but-never-called. Fixed: utils.js auto-wires the reconciler once for EVERY page (retry loop until the getDb singleton exists; best-effort, never page-breaking). Proven live: staged 'Baguio Textile Mills' against the Manila id on dayplanner (no hand call site) -> reconciled to 'Manila Electronics Assembly'. Gate legs: helper-reads-truth-view, the auto-wire, hive.html's richer board call; self-test reddens each removal.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "modal-back-contract",
        "script":  "tools/validate_modal_back_contract.py",
        "args":    [],
        "label":   "T42 (critic deepwalk): MODALS TRAP BACK. Walked live: opening inventory's Add-Part modal pushed no history entry, so the phone hardware-Back gesture navigated away from the WHOLE page mid-form (inventory -> logbook, task + typed input gone); no page implemented modal-back-to-close. Fixed in the ONE shared primitive every hand-rolled modal already opts into (whModalA11y/whSheetA11y): activate() pushes {whModalBack:true}, a shared popstate handler closes the TOP open modal via its OWN close path (the same logic Escape uses), and deactivate() consumes the entry with a suppressed history.back() when the modal closed any other way (X/backdrop/save) so no ghost states accumulate. Proven live on the walked defect: Back dismissed the modal and STAYED on inventory; after an X-close the stack drained (next Back exits cleanly). This gate asserts the contract's three legs in utils.js; self-test reddens on each leg's removal.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "vehicle-foundations",
        "script":  "tools/validate_vehicle_foundations.py",
        "args":    [],
        "label":   "VEHICLE SEED lock (Iteration 1, plan approved 2026-09-02): a car is an ASSET - lane, not silo. Four foundations: solo asset_nodes RLS (the sibling tables' hive_id-IS-NULL branch, without which a solo owner cannot own their car), asset_nodes.vehicle_meta jsonb (VIN/plate/odometer - NOT the CMMS-typed external_ids), mileage-PM primitives (pm_scope_items.interval_km+interval_kind, pm_completions.meter_at_completion), and the meter-aware v_pm_scope_items_truth (next_due_km/current_km/km_until_due, is_overdue OR'd with the meter half, 500km due-soon window) + Vehicle reading templates + the monotonic roll_vehicle_odometer trigger. All proven by an 11-probe rolled-back tx: solo insert/read + cross-user invisibility; due_km 130,000 from meter 120,000 + 10,000km interval; due-soon at 400km out; overdue past it; roll-forward 131,000; no-rewind on a typo'd 13,100. Migrations 20260902000004-06.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "vehicle-extract-accuracy",
        "script":  "tools/validate_vehicle_extract_accuracy.py",
        "args":    [],
        "label":   "VEHICLE SEED VM2 lock (Iteration 3, 2026-09-02): MEASURED extraction accuracy on the golden Ranger fixture, the evidence Ian required before the doc-upload UI opened. Drives the LIVE vehicle-doc-extract fn with miner_only:true (the deterministic interval/part miners - single source of truth in the fn's own code; no model, no flake): 12/12 schedule rows with EXACT km/month intervals (a wrong interval schedules real maintenance at the wrong mileage, so any mismatch FAILs), 8/8 exact part numbers, and the fixture's IGNORE-ALL-INSTRUCTIONS line stripped with BANANA never surfacing (the resume-extract injection lesson). The first run BIT: the miner took '5W-30' from spec parentheses over the real part number - paren-strip + rightmost-token fixed it. Skips clean when the edge runtime is down.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "embed-retry-queue",
        "script":  "tools/validate_embed_retry_queue.py",
        "args":    [],
        "label":   "Critic-deepwalk T10 AI4 lock: a failed RAG-index write retries and queues, never silently vanishes. Walked live: embed-entry 500'd on a PM save's logbook echo and the entry went missing from the RAG index with only a console.warn (write-only-index class - the assistant's recall quietly loses exactly the entries saved during a bad minute). Fixed 2026-09-02: utils.js whEmbedEntry = one 2s retry then persist to wh_embed_retry (cap 20, 7-day expiry) drained on a later load; idempotent server-side so duplicate drains are safe. VERIFIED LIVE both halves against the genuinely-down edge runtime (double-fail queued the payload intact) + mocked-ok drain (sent, not re-queued). pm-scheduler's fault-mirror routes through it; other embed sites adopt as touched. sw v252. Teeth: missing helper + unwired site both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "pick-prefills-category",
        "script":  "tools/validate_pick_prefills_category.py",
        "args":    [],
        "label":   "Critic-deepwalk T21 lock: pick-from-registered prefills the REQUIRED Category. Walked live: name/tag/location prefilled but Category stayed empty - TYPE_TO_CATEGORY was keyed by a hypothesized machine vocabulary ('Pump','Motor','HVAC'...) while the picker aliases type:iso_class whose REAL values are discipline words (Mechanical 58 / Electrical 17 / Pneumatic 11 / Instrumentation / Hydraulic / legacy 'PUMP') - every lookup missed; an oracle's vocabulary is part of the oracle. Fixed 2026-09-02: discipline keys + the case-insensitive _typeToCategory resolver at the pick site; verified in-page 6/6 real values resolve, unknown/empty degrade safely. sw v251 (pm-scheduler is shell). Teeth: dropped key + raw-lookup revert both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "report-reason-deliberate",
        "script":  "tools/validate_report_reason_deliberate.py",
        "args":    [],
        "label":   "Critic-deepwalk T31 lock: reporting a post requires a DELIBERATE reason and the audit row names whose post it was. Walked live: the select DEFAULTED to 'Harassment' (an untouched Send silently filed the gravest accusation - and openReport reset it back to 'harassment' every open, defeating any placeholder), and the audit row's target_name was blank (a report pointing at nothing). Fixed+verified live 2026-09-02: placeholder-first, submitReport refuses empty with the triage-why, openReport resets to the placeholder, audit carries the author (verified row 'report_post | Bryan Garcia | Wilfredo Malabanan | unsafe'; probe flags cleared after). Teeth: removed placeholder + reverted reset both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "approval-queue-aggregates",
        "script":  "tools/validate_approval_queue_aggregates.py",
        "args":    [],
        "label":   "Critic-deepwalk T20 lock: the hive board's approval badge counts EVERYTHING a supervisor owes a decision on. Walked live: the queue showed 4 (assets) while 30 more pending approvals sat scattered (19 FMEA modes + 9 strategies + 2 AMC briefings) with no aggregate anywhere. Fixed+verified live 2026-09-02 as the supervisor: badge=34 (the walk's exact number), 'Also awaiting your review in Asset Hub: 19 FMEA modes - 9 strategies - 2 shift briefings in Alert Hub' - every type counted and linked to its review surface (briefings were the missing third type, completed this pass). sw v250 (hive.html is shell). Teeth: dropping any count or shrinking the sum reddens.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "draft-age-visible",
        "script":  "tools/validate_draft_age_visible.py",
        "args":    [],
        "label":   "Critic-deepwalk T55 lock: a seller's pending-review draft states how long it has waited. Walked live: listings sat 'Draft: Pending Review' for 43 and 71 days (P23,474 parked) with no age voice, no expectation - review latency invisible. Fixed (verified live on the exact walked listings: '43d waiting' / '71d waiting'): the draft chip carries the wait, deliberately NO invented SLA - only what is true. Teeth: removing the age suffix + unwiring the draft branch both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "modal-back-helper",
        "script":  "tools/validate_modal_back_helper.py",
        "args":    [],
        "label":   "Critic-deepwalk T42 lock: hardware Back closes an open modal instead of leaving the page. Walked live at 390: inventory's Add-Part modal pushed NO history entry, so the phone Back gesture navigated inventory -> logbook, losing the worker's mid-form input; no page wired Back to modal state. Fixed 2026-09-02: utils.js ships whModalHistory (opened() pushes one entry; popstate closes the top modal; closed() consumes on explicit close - re-entrancy safe) with inventory's part-modal the first wired consumer. Verified live both directions: Back closed the modal and STAYED on inventory; X-close balanced history. sw v249 (utils.js is shell-precached). Teeth: missing helper + unwired open both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "staged-stock-guard",
        "script":  "tools/validate_staged_stock_guard.py",
        "args":    [],
        "label":   "Critic-deepwalk T11 S4 lock: a part staged for a predicted failure cannot be silently consumed. Walked live: the Use dialog computed 'Available: 0 pcs (1 on hand - 1 staged)' and Confirm Use COMMITTED anyway - the staged unit for the predicted repair vanished (the display had the number; the WRITE PATH had no guard). Fixed via migration 20260902000003 mirroring the marketplace credit-hold: BEFORE INSERT guard refuses a consuming write that would push on-hand below the actively-staged total, errcode check_violation so whWriteError surfaces the sentence verbatim ('Only 0 free: 0 on hand and 1 staged for a predicted failure on CR-001... nothing was saved' + release/restock hint). Proven both directions in rolled-back txs (walked item refused; free item inserts - no over-blocking). The gate REPLAYS the refusal live each run (rolled back) when a fully-staged item exists.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "inventory-tx-attribution",
        "script":  "tools/validate_inventory_tx_attribution.py",
        "args":    [],
        "label":   "Critic-deepwalk T11 lock: an inventory transaction's worker_name is the SESSION's identity, never the payload's. Walked live (x2): Bryan's Use writes stored worker_name='Leandro Marquez' (client-supplied column; audit surfaces display it - any part-take can bear anyone's name). Fixed via migration 20260902000002: BEFORE INSERT trigger derives worker_name+auth_uid from auth.uid()+hive_members for the row's hive (JWT-not-body, the XP/exam discipline; vetted service-write bypass); 2 existing rows repaired. Proven in a rolled-back tx: payload 'Leandro Marquez' -> stored 'Bryan Garcia'. Gate checks trigger wired + function derivation + zero live mismatches; skips clean when docker down. Teeth: missing trigger + planted mismatches both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "hive-name-reconciles",
        "script":  "tools/validate_hive_name_reconciles.py",
        "args":    [],
        "label":   "Critic-deepwalk T19 lock: a cached hive name cannot survive against server truth (on the GLASS, not just the cache), and the risk feed carries no test-artifact phantoms. Walked live: Bryan saw 'Lucena Pharmaceutical Mfg.' chrome over his own correctly-scoped Baguio data (wh_hive_name written at join/switch, rendered forever, never revalidated) AND ops-home's top action headlined 'CRITICAL RISK: CP-201 (alert-test)' - an orphan test row with no asset behind it. Fixed+verified live 2026-09-02 (poisoned cache -> '🐝 Baguio Textile Mills' on glass, zero Lucena): utils.js auto-wires whReconcileHiveName on every page (C11 built-but-barely-called closed), index's hive button name now sits in a data-wh-hive-name span the repaint reaches; DB layer asserts zero test/orphan risk rows. Teeth: missing auto-wire + untagged span both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "pm-completion-repaints-truth",
        "script":  "tools/validate_pm_completion_repaints_truth.py",
        "args":    [],
        "label":   "Critic-deepwalk T10 lock: completing a PM repaints the detail from the TRUTH VIEW, never stale in-memory state. Walked live: the worker completed a PM and the detail kept 'Last done Jul 18 - Next due Jul 25 (39d overdue)' while v_pm_scope_items_truth (same session) said completed-TODAY, is_overdue=false - the post-save flow re-rendered from the in-memory object, violating the page's own doctrine (client date fallbacks WERE the bug, deleted 2026-05-20). Fixed 2026-09-02: completion awaits loadData(), re-finds currentAsset, then renders; verified live post-save 'Last done: Sep 2, 2026 - Next due: Sep 9, 2026 (Due in 7d)' with genuinely-overdue siblings keeping honest states. sw bumped v248 (pm-scheduler is shell-precached). Teeth: removing the refetch from the completion block reddens.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "assistant-no-orphan-fragment",
        "script":  "tools/validate_assistant_no_orphan_fragment.py",
        "args":    [],
        "label":   "Critic-deepwalk T16 lock: a mid-generation stream death can never render a token fragment as the assistant's answer. Walked live: ai-gateway's downstream died mid-generation (console 546) and the chat showed 'What did I log today?' answered by a bare 'I' - a 200 whose answer was the first token, rendered as if complete. Fixed 2026-09-02: Step-1 floors fragments through to Step-2; a Step-2 fragment THROWS to the inline-error catch (bubble + question restored). Predicate verified live ('I' and 'I think the' trapped; 'Yes.' and real answers pass). Also locks the signed-in auto-start (wh_last_worker -> startChat) that resolved the same walk's identity-amnesia + dead-personalization (verified live: Bryan auto-started, 'I can see your job records', 317 entries). Teeth: disabled floor + removed auto-start both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "xp-feedback-reaches-worker",
        "script":  "tools/validate_xp_feedback_reaches_worker.py",
        "args":    [],
        "label":   "Critic-deepwalk T17 lock: when a worker EARNS XP they are told at the earning moment, undisplaced. Walked live: a worker earned 140 XP in 11 min and was never told - logbook's single-slot toast let the tasklist-ack toast DISPLACE the XP toast, and pm-scheduler granted 60+20 XP with zero feedback. Fixed+verified live 2026-09-02: logbook has a toast QUEUE (proved live - 'tasklist' toast then '+60 XP' both shown, not dropped) and appends _xpNote; pm-scheduler appends _xpNoteFor(compData.id) on completion. Teeth: removing the queue drain or the pm XP note both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "skill-privacy-copy-consistent",
        "script":  "tools/validate_skill_privacy_copy_consistent.py",
        "args":    [],
        "label":   "Critic-deepwalk T18 lock: the skill-matrix privacy promise and the exam-pass result copy cannot contradict on one screen. Walked live: header promised 'visible only to you, not to your supervisor or teammates' then the pass copy said 'it shows on the hive board' (reads as public). RLS confirmed skill_badges is auth_uid=auth.uid() - the worker-profile drawer returns badges ONLY to the worker's own view, so the promise is the truth and the copy over-claimed. Fixed 2026-09-02: result names the private truth ('on your own hive-board profile - still private to you'). Teeth: the bare over-claim and a missing promise-header both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "risk-pm-linkage",
        "script":  "tools/validate_risk_pm_linkage.py",
        "args":    [],
        "label":   "Critic-deepwalk T26 lock: the risk engine's PM-overdue factor reads REAL PM data, never emits 'No PM data linked' from a join miss. Walked live: asset-hub PB-001 showed 'No PM data linked; assumed medium overdue' (+15% cry-wolf) on the same screen whose header counted PM COMPLETED 14 - the engine keyed pm_assets by exact-case name while logbook's machine is the TAG, and last_anchor_date was never selected. Fixed in batch-risk-scoring (dual name+tag normalized keys + last_anchor_date selected); stale data regenerated - PB-001 -> 'Last PM anchor 60 days ago; partial overdue', and a D3 local-substitute recompute cleared 2760 historical rows across 2 more hives that the old engine mislinked. Two layers: SOURCE (dual-key + anchor-select + normalized lookup) + DATA (no live risk row says 'No PM data linked' for an asset with pm_completions). Teeth: dropping the anchor-select or collapsing to name-only keying both redden.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "kpi-evidence-links",
        "script":  "tools/validate_kpi_evidence_links.py",
        "args":    [],
        "label":   "Critic-deepwalk T9+T47 locks: a KPI counts what it claims, and the headline figure carries its evidence. T9: logbook's 'Jobs closed out' strip derived its denominator from the LOADED WINDOW (_allEntries.length, row-capped) - a cap shown as a total; now prefers _mineTrueTotal/_mineTrueOpen from count:exact (verified live: 'JOBS CLOSED OUT 313 of 317' with the window at 0). T47: analytics' worst-MTBF card was accurate (0.6d = 137 breakdowns/90d, independently recomputed) but a DEAD END; now wired role=link click/Enter -> logbook.html?view=team&q=<asset> (verified live: landed ?view=team&q=CT-001, 28 evidence cards - riding the T29-fixed deep-link + one-clock render in the same receipt). Teeth: the window-denominator shape and the unwired-card shape both redden in --self-test.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "journal-transcript-is-raw",
        "script":  "tools/validate_journal_transcript_is_raw.py",
        "args":    [],
        "label":   "Cluster 4 (critic deepwalk T12): a voice-journal transcript is the WORKER'S OWN WORDS, never the RAG-augmented model prompt. Walked live the journal rendered entries whose transcript was the question + the whole retrieval scaffold ('--- RELEVANT TEAM KNOWLEDGE --- RELEVANT FAULT HISTORY... RELEVANT SKILL PROFILES: - <teammate>: Facilities Level 5 --- END KNOWLEDGE ---') - unreadable, teammates' data in a personal journal, and the stored record not what was said (re-embedding it poisons recall recursively). Reading the code proved both LIVE client paths already send the raw utterance (voice-journal.html callJournalAgent(text):1335, voice-handler.js gateway message: transcript:7937; grounding rides context.platform_prompt, and ai-gateway persists message verbatim) - the augmented rows were pre-platform_prompt-refactor legacy + probe artifacts, purged 2026-09-02. This gate keeps it raw: DB has no transcript carrying the RAG-block markers, AND neither client send-site concatenates a knowledge/memory block into the message. Two-layer (DB + static source), teeth: augmented-DB rows and a concatenated send-var both redden; skips cleanly if docker is down.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "critic-registry",
        "script":  "tools/validate_critic_registry.py",
        "args":    [],
        "label":   "CRITIC DEEPWALK anti-drift: the UI/UX extension's critique bank cannot overstate. critic_registry.json (480 rows, one per in-scope trajectory) is the SSOT behind the roadmap header's '-> CRITIC DEEPWALK <pct>%' pointer line; this gate holds it to the same discipline as the trajectory registry: R1 structure/status vocabulary; R2 honesty (a critiqued row carries graded dims or a clean_note, walked rows carry walked_at, findings carry dim/layer/severity/evidence/owner); R3 coverage (every in-scope trajectory exactly once, descoped ids absent); R4 dimension vocabulary (a finding on a dim the rubric spec does not define is instrument drift); R5 walkability (pages or an explicit no_ui_basis). Teeth: hollow-critique, invented-dim, dupe, silent-drop and descoped-present all redden in --self-test. Registered 2026-09-02 when the extension landed at root - the same change that taught momentum_stop_guard the critic queue (the star-x16 lesson: the target grew, the bind must grow WITH it).",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "one-clock-per-string",
        "script":  "tools/validate_one_clock_per_string.py",
        "args":    [],
        "label":   "Cluster 1 (critic deepwalk): every rendered timestamp keeps ONE clock, and the ops-home shift line is plant-anchored + labeled. Walked live 2026-09-02 at GMT+12: logbook's card read 'Sep 1, 2026 - 12:09 AM' for a save made at 20:09 Manila - whFmtDate pinned the DATE to Asia/Manila while toLocaleTimeString (no timeZone) floated the TIME on the viewer's clock: a pair no clock on earth shows, and any worker east of UTC logging after local midnight filed under yesterday. Meanwhile ops-home derived 'Morning Shift' from viewer-local hours while shift-brain used the plant clock - three answers to 'what shift is it' in one moment. Fixed: formatDate's both halves pin Asia/Manila (whFmtDate + its timeOnly variant); index's _dateStr derives wholly from Asia/Manila and carries '(PHT)' beside the figure (the asset-hub '13:00 PHT' precedent - a shift belongs to the plant, a greeting to the person). This gate reddens on the concatenation shape (pinned date + floating time) anywhere, and on index dropping the Manila derivation or the PHT label. Resurrection: the pre-fix shape matches in self-test.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "team-deeplink-survives",
        "script":  "tools/validate_team_deeplink_survives.py",
        "args":    [],
        "label":   "T29: a deep-linked team search survives init and keeps ALL-history scope. The alert->asset->'Fault history' chain hands off to logbook.html?q=<tag>&view=team; walked live 2026-09-02 (Pablo, PV-002) the supervisor-default init re-ran setViewMode('team') AFTER the URL handler had started searchTeam(), wiping the in-flight results back to the browse prompt - 36 real team rows invisible to the exact persona who walks the diagnostic chain (workers skip that branch, so only supervisors broke). Second half: loadTeamMembers() lands a 7-day date default ASYNCHRONOUSLY, so even a surviving hand-off got silently re-scoped to a week on the next search - 'has this failed before' is an all-history question, and the scoped-zero card's own copy admitted it ('Only that date range was searched - it is not proof the entry does not exist'). Both fixed: the default is view-guarded (_viewMode !== 'team') and the browse window applies only pre-search (!_teamSearched). Static shape-lock with resurrection teeth: the pre-fix unguarded default and an unconditional window both redden the self-test. Fix verified live: the same deep-link now renders 30 cards all-time.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "cold-page-teaches",
        "script":  "tools/validate_cold_page_teaches.py",
        "args":    [],
        "label":   "T175: every page introduces itself to a first-time reader. A brand-new account has no assets, no PMs, no history - every list on every page is empty, and an empty page has one job: say what it is FOR and what the first action is. Get it wrong and the reader concludes the product is broken or not for them, on the one visit they are guaranteed to make. WALKED 19 PAGES with a genuinely cold account (created, walked, deleted, residue re-counted at 0 across auth.users, worker_profiles, voice_journal_entries and hive_members - the CASCADE from erasure-path-intact doing exactly what it promises). Every page speaks; two bounce, and they bounce to hive.html, the right destination for someone with no hive rather than a dead end. THE ONE REAL GAP, fixed: analytics-report presented a fully-armed generator - period chips, audience toggle, 'Generate Report' - to a reader with no hive and therefore no data, stating no precondition. Its siblings already refuse that state by name, so it now carries the same gate in the same words ('Reports need a hive... Go to Hive'), hiding BOTH <main> and #ar-print-wrapper - the latter a deliberate sibling of <main> for the print rule, whose empty-state hint otherwise told a hive-less reader to 'Click Generate Report' directly beneath a gate saying Go to Hive. ★THREE INSTRUMENT ERRORS ON THE WAY, NONE BANKED: (1) the probe cleared wh_last_worker as well as the hive and 14 of 19 pages bounced to marketing - pure artifact, since signup SETS that key (index.html:3196) and the shared guard redirects on a missing WORKER NAME, not a missing hive; COLD means no hive, not no identity. (2) The threshold was 'chars < 200', which flagged four of the BEST cold states on the platform - hive.html at 134 ('Create a Hive | Join with Code'), asset-hub at 188, shift-brain at 137, integrations at 193 - pages that are short BECAUSE they are focused; brevity is not emptiness. (3) It read querySelector('main'), and innerText on a display:none element still returns raw text, so on the very page it had just gated it captured the HIDDEN generator and missed the VISIBLE gate outside <main>; it now reads body innerText, which respects visibility. It asserts only the objective - something rendered, nothing threw, the account cleaned up - and PRINTS each page's cold text, because an oracle that grades prose is an oracle that will be wrong about prose.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "waiting-is-spoken",
        "script":  "tools/validate_waiting_is_spoken.py",
        "args":    [],
        "label":   "T181: waiting must SAY it is waiting. A page that fetches its content leaves a gap between paint and data, and what fills that gap is the whole question - a skeleton or a 'Loading...' line says WAIT, while an empty region says THERE IS NOTHING HERE, and a reader acts on that: they leave, they re-tap, or they report missing data that arrives 200ms after they looked away. MEASURED LIVE across 8 signed-in pages with REST throttled 1.2s so the gap is real rather than a race usually lost: ZERO silent gaps - four pages fill it with skeletons (logbook, community, alert-hub, analytics), four with loading text (inventory, asset-hub, marketplace, pm-scheduler). ★IT ASSERTS THE FLOOR, NOT THE VOCABULARY, AND THE NARROWING IS DELIBERATE: the census called waiting 'not yet one product's language', which is true as an observation, but WHICH metaphor a page picks is a design judgement, and failing a page for choosing text over a skeleton would enforce taste across 24 pages and generate dozens of arguable findings. Whether the gap says ANYTHING is not a matter of taste, so the floor is gated and the idiom split is REPORTED - the design question stays visible without a machine deciding it. ★A CENSUS FINDING CORRECTED BY READING THE CODE: analytics appeared to mix metaphors (a skeleton and two spinners in one sample) and does not - the skeleton CONTAINS the spinner (space reserved, action animating inside) and the others mark in-flight generation, which is the declared system working. ★AND THE ORACLE WAS WRONG FIRST: the original silence test read 'during.chars < 120', and teeth-testing it by stripping inventory's Loading lines produced idiom=NOTHING and still PASSED, because the page rendered 389 characters of CHROME while the awaited region sat empty - an absolute threshold over the whole main region measures the furniture, not the gap. It now compares GROWTH, and with that correction the same teeth test fails and names the page.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "fab-anchor-stable",
        "script":  "tools/validate_fab_anchor_stable.py",
        "args":    [],
        "label":   "T184: the one control that must never move. The nav-hub FAB is on every page in the same corner and it is the platform's single piece of muscle memory - a worker's thumb goes there without looking. Every other inconsistency between 24 pages costs a moment of thought; this one costs a MISSED TAP, repeatedly, on the control whose whole job is getting you out of wherever you are. MEASURED AT 390: x = 303, 312 and 318 across eight pages, a 15px spread, while every page carried an identical '#wh-hub { right: 16px }' and an identical 56px FAB. ★THREE HYPOTHESES WERE WRONG BEFORE THE MEASUREMENT SETTLED IT, and that is the part worth keeping: 'a scrollbar is narrowing the viewport' (refuted - innerWidth minus clientWidth was 0 on every page), 'an ancestor transform is creating a containing block' (refuted - the hub is a child of BODY everywhere, no ancestor carried transform/filter/contain/will-change), 'inventory sets scrollbar-width: thin' (refuted - no such declaration). What settled it was a virgin 'fixed; right:0; width:0' probe reporting where the containing block's right edge ACTUALLY sits: 375 on community, 384 on inventory, 390 on logbook - while documentElement.clientWidth read 390 on all three, which is precisely why nothing in the DOM revealed it. THE CAUSE: a fixed right-anchored element is measured from the SCROLLPORT, and the reserved gutter narrows it whether or not a bar is drawn - so the spread was the CROSS-PRODUCT of two unrelated per-page decisions, 'html { scrollbar-gutter: stable }' (components.css:231, linked by some pages) and '::-webkit-scrollbar { width: 6px }' (declared inline by others): 15px reserved -> 303, 6px -> 312, none -> 318. Neither was ever made with the FAB in mind. THE FIX belongs at the altitude that owns the stack - nav-hub.js now injects both halves on every page, beside the reserve and lift rules already there for the same stated reason ('this is one shared bar, so it takes one shared reserve'), and BOTH halves are required because the gutter's width IS the scrollbar's width. Spread now 0 across all eight. ★y IS DELIBERATELY NOT ASSERTED: --wh-fab-lift raises the stack on pages with a fixed bottom element (pm-scheduler rides 65px higher), which is collision avoidance, not drift - forcing it down would put the hub on top of that page's own bottom bar. Also asserts zero pageerrors, since the rule now ships from a file on all 24 pages. RESURRECTION PROVEN: with the injection disabled the gate reports exactly the original 303/312/318 spread, and green on HEAD.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "composer-caps-visible",
        "script":  "tools/validate_composer_caps_visible.py",
        "args":    [],
        "label":   "T168: a cap must announce itself. A maxlength with NO counter is a silent wall - the textarea simply stops accepting keystrokes, and the person typing does not read that as 'you reached 1000 characters', they read it as a broken keyboard, and they lose the thought they were part-way through writing. The guard is right; the silence is the defect, and that is the whole of T168 - guards that protect without insulting the person they stop. FOUND ON community.html, which had built the right pattern and applied it UNEVENLY: the post composer (2000) and the report box (500) each carried a live 'n / max' counter, and the reply box (1000) carried none - one page, three composers, two of them honest. Added the counter in the page's own idiom, wired its input listener, and reset it at BOTH clear sites (opening a thread, and after a reply posts), because a counter still showing the last message's length is a STALE reading and worse than no counter at all. Live-verified: all three now read '37 / cap' while typing and return to '0 / cap' when cleared, 0 pageerrors. ★THE STATIC SCAN THAT FOUND IT WAS WRONG TWICE FIRST, AND THAT IS WHY THE PROVER IS LIVE: inferring the counter's id from the field's id ('<id>-char-count') reported #post-content as uncounted, when its counter is #post-char-count - the convention is not a convention. A counter is whatever visible element MOVES WHEN YOU TYPE, so the only honest oracle types into the field and watches. Teeth proven by disabling the new counter's id: flagged 'reply-content: no counter', exit 1.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "compliance-claim-honesty",
        "script":  "tools/validate_compliance_claim_honesty.py",
        "args":    [],
        "label":   "T171: what an ISO-minded evaluator would check. Swept all 117 public pages for certification and compliance language, and the headline result is good AND worth stating precisely, because 'we claim no certifications' is easy to assert and had never been verified: there is NO WorkHive self-certification anywhere - no SOC 2, no ISO 27001, no 'GDPR-compliant', no HIPAA, no PCI-DSS, no bank-grade or military-grade security. All 30 raw hits were a standard the CUSTOMER is audited against (ISO 9001/14001/45001, DOLE OSHS), a PERSON's credential (TESDA NC II/III, CMRP, PSME/IIEE), or the skill matrix's own 'certified level' vocabulary. ★THE ONE REAL OVERCLAIM WAS A CATEGORY ERROR, NOT A BOAST: dole-iso-audit-trail-from-logbook said WorkHive logbook entries 'SATISFY Clause 10.2', the PM dashboard 'SATISFIES Clause 7.1.5', safety observations 'SATISFY Clause 9.1.1'. A clause is satisfied by an ORGANISATION's management system, evidenced by records - a tool holds the evidence, it cannot confer conformity - and the sentence reads as reassurance, so a plant could walk into a surveillance audit believing a feature had covered them. Rewritten to 'gives you the records Clause 10.2 asks you to retain' / 'evidences Clause 9.1.1', plus a callout stating the distinction outright: same value claimed, no false comfort. Live-verified rendering, 0 pageerrors. TWO ASSERTIONS: (clause) no feature may satisfy/meet/fulfil a NUMBERED clause - and note what is deliberately NOT caught, since 'the digital record satisfies three properties' and 'export patterns that satisfy auditors' both appear on that same page and are both fine, because a record CAN satisfy a property and an export CAN satisfy a sampling need; only a numbered clause as the object is the error; (selfcert) no page may claim a certification the platform lacks - zero today, guard-the-absence, because that is the single most tempting sentence to add to a sales page and the most expensive to be wrong about. Teeth proven on both.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "erasure-path-intact",
        "script":  "tools/validate_erasure_path_intact.py",
        "args":    [],
        "label":   "T164: the deletion the privacy policy promises must actually be possible. The policy tells a person to email admin@workhiveph.com with a 'Data Rights Request' and their personal data is purged within 30 days while compliance records are retained - a legitimate PDPA path, since a MANUAL route is fine if it is HONORED. Honoring it means the database must let the deletion happen and remove the right things. READ FROM THE LIVE FK GRAPH 2026-08-26 and the model is sound, matching the policy's own stated tension: CASCADE on 10 tables carries personal data away with the account (worker_profiles, voice_journal_entries, resume_documents/versions, worker_achievements, push_subscriptions, mfa_enrollments, the service_* trio) while SET NULL on 20 lets operational records SURVIVE with the identity detached (logbook, pm_completions, asset_nodes, inventory_transactions, community_posts, hive_members, skill_badges...) - the plant keeps its maintenance history, the person stops being in it. Nothing RESTRICTs. THREE ASSERTIONS, each guarding a way this quietly stops working: (blocked) no FK may be RESTRICT/NO ACTION, since one such migration fails every erasure request at the database with no symptom until someone exercises the right; (throws) ★no SET NULL may point at a NOT NULL column - it passes every graph inspection and then RAISES at DELETE time, which is why the gate reads attnotnull and not just confdeltype, because a graph that looks correct is not the same as a deletion that runs; (drifts) no existing table may change action, since CASCADE->SET NULL leaves personal data behind after a purge that reported success while SET NULL->CASCADE destroys the plant history the policy promised to retain - both directions are failures, so the baseline pins the exact mapping. New tables are ADMITTED with their action reported provided they pass blocked and throws, so the gate never stalls schema work. Teeth proven on ALL THREE: a throwaway RESTRICT table and a NOT-NULL SET NULL table (both flagged, then dropped), plus a flipped baseline entry.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "review-cadence-kept",
        "script":  "tools/validate_review_cadence_kept.py",
        "args":    [],
        "label":   "T162: a promise to re-check is a promise. The competitor-comparison pages are the most perishable content on the platform and they are written WELL - every competitor price dated ('published entry pricing, August 2026'), sourced ('vendor published pricing and feature pages, accessed August 2026'), caveated ('SaaS pricing moves, confirm current terms with the vendor'), and generous to the competition ('the strongest mobile-first product in the category'). Nothing in the comparisons themselves needed fixing. ★THE RISK IS HIDDEN INSIDE THAT QUALITY: each page also prints 'Pricing verified against vendor and directory listings on 5 August 2026' and 'This comparison is reviewed quarterly for pricing and feature accuracy'. Those are not descriptions, they are COMMITMENTS with a clock running - and left alone they decay into the worst kind of claim, an explicit confident promise of currency printed beside prices nobody has re-checked in a year. A reader trusts the page MORE because of that sentence, which is precisely what makes it expensive when it stops being true. THE ASSERTION: where a page states a verification date AND a review cadence, the date must still be inside the cadence it promised (quarterly = 92 days). Measured 2026-08-26: 3 pages, all verified 5 August 2026, 21 days old and comfortably inside. ★THIS GATE IS BUILT TO GO RED - around 5 November 2026 for these three, unless someone re-verifies. That is the point, not a defect: it converts a marketing sentence into a maintained obligation, and the fix when it fires is a real one (re-check the prices and move the date, or delete the commitment). NO GRACE PERIOD, deliberately - the page names its own cadence, so the cadence is the bar; the passing output prints the days remaining so it arrives as a countdown rather than a surprise. Teeth proven by ageing one date to 5 January: 233d against a 92d promise, exit 1.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "dated-claim-freshness",
        "script":  "tools/validate_dated_claim_freshness.py",
        "args":    [],
        "label":   "T161: content ages with supervision, not heroics. A statistic on a public page is a claim a reader - or an answer engine - will repeat, and the dangerous shape is not an OLD year but a year that silently became a LIE: 'as of 2024, the average plant...' was true when written and is quietly false now, because nobody re-reads a sentence that already shipped. ★THE INVENTORY CHANGED WHAT THIS GATE ASSERTS, and that is the lesson: a first pass across 114 public pages flagged 33 'claims 3+ years old' and nearly every one was a CITATION or a STANDARD EDITION - ISO 14224 (2016), CIBSE Guide D:2015, IEC 62305:2010, DENR DAO 2016-08, Nakajima 1988 for the origin of OEE. Those years are part of the source's IDENTITY; flagging them would have demanded the pages stop citing their standards precisely, the exact opposite of the goal, and 33 false findings on the way. SO IT ASSERTS THE SHAPE, NOT THE AGE. Two things rot and neither is a citation: (as-of) a present-tense claim pinned to a past year - 'as of 2024', 'currently ... 2023', 'latest figures 2022' - allowed only within the current year and the two before it, the window SLIDING with the clock so the gate keeps supervising without anyone re-tuning it; and (future) a statistic dated in a year that has not happened, always a typo or a template copy, which reads as authoritative until someone checks. BOTH ARE ZERO TODAY across 115 pages - this is a guard-the-absence gate in the no-clock-driven-push family, because a property that holds by CARE rather than by structure lapses in silence. Teeth proven on both shapes at once: an injected 'as of 2021' and a '2029 survey' -> 2 flagged, exit 1.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "structured-data-parity",
        "script":  "tools/validate_structured_data_parity.py",
        "args":    [],
        "label":   "T154: machine-readable truth = human-readable truth. JSON-LD is what an answer engine QUOTES when it cites this platform, and nobody proofreads it because nobody SEES it - so it drifts away from the page underneath and keeps being served, confidently, to the machines that summarize us. TWO CONTRACTS, both born from real drift. FAQ: every FAQPage Question name must appear in the page's visible text - the class T1 caught on the landing page, where the LD still told the provisioning-waitlist story after the page had moved to instant signup; an FAQ rich result PROMISES the page answers that question, so if the words are not there the promise is to content that does not exist (508 questions across 115 public pages, all present). NAME: an Article headline must still be recognisable as the page's own og:title/<title> - FOUND HERE, predictive-alert-thresholds-plants had been retitled to lead with 'ISO 10816 Vibration Thresholds and Predictive Alert Limits', a deliberate move toward the citable standard, while its LD headline kept the older vaguer 'Predictive Alert Thresholds for Industrial Plants', so an answer engine reading the structured data got the version WITHOUT the ISO anchor that makes the page worth citing (synced; 53 headlines now match). ★HowToStep NAMES ARE DELIBERATELY NOT GATED, AND THE EXCLUSION IS THE FINDING: a first pass flagged 31 'gaps' across 12 HowTo pages and every one was editorial paraphrase - the OEE guide's LD says 'Measure planned production time' where the page heads that section 'Step 1: Planned Production Time'. Same step, different register. A step name is a LABEL for a procedure; demanding substring identity would have shipped 31 false findings and taught everyone to ignore this gate. The FAQ question and the headline are different in kind - both are read VERBATIM by their consumers, so identity is the actual contract there. Teeth proven on BOTH halves: a fabricated question and a reverted headline, 2 flagged, exit 1.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "canonical-resolves",
        "script":  "tools/validate_canonical_resolves.py",
        "args":    [],
        "label":   "T158: a canonical must name a URL that EXISTS. rel=canonical is a claim - 'the real version of this page lives here' - and when the URL it names does not exist, the page tells every crawler its authoritative copy is at nothing. FOUND AND FIXED 2026-08-26, VERIFIED AGAINST PRODUCTION rather than inferred: workhiveph.com/workhive/public-feed.html returned 404 while workhiveph.com/public-feed.html returned 200, and FIFTEEN pages shipped the dead form - a /workhive/ segment left over from an earlier deploy path, still being served long after the site moved to the domain root. ★WHY IT SURVIVED EVERY EXISTING CHECK: they all asked whether a canonical was PRESENT, and all 15 were present, well-formed, absolute and unique - and pointed at a 404. Presence is the easy half of a claim; this gate checks the half that costs something. ★THE ONE THAT MATTERED MOST was public-feed, the anonymous shop window and the only one of the 15 meant to be found by strangers: a shared link to it carried a signal saying the real page was elsewhere, at a URL that does not resolve. Now 145 on-site canonicals all name a page that exists. STATIC BY DESIGN - the path IS the defect, and a live HTTP check would make the gate depend on the network to state a fact about a string. TEETH PROVEN by re-breaking public-feed's canonical: 1 flagged, exit 1.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "public-feed-dated",
        "script":  "tools/validate_public_feed_dated.py",
        "args":    [],
        "label":   "T158: the public window does not pretend to be fresh. public-feed is the shop window - an anon visitor's first sight of whether anyone is actually here - and a community feed's worst failure is not being QUIET but being quiet while LOOKING live, because a reader who cannot date what they are reading assumes it is current. MEASURED 2026-08-26: 15 public posts, newest 42 DAYS old, ZERO published publicly in the last 30 days, and of 111 posts only 14% are public. On a seeded fixture those numbers say nothing about the product - but they are exactly the state in which a feed either tells the truth or flatters itself, and this one tells the truth: every card carries its date, so a reader judges freshness for themselves. THE ASSERTION IS THE DATE, NOT THE FRESHNESS: freshness is a community outcome no gate can honestly demand, so demanding it would produce a permanent red that teaches nothing; what IS enforceable is that the page never hides how fresh it is. ★WHY GATE SOMETHING THAT ALREADY PASSES: the timestamp is the smallest and most droppable element on a card, and a redesign that tightens the layout and loses it costs nothing visible while quietly converting an honest quiet feed into one that looks live - the single change that would turn this surface from truthful to misleading. TEETH PROVEN by replacing the date span with a literal: 15/15 cards undated, exit 1. It also refuses to pass on ZERO cards, since no undated cards over no cards is a failed load, not a clean bill.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "funnel-cls",
        "script":  "tools/validate_funnel_cls.py",
        "args":    [],
        "label":   "T160: the SEO funnel must not jump while you read it. CLS is where a page loses a searcher without erroring - text settles, the reader's eye is already moving, and the line they were on is somewhere else - and on the funnel it costs twice, because Core Web Vitals feed ranking, so the pages most dependent on search were the ones shifting. MEASURED, ISOLATED, THEN FIXED. The learn template shifted 0.384 CLS at 390, nearly 4x the 0.1 bar. \u2605THE RECORDED SUSPICION WAS WRONG AND THE MEASUREMENT SAID SO: the note blamed an element injected at ~1s, and a MutationObserver armed alongside the shift observer recorded NO insertion of any size. The timeline showed a 0.182 shift at 903ms and document.fonts.ready at 907ms; blocking the font stylesheet took CLS to 0.000 while blocking the Tailwind CDN only reached 0.156. The font swap was the whole cause. THE FIX was one parameter - display=swap to display=optional across 113 public pages: optional gives the font ~100ms then never swaps, so the page cannot jump, and measured on a real load it STILL renders Poppins. The only reader who now sees the fallback is on a genuinely slow first load, which is exactly the reader who previously got the jump instead. Verified 0.000 CLS across learn, tools and the landing page. \u2605IT ASSERTS THE PARAMETER, NOT A CLS NUMBER: a lab CLS reading on loopback is optimistic and noisy, and a gate that fails intermittently gets ignored - the parameter is deterministic and it IS the defect.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "tools-cta-honesty",
        "script":  "tools/validate_tools_cta_honesty.py",
        "args":    [],
        "label":   "T3/T153: the calculator pages state the account requirement. The 60 /tools/ pages are the top of the funnel - a searcher looking for 'OEE calculator' lands on one, and what it promises decides whether the next click is trust or a bounce. MEASURED 2026-08-26 and the human-facing story is HONEST: none of the 60 embeds a calculator (they are landing pages that explain a calculation and hand off) and every one says so plainly - 'free with a WorkHive account. Sign-up takes about 30 seconds.' ZERO still carry the 'no sign-up needed' claim T3 recorded, which linked into a gated page and was false twice over. The gate keeps a fixed claim fixed, because that wording lived in a TEMPLATE - it came back everywhere at once when it came back at all. \u2605A RECORDED FINDING IT DELIBERATELY DOES NOT ENFORCE: every one of the 60 declares JSON-LD @type SoftwareApplication with an Offer of price 0 and a url pointing at the LANDING page, so a search or answer engine reads 'there is a free web application here', follows the url, and finds a page about an application that lives somewhere else. The human CTA is honest; the MACHINE claim overclaims. Pointing that url at the app would make it true in one line, but rewriting the structured data of 60 pages changes how the platform is presented in search - an outward-facing decision nobody can quietly reverse - so it is written up for Ian rather than done. Teeth-proven by restoring the exact old wording.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "share-card-completeness",
        "script":  "tools/validate_share_card_completeness.py",
        "args":    [],
        "label":   "T156: a shared link should look intentional. T125 established what sharing here actually looks like - workers paste links into Viber and Messenger group chats - which makes the Open Graph card the first thing most people see of a page, often before the page itself. THE FINDING: 60 of ~116 public pages shipped NO og:image and no twitter:card, and they were all the /tools/ CALCULATORS - the top of the funnel, the pages most likely to be shared by a worker who just used one. Every one rendered as a bare grey link while the learn cluster's 53 articles rendered a proper card. public-feed had an image and a title but no og:description, so its card showed a headline, a picture, and whatever text the platform chose to guess. Fixed by reusing the SAME asset the learn cluster already uses rather than inventing a second one: two social images drift and one ends up stale. \u2605IT CHECKS THE IMAGE FILE EXISTS, not merely that the tag does - a card pointing at a missing PNG renders exactly like a card with no image, and the tag being present makes it look handled, which is the worst combination because nobody re-checks a field that is filled in. App pages are deliberately out of scope: a signed-in surface behind an auth wall is not shared to a group chat. Teeth-proven both ways - card removed RED, image pointed at a missing file RED.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "long-fetch-bounded",
        "script":  "tools/validate_long_fetch_bounded.py",
        "args":    [],
        "label":   "T146: a hang must END, not spin forever. A fetch to an AI or transcription edge function is the one a plant floor will hang - a dead spot, a cold function, a slow uplink - and without a bound there is no failure at all, just a control stuck in its working state saying nothing. That is the eternal-skeleton shape wearing a spinner, and it is worse than an error because an error at least tells someone what to do next. THE GATE EARNED ITS KEEP ON ITS FIRST RUN: written after finding voice-journal's transcribeBlob unbounded, it immediately found THREE MORE - report-sender's transcribe, voice-handler's transcribe, and analytics' ORCHESTRATOR, the longest-running call on the platform, where a supervisor waiting on a report could not tell 'still thinking' from 'never coming back'. All four now route through the platform's own fetchWithTimeout rather than a second hand-rolled controller: it already owns the AbortController, the transport-retry policy and the null-on-abort contract. \u2605THE BOUNDS ARE SIZED FOR THE SLOWEST LEGITIMATE SUCCESS, NOT A DEVELOPER'S PATIENCE - 45s for a long spoken entry over a weak uplink, 120s for a full AI analytics phase - because a timeout tuned short turns working software into a lottery for the plants furthest from the server. Deliberately scoped to slow-by-nature endpoints: a quick REST read is already covered by whQueryTimeout, and demanding a timeout on every call in the codebase would be noise. ★CORRECTED 2026-08-27 (T40): this gate accepted a bare AbortController or a bare `signal:` as proof of a bound — and those are a CANCEL BUTTON, somewhere for a person to press, not a clock that fires on its own. analytics-report carried both and was still unbounded: the 22-page retry sweep graded it SILENT, sitting on 'Compiling analytics across all 4 phases' forever under a read outage because nothing would ever abort it if the reader simply waited — and this gate passed that page every run. A real bound now requires a TIMER: fetchWithTimeout, AbortSignal.timeout, a setTimeout that calls abort, or a Promise.race against a timed promise. The controller may still sit beside those (it is how the abort is delivered) but can no longer stand in for them. A --selftest pins all five cases, the first being the pre-fix analytics-report source, which must be RED.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "learn-graph",
        "script":  "tools/validate_learn_graph.py",
        "args":    [],
        "label":   "T152: the teaching cluster is navigable in BOTH directions. 53 articles only teach as a cluster if a reader can move through them, and two different failures break that while being invisible to each other. AN ORPHAN is an article no hub link reaches: it exists, it is indexed, a searcher can land on it, and nobody browsing the library will ever find it - writing it was work that reaches nobody. A DEAD END is an article that links onward to nothing: a reader who finishes it has to go back and start again, which is where a session ends. Measured: 53 articles, 53 reachable from the hub, ZERO orphans, ZERO dead ends, and the hub links to nothing missing. The gate keeps it that way, because adding an article and forgetting the hub entry is a one-line omission nobody notices. \u2605IT ALSO CHECKS THE REVERSE OF AN ORPHAN - a hub link to an article that does not exist - because a library index offering a guide that 404s is worse than not listing it, and this cluster has been renamed before. \u2605AND ONWARD LINKS MUST REACH OTHER LEARN ARTICLES, not merely somewhere: a page whose only outbound link is a signup CTA is a dead end for a reader who came to learn, however many links it technically has. Teeth-proven by renaming one hub slug, which fired BOTH arms at once - the article became an orphan and the hub gained a promise it could not keep.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "answer-presence",
        "script":  "tools/validate_answer_presence.py",
        "args":    [],
        "label":   "T151: a searcher's question answered above the fold, swept across all 53 learn articles. Somebody arrives from a search result with a question the snippet promised this page answers; what they meet in the first screen decides whether they stay, and the failure mode is not a broken page but one that opens with throat-clearing and buries the answer four scrolls down. \u2605THE ORACLE IS ANSWER-PRESENCE, NOT META-VERBATIM - T151 learned that the hard way when an earlier pass checked whether the meta description's words appeared on the page and produced a false red, because a description SUMMARIZES rather than quotes. Four assertions per article: an H1 exists; substantive opening prose follows it; that opening carries a distinctive term from the title; and a freshness stamp is visible, because a searcher landing on undated maintenance guidance cannot tell if it is current. \u2605THREE INSTRUMENT CORRECTIONS BEFORE ANY VERDICT WAS TRUSTED, each found by READING the flags rather than acting on them: a {3,} filter silently dropped this platform's most important short terms (PM, AI, OEE) so an article opening 'Most Philippine plants run PMs' read as off-topic; an exact match called 'measurement' a miss for a title saying 'Measuring'; and scanning only <p> elements flagged the WorkHive-vs-MaintainX page, which opens with a textbook answer ('Choose MaintainX if... Choose WorkHive if...') written in a <div> of <strong> lines - the <p> tags it found were the byline and a repeated pricing disclaimer, so the gate read the furniture and missed the answer. It now reads VISIBLE TEXT, and allows one paragraph of runway, because enforcing sentence-one would legislate prose style rather than measure arrival. All 53 pass. Mutation-proven both ways: burying an answer behind preamble goes RED, and stripping a freshness stamp goes RED.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "hive-rename-reaches",
        "script":  "tools/validate_hive_rename_reaches.py",
        "args":    [],
        "label":   "T140: a rename must reach the OTHER devices. \u2605THE TRAJECTORY'S RECORDED PREMISE WAS WRONG IN BOTH HALVES, and this gate is the correction. Its basis read that hive rename 'does not exist - no UI, no update path (grep: zero rename/hive_name edit sites in hive.html)' and concluded that no stale-name propagation class could exist. hive.html DOES rename a hive, and a live test showed the consequence: after another device renamed the plant, the measured session kept showing the OLD name on its board and in its chrome, because every page trusts localStorage.wh_hive_name and nothing re-read hives.name - a cache written at join/switch time, so 'eventually' can mean weeks. A grep that misses one line turns a real defect into a recorded absence. FIXED CENTRALLY: whReconcileHiveName (utils.js) asks the server what the hive is called once per load and corrects the cache and the glass when they disagree, best-effort by construction (a failed check keeps the cached name, which is what the page would have shown anyway) and inside the board's allSettled so it can never stop a page loading. \u2605THE TEST'S SHAPE IS THE ONLY ONE THAT CAN SEE THIS: the rename happens OUTSIDE the measured session, straight into the database, exactly as another supervisor's browser would - renaming through the same page would prove only that a page can update itself. Three assertions, the third being the one that matters: cache corrected, title corrected, and the old name NOWHERE on the page, because chrome that fixes its title while a sidebar keeps the old name is still showing two truths. Resurrection: all three RED pre-fix. The hive's name is restored in a finally block and the restore is verified.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "approval-race",
        "script":  "tools/validate_approval_race.py",
        "args":    [],
        "label":   "T145: two supervisors, one pending item, ONE outcome. The sharpest concurrency case on this platform - both press Approve within the same second - and the walk found it already correct, needing no fix. This gate exists so it STAYS correct: the optimistic guard is one .eq('status','pending') away from being deleted by somebody tidying a query, and the failure is invisible in normal use because you only meet it when two people happen to act at once. \u2605A REAL RACE, NOT TWO AWAITED CALLS: awaiting the first update before firing the second proves nothing, since the second simply sees the finished state. Both are dispatched concurrently and settled together - the only shape that exercises a guard, and this codebase already carries a scar from a double-submit test that awaited its own clicks. Four assertions: exactly one update reports a changed row; the other reports ZERO rows (a no-op the UI can explain, not an error); the item ends approved once by one person; and the audit records ONE approval, because a log showing two approvals of one thing is a log nobody can use. \u2605IT PICKS ITS HIVE BY THE PROPERTY IT NEEDS: the first version pinned a fixture with ONE supervisor and SKIPped - a race test that cannot find two racers is not a pass, and a pinned hive would have quietly disabled this gate the day the fixture changed. Measured live: Christine Dizon won, Pablo Aguilar got zero rows, one DB outcome, one audit entry.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "oc-guard-speaks",
        "script":  "tools/validate_oc_guard_speaks.py",
        "args":    [],
        "label":   "T138: a concurrency guard that says nothing is a LOST EDIT. An optimistic-concurrency guard filters an UPDATE on the row's updated_at so a write loses if somebody else changed the record first - that is the easy half. The half that decides whether it HELPS is what happens next: if nothing checks that ZERO ROWS changed, the update silently does nothing, the form closes, and the person walks away believing they saved work that no longer exists. A guard that returns without speaking is WORSE than no guard, because it converts a visible conflict into an invisible loss. \u2605THE CENSUS CORRECTED THIS TRAJECTORY'S OWN RECORD: the basis read that logbook amends, community post edits and PM scope edits were 'presumed LAST-WRITE-WINS'. They are not. There are TEN hand-rolled guards across EIGHT pages (asset-hub, community, founder-console, integrations, logbook, platform-actions, pm-scheduler, project-manager x3) plus inventory through the central ocUpdate - eleven protected edit surfaces, not two - and every one of the ten detects the zero-row case AND tells the person. The gate does NOT demand migration to ocUpdate: that is ten rewrites of live edit paths for a refactor, and the property that protects a person is 'the guard SPEAKS', not 'the guard is shared'. The install-the-guard lesson still governs anything NEW - reach for ocUpdate first - and this exists so a hand-rolled one cannot ship silent. \u2605IT WOULD RATHER MISS THAN LIE: the window is the 40 lines after the filter, so a guard whose reporting lives further away reads as a miss, which is the safe direction for a gate whose other failure mode is a false accusation about working code. Teeth-proven with an injected silent guard.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "inventory-listing-handoff",
        "script":  "tools/validate_inventory_listing_handoff.py",
        "args":    [],
        "label":   "T104 / T30: the sell hop keeps its intent. inventory offers 'Sell surplus' only on rows holding 3x+ their minimum, with the reason in the control's own title, and hands off to marketplace.html?post=1&from_inventory=<id>; marketplace RE-FETCHES the row server-side, so the prefill is authoritative and cannot be spoofed through the URL - a person can only list from inventory their own session may read. VERIFIED-GOOD then locked: a handoff that drops what the system already knows makes the seller retype it, which is the same intent-loss as a wall that forgets where you were going, landing on the least-motivated user in the funnel - someone listing a spare part for the first time. Measured: title, part number, quantity (into the description), category classification and the source id all arrive. \u2605THE MOST INTERESTING ASSERTION IS AN ABSENCE: the PRICE is deliberately NOT prefilled and the gate holds it that way, because what a plant PAID is not what it should ASK, and a guessed price is the one field a seller must own - prefilling it would be the system making a commercial decision on their behalf. The mutation test confirmed that arm is independent: with the prefill disabled every other assertion flipped RED while priceLeftToTheSeller stayed green. The prover picks a real surplus row by the same 3x rule the button uses rather than hardcoding an id, so a reseed cannot turn it into a false red, and it SKIPs cleanly when the fixture holds no surplus row.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "listing-preview",
        "script":  "tools/validate_listing_preview.py",
        "args":    [],
        "label":   "T100: see it as a buyer before it is a commitment. The walk found the AI listing assist SOUND - it names its own inputs and invented no specs from a bare title - and recorded exactly ONE gap: a seller could not see their listing AS A BUYER WILL before publishing. Posting is a commitment: it spends a credit hold, it goes to moderation, and it is the first thing a stranger judges the seller by, so J3's bar applies. \u2605THIS GATE EXISTS BECAUSE THE FIX FAILED TWICE, SILENTLY, in ways only a CLICK could find. (1) The handler called getElementById at script-parse time and returned early when the composer was not yet in the DOM - the button rendered perfectly and did NOTHING: declared, never wired. (2) Rewritten as a delegated listener, it was inserted INSIDE the submit handler's function body, so it only registered once a seller pressed Post - the one moment a preview is useless. BOTH loaded with ZERO console errors, and a gate that merely checked the button EXISTS would have been green through both. Four assertions, all behavioural: it opens; it shows the draft the seller typed; a blank price reads 'Negotiable' while a filled one renders through whFmtPeso (a preview that formats money its own way is showing the seller something the buyer will not see); and it closes again, because a preview that traps you is a modal. Resurrection BOTH ways: RED with no preview control at all, RED again with the listener deliberately un-wired.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "no-probe-residue",
        "script":  "tools/validate_no_probe_residue.py",
        "args":    [],
        "label":   "T136: demo and probe content must never read as real. Every walk in this program writes marked rows and deletes them, but that discipline is per-walk and per-author - it holds exactly as well as the last person's memory. A BROAD sweep on 2026-08-26 found 245 marker rows surviving earlier sessions: 3 in VOICE_JOURNAL_ENTRIES (probe transcripts AND the AI's replies to them, in the platform's most private surface, inside a worker's own journal); 1 in LOGBOOK.knowledge ('probe: checked, within tolerance' in a real-looking entry - T136's earlier pass had found and removed the pm_completion twin of this and missed the logbook copy: one probe, two homes); and 241 in HIVE_AUDIT_LOG. \u2605THE AUDIT ROWS ARE THE LESSON: they were MINE, from the same day. My pending-submission prover created probe assets and deleted them, and the audit trigger honestly recorded every deletion - TRUE rows describing objects that were never real, in a compliance-class table the retention gate correctly forbids any scheduled job from purging. A probe that touches an AUDITED table leaves residue its own cleanup cannot reach, because the residue lands in a DIFFERENT table than the one it wrote to; removing it needed a deliberate scoped delete, never a cron. AND THE GATE EARNED ITS KEEP ON ITS FIRST RUN: after a targeted WH-T108B cleanup it still found 34 rows under a different marker (WH-EFFECT-PROBE, dating back to 2026-08-18) that the scoped delete had missed. Scoped to user-facing tables and deliberately NOT the seeder's fixtures - demo DATA is legitimate, and a gate that could not tell seeded plant data from probe residue would fire forever.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "page-contrast",
        "script":  "tools/validate_page_contrast.py",
        "args":    [],
        "label":   "T116: the contrast zero that IS certified, and the one page where it is not. tools/prove_page_contrast.mjs is a mature TWO-ORACLE instrument - axe for WCAG 2.x, live-state-runner's APCA maths for this platform's dark surfaces - and it had NO GATE, so a contrast regression was invisible to the board. \u2605READING THE INCOMPLETE SET IS WHY THIS EXISTS: axe reports 0 violations across 26 pages, and alone that is a FALSE 100, because it also reports 1,534 INCOMPLETE - 'element's background color could not be determined' - since axe cannot see behind composited alpha or a gradient, which is most of this platform. A zero over an abstention that large means nothing by itself. WHAT MAKES IT REAL is the second oracle: APCA measured 3,007 text nodes with 0 failing, 0 inconclusive and NO truncation, and on 23 of 24 pages its denominator is LARGER than axe's abstention set - so the nodes axe declined to judge have a verdict from an instrument built to composite alpha, average gradient stops and resolve background-clip:text glyphs. \u2605THE EXCEPTION IS NAMED RATHER THAN ROUNDED AWAY: on assistant, axe left 18 incomplete while APCA measured 15. The counts are in DIFFERENT UNITS (elements vs text nodes) so it is not proof of three unjudged nodes - it is the one page where the covering argument is not clearly safe, recorded as a residual instead of folded into the green. Asserts violations 0, APCA failing 0, inconclusive 0, no truncation (a cap must never hide under '0 failing'), and refuses to grade a --teeth run, which plants deliberately failing nodes into the same report file.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "width-utilisation",
        "script":  "tools/validate_width_utilisation.py",
        "args":    [],
        "label":   "T114 + T115: the widths between phone and desk. T114 asks whether 768 - the awkward middle nobody designs for - is coherent; T115 recorded a nit at 1920 of prose lines running 104-131ch, well past the 45-90ch of a comfortable reading measure. \u2605THE RECORDED NIT WAS THE INSTRUMENT, and that is this gate's main finding: every one of those long 'prose lines' was a SOURCE CHIP - 'Live - refreshed on load - Based on...', 'Saved snapshot, computed 5h ago - ...' - the platform's freshness metadata, SCANNED in a glance rather than read left-to-right. Capping those at a reading measure would WRAP them, turning a one-line label into three, which is worse than what it was meant to fix. With chips excluded the longest genuine prose line is 59-93ch: inside the comfortable range, logbook@768 marginally over at 93. There is no line-length defect, and a future session should not go hunting one. WHAT THE FILL NUMBERS SAY: at 768 the pages fill 87.5-100% of the viewport, so the middle width is used rather than treated as a big phone; at 1920 index, analytics and pm-scheduler run full width while hive (35%) and logbook (57%) cap their column - a legitimate readability choice, recorded so it stays visible. The ratchet holds longest-prose-line per page+width; FILL is reported but NOT gated, because 'uses the width' is a design judgement and a gate demanding a fill percentage would be enforcing an opinion. The ch measure is taken from each block's OWN font metrics via canvas, not guessed from a max-width.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "money-formatting",
        "script":  "tools/validate_money_formatting.py",
        "args":    [],
        "label":   "T133: absent is not zero, especially in pesos. whFmtPeso exists for one recorded reason - Number(null) is 0 and finite, so a null amount slips through as a confident 0.00. A filed top-up of PHP300 once rendered as PHP0.00 and a ledger line of unknown value rendered as +0, a sentence saying the entry moved nothing. The helper prints a GAP for absent and still prints 0 for a real zero, because a person can act on 'we do not know' and cannot act on a wrong number. THE FINDINGS: founder-console - the owner's own cockpit - had ZERO adoption and a formatter that was literally Number(n || 0), the bug itself, while the same file carried a note saying a money number frozen at zero 'does not read as by design, it reads as a feed that died'; its GCash top-up row had no null guard at all. marketplace-seller's earnings formatter used Number(n), so an absent figure printed NaN on the dashboard of the person being paid. marketplace's service picker guarded <= 0, which catches null but NOT undefined - Number(undefined) is NaN and NaN <= 0 is FALSE - so an absent rate fell through as NaN to a buyer. All routed through the helper. \u2605THE GATE'S OWN FIRST RUN FLAGGED FOUR FALSE POSITIVES and they were NOT acted on: a ternary spans lines, so the GUARDED FALLBACK half of `whFmtPeso(n) : '+Number(n||0)` looked hand-rolled because the helper sat on the line above. 'Fixing' those would have damaged correct code whose fallback exists so the page still renders when utils.js has not loaded. It now reads a window and honours an explicit null branch. Scoped to money surfaces by design - a gate policing every peso sign would catch option labels and prose and be switched off.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "counted-noun-ratchet",
        "script":  "tools/validate_counted_noun_ratchet.py",
        "args":    [],
        "label":   "T131: software that cannot count to one. '1 entries found', '1 assets', '1 parts' - individually trivial, collectively the thing that makes a product feel unfinished, and n=1 is not an edge case here: it is what a filtered list shows the moment a search gets specific. WHAT IS COUNTED is a count interpolated straight into a plural noun with NO singular branch nearby, and the count must be one that can GENUINELY be 1 - a .length, a *Count, a *_count, a *Total. Deliberately NOT cases like ${period} days, which is 30/60/90 by construction and never reads '1 days': the first pass of this measurement counted 82 that way, and a census padded with cases that cannot occur produces a number nobody trusts and a gate everyone learns to ignore. The honest figure was 30. ZERO IS NOT THE GOAL and this ratchet must not be read as demanding it - the sites span 13 files, mostly secondary and admin surfaces, and rewriting all of them in one sweep would be a large unreviewed change to render paths for a grammar nit. What was FIXED is the one a worker meets daily: the counter under the logbook's search box, which said '1 entries found' the moment a search narrowed to a single result. Baseline 27 after that fix; growth is the failure. The idiom to use is the platform's own, already present in several files - ${n} asset${n === 1 ? '' : 's'} - no helper and no new dependency. Teeth-proven by injecting one new offender: RED, then removed.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "idle-network",
        "script":  "tools/validate_idle_network.py",
        "args":    [],
        "label":   "T118: what a page costs while NOBODY IS TOUCHING IT. A worker at 8% battery at the end of a shift and a tablet left running on a workshop wall are the same measurement. index had been measured once; the rest of the roster never had, and a polling loop is exactly the cost nobody notices while developing on mains power. MEASURED: eight pages, 8s to settle then 75s of watching with no interaction. Seven are completely silent; alert-hub makes ~11 requests per 75s from setInterval(loadAll, 60000) - correct for a board somebody is watching, and roughly 500 requests an hour if left on a wall. \u2605THE WINDOW MUST OUTLAST THE SLOWEST POLL OR A ZERO MEANS NOTHING: the first run watched for 45 SECONDS and reported ZERO on all eight pages, including alert-hub, whose 60s poll simply never fired inside the window - the cleanest-looking result in the sweep was the busiest page. \u2605AND THE PAUSE IS PROVEN, NOT TRUSTED: what makes a 60s foreground poll defensible rather than a battery leak is that it stops when nobody is looking, so the probe HIDES THE TAB and asserts the requests stop - measured, 0 requests in 75s hidden. A poll still running in a backgrounded tab would be invisible to every foreground measurement. \u2605WHAT IT CANNOT SEE, said plainly so a zero is not misread: WEBSOCKETS. Supabase realtime holds a socket open and its frames are not HTTP requests, so a zero here means 'no polling', never 'no activity' - and an already-open socket is genuinely cheaper than repeated polling, making this a scope note rather than a gap to close by inflating the number.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "composer-audience",
        "script":  "tools/validate_composer_audience.py",
        "args":    [],
        "label":   "T166: 'who sees this?' answered where it is WRITTEN. Every write surface makes a person decide how candid to be, and that decision happens at the composer - answering it on a welcome card, a help page or a toast after the fact is answering it too late or to the wrong person. THE FINDING: voice-journal.html had ZERO audience statements while being the platform's MOST private surface - voice_journal_entries carries auth.uid() = auth_uid on read, update AND delete, so nobody else in the hive can reach a single row. The only place a worker was ever told lived on hive.html's join-flow welcome card: seen once, on a different page, at a moment they were not writing anything personal. A privacy guarantee a person cannot see is one they cannot rely on. THE WORDING IS READ OFF THE RLS, NEVER FROM MEMORY, and this trajectory carries the scar - a welcome card written the same week claimed the Voice Journal was the ONLY worker-private surface while the policies said otherwise (skill_profiles, badges and exam_attempts are auth_uid-scoped too). So the gate checks BOTH ENDS: the claim on the page AND the policies in the database, failing if a page claims a privacy the policies do not give - a promise the database does not keep being worse than no promise at all. Deliberately a SHORT NAMED LIST of surfaces where a person commits words others might read, not every input on the platform: a gate demanding an audience label on search boxes and filters would be switched off within a week. Resurrection: RED on the pre-fix voice-journal.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "retention-matches-policy",
        "script":  "tools/validate_retention_matches_policy.py",
        "args":    [],
        "label":   "T137: a promise to KEEP is as checkable as a promise to delete. The privacy policy states that compliance records (DOLE OSHS, ISO audit logs) are retained 10 years for general records and 15 for incident investigations - and this trajectory has ALREADY caught the platform making a retention promise it did not keep, claiming a 90-day retention of voice recordings when the product stores no audio at all. Measured 2026-08-26 and this promise holds: five retention crons exist and every one purges a TECHNICAL or DERIVED table (achievement_xp_log, agent_memory, embedding_cache, gateway_audit_log, hive_route_calls); none touches a compliance-class record, and hive_audit_log holds 9,019 rows with nothing scheduled to remove them. WHY IT NEEDS A GATE AND NOT A NOTE: that agreement is currently an accident of nobody having written the cron. Purging old logbook rows to save space is exactly what somebody adds on a slow afternoon, and the moment it lands a legal document about DOLE OSHS and ISO compliance becomes false - silently, with no user-visible symptom, because the records that would prove a plant's maintenance history in a regulatory dispute are precisely the ones nobody notices missing until they are needed. Asserts that no scheduled job deletes from logbook, pm_completions or hive_audit_log while the policy makes that promise; if the retention policy genuinely changes, the policy text and this list change together, which is the point of pairing them. It SKIPS rather than passes if the policy sentence disappears, so the gate cannot go quietly vacuous. Deliberately does NOT assert record AGES - nothing is 10 years old yet, so that check would pass forever without measuring anything. Teeth-proven by scheduling a logbook purge cron: RED, then removed.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "legal-claim-honesty",
        "script":  "tools/validate_legal_claim_honesty.py",
        "args":    [],
        "label":   "T172: the terms may not promise what the product lacks. terms-of-service said, in two places, 'You may export your data at any time and delete your account' and 'You may stop using WorkHive and delete your account at any time'. NEITHER SURFACE EXISTS - there is no export-my-data control (T77) and no account-deletion door (T164). A marketing page overclaiming is bad; a TERMS page overclaiming is a promise at legal weight, made to a Philippine user exercising a PDPA right, and it is the document they would point to. THE FIX WAS NOT TO DELETE THE RIGHT: the right is real and the privacy policy already documents how it is honoured - email admin@workhiveph.com with the subject 'Data Rights Request'. The terms now say exactly that, and say plainly it is handled by hand rather than through a button. Same right, true sentence. Both the HTML and its .md twin were corrected, because a promise surviving in either copy is a promise still unkept. THE ASSERTION: no legal surface may claim a SELF-SERVE capability the product does not have - and each banned phrasing is PAIRED WITH THE CONTROL that would make it true, so the day somebody ships account deletion the claim becomes allowed by the same gate that forbids it now. NOT A PROSE LINT: it checks specific self-serve promises against specific missing controls, because a gate that graded legal writing would produce false reds forever and teach everyone to ignore it. Resurrection: RED on all three pre-fix claims.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "name-keyed-aggregates",
        "script":  "tools/validate_name_keyed_aggregates.py",
        "args":    [],
        "label":   "T127: the day two machines share a name. SEVEN reliability functions group logbook rows by the machine NAME and none uses asset_node_id - get_mtbf_by_machine, get_mttr_by_machine, get_failure_frequency, get_downtime_pareto, get_repeat_failures, get_oee_by_machine, compute_anomaly_signals. Names are not unique: this database already holds 24 duplicate asset names inside a single hive - Caterpillar 3516B twice, Cummins QSK60-G4 twice - which is not a data-entry mistake but the ordinary reality of a plant that bought two identical generators. WHY THE NUMBERS ARE RIGHT TODAY IS LUCK, NOT DESIGN, and it was measured: ZERO machine strings map to more than one asset_node_id and ZERO duplicated names appear in the logbook at all, because nobody has yet logged a repair against the SECOND Caterpillar. The moment somebody does, its failures merge into the first one's MTBF, its downtime joins the first one's Pareto bar, and its anomalies are scored against a history that is not its own - silently, inside numbers used to decide maintenance strategy for a machine that might be about to fail. THIS GATE DOES NOT RE-KEY THOSE FUNCTIONS: that is a real design change with blast radius across every analytics surface and deserves to be decided rather than slipped in. It makes the failure LOUD instead of silent, failing the moment a machine string maps to more than one asset OR a duplicated asset name appears as a logbook machine. Teeth-proven by staging the exact future - a repair logged on the second duplicate-named asset - which turned it RED, then removing it. A CLEAN RUN IS NOT AN ALL-CLEAR: it means the collision has not happened yet. The 98% of logbook rows already carrying asset_node_id are what would make the re-key cheap when it is decided.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "supervisor-hygiene",
        "script":  "tools/validate_supervisor_hygiene.py",
        "args":    [],
        "label":   "T190/T56: authority must not outlive membership, and a hive must not lose it. Two invariants, opposite failures, both about who can approve things in a plant. (outlived) A member kicked or deactivated must not still carry role 'supervisor' - authority surviving departure is the quiet kind of permission rot, because nothing changes on screen until the day that row is consulted for an approval, and team churn in a plant is continuous. (orphaned) Every hive must retain at least ONE active supervisor - without one nobody can approve an asset, clear a report or promote anyone, so the work does not stop loudly, it just quietly cannot be completed, and the remedy is the exact thing that is missing. T56 named this the last-supervisor case, and hive.html's transfer-then-leave flow exists so it cannot happen. MEASURED: 3 hives, 4 active supervisors, ZERO departed members holding the role, ZERO hives without one. ★IT READS STATE, NOT CODE, and that is the point: hive.html's guard is a claim about a PATH, while this is a claim about the WORLD - whatever combination of kicks, role changes, transfers and direct writes happened, the invariant either holds now or it does not. ★ON A SEEDED FIXTURE THIS IS A WEAK SIGNAL BY ITSELF and that is worth saying: 3 hives is not a stress test, so its value is as a standing invariant that fires the first time a churn path breaks it, not as proof the churn paths are correct today. Teeth proven by kicking a supervisor (caught by name) and restoring; fixture re-counted unchanged.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "migrations-additive",
        "script":  "tools/validate_migrations_are_additive.py",
        "args":    [],
        "label":   "T199: a migration must not break the page already open. Deploys happen while people are working - a worker who opened the logbook at 13:58 and presses Save at 14:00 is running the OLD page JavaScript against the NEW schema, and whether that write lands is decided entirely by whether the migration was ADDITIVE. Adding a nullable column is invisible to the open page; dropping one, renaming one, or making one NOT NULL without a default is not - the in-flight write fails at the worst possible moment, after the person has typed everything. MEASURED across 561 migrations: only NINE non-additive operations exist in the whole history, every one a deliberate named cleanup from May-June 2026 (the Phase 5b/5c retirement of the old assets model, four drop_phantom_columns_* migrations removing columns that were never real, one NOT NULL to make a benchmarks upsert key work, and the Stripe removal), and NONE since 2026-06-30. ★IT IS AN ALLOWLIST, NOT A COUNT: those nine are grandfathered BY NAME with their reason, so the gate still fires on a new non-additive migration even while an old one is deleted - a count would silently absorb that. ★WHAT IT DOES NOT CLAIM: it does not prove a deploy is safe, since an additive migration can still break an open page through a changed CHECK constraint or a trigger, and the deploy-window question (PHT night rather than 2pm on a workday) is a runbook decision, not a schema property. It catches the one class objectively readable from the SQL. Teeth proven with an injected DROP COLUMN.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "absence-guard-reachable",
        "script":  "tools/validate_absence_guard_reachable.py",
        "args":    [],
        "label":   "T198: an 'it could not be computed' branch must be REACHABLE. A page that distinguishes 'computed, and the answer is nothing' from 'we could not compute it' has done the hard part of honest degradation - so the failure this catches is subtler and worse than not having the branch: the branch EXISTS, somebody wrote it deliberately, and the fallback hands it a value that can never trigger it. THE INSTANCE THAT PROVED THE CLASS: project-manager's renderCpm() guards `if (!cp)` and prints 'The critical path could not be computed right now', while its client fallback returned critical_path: { item_ids: [], total_days: 0, slack_per_item: {} } - AN EMPTY OBJECT IS TRUTHY, so the guard never fired on the path it was written for, and a shutdown project whose real schedule is '12d, 7 of 7 items on the critical path' rendered as 'CRITICAL PATH 0d - 0 of 7 items' with a full Gantt beneath it. Fixed with critical_path: null. THE ASSERTION: where a consumer's if (!x) branch says something could not be computed, no producer in that file may assign x an empty-but-truthy value - measured at 11 such guards platform-wide and ZERO defeated, so the CLASS is extinct rather than the one instance being patched. ★AN EMPTY ARRAY AS A PLAIN DEFAULT IS NOT THIS: parts: [], rows: [], work: [] are correct ways to say 'none', and a first sweep flagged 33 of them; what makes it a defect is a consumer that DISTINGUISHES uncomputed from empty and a producer that collapses the distinction. ★AND THE DETECTOR FAILED ITS OWN RESURRECTION TEST FIRST - its producer pattern could not match a value containing nested braces, and the real defect contained slack_per_item: {}, so it reported 0 with the bug present. A detector that cannot find the bug it was written for is worse than none; it now allows one nesting level and is proven to go 1 -> 0 across the fix.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "edge-down-admits-it",
        "script":  "tools/validate_edge_down_admits_it.py",
        "args":    [],
        "label":   "T198: a dead edge fn must not become a confident answer. Edge functions fail independently of the database - a cold start times out, one is mid-deploy, a region hiccups - and the page keeps working, which is the point of the client fallback; the danger is the fallback filling the gap with a POSITIVE-LOOKING ZERO. ★MEASURED AND REAL: project-manager's clientRollup returned critical_path: { item_ids: [], total_days: 0, slack_per_item: {} } when project-progress was unreachable, and renderCpm() carries a PJ8 guard written for exactly this - if (!cp) prints 'The critical path could not be computed right now' - but AN EMPTY OBJECT IS TRUTHY, so the guard never fired on the path it was written for. With the function down, a shutdown project whose real schedule is '12d, 7 of 7 items on the critical path' rendered as 'CRITICAL PATH 0d - 0 of 7 items on critical path' with a full Gantt beneath it. Not a blank and not an error: a confident INVERSION, on the screen a supervisor uses to plan a plant outage, telling them nothing is critical for a project where everything is. Fixed by returning critical_path: null so the existing message speaks - every consumer already used optional chaining. ★THE EVM HALF OF THAT SAME FALLBACK HAD ITS MARKER (evm_reason: 'unavailable') AND THE CPM HALF WAS LEFT BEHIND - the sibling-fix pattern this codebase keeps meeting. Asserts BOTH directions: with the function forced to 503 the pane says it could not compute, and with it up the pane still renders a real schedule, because a gate that only checked the failure case would pass on a pane that is permanently broken. Resurrection-proven against the pre-fix empty object.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        # T147 (2026-08-28) — the runtime instrument this trajectory said it needed. Its static
        # sweep for "a paint before an awaited write" returned 78 functions, 67 supposedly without
        # rollback, opening with a colour utility and a label formatter — the true pattern needs a
        # LOCAL STATE MUTATION before the paint, which a regex cannot tell from a render call that
        # merely appears earlier. So this fails the write deliberately and watches the UI. Asserts
        # the paint IS optimistic FIRST, because "the heart is empty after a failed write" passes
        # just as happily on a heart that never filled — a pass has to be non-vacuous or it is a
        # gate that cannot go red. ★THE INJECTED FAILURE IS DELAYED 1.5s ON PURPOSE: a route handler
        # answers in microseconds, so the first version outran the paint→refusal→rollback cycle and
        # concluded "not optimistic" about a paint already made and taken back.
        "id":      "optimistic-ui-reconciles",
        "script":  "tools/prove_optimistic_ui_reconciles.mjs",
        "args":    [],
        "label":   "Optimistic UI reconciles (marketplace's watchlist heart fills before the server answers, and a refused write takes the paint back AND says why; the optimism itself is asserted so the check cannot pass vacuously)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        # 2026-08-28 — a comment opened INSIDE a tag is not a syntax error to a browser. It reads
        # the comment's words as BOGUS ATTRIBUTES on the element and carries on: nothing throws, the
        # console stays clean, and whatever attribute came after it may quietly vanish. ★THIS GATE
        # EXISTS BECAUSE THE MISTAKE HAPPENED TWICE IN ONE SESSION — once between pm-scheduler's
        # deferral-button attributes, once between alert-hub's data-rag-tile and data-rag-label,
        # the second time while fixing a defect in that very attribute. Both were caught by reading
        # the file back afterwards, which is luck dressed as discipline; a page-loads-clean smoke
        # cannot see it. Does not flag comments BETWEEN elements (the correct place), comments in
        # script/style, or a `<!--` inside a quoted attribute value, which is legal text. The tree
        # is clean at 242 files, so this is a floor rather than a ratchet; teeth-proven by
        # reproducing the exact mistake and watching it name the line.
        "id":      "no-comment-inside-tag",
        "script":  "tools/validate_no_comment_inside_tag.py",
        "args":    [],
        "label":   "No comment inside a tag (an HTML comment opened between an element's attributes is parsed as bogus attributes and silently drops what follows — the browser never errors)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        # T182 (2026-08-28) — WorkHive celebrates progression on TWO ladders that live in DIFFERENT
        # FILES: ACHIEVEMENT_TIERS in utils.js (11/26/51/76/91) and a round-number milestone in
        # achievements.html (every 25th level). Both raise the same full-screen modal, and neither
        # file can see the other's ladder — which is exactly how they drifted into adjacency. Every
        # milestone sat ONE LEVEL BELOW a tier boundary, so the three biggest moments in the whole
        # progression (bronze→silver, silver→gold, gold→platinum) each fired TWO full-screen modals
        # on consecutive level-ups: "Level 25!" then "Tier Up!". Nine modals where six belonged.
        # ★REGISTERED BECAUSE THE INVARIANT IS INVISIBLE FROM INSIDE EITHER FILE: moving a tier
        # `min` is a plausible balance change that can silently re-create a back-to-back pair in a
        # page the editor never opened. Teeth-proven three ways — RED on the pre-fix ladder naming
        # all three pairs, GREEN on HEAD, and a tier `min` moved in utils.js to confirm it reads the
        # shipped configuration rather than a copy of it. That last test is also what caught the
        # first fix suppressing only a tier-up AHEAD of the milestone and not one just behind.
        # T169 follow-up (2026-08-28) — `_headers` is PROD-ONLY behavior, and this file has now
        # produced FIVE incidents of the same shape: Permissions-Policy killing mic+camera, a CSP
        # predating GA4 so the shipped analytics never fired, script/frame-src lacking Turnstile so
        # enabling bot defense would have killed signup, and then — found by sweeping the class
        # instead of patching the instance — cdn.plot.ly, cdnjs and OpenFreeMap missing, leaving
        # the analytics charts, the PDF export and the marketplace map dead in production.
        # ★THE MAP IS WHY THE SECOND CHECK EXISTS: wh-map.js states its requirement in its OWN
        # header ("Prod CSP needs connect-src https://tiles.openfreemap.org + worker-src blob:").
        # It was written down by the person who knew it and never reached the file that ENFORCES
        # it. So the gate asserts both halves — every external <script src>/stylesheet host, and
        # every CSP requirement stated in a source comment. Teeth-proven by reverting the four
        # grants and watching it name all four, including that an ABSENT worker-src is a denial
        # (it inherits default-src) rather than a neutral omission. Self-test: 7 cases.
        # T55/T78 (2026-08-28) — LIVE reachability, because both fixes it guards were made
        # statically and static was not enough to know a person can get there. marketplace-seller
        # (a supplier's entire platform) had NO entry in nav-hub's TOOLS registry — not hidden,
        # ABSENT — so it was missing from the All Tools grid AND unfindable by search, which reads
        # that same registry; and search-overlay filtered `!t.hidden`, so Audit Log, AI Quality,
        # PH Intelligence and Project Report could not be reached by typing their own names from
        # anywhere on the platform. An audit trail nobody can navigate to is write-only.
        # ★ASSERTS REACHABILITY, NOT MARKUP: it opens the real hub, reads the links a person could
        # click, and drives the search with Ctrl+K (the documented shortcut) — so it would fail if
        # the grid never rendered or the overlay never opened, which a registry-string check would
        # not. Teeth-proven by removing the registry entry AND restoring the hidden filter, then
        # watching all three reachability checks go red with the counts that show why.
        "id":      "seller-dashboard-is-reachable",
        "script":  "tools/prove_seller_dashboard_is_reachable.mjs",
        "args":    [],
        "label":   "Seller dashboard + hidden pages are reachable (the All Tools grid shows a seller their own dashboard, and global search finds curated-out pages like the audit trail by name — driven live in a browser, not asserted from the registry)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "csp-covers-what-ships",
        "script":  "tools/validate_csp_covers_what_ships.py",
        "args":    [],
        "label":   "Prod headers cover what ships (Content-Security-Policy must permit every external script/stylesheet the pages load and every requirement written into the source; Permissions-Policy must permit every browser API they call — an empty allowlist like geolocation=() denies our own pages too. _headers has no local equivalent, so all of it fails only in production, silently)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "celebration-proportionality",
        "script":  "tools/validate_celebration_proportionality.py",
        "args":    [],
        "label":   "Celebration proportionality (the tier ladder and the round-number ladder live in different files and must not fire full-screen modals on consecutive level-ups — two celebrations in a row is one interruption, and it devalues the larger)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        # T112/T63 (2026-08-28) — the other half of automation-log-status. That gate asserts every
        # status literal is PERMITTED by its CHECK constraint, which answers "may this be stored"
        # and never "does anything downstream know what it means". cmms-push-completion wrote
        # external_sync.sync_status='failed' as the durable marker for a completion that never
        # reached the CMMS; the CHECK permits five values so a constraint-shaped gate passes it,
        # but v_external_sync_truth — the view whose whole job is classifying sync state — buckets
        # exactly three (is_active/is_deleted/is_error). The marker built to make a lost completion
        # visible landed in the one value that made it invisible to the truth view too, and no
        # surface ever showed it. Reads the LIVE view definition rather than a migration, so it
        # measures what the database actually classifies. Resurrection-proven: restoring 'failed'
        # names the orphan and its line. (The column pattern carries a word boundary — without it
        # `sync_status` matches the tail of `last_sync_status`, a different column on a different
        # table with its own vocabulary and its own reader.)
        "id":      "status-literals-are-classified",
        "script":  "tools/validate_status_literals_are_classified.py",
        "args":    [],
        "label":   "Status literals are classified (every status value the code WRITES must be one the consuming truth view classifies — a value permitted by a CHECK but bucketed by nothing is a value that means nothing)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        # T135 (2026-08-28) — the reversal that did the reverse of its job. The rollback matched
        # target rows by worker_name + created_at BETWEEN the audit row's stamp and +5min, and both
        # halves were wrong in opposite directions: the audit row is written AFTER the data rows
        # (measured 51ms for asset_nodes, 88ms for external_sync) so every imported row sits just
        # BEFORE the window and .gte() excludes it, while anything the same person creates in the
        # next five minutes is INSIDE it. Measured: two imported assets survived a rollback that
        # said "Import rolled back successfully", and a hand-made asset created 10s later was
        # destroyed by it. Work orders were worse still — logbook rows carry the SOURCE system's
        # created_at (often years old) and logbook has no external-id column at all, so they could
        # never be matched. Now matched by BATCH MEMBERSHIP via external_sync, bounded by the
        # batch's own rows_written. Keeps a BYSTANDER row present throughout, because "the import
        # was removed" and "nothing else was" are two claims and the old code passed the first
        # while failing the second. Resurrection-proven: restoring the inverted window reddens
        # exactly the imported-rows-gone and row-count assertions. Writes, and cleans up after
        # itself — every row is tagged WH-PROBE-UNDOGATE and the run asserts none survive.
        "id":      "import-undo-removes-the-import",
        "script":  "tools/prove_import_undo_removes_the_import.mjs",
        "args":    [],
        "label":   "Import undo removes the import (undoing a batch deletes the rows THAT batch created and leaves an unrelated row the same person made minutes later untouched; the toast states the count actually removed)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        # T135 (2026-08-28) — the import's consent surface. Step-4 warned when a WORKHIVE FIELD had
        # no source column and never when a SOURCE COLUMN had no WorkHive field: the first protects
        # the schema, the second protects the user's data, and carrying data across is the entire
        # point of a legacy import. Measured on the real wizard — a 4-column Maximo export whose
        # EQTYPE header is in neither dictionary imported three assets that all landed iso_class
        # 'General', the type gone, under "3 rows · 3 ready to import · all valid" and an EMPTY
        # warnings box. The dictionary was right to decline to guess at a header it does not know;
        # the silence about dropping the column is the defect, and a five-year import has no other
        # moment to catch it. Asserts the clean case too (Maximo's real ASSETTYPE maps all four and
        # must raise NO notice) because a warning that fires on every import is furniture.
        # READ-ONLY: stops at the preview, never presses Import — verified leaving the DB at its
        # 96/16/14 baseline, which is why it is safe on every board.
        "id":      "import-names-dropped-columns",
        "script":  "tools/prove_import_names_dropped_columns.mjs",
        "args":    [],
        "label":   "Import names dropped columns (a source column mapped to nothing is NAMED in the preview with the step that can fix it, before any row is written; a fully-mapped file raises no notice)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        # T198 (2026-08-28) — the cold-vs-down half. status.html probes ten edge functions' /health
        # with an 8s budget and classified a TIMEOUT as 'down', while utils.js sets its own default
        # to 30s precisely because that is the documented cold-start allowance. So the likeliest
        # cause of a timeout here is a healthy function that has not run recently, and the page
        # whose whole job is saying whether the platform is up was publishing that as DOWN in red.
        # Measured before/after with one function hung and one genuinely 503: "8/10 healthy (80%) ·
        # 2 DOWN" with a raw "AbortError: signal is aborted without reason" on the card, became
        # "8/9 healthy (89%) · 1 DOWN · 1 did not answer in time (not counted either way)". Three
        # defects in one line — an unobserved outage asserted, a raw internal string on a trust
        # surface, and a denominator counting a surface the probe never read. Asserts all three
        # states together, because the cheapest way to make "no false DOWN" true is to stop saying
        # DOWN at all: a real 503 must still read DOWN in the same run.
        "id":      "status-cold-is-not-down",
        "script":  "tools/prove_status_cold_is_not_down.mjs",
        "args":    [],
        "label":   "Status: cold is not down (a hung /health probe reads as silence naming a likely cold start, never DOWN, and never leaks the raw AbortError; a real 503 still reads DOWN; the unanswered surface leaves BOTH the healthy numerator and the DOWN count and is named)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "media-fails-alone",
        "script":  "tools/validate_media_fails_alone.py",
        "args":    [],
        "label":   "T197: storage down must not take the write with it. Supabase Storage is a separate service that fails independently, so the question is whether the feature that ATTACHES a photo takes the whole submission down with it - a seller who has written a listing, priced it and typed a description should not lose that because a bucket is refusing writes. Same decoupling ethic T12 established for the voice journal, where an AI 429 was losing the worker's typed note: the SECONDARY enrichment must never destroy the PRIMARY work. MEASURED with every bucket write forced to 500 while the database stays up: the picker announces the real reason ('storage unavailable') instead of swallowing it, #post-image-url is CLEARED so the listing submits with no photo rather than a broken link, and the rest of the form is untouched and still submittable. ★IT INJECTS A STORAGE-ONLY OUTAGE, not a general one - failing everything proves nothing about isolation; the point is that ONE service is down and the others carry on, which is the only shape that distinguishes a decoupled feature from a lucky one. ★AND THE PROVER FAILED ITS OWN FIRST TEETH TEST, which is the reusable part: it checked toast.offsetHeight > 0, and the toast CONTAINER is always in the DOM, so an EMPTY toast counted as 'the user was told' and silencing the error still passed. It now requires the toast to contain actual words - a string is not an announcement until it has some. ★THE READ SIDE ADDED 2026-08-28: everything above is about ATTACHING a photo while storage is down; the other half is a photo already STORED and now unreachable, which five render sites across four marketplace pages emitted as a bare <img> with no error handling. Measured before assuming, and the roadmap's premise was wrong - it predicted 'layout wreckage', but a failed image holds its box exactly (218x164, naturalWidth 0, complete true) because the card's CSS sizes it, so nothing reflows. What the reader loses is not geometry but MEANING: a broken-image glyph that never says whether the listing has no photo, the photo is gone, or the page is broken. One capture-phase document listener in utils.js (error does not bubble but it DOES capture) now replaces an unreachable STORED photo with a labelled box that keeps the failed image's own dimensions and names which photo is missing via its alt text - scoped by src to /storage/v1/object/ so a decorative logo is never relabelled 'photo unavailable'. Teeth-proven both ways: neutering the fallback fails it, and removing the scope guard so decorative images get eaten fails it too.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "permission-in-context",
        "script":  "tools/validate_permission_asked_in_context.py",
        "args":    [],
        "label":   "T46/T105: ask for push when the worker WANTS it. A notification prompt at first paint is the fastest way to lose the permission permanently - the person has no reason to say yes yet, they say no, and the browser remembers that answer forever with no in-app way to undo it. There is no second chance, so WHEN the prompt fires matters more than how the feature is built. MEASURED - two call sites, both exemplary: marketplace-seller's svcEnablePush() runs from an onclick ('Turning on…') behind online/support/signed-in checks and refuses honestly when the answer is no ('Alerts stay off until you allow notifications'); voice-handler's _requestPushPerm() fires ONLY when the worker has VERBALLY opted in (_isPushOptInReply) and only while the state is still 'default', inside the open user-gesture window, so a prior denial is never re-prompted. TWO ASSERTIONS: every requestPermission() sits behind a permission-state check or an explicit user action, and no call site is reachable from a load-time path. ★A LOOKBACK WINDOW CANNOT SEE A GESTURE BINDING, and the first version proved it by calling a correctly-gated prompt 'bare': svcEnablePush IS bound to an onclick 350 lines earlier, far outside any sane window. The binding is a property of the enclosing FUNCTION, not of nearby text, so the gate now resolves the function name and asks whether the file wires it to a click anywhere - the same structural-over-proximity correction that keeps coming up. ★IT CHECKS THE ASK, NOT THE FEATURE: whether push is wired, which VAPID key, whether the message is good all belong elsewhere; this is only about the one moment that cannot be taken back. Teeth proven with an injected DOMContentLoaded prompt.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "partial-metric-declared",
        "script":  "tools/validate_partial_metric_declares_itself.py",
        "args":    [],
        "label":   "T47: a partial OEE must say so. OEE is a product of THREE factors - Availability x Performance x Quality - so computing two and calling the result 'OEE' is not a rounding difference, it is a different and HIGHER number, because the missing factor can only reduce it. A plant reporting 88% to management when the three-factor figure would be lower has not made a small error; it has made a claim it cannot defend the moment somebody asks how it was computed - and that reader is exactly who T47 is about. THE PLATFORM GETS THIS RIGHT and the gate keeps it right: analytics renders 'OEE (AVG, PARTIAL) 88% · Avg across 20 assets · WORLD CLASS · ISO 22400-2:2014 · Availability x Quality only. Add each asset's cycle time to include Performance.' - value, denominator, standard, WHICH FACTORS ARE IN IT, and what to do about the missing one, so a skeptic can hand-check the arithmetic and knows precisely what they are checking. THE ASSERTION: wherever a headline OEE figure renders, the same card names its factor basis - a partial marker plus the factors included, or a full three-factor statement; what it may never be is a bare 'OEE 88%'. ★IT DOES NOT ASSERT THE VALUE NOR DEMAND FULL OEE: whether this hive has cycle times is a data question, not a defect, and the honest response to missing data is to say what is missing, which is what the card does. The failure gated is SILENCE about the basis, not the partiality. Teeth proven by stripping the factor clause: namesFactors false, exit 1.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "embedding-source-enqueues",
        "script":  "tools/validate_embedding_source_enqueues.py",
        "args":    [],
        "label":   "T31: a registered knowledge source must actually enqueue. The RAG corpus is built by a mirror - embedding_registry names each source table and the knowledge table it feeds (logbook -> fault_knowledge, pm_completions -> pm_knowledge, skill_badges -> skill_knowledge), a trigger enqueues each new row into embedding_outbox, and embed-entry drains it. Registering a source WITHOUT its enqueue trigger is the quietest possible failure: the config says active, the knowledge table simply never grows, and nothing reports a problem because nothing errored. Asserts every active registry row has a matching enqueue trigger; measured 3 of 3. Teeth proven by registering community_replies with no trigger - which is exactly T31's own scenario, since accepted community answers are NOT currently indexed anywhere. ★WHAT IT DELIBERATELY DOES NOT ASSERT, AND WHY: the outbox holds 6,223 pending rows (logbook 4,617 of 8,058; pm_completions 1,606 of 1,611), which reads like a broken pipeline and is not - the drain is embed-entry called FROM THE BROWSER at write time, so rows inserted by the SEEDER, with no browser present, enqueue and sit (last drain 2026-08-20, when someone last worked through the UI). Gating on backlog depth would be a permanent red against a fixture, which is how a gate teaches people to ignore it. ★THE ARCHITECTURAL POINT IS RECORDED RATHER THAN GATED: the outbox has no drainer other than a client, so any write that does not pass through a browser - a seeder, a CSV import, the server-side PM mirror - enqueues and never drains, with no cron backstop. Whether to add a scheduled drain costs embedding calls and is therefore Ian's decision, not this gate's.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "write-failure-speaks",
        "script":  "tools/validate_write_failure_speaks.py",
        "args":    [],
        "label":   "T147: a failed write must not be silent. `if (!error) { paint }` with no else is the quietest bug this platform can ship - the write fails, the screen does not move, nothing is said, and from the user's side that is indistinguishable from a dead control. The natural next move is to press it again, which is how one silent refusal becomes repeated writes. FOUND ON community's toggleReaction, BOTH branches: tapping an emoji whose insert or delete failed left the count sitting still with no message. Fixed with the vocabulary the same page already owns (submitReply uses whWriteError), and the add path names the one case where doing nothing IS correct - a duplicate reaction (23505) says 'You have already reacted with this emoji' rather than reporting a failure the person cannot act on. VERIFIED LIVE with an injected 500: the count correctly does NOT move (no false optimistic paint) and the toast reads 'Your reaction was not saved. Tap again to retry.' MEASURED AFTER: 223 awaited db writes destructure `error` across the app pages and ZERO now guard with a bare `if (!error)` and no else; resurrection-proven, since removing the new else-branches puts the count back to 1. ★DELIBERATELY NARROW: it does not demand a particular message, nor that every write speak - plenty correctly stay quiet because the render that follows IS the receipt. It catches one specific shape, where the code explicitly took the error into its hands by destructuring it, branched on it, and then dropped it. That is never intentional.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "repaint-spares-typing",
        "script":  "tools/validate_repaint_spares_typing.py",
        "args":    [],
        "label":   "T142: a live update must not evict a typist. The recorded incident is precise - a 15-second poll rebuilt a region while somebody was typing in it and the repaint threw focus to <body>. The DRAFT survived, which is exactly what made it hard to notice: the text was still there, so the person kept typing into nothing. Realtime is the same hazard with worse timing, because the update arrives when a colleague acts rather than on a schedule anyone could learn. THE INVARIANT IS STRUCTURAL, NOT BEHAVIOURAL: a container a realtime handler REPLACES must not contain a typing surface. If the composer is not in the subtree being rebuilt, no repaint can evict it and no focus-restoration code is needed - which is better than restoring focus correctly, because restoration has to be right every single time. MEASURED, and both of T142's named subjects hold: community updates one card via _updateRenderedCard() for the ordinary case and rebuilds #feed-list ONLY when a pin moves (a genuine reorder), with both composers outside it - the post box on the page, the reply box inside #thread-overlay - confirmed in the LIVE DOM rather than from source offsets, after a character-offset reading had wrongly suggested nesting; and hive reloads targeted display cards (loadAdoptionCard, loadMaturityStairway) holding no inputs at all. ★IT ASSERTS CONTAINMENT, NOT FOCUS SURVIVAL: driving a real focus test needs the composer open and an update to land, and proves one path on one run, whereas containment is what makes every path safe and is checkable every run. ★AND IT READS THE POST-RENDER DOM, WHICH IS WHY THE FIRST TEETH TEST DID NOT FIRE - a textarea pasted into the empty #feed-list markup is wiped by the page's own render before the probe looks. The faithful test puts one in the CARD TEMPLATE so the render itself places it inside the rebuilt container, and the gate then reports 16 typing surfaces and fails.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "portable-record",
        "script":  "tools/validate_portable_record_is_portable.py",
        "args":    [],
        "label":   "T58: what the platform calls PORTABLE must survive leaving. WorkHive pitches a 'portable career record' to OFW-track engineers - people whose whole reason to use it is showing evidence to the NEXT employer - so portable is not a marketing adjective here, it is a testable property: does the worker still have it once they are no longer a member? MEASURED AS THE WORKER under RLS, inside a transaction that rolls back: logbook entries 571 while active -> 0 as 'kicked' -> 0 as 'deactivated'; skill badges 12 -> 12 -> 12. The portability story is HALF true and the halves are cleanly separable - badges and certifications are held against the WORKER and travel, while logbook entries are hive-scoped by the logbook_read policy (an ACTIVE membership is required) and stop being readable the moment membership ends. ★THAT SCOPING IS CORRECT TENANCY, NOT A BUG: a plant's maintenance records are the plant's, and letting a departed worker read them is precisely the cross-tenant leak this platform gates everywhere else. ★THE APP WAS ALREADY HONEST AND THE MARKETING WAS NOT: hive.html's leave confirm says 'Your past logbook entries stay in the hive's records' - exactly right, at the moment it matters - while two learn pages promised the opposite ('portable evidence that you can take with you wherever you go. Whether you're applying for a job in Saudi Arabia...', and a personal export available 'at any time'). Both now carry the CONDITION (export while still a member) instead of the promise, in the JSON-LD and the visible FAQ together; the gamification page's badge claim was left untouched because measurement says it is TRUE. THE ASSERTION: a worker with no active membership can still read their skill_badges - the half the corrected claims now rest on - so if a future policy makes badges hive-scoped too, the pitch becomes false again SILENTLY and this says so. ★HOW IT IS KNOWN TO DISCRIMINATE, since a policy cannot be flipped without a migration: the identical probe against logbook reports 571->0 while badges report 12->12, same worker, same transaction, same mechanism. Non-destructive (BEGIN/ROLLBACK), and it re-checks that memberships are still active before trusting its own reading, because a probe that modified the fixture would otherwise report a pass on damage.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "first-paint-throttled",
        "script":  "tools/validate_first_paint_under_throttle.py",
        "args":    [],
        "label":   "T37: something must be on screen fast. Plant wifi at its worst is the condition this platform is actually used in, and the first second decides whether a worker believes the app is working at all - PP1's bar is that SOMETHING paints quickly (chrome, a heading, a skeleton) rather than a white screen while data is fetched. It measures FIRST CONTENTFUL PAINT, not settle time, and the distinction is the point: a page that takes 6s to fill with data but paints its frame in 400ms feels alive, while one that paints nothing for 2s feels broken even if it finishes sooner. REST is throttled 1.5s so the measurement is about the SHELL - a page whose first paint waits on data is exactly the failure being sought. MEASURED: all 8 core signed-in pages paint in 208-456ms against an 1800ms budget. ★AND THE FIRST VERSION OF THIS PROVER MANUFACTURED A FINDING, which is why it is built the way it is: reusing one browser context and walking pages in sequence, analytics.html - the SIXTH in the run - reported 2220ms against 212-292ms for everything else, and 2220 sits suspiciously close to the 1500ms injected delay plus overhead. Measured ALONE it paints in 400ms unthrottled and 368ms THROTTLED; the 2220 was contention from the five pages before it, still holding delayed routes and competing for one renderer. The number described the HARNESS, not the page. So the prover closes each page before opening the next and - the part that matters - RE-MEASURES anything over budget in a FRESH context before reporting it, so only a page still slow BY ITSELF is a finding. A performance gate without that step eventually accuses a healthy page, and that accusation is expensive because someone then hunts a cause that is not there. Teeth proven end to end at a 120ms budget: 6 pages flagged, each confirmed alone.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "scroll-gate-at-open",
        "script":  "tools/validate_scroll_gate_evaluates_at_open.py",
        "args":    [],
        "label":   "T18: a read-gate must not depend on an event that may never fire. skillmatrix gates its exam behind 'read the lesson first' - the Take-exam button starts disabled and a scroll handler enables it once the reader reaches the bottom. Correct intent, and it had a hole: the button was enabled ONLY from inside that handler. ★A LESSON WHOSE CONTENT FITS THE BOX NEVER SCROLLS, so the scroll event never fires, checkScroll never runs, and the reader is left in front of a permanently disabled 'Take exam' beside a hint telling them to scroll something that does not scroll - with no way out of that screen but closing it. Latent only because every lesson written so far happens to overflow, and lesson bodies are AUTHORED CONTENT, so the first short one ships the trap to whoever writes it. FIXED by evaluating the gate once at open, after the scroll reset: self-correcting, because for a non-scrolling box scrollTop + clientHeight already exceeds scrollHeight - 80 so the check passes immediately, while a long lesson at scrollTop 0 still fails it and the gate holds. ★VERIFIED LIVE ON BOTH BRANCHES, since the risk of this fix is that it opens the gate for everyone: long content (scrollHeight 822 vs clientHeight 502) still reports disabled=true with the hint shown, short content (54 vs 54) reports disabled=false with the hint cleared, 0 pageerrors. ★AND THE GATE ITSELF FAILED ITS FIRST TEETH TEST: the invocation count matched `function checkScroll()` - the DECLARATION - so deleting the real call still left a count of 1 and it reported PASS on the exact defect it exists for. A negative lookbehind for 'function ' fixed it; the teeth test refusing to go red is the only reason that was caught. Narrow by design: this is one known control, and inferring 'enabled only by an event' across the platform would be guessing at intent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "lang-declared",
        "script":  "tools/validate_lang_declared.py",
        "args":    [],
        "label":   "T159: the page must say which language it is in. A screen reader picks its pronunciation rules from <html lang>, so Filipino text announced under lang='en' comes out as English phonemes applied to Tagalog - not accented, but unrecoverable. WCAG 3.1.1 asks for the page's language and 3.1.2 that a change of language be marked. ★THE HARD HALF IS THAT THIS PLATFORM IS BILINGUAL BY TOGGLE, NOT BY URL: one page, _t(en, fil) at every string, and a wh_lang preference - so the declaration is not a static fact set at build time, it has to FOLLOW the toggle, which is exactly the half a static audit misses. MEASURED and the discipline is clean: all 156 public pages declare <html lang> (none silent); utils.js:219 sets documentElement.lang from the stored locale on EVERY page load, so a reader who chose Filipino gets the right declaration even on pages with no translation engine of their own; and all three pages that WRITE wh_lang (index, analytics, analytics-report) also assign documentElement.lang in their toggle handler, so switching mid-session updates the declaration without a reload. TWO ASSERTIONS: every public page carries <html lang>, and any page that writes the wh_lang preference must also assign documentElement.lang - a toggle that changes the words while leaving the declaration behind is WORSE than no toggle, because the page then asserts a language it is not in. ★NO hreflang IS ASSERTED, deliberately rather than by omission: hreflang describes ALTERNATE URLs for the same content and this platform has none - the locale lives in localStorage, so /learn/x is one URL serving whichever language the reader chose, and demanding hreflang would be demanding a second site. Whether to publish a Filipino public corpus at all is T159's open posture question and Ian's to answer; this gate holds the part that is true under either answer. Teeth proven on both halves.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "no-drag-only",
        "script":  "tools/validate_no_drag_only.py",
        "args":    [],
        "label":   "T117 / WCAG 2.5.7: nothing may become reachable ONLY by dragging. A worker in gloves, a person with a tremor and anyone on a trackpad share one need - no function should require a dragging movement. The census found the platform CLEAN, and every gesture it has is harmless: nav-hub and companion-launcher let a person DRAG the floating button out of the way while opening it stays a TAP; session-timeout listens for touchstart as an activity SIGNAL, not a gesture requirement; wh-persona sets draggable=false, disabling dragging rather than requiring it. The one genuine keyboard trap in this family was already closed centrally - .wh-scroll-x regions scrolled by swipe were unreachable from a keyboard (axe scrollable-region-focusable, eight on project-report), fixed where the shared class lives rather than per call site. THE GATE FOUND A HIT ON ITS FIRST RUN and it was checked rather than assumed: integrations.html registers a drop handler for CMMS imports - and it is guarded by if (dropZone && fileInput), with a real labelled input type=file wired to the SAME handleFile on change. Drag-and-drop over a working file picker is exactly what 2.5.7 asks for, so it is justified by name with that reason recorded. A NEW gesture file fails - not because dragging is forbidden, but because 'is there a tap that does the same thing?' is a question somebody must answer out loud before it ships. Scoped to product surfaces: vendor bundles and the measurement tooling whose JOB is to find drag handlers are excluded, since a lens that greps for dragstart would otherwise fail a gate about dragstart. ★MERGED WITH T75 (2026-08-26), which asked the same question of dayplanner and found it has NO drag machinery at all - the walk's premise was wrong, there was no drag to alternate. That pass added the rule that now decides the FAB repositioners: a handler assigning only style.left/right/top/bottom MOVES a control and confers no capability, so it passes, while one that also mutates data (insert, splice, sort_order) is load-bearing and must offer a tap. Deciding by what the handler WRITES rather than by which file it lives in keeps the exemption from becoming a licence. ★AND A REAL MISTAKE WORTH RECORDING: driving T75 I wrote a second version of this gate straight over the T117 file, having read 'updated successfully' as 'created', then removed 102 unrelated gate registrations with an over-greedy cleanup regex. The duplicate-id guard caught the first, a HEAD diff caught and repaired the second. A duplicate id is a signal to MERGE two efforts, never to register the same name twice.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "auto-read-retry",
        "script":  "tools/validate_auto_read_retry.py",
        "args":    [],
        "label":   "T126: a failed READ recovers itself on reconnect. Reconnect handling here was write-only - offline queues drain on online, logbook syncs, banners repaint - but a failed READ sat behind a Retry button waiting for a tap, and manual assumes somebody is standing there. A wall-mounted alert board has nobody, so one network blip left a plant staring at a stale error for the rest of a shift; a phone in a pocket has nobody at the moment the signal returns. whListError now registers its retry for auto-recovery. SIX assertions, and three are RESTRAINT rather than recovery, because an auto-retry that misbehaves is worse than none - it hammers a backend that is already unwell: it re-runs on an online event (the defect), does NOT re-run on a second reconnect inside 3s, leaves alone any section that is no longer showing an error (a person who fixed it by hand must not have their view yanked), and DROPS an element removed from the DOM instead of retrying it forever - the listener-lifecycle leak this repo already gates for. NO TIMER, EVER: it fires on connectivity events only, because a polling retry would turn a backend outage into a self-inflicted load test. Resurrection goes RED on the two assertions that decide it (helperPresent, retriedOnReconnect); two others also read false pre-fix purely because they assert exactly-one-call against a world that made zero, and the gate says so rather than counting them as evidence. THE OTHER HALF OF T126 STAYS OPEN BY DESIGN: the hourly idle sign-out is CORRECT for shared plant tablets, so a kiosk exemption is a product decision for Ian, not a silent weakening of a protection that exists on purpose.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "interruption-steps",
        "script":  "tools/validate_interruption_steps.py",
        "args":    [],
        "label":   "T122: nothing lost, at EVERY step. The first walk injected an interruption at ONE step of the logbook wizard and found it green - but a multi-step form saves on its own schedule, so the step where the draft is NOT yet written is invisible to a probe that only tests the step where it is, and a phone rings whenever it rings. This fills the wizard progressively and at the end of each step injects the harshest realistic interruption, a FULL RELOAD (the OS killing a backgrounded tab), then asserts everything typed so far comes back. THE MECHANISM IS logbook OWN saveDraft/restoreDraft, NOT whAutoSaveDraft - T122 first write-up credited the shared helper, and logbook does not use it. That matters beyond tidiness: the shared helper got OWNER STAMPING in T121 so one worker draft cannot surface for the next person on a shared plant phone, and logbook - the platform richest personal-text form - missed that change entirely. Checked rather than assumed: logbook is safe anyway by a different route, DRAFT_KEY being wh_logbook_draft_ + WORKER_NAME, so the next person page computes a key it cannot read. Two mechanisms, one guarantee, worth knowing which is which before someone unifies them and removes a protection they did not know was there. AND THE PROBE REFUSES TO MEASURE A VALUE THAT DID NOT TAKE: its first run reported the draft lost f-root-cause, which is a select where assigning a non-option value silently yields empty - nothing had been set, so nothing could be lost, and the defect did not exist. Mutation-proven with restoreDraft disabled: 0 of 3 steps keep their work, every lost field named.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "numeric-paste",
        "script":  "tools/validate_numeric_paste.py",
        "args":    [],
        "label":   "T123: an unparseable paste must not blank a number box in silence. ★THE CLAIM HERE IS DELIBERATELY NARROWER THAN THE ONE FIRST WRITTEN, because the resurrection refuted it. The first measurement set el.value = '1,500' directly, watched the field empty with validity VALID, and got written up as 'pasting a thousands separator silently empties the box'. Running the finished prover against the PRE-FIX utils.js then passed four of five cases: with a REAL clipboard, Chromium's paste pipeline already strips the comma, the spaces and the trailing unit before sanitization. ASSIGNING a value and PASTING one are different mechanisms and only one is what a person does - the resurrection existed to catch exactly that, and did. THE DEFECT THAT SURVIVED, and it is real: an unparseable paste ('abc') blanks the field and says NOTHING, and in a quantity box a silent blank is easy to miss while the consequence is submitting no quantity. It announces now. THE NORMALIZATION IS HARDENING, LABELLED AS SUCH: whCleanNumericPaste makes the behaviour the platform's own rather than depending on one engine's undocumented paste filtering; whether other engines empty the field instead is UNMEASURED (only Chromium is installed) and neither gate nor prover claims otherwise. The probe uses a real clipboard, never a synthetic ClipboardEvent - that shortcut produced a second false reading, because the already-clean case deliberately falls through to the native paste and a synthetic event has no clipboard behind it.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "dialog-floor",
        "script":  "tools/validate_dialog_floor.py",
        "args":    [],
        "label":   "T113: every dialog measured at the 320px budget-Android FLOOR. cj_dialog_layout sweeps 390/641/1280 - the widths with banked owed rows - and 320 had never been measured at all, so this runs the same prover under WH_DIALOG_WIDTHS=320 as a SEPARATE gate rather than widening the existing one: silently adding a fourth width would change what cj_dialog_layout means without anyone deciding to, and its rows describe the three it has. THE FIRST FLOOR RUN FOUND THREE REAL DEFECTS in a place nobody had looked. (1) pm-scheduler's PM completion sheet hung its PRIMARY ACTION 21px past the panel - btn-primary is width:100% by design but shares one flex row with Cancel, and at 320 the content box is 278px, which cannot hold both however the remainder is divided; two attempts to make them share went 21 -> 10 -> 14px, the measurement saying so, and they now stack, which is the ordinary mobile pattern. (2) analytics' results panel pinned THREE columns by inline style regardless of width and pushed tiles 52px past it; auto-fit keeps three where they fit. (3) public-feed showed a 229x12px link - and it was NOT a floor defect at all: it lives in whListError, so it sat in ~20 pages' read-failure states and was failing at 390 too, unnoticed. THAT THIRD ONE IS THE ARGUMENT FOR THE GATE: measuring a width nobody had measured surfaced a platform-wide defect in the exact state a person reaches when something has already gone wrong. The prover's own rail protects it - it refuses to call an empty denominator a pass, which is how the first run (0 of 50 graded, a probe bug) was caught in five minutes instead of banked as 'the floor is clean'.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "fixed-chrome-budget",
        "script":  "tools/validate_fixed_chrome_budget.py",
        "args":    [],
        "label":   "T113's floor-viewport budget. 320x640 is the budget-Android floor this platform targets, and at that size the question is not whether things FIT but how much room REMAINS after the platform's own furniture - a fixed nav, a hub FAB, an AI widget, a page-guide chip. Each is individually reasonable, none was ever measured TOGETHER, and that is how a page ends up spending a third of its screen before one row of content is drawn. Measured across the eight pages a field phone lives in: mean 26.1% covered, worst 36.6% (pm-scheduler), and on seven of eight the largest single contributor is the same 64px guide chip - a component this codebase has already caught covering every modal's Save. Coverage is a UNION of viewport rows, never a sum: overlapping chrome costs one band, and summing would report a number nobody experiences. Forward-only per page. ★THREE INSTRUMENT CORRECTIONS BEFORE ANY NUMBER WAS BANKED, which is why this one is trustworthy: run 1 said index was 87.5% covered and blamed #cursor-glow, a 440px pointer-following gradient that is pointer-events:none, z-index 0 and 5% alpha - decoration sitting BEHIND everything; run 2 said logbook was 50.9% and blamed a 280px div.card that turned out to be the Log a Repair FORM, sticky top-6 and 843px tall on a 640px screen, which cannot stick at all and scrolls exactly like the content it is. Banking either would have sent someone deleting furniture that does not exist or a panel a worker needs. The surviving rules: chrome is what INTERCEPTS A THUMB or gets ANNOUNCED, and chrome PERSISTS WHILE CONTENT MOVES UNDER IT.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "outbound-email-honesty",
        "script":  "tools/validate_outbound_email_honesty.py",
        "args":    [],
        "label":   "T111: comms held to the same bar as the glass. The DP manipulation-absence dims are measured on PAGES, so EMAIL escapes them entirely - and it is the one surface that reaches a person who may not even have an account, cannot be corrected once sent, and is where urgency-shaped copy is most tempting and least visible to any UI test. T111's census found exactly ONE outbound template (send-report-email), audited it clean, and fixed its real gap: the footer said only 'Sent via WorkHive Report Sender', so the recipient of an outward irreversible email could not tell WHICH PERSON sent it nor how to stop receiving them. That fix shipped with no gate. FOUR assertions: (1) exactly the known set of functions builds outbound email - the census made LIVING, so a new marketing or re-engagement lane cannot appear un-audited, which is the very scenario this trajectory is named for; (2) the footer names the sending PERSON; (3) it carries a stop path AND an accurate one - recipients live in the sender's own contact list, so 'ask the sender to remove you' is truthful where a fake one-click unsubscribe would be a worse lie than none; (4) no urgency/scarcity/guilt vocabulary in the literals the TEMPLATE owns - checked against template copy only, never report content, because reports carry real words like overdue and risk and a lint policing those would demand the product misdescribe genuine urgency to satisfy a gate. Teeth-proven three ways: the pre-fix footer (both arms RED), an injected 'Your hive needs you - act now' (RED), and a new un-audited resend-calling function (RED).",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "no-clock-driven-push",
        "script":  "tools/validate_no_clock_driven_push.py",
        "args":    [],
        "label":   "T109: the structural reason this platform needs no quiet hours YET, guarded so it stays true. The walk found no quiet-hours machinery anywhere AND - the part that mattered - nothing that CLAIMS it, so the platform is silent rather than lying. What makes that absence defensible is measurable: EVERY push producer is EVENT-DRIVEN (mention, reply, best answer, assignment, approval decision, report, submission, service completion). Not one is clock-driven, so a phone cannot buzz at 3am unless somebody in that hive acted at 3am - which on a round-the-clock plant is a night shift and is legitimate news. The day a scheduled digest or nightly reminder ships that stops being true instantly, because a clock-driven push CHOOSES its own hour, and choosing 3am for someone is precisely what quiet hours exist to prevent. Asserts no cron job invokes anything that enqueues a push - a DB function calling enqueue_user_push (checked transitively one hop, since fanout_completion_push is reached through a trigger) or an edge function that does. notify-push and the outbox drain are exempt BY NAME: they deliver what is already queued, decide nothing, and must run every minute or nothing arrives. THIS GATE IS MEANT TO FIRE ONE DAY - when the digest T106 records as a scoped decision gets built, it goes red, and the red is the point: it forces the quiet-window question to be answered BEFORE the first scheduled push reaches a phone. Mutation-proven by scheduling a 3am-PHT digest cron: caught by name, then unscheduled. SKIPs without docker.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "push-dedupe-window",
        "script":  "tools/validate_push_dedupe_window.py",
        "args":    [],
        "label":   "T110: the ONLY place a notification storm is bounded before delivery. enqueue_user_push inserts one service_outbox row per event and the drain sends one push per row, so nothing between an event and a phone collapses repeats - two triggers on one PM going overdue, a supervisor's repeated save, a retrying client each become their own buzz. Mig 20260826000001 added the narrow safe half (an identical push still UNSENT within 2 minutes hands back the pending row's id) but shipped WITHOUT a gate, and it has since become load-bearing: four producers were added the same week (mentions, replies, best-answer, and a trigger on every pending submission), and the submission trigger depends on this window BY DESIGN - its copy names no row precisely so a bulk CMMS import of 200 assets collapses to ONE push. If the dedupe regresses that trigger stops being a summary and becomes a cannon. FIVE assertions, and the third separates a correct dedupe from a mute button: a repeat whose predecessor was already DELIVERED must be treated as NEWS and get its own row, because suppressing it would silently drop real notifications. Also asserts a different body and a different RECIPIENT each get their own row - a blanket one-push-per-user-per-window would swallow genuinely different news and merge two people's deliveries. Runs entirely inside a transaction that is ROLLED BACK, and then verifies nothing survived: the gate writes nothing it must clean up. Resurrection: with the dedupe removed (also inside a rollback, since DDL is transactional) identical pushes returned different ids and wrote 2 rows - both assertions RED. SKIPs without docker.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "automation-log-status",
        "script":  "tools/validate_automation_log_status.py",
        "args":    [],
        "label":   "T112's bug-CLASS gate, built after finding two live instances. automation_log carries CHECK (status IN success|failed|skipped|warning). resend-webhook-receiver wrote 'failure' (the word is 'failed') and send-report-email's circuit breaker wrote 'deferred' - both refused with 23514, and NEITHER read the insert error, so supabase-js returned it into the void and both functions carried on reporting success. The bounce receiver would have answered Resend 200 'recorded' while storing NOTHING, leaving the sender-side bounce surface permanently empty with every layer above it looking healthy; the breaker's trip would never reach the log an operator reads exactly when the breaker trips. Neither had fired - the receiver fails closed without its signing secret and the breaker needs Resend to be failing - so both were latent, primed to go off at the first real bounce after Ian sets the secret and at the first email outage. Asserts (1) every status literal is in the allowed set, read from the MIGRATION and cross-checked against the LIVE constraint, because a repo that says one thing while the database enforces another is its own defect a one-sided gate cannot see; and (2) a forward-only ratchet on UNCHECKED automation_log inserts (12 of 14 today - not zero, because rewriting every audit path in one pass to move a number is a large unreviewed change for no measured defect; what must not happen is a NEW one, since an unchecked insert is what turned both bugs from a loud 23514 into silence). Resurrection: RED on the pre-fix functions for both instances.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "bounce-idempotency",
        "script":  "tools/validate_bounce_idempotency.py",
        "args":    [],
        "label":   "T112: one delivery id, one bounce row. resend-webhook-receiver ended in a bare INSERT, while Svix (which Resend uses) delivers AT LEAST ONCE, retries on any non-2xx or timeout, and carries a stable delivery id in the svix-id header for exactly this purpose - which the function read to verify the signature and then threw away. A retried delivery wrote a SECOND bounce row: the sender-side bounce surface would list one failed send twice, and any count over it over-reports how bad deliverability is, which is the direction that gets acted on. Fixed with a PARTIAL UNIQUE EXPRESSION INDEX (mig 20260826000004) rather than a check-before-insert, because two near-simultaneous retries both pass a SELECT-then-INSERT - a shape this codebase already has a scar from. FOUR assertions, and the fourth is what distinguishes the two designs: two writers racing on one id in CONCURRENT transactions must end with exactly one row and one refusal, measured rather than argued. DELIBERATELY NOT CLAIMED: the HTTP round trip stays owed (the fn is not served locally and its secret is Ian's), but a live smoke could only show one duplicate handled, whereas the index shows none can ever be stored. SKIPs without docker.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "report-link-fidelity",
        "script":  "tools/validate_report_link_fidelity.py",
        "args":    [],
        "label":   "T106's arrival-fidelity oracle for EMAIL. Every 'View in WorkHive' link in send-report-email pointed at a bare page root, so a PM Overdue report - a document whose entire subject IS the overdue set - landed its reader on the unfiltered schedule, and both logbook reports landed on that page's default MINE view while the report itself is hive-wide: T19's two-windows-one-metric mismatch arriving by email, at whoever the supervisor forwarded it to. Measured before changing anything - ?filter=overdue is a real chip value on pm-scheduler AND genuinely filters (live: ontrack 0 rows, duesoon 2, overdue 10 vs 10 bare), so the parameter works rather than merely highlighting a chip. THE ASSERTION THAT MATTERS is not that links carry params but that every param they carry is one the TARGET PAGE ACTUALLY READS, checked against the generated param_route_registry: a link decorated with ?window=7d would look more specific while delivering nothing, which is worse than a bare link because it reads as context that was never handed over. Also asserts every target page exists - a link into a renamed page is a dead end reached from an inbox, where nobody can press Retry. DELIBERATELY NOT ASSERTED: that all eight carry a param; five target analytics.html and project-manager.html, which read NO params at all, so their links are already as specific as those pages allow and demanding more would push someone to invent one that does nothing. Resurrection BOTH ways: RED on the pre-fix bare links, and RED again on a mutation adding a plausible ?window=90d that analytics never reads.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "pending-submission-reach",
        "script":  "tools/validate_pending_submission_reach.py",
        "args":    [],
        "label":   "T108's LAST silent row. A supervisor's only signal that work was waiting on them was a realtime postgres_changes subscription living on hive.html - it fired while, and only while, they had the board open, so a supervisor reading the logbook or with the app closed learned of a submission by happening to visit. Mig 20260826000003 puts a trigger on asset_nodes and inventory_items instead of wiring call sites, because pending rows come from asset-hub (submit + resubmit), inventory (two paths), logbook and integrations' BULK CMMS upsert - six-plus paths across five pages, where a missed one is invisible: the row lands, nobody is told, nothing errors. FIVE assertions, and two of them are what make the design safe rather than merely functional. THE STORM TEST: twenty rows inserted at once must produce ONE push, not twenty - the copy deliberately names no row so enqueue_user_push's 2-minute dedupe collapses them, which turns the existing dedupe window into the storm guard instead of adding a second mechanism. THE BLOCKING TEST: with the push helper deliberately renamed away, the submission must still land - a notification is an extra, the user's work is not, and this codebase has already paid once for a guard that broke the writes it rode on, so the gate proves it rather than trusting the EXCEPTION clause. The prover repairs itself, re-checking the renamed helper at start and end, because a probe that can outlive its own damage is not acceptable. SKIPs without docker/stack.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "community-notifications",
        "script":  "tools/validate_community_notifications.py",
        "args":    [],
        "label":   "T108's last three silent rows. The someone-to-you event registry named eight ways a person is addressed by name; five were wired in earlier passes and three stayed BADGE-ONLY, meaning the platform knew and the person did not. A MENTION was the sharpest: parseMentions runs on both the create and the edit path and renderContentWithMentions paints the result, so being named was a first-class fact that reached everyone EXCEPT the person named - the nav-hub badge counts posts generally, which is a different sentence. A REPLY was T31's finding: the asker learned an answer had arrived only by coming back to look. A BEST ANSWER was recorded in the XP ledger and discovered by revisiting. Mig 20260826000002 adds three guarded RPCs on the notify_wo_assigned shape - the caller names a ROW, never a recipient and never the copy, because a client that could pass an auth_uid and a body would be a push cannon pointed at the hive. Six assertions, THREE OF THEM NEGATIVE (self-mention, self-reply and an un-accepted reply must all stay silent): a notifier that fires on everything passes a positive-only test and turns a hive into spam. Driven as a signed-in member, never as postgres - these are SECURITY DEFINER functions guarding on auth_worker_names(), so an owner-run proof would show they work for exactly the caller they must never trust. Probe rows marked and cleanup re-counted. ★AND IT NOW CARRIES T149'S EDIT-DOESN'T-RE-NOTIFY CHECK, whose measurement corrected an assumption: the 2-minute dedupe does NOT protect against a re-notify after an EDIT, because it compares whole payloads and the push body carries the post's content snippet - an edited snippet reads as different news and a second push fires. So the only thing between a supervisor's typo fix and a second buzz for everyone named in an announcement is that the EDIT PATH DOES NOT CALL notify_post_mentions. The gate asserts that single-call-site fact directly, and records the dedupe's blind spot as a FACT rather than pretending it is a defence - anyone adding that call in good faith would create a re-notify bug the dedupe would not catch. SKIPs without docker/stack.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "push-deeplink",
        "script":  "tools/validate_push_deeplink.py",
        "args":    [],
        "label":   "T105's arrival-fidelity oracle for Web Push. sw.js's notificationclick matched an already-open tab with c.url.includes(target.split('?')[0]) - the PATH, query discarded - and returned focus. Every url notify-push sends carries its destination in the QUERY (?tab=services, ?asset=, ?post=), so a provider with marketplace-seller open on ?tab=listings tapped a job-offer push and got their existing view focused, with nothing to say a destination had been promised and missed: the same class as a sign-in wall that drops the return-to. The prover loads the SHIPPING sw.js, installs the listener it registers, and dispatches a synthetic click against a fake clients registry - so it tests the file that ships, not a re-implementation. Three cases: no tab -> openWindow at the full target; tab on a different query -> NAVIGATED there; tab already there -> focus only, never a reload under someone mid-task. Resurrection: RED on the pre-fix worker for the wrong-query case ONLY, with cases 1 and 3 green in both worlds. Static tier - no browser, no stack; node only because sw.js is JavaScript.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "notification-taxonomy",
        "script":  "tools/validate_notification_taxonomy.py",
        "args":    [],
        "label":   "T107's one-honest-concept oracle for 'things for me'. The hive board's bell held four entries on the live board - PM Due Soon, Assets With PM Overdue, Open work orders, Low stock - every one a PLANT CONDITION already painted as a tile on the same page and again in alert-hub, blended with the approval entries, which are the only ones actually about the person reading them: one bell, two concepts, no way to tell which you were looking at. And the only unread signal was a 3% white tint on a dark surface, present in code and invisible in use, so a badge over ten rows gave no way to see WHICH were new. Asserts STRUCTURE, never wording: the two lanes are named, the plant lane links to the surface that owns those conditions, unread carries a marker BOTH a sighted user and a screen reader can find, and every pushNotif type is classified - a new one lands here as a decision rather than drifting into whichever lane the default sends it. Resurrection: RED on the pre-fix page for 3 of 4, and the 4th mutation-proven with an injected 'mention' producer.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "policy-rls-parity",
        "script":  "tools/validate_policy_rls_parity.py",
        "args":    [],
        "label":   "T163's 'the privacy policy is a claim' gate. privacy-policy/index.html promises 'We never share your hive data with another hive without your explicit consent' and NAMES the classes; RLS is what has to keep that promise, so this puts the two side by side. Per class it impersonates a REAL member of hive A (authenticated role + their JWT claims - the shape RLS actually sees) and asks for hive B's rows; the promise holds only at zero. NON-VACUITY is asserted both ways: the other hive must genuinely hold rows (else 0 proves nothing) and the same member must still read their OWN hive (else a blanket-deny would pass while the product is broken). CONSENT-AWARE: an author-published community post is not a leak but the consent clause working, so consented rows are subtracted from the leak test while the un-consented remainder must still be invisible - the gate's first run called 8 such posts a LEAK and the classifier, not the product, was wrong. TEETH-PROVEN: removing the consent predicate makes it correctly report the 8 public rows as visible. SKIPs without docker (RLS is the oracle).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "safety-isolation-visible",
        "script":  "tools/validate_safety_isolation_visible.py",
        "args":    [],
        "label":   "T15: the READ path shows LOCK-OUT (2026-08-27). The whole point of searching fault history before opening a machine is to learn how it was worked on last time, and 'the last person locked this out / worked under a permit' is the most consequential thing on the card. logbook CAPTURED loto_applied and permit_reference on the form, STORED them on the row, and RESTORED them when an entry was EDITED — and never showed them to anyone READING. 419 of 3,811 entries carry that data, so this was 11% of the fault history withholding its safety-isolation fact from the person about to open the machine. ★THE ORACLE COMPARES COUNTS, not existence: the rendered badge count must equal what the database says is visible on that worker's most recent page, because a badge that renders on the WRONG rows is its own defect. Verified live at 20 cards / 3 badges / 3 expected, sample 'BF-001 MECHANICAL CLOSED LOTO PTW-2026-9385'. ★AND THE SUBJECT MUST HAVE SOMETHING TO SHOW: the first run used an account whose recent 20 entries contained no LOTO, read 0 badges, and 0 was CORRECT — a pass that proved nothing. The gate now FAILS rather than passes when the chosen account has nothing to display, because a check that cannot fail is not a check. Read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "report-window-parity",
        "script":  "tools/validate_report_window_parity.py",
        "args":    [],
        "label":   "T23: the window on the CHIP must be the window in the EMAIL (2026-08-27). The four selectable reports cover wildly different periods — Shift Handover is the last EIGHT HOURS, Failure Digest the last 7 days, Predictive the last NINETY DAYS, PM Overdue a current-status snapshot — and the chooser showed a coloured dot and a label. A supervisor could send an 8-hour snapshot and a quarter of history to their boss in one email with nothing on screen distinguishing them, which is T23's own open question ('what is actually IN each of the 4 chips') answered as a defect. ★THE WINDOW WAS NEVER UNKNOWN: send-report-email's REPORT_META has carried it all along and prints it INSIDE the delivered email — it simply never reached the person CHOOSING. Now on every selectable chip, verified live: 'PM Overdue · current status as of this report', 'Shift Handover · last 8 hours', 'Predictive · last 90 days'. ★AND THAT PUTS A SECOND COPY OF A SERVER FACT IN THE CLIENT, the hazard this repo already names (two windows, one metric name) — two copies that can drift are worse than one copy nobody can see, unless something asserts they agree. This gate is that assertion: every offered id must exist in REPORT_META and every stated window must match the server string exactly; a phase3 chip may omit its window because it cannot be sent, so it promises nothing. ★ITS OWN SELF-TEST CAUGHT ITS OWN BUG: the first parser put an OPTIONAL window group behind a lazy prefix, and a lazy quantifier always prefers to skip the optional group — five logic cases passed while the live check reported four chips 'stating no window' that plainly stated one. Matching the row first and reading inside it fixed it. Teeth (--selftest, 6 cases) incl. a drifted window, a selectable chip with none, and an id the server does not know. Static, read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "success-is-checked",
        "script":  "tools/validate_success_is_checked.py",
        "args":    [],
        "label":   "T63: a page may not ANNOUNCE a write it never confirmed (2026-08-27). The mirror of unconfirmed-write-is-not-a-failure, and the worse half — telling someone their work failed when it landed costs a retry; telling them it SAVED when it did not is a lie they act on: they close the tab, walk away from the machine, stop chasing the payment. ★supabase-js DOES NOT THROW WHEN A WRITE IS REFUSED — it resolves with { error } — so `try { await db.from(t).delete()… } catch` is not protection: the catch only ever sees THROWN errors and an RLS refusal never throws. Same root as the bare-builder class seen from the other end (there the promise was never sent; here it was sent, refused, and the refusal discarded). ★IT FOUND ONE, IN THE WORST PLACE: integrations' rollbackBatch ran three writes — delete from external_sync, delete the imported rows from the target table, mark the audit entry rolled back — discarded all three results and said 'Import rolled back successfully.' A refusal on any step left every imported row in place while the person was told the import had been undone. On a REVERSAL that is the worst available lie: they stop looking and the bad data stays in the hive. Fixed, so this gate carries NO baseline — any hit is new. ★A ROWS-RETURNED CHECK COUNTS: pm-scheduler verifies with `if (upgraded && upgraded.length)` instead of reading `error`, which is equally valid; an earlier draft of the detector called that a defect, so the rule accepts either shape. Teeth (--selftest, 5 cases) incl. both valid check shapes and an error-toast that is not a success claim. Static, read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "unconfirmed-write-is-not-a-failure",
        "script":  "tools/validate_unconfirmed_write_is_not_a_failure.py",
        "args":    [],
        "label":   "T176: an empty RETURNING is an UNCONFIRMED write, not a failed one (2026-08-27). supabase-js resolves `.insert(...).select(...)` with { data: [], error: null } when the INSERT SUCCEEDED and the RETURNING read was refused — RLS gates SELECT separately from INSERT. So `if (!data || !data.length)` has learned exactly one thing: the write is unconfirmed. It has NOT learned that it failed. Eighteen sites branch this way; eight told the user their work was lost, and two were actively dangerous because the advice was 'try again': re-hailing broadcasts a SECOND job to every nearby provider for work the client wants done once, and re-registering creates a second provider profile for the same hive. ★EACH OF THOSE WAS TWO SITES, NOT ONE — the hail has a catalog path and a custom-scope path, registration has a hive path and a freelancer path — and the sweep's dedup-by-message had hidden the twins; the editor refusing an ambiguous match is what exposed them. All four now say what is actually known ('may have been sent — we could not confirm it') and point at the LIST rather than back at the button. ★THE PLATFORM ALREADY SPOKE CORRECTLY in four other places ('the job may have moved on', 'that top-up was already decided', 'changed since you opened it, reloading'), which makes the eight a drift rather than a gap. ★THE BASELINE IS FULLY TRIAGED AGAINST THE SCHEMA, never waved through: every survivor is an idempotent UPDATE, an UPSERT with onConflict, or an INSERT the database makes un-duplicatable (service_vouchers_code_key UNIQUE on code; marketplace_reviews_one_per_request_direction UNIQUE). Where a retry cannot cost anything, 'try again' is honest advice even if 'didn't save' is imprecise — a wording item, not a hazard. A baseline holding UNTRIAGED entries would launder unknowns as accepted, the failure na-premise-holds exists to prevent. Teeth (--selftest, 6 cases). Static, read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "noun-slot-takes-a-noun",
        "script":  "tools/validate_noun_slot_takes_a_noun.py",
        "args":    [],
        "label":   "T176: whReadError's second argument is SPLICED MID-SENTENCE (2026-08-27). The helper does not print `what` — it composes 'Your session expired, so ' + thing + ' could not be loaded. Sign in again to see it.' — so it must receive a NOUN PHRASE ('your equipment list', 'the audit log'). Four callsites passed a whole SENTENCE, and what shipped was: \"Your session expired, so Could not load your equipment list. Check your connection. could not be loaded. Sign in again to see it. Nothing you did was lost.\" MEASURED in the browser, not reasoned — the same call with a noun returns the sentence it should. ★THE MISTAKE HAD A GOOD MOTIVE, which is why it needs a gate and not a scolding: each site was CONVERTED from a hand-rolled message to the taxonomy helper — exactly the right move — and the old sentence was forwarded into the argument slot. logbook even carries a comment above it explaining that the upstream sentence belongs in console.error and never in the notice; the author understood the principle and still handed the helper a sentence. Converting to the taxonomy means REPLACING the sentence with a noun, not passing it through. ★AND IT IS INVISIBLE TO EVERY OTHER CHECK: the helper is present, so an adoption sweep passes it; the string holds no raw error text, so the raw-echo ratchet passes it; nothing throws. Only reading the composed sentence shows it — and that sentence only appears when a session dies mid-read, which is precisely when nobody is watching. whWriteError and whAiError are OUT OF SCOPE by design: their second argument IS a fallback message. Teeth (--selftest, 7 cases) incl. both out-of-scope helpers and a verb-opener with no period. Static, read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "no-write-only-tables",
        "script":  "tools/validate_no_write_only_tables.py",
        "args":    [],
        "label":   "T105: a table the UI writes should have a READER (2026-08-27). The table-level twin of captured-columns-reach-a-reader — that one asks which COLUMNS never reach a person, this asks which whole TABLES the UI fills and never opens again. ★IT FOUND A REAL DEFECT ON ITS FIRST RUN: marketplace-seller writes push_subscriptions when a provider enables job alerts and never reads one back, and the push card renders only while `Notification.permission !== 'granted'` — so the moment permission is granted the card vanishes forever and NOTHING ever confirms a subscription row exists. Browser permission and a live subscription are different facts: the endpoint rotates, the service worker is replaced, an upsert fails, a row is cleared, and permission stays 'granted' through every one of them — leaving the provider shown nothing while believing the page's own promise that they will 'hear new hails with this tab closed'. Measured at 0 rows. The table having no reader is precisely what let the page never check; the fix gave the card a third state ('granted but not actually subscribed', with a one-tap repair) and thereby gave the table its reader. ★READS THROUGH VIEWS COUNT, and that is the whole difficulty: naively 14 of 54 written tables looked write-only, including community_posts, hives and service_requests — all read constantly, through TRUTH VIEWS (community reads v_community_posts_truth). Resolving view→base-table from information_schema took 14 down to 2, and both survivors were real. A sweep that cannot see through a view manufactures a dozen false findings. ★A FAILED READ MUST NOT ACCUSE: the page's lookup returns null for UNKNOWN and only an explicit false warns, so a refused read can never tell a correctly-subscribed provider their alerts are off. Ratchet: the one survivor (early_access_emails — service_role-only RLS, an Ian decision on T67) is baselined with its reason. Teeth (--selftest, 6 cases) incl. the view-resolution case in both directions. Needs the local DB for the view map; SKIPs rather than guesses when it is down.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "captured-columns-reach-a-reader",
        "script":  "tools/validate_captured_columns_reach_a_reader.py",
        "args":    [],
        "label":   "T15: a column written for a PERSON must reach one (2026-08-27). logbook captured loto_applied, permit_reference, readings_json and failure_consequence on its form, stored them on the row, restored them into the EDIT FORM when an entry was edited, and rendered them to a reader NOWHERE — 1,767 of 3,811 entries carried equipment readings nobody could see. ★THE PHANTOM-COLUMN AUDITOR CANNOT CATCH THIS: it asks whether a column has a READ CONSUMER, and a `.select('loto_applied')` satisfies it. SELECTED IS NOT RENDERED — that gap is the whole finding. Asked platform-wide over 121 written columns it returned 9, and project_roles.assigned_by was a real one: 36 of 36 rows record who assigned a project role and the pill showed the name alone, so 'who put me on this?' had no answer in the app (fixed; the sweep now confirms it dropped off the list). A RATCHET, not a cliff — the survivors are baselined WITH their verdicts, because the triage is the valuable part. THREE are FIXED and deliberately absent from the baseline so a regression fails this gate: assigned_by, variance_reason (the provider's finished-job card now reports the amount, method, reference and the payer's reason) and provider_type (the quote card now says 'Hive team' or 'Freelancer' where a buyer chooses). Of the rest: 3 are NOT defects (timestamps whose fact the rendered status already carries; a total the page shows as its two parts), 2 are unused rather than withheld (MEASURED at 0 rows — live_location has never been populated, so no client is being denied it), and 1 is a raw auth uid whose display would trade this defect for the no-raw-internals one. ★SCOPE IS PLATFORM-WIDE ON PURPOSE: per-page it reported ~30 false candidates, because pages WRITE audit rows (actor/target_id/target_name/target_type) and the AUDIT-LOG page renders them. ★AND WRITERS ≠ RENDERERS: shared scripts count as renderers so a column shown by utils.js is not 'invisible', but treating them as writers too pulled in ten AI-telemetry columns (response_time_ms, model_confidence…) written for a machine, not a person. Credential columns (key_hash, p256dh, auth uids) are excluded outright — not rendering those is the security property. Teeth (--selftest, 6 cases) incl. cross-page rendering, form-restore-is-not-rendering, and secrets. Static, read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "na-premise-holds",
        "script":  "tools/validate_na_premise_holds.py",
        "args":    [],
        "label":   "T29: an exclusion's stated REASON must still be true (2026-08-27). Every declared_na removes a cell from the denominator, so a false one reads exactly like coverage. The banks carry 105 of them, each states a reason — which means most are CHECKABLE, and nothing was checking them. The prompt was the handoff N/A found the same day: a reasoned-looking exclusion whose premise was false, carried for weeks in the roadmap as product work. THE RULE, matching evidence to the CLAIM: 'no page writes / zero writes / pure render surface' means the page must not call .insert/.update/.upsert/.delete nor an .rpc() whose NAME carries a mutating verb; 'renders no peso/credit figure' means it must render no ledger-style money — a currency amount inside PROSE does not count, which is why index's anon landing citing 'replacing it costs ₱800K to ₱2.4M' as an industry statistic is correctly excluded. ★THE ORACLE MUST MATCH THE CLAIM, and this gate's own drafts prove why it is worth saying: the first version judged 'renders no peso figure' by whether the page WRITES (flagging alert-hub and analytics, since writing has nothing to do with rendering money) and treated any unfamiliar .rpc() name as a write (flagging achievements' plainly read-only my_service_provider_ids); the second added toFixed(2) as a money signal and fired on an animation duration, a risk score and a statistical range — two more false findings against earned exclusions. The signal has to be the CURRENCY, never the precision. Corrected: 28 premises checked, 0 contradicted. ★AND IT REPORTS WHAT IT COULD NOT CHECK (77 not mechanically checkable) — a gate that silently ignored the reasons it does not understand would claim the whole exclusion set is sound while reading a quarter of it, which is the shrunk-denominator move one level up. Teeth (--selftest, 8 cases) incl. both cross-wirings above. Static, read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "asset-reaches-its-history",
        "script":  "tools/validate_asset_reaches_its_history.py",
        "args":    [],
        "label":   "T15: the machine reaches its own FAULT HISTORY (2026-08-27). T15's done-definition is a JOURNEY, not a field — 'machine → its history → a usable prior fix, under a minute' — and asset-hub failed it in the most pointed way available: it COUNTED the asset's logbook entries in a stat card and offered no way to open them. A number with no door, on the page a worker is standing in front of the machine with. ★TWO WAYS THE FIX COULD LOOK RIGHT AND BE USELESS, both asserted: (1) logbook.machine stores the TAG ('M-001'), not the asset NAME ('Siemens Simotics SD 200L') — the sibling chips on that page use tag-with-name-fallback, and copying that shape would land a NAME search on ZERO results for a machine with 47 entries, an empty page indistinguishable from an empty history; so the gate requires ROWS on arrival counted against the database, never merely a link that navigates. (2) Fault history is the TEAM's history — without ?view=team the reader sees only their own entries and reads a colleague's fix as ABSENT. Verified live: M-001, 47 entries in the table, one tap, tag survives the hop into the search box, team window on, 30 cards plus the page's own Load More. Read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "captured-fields-are-readable",
        "script":  "tools/validate_captured_fields_are_readable.py",
        "args":    [],
        "label":   "T15: the READ path shows what the WRITE path captured (2026-08-27). Having found ONE captured-but-never-read column (loto_applied), this asked the general question — of the 15 columns logbook writes, which does a reader never see? FOUR sat in the identical shape: selected by the loader, mapped onto the entry, restored into the EDIT FORM when an entry is edited, and rendered to a reader NOWHERE. Two are T15's own scenario — someone searching fault history before opening a machine: readings_json on 1,767 of 3,811 rows (nearly HALF the logbook — what the last person measured, the exact thing you compare today's reading against) and failure_consequence on 1,075 rows, whose four values include 'Safety risk'. ★AND THE LOTO FIX WAS HALF A FIX, WHICH IS WHY THIS GATE CHECKS BOTH READ SURFACES: the badge went on the entry CARD only, so the safety flag DISAPPEARED on drill-down — the detail modal being precisely where a person reads carefully before working on a machine. The cause was structural: lotoBadge was declared `const` INSIDE renderEntries, so the modal could not have called it; it is now a shared top-level helper both surfaces use. ★THE ORACLE IS THE DATABASE PER SURFACE: the card list's consequence-badge COUNT must equal the rows on the visible page carrying one (verified live 20 cards / 3 badges / 3 expected), and a modal opened on an entry the DB says has readings must render them WITH UNITS — a bare '97' is the naked-number defect. Counts, not presence, because the first LOTO run read 0-and-was-correct and proved a presence check can pass while blind. Teeth: RED on all four assertions against the pre-fix page. Read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "failed-read-is-not-empty",
        "script":  "tools/validate_failed_read_is_not_empty.py",
        "args":    [],
        "label":   "A read that FAILED must not render as 'there is nothing' (2026-08-27). The shape: a loader catches, blanks its list, toasts, re-renders. The toast FADES; the empty state is what stays — so an unread table ends as 'No pending listings' or 'be the first to sell', factual CLAIMS about the world made at the exact moment the platform could not read it. ★THE PLATFORM ALREADY KNEW, TWICE: marketplace.html carries the diagnosis in its own source from a deepwalk ('the toast alone was insufficient — it fades, leaving the GRID showing the first-run CTA on a full marketplace') and sets _loadError; marketplace-seller-profile carries its own ('Only a read that ANSWERED may license a conclusion about this seller') and sets _reviewsFailed. Both fixed. marketplace-admin — the MODERATION queue, where 'nothing to review' is the sentence most likely to make someone stop looking — never got the fix, and neither did dayplanner's plant-PM rail, which on a failed read returned '' and simply VANISHED (a worker cannot tell an absent section from a quiet day). Second fix-that-did-not-travel this session; a lesson in one file's source protects that file, a gate makes it the platform's. ★THE RULE: a catch that blanks a list on a page owning an empty state must record that the read failed. The flag's NAME is not prescribed — three pages spell it three ways (_loadError, _reviewsFailed, _readFailed.listings) and all three are correct. ★SCOPED TO LOADERS: the catch must wrap a query, because not every `catch { x = [] }` is a load failure — alert-hub falls back to [] when a legacy double-encoded parts field will not parse, which is correct, and the first version of this gate called it a defect. Teeth: 5 selftest cases incl. bare and property-style flags.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "taxonomy-speaks-filipino",
        "script":  "tools/validate_taxonomy_speaks_filipino.py",
        "args":    [],
        "label":   "T45: the shared FAILURE VOICE speaks Filipino (2026-08-27). Every page is bilingual through _t(en, fil) and the error taxonomy they all delegate to was NOT, so a worker running the FIL toggle got Filipino chrome and an ENGLISH sentence at the exact moment something failed and precision mattered most. Seventeen branches across whAiError, whReadError, whWriteError and whVoiceError now return through _t. ★DRIVEN IN THE PAGE, twice per branch, once under each WH_LANG — because a source grep for `_t(` only proves the CALL was written, not that the returned string changes. ★AND TWO BRANCHES MUST NOT TRANSLATE, asserted as SAME rather than skipped: the caller-supplied fallback (the PAGE's own English sentence to own) and whWriteError's deliberate-guard passthrough, which carries a policy refusal that took the trouble to explain itself ('Listing needs PHP50 credits held and you have 0 available') — rewriting someone else's explanation in another language is replacement, not translation. A gate that only checked 'everything differs' would call both of those defects. 14 branches: 12 must differ, 2 must not. ★The FIL copy is article-aware: callers pass the noun with its English article ('the activity log') because the EN sentences need it, and Filipino carries its own — the first draft read 'ang THE activity log' until the output was read rather than the diff. Register matched to lines already shipping ('Hindi ma-load ang activity log. Suriin ang koneksyon at subukan muli.'). Read-only.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "alert-loop-closes",
        "script":  "tools/validate_alert_loop_closes.py",
        "args":    [],
        "label":   "T29: acting on an alert must make the ALERT GO AWAY (2026-08-27). An alert inbox that never empties is an inbox people stop reading, so the chain has to close: the alert names an action, links to where that action is taken, and DOING it removes the alert — with nobody marking anything by hand. ★THE CLOSURE IS STRUCTURAL, WHICH IS THE STRONGEST KIND: alert-hub's PM alerts are DERIVED, not stored — they come from v_pm_scope_items_truth filtered is_overdue=true, so there is no status to update and no second write to forget. Complete the PM and the row simply stops matching. PROVEN LIVE rather than reasoned: inserting a 'done' completion for an overdue scope item inside a transaction takes its overdue count 1 → 0 and moves days_until_due −34 → +7, then rolls back with zero residue. ★TWO ASSERTIONS, because either alone is a half-truth — THE LINK EXISTS (an href to pm-scheduler for the NAMED asset plus an action label; an alert that states a problem and offers no way to it is a notification, not an inbox item) and THE CONDITION CLEARS (a link to a surface that does not actually resolve the alert leaves the loop open while looking closed). ★The gate's own link check was wrong first: a lazy any-character match ending at the first closing brace stopped INSIDE the first nested brace — inside a template expression, well before href and actionLabel — and reported a correctly-linked alert as offering no way to act. Read as a plain forward window now, because that is what the block is.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "audit-entry-readable",
        "script":  "tools/validate_audit_entry_readable.py",
        "args":    [],
        "label":   "T28: an audit entry must read as a CHANGE, not as JSON (2026-08-27). audit-log's own meta description promises 'every CRUD + approval + permission change with actor, BEFORE/AFTER, and timestamp' — and the data can deliver it: hive_audit_log rows carry changed_fields: {\"sw\": {\"from\": 768, \"to\": 1920}}, literally a before and an after. The page rendered the whole payload as <pre>JSON.stringify(detail, null, 2)</pre>, so a supervisor reconstructing a disputed change read a JSON blob to find the two values that mattered. The claim was already on the page; the delivery was not. Now each entry is a labelled row and a from/to pair reads '768 → 1920'. ★ASSERTED ON THE RENDERED DOM, not the source, because the question is what a PERSON sees: expand a real entry and check the block carries no braces and no quoted keys. ★AND IT MUST RUN AS A SUPERVISOR — audit-log is supervisor-gated and a worker sees ZERO entries, which the first run of the probe reported as '0 entries with details': a verdict about the ACCOUNT, not the page. An empty feed is now a failure to measure, never a pass. Teeth: the oracle's regex flags the pre-fix JSON string (True) and leaves the current render alone (False), so it demonstrably separates the two worlds. Read-only; writes nothing.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "held-credits-are-told",
        "script":  "tools/validate_held_credits_are_told.py",
        "args":    [],
        "label":   "T94: a wallet must not offer credits that are already spoken for (2026-08-27). Listing HOLDS credits — guard_listing_requires_reservation writes a credit_reservations row, sweep_listing_holding_fee eats into it monthly, release_reservation_on_delist gives it back — and the seller page SAYS so at listing time ('holds ₱50 in credits'). Held credits are not spendable. ★THE PLATFORM ALREADY COMPUTED THE SPLIT: seller_credit_balance(worker_name) returns available / reserved / total, where available is the ledger balance MINUS everything held. The wallet card called provider_credit_balance() instead — the ledger sum alone — and labelled it the balance, so a seller with live listings saw a number larger than they could spend, on the same page that had just told them listing would hold some of it. Not a missing feature: the glass was reading the wrong one of three numbers that were already sitting there, the 'built but never called' shape with money in it. ★THE HEADLINE FIGURE IS DELIBERATELY NOT SWAPPED: provider_credit_balance distinguishes a person-owned provider (shows the PERSON's balance) from a hive-owned one (shows the HIVE's), and its own comment records why — 'two cards for one human showing different numbers is how this defect stayed invisible'. seller_credit_balance is person-keyed, so replacing the call wholesale would silently change WHICH ACCOUNT a hive-owned card reports: fixing one money defect by introducing another. The total stays; the HELD figure is added beside it, and renders only when something is actually held (a 'reserved ₱0' on every wallet teaches people to stop reading the card). ★MEASURED LIVE: credit_reservations is empty on this stack, so the gate CREATES a 50-credit hold inside a transaction and rolls it back — available=-50, reserved=50, total=0, split exact, 0 rows left. The probe row references a real listing because the table enforces num_nonnulls(listing_id, request_id)=1. Teeth (--selftest): a single-balance wallet goes RED, fetching the split but not showing it still goes RED, fetching AND naming it passes.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "last-unit-race-is-told",
        "script":  "tools/validate_last_unit_race_is_told.py",
        "args":    [],
        "label":   "T11: a deduction that moved NOTHING must not read as success (2026-08-27). Two workers finish repairs on the same part; inventory_deduct locks the item FOR UPDATE so the race is serialised, and the loser's call runs against an empty shelf. ★WHAT THE LOSER GOT, MEASURED LIVE (real item set to 0 inside a rolled-back transaction): `v_qty := GREATEST(0, v_qty - p_qty)` CLAMPS instead of refusing, so asking for 1 with 0 on the shelf returned 0, wrote a ledger row with qty_change 0, changed nothing, and raised NO error. Every caller checked only `error`, so the worker was told 'Entry saved' while their entry claimed a part the shelf never gave up — the shelf and the system disagreeing silently, which is the one thing this trajectory exists to prevent. ★AND THE RETURN VALUE CANNOT TELL THEM APART: it is the new quantity, and 'took the last one' and 'there were none' both end at 0. The LEDGER row can, because it records what MOVED — so callers pass p_txn_id (the function always accepted one) and read that row back through whDeductMoved(); verified under RLS that a member can read their own hive's inventory_transactions row. ★THREE SITES, NOT ONE: logbook's SAVE path, logbook's EDIT path, and inventory's Use modal — and inventory additionally cached `addTransaction(..., -qty, ...)`, recording the amount ASKED, so the page's own transaction list claimed a movement the database never made; it now records what moved. The first version of this GATE had the same disease it was built to catch, grading only the first call it found with re.search while two others kept the defect. ★TEETH (--selftest): the pre-fix error-only shape goes RED, an id passed but never read back goes RED, id+readback+comparison passes, and every live site is asserted green. The DB half rolls back and re-reads the shelf to prove it.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "alert-scope-is-told",
        "script":  "tools/validate_alert_scope_is_told.py",
        "args":    [],
        "label":   "T19: hiding a plant alert is a HIVE action and must say so (2026-08-27). alert_dismissals is keyed onConflict 'hive_id,alert_key' — ONE row per alert per hive — so every control that writes it changes what the WHOLE CREW sees: marking handled, snoozing, and marking seen alike. Nothing about the surface said that. The local state is `_seen`, the control is an aria-pressed toggle, the copy was two words: everything reads personal bookmark while the write is shared state. ★THE FAILURE IS WRITTEN IN dismissAlert's OWN COMMENT — 'a plant alert quietly blinded for the whole crew by someone who thought they were tidying their own view' — and that path was fixed when it was written while its SIBLING acknowledgeAlert was not: nothing about scope on the way in, and on the way out (un-marking DELETES the hive's row and returns the alert to unseen for everyone) nothing at all, so the crew could watch an alert reappear with no idea who or why. Both now name the scope and the actor, in EN and FIL. ★THE RULE: every user-facing message on a successful alert_dismissals write must name the scope, matched by MEANING across both languages rather than a fixed sentence — the copy ships bilingual via _t(en, fil), and a gate that pins wording punishes improving it. ★TEETH ARE THE PRE-FIX WORLD VERBATIM (--selftest, 5 cases): the old `_t('Marked seen', 'Minarkahang nakita')` is the 'two words with no scope' RED, and the old un-mark is the 'silent write' RED; a read-only query owes nothing and stays green. ★The gate's own window was wrong first and its selftest caught it: a fixed 900-char scan ended before both announcements (they sit past a writeAuditLog call and a long comment) and reported two correct paths as silent, so the search is now bounded by the `catch (` that actually ends the success path.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "kicked-rejoin-is-explained",
        "script":  "tools/validate_kicked_rejoin_is_explained.py",
        "args":    [],
        "label":   "T7: the code still works, and the door is still shut (2026-08-27). A removed worker usually still HAS the invite code — read aloud at a briefing, sitting in a chat thread — so rejoining is not an edge case, it is the next thing that happens, and it has two halves that must agree. ★THE DOOR, asserted LIVE: join_hive_by_code refuses a kicked member twice over — by auth identity, and again by (hive, worker_name) so a second account cannot walk a ban back in. Both are driven, because 'the function has an IF' is not 'the function refuses', and the second branch only fires for an identity with no membership row at all, which is exactly the shape a reading of the source talks itself out of. ★AND THE SENTENCE: a refusal reaching a person as a raw code, or as 'could not join', says the app is broken rather than that a decision was made about them. hive.html must key on HIVE_MEMBER_KICKED and say WHAT (removed) and WHAT NOW (a way that is not retrying the code, since retrying is the one action that cannot work) — matched by MEANING, not a fixed sentence, because a fixed sentence is a gate that fails when someone improves the wording. Distinct from removal-tells-the-worker (T25), which covers the removed worker's ARRIVAL; this covers the door they knock on afterwards. ★SAFETY: the kicked state is created inside a transaction and ROLLED BACK, verified by re-reading the member's status. ★TEETH (--teeth): Postgres has transactional DDL, so the run replaces join_hive_by_code with a permissive version INSIDE the same transaction — both branches then report 'allowed' and the gate fails naming both, while the definition is re-read afterwards to prove the real function came back. A green teeth run would mean this gate cannot see an open door. ★The copy check itself was caught wrong by its own selftest first: a naive quote-to-quote regex paired the closing quote of one literal with the opening quote of the next and read the CODE between them, so a correct page looked silent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "report-clobber-guard",
        "script":  "tools/validate_report_clobber_guard.py",
        "args":    [],
        "label":   "INSTRUMENT INTEGRITY: a spot-check must never destroy a full sweep's report (2026-08-27). MEASURED, not hypothesised — prove_retry_path.mjs wrote retry_path_report.json on EVERY run, whether it walked 22 pages or 1, and a single `--page <fixture>` probe replaced the whole sweep's verdicts with one UNGRADED row. ★WORSE THAN A LOST LOG: tools/bank_prover_reports.py READS these files to bank whole families and run_platform_checks registers several as a gate's `report`, so a truncated report does not announce itself — it just looks like a small run, and a narrowed probe can quietly feed one page of verdicts into a bank that believed it was getting twenty-two. ★AND IT WAS A KNOWN CLASS THAT NEVER TRAVELLED: prove_journey.mjs carries the fix in its own header ('a fixed filename let a one-page test DESTROY a 66-persona run'), written for ONE prover — 29 others had the identical shape and none had the guard. A lesson recorded in one file's comments is not a lesson the platform has learned; this gate is where it becomes one. THE RULE: if a prover accepts a narrowing flag (--page/--case/--journeys/--only/--lane/--family), the path it writes its report to must DEPEND on that flag; the guard's SHAPE is not prescribed (a ternary at the write site or a variable derived from the flag both satisfy it). All 29 were fixed in one pass — full runs still write exactly the same filename, so no baseline moved and no banked row was disturbed — and the retry sweep was re-run to repair the damage (22 pages, 21 PASS / 1 NO-RECOVER). Now 67 narrowing provers, 0 offenders. Teeth (--selftest, 5 cases): a fixed filename behind a narrowing flag goes RED; a ternary, a derived variable, a non-narrowing tool and a non-report write all stay green.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "photo-survives-queue",
        "script":  "tools/validate_photo_survives_queue.py",
        "args":    [],
        "label":   "T14's LAST PROBE (LIVE Playwright): the dead-zone entry a worker most wants to keep is the one with the PICTURE on it — the cracked bearing, the leak, the burnt contactor. logbook puts it in the row's `photo` column and offline the whole row goes to IndexedDB, so this asks whether the picture comes back intact after the phone was pocketed and the app closed. SCOPE STATED: it does not re-drive the save form (prove_offline_queued's logbook-entry case already covers form→queue, including that the save is a form SUBMIT and not a click on a zero-width button); what was never covered is photo FIDELITY across storage and a restart. The photo half IS driven for real — a genuine hand-built PNG goes to the page's own #f-photo input and the page's own pipeline (FileReader→Image→canvas→toDataURL plus its 700KB re-encode guard) produces the data URL, 36,783 chars from a 420x320 source. FOUR assertions: PRODUCED, QUEUED (zero server traffic), SURVIVES (byte-identical after a full document restart), STILL AN IMAGE (decodes back to a real bitmap). Writes NOTHING to the database — the row never leaves the queue, so no PM mirror, XP ledger or embedding is disturbed; the queue entry is removed and the removal verified. ★TEETH, and they are the interesting part: `--teeth` stores a photo of exactly the SAME LENGTH with ONE character changed. SURVIVES goes false while both lengths still read 36783 — the only way to show the comparison is byte-wise rather than the 'roughly the right size' check that lets a corrupted image pass. That run also shows STILL AN IMAGE staying TRUE for the corrupted photo, because a one-character JPEG corruption still decodes: either assertion ALONE would have passed it, and only the pair is honest. Retry-once.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "queue-survives-restart",
        "script":  "tools/validate_queue_survives_restart.py",
        "args":    [],
        "label":   "T14's THIRD HALF (LIVE Playwright + psql): the offline family proves a write is HELD, the drain gate proves it leaves EXACTLY ONCE — and both stay inside ONE page life. This puts a full document RESTART between the enqueue and the reconnect, which is what actually happens: the worker logs the job in a dead zone, pockets the phone, and the PWA is gone (OS-reclaimed or closed) before signal returns. ★NOT A VARIATION — A DIFFERENT SURFACE, and T14 already paid to learn why: the queue is created by a `_ensure<X>Queue()` accessor with auto-sync attached INSIDE it, so the drain only ever happens if a FRESH page life calls it, and pm-scheduler's measured defer race lost that queue on 2 of 5 loads silently. A queue whose rows persist but whose sync is never re-attached is WORSE than no queue: the work is neither sent nor lost but INVISIBLE, and the worker was told it was saved. FIVE assertions: HELD (zero server writes offline), PERSISTS (still in IndexedDB after the restart — read from the store DIRECTLY, never through the page object a restart destroys, which would be asking the wrong witness), REWIRED (the fresh life re-creates the queue and re-attaches sync), DRAINED, ONCE (one row after a further settle, store emptied). The restart document is fetched with the transport up ON PURPOSE: serviceWorkers are blocked so there is no cached shell, and a goto under setOffline dies at ERR_INTERNET_DISCONNECTED — a failure of the SHELL, which is a real question but a different one (T44's). The window stays write-tight regardless (route still aborted, navigator.onLine still false via addInitScript) and the DB is re-checked to prove it. TEETH: `--teeth` suppresses whCreateQueue on the restarted document, reproducing the defer race's end state — REWIRED goes false and the row is left STRANDED in the store, while PERSISTS stays true, so the assertions are shown independent and the prover is shown to see its defect. Writes a MARKED row, deletes it, ABORTs on dirty state. Retry-once.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "queue-drain",
        "script":  "tools/validate_queue_drain.py",
        "args":    [],
        "label":   "T14's OTHER HALF (LIVE Playwright + psql): the offline-queued family proves a write is HELD and announced; this proves it DRAINS. Offline (navigator.onLine overridden AND the route cut): the enqueue lands in the queue's own IndexedDB store and ZERO writes reach the server. Back online with an 'online' event: the row appears in the target table, EXACTLY ONCE (re-checked after a further settle window so a second drain tick cannot hide), and the queue store empties. The offline prover's header deferred this deliberately because 'a drain probe that reconnects into a shared database writes real rows' - so this one writes a MARKED row, deletes it, verifies the delete, and ABORTs rather than measuring on dirty state. THREE probe-shape errors were caught and fixed before any verdict was banked (async getPending awaited; RLS needs auth_uid on the row; the queue item shape is {id, op, payload}, not a flat row) - each would have been a FALSE RED against a working queue. Subject: dayplanner's schedule_items queue (no FK cascade, no ledger side-effects). Retry-once.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "calc-suite",
        "script":  "run_all_checks.py",
        "args":    ["--fast"],
        "label":   "Engineering Calc Suite (L1+L2a+L2b)",
        "group":   "Engineering Calculator",
        "report":  None,   # run_all_checks.py manages its own reports
        "skip_if_fast": False,
        "severity": "blocker",
    },
    # ── Platform Validators ───────────────────────────────────────────────────
    {
        "id":      "auto-discovery",
        "script":  "validate_auto_discovery.py",
        "args":    [],
        "label":   "Auto-discovery Validator (HTML classified, edge fns in config, validators registered)",
        "group":   "Platform",
        "report":  "auto_discovery_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "gate-observability",
        "script":  "validate_gate_observability.py",
        "args":    [],
        "label":   "Gate Observability (Mega Gate persists a durable log + verdict on every terminal path)",
        "group":   "Platform",
        "report":  "gate_observability_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "seeder-insert-columns",
        "script":  "validate_seeder_insert_columns.py",
        "args":    [],
        "label":   "Seeder Insert-Columns (forward-only: no NEW seeder writes a column absent from the live table)",
        "group":   "Platform",
        "report":  "seeder_insert_columns_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "frequency-map-consistency",
        "script":  "validate_frequency_map_consistency.py",
        "args":    [],
        "label":   "Frequency-Map Consistency (every PM frequency maps to its canonical interval days; live view + code copies agree)",
        "group":   "Platform",
        "report":  "frequency_map_consistency_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "fab-consolidation",
        "script":  "validate_fab_consolidation.py",
        "args":    [],
        "label":   "FAB Consolidation Contract (bottom-right corner stays consolidated into the nav-hub; companion/feedback/connectivity launch from inside the hub, no standalone corner FABs)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "component-purity",
        "script":  "tools/component_purity_census.py",
        "args":    ["--check"],
        "label":   "Component Purity Ratchet (PLATFORM_CENTRALIZATION C-P0/Axis-2: shared-chrome SSOT files must not add RAW brand literals — use var(--wh-*, <fallback>); fallback-aware, forward-only, count may only fall)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "storage-keys",
        "script":  "tools/validate_storage_keys.py",
        "args":    ["--check"],
        "label":   "Storage-Key Registry (PLATFORM_CENTRALIZATION C-P4: every localStorage/sessionStorage key is CANONICAL or a registered ALIAS in storage_key_registry.json; a new UNKNOWN key FAILs until registered — stops the wh_active_hive_id/wh_hive_id drift class)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "role-checks",
        "script":  "validate_role_checks.py",
        "args":    ["--check"],
        "label":   "Client RBAC Ratchet (PLATFORM_CENTRALIZATION +RBAC: raw `role === 'supervisor'` comparisons may only FALL — new client role-gates must use window.WHRoles.isSupervisor()/.can(); wh-roles.js is the SSOT)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        # T16's cited gate, BUILT 2026-09-03 (the 'a lock nothing runs locks nothing' repair):
        # the citation predated the script, so the lock rested on nothing until now.
        "id":      "ai-context-quality",
        "script":  "tools/validate_ai_context_quality.py",
        "args":    [],
        "label":   "AI Context Quality (T16: every hardcoded 'NN calc types' claim equals the CALC_TYPES_UI registry count — the 46-vs-55 mirror-drift class; assistant declares its grounding on empty + live states)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
        "severity": "regression",
    },
    {
        # T43's cited gate, REGISTERED 2026-09-03 (same repair class — the builder existed
        # with a --check mode and nothing ran it).
        "id":      "param-route-registry",
        "script":  "tools/build_param_route_registry.py",
        "args":    ["--check"],
        "label":   "Param-Route Registry current (T43: the param->page routing registry on disk matches a fresh derivation; a changed param route must regenerate it)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
        "severity": "regression",
    },
    {
        "id":      "truth-view-consumer-columns",
        "script":  "validate_truth_view_consumer_columns.py",
        "args":    [],
        "label":   "Truth-View Consumer-Columns (forward-only: no NEW consumer reads a column absent from the v_*_truth view it queries — catches PROJ-DRIFT)",
        "group":   "Platform",
        "report":  "truth_view_consumer_columns_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "interactive-lineage",
        "script":  "tools/validate_interactive_lineage.py",
        "args":    [],
        "label":   "Interactive Lineage Axis (forward-only: per-field downstream blast-radius dead-ends, display-anchor resolution, and redundancy verdicts don't regress — INTERACTIVE_LINEAGE_ROADMAP Phase F)",
        "group":   "Platform",
        "skip_if_fast": True,
        # Promoted warn -> fail (2026-07-08, Ian: "drive to 100%"): this D22 deep-interaction ratchet
        # PASSES with real coverage (5 dead-end + 38 cascade fields, 59 anchors resolved, 0 pending) —
        # locking it makes the 40 D22 page cells ✅ and blocks any future regression, per the deepwalk
        # flywheel's forward-only floor. It was advisory only while the axis was still being built.
        "severity": "fail",
    },
    {
        "id":      "causal-cascade-coverage",
        "script":  "tools/validate_causal_cascade_coverage.py",
        "args":    [],
        "label":   "Causal Cascade Coverage (Phase A anti-rot: both legs — every DB-trigger AND every edge-fn cross-table data write is mapped in causal_cascades.json — surfaces a new unmapped cascade for review)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "reactivity-wiring",
        "script":  "tools/validate_reactivity_wiring.py",
        "args":    [],
        "label":   "Reactivity Wiring (Phase D anti-rot: every write surface with cross-page fan-out emits a cross-surface receipt [D1], and every high-blast surface has impact-preview.js + a live save-button anchor [D2] — a new silent write surface FAILs)",
        "group":   "Platform",
        "skip_if_fast": True,
        # Promoted warn -> fail (2026-07-08, Ian: "drive to 100%"): this D3 cross-surface-receipt
        # ratchet PASSES (7/7 write surfaces have a verified receipt, 4/4 high-blast wired, 4/4
        # snapshot-KPI owners fresh) — locking it makes the 40 D3 page cells ✅ and blocks a new
        # silent write surface. Advisory only while the receipt wiring was still being rolled out.
        "severity": "fail",
    },
    {
        "id":      "attribution",
        "script":  "tools/validate_attribution.py",
        "args":    [],
        "label":   "Attribution integrity (every CLIENT insert/upsert into an auth_uid-no-default table must set auth_uid — locks the auth_uid-drop bug class found live 2026-07-06 on inventory_transactions/pm-scheduler/logbook/integrations/marketplace-seller/asset-hub; unresolvable payloads need an attribution-allow marker)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "client-singleton",
        "script":  "tools/validate_client_singleton.py",
        "args":    [],
        "label":   "Client singleton / idle-refresh (every Supabase client routes through getDb() so it inherits the Finding-#6 token auto-refresh + visibilitychange refresh + timeout fetch and does not spawn a 2nd GoTrueClient — locks the raw-createClient idle-401 class found live 2026-07-06 on voice-handler.js/search-overlay.js; standalone public pages carry a singleton-exempt marker; dim-14)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "embed-auth",
        "script":  "tools/validate_embed_auth.py",
        "args":    [],
        "label":   "Embed-auth / tenancy-gated edge-fn JWT forwarding (every browser fetch to embed-entry forwards the user session JWT as Bearer so its Pillar I tenancy check resolves — locks the client-drops-JWT 401 class found live 2026-07-07 on skillmatrix/logbook/pm-scheduler/voice-handler; without it the write lands but the skill/fault/PM embedding is silently dropped from the RAG index; dim-4)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "voice-journal-single-write",
        "script":  "tools/validate_voice_journal_single_write.py",
        "args":    [],
        "label":   "Voice-journal single-write (the companion agent:'voice-journal' gateway call already persists the turn server-side via persistJournalEntry with an embedding — so NO client _saveJournalTurn/insert may run on its success or clarify path; locks the double-write class found+proven-live 2026-07-07 on voice-handler.js where every companion voice turn was journaled twice, one copy embedding-less; client saves belong only on non-gateway paths [shortcuts / RAG short-circuit / offline catch]; dim-4)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "agent-memory-persist-complete",
        "script":  "tools/validate_agent_memory_persist_complete.py",
        "args":    [],
        "label":   "agent_memory persist-complete (the store_memory_turn RPC's INSERT names every NOT NULL column [worker_name/agent_id/kind] + sets kind to a CHECK-allowed literal — locks the class found live 2026-07-07 where the RPC omitted 3 NOT NULL cols and failed 100% silently, killing the companion's session-memory layer [voice-handler _storeTurn -> _fetchRecentMemory]; dim-4/dim-13)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "logbook-asset-linkage",
        "script":  "tools/validate_logbook_asset_linkage.py",
        "args":    [],
        "label":   "Logbook->asset linkage (LIVE: 0 logbook entries whose `machine` EXACTLY matches a registered asset tag may be asset_node_id NULL — locks the asset-history fragmentation class found live 2026-07-08 [deep-walk CL1], where 2700 entries [46% in Baguio] named a real tag yet were unlinked, so v_asset_truth.lifetime_logbook_entries + asset-brain + analytics UNDERCOUNT an asset's history [PB-001: 18 shown vs 37 real]. Backfill migration 20260708000000 fixed existing rows; this is a fix-to-ZERO down-ratchet. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "inventory-ledger-reconciled",
        "script":  "tools/validate_inventory_ledger_reconciled.py",
        "args":    [],
        "label":   "Inventory balance<->ledger reconciliation (LIVE: qty_on_hand must == the ledger's newest qty_after AND the ledger must chain [qty_after = prev + qty_change] — locks the stock-level seesaw found live 2026-07-08 [ARC DI §10.5], where 77/82 items' stored balance drifted from the transaction ledger [seeder wrote an OPENING balance + random-order txns]. Migration 20260708000001 added a reconcile trigger [balance mirrors the ledger on every movement] + backfill; the seeder is now born-consistent; this is a fix-to-ZERO down-ratchet. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "inventory-txn-isolation",
        "script":  "tools/validate_inventory_txn_isolation.py",
        "args":    [],
        "label":   "Inventory ledger-write hive isolation (LIVE two-tenant, rolled-back: simulates a real authenticated member and asserts a hive-A member CANNOT insert an inventory_transaction against a hive-B item [42501] — locks the cross-tenant ledger-tamper hole live-exploited 2026-07-12 [Inventory PDDA], where `inventory_transactions_write` WITH CHECK was only `auth.uid() IS NOT NULL` so a foreign-item txn's bogus qty_after was mirrored onto that item's stored qty_on_hand by the SECURITY DEFINER sync trigger [78->88888 cross-hive]. Migration 20260712000011 scoped the write policy to hive_members membership + item-hive match, hive-guarded the trigger, and added a qty_after>=0 CHECK [negative-stock tamper]. Also asserts a legit in-hive insert still succeeds [no regression]. Actors picked dynamically = reseed-robust. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "pm-write-isolation",
        "script":  "tools/validate_pm_write_isolation.py",
        "args":    [],
        "label":   "PM-write hive isolation (LIVE two-tenant, rolled-back: simulates a real authenticated member and asserts a hive-A member CANNOT [42501] inject a pm_scope_item onto a hive-B asset, a pm_completion into hive B [compliance poisoning of v_pm_compliance_truth -> analytics/shift-planner/hive-health/predictive], nor a phantom pm_asset into hive B — the same child/ledger-table WITH-CHECK class as the inventory ledger tamper, THREE holes live-exploited 2026-07-12 [PM Scheduler PDDA]: pm_scope_items_write WITH CHECK was `auth.uid() IS NOT NULL` only; pm_completions_write + pm_assets_write had WITH CHECK=null so INSERT fell back to a USING with no hive gate [completions] / an `auth_uid=self OR member` OR [assets]. Migration 20260712000012 membership-joins the parent/own-hive in every WITH CHECK. Also asserts a legit in-hive completion still succeeds [no regression]. Actors picked dynamically = reseed-robust. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "display-correctness-fixes",
        "script":  "tools/validate_display_correctness_fixes.py",
        "args":    [],
        "label":   "Display-correctness fix regression gate (STATIC: asserts the 2026-07-13 bug-hunt render-logic fixes are still present — a revert removes the marker and this FAILs, catching what a DB probe can't see. Locks: achievements 'XP this week' summed from a dedicated 7-day query not the .limit(30) list [limit-as-aggregate undercount] + init surfaces whListError on a query error not a fake 'No achievements yet'; asset-hub 'Total assets'/'Critical' via count:'exact' not the .limit(200) list length; ai-quality loadCostLog/loadReplyFeedback throw on {error} instead of swallowing it as [] + dropped the unused user_feedback column; inventory supervisor fetches the full pending approval queue + stock counts exclude non-approved; hive adoption card shows a neutral 'No data yet' tier when there's no snapshot not a green 'Healthy' + PM-overdue count/banner reset on hive-switch. Complements the LIVE validate_hive_write_isolation.py)",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "hive-write-isolation",
        "script":  "tools/validate_hive_write_isolation.py",
        "args":    [],
        "label":   "Hive-write isolation for the sibling tables the 2026-07-12 sweep MISSED (LIVE two-tenant, rolled-back: asserts a hive-A member CANNOT [42501] inject a phantom inventory_items part into hive B, nor a report_contact into hive B [cross-tenant recipient exfil], and a WORKER CANNOT mint an api_key [supervisor-only] — three holes live-exploited 2026-07-13 [platform bug-hunt]: inventory_items_write WITH CHECK owner branch `auth_uid=self` had no hive gate [status<>approved dodges the approval trigger]; report_contacts_write WITH CHECK was `auth.uid() IS NOT NULL` only; api_keys_hive_rw had no role gate [worker minted a hive credential]. Migrations 20260712000019 [hive-gate inventory_items+report_contacts WITH CHECK] + 000020 [supervisor-scope api_keys/project_roles, approval-trigger project_change_orders]. Also asserts a legit in-hive part add + a supervisor api_key mint still succeed [no regression]. Actors picked dynamically = reseed-robust. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "hive-isolation",
        "script":  "tools/validate_hive_isolation.py",
        "args":    [],
        "label":   "Hive cross-tenant READ + MEMBERSHIP + ATTRIBUTION + ROLE isolation (LIVE two-tenant, rolled-back — bug-hunt 2026-07-13/14, migs 20260713000001-012). 25 invariants as a real authenticated member (+engineering_calcs/parts_records/voice_journal authorship pin [mig 012: eng_calc_attr_pin + parts_rec_attr_pin + voice_j_attr_pin — substrate closure-pass siblings; parts_records has no auth_uid so worker_name is its only attribution; project_items.owner_name deliberately NOT pinned = it's an assignment field not authorship] +community_posts + inventory_items attribution [mig 011: comm_post_attr_pin + inv_item_attr_pin — a spoofed author_name/registrant on a community_posts/inventory_items INSERT is server-pinned to the caller via bind_community_post_submitter/bind_inventory_item_submitter; substrate-found siblings of the mig-007/010 sweep], +pm-scheduler attribution [mig 010: pm_asset_attr_pin + pm_completion_attr_pin — a spoofed registrant/completer on a pm_assets/pm_completions INSERT is server-pinned to the caller via bind_pm_*_submitter], +marketplace listing trust-forge sourced from canonical [mig 009], credential/gamification server-mediated forge-block [skill_badges/worker_achievements], logbook owner-scoped edit; 13/14 = community_replies attribution: comm_reply_attr_pin [a spoofed auth_uid/author_name on a reply INSERT is server-pinned to the caller] + comm_reply_hijack_block [a member cannot UPDATE another member's reply; author-or-supervisor-only]; mig 007 bind_community_reply_submitter + author-scoped write policies, closing the within-hive forge + cross-author BOLA on community.html): (1) read_logbook_xhive + (2) read_cxp_xhive — a FOREIGN hive's v_logbook_truth / community_xp return 0 rows [mig 001: v_logbook_truth/v_project_truth/v_marketplace_listings_truth were missing security_invoker so the view ran as superuser and BYPASSED base RLS — a non-member read 1105 rows of another tenant's logbook LIVE; +community_xp_read was auth.uid()-only, leaking every hive's roster/XP/UUIDs]; (3) mem_selfjoin [P5-01: self-join any hive_id] + (4) mem_kickrestore [P5-02: DELETE-then-reINSERT un-bans a kicked worker] — closed by mig 002 (server-verified join_hive_by_code RPC + founder-only INSERT + sticky-kicked DELETE); (5) asset_attr_pin + (6) logbook_attr_pin + (7) projects_attr_pin — a spoofed auth_uid/worker_name on INSERT is server-pinned to the caller [migs 003/004/005 bind_*_submitter triggers]; (8) comm_announce_gate — a WORKER cannot post a category='announcement' [UI-only-gate BOLA closed by mig 006]; (9) audit_actor_bind [P4-03: forged hive_audit_log.actor rebound] + (10) text_caps_present [P4-02: hives/hive_members cap triggers] [mig 003]; (11) join_rpc_ok + (12) founder_create_ok — no-regression. Actors + a data-rich foreign hive chosen dynamically = reseed-robust. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "bughunt-scoreboard",
        "script":  "tools/build_bughunt_scoreboard.py",
        "args":    ["--check"],
        "label":   "per-page bughunt v3 ANTI-DRIFT scoreboard — regenerates PER_PAGE_BUGHUNT_SCOREBOARD.md (every page's 12x6 matrix mapped to its covering gate) and FAILs if any page has an uncovered GAP: a footprint item no standing gate hunts (an edge fn not in the 57-fn edge-auth sweep, a hive_id view not in read-isolation, or a page-battery finding). This is the drift-guard — a NEW page or a new edge fn/view/write that isn't gate-covered trips it. Reads the page-battery + read-isolation reports (so runs in full mode, after those refresh). 2026-07-20: 42 pages · 1 DEEP · 41 COVERED · 0 GAP",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "rate-limit-handling",
        "script":  "tools/validate_rate_limit_handling.py",
        "args":    [],
        "label":   "per-page SaaS-LAYER · Layer RL (Rate Limiting) — METHOD-LAW central-adoption gate. When a page's AI/edge call is rate-limited (server checkAIRateLimit → structured 429), the user must see a SCOPE-CORRECT message, not a raw error. The mapping is ONE central helper `window.whAiError(err, fallback)` in utils.js (429→'you hit the rate limit, wait' · 503→'AI busy' · network→'check connection'). Every page invoking a rate-limited AI fn (ai-gateway/*-orchestrator/*-assist/voice-*/semantic-search/asset-brain) must handle 429 via whAiError OR an inline check. Adopted 2026-07-22 on the 8 that lacked it (analytics/analytics-report/marketplace/project-manager/project-report/report-sender/shift-brain/voice-journal) → 12/12 covered; alert-hub exempt (best-effort empty-catch-allow keeps the stored fallback). Server 429→graceful is separately gated by perf_l5_llm_resilience; companion 429 UX is central in companion-launcher.js. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "sw-shell-membership",
        "script":  "tools/validate_sw_shell_membership.py",
        "args":    [],
        "label":   "CA (Caching/CDN) deep-walk cell — every page in the service-worker OFFLINE SHELL (sw.js SHELL_FILES) must (a) exist on disk (a stale entry 404s the SW precache install → the whole offline shell breaks for every PWA user) and (b) the shell must be cache-VERSIONED (CACHE_NAME), so a shell change re-primes rather than serving stale cached markup. Emits its exact per-page pass-list to deepwalk_layer_pages.json[CA] for the deepwalk flywheel's CA architectural-layer cell (the gate-emitted-pass-list mechanism: the gate publishes its EXACT scope, the flywheel never regex-approximates). 8 shell HTML pages. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "degraded-state-central",
        "script":  "tools/validate_degraded_state_central.py",
        "args":    [],
        "label":   "per-page SaaS-LAYER · Layer AV (Availability & Recovery) — METHOD-LAW central-component adoption gate. The device-offline / degraded warning is ONE shared idempotent component (`offline-banner.js`, __whOfflineBannerLoaded guard), NOT per-page code. Every USER-FACING backend-touching page (getDb/db.from/functions.invoke/rpc) must ADOPT it, so a page that ships stale/failed actions with no offline warning FAILs. Adopted 2026-07-22 on the 4 that lacked it (analytics-report/founder-console/platform-actions/resume) — 31/31 user-facing backend pages now covered. Internal dev/observability consoles + backups exempt (documented). The OTHER AV half (backend-down fail-closed / empty-vs-error) is reused: L-backbone (error-capture) + P12 (page-battery) + read-battery. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "error-capture",
        "script":  "tools/validate_error_capture.py",
        "args":    [],
        "label":   "per-page SaaS-LAYER · Layer L (Error Tracking & Logs) — a catch around a BACKEND op (db.from/db.rpc/db.functions.invoke/fetch) that SURFACES the error to the user (showToast/addBubble/showFormError/textContent/alert) must ALSO CAPTURE it (console.error/warn, logEvent, window.onerror, captureException). A show-but-don't-log catch leaves a backend op that starts failing for users INVISIBLE in the console/aggregator (the L-layer 'ungreppable logs' failure mode). REAL bug locked 2026-07-22: assistant.html sendMessage swallowed the ai-gateway failure from observability (shown, never logged). Best-effort/silent-by-design catches (comment `empty-catch-allow`/`best-effort`/`non-fatal`) + no-user-surface + non-backend catches are excluded. FORWARD-ONLY ratchet on the swallow count (baseline seeds; a NEW swallow FAILs; `--accept` ratchets DOWN as the backlog is fixed to 0). Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "saas-layer-scoreboard",
        "script":  "tools/build_saas_layer_scoreboard.py",
        "args":    ["--check"],
        "label":   "per-page SaaS-LAYER bughunt ANTI-DRIFT scoreboard (PER_PAGE_SAAS_LAYER_BUGHUNT_ROADMAP.md §0) — regenerates PER_PAGE_SAAS_LAYER_SCOREBOARD.md (every page × the 13 SaaS production layers F/A/D/AU/H/C/CI/S/RL/CA/LB/L/AV, each cell COVERED/OPEN/N.A derived from the page's substrate footprint) and holds the count of OPEN operational cells (C/RL/CA/L/AV — the genuine new hunt) as a FORWARD-ONLY CEILING. FAILs if it RISES above baseline: a new page / new edge fn / regressed operational gate that escapes coverage = drift, caught by CI not vigilance. As each per-page operational probe (L error-capture, C LLM-fallback, RL 429-backoff, CA cache-hit, AV degraded) is built + registered, edit COVERAGE in the builder to flip its cells ○→✓ and `--accept` ratchets the baseline down toward 0. Seed 2026-07-22: 42 pages · 138 OPEN ops · 251 COVERED · 157 N/A.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "edge-fn-auth-gate",
        "script":  "tools/validate_edge_fn_auth_gate.py",
        "args":    [],
        "label":   "per-page bughunt v3 L6 — every hive-touching edge fn must gate its caller (STATIC, no DB). A Supabase edge fn runs with SERVICE_ROLE (RLS-bypass); one that reads/writes tenant data keyed by a CLIENT hive_id WITHOUT first proving caller entitlement = cross-tenant write/read injection by any anon caller. The 2026-07-20 sweep of all 57 fns found every one gated (resolveTenancy/resolveIdentity/requireServiceRole/getUser/authenticate/cron-secret, or verify_jwt=true); 4 exempt (login = session-creator; resume-extract/resume-polish IGNORE hive_id [Pillar P back-compat]; voice-journal-agent uses hive_id only as ai_cost_log telemetry). A NEW fn that references hive_id but ships without a caller gate FAILs until reviewed + explicitly allowlisted. Baseline 0 ungated",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "hive-battery",
        "script":  "tools/validate_hive_battery.py",
        "args":    [],
        "label":   "hive.html LIVE per-page battery — PER_PAGE_BUGHUNT_ROADMAP Tier-1 (P1 Smoke / P2 Console+Network / P8 Visual). Headless Playwright signs in as the REAL Baguio supervisor (leandromarquez / hive 636cf7e8) and asserts, on the live board load: P1 — renders REAL data [no undefined/NaN/[object Object]], NOT the no-hive empty state, the primary anchor stats #stat-open == v_logbook_truth open-WO count and #stat-members == hive_members active count [rendered==DB truth], and ZERO console errors; P2 — every Supabase REST/RPC/auth/functions response is <400 [no silent 4xx/5xx / swallowed error]; P8 — no horizontal overflow at 390px [mobile] or 1280px [desktop]. Re-drive: node tools/validate_hive_battery.mjs [--headed]. Skips cleanly if node or the local stack [Flask :5000 / Supabase :54321] is absent; a render-drift / console-error / 4xx-5xx / overflow regression is a FAIL)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "page-battery",
        "script":  "tools/validate_page_battery.py",
        "args":    [],
        "label":   "Platform-wide page battery (LIVE headless Playwright, real Baguio supervisor sign-in) - PER_PAGE_BUGHUNT_ROADMAP section 5 mechanical floor across ALL ~30 interactive pages. Locks the page-agnostic phases so a regression FAILs: P1 Smoke (every page loads signed-in, renders a non-blank body, NO error-state banner ['failed to load'/'unexpected error'], ZERO console errors on load); P2 Console/Network (no 5xx response during load = no silent server error); P4 Inputs (the SAFE reflected-XSS probe: typing an <img src=x onerror> payload into every visible input NEVER executes [window flag stays unset] NOR reflects as a live <img onerror> node - locks the reflected-DOM-XSS invariant across every page's inputs; submit-path P4 stays MCP-interactive per the roadmap); P8 Visual (no horizontal overflow at 390px [mobile-first invariant] on any page). Complements the DEEP per-page truth gate validate_hive_battery.py (asserts rendered==DB for hive.html). Re-drive: WH_TEST_HIVE=084c113b-99c0-45c6-a8e8-b4b8349da46d node tools/page_battery.mjs --gate [--headed]. Skips cleanly if node or the local stack (Flask :5000 / Supabase :54321) is absent)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "page-crud",
        "script":  "tools/validate_page_crud.py",
        "args":    [],
        "label":   "Per-page P3 CRUD-at-DB gate (LIVE headless Playwright, real WORKER sign-in via live_page_journeys). Three invariant shapes, because not every entity is a worker's to create or destroy. (1) CREATE-AND-PIN (voice_journal_entries/engineering_calcs/community_posts): INSERT with a FORGED display name -> assert it PERSISTED but the name is PINNED to the caller (bind_*_submitter, migs 010/011/012) -> owner-scoped DELETE -> assert cleaned. (2) REFUSED-by-design (pm_assets): a worker's INSERT must be DENIED — mig 20260728000011 made creating a PM asset supervisor-only, since every added asset moves the hive's PM-compliance denominator; a row coming back means the guard was lost. (3) deleteRefused (projects): a worker MAY create and the name pins, but removal is supervisor-only (guard_and_audit_project_removal), so a worker DELETE that lands is the regression. RETURNING clauses are explicit everywhere: a bare .select() is `select *` and 42501s on a SELECT-revoked column (projects.budget_php) AFTER the row has committed — see the `star-returning` gate. Probe rows use a per-run unique key and are swept by the Python wrapper. Skips cleanly (exit 0) if the local stack/sign-in is absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "fixture-hive-exists",
        "script":  "tools/validate_fixture_hive_exists.py",
        "args":    [],
        "label":   "FIXTURE HIVE EXISTENCE — a pinned test hive that no longer exists is a SILENT instrument, and the worst shape a test failure can take. FOUND 2026-07-30 while triaging read-battery, which reported SIX pages failing 'DB empty -> empty-state (no error)'. Every one read db=0, and the cause was not the pages: the hive UUID hardcoded as the battery's fallback (636cf7e8-...) DOES NOT EXIST — a reseed removed it, and every instrument still pinned to it began measuring an empty world. It does not error and it does not skip; it renders plausible, specific, entirely fictional page defects and sends you to read page code that was never wrong. Seventeen REGISTERED instruments were pinned to hives that are gone. Scoped deliberately: only files the suite actually runs (registered scripts, their .mjs helpers, and specs) — an abandoned scratch diagnostic pinning a dead hive is dead code, not a live lie, and including them returned 24 hits of mostly noise. Two exemptions, both principled: the all-zeros UUID is deliberately unresolvable (it proves a NEGATIVE — a caller with no tenancy), and a UUID the file itself INSERTs is self-minted inside a rolled-back probe and is SUPPOSED not to exist (the first cut flagged 37, most of them correct code). The durable fix is dynamic resolution — read-battery now resolves the signed-in supervisor's own most-populated hive with a deterministic tie-break — and this gate makes the rot LOUD in the meantime: one line naming the file instead of an afternoon of phantom triage.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "marketplace-state-inducers",
        "script":  "tools/validate_marketplace_state_inducers.py",
        "args":    [],
        "label":   "MARKETPLACE STATE INDUCERS — the two bank states SQL altitude cannot reach. TB-STATE-inducers induces `empty`, `filtered0` and `edge` in rolled-back SQL and deliberately stops there; `error` (the listings read FAILS, induced by aborting that request at the network layer) and `degraded` (the device is offline, induced at the browser-context level rather than by stubbing navigator.onLine, which would test the stub instead of the guard) are BROWSER facts and live here. WHY THE `error` ONE IS THE POINT: a failed read and an empty result are the same thing to a row count, and this platform has already been bitten by that — read-battery once reported SIX named page failures, all 'DB empty -> empty-state (no error)', and none were real. From the USER's side the ambiguity is worse: a seller whose query merely failed must not be told 'be the first to sell', because that reads as *your listings are gone*. marketplace.html already gets this right — `_loadError` is documented at line 1064 as 'P7: a FAILED listings fetch must render an error state, not the first-run be-the-first-to-sell CTA' — but that behaviour had NO browser test, because a static grep cannot prove which branch renders when the network actually fails. This gate LOCKS the fix rather than re-implementing it, and asserts the CTA is absent as well as the error being present. TWO THINGS THE SPEC HAD TO GET RIGHT, both learned by getting them wrong first: (1) POLL, never sleep-then-assert — the page RETRIES the read (abort count climbs 12 -> 20 -> 32) and declares failure only once retries are exhausted at ~8s, so a 2.5s sleep measured the retry budget and failed while the product was correct; (2) assert INSIDE #listing-grid, never over body.innerText — both the error copy AND the CTA exist elsewhere in the document as other sections' markup, so a whole-page match goes green for the wrong reason, and innerText silently omits anything in an inactive tab. The offline half requires the guard to BOTH refuse and announce, including the sentence that nothing was sent: a guard that refuses in silence is the failure mode this platform found live when a centralised offline guard produced no toast because showToast is page-local. Both assertions are non-vacuous by construction — the error test fails if the request was never intercepted, the offline test fails if whRequireOnline is absent rather than merely permissive. skip_if_fast: browser minutes.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "guard-mutation-score",
        "script":  "tools/validate_guard_mutation_score.py",
        "args":    [],
        "label":   "GUARD MUTATION SCORE — would the marketplace test bank NOTICE if a guard behaved differently? The bank's SQL lane is 130/130 green and the transition board reads 99.6%; both say the cells RAN and agreed with the guards, and neither says anyone would have noticed had a guard changed. The 2026-07-30 triage is why that distinction is not academic: of seven suite failures, FOUR were defects I had just introduced, two were instruments, one was contamination, and ZERO were product defects the suite had found — every genuine defect came from BUILDING a new instrument, not from a gate going red. Mutation testing is the discipline for exactly this, and the research is blunt: a suite with 100% line coverage can score BELOW 50% when assertions are weak. HOW: for each of the four status-machine guards (service_requests / service_credit_topups / marketplace_orders / marketplace_listings) it applies semantically meaningful operators — negate the party gate, drop it entirely, force v_is_party false, force v_is_client/v_is_matched_provider true, make the admin bypass unconditional again, turn a `raise exception` into `return new` (fail OPEN), widen an authorised from-state list — then re-runs ONLY that table's bank cells and requires at least one to go RED. Each mutant carries exactly ONE fault, because two can mask each other. SAFETY: the mutated CREATE OR REPLACE is injected INSIDE each cell's own begin/rollback, so the mutation and the cell that judges it live and die together; DDL is transactional, so a crash cannot leave a weakened guard installed, and no migration is ever written. The run asserts 0 mutated guards persist afterwards. HONESTY: a SURVIVOR is a punch-list item naming a behaviour change no cell objected to; a MALFORMED mutant (will not compile) is an operator bug, discarded and reported rather than inflating the score; an UNREACHABLE mutant is excluded WITH a printed reason, because a silently-excluded mutant is how a mutation score gets flattered — the same no-silent-skip rule this arc applied to the anon/admin partitions. THE FIRST RUN CAUGHT MY OWN INSTRUMENT: it reported guard_service_topup_status at 0% on 1 cell, because authored probes carry no `transition` field and my selection could not see them — while TB-I2 was asserting precisely the top-up self-deal those mutants create and reading the credit ledger back. Probes now declare `covers_tables` explicitly (auditable, cannot drift silently). ★AND THEN IT REPORTED A FABRICATED 100% THREE TIMES RUNNING (27/27, 36/36, 42/42), all of which were recorded as verified before being caught: `pg_get_functiondef()` returns CREATE OR REPLACE with NO trailing semicolon, so the injected definition swallowed each cell's next statement and every mutated run died on a syntax error before one assertion executed — and because the oracle reads an ERROR as a REFUSAL, negative cells passed silently while positive cells failed, so every mutant looked killed by a fixture that had never run. The honest score was 50.0%. It was caught by an operator written EXPECTING a survivor coming back killed on a branch no fixture could reach: a kill that is impossible on the evidence is a report about the instrument, not the code. Three sibling defects had also flattered the number — an `is_party_false` operator injecting a `false or` PREFIX (false OR X is X: an unkillable no-op counted as a SURVIVOR), then `false and` on guards whose party test is a DISJUNCTION ((false and A) or B is B, so it silently collapsed), a duplicate operator under re.IGNORECASE inflating the denominator, and a mutated-guard leak check still grepping for the first draft's mutation text, i.e. a safety check that had silently stopped checking (it now compares each guard's definition byte-for-byte against its pre-mutation capture). THE HONEST MEASUREMENT THEN FOUND THE REAL GAP: guard_service_request_status scored 31.6% — 13 of 19 faults surviving across 107 cells — because every cell in the bank derives from the authorised-TRANSITION set, so every cell is an UPDATE ... SET status, and two whole regions of each guard had never been entered: the TG_OP='INSERT' branch (what state a row may be BORN in — a top-up born `verified` mints credit without ever entering the verification path) and the `status unchanged` branch (field reassignment, ownership transfer, a stranger's edit). TB-BIRTH, TB-FIELD and TB-MINT close those. THE RATCHET IS NOW FORWARD-ONLY ON THREE AXES, because adding operators legitimately lowers the score and a score-only ratchet called that a REGRESSION and failed the gate — which would have pressured me to drop the new operators or fake the baseline: `killed` may never fall (the absolute count of faults the bank catches), `score` may fall only when `viable` GREW (new questions asked), and `fixture_kills` may only fall — a mutant that dies because a cell could not RUN is not a mutant the bank noticed, and a spike in it is the exact signature of the broken injection above, which the score alone could never have seen since the fabricated 100% and the honest 100% are numerically identical. Self-test proves the harness measures CELLS and not scaffolding (one derived cell kills 1, the 103-cell derived grid kills 6, the full 109 kills 18) and discloses that 12 of 18 kills come ONLY from authored probes — the derived grid is broad and shallow, which is the standing punch list. skip_if_fast because it re-runs cells per mutant.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "fetch-retry-contract",
        "script":  "tools/validate_fetch_retry_contract.py",
        "args":    [],
        "label":   "FETCH RETRY CONTRACT - one retry for idempotent READS, never for writes. fetchWithTimeout (utils.js) retries ONCE on a transport failure, because two gates flaked on that exact shape: push-runtime-delivery went red once against four greens, and the Playwright smoke tier carries an intermittent Supabase blip. Neither was a product defect and neither was a timeout - the wrapper's budget is 45s and both failures landed in milliseconds, so they were the network briefly refusing a connection. Widening each spec's budget would have measured network weather instead of the product, so the fix went to the source. THE RETRY IS SCOPED BY HTTP METHOD, NOT BY AN OPT-IN FLAG, deliberately: the helper cannot know whether its caller is safe to repeat, a flag can be forgotten at any of ~20 call sites, and a retried write is how one payment becomes two. GET is idempotent by contract, so every write method is excluded BY CONSTRUCTION rather than by discipline. This gate asserts four properties, and the two that must NOT retry are the ones that license the feature at all: a GET retries once and resolves on the second attempt; a POST is NEVER retried and its error propagates; an AbortError (the timeout path) still returns null on the FIRST attempt, because silently doubling a budget breaks the contract callers reason about - and three callers were just fixed for mis-handling that very null; and a persistently dead endpoint stops at EXACTLY two attempts rather than looping, the recursion guard being the only thing between 'one retry' and an unbounded budget. The assertions run against the SHIPPED text of utils.js - the helper is lifted out of the real file rather than copied into the test - so the gate cannot drift from the code it guards, which is the failure mode of every hand-mirrored fixture this platform has found. Invoked via node directly, never npx, because this repo's path contains an ampersand that npx's own path resolution splits on. Static/fast, no DB, no browser.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "audit-actor-hive-scoped",
        "script":  "tools/validate_audit_actor_hive_scoped.py",
        "args":    [],
        "label":   "AUDIT ACTOR RESOLUTION — every audit trigger must resolve the acting worker IN THE AUDITED ROW'S HIVE. Seven triggers do `SELECT hm.worker_name INTO v_actor FROM hive_members hm WHERE hm.auth_uid = auth.uid() ... LIMIT 1`, and two of them (fixed by mig 20260730000007) did it WITHOUT constraining the hive. `LIMIT 1` with no ORDER BY and no hive predicate picks an ARBITRARY membership, so a member of two hives could have an amendment in hive A logged under the worker_name they use in hive B — the same limit(1)-picked-the-wrong-hive shape this platform has been bitten by before. MEASURED, not assumed: it is LATENT rather than live, because there are 2 multi-hive members today and ZERO use a different worker_name across memberships, so the arbitrary pick currently returns the same string either way; it becomes a real misattribution the first time one person is 'Pablo Aguilar' in one hive and 'P. Aguilar' in another, which needs no code change to happen. WHY THIS IS A STATIC GATE AND NOT A BANK CELL: the fix is correct by construction but NOT verifiable by an outcome test, and I proved that rather than assuming it — a behavioural probe manufactured the missing state (two hives, deliberately different names for one person, an amendment in hive B), then restored the PRE-FIX definition inside the same rolled-back transaction to watch it fail, and it did NOT fail: the arbitrary LIMIT 1 returned the hive-B row anyway, so the probe reported the correct actor in BOTH worlds. A test that passes against the bug is not evidence, and banking it would have been precisely the false-green this platform keeps finding — so the property is locked where it IS deterministic, in the source. The check requires the predicate in the SAME STATEMENT as the lookup, because several of these functions also read hive_members elsewhere for a role check and accepting that would let an unscoped actor lookup hide behind an unrelated scoped query. Self-test strips the predicate from one function THROUGH THE READER (nothing on disk or in the DB is touched) and requires the gate to go red naming it. Static, fast, always runs.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "membership-auth-uid-resolution",
        "script":  "tools/validate_membership_resolved_by_auth_uid.py",
        "args":    [],
        "label":   "MEMBERSHIP RESOLVES BY AUTH_UID, NOT DISPLAY_NAME — a CROSS-TENANT PRIVILEGE ESCALATION a self-rename could drive, found 2026-07-31 while building behavioural teeth on the shared tenancy helper. `v_worker_truth` is the membership anchor `_shared/tenant-context.ts::resolveTenancy` trusts, and through it 30 SERVICE-ROLE edge functions decide which hive a caller belongs to. Its membership join was `LEFT JOIN hive_members hm ON hm.worker_name = wp.display_name` — keyed on a DISPLAY STRING, not an identity. `worker_profiles.display_name` is UPDATE-granted to authenticated/anon, the RLS policy `profiles update own` (USING auth.uid()=auth_uid, no WITH CHECK) lets a user set it to ANY value, and there is NO UNIQUE constraint on it. So a member of hive A renames their own profile to a member of hive B's display name and the view joins their auth_uid to hive B's membership row. PROVEN, rolled back: Pablo Aguilar (a member of Manila Electronics, NOT of Baguio Textile Mills) runs `update worker_profiles set display_name='Leandro Marquez'` as himself under RLS (allowed), then resolveTenancy(<pablo>, <baguio>) run SERVICE-ROLE exactly as the edge functions run it returns {ok:true, role:'supervisor'} for a hive he never joined. The helper itself was correct — it filters v_worker_truth on auth_uid AND hive_id AND active — the VIEW lied to it. Why anon/authenticated did not already leak it: v_worker_truth is security_invoker, so under the caller's own role RLS on hive_members hides other hives; but the edge functions call it on a service-role client (RLS bypassed) scoped only by the client-supplied hive_id, so for them the phantom row is fully visible. FIXED by 20260731000001, which re-keys both the membership join and the active_hive_count subquery on the IMMUTABLE auth_uid — proven behaviour-identical on current data (all 17 active memberships carry a non-null auth_uid; name-keyed and auth_uid-keyed joins return the SAME 17 rows, 0-row delta), so no legitimate resolution changes and only the rename attack is closed. THIS GATE IS A BEHAVIOURAL PROBE, NOT A STATIC ONE, because the escalation is DETERMINISTIC (unlike the audit-actor lookup, whose failure was non-deterministic and had to be locked in source): it discovers an (attacker not in hive H, victim active in H) pair from live data, and inside ONE rolled-back transaction renames the attacker to the victim's name as `authenticated` (proving the rename is actually allowed), asks the REAL view under service_role whether the attacker is now a member of H (MUST be 0), then mutates the view BACK to the name-keyed join and asks again (MUST be >=1 — the teeth: a probe that cannot reproduce the bug proves nothing), then rolls back and re-reads the live definition to confirm no mutant persists. Self-test control: WITHOUT the rename even the name-keyed mutant escalates 0 rows, proving the escalation is caused by the rename and nothing else. Sibling *_truth views that name-key a DECORATION join (skill_badges, achievements, assignment, worker_skill) were triaged: none is on a service-role authz-resolution path (0 of the sensitive ones have any edge-fn consumer; the two used by orchestrators scope by hive_id at query time and carry auth_uid in their base) — recorded as a lower-severity hygiene backlog, not this critical class. skip_if_fast is OFF: it is a fast rolled-back probe, and a tenancy escalation is worth the seconds.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "tenant-context-contract",
        "script":  "tools/validate_tenant_context_contract.py",
        "args":    [],
        "label":   "TENANT-CONTEXT HELPER CONTRACT — behavioural + mutation teeth on `_shared/tenant-context.ts`, the tenancy boundary 30 edge functions trust. It is imported by 42 functions; 30 route their tenancy decision through resolveContext/resolveTenancy. FOUR registered gates already 'cover' it (edge-fn-auth-gate, public-fn-authz, gateway-bypass, public-fn-write-authz) and EVERY ONE is a regex marker over CALLER source — it asserts callers IMPORT the helper — so a weakened isServiceRoleBearer or a dropped 401 guard would pass all four. This gate exercises the helper's ACTUAL branch logic. IT CANNOT DO SO OVER HTTP: the edge runtime serves a CACHED module, proven 2026-07-31 with a sentinel — the message string of resolveTenancy's 403 was changed on disk and the running function kept returning the ORIGINAL over two calls two seconds apart. A mutation harness that edits the file and calls the function over HTTP would therefore score EVERY mutant 'killed' while nothing actually changed — the exact fabricated-100% shape the guard mutation arc already corrected once (a suite that never ran, reading green). So the runner (tenant_context_contract.mjs) loads the REAL helper under Node v24, which strips the TS types natively — only the type-only SupabaseClient import is neutralized to `type SupabaseClient = any`, behaviourally identical after stripping — runs its functions against a stubbed Supabase client that records every .eq() filter, and MUTATES the source text to prove the assertions bite. 16 behavioural assertions: a service-role bearer is accepted on EXACT match only (a prefix, a superstring, and empty are all rejected); with no env key configured ANY bearer is rejected (fail-closed); a service-role bearer resolves isServiceRole with authUid null; a user bearer resolves authUid from the JWT; null authUid is 401; an active member is ok; the membership lookup is filtered by auth_uid AND hive_id AND active status; a non-member is 403; a hiveless caller with a profile is solo-ok and without one is 403. SEVEN mutants, 6 viable all KILLED: exact-match weakened to a prefix; the empty-bearer guard dropped; the null-authUid 401 removed; the non-member 403 removed; the hive_id filter dropped; the active-status filter dropped. ONE is EXCLUDED as equivalent WITH a falsifiable mechanism — dropping the redundant Boolean(key) conjunct cannot change any result (bearer===key with a truthy bearer already implies a truthy key, and a shared '' short-circuits on Boolean(bearer)) — and if that mutant is ever KILLED the run fails as a STALE EXCLUSION, the same discipline the guard arc used for its six exclusions. The DB-level escalation (a self-rename into another hive) is locked SEPARATELY by membership-auth-uid-resolution; this locks the helper's own service-role / null-auth / non-member branches. Node directly, never npx (this repo's path contains an ampersand npx mis-parses). Static/fast, no DB, no browser.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "embedding-space-integrity",
        "script":  "tools/validate_embedding_space_integrity.py",
        "args":    [],
        "label":   "EMBEDDING SPACE INTEGRITY - every corpus must live in exactly ONE vector space, matching its registry pin. Caught live 2026-07-31: fault_knowledge held 534 rows in bge-small-en-v1.5-local while a newly written pm_knowledge row came back nomic-embed-text-v1_5. Both writes SUCCEEDED and nothing errored - but a vector embedded by a different model lives in a DIFFERENT GEOMETRY, so cosine against it is noise: the row is silently unfindable AND can outrank real matches. A row COUNT cannot catch this (every row is present, non-null, correctly shaped); only the model column distinguishes a usable vector from a useless one. Running it immediately found MORE than the row that prompted it: skill_knowledge sits ENTIRELY in nomic space (4 rows), so that corpus is unusable against bge queries, and three different tag strings were in play for the same model. Asserts: one distinct embedding_model per corpus, no NULL model (which would make single-space unprovable), and agreement with the registry pin so ingest and query cannot drift into different spaces. Corpora are DISCOVERED from the catalog, so a new knowledge table is covered the day it appears. FIX is DELETE-first re-embed, never an upsert - a hash-keyed upsert will not replace a vector when only the MODEL changed and the text did not. Self-test proves the detector on synthetic input (split caught, NULL caught, pin-disagreement caught, clean corpus accepted) and main() refuses to report if it fails.",
        "group":   "AI Validation",
        "severity": "fail",
        "skip_if_fast": True,
    },
    {
        "id":      "knowledge-is-retrievable",
        "script":  "tools/validate_knowledge_is_retrievable.py",
        "args":    [],
        "label":   "KNOWLEDGE IS RETRIEVABLE - a logbook entry is the hive's memory of a failure and earns its keep only if someone can FIND it months later, which means reaching fault_knowledge (the embedded, semantically searchable copy keyed logbook_id). A row with no fault_knowledge row is WRITTEN-ONLY: on the board, invisible to every search, RAG answer and 'has this happened before?'. Measured 2026-07-31: 3,811 qualifying entries, 533 retrievable (14.0%), 3,278 written-only. The denominator is honest - embed-entry SKIPS an entry whose composed text is under 50 chars, so this gate composes the SAME text the function does (Equipment/Problem/Root cause/Action taken/Lesson learned/Category) and counts only rows that would actually qualify; on this database ZERO rows are short enough to be skipped, so the 86% gap is real and not an artifact of the filter. Self-test asserts the composition reproduces the function's own 50-char rule on a rich-vs-bare pair, because a denominator that does not match the product invents a backlog. WHY THE BACKLOG EXISTS: the trigger meant to embed these rows POSTed them to a PRODUCTION url (see local-triggers-dont-call-prod), so embeddings landed in production while the local index stayed empty, and that trigger is now DISABLED. Backfilling means 3,278 free-tier embedding calls - a costed decision, not something a gate does behind anyone's back - so this is FORWARD-ONLY: the uncovered count may FALL, never rise. It does not fail on the existing backlog; it fails when a NEW write surface puts knowledge on the board without indexing it.",
        "group":   "AI Validation",
        "severity": "fail",
        "skip_if_fast": True,
    },
    {
        "id":      "credit-economy-simulation",
        "script":  "tools/simulate_credit_economy.py",
        "args":    ["--runs", "400"],
        "label":   "CREDIT ECONOMY SIMULATION - generated job lifecycles driven through the REAL guards, triggers and ledger inside ONE transaction that is ROLLED BACK, so no simulated peso survives. THIS IS THE HONEST FORM OF `massive simulation`: tools/run_companion_100turn_flywheel.py advertises 100 turns x 1225 tests and FABRICATES every outcome (simulate_turn_observations draws pass rates from random.gauss against a sigmoid), which for a money economy is worse than no test because it produces a confident number describing transactions that never happened ([[feedback_measure_the_worked_state_not_the_generator]]). A single probe proves a rule on one job; this asks whether it still holds across hundreds, over SIX exit paths so all 12 request states are genuinely reached. Six invariants: net take = commission - cashback over settled GMV (MEASURED, not asserted - it reads ~4.8% blended, not the headline 4%, because consumer jobs carry 10% commission and industrial 5%, so the real rate depends on MIX); solvency sampled DURING the run rather than only at the end, since a balance that went negative and came back would otherwise be missed; exactly-once commission and cashback per settled job under a deliberate double-tapped release; worst liability cover reported rather than the average, because the study's failure modes are TAIL events; order-independence as a metamorphic relation (same two top-ups in either order reach the same balance - no expected value needed, which is why this property had no test); and a reached-state histogram, because a run that never produced an `expired` or `disputed` arc has not tested them and must SAY so ([[feedback_a_skipped_partition_reads_as_a_covered_one]]). TEETH PROVEN by --inject double-mint / ledger-delete / negative-consumer, each of which must turn the run RED. That teeth test caught ITSELF first: the injections originally fired at i=2, and 2 %% 6 = 2 is the provider-cancel path which never settles, so two of the three reported PASS while never executing - and double-mint turned out to be STRUCTURALLY impossible while the partial unique index stands, so it now drops that index first and asks whether the invariant would notice if the guarantee were ever removed. The daily row cap is announced past via the platform's own workhive.row_cap_system_write GUC and that is stated in the code rather than done silently: it is a real anti-abuse control with its own gate, it fired correctly on synthetic volume, and nothing else is bypassed.",
        "group":   "Platform",
        "severity": "fail",
        "skip_if_fast": True,
    },
    {
        "id":      "money-economy-board",
        "script":  "tools/money_economy_board.py",
        "args":    ["--check"],
        "label":   "MONEY ECONOMY BOARD (M1-M8) - the anti-drift compass for the credit-economy arc, per [[feedback_follow_framework_antidrift_before_building]]: the structure is laid out BEFORE the build and the board is what decides what to do next, so a tangent cannot quietly replace the roadmap. Eight MEASURED rows - M1 money spine (payment record / cashback wired / commission bills what was paid / dispute path), M2 knobs READ, M3 deterministic SQL money cells, M4 the 12 request states reached by a LIVE walk, M5 diverse-persona pairings scored task-success, M6 the six economic simulation invariants, M7 the fraud model, M8 money gates registered. WHY A COMPUTED BOARD AND NOT A TABLE IN A DOC: a table drifts from reality the moment work starts and then it flatters; every row here is read from the live catalog, the test bank and this very registry, so it cannot claim a unit that does not exist. The denominators are fixed in the file BEFORE building precisely so they cannot be shortened later to manufacture a 100% ([[feedback_short_denominator_is_a_false_100]]). DONE = EVERY ROW AT 100%, never the greenest one - a single headline metric at 100% has already masked an open axis here ([[feedback_phase_table_is_one_axis_build_the_compass]]) - and every owed item prints WITH ITS REASON rather than being averaged away ([[feedback_a_skipped_partition_reads_as_a_covered_one]]). The M2 bar is deliberately strict: a knob counts as READ only when its consumer can actually FIRE (has a trigger, or is called), because the first draft credited cashback_pct to mint_service_cashback - a function that reads it and has NO trigger, so it mints for nobody. Forward-only ratchet; --check fails on ANY row regressing.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "persona-task-success",
        "script":  "tools/validate_persona_task_success.py",
        "args":    [],
        "label":   "PERSONA TASK-SUCCESS - can a DIVERSE human finish the money task, not just an idealised one? (Ian 2026-07-31: consider the diversity of human beings.) Every journey in the bank implicitly walked a competent, sighted, literate, English-reading, fast-network, calm user, and almost none of this platform's real users are all six at once - PH plant technicians in their fifties, first-time e-wallet users, someone outdoors in glare wearing gloves, someone on 3G. DIVERSITY IS NOT A PAGE PROPERTY, it is WHO IS WALKING, so it lives on the journey axis. DIVISION OF LABOUR, deliberately: UFAI grades the PAGE (validate_service_ufai_deep measures 390/1280 tap targets and overflow), validate_i18n_coverage owns language, validate_clickable_keyboard_a11y owns keyboard - none can answer 'could THIS PERSON complete THIS TASK'. The 25 personas and their journey pairings live in tools/service_personas.mjs as RUNTIME CONDITIONS (viewport, zoom, colour filter, throttle, locale, input delay), not comments, and 129 unpaired combinations are recorded WITH REASONS rather than dropped - 25 personas x 7 families is 175 walks nobody runs, and an unrunnable matrix silently becomes an unrun one. Seven checks on the families where failure costs pesos: 12 states carry DISTINCT TEXT labels not colour alone (P-COLORBLIND); the release screen discloses who gets what, that the platform never holds the money, and what happens if the job was bad (P-SCAMWARY, the persona who decides whether this economy works at all); the min-balance refusal is announced AND says how to fix it (P-UNBANKED); the release resists a double tap (P-IMPULSIVE); money terms are translatable (P-FILIPINO); the map has a text equivalent, since a map is unusable by a screen reader and without a list discovery is closed entirely (P-SCREENREADER); and a 48px target floor (P-GLOVED, a real industrial constraint). ITS OWN FIRST TWO READINGS WERE INSTRUMENT BUGS: it reported the 12 status labels MISSING because its regex assumed quoted object keys while the source uses bare ones, and reported the min-balance refusal owed because it read only marketplace.html while the message lives on marketplace-seller.html. Both are fixed and both are why it now reads the real SVC_CHIP mapping across BOTH money surfaces instead of pattern-matching one.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "commission-leakage",
        "script":  "tools/validate_commission_leakage.py",
        "args":    [],
        "label":   "COMMISSION LEAKAGE - found by ATTACKING, not by testing (TB-FRAUD-money-economy-attacks A3). Commission bills what was actually PAID, which is the honest base and a deliberate fix, but it hands both parties a lever: a client and provider who agree a PHP50,000 job and declare PHP1 on the payment record pay PHP0.10 instead of PHP2,500. The attack probe measured exactly that, and NOTHING refused it or noticed. This DETECTS rather than refuses, on purpose: a declared payment below the quote is often legitimate (job under scope, a discount, part-payment in materials), and refusing those would block honest work and push people off-platform entirely, which costs more than the leak. So the platform bills honestly on what was declared and MEASURES the gap - the fraud model's standing disposition that an attack is either refused or DETECTED AND NAMED, never silently absorbed. Measures, for every settled job with a payment record, the ratio of declared to agreed (selected offer price, else catalogue rate, else stated budget); severe below 50% and only on jobs over PHP1,000, since a PHP200 job declared at PHP80 is noise. Forward-only ratchet on the severe COUNT so an existing backlog does not block the build while a NEW pattern shows immediately; --accept only ratchets DOWN. Self-test proves the arithmetic on the real attack (PHP1 of PHP50,000 caught, PHP1,800 of PHP2,000 not) without needing the database.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "marketplace-sim",
        "script":  "tools/run_marketplace_sim.py",
        "args":    [],
        "label":   "MARKETPLACE SIMULATION - 115 live scenarios composed from DECLARED dimensions (Ian 2026-08-02: 'we need around diverse 100+ live simulation tests, and from there we could iteratively improve the entire marketplace'). GENERATED by tools/build_marketplace_sim_scenarios.py rather than hand-written, because 100+ hand-authored scenarios drift, duplicate and skew toward whatever the author found interesting that day - composing them from dimensions makes coverage auditable by family, role-pair, persona and defect class, and proves no dimension was skipped. The dimensions are all already measured here: the 12 request states (from the CHECK constraint, not a guess), the 7 journey families, the 25 personas in service_personas.mjs, the 8 user-facing defect classes found on the live walks of 2026-08-01/02, and the money invariants. TWO RULES ENFORCED AT GENERATION TIME, both learned the hard way: every state-CHANGING scenario must declare BOTH roles (feedback_two_sided_journeys_need_a_role_pair measured FOURTEEN of 54 journeys as one-sided - a walk that drives one side proves nothing about what the other sees), and every scenario must assert something FALSIFIABLE rather than 'the page loads', since a vague assertion reports green forever. `mutates` is DECLARED, never inferred - the first validator guessed it from family+oracle and immediately produced two false flags on read-only checks. THE RUNNER REPORTS EXECUTED, NEVER AUTHORED: 15 scenarios are decidable against the live database and run here; 97 need a real browser and 3 need human judgement, and every one of those is listed BY ID as owed-with-reason rather than counted. Reporting 115 as though it were 115 tests would be exactly the false green this bank exists to prevent.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "hail-geo-coverage",
        "script":  "tools/validate_hail_geo_coverage.py",
        "args":    [],
        "label":   "HAIL GEO COVERAGE - is the broadcast radius filtering anything? FOUND ON A LIVE CLIENT WALK 2026-08-01: a hail placed through the real UI lands with location IS NULL, because the form asks for a free-text address and never captures a point (there is no getCurrentPosition anywhere in marketplace.html). That is not cosmetic - the accept gate reads `if v_req.location is not null and v_provider.base_location is not null and not st_dwithin(...)`, so a NULL location makes the whole distance test SKIP while the request still carries broadcast_radius_m=5000 and the UI still speaks of notifying NEARBY providers. The number is displayed and inert, and a provider on the far side of the country is as eligible as one next door. IT STAYED HIDDEN BECAUSE THE SEED DATA IS HEALTHIER THAN THE PRODUCT: every seeded request carries geo, so probes, journeys and scoreboards all see a working radius, and only a hail created through the real form exposes it - the mirror of the dead-fixture class, where seeded state invents a reality the product does not have. This gate deliberately does NOT fail the build for the missing capture: obtaining a client's coordinates needs either a browser geolocation PROMPT or an external geocoder, and both are product/privacy decisions that belong to Ian rather than to a validator. What it does is make the gap MEASURED and FORWARD-ONLY - the count of geo-less hails may never RISE, so it ratchets toward zero if capture is ever added and goes red the day someone adds another geo-less write path. Self-test proves the arithmetic (an inert radius is reported, a fully-geocoded set is not) without touching the database.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "credit-solvency",
        "script":  "tools/validate_credit_solvency.py",
        "args":    [],
        "label":   "CREDIT SOLVENCY - are the credits given away backed by anything? MARKETPLACE_CREDIT_SUSTAINABILITY §5 named liability cover the number that matters, and §4.5 named spending the float the most dangerous failure mode BECAUSE IT IS INVISIBLE UNTIL IT IS FATAL. Credits enter three ways and they are NOT equal: topup is backed by CASH (a provider paid GCash), cashback is backed by REVENUE (funded out of commission, and the hive_service_settings CHECK already refuses cashback_pct > commission_pct + listing_fee_pct so it cannot outrun the take per transaction), and voucher_grant is backed by NOTHING - platform-funded acquisition minted from thin air. Vouchers are therefore the LIVE GAP: nothing currently bounds them, and a generous promo is indistinguishable from an accident until the float is gone. The invariant held here is `unbacked credits (vouchers) <= credits actually EARNED (commission)` - if the platform has given away more than it has ever earned, it is funding acquisition out of other people's prepayments, which works right up until they spend them. ALSO ASSERTED: a negative CONSUMER balance is impossible by construction (consumers only receive cashback and spend it), so one appearing means credits were spent that were never minted; a negative PROVIDER balance is expected and fine, since commission is debited on completion and min_list_balance is what keeps wallets funded. The arithmetic is a pure function so the self-test proves it catches over-granting and negative consumer balances and passes a healthy ledger WITHOUT needing the real ledger in any particular state - and an empty ledger PASSES vacuously while saying so, rather than reporting a false green. Reads service_credit_ledger via docker exec psql; skips when the DB is down.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "payment-rails",
        "script":  "tools/validate_payment_rails.py",
        "args":    [],
        "label":   "PAYMENT RAILS - three GCash accounts meet in this product and only ONE of them may ever be on a given screen. WorkHive has no business registration and therefore no merchant account, so every rail is a PERSONAL number: a buyer pays the PROVIDER's own GCash directly and the platform never touches it; a provider tops up credits by paying the FOUNDER's personal 0995 009 2416; a credits spend moves no GCash at all, being a ledger transfer between two wallets. THE INVARIANT: the founder's number may appear ONLY on the provider's top-up card. Put it on a buyer-facing payment step and the buyer sends job money to a person who is not party to the job, cannot fulfil it, and cannot reconcile it against any request - and NOTHING ERRORS. The money is not lost to a bug but to a stranger's honest reading of the screen, which is worse, because nobody finds out until the provider asks where their payment is. This is also the exact friction Ian named by hand ('I want it hassle free for my users, like the hassle of payment two gcash accounts'): a buyer must see exactly one number, the provider's. Four assertions: every occurrence of the founder number sits on an allowed surface; no buyer-facing payment surface contains it in ANY spacing or punctuation (the regex tolerates '0995 009 2416', '0995-009-2416' and '09950092416' as one number); the buyer's confirm step states that WorkHive holds no money, which is the sentence a wary first-timer actually decides on; and that step names the PROVIDER as payee so 'who do I pay' has one answer where it is asked. Comments and doc blocks are stripped before matching - only RENDERED strings can mislead a person. Teeth: planting the founder's number on marketplace.html's payment step makes this FAIL naming that surface and that consequence; selftest proves all four failures are caught and a correct topology passes. Static, fast, no DB.",
        "group":   "Security",
        "severity": "fail",
    },
    {
        "id":      "cl_page_contrast",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "page_contrast", "--gate"],
        "label":   "CL ui-visual CONTRAST on every production page, populated - BOTH oracles, run on the platform's OWN two calibrated lenses (live-state-runner's composited APCA + axe's WCAG) rather than a third implementation. It settles the 78 rows the bank had owed behind a recorded belief that contrast is 'NOT computable from the CSSOM here' - true of the NAIVE probe that produced it (uncomposited alpha reads ratio 1.00, a gradient defeats a single-colour sample) and false of this one, which composites alpha up the ancestor chain, averages gradient stops, resolves background-clip:text, and reports candidates/truncated so a cap can never hide under '0 failing'. IT WAS AN ORPHAN: built, working, and invokable from NOWHERE - registered in no suite and absent from PROVERS - so it enforced nothing for months while reporting 0 failing across 22 pages on the strength of a teeth claim that was a COMMENT ABOUT A DIFFERENT FILE'S MATHS. Now falsifiable: --teeth plants an 18px #6b6b6b-on-#5a5a5a violator and a #fff-on-#000 satisfier BEFORE the first lens runs, and requires the violator caught on BOTH lenses and the satisfier clean (measured 0/305 -> 1/307 when planted). Baseline: 22 pages, 2503 APCA nodes, 0 failing, nothing truncated. Reports carry teethRun so a teeth run can never be misread as a measurement - a mistake made once here.",
        "group":   "Frontend",
        "severity": "fail",
        "skip_if_fast": True,
    },
    {
        "id":      "cl_view_contrast",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "view_contrast", "--gate"],
        "label":   "CL ui-visual CONTRAST inside the opened V2/V3 dialogs - 43 declared targets across 22 pages, each opened through its own source-read path. Same two lenses, same teeth (2/2 views caught the planted violator on both). Baseline: 37 targets reached, 36 graded, 1151 nodes, 0 failing. The dialog surface is where contrast debt hides: a modal's text sits on a translucent card over a gradient, which is exactly the shape the naive CSSOM probe could not read and this one can.",
        "group":   "Frontend",
        "severity": "fail",
        "skip_if_fast": True,
    },
    {
        "id":      "revoke-actually-revoked",
        "script":  "tools/validate_revoke_actually_revoked.py",
        "args":    [],
        "label":   "REVOKE ACTUALLY REVOKED - a `REVOKE EXECUTE ... FROM anon, authenticated` is a NO-OP, because every function is created with EXECUTE granted to PUBLIC and every role inherits PUBLIC. Measured 2026-08-20: of 21 functions a migration revokes, ELEVEN were still callable by any signed-in user. Three were exploitable - award_achievement_xp (self-award arbitrary XP; its own migration says \"Block direct client calls: XP must come from DB triggers only\" and that was aspirational for three months), reverse/restore_community_post_xp (cross-tenant XP IDOR: takes a post_id, derives hive_id FROM THE ROW, checks nothing), and enqueue_service_push_uids (arbitrary push title/body/URL to ANY user, no auth.uid() at all). This gate asks the LIVE catalog via aclexplode(proacl) whether grantee 0 (PUBLIC) still holds EXECUTE - never proacl::text, because `postgres=X/postgres` contains `=X/` and fools a LIKE. Exemptions must NAME THEIR CALLER: store_memory_turn and update_dialog_state are called from voice-handler.js and gate themselves, so revoking them would be an outage wearing a security fix's clothes; five reads gate via the platform helper user_can_access_hive() rather than a literal auth.uid(), which a narrower grep missed and nearly caused a working guard to be patched. Teeth: --selftest. Skips cleanly when the DB is down.",
        "group":   "Security",
        "severity": "fail",
    },
    {
        "id":      "session-death-is-not-removal",
        "script":  "tools/validate_session_death_is_not_removal.py",
        "args":    [],
        "label":   "SESSION DEATH IS NOT REMOVAL - an expired session returns ZERO ROWS with NO ERROR under RLS, which is indistinguishable from \"you were removed from this hive\" unless the session is checked. asset-hub and shift-brain did not check, so a lapsed session fell into the not-a-member branch, DELETED the user's saved hive keys and told them to \"join or create one\". Requires every page that wipes those keys behind a membership check to gate on a resolved auth uid FIRST, at a brace depth the check shares - depth, not indentation, because the first fix put the gate inside `if (!WORKER_NAME)` where it read as correct and would never have run.",
        "group":   "Security",
        "severity": "fail",
    },
    {
        "id":      "trigger-writes-need-definer",
        "script":  "tools/validate_trigger_writes_need_definer.py",
        "args":    [],
        "label":   "DEFINER FOR CLIENT-UNWRITABLE WRITES - a SECURITY INVOKER function that writes a table the end user cannot write raises 42501 INSIDE the trigger and takes the user's own statement down with it. Found 2026-08-20: migration ...061 added an AFTER DELETE trigger on logbook that INSERTs into achievement_xp_log, where anon/authenticated hold SELECT and nothing more (deliberately - a client that can write its own XP can self-deal), so a worker could no longer delete their OWN logbook entry. The award side it reverses, award_achievement_xp, has been SECURITY DEFINER + SET search_path since 2026-05; the reversal simply never copied the clause, which is the third time in this codebase a compensating write has not inherited its counterpart's protection. The gate builds the client-unwritable set from the migrations' own GRANTs (SELECT to anon/authenticated and never INSERT/UPDATE/DELETE), then FAILs any function body writing one without SECURITY DEFINER. Result on the real tree: 548 migrations, 229 function bodies, 89 client-unwritable tables -> exactly 2 findings, both in ...061, zero false positives across the whole history. Teeth: --selftest plants an INVOKER writer and a DEFINER writer of the same table and requires the first caught and the second NOT. Static, no DB; ABSTAINS loudly rather than passing when it can prove nothing.",
        "group":   "Security",
        "severity": "fail",
    },
    {
        "id":      "no-client-truncate",
        "script":  "tools/validate_no_client_truncate.py",
        "args":    [],
        "label":   "NO CLIENT TRUNCATE - RLS IS NEVER CONSULTED FOR TRUNCATE, so the grant is the only control. FOUND 2026-08-03 by the live-MCP flywheel, three steps from where it started: walking G-trust as an anonymous visitor showed every seller as an unverified Bronze; the read policy turned out to be signed-in-only; widening the READ meant auditing the WRITES; and the write privileges included TRUNCATE. Proven as `anon` in a rolled-back transaction - `truncate table public.marketplace_sellers cascade` SUCCEEDED and the table read 0 rows afterwards. Every seller on the platform, removed by someone who never signed in. Blast radius measured at the time: 140 tables anon-truncatable, 142 for authenticated, ~104,000 rows. WHY EVERY EXISTING GATE MISSED IT: validate_unprotected_write_grant holds 'a base table may grant an end-user write verb ONLY IF row-level security is enabled on it', which is correct for INSERT/UPDATE/DELETE and INERT for TRUNCATE, because RLS is not consulted for it at all - marketplace_sellers has RLS enabled with a full policy set, so it passed that gate cleanly while an anon TRUNCATE emptied it. The July sweep had even flagged the shape in its own notes (an anon TRUNCATE on marketplace_listings failed only with 0A000 'referenced in a foreign key constraint', a coincidence of the schema rather than a control) - marketplace_sellers had no such FK, so nothing stood there. Closed by migration 20260803000028 for the public schema plus ALTER DEFAULT PRIVILEGES so new tables cannot re-open it. The storage.* grants are TRACKED rather than enforced and printed every run: they are owned by supabase_storage_admin, postgres is not superuser in this stack and cannot `set role` to it, so no migration this project writes can revoke them - closing those needs a Supabase-side superuser action. Teeth: re-granting TRUNCATE on any public table makes this FAIL naming that exact table; selftest proves it enforces the public schema, tracks the vendor residual rather than hiding it, and stays quiet when clean.",
        "group":   "Security",
        "severity": "fail",
    },
    {
        "id":      "credit-guards-refuse",
        "script":  "tools/validate_credit_guards_refuse.py",
        "args":    [],
        "label":   "CREDIT GUARDS REFUSE - installed is not the same as enforcing, and the credit-posture gate only proves the triggers EXIST. A trigger can be present while its function has been replaced by `return new`, and every existence check in the suite stays green. The plan's verifications #1, #2 and #4 are the same demand in three forms, so all three are walked against the real schema: (1) a listing cannot outrun its reservation - publishing with less than 10% of the price available is refused; (2) an unsold listing costs NOTHING - publish then delist and the balance returns to EXACTLY its starting value, which is the whole difference between a reservation and the listing fee rejected in July, because a fee is consumed whether or not the item sells and a reservation that returns less has quietly become one; (3) a balance DRAINS - a job carries reward_earn OR reward_spend, never both, because with the reward rate and the spend cap both at 10% a job that pays a reward on the job you spent on is a treadmill where the balance never empties and credits stay a liability forever. TEETH: --inject 1|2|3 NEUTERS that guard in-transaction (`create or replace` to `return new`) and requires the refusal to DISAPPEAR - a probe that cannot be made to pass was never testing the guard. THE TEETH MODE ITSELF SHIPPED A REAL BUG worth remembering: `docker exec psql` runs in AUTOCOMMIT, so a bare `create or replace function` is its own transaction and COMMITS. The first version permanently gutted three live guards on the local database while the DO block rolled back exactly as designed and printed TEETH CONFIRMED; the damage only surfaced on the NEXT clean run, which failed two assertions that had passed minutes earlier, and repair meant replaying migrations 09-26 in order (re-applying 08 alone had also reverted the entry_type CHECK, dropping reward_fund). DDL is transactional in Postgres - but only if you open a transaction, so the whole probe is now wrapped in an explicit BEGIN/ROLLBACK and the guards are verified intact after every injected run. Skips cleanly when the stack is down.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "adversarial-personas",
        "script":  "tools/probe_adversarial_personas.py",
        "args":    [],
        "label":   "ADVERSARIAL PERSONAS REFUSED LEGIBLY - the 500-run simulation proved the ECONOMICS hold (a collusive pair extracts PHP0, the spam cap blocks 274,428 listings, sybil is bounded only by ID strength). It never proved the SCREEN refuses these people in words they can act on, and a guard that blocks in silence cannot be told apart from a guard that is not firing. So each of the four personas asserts TWO things: the attack is contained, AND the refusal is a sentence a human could act on rather than a constraint name (a bare 'violates check constraint' fails the bar). Spammer: capped at 3 live listings with 'sell one, or take one down to make room'. Sybil: the second starter grant returns already_claimed. Collusive pair: a naked user-to-user transfer is refused AND circulation delta is exactly 0. Scam provider: the buyer gets a dated objection deadline and raise_service_objection moves the job to disputed. THE PROBE'S OWN THREE BUGS ARE THE REASON IT IS TRUSTWORTHY, and each is a banked class: (1) it read only stdout while every verdict line is a psql NOTICE on stderr, so all four personas reported 'no verdict' - a false red; (2) it probed the spam cap with NO JWT, and guard_first_listings_need_a_sale opens with `if auth.uid() is null return new` (the backend path seeders need), so all 8 listings published and it read as 'the cap does not work' when the probe was never a person; (3) even with an identity, TWO earlier guards fired first - self-publish is refused outright, and then the reservation guard refused for want of credits - so the cap was never consulted and a PASS proved only the outermost gate (the 'survivor masked by a sibling constraint' shape). It now funds the seller and picks one already AT the cap by the cap's own arithmetic, so the deepest layer is the one that answers. Every persona runs inside BEGIN/ROLLBACK because docker exec psql is AUTOCOMMIT; verified to leave the ledger byte-identical and zero residue.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "credit-loop-closes",
        "script":  "tools/validate_credit_loop_closes.py",
        "args":    [],
        "label":   "THE CREDIT LOOP - cash enters once, and the credits go round. This is the central claim of the whole economy and the plan's verification #3, and every OTHER credit gate checks a rule in isolation: the cap binds, the reservation returns in full, earn-or-spend is exclusive, the supply cannot be exceeded. None of them asks the question the design exists to answer - can a provider fund a listing with credits they RECEIVED, without anyone paying in new cash? If the answer is no, this is not a circuit but a prepaid balance that drains one way, every provider must keep buying credits forever, and 'no revenue' quietly becomes 'revenue, collected as float' (Ian: 'I don't have to earn revenue, it is like, I hold the money I get, in a form of credits exchange'). Walks the REAL triggers and the REAL RPC at every step: one top-up funds seller A, A drafts and an ADMIN publishes (self-publish is refused and backend writes are exempt from the reservation guard, so a probe that inserts straight to 'published' as postgres creates no reservation and proves nothing), the reservation is HELD, the listing SELLS and grant_listing_reward hands that reservation to the BUYER, the buyer pays for a job with apply_credits_to_request and provider B RECEIVES the credits, and B then publishes a listing funded ENTIRELY by what B was paid. Also asserts CIRCULATION NEVER MOVES after that single cash entry - every step is a transfer between two wallets, so the ledger sum must be identical start to finish, and that is not hypothetical: apply_credits_to_request shipped writing only the payer's leg, which destroyed the credits and left the platform holding the cash that backed them while every per-side guard passed. Three of its own bugs are documented in-file, each a real class: a fixture seller sitting at guard_first_listings_need_a_sale's cap (the guard was right, the probe picked the wrong person); a job with NO agreed price, so the spend was asking for 10% of zero; and `set local role postgres` WITHOUT clearing request.jwt.claims, which is transaction-scoped in a DO block - the probe kept running as the buyer and blamed the wallet-privacy guard for refusing to show B's balance to someone else. TEETH: --inject spend (the buyer pays in pesos, so B is never funded) and --inject reward (the sale does not hand over its reservation, so the buyer has nothing to spend), each verified to break the circuit. Always rolled back; verified to leave zero residue.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "credit-posture",
        "script":  "tools/validate_credit_posture.py",
        "args":    [],
        "label":   "CREDIT POSTURE - the structural facts that keep WorkHive Credits out of the heavy regimes, asserted against the LIVE CATALOG instead of documented. Migration 20260803000012 stated that this gate asserts the posture; the file did not exist, so for a day the posture was a comment - which is exactly the failure the comment was written to prevent. Five facts, each one well-meaning feature away from disappearing: NON-WITHDRAWABLE (no function matching withdraw/cash_out/redeem_for_cash/payout_credits, because cash redemption is the prong that separates a closed-loop prepaid instrument from e-money, and the BSP moratorium on new VASP authorities in force since 1 Sep 2022 means that reclassification cannot be applied away); NON-TRANSFERABLE (guard_credits_non_transferable installed, so there is no person-to-person movement and therefore no secondary market - the SEC investment-contract framework weights resale most heavily, and no resale means no realizable gain); FIXED VALUE (1 credit = PHP1 exactly, so nothing appreciates and a Howey analysis has no expectation of profit to bite on); CAPPED SUPPLY (issued <= authorised, the ceiling on lifetime liability); and APPEND-ONLY BY PRIVILEGE (authenticated and anon hold no INSERT/UPDATE/DELETE on service_credit_ledger - load-bearing beyond bookkeeping, since a guard was once added to refuse hand-minted positive entries and it broke settlement while defending against something table privileges already made impossible). The named-trigger roster is the part that earns its keep: CREATE OR REPLACE on a guard function is harmless, but DROPPING its trigger silently un-enforces the rule and nothing else in the suite would notice. Its own first run reported two catastrophic failures on a perfectly healthy schema, because psql renders a raw boolean as 't' while the ::text cast in the query yields 'true' - the gate was wrong about the schema, not the schema about itself, so both spellings are now accepted. TEETH: --inject cashout|transfer|cap|rate|append|trigger, each verified to break precisely its own assertion and no other. Reads pg_proc/pg_trigger/information_schema via docker exec psql; skips when the DB is down.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "credit-circuit-sim",
        "script":  "tools/simulate_credit_circuit.py",
        "args":    [],
        "label":   "CREDIT CIRCUIT SIMULATION - do the economy's defaults still deserve their values? Every knob in the credit circuit was chosen from simulation rather than instinct, and TWO were set AGAINST the first instinct because the numbers contradicted it: reward_max_per_listing=500 (measured 4.9x throughput at scale versus a flat 10%, because a flat rate locks PHP2,500 on a PHP25,000 listing and exhausts the supply) and reward_min_per_listing=0 (a PHP200 floor was the MOST HARMFUL knob tested, 66%->38% healthy, since it makes a PHP500 listing reserve 40% of its own value). A design justified by a measurement has to KEEP being measured, or the justification quietly becomes folklore and the knob drifts to whatever felt right later. Seven assertions, each a claim the design rests on rather than a statistic: the per-listing cap raises throughput at scale; the starter grant raises cold-start throughput (the largest single gain, and cold-start failure lands on the DEMAND side as fill rate falling 51%->30%); hoarding raises cash demand ~71% WITHOUT stopping trade (which is why appreciation was dropped - a credit that grows is a credit you hoard); the holding fee separates spam from slow selling (PHP8 honest versus PHP2,400 for 50 junk listings a year); and the supply cap binds at ~20,000 providers. Two more join the economy to the MARKETPLACE, because the first five only measure whether providers can LIST, which is not the question a buyer asks: credit starvation shows up as UNSERVED BUYERS (half the providers starved costs double digits of fill - a provider inconvenience is a demand-side collapse, which is what makes the starter grant an intervention rather than a courtesy), and widening the broadcast radius 15km->30km moves fill by 20+ points (the largest single lever, and a knob we already own - it was INERT for months because UI hails carried no location so st_dwithin was skipped entirely). Only the DELTAS are asserted, never the absolute fill level: this model's baseline differs from the roadmap sweep's because the geography differs, and tuning constants until the number matched the document would be fitting the instrument to the answer it exists to test. TEETH: --inject cap|grant|fee removes a LEVER and --inject fill|radius removes a GATE from the matching chain, each verified to break precisely its own claim and no other - if assertion 6 survived with the credit gate gone it was never measuring starvation at all. It also guards against ITS OWN last bug - an early version reported a catastrophic industrial jam that did not exist, because int(1*0.5) truncates to zero so one-listing providers never sold; a pinned known-good case now fails if that regresses, and main() refuses to report results when the selftest fails. Pure arithmetic, no DB, fast.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "d9-knob-integrity",
        "script":  "tools/validate_d9_knob_integrity.py",
        "args":    [],
        "label":   "D9 KNOB INTEGRITY - the per-hive service knobs (migrations 20260731000007/8/9) must stay SAFE and stay READ. TIGHTEN-ONLY TRUST: per-hive trust thresholds are a forgery vector - a hive that could set gold@1 would mint its own gold sellers and the tier ladder would stop meaning anything platform-wide. The floors (silver >= 11, gold >= 51, gold > silver) are what keep a badge comparable ACROSS hives: a hive may make its OWN sellers work harder, never easier. Drop those CHECKs and the marketplace's headline trust signal becomes self-service, so this gate reads them from the LIVE CATALOG rather than the migration text - what matters is the constraint that IS on the table, not the one a file once declared. THE KNOBS MUST BE READ: a knob nobody reads is write-only configuration, and this was not hypothetical - the trust half shipped UNWIRED for an hour, with tier_silver_sales/tier_gold_sales existing, validated, and consulted by NOTHING but the resolver returning them. So each family must have a real consumer (sweep_service_broadcasts for timing/reach, recompute_seller_sales_and_tier for trust); service_knob() itself deliberately does NOT count, because it RETURNS a value rather than USING one and counting it would let write-only config pass. Its own first run reported a FALSE UNREAD on both families: prosrc is multi-line and psql returns one row per line, so it was reading only each function's first line - the code was right and the instrument was not, which is why the body is now flattened before matching. Self-test refuses to judge constraints on a table it cannot see (a missing table is a FAILURE, never a silent pass). Static catalog read via docker exec psql; skips when the DB is down.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "local-triggers-dont-call-prod",
        "script":  "tools/validate_local_triggers_dont_call_prod.py",
        "args":    [],
        "label":   "LOCAL TRIGGERS MUST NOT CALL PRODUCTION — found 2026-07-31 while extending the S9-knowledge layer. THREE AFTER-INSERT triggers on the LOCAL database (embed-logbook, embed-pm-completions, embed-skill-badges) called supabase_functions.http_request() against a PRODUCTION URL with a SERVICE-ROLE BEARER EMBEDDED IN THE TRIGGER DEFINITION. Three things were MEASURED, not assumed: the definitions carry the production host and a JWT (read from pg_trigger); net._http_response holds delivered responses including 200s from the same day, so pg_net is live here; and the DB container resolves the production host (getent returns an address). So every COMMITTED local insert into those tables POSTed the row to production — and seeding/manual testing hammer `logbook` — meaning dev data flowed into the production embedding store, the mirror image of [[feedback_live_mcp_writes_pollute_test_db]] and worse because embeddings are not obviously reversible. Separately a service-role key BYPASSES RLS ENTIRELY, and this one sat in the local catalog, readable by anyone with catalog access or a dump — a secret in a place secret scanners never look ([[feedback_pasted_keys_in_docs_leak_scanner_blindspot]]). CONTAINED the same session by DISABLING all three (ALTER TABLE ... DISABLE TRIGGER — local, immediate, reversible with ENABLE); they can no longer fire. The gate distinguishes ENABLED (a live outbound path → FAIL) from DISABLED (contained → reported, not failed) from the KEY still being present in the definition (reported every run regardless, because containment is NOT rotation and only Ian can rotate a production credential). The JWT is matched STRUCTURALLY (three base64url segments), never by the word 'service_role', which lives inside the ENCODED payload — the first cut searched for that word and reported clean on a definition that was carrying a key. Definitions are never printed. Self-test proves all four behaviours (enabled-external caught, disabled-external reported as contained not live, embedded JWT caught, clean local trigger accepted) and main() refuses to report if it fails. It also SKIPPED rather than false-passed when its own query errored on a `text || \"char\"` ambiguity — the cast is now explicit. Static catalog read via docker exec psql; skips when the DB is down.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "voice-paraphrase-invariance",
        "script":  "tools/validate_voice_paraphrase_invariance.py",
        "args":    [],
        "label":   "VOICE PARAPHRASE INVARIANCE (MR5) — a worker says the same thing three ways; voice-action-router must pick the same `kind` from its closed vocabulary (logbook.create | inventory.deduct | pm.complete | asset.lookup | query.ask | unknown) all three times. The transform is a PARAPHRASE, the invariant is the route. This is an MR and NOT an eval on purpose: there is no correct wording, and asserting 'this sentence must yield logbook.create' grades a free-tier model's mood ([[feedback_llm_parrots_fewshot_example_codes]]), so expected-kind agreement is REPORTED and only the relation is enforced. THE NON-VACUITY HALF IS THE WHOLE DESIGN, and it is sharper here than in MR1-MR4: a CONSTANT CLASSIFIER IS PERFECTLY INVARIANT — a router answering 'unknown' to everything would score 100% while being useless — so the gate also requires DISCRIMINATION, that the intents do not all collapse to one kind. Invariance alone would have been a metric that improves as the product gets worse ([[feedback_a_metamorphic_relation_needs_a_non_vacuity_check]]). Live and deliberately so: the DETERMINISTIC half of the router (kind allowlist, confidence clamp, slot-fill guard, asset disambiguation) is already locked by tests/voice-router-determinism.spec.ts against _shared/voice-router-core.ts — what no static test can see is the CHOICE the model makes, which is exactly what a paraphrase perturbs. Reuses validate_ai_live_invoke's plumbing (anon-key discovery, persona sign-in, RUNTIME hive derivation so it survives a reseed) instead of restating it. ~12 free-tier chain calls per run against a 50/hour limit. SKIPS rather than fails when the stack is down, the persona cannot sign in, or the model is unavailable — an infrastructure absence is not a product defect. First live run: 4/4 groups invariant, 4 distinct kinds, 4/4 also matching the humanly-expected kind. Banked as TB-MR5-paraphrase-invariance.",
        "group":   "AI Validation",
        "severity": "fail",
        "skip_if_fast": True,
    },
    {
        "id":      "paginated-order-totality",
        "script":  "tools/validate_paginated_order_totality.py",
        "args":    [],
        "label":   "PAGINATED ORDER TOTALITY — every query that pages or truncates must sort by a TOTAL order, i.e. end its ORDER BY with a column unique per row. Found live 2026-07-31 by metamorphic relation MR4 (rank stability), and PROVEN before it was fixed: marketplace.html browsed with `.order('created_at',{ascending:false}).limit(PAGE_SIZE)`, and created_at alone is not total because a bulk import inserts many rows inside ONE transaction where now() is FIXED, so they share an identical timestamp. Postgres promises nothing about the order of tied rows and it genuinely moves — touching an UNRELATED column on one tied row (an ordinary edit; MVCC appends a new tuple at the end of the heap) made 4 of 12 rows come back at a DIFFERENT RANK while the row SET was identical, and adding the unique id as a tiebreaker took that to 0. Under .limit()/.range() the user-visible failure is a row shown TWICE across a refresh or SKIPPED between pages; under .limit(1) it is quieter and worse — 'the most recent X' silently picks an ARBITRARY row among ties, the shape that once resolved the WRONG HIVE for a signed-in worker ([[feedback_resolving_live_is_not_enough_be_deterministic]]) — so limit(1) is IN scope, not exempt. Detection is conservative: a chain counts only when a .from() binds it (so .order()/.limit() on a plain JS array is never flagged), it has at least one .order('col'), and a .limit(/.range( closes it; a chain whose LAST ordering column is unique-by-construction passes. The unique-column list is declared WITH the reason each is unique (id/uuid = primary key; service_area = v_service_area_presence is one row per area, verified 4 rows / 4 distinct; co_number = unique by constraint), because a wrong entry silently converts a real violation into a pass — the same 'an exclusion must name its mechanism' discipline the guard mutation harness uses. Comments are stripped BEFORE scanning, with line numbers preserved: the gate's own first run flagged the very query the MR4 fix had just corrected, because a six-line explanatory comment between `.order('created_at')` and `.order('id')` pushed the tiebreaker outside the chain window — a gate that reddens on a CORRECT fix teaches the wrong lesson and gets worked around ([[feedback_teach_the_gate_not_bend_the_code]]), so that exact shape is now a self-test case. Self-test proves teeth four ways: a known-bad chain must be CAUGHT, a known-good one (id tiebreaker) must PASS, .limit(1) must be treated as pagination, and a tiebreaker separated from its .order() by a comment must still be seen; main() refuses to report a count if its own self-test fails, because a detector that silently stops detecting is this platform's recurring false-green. Forward-only ratchet over 162 known non-total orders across 38 files (169 paginated+ordered chains scanned) — a backlog named rather than averaged away, where the fix is always the same one line: end the ordering with .order('id'). Static file scan, fast, no DB.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "hive-capture-invariant",
        "script":  "tools/validate_hive_capture_invariant.py",
        "args":    [],
        "label":   "HIVE-CAPTURE INVARIANT — a load-time hive capture is only safe while nothing can change the active hive mid-page. marketplace.html does `const HIVE_ID = whHiveId()` once at load and stamps it on every hail and listing, so if the active hive could change while the page is open, work would be filed into the WRONG TENANT. It cannot today: the only pages that write `wh_active_hive_id` are hive.html (the switcher) and index.html (the sign-in bootstrap), and reaching the marketplace from either is a full page load, so the const is always read fresh. That was recorded as the A4 finding on `TB-A345-architecture-quality` — no defect — and it is a conclusion resting on an invariant nobody enforced: add an in-page hive switcher to the marketplace and every subsequent hail carries a stale tenant id with no test going red. Per this platform's disposition rule, a covered-by-nature cell still gets a gate asserting the contract it rests on. TWO ASSERTIONS: (A) the WRITER ALLOWLIST — exactly hive.html and index.html may write the key, and a third page appearing goes red with the instruction to re-check capture scope on every page that reads it, because adding a writer is a decision rather than automatically a bug; (B) THE CAPTURING PAGES MUST NOT WRITE IT — marketplace.html / marketplace-seller.html / marketplace-admin.html must contain zero writers, which is the live-bug condition, since a writer in the same document can run after the capture. CHECKING THE PREMISE CORRECTED THE FINDING TWICE, both errors mine: the cell claimed every write lives on hive.html (index.html:2953 writes it too — the conclusion survived, the stated premise was false), and index.html ALSO reads whHiveId() into a `const HIVE_ID`, which looks like the same stale-capture shape but is not, because that capture is FUNCTION-scoped inside the ops-home renderer (re-read per call) and the write is deliberately followed by `_initDashboard(...)` to re-render against the new hive — verified by reading both sites rather than trusting the comment. So the distinction that matters is SCOPE, not filename. The gate deliberately does NOT try to compute JS scope from a regex: brace and regex-literal counting over inline script is the brittle-validator trap this platform has already been bitten by, so it asserts the two cheap, unarguable properties instead. Self-test injects a writer into marketplace.html THROUGH THE READER (no file on disk is touched, the same discipline as injecting a mutant inside a transaction) and requires the gate to go red naming that page — without it, a green result would mean nothing. Static file scan, fast, always runs.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "push-runtime-delivery",
        "script":  "tools/validate_push_runtime_delivery.py",
        "args":    [],
        "label":   "PUSH RUNTIME DELIVERY (TB-S5 runtime tier) — the rung above push-handler-contract (which asserts sw.js STATICALLY) and below the OS notification tray (which no harness can read, and stays the honest residual gap). Runs tests/push-runtime-delivery.spec.ts: registers the worker, delivers a REAL push through the DevTools protocol ServiceWorker.deliverPushMessage — the same entry point the browser uses for a push from FCM, so sw.js's `push` listener runs its actual code path rather than a hand-called function — and reads the notification back off registration.getNotifications(). WHY IT EXISTS: building it found a LIVE PRODUCT DEFECT. svcEnablePush() in marketplace-seller.html did a bare `await navigator.serviceWorker.ready`, and `ready` resolves ONLY when an active registration exists for the scope. A repo-wide grep finds serviceWorker.register on exactly ONE page in the entire app (report-sender.html) and NOT on this one, nor in utils.js, nor any shared include. Measured live: getRegistrations()=0, controller=false, ready NEVER SETTLED. So a provider tapped 'Enable job alerts', granted the notification permission, and then nothing happened — no subscription, no toast, no error, no log, forever. The function's catch block is thorough about permission / VAPID / unsupported-browser failures, but `ready` does not REJECT, it simply never resolves, so the ONE failure that fires in practice was the only one the catch could not see. Fixed by registering the worker on that path and bounding the wait so a worker that will not activate says so instead of leaving a dead button. THREE CONTEXT SETTINGS the spec cannot work without, each a dead end that first looked like a product failure: serviceWorkers:'allow' (Playwright BLOCKS registration by default, replacing navigator.serviceWorker.register with a warning stub, so any SW assertion under a default context measures NOTHING); permissions:['notifications']; and channel:'chromium' — the deciding one, because the BUNDLED headless Chromium has no notification platform bridge, so the permission cannot be granted at all and getNotifications() is always empty, which is the exact symptom of a handler that renders nothing. Probed both ways: default headless -> perm 'denied', count 0; Chrome's new headless -> perm 'granted', count 1. I was one step from recording that as an un-probeable ceiling; it was a one-line config. Invoked via `node node_modules/@playwright/test/cli.js` (this project's path contains an '&' the npx shim mis-parses), and 'No tests found' is reported as the gate's OWN invocation failure, never as a broken product. skip_if_fast: browser minutes.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "push-handler-contract",
        "script":  "tools/validate_push_handler_contract.py",
        "args":    [],
        "label":   "PUSH HANDLER CONTRACT — a delivered push must actually reach the person. FOUND 2026-07-30 while dispositioning the test bank's TB-S5-edge-push-delivery-roundtrip cell, whose note read 'nothing proves a real subscription RENDERS the notification'. Checked rather than assumed: the SEND side is well covered (notify-push is VAPID-signed and was proven E2E against the real FCM service) and TWO service-worker validators already exist — validate_sw_offline and validate_sw_shell_membership — but NEITHER mentions push, showNotification or notificationclick even once. The receive side had no gate at all. That gap matters because every failure mode is SILENT, and sw.js says so itself: 'Without these handlers a delivered push silently no-ops.' The push arrives, the worker wakes, nothing appears, and nothing errors anywhere a person or a log would see it — a provider just misses the job. SEVEN invariants, each a silent failure: (1) a `push` listener exists (else the push is delivered and discarded); (2) the handler calls showNotification (else the worker wakes and renders nothing); (3) that call is INSIDE event.waitUntil — brace-matched, and the check requires waitUntil to WRAP it rather than merely appear in the handler, because a bare showNotification works on a warm worker and fails on a cold one, which is the hardest kind of bug to reproduce; (4) e.data.json() is guarded by try/catch, since a non-JSON payload THROWS and one malformed push then silently suppresses the notification; (5) a `notificationclick` listener exists (else the tap does nothing); (6) it focuses a tab or opens one (else the notification is a dead end); (7) it closes the notification (else the tray keeps an item the user already acted on). Static by design — these are properties of the file that ships to installed PWAs, and the CACHE_NAME comment records the cost of getting it wrong: 'without this bump installed PWAs keep the handler-less worker'. WHAT THIS DELIBERATELY DOES NOT CLAIM: the OS notification tray is outside any harness, so 'the human saw it' stays unprobeable; the runtime tier (dispatch a push event to a registered worker and assert showNotification fired) remains recorded as owed with this gate named as its first half, so the residual gap has a shape instead of a shrug. Self-test proves all 7 hold on the shipped worker AND that five deliberately broken workers are each caught.",
        "group":   "Platform",
        "severity": "fail",
    },
    {
        "id":      "unprotected-write-grant",
        "script":  "tools/validate_unprotected_write_grant.py",
        "args":    [],
        "label":   "UNPROTECTED WRITE GRANT — no end-user write privilege may stand on the GRANT alone. FOUND 2026-07-30 by teaching the marketplace test bank's SQL runner the `anon` partition: it had enumerated 18 anon obligations and executed NONE, because the runner needed a uid to mint and `anon` is not a different identity, it is the ABSENCE of one (`set local role anon`, no JWT). On the marketplace surface RLS held everywhere — anon moved 0 rows on insert/update/delete and the service tables refuse at the GRANT layer (42501) before RLS is consulted. The finding was one level up, in 'where is RLS not there to catch it?': 16 public tables with relrowsecurity=false that granted anon INSERT/UPDATE/DELETE/TRUNCATE. A rolled-back `delete … where true` as anon destroyed 1,430 ROWS — 766 embedding_cache, 434 persona_knowledge, 207 multilingual_terms, the 3 service_slo_targets the gate panel grades against, and the single ai_global_budget row that caps AI spend (delete that and either the AI chain stops for everyone or, if any reader treats a missing row as 'no cap', the spend ceiling silently disappears). WHY THE CLASS HIDES: the grants are the Supabase template default (GRANT ALL … TO anon, authenticated), harmless on the ~130 tables where RLS is on and catastrophic on these 16 — the same grant, and the only difference is one line of DDL somewhere else. So the invariant is a CONJUNCTION, never a grant rule: a base table may grant a write verb ONLY IF row-level security is enabled on it. Corollary: no view may grant one — 13 v_* views were auto-updatable with anon write and 11 were not security_invoker, the textbook bypass setup (a write through a non-invoker view runs as the view OWNER, postgres, and an owner is RLS-exempt when forced=false); PROBED, it did not bypass (0 rows, and even SELECT returned 0), recorded as measured rather than reasoned, and revoked anyway because a repo-wide grep finds zero writes through any v_* view. TRUNCATE is the verb RLS would never have caught — RLS does not apply to TRUNCATE, and anon TRUNCATE on marketplace_listings fails today only with 0A000 'referenced in a foreign key constraint', a coincidence of the schema that evaporates the day that FK is dropped. Fixed by mig 20260730000002; this gate stops the next GRANT ALL or the next table created without ENABLE ROW LEVEL SECURITY from re-opening it silently. Runs in --fast (two catalog queries), skips cleanly if the stack is down, and its self-test PLANTS the defect and requires detection.",
        "group":   "Security",
        "severity": "fail",
    },
    {
        "id":      "marketplace-test-bank",
        "script":  "tools/validate_marketplace_test_bank.py",
        "args":    [],
        "label":   "MARKETPLACE TEST BANK — SQL lane (MARKETPLACE_DEEPWALK_EXPANSION_ROADMAP §10). The bank's denominator is DERIVED from the four guard functions by tools/derive_transition_matrix.py — each guarded transition names its authorised actor, so the obligations compute (212: 22 positive · 102 authority-negative · 88 sneak-path) and a transition added by a later migration grows the denominator BY ITSELF rather than silently not existing. WHY THIS EXISTS: deriving it exposed that of 22 authorised transitions only 4 had any gate — every existing gate was written AFTER a security finding, so they all encode 'this must not happen' and NOTHING asserted 'this must still work'. That asymmetry is how a guard tightened one notch too far ships green (this platform watched pm_assets go supervisor-only and two gates redden for the old permission, not a real break). This lane proves BOTH directions: the authorised actor CAN fire each transition (expect 1 row) and every other actor is refused (exception or 0 rows). SQL altitude on purpose — a transition's legality is a database fact, and proving it through a browser would buy nothing and cost determinism (Google: UI tests are the most flake-prone, and flakiness masks real bugs). Every check runs inside begin/rollback with SELF-MINTED probe identities: zero pollution, no dependence on seeded state. Fixtures mirror reality — a request in an ACTIVE state always has an on_job provider, because accept_service_request sets that under the workhive.service_system_write bypass; planting 'online' instead tests an unreachable world and looks exactly like a product bug. Self-test proves teeth in both directions. Skips if docker/psql is absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "service-triage-eval",
        "script":  "tools/validate_service_triage_eval.py",
        "args":    [],
        "label":   "SERVICE TRIAGE EVAL (§10 S7-ai) — the AI's suggestion must be something the PRODUCT CAN USE. The one layer of the marketplace test bank that cannot have an exact-match oracle: asking a free-tier LLM to return one specific string every time tests the model's mood, not the product. So the oracle is a RUBRIC — never 'did it say Plumbing', always 'is what it said in the vocabulary'. WHY THAT IS THE INVARIANT THAT BITES: marketplace.html applies a triage suggestion by finding the matching <option> (`sel.options.find(o => o.textContent.startsWith(t.category))`), so an out-of-vocabulary answer — 'Plumbing Services', 'Aircon Repair', 'urgent' — finds nothing, sets nothing and reports nothing; the person presses Ask AI, watches the form not move, and cannot tell whether the AI failed or simply had no opinion. Nothing throws. Same shape as the `urgency = 'emergency'` branch this session found DEAD in the push fan-out (mig 20260729000017): a value written against a vocabulary that does not exist. The catalog categories and the urgency/mode CHECK vocabularies are read from the DATABASE every run, so a new category is graded against without touching the gate. An ABSENT field is legitimate ('no clear match' is honest). A rate-limited or unreachable chain is a SKIP, never a PASS — green there would mean the feature is down. Driven through supabase-js (tools/service_triage_capture.mjs), the page's own client path, because the gateway resolves its caller via auth.getUser() and a raw HTTP POST tests the auth wall instead of the agent. Self-test proves the grader in five directions.",
        "group":   "AI Validation",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "payment-disclosure",
        "script":  "tools/validate_payment_disclosure.py",
        "args":    [],
        "label":   "PAYMENT DISCLOSURE (§10 MK8) — while the platform holds no money, it must SAY so, AT the money step. WorkHive's marketplace is contact-only: PAYMENTS_ENABLED=false, no escrow, the buyer arranges payment with a stranger off-platform — so 100% of the money risk sits at one moment, the inquiry that takes the conversation off the site. RA 11967 (the PH Internet Transactions Act) requires consumer education on the red flags in internet transactions, and silence lets a buyer assume protection by default, because the default assumption everywhere else online is that the platform holds the money. TWO INVARIANTS: (1) the money step states that WorkHive NEVER HOLDS the payment, states the consequence (it therefore CANNOT REVERSE it), and gives a concrete precaution — 'payment is off-platform' states a fact but does not warn; (2) no buyer-facing copy promises buyer/purchase protection, a money-back guarantee, held funds or escrow — promises the switch makes false. Checked as CLAIMS rather than one exact sentence, so the wording stays free to improve while the substance may not quietly drop out. <script> and HTML comments are stripped first: a reassurance living in a comment reassures nobody. marketplace_orders DOES carry an escrow-shaped vocabulary (pending_payment/escrow_hold/released/refunded) for a flow that is switched off — fine as schema, which is why the gate searches PROSE and not status strings. SKIPS entirely if PAYMENTS_ENABLED flips to true: these invariants describe a different product then and must be re-derived, not silently kept. Self-test proves all four directions including the comment-only case.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "lifecycle-state-reachability",
        "script":  "tools/validate_lifecycle_state_reachability.py",
        "args":    [],
        "label":   "LIFECYCLE STATE REACHABILITY (§10 MK4) — every state in a `status` CHECK must be both SURFACED and REACHABLE. A CHECK vocabulary is a promise about what the product does, and it breaks two ways, silently in both directions because unreachable code raises nothing. FOUND 2026-07-29: `marketplace_listings.sold` is fully dressed — a blue 'Sold' chip, a `.listing-item.sold` border rule, a schema.org/SoldOut mapping in the runtime JSON-LD, and `recompute_seller_sales_and_tier` COUNTS listings WHERE status='sold' to drive the seller's tier — and NOTHING anywhere writes it. A seller who sells something cannot say so, and the tier that counts sales counts a number that is always zero. (The mirror case is already recorded as a schema question for Ian: the same table carries moderation_reason/moderated_at/moderated_by with no `rejected` in its vocabulary — a consumer with no state.) THE PRODUCER TEST UNIONS THREE SIGNALS because none alone is right: the DERIVED guard matrix (a guard's allow-list only covers USER writes, missing `accepted` via the workhive.service_system_write bypass and `expired` via the sweep cron), a DB assignment (`SET status =` / `status :=` ONLY — matching `status = 'x'` anywhere counts WHERE-clause READS as writes, which produced a FALSE PASS on the one real finding twice in a row), and a page write including the literal passed as an ARGUMENT (the seller advances a job through `svcAdvance(id, next)`, so a payload-only test calls seven wired states unreachable). Forward-only: the known gap is NAMED in lifecycle_reachability_baseline.json and still prints every run; a NEW unreachable or invisible state FAILs. Self-test proves both baseline directions.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "jsonld-truth",
        "script":  "tools/validate_jsonld_truth.py",
        "args":    [],
        "label":   "JSON-LD TRUTH (§10 MK7) — RUNTIME structured data may not claim more than the canonical row. The registered `seo-technical` gate reads JSON-LD WRITTEN INTO the HTML (retired @types, malformed blocks); it cannot see structured data BUILT AT RUNTIME from database values — and that is exactly where the claims with something at stake live: a star rating and a listing count are what a buyer sees in the search result before they ever reach the page. Two failure modes, both reasoned about correctly in marketplace-seller-profile.html#injectJsonLd and neither of them locked until now. (a) A FALSE STAR RATING: an `aggregateRating` gated on the AVERAGE ALONE ships a rating for a seller with no reviews — an average can be non-null with zero reviews, which is precisely how a trust signal ends up standing on nothing, and Google treats a fabricated AggregateRating as a structured-data violation, so it is a trust lie AND a ranking penalty. The gate requires the claim to sit behind a review COUNT. (b) STRUCTURED DATA FOR A MISSING ENTITY: a Person schema on a page whose own body says 'Seller not found' is a claim the page contradicts — the emitter must bail on its subject's absence. Also catches a truncated `itemListElement` shipping alongside a `numberOfItems` that claims the full total. Balanced-brace extraction, static/fast. Self-test proves all four directions.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "gate-panel-honesty",
        "script":  "tools/validate_gate_panel_honesty.py",
        "args":    [],
        "label":   "GATE PANEL HONESTY — the surface that REPORTS the gates was itself watched by nothing, and it had already failed twice. (a) G2: the failing-gate list printed each gate's `label`, prose written for the engineer reading run_platform_checks.py, carrying file paths and roadmap filenames straight into user-facing chrome. (b) TRUNCATION, found 2026-07-29 by this gate's own hunt: the regressions block rendered `regs.slice(0, 6)` with NO overflow line — and a regression is the highest-severity signal the console carries, a gate that was PASSING and now is not. Everything past the sixth vanished, and a truncated list with nothing after it reads as 'that is all of them'. Three invariants: the visible text is the short id-derived name; the full label survives as the tooltip so shortening is not hiding; and any list that truncates DISCLOSES the remainder. Static on founder-console.html because the claim is about rendered text — a browser walk would add flake and prove the same thing. NOTE: this gate reddened TWICE on correct page code while its own detector was wrong (a regex spelling out the title-attribute quoting, then a .find() that anchored on the CSS rule instead of the render site); both were fixed in the detector, never by bending the page.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "offline-write-guard",
        "script":  "tools/validate_offline_write_guard.py",
        "args":    [],
        "label":   "OFFLINE WRITE GUARD — a user-triggered write must REFUSE before it fires, not after. Found 2026-07-29 while building the test bank's S2-pwa cell: marketplace.html already had svcRequireOnline() ('You're offline. <action> needs a connection - nothing was sent, so nothing is half-done.') wired into exactly TWO of its seven client writes. The other five fired into a dead network — svcSettle ('I paid', money, no idea whether it landed), svcApplyVoucher, svcPickQuote, svcPickStar, and svcClientCancel, whose confirm dialog made the person commit to cancelling before failing opaquely. marketplace-seller.html was worse: svcAccept reasoned about the offline race in a comment and the other NINE writes had nothing, including svcFileTopup where the provider may have ALREADY sent the GCash payment. WHY THE EXISTING GATES MISSED IT: degraded-state-central proves every page adopts the offline BANNER — and both pages did, the whole time; a passive warning does not stop a button. offline-queue-confirm proves a drain never calls 0 rows 'synced' — but these surfaces deliberately do NOT queue, because a queued accept would claim a job someone else already took. Adoption of a warning and correctness of a drain both passed while every individual write was unguarded. This gate asserts the third thing. Balanced-brace function extraction (not a fixed window), only user-invokable functions (window-attached or inline handler), read-only RPCs exempted BY NAME, and the check's POSITION is verified — a guard after the write is a post-mortem. Self-test proves all four directions including the order case.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "marketplace-bank-journey",
        "script":  "tools/validate_marketplace_bank_journey.py",
        "args":    [],
        "label":   "MARKETPLACE TEST BANK — journey lane, TWO BROWSER CONTEXTS (§10). Runs tests/marketplace-bank-two-context.spec.ts: two independent Playwright contexts holding two real Supabase Auth sessions, one shared row of truth between them. WHY IT EXISTS: every other spec in this repo drives ONE identity, which is why the marketplace's two-sided journeys were never end-to-end — J29 (live map) was walked by P-client-supervisor and P-client-worker, two WATCHERS and zero PUBLISHERS, and still satisfied the arc's '>=2 personas' rule because the rule counted personas instead of naming the SIDES. A handoff between two parties cannot be proven from one browser. The cell: the client presses the real 'Track provider' control, the matched provider's own seller page publishes via watchPosition from its own session (the run logs which path fired), and the client's map must repaint at the NEW coordinate within two 10s poll cycles with no reload. The SQL half — publisher writes, watcher reads, stranger reads 0 rows — is already banked separately; this lane asserts only what SQL cannot, because a silent tracking failure is indistinguishable from a provider who has not moved. Teeth are PROVEN, not assumed: --selftest sets WH_TB_FREEZE=1 to park the publisher at its original position and requires the watcher assertion to go RED. Invoked as `node node_modules/@playwright/test/cli.js` because this project's path contains an '&' that the npx shim mis-parses, and 'No tests found' is reported as the gate's own invocation failure rather than as a broken journey. skip_if_fast: browser minutes, while the SQL lane stays in --fast.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "service-idempotency",
        "script":  "tools/validate_service_idempotency.py",
        "args":    [],
        "label":   "C14 (SERVICE_HAILING_ROADMAP §4b) once-only on the money/dispatch paths. The roadmap originally asked for an `Idempotency-Key` header contract; reading the code first showed the platform already does something STRICTLY STRONGER — partial UNIQUE indexes enforce once-only in the DATABASE, so the guarantee holds even when the client sends no key, the wrong key, or is a script hitting PostgREST directly. 8 invariants: the four indexes exist (one commission per request · one top-up per GCash ref · one offer per provider+request · one open PM auto-hail) AND still BITE (a partial predicate can be narrowed to uselessness without dropping the index, so presence alone is not proof — each replay is attempted live in a rolled-back transaction and must be REFUSED), plus the duplicate must be reported HONESTLY ('That reference number is already filed'), never as raw 23505 text a user reads as failure and re-sends. Skips if docker/psql is absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "slo-budget",
        "script":  "tools/validate_slo_budget.py",
        "args":    [],
        "label":   "C15 (SERVICE_HAILING_ROADMAP §4b) the arc's north-star metrics are MEASURED, not adjectives. D9 named allocation rate / time-to-accept / completion rate and never gave them a number; `v_service_slo` now computes each against a target stored as tunable DATA (`service_slo_targets` — changing a target is Ian's business call, never a migration). Asserts every SLI has a numeric target + valid comparator, computes from live data, and reports an empty denominator as NULL ('not measurable yet') rather than a fabricated 0 that would read as a breach. Also asserts the AI-chain breaker is INTACT (recordSlotFailure parks a failing model/provider, isSlotBlocked skips it, Retry-After is honored) — validated as already-present, not rebuilt. DELIBERATELY does NOT fail on a BREACHED SLO: a breach is a business signal, and a gate that reds for non-code reasons is exactly what gets excluded — which is how 9 cron jobs stayed dead behind a 'reasonable' exclusion for weeks. Breaches print loudly; exit stays 0.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "outbox-delivery",
        "script":  "tools/validate_outbox_delivery.py",
        "args":    [],
        "label":   "C12 (SERVICE_HAILING_ROADMAP §4b) durable side-effects: a boundary-crossing effect must survive a consumer that is down. 12 live invariants over `service_outbox` + the `FOR UPDATE SKIP LOCKED` relay: schema/RLS/no-client-grant · enqueue+drain+reconcile are EXECUTE-revoked (a DEFINER helper is user-callable BY DEFAULT — this arc already shipped one live IDOR from that) · enqueue is a plain INSERT so it commits iff the transition does (an HTTP call inside the transition tx is the coupling the outbox removes) · a failed delivery is pushed OUT in time, not retried hot · a payload past max_attempts DEAD-LETTERS instead of looping · nothing is stranded 'in_flight' · SOMETHING ACTUALLY CALLS THE FAN-OUT · and a real enqueue→relay→2xx→done round-trip, minted and swept by the gate itself (residue is not evidence). WHY THE BLUNT ONES: Web Push shipped fully built and NEVER INVOKED — subscriptions, sw.js handler, VAPID, subscribe UI and notify-push all existed while no caller did, so providers subscribed and received nothing. 'Built but never called' must never be green again. Skips if docker/psql is absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "star-returning",
        "script":  "tools/validate_star_returning.py",
        "args":    [],
        "label":   "A bare .select() after a write is `select *`, and a star RETURNING on a table with a column-level SELECT revoke fails 42501 *after the row has already committed* — a PHANTOM WRITE: the user is told the create failed while the row is there, inviting a retry that makes another one. FOUND LIVE 2026-07-29: project-manager.html ended both create paths with .insert(payload).select().single() while projects.budget_php is supervisor-only (migs 024/030), so project creation was wholly broken AND leaking orphan rows. FAILs a star read OR write on any table that has a SELECT-revoked column; WARNs bare-select writes on other tables as latent debt (they become the same bug the day a column there is revoked). Reads the grant catalog LIVE rather than hardcoding the table list, so a FUTURE revoke arms the check automatically. Backup mirrors (dot-dirs) are excluded — they are not served. Skips (exit 0) if docker/psql is unreachable.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "crud-rollback",
        "script":  "tools/validate_crud_rollback.py",
        "args":    [],
        "label":   "Per-page P3 CRUD-at-DB gate for SIDE-EFFECT tables (LIVE, rolled-back psql) — 9 tables (logbook / inventory_items / resume_documents / marketplace_listings / ai_reply_feedback / asset_nodes / worker_profiles / resume_versions / report_contacts) that can't go through `page-crud` because a persisted INSERT fires expensive/stateful triggers (embed http_request, achievement XP, rate-limit, daily-cap, approval-guard). Runs a single WORKER-JWT transaction per table (INSERT with FORGED worker_name -> assert PINNED by bind_*_submitter where applicable + correct hive_id/auth_uid -> own-scoped UPDATE [or NO-OP for immutable/update_noop tables like resume_versions] -> own-scoped DELETE) then ROLLS BACK (undoes the row AND the after-commit side-effects = 0 pollution). Data-driven (each table emits its own pin/hive/auth booleans in SQL). Modes: default (pinned) / immutable (ai_reply_feedback: update+delete no-op) / update_noop (resume_versions: no UPDATE policy but own-DELETE works) / update_only (worker_profiles: create-ONCE non-deletable identity row — gates a forged-auth_uid insert is RLS-blocked + own-UPDATE lands, no delete round-trip by design). Bug-hunt roadmap P3, 2026-07-19..22 (asset_nodes closes asset-hub P3, worker_profiles closes index P3 — P3 axis now 100 platform-wide). A dropped bind trigger / broken own-write RLS / lost hive_id FAILs. Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "role-gate-server-backstop",
        "script":  "tools/validate_role_gate_server_backstop.py",
        "args":    [],
        "label":   "P5 UI-role-gate server-backstop gate (LIVE) — the LAST P5 sub-property: several pages source HIVE_ROLE from localStorage (tamperable) and hide supervisor-only actions on `HIVE_ROLE==='supervisor'`/`WHRoles.isSupervisor()`. That is safe ONLY because the server independently enforces supervisor/admin on every such write — so a worker who tampers localStorage sees a button the server still rejects (42501), not a privilege escalation. This gate LOCKS that invariant: every table written behind a supervisor UI gate (asset_nodes/rcm_fmea_modes/rcm_strategies/inventory_items/shift_plans/integration_configs/hive_retention_config/api_keys/sso_configs/marketplace_sellers) MUST carry a server-side supervisor enforcement — an RLS write policy referencing the supervisor role / is_marketplace_admin, a `tg_guard_approval` trigger, OR a fully write-locked (`WITH CHECK false`, service-role-only) policy. A FUTURE page adding a client-only supervisor gate on an un-backstopped table = the UI-only-auth privilege-escalation class found+exploited live 2026-07-07 (asset_risk_scores), and this FAILs it. Curated list (adding a supervisor-privileged write is a conscious security decision that updates it). Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "input-validation-guard",
        "script":  "tools/validate_input_validation_guard.py",
        "args":    [],
        "label":   "P4 client input-validation gate (static teeth) — every write-submit handler that reads a USER-TYPED field (getElementById().value / .trim()) and issues a `db.from().insert/upsert/update` or `db.rpc` mutation MUST validate that input BEFORE the write, via EITHER a runtime capture contract (whValidateCapture) OR a pre-write validation guard (an if→return branch and/or an error surface: showFormError/showToast/errEl/.textContent/.focus/classList error). A form that POSTs raw user input with no guard leans entirely on the server and shows a confusing raw PostgREST error. REAL bug locked 2026-07-21: project-manager.html saveProgressLog inserted log_date (date NOT NULL) + pct_complete (smallint 0..100 CHECK) straight from the inputs → an empty date / out-of-range % produced a raw 22007/23514; fixed with a friendly pre-write guard. Server-derived writes (no user field) + read-only .select handlers are excluded. This closes the LAST P4 sub-property (client empty/format) not already gated (XSS: innerhtml-eschtml/dom-xss-fields; server bounds: text-caps/numeric-bounds; duplicate-submit: double-submit-lock). Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "double-submit-lock",
        "script":  "tools/validate_double_submit_lock.py",
        "args":    [],
        "label":   "P7 double-submit lock gate (static teeth) — every `getElementById('...').addEventListener('click', H)` bound to a WRITE handler H (name submit/save/confirm/create/add/send/publish/approve/reject/delete OR the body issues a `db.from().insert/upsert/update/delete` / `db.rpc` write) MUST be single-flight-locked, EITHER by wrapping the binding in `withButtonLock(this, H)` OR by H disabling its button before the await. A bare unlocked click->write binding = a double-tap fires the write TWICE (PRODUCTION_FIXES #47). REAL bugs locked 2026-07-21: inventory.html submitUse/submitRestock/submitPart (bare -> the non-idempotent inventory_deduct/restock RPC = a DOUBLE stock deduction) now wrapped in withButtonLock; logbook.html submitAsset (bare -> 2nd tap 23505s the (hive_id,tag) unique index) now self-disables. Read-only handlers (all db.from().select) + JS Set/Array .delete()/.update() are excluded (Map.size class). Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "p6-concurrency-class",
        "script":  "tools/validate_p6_concurrency_class.py",
        "args":    [],
        "label":   "P6 concurrent-edit disposition gate (LIVE + static teeth) — locks the concurrency-safety CLASS of the 9 remaining P6-partial pages so each reaches gated-100, complementing oc-updated-at-backed (OC-guarded: inventory/pm-scheduler) + readonly-p6-no-edit (12 read-only). Classes + load-bearing DB invariant each asserts: (a) idempotent-upsert (skillmatrix skill_profiles onConflict worker_name · marketplace-seller marketplace_sellers · dayplanner schedule_items) — full-object upsert on a UNIQUE-index-backed key => two concurrent writes CONVERGE to 1 row, no partial lost-update (verified: index exists + no read-modify-write delta on the table + a LIVE rolled-back double-upsert converges to 1 row w/ 2nd value); (b) owner-scoped-update (resume resume_documents · marketplace marketplace_saved_searches · marketplace-seller marketplace_inquiries · voice-journal worker_profiles) — UPDATE RLS is own-identity/party-scoped (references auth.uid()/auth_worker_names(), not true) so no CROSS-user lost-update; (c) forward-only-status (shift-brain shift_plans) — a forward-only status trigger blocks a concurrent regress; (d) create-once-insert (index worker_profiles, unique auth_uid). Also static-asserts each page still contains its declared write shape, so a class change (e.g. a page adds a read-modify-write delta) trips the gate for re-scoring. Bug-hunt roadmap P6, 2026-07-21. Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "empty-state-discrimination",
        "script":  "tools/validate_empty_state_discrimination.py",
        "args":    [],
        "label":   "DEEPWALK D3 gate (static teeth) — a list render that owns BOTH a first-run empty-state ('No entries yet — log your first X' CTA) AND a search no-results ('nothing matched your filters') must route a 0-result SEARCH to no-results, NEVER to the first-run empty-state. For a SERVER-filtered view (the list var IS the query result => `filtered === entries`) an early `if (entries.length === 0){ show empty-state; return }` fires FIRST and makes the no-results branch DEAD, so a supervisor who searched the whole TEAM feed and got 0 rows sees 'Log your first repair' (a nonsensical CTA). REAL bug found + fixed live 2026-07-22 in logbook.html renderEntries (the length-0 branch is now view-aware: team->no-results, mine->empty-state). Client-filtered views (inventory `filtered = items.filter(...)`) are correct and out of scope. Curated list of server-filtered search views; adding one is a conscious UX decision that must extend the gate. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "qty-input-contract",
        "script":  "tools/validate_qty_input_contract.py",
        "args":    [],
        "label":   "DEEPWALK D5 gate (static teeth) — the 'number input's declared min/step/bounds are NOT enforced on the write path' class, across TWO central parsers. (a) QTY: a `<input type=number min=1 step=1>` in a modal that submits via a BUTTON (not a native <form>) never has min/step enforced; a bare `parseFloat()||0` lets 2.5 / 1e-9 through => a fractional/absurd stock deduction (inventory submitUse/submitRestock). (b) PRICE: the marketplace post/edit forms are `novalidate`, so min=0 is unenforced; an unvalidated negative hit the DB price_nonneg CHECK (raw 23514) and an over-precision value hit numeric(14,2) overflow, both cryptic to the seller (marketplace handlePostSubmit / marketplace-seller handleEditSubmit). REAL bugs found+fixed live 2026-07-22. FIX (central, METHOD LAW): window.whParseQty (integer qty) + window.whParsePrice (currency: blank=negotiable, 0=free, 2dp, ₱10M cap) in utils.js; every curated number-write handler adopts the right one. Gate locks: both helpers exist + window-exposed, every handler calls its helper, and none has a bare parseFloat/Number inline read (a naive revert). Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "refresh-retry-dedup",
        "script":  "tools/validate_refresh_retry_dedup.py",
        "args":    [],
        "label":   "DEEPWALK D2 gate (static teeth) — a NON-idempotent client write (a fresh-id INSERT or a decrement/increment RPC) has no idempotency key, so a refresh-mid-submit then retry creates a DUPLICATE / double effect (the button-lock only stops a same-page double-tap; a refresh spawns a fresh page that bypasses it). REAL, live-confirmed 2026-07-22: logbook addEntry (fresh Date.now() id => dup entry), inventory submitUse (inventory_deduct AGAIN => DOUBLE stock deduction), submitRestock (double restock), marketplace handlePostSubmit (dup listing). FIX (central, METHOD LAW): window.whRecentDuplicate(db, table, matchObj, {windowMs, tsColumn}) in utils.js queries for an identical recent row (tight window + specific match => no false-block; best-effort, never blocks a write on error); each handler calls it BEFORE the write and skips on a hit. Live-verified: retry dedups, different value not matched. Gate locks: the helper exists + window-exposed + every curated non-idempotent write calls it. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "optimistic-input-restore",
        "script":  "tools/validate_optimistic_input_restore.py",
        "args":    [],
        "label":   "DEEPWALK D4 gate (static teeth) — a chat/message send that clears its input OPTIMISTICALLY (`input.value=''` BEFORE the async turn, to show the user bubble immediately) MUST restore it on the failure path, else a 429/timeout/network error WIPES the user's typed question even though the error says 'try again' (forcing a full retype). REAL bug found+fixed live 2026-07-22 (assistant.html sendMessage): the catch now does `if(!input.value.trim()){ input.value = text; }` — one-tap retry, guarded so it never clobbers a NEW question typed during the wait. Sibling sweep: community submitReply clears AFTER success (correct, N/A). Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "cmms-import-rollback",
        "script":  "tools/validate_cmms_import_rollback.py",
        "args":    [],
        "label":   "P3 gate for integrations.html's bulk CMMS import (LIVE rolled-back SUPERVISOR-JWT psql + static teeth) — the last deferred P3-write frontier, closed 2026-07-21 with 4 REAL fixes it now locks: (1) normalizeRow STATUS CLAMP (an unmapped raw code passed through as-is 23514-killed the WHOLE 500-row chunk via external_sync.status CHECK; a mapped 'Cancelled' killed the logbook insert via logbook_status_check); (2) NO client fault_knowledge write (client-INSERT-LOCKED since mig 20260513000003; the import's direct insert silently failed for ~2 months and would have poisoned the RAG index with unembedded duplicates — knowledge flows via embed-logbook trigger -> embed-entry edge fn); (3) ERROR-CHECKED batches (supabase-js does not throw, the old try/catch was dead code and failed chunks counted as imported); (4) inventory_items.id text-NOT-NULL-no-default -> import inserts 23502-failed (mig 20260721000001 gives the 3 opaque text-id tables a DB default — the Arc-K logbook.id class closed platform-wide). Live half mirrors all 4 entity batches in the page's exact shapes (upserts run TWICE = re-import idempotency; attribution pins asserted; duplicate-guard read via v_external_sync_truth; cmms_audit_log write) then ROLLS BACK = 0 pollution. Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "onconflict-index",
        "script":  "tools/validate_onconflict_index.py",
        "args":    [],
        "label":   "Per-page P3/P4 gate: every supabase-js `.upsert(rows,{onConflict:'a,b'})` in the page HTML must have a MATCHING unique index in the live DB (Postgres ON CONFLICT arbiter inference is set-based, order-insensitive). ROOT BUG locked (found live 2026-07-19): integrations.html's CMMS inventory import upserted `inventory_items` with onConflict:'part_number,hive_id' but NO such unique index existed (only the pkey) -> Postgres threw 'no unique or exclusion constraint matching the ON CONFLICT specification' at RUNTIME = the supervisor's inventory import CRASHED; the same gap left submitPart's client-only 'already exists' check a double-submit RACE. Fixed by migration 20260719000001. Parses the (possibly multi-line) `.from('t').upsert(...onConflict)` and set-matches against pg_index. A new onConflict without a backing unique index FAILs before it ships. Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "marketplace-trust-integrity",
        "script":  "tools/validate_marketplace_trust_integrity.py",
        "args":    [],
        "label":   "Per-page P5/P6 marketplace SELLER-TRUST forge lock (LIVE, rolled-back psql as a real authenticated worker). The marketplace runs on the seller trust signal (rating_avg/rating_count/total_sales/tier shown in search/community/seller-profile/schema.org). Two live-found self-dealing vectors: (A) FAKE SALES — `trg_seller_tier` bumps total_sales on a marketplace_orders `status->released` transition, and RLS let a buyer self-insert an order naming ANY seller then jump status straight to released (no escrow/payment) -> +1 fake sale + tier promotion; locked by guard_marketplace_order_status (mig 20260719000002: a JWT client cannot set status released/refunded). (B) FAKE REVIEWS — `update_seller_rating` averaged ALL reviews with no verified_purchase filter, and RLS let a worker self-insert a 5-star verified_purchase=false review for any listing -> inflated (or overwrote the seeded) rating; locked by mig 20260719000003 (only verified_purchase=true reviews move the rating; an unverified review is a no-op). (C) regression — guard_marketplace_seller_trust_columns still blocks a direct client update of total_sales/rating_avg. Asserts all three forges are blocked/no-op, then ROLLS BACK (0 pollution). Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "service-state-machine",
        "script":  "tools/validate_service_state_machine.py",
        "args":    [],
        "label":   "SERVICE_HAILING C1 lock (LIVE, rolled-back psql as a runtime-resolved NON-admin worker — an admin identity legitimately bypasses every guard, so an admin-run suite reads as fail-open). Re-runs the P1 adversarial suite as a standing anti-regression over mig 20260728000024: born-terminal request blocked; illegal jump (requested->completed) blocked; the legal client path works AND journals 2 service_job_events; attribution forge (filing a request as another uid) blocked; client credit-mint into service_credit_ledger blocked (money = trust-forge class); GCash top-up self-verify is a no-op (row stays pending_verification — the founder/GUC path is the only mint); provider self-verification and self-set on_job blocked; the atomic accept guard's 2nd status-guarded UPDATE hits 0 rows; live_location column privacy holds (revoke-first grant). Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "service-dispatch-isolation",
        "script":  "tools/validate_service_dispatch_isolation.py",
        "args":    [],
        "label":   "SERVICE_HAILING C3 lock (LIVE, rolled-back psql; every check SELF-CONTAINED — it mints its own probe auth.users + temp providers/requests inside the transaction, because a borrowed seeded identity is never clean: my_service_provider_ids() surfaces hive providers for their MEMBERS and a verified pre-existing identity outranks the probe's temp provider = false refusals). Asserts the dispatch rules of mig 20260728000025: accept_service_request refuses your own hail (own_request), a category you don't serve (category_mismatch), and an out-of-radius job even when the id is obtained out-of-band (RLS hides stranger broadcasts from the raw table — the feed view is the sanctioned path); the accept race has EXACTLY ONE winner (2nd caller gets lost_race_or_closed — the fix for the reference repo's select-then-insert TOCTOU); v_service_open_broadcasts never shows your own hail but shows an in-scope stranger hail; v_service_job_tracking (the ONLY live_location read path) shows an active job to its client and provider and NOTHING to a stranger. Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "service-geo-privacy",
        "script":  "tools/validate_service_geo_privacy.py",
        "args":    [],
        "label":   "SERVICE_HAILING C10 lock (LIVE, rolled-back psql; self-minting hex-uuid actors at literal Manila/Davao coordinates). The arc's §3 D-Geo axis had column privacy locked by C1 but NOTHING asserting the MATCHING behaviour a hailing product lives on: a provider 2km inside a 10km broadcast FINDS the hail in v_service_open_broadcasts; one ~700km away is refused out_of_radius by accept_service_request even holding the id; the client CAN read the matched provider's live position mid-job through v_service_job_tracking (the only path); and a stranger reads 0 rows from it - location is never ambient. Skips if docker/DB unreachable.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "service-roadmap-executed",
        "script":  os.path.join("tools", "verify_service_hailing_roadmap.py"),
        "args":    [],
        "label":   "SERVICE_HAILING itemized execution audit (Ian 2026-07-29: 'itemize everything written in the roadmap, then check one by one if we have executed it'). 85 concrete promises - every Fuel table, Engine view/RPC, Brain agent, Dashboard surface, stack layer, §1c booster, C1-C11 lock, §3 dimension, G1-G4 gap, phase deliverable, D1-D14 decision and safety rail - each re-derived from the LIVE database, filesystem and gate registry, never from a hand-kept checklist. FAILs naming any promise that stops being true.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "regression",
    },
    {
        "id":      "service-ufai-deep",
        "script":  os.path.join("tools", "validate_service_ufai_deep.py"),
        "args":    [],
        "label":   "SERVICE_HAILING UFAI DEEP (LIVE browser): the verification ufai_pillar_map.py explicitly excludes from its coarse lens slice - measured 44px tap targets at 390, vendored axe-core, and a 360/390/768/1280/1920 horizontal-overflow sweep across the arc's four surfaces. Caught two SERIOUS defects the page-level rubric could not see (a bare a.btn with NO rule rendering browser-default blue at 1.88:1 and 21px tall; a select labelled only by title=). Bar: 0 sub-44px, 0 overflow, 0 serious/critical axe.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "regression",
    },
    {
        "id":      "service-hailing-scoreboard",
        "script":  "tools/service_hailing_scoreboard.py",
        "args":    ["--check"],
        "label":   "SERVICE_HAILING arc compass (forward-only ratchet over service_hailing_state.json). The P0-P9 phase table went green while three axes the roadmap ALSO names were unmeasured - §3 journey coverage, §3b per-surface UFAI, §2 class gates - so a green headline masked them. This scores all three boards and FAILs if any regresses. The W (walk) phase is DERIVED from walked personas x states (>=2 x >=2), never hand-set, so 'I drove it once as the admin' cannot score as covered. --accept ratchets up.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "regression",
    },
    {
        "id":      "read-battery",
        "script":  "tools/validate_read_battery.py",
        "args":    [],
        "label":   "Per-page P3 read-correctness + P7 empty-vs-error gate (LIVE headless Playwright, real Baguio supervisor). For 8 READ-heavy pages compares what the page RENDERS to the DB truth (docker-psql admin) for the signed-in hive: audit-log #feed child-count == count(hive_audit_log) [EXACT rendered==DB]; integrations/plant-connections DB==0 -> empty-state + hero counters read 0 (error NOT swallowed as empty) [P7]; public-feed/project-report/shift-brain/analytics render real rows when DB>0; ai-quality renders #content OR its intentional maturity gate (feedback_platform_intentional_blank_states). Complements validate_hive_battery.py (hive.html deep render==DB) + truth-view-read-isolation (DATA/RLS layer). A regression (stale/dropped/mangled render, error swallowed as empty, stuck skeleton) FAILs. Every expectation derived from a LIVE DB count = reseed-robust. Re-drive: node tools/validate_read_battery.mjs [--headed]. Skips cleanly if node or the local stack is absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "offline-queued",
        "script":  "tools/validate_offline_queued.py",
        "args":    [],
        "label":   "T14: offline_queued family gate — T14's lock spoke (2026-08-25). Runs tools/prove_offline_queued.mjs across ALL 8 queue-page cases (the complete whCreateQueue roster + logbook's own queue) and FAILs on any case that cannot prove the queue-and-tell oracle: offline, the write is HELD (the queue's own store grows or the record is on screen), the person is TOLD it will sync, and nothing claims the refusal wording. WHY A GATE: the CG family's offline cell was `offline_refusal` — the WRONG oracle for queue pages (green credited a refusal that must not exist). The prover replaced it for these 8 pages; without a registered gate the family regresses silently (the queue-registration defer race THIS oracle caught on pm-scheduler — 2 of 5 runs lost — is exactly the class that returns quietly). Discipline carried from the bank's own lessons: - retry-once per case before failing (full-suite live gates flake under load; the pm-completion case's comment records the re-run-first rule). - node invoked DIRECTLY, never npx (the repo path contains an ampersand). - skips cleanly when node or the local stack (Flask :5000 / Supabase :54321) is absent — a gate that cannot run must say SKIP, not PASS. Re-drive one case: node tools/prove_offline_queued.mjs --case <name>",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "findability",
        "script":  "tools/validate_findability.py",
        "args":    [],
        "label":   "T173: Findability benchmark gate — T173's lock (2026-08-25). Wraps tools/findability_benchmark.mjs: 20 common tasks phrased as USER QUESTIONS, each answered from a cold page via the global hub in <= 2 interactions, asked AS the persona who owns the question (worker default; supervisor-lane questions declare role:'supervisor' — the hub's role scoping is design, not a defect). Its FIRST runs caught four real wayfinding defects (Hive Board supervisor-gated against its own worker-daily declaration; Voice Journal and Reports hidden from the personas who own them; Eng. Design excluding supervisors, who ARE the engineer persona in practice). This gate keeps the 20/20 from regressing. Skips cleanly when node or the local stack is absent. Re-drive: node tools/findability_benchmark.mjs",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "someone-to-you",
        "script":  "tools/validate_someone_to_you.py",
        "args":    [],
        "label":   "T108: someone->you event coverage gate — T108's registry discipline (2026-08-25). The registry (substrate/reference/someone_to_you_event_registry.json) enumerates every event where person A's action creates work or attention for person B. The T108 bar: each event needs a notification path and a tap that lands ON the item; 'none' rows are the silent-work class to extinguish. WHAT THIS GATE HOLDS (a forward-only ratchet, not a green demand): 1. The registry file exists, parses, and every event row carries the required fields (event, source, notification, finding) with notification in the declared vocab. 2. The count of 'none' (silent-work) rows never GROWS past the accepted baseline — shipping a new someone->you event without a notification path FAILs here by name. Fixing one (none -> badge-only/notified/realtime) tightens the baseline. Baseline lives inline (SILENT_BASELINE); update it DOWNWARD as silent rows are fixed.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "join-lands",
        "script":  "tools/validate_join_lands.py",
        "args":    [],
        "label":   "T6/T7: join-lands-on-roster gate — T6+T7's shared instrument close (2026-08-26). Runs tools/prove_join_lands.mjs: a two-context paired journey where an OUTSIDE worker joins the supervisor's hive by invite code (the same SECURITY DEFINER join_hive_by_code path the UI submit calls) while the supervisor's hive.html board is open. PASS bar: the join is VISIBLE on the supervisor's roster after at most one reload, the DB row landed, and the probe row is cleaned + verified. The prover also measures (never asserts) the realtime half: hive.html registers no postgres_changes channel on hive_members, so realtime=false is the current truth — recorded in the output so the day a channel is added the receipt flips. Discipline (carried from validate_offline_queued.py): - retry-once before failing (full-suite live gates flake under load). - node invoked DIRECTLY, never npx (the repo path contains an ampersand). - utf-8 pinned on the subprocess decode; PASS matched line-anchored. - SKIPs cleanly when node / the local stack is absent — a gate that cannot run must say SKIP, not PASS. - the prover ABORTS (exit 2) on dirty pre-state rather than deleting rows it did not create; that surfaces here as FAIL with the abort line.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "g5d-reauth-restore",
        "script":  "tools/validate_g5d_reauth_restore.py",
        "args":    [],
        "label":   "T8: g5d-reauth-restore gate — T8's return-trip context contract (2026-08-26). Runs tools/prove_g5d_reauth_restore.mjs: on each covered page, set a filter, kill the session, re-auth through index.html?signin=1&return=<page>, and assert the page comes back WITH the filter still applied. Session expiry must cost one sign-in and zero context. Covered this slice: logbook (filter-category) + pm-scheduler (cat-filter) — the two pages wired through the central whAutoRememberFilters helper. Pages with bespoke persistence are later slices; the prover states its scope. Instrument lesson baked into the prover: the G5a wiring lands DEEP inside each page's async init(), seconds after the filter element parses — the setter is therefore SELF-VERIFYING (retries until the wh_view_* store actually holds the save) so a probe race can never again read as a page defect. Discipline: retry-once, node direct (ampersand path), utf-8 pinned, line-anchored PASS, SKIP when node/stack absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "timed-journey-family",
        "script":  "tools/validate_timed_journey.py",
        "args":    [],
        "label":   "T9/T10/T13: timed-journey gate — the T9/T10/T13 shared oracle, slice 1 (2026-08-26). Runs tools/prove_timed_journey.mjs: each case is one user JOB with a stated budget - the prover walks the scripted core path and reports measured wall time vs budget. Slice 1: T13's handover-read (worker @390 opens shift-brain; usable = verdict resolved + carry-forward list populated or honestly empty). The machine measures the PLATFORM's share (time until the read is possible), never the human's reading time; a breach is a slowness-regression signal. Write-journeys (T9 time-to-logged, T10 completion loop) are later slices. Discipline: retry-once, node direct (ampersand path), utf-8 pinned, line-anchored PASS, SKIP when node/stack absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "stock-ripple",
        "script":  "tools/validate_stock_ripple.py",
        "args":    [],
        "label":   "T11: stock-ripple gate - T11's cross-page write-propagation oracle (2026-08-26). Runs tools/prove_stock_ripple.mjs: stage a part at min_qty+1, read index's low-stock tile (pre), issue 1 through inventory's OWN Use modal (the atomic inventory_deduct RPC), reload index - the tile must read pre+1. Write-side sibling of cross_surface_agreement (which locks READ-parity only). Hygiene: the part's qty is snapshotted + restored, probe transactions (WH-T11-PROBE) deleted with NO time window (marker rows are artifacts by definition - a window once spared an older orphan), both verified; ABORTs on no stageable part. Discipline (carried from validate_offline_queued.py): - retry-once before failing (full-suite live gates flake under load). - node invoked DIRECTLY, never npx (the repo path contains an ampersand). - utf-8 pinned on the subprocess decode; PASS matched line-anchored. - SKIPs cleanly when node / the local stack is absent — a gate that cannot run must say SKIP, not PASS. - the prover ABORTS (exit 2) on dirty pre-state rather than deleting rows it did not create; that surfaces here as FAIL with the abort line.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "tile-count-agreement",
        "script":  "tools/validate_tile_count_agreement.py",
        "args":    [],
        "label":   "T19: tile-count-agreement gate - T19's tile->list handoff oracle (2026-08-26). Runs tools/prove_tile_count_agreement.mjs: the number a person taps must be the number they land on. pm-overdue: tile N -> ?filter=overdue chip active + the page's own 'N of M assets past due' agrees. open-jobs: tile N (a HIVE count) -> ?view=team&status=Open -> N team rows (cap-guarded). At birth it caught tile=9 landing on the mine-pill's 2 - the two-windows-one-metric class as a tap; fixed by carrying the window in the href. The low-stock tile's multi-band handoff is a RECORDED follow-up, not a silent skip. Discipline (carried from validate_offline_queued.py): - retry-once before failing (full-suite live gates flake under load). - node invoked DIRECTLY, never npx (the repo path contains an ampersand). - utf-8 pinned on the subprocess decode; PASS matched line-anchored. - SKIPs cleanly when node / the local stack is absent — a gate that cannot run must say SKIP, not PASS. - the prover ABORTS (exit 2) on dirty pre-state rather than deleting rows it did not create; that surfaces here as FAIL with the abort line.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "chain-journey",
        "script":  "tools/validate_chain_journey.py",
        "args":    [],
        "label":   "T29: chain-journey gate - T29's per-hop context harness, slice 1 (2026-08-26). Runs tools/prove_chain_journey.mjs: a multi-page chain must CARRY its context at every hop and Back must walk it in reverse. Slice 1: alert-hub's PM alert names its asset -> following it opens THAT asset's pm-scheduler detail (#det-name matches, no re-find) -> one Back returns to a rendered alert-hub. ABORTs (exit 2) when no PM alert exists - never invents a subject. Discipline (carried from validate_offline_queued.py): - retry-once before failing (full-suite live gates flake under load). - node invoked DIRECTLY, never npx (the repo path contains an ampersand). - utf-8 pinned on the subprocess decode; PASS matched line-anchored. - SKIPs cleanly when node / the local stack is absent — a gate that cannot run must say SKIP, not PASS. - the prover ABORTS (exit 2) on dirty pre-state rather than deleting rows it did not create; that surfaces here as FAIL with the abort line.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "kpi-parity",
        "script":  "tools/validate_kpi_parity.py",
        "args":    [],
        "label":   "T22: kpi-parity gate - T22's cross-surface headline-KPI oracle, slice 1 (2026-08-26). Runs tools/prove_kpi_parity.mjs: one datum, one story - a headline KPI on two surfaces must show the SAME figure with its BASIS on the glass. Slice 1: PM compliance (SMRP 2.1.1) - analytics #an-pm-hero vs pm-scheduler's 'N% PM compliance (SMRP, last 90 days)' line. At birth it caught 78% vs 77%: analytics painted the orchestrator snapshot's copy while pm-scheduler read the canonical RPC live; fixed by patching the card from the RPC when it answers (snapshot stays the instant-paint fallback). Discipline (carried from validate_offline_queued.py): - retry-once before failing (full-suite live gates flake under load). - node invoked DIRECTLY, never npx (the repo path contains an ampersand). - utf-8 pinned on the subprocess decode; PASS matched line-anchored. - SKIPs cleanly when node / the local stack is absent — a gate that cannot run must say SKIP, not PASS. - the prover ABORTS (exit 2) on dirty pre-state rather than deleting rows it did not create; that surfaces here as FAIL with the abort line.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "fil-parity",
        "script":  "tools/validate_fil_parity.py",
        "args":    [],
        "label":   "T45: fil-parity lint — T45's translation-parity gate (2026-08-26). Scans every _t('en','fil') call site for IDENTICAL en/fil arguments — the signature of a string that was ADDED to the EN branch without a Filipino translation (my 2026-08-25 error-taxonomy sweep created exactly this debt: new taxonomy strings landed EN-only inside _t() calls). Not every identical pair is debt: proper nouns, codes, and ALL-CAPS tokens ('OK', 'PM', 'QR') legitimately read the same in both languages — those are excluded by shape (short, or all caps/digits/punct). Everything else counts. Forward-only ratchet: the count may only fall. Baseline stored inline in fil_parity_baseline.json (created on first run with the measured count).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "fee-parity",
        "script":  "tools/validate_fee_parity.py",
        "args":    [],
        "label":   "T103: fee-parity gate — T103's stated-vs-charged oracle (2026-08-26). Every fee percentage a page STATES to a user is a promise; the DB is what actually charges. They are wired independently — the glass hardcodes '10%' while `listing_reservation_amount` resolves a per-hive `reward_pct` knob through `service_knob_pct` (defaulting from the platform table) — so a knob change silently makes the copy a lie. That gap is exactly the surprise-charge class this gate exists to prevent. What it checks, per claim in CLAIMS below: 1. the stated percentage still appears on its page (the copy has not moved without this gate noticing); 2. the DB's own effective rate for that knob equals the stated number. The DB side is read LIVE via psql (the platform default plus every hive override, so a single divergent hive fails the gate rather than hiding behind an average). SKIPs when the stack is down — a gate that cannot read the DB must say SKIP, never PASS.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "persona-follows-account",
        "script":  "tools/validate_persona_follows_account.py",
        "args":    [],
        "label":   "T85: the companion follows the account — T85's cross-device oracle (2026-08-26). The persona lives in TWO places: localStorage, which every renderer and prompt-builder reads, and worker_profiles.preferred_persona, the account-level choice. Only index.html and voice-journal.html bridged them, each with its own copy of the query. assistant.html — reachable directly, from the nav hub, a deep link, or simply a different browser — read localStorage alone. MEASURED: account set to hezekiah, localStorage empty, assistant resolved ZANIAH. That is not a wrong default, it is the worker's choice being ignored, and it is SILENT because the wrong answer IS the default — nothing errors, the companion is just not the one they picked. ★AND THE COPY IT REPLACED CLOBBERED ON FAILURE: index's inline version destructured { data: profile } without checking the error, so a transient blip left profile null, fell through to 'zaniah', and WROTE it — permanently resetting a Hezekiah user because one read failed. An absent or unreadable preference is not evidence of a preference. Both surfaces now use one helper (wh-persona.js hydratePersonaFromCloud) that writes only when the cloud names a choice. The prover drives BOTH directions, since a one-way check would pass on a page that always overwrites: the choice must REACH a fresh device, and an injected 500 on the profile read must CHANGE NOTHING. Teeth: against pre-fix HEAD direction one resolves 'zaniah'. Sibling to CL9, which found the same class in companion-launcher — that check guarded ONE file while the identical bug sat in assistant.html; its scope is now all five persona surfaces, plus a rule against stamping a CONSTANT on a persona column (voice-handler logged 13 of 13 tts_quality_log rows as 'zaniah').",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "edge-refusal-reaches-user",
        "script":  "tools/validate_edge_refusal_reaches_user.py",
        "args":    [],
        "label":   "T82: edge refusals reach the user — the static census. supabase-js collapses EVERY non-2xx from functions.invoke into ONE FunctionsHttpError whose message is the literal string \"Edge Function returned a non-2xx status code\". The status and body survive only on error.context (a Response), so a caller reading error.message never sees them - and shows that placeholder to a person. ★THE CLIENT THEN SILENTLY UNDOES THE WORK THE BACKEND DID: rate-limit.ts answers a drained hive with 429 and \"AI call limit reached for this hive. Try again in an hour.\" - cause named, clearing time named, exactly the bar the AI-error taxonomy exists to hold. MEASURED on asset-hub (met by genuinely exhausting the hourly quota): the worker read \"Could not reach Asset Brain: Edge Function returned a non-2xx status code\", a CONNECTION-flavoured sentence for a QUOTA event, sending them to check their signal instead of waiting an hour. whAiError could not rescue it either - it keys on /429|rate.?limit|quota/ and the placeholder carries none of them. Fixed centrally: utils.js whFnError() reads error.context, so the function's own sentence survives and only a body-less failure falls back to the status taxonomy. The unwrap had been hand-rolled in companion-launcher, analytics and assistant - three copies, thirteen other files without it. WHAT IT COUNTS: invoke error branches that put error.message in front of a person (toast, innerHTML, textContent, a whListError/whReadError call) without consulting error.context or whFnError. ★IT COUNTS RATHER THAN FORBIDS - 39 invoke sites, 19 speaking the placeholder at first measure; a gate that failed all of them on day one would be turned off. Forward-only against tools/edge_refusal_baseline.json; asset-hub's four sites were cleared in the same pass, 19 -> 16. ★AND IT ONLY FLAGS BRANCHES THAT SPEAK: an invoke whose error is logged, swallowed or rethrown shows nobody the placeholder, and charging for it would measure code shape rather than harm.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "edge-refusal-survives-client",
        "script":  "tools/validate_edge_refusal_survives_client.py",
        "args":    [],
        "label":   "T82: edge refusals survive the client — the live half. supabase-js collapses EVERY non-2xx from functions.invoke into ONE FunctionsHttpError whose message is the literal string \"Edge Function returned a non-2xx status code\". The status and body survive only on error.context (a Response), so a caller reading error.message never sees them - and shows that placeholder to a person. ★THE CLIENT THEN SILENTLY UNDOES THE WORK THE BACKEND DID: rate-limit.ts answers a drained hive with 429 and \"AI call limit reached for this hive. Try again in an hour.\" - cause named, clearing time named, exactly the bar the AI-error taxonomy exists to hold. MEASURED on asset-hub (met by genuinely exhausting the hourly quota): the worker read \"Could not reach Asset Brain: Edge Function returned a non-2xx status code\", a CONNECTION-flavoured sentence for a QUOTA event, sending them to check their signal instead of waiting an hour. whAiError could not rescue it either - it keys on /429|rate.?limit|quota/ and the placeholder carries none of them. Fixed centrally: utils.js whFnError() reads error.context, so the function's own sentence survives and only a body-less failure falls back to the status taxonomy. The unwrap had been hand-rolled in companion-launcher, analytics and assistant - three copies, thirteen other files without it. THE ASSERTION: with both Asset Brain lanes forced to the real 429 payload, the answer pane shows the FUNCTION'S sentence (quota named) and never the placeholder. Teeth: against pre-fix HEAD the pane reads \"Could not reach Asset Brain: Edge Function returned a non-2xx status code\" and the gate fails. ★The 429 is INJECTED rather than waited for, so the probe is deterministic and spends no AI call.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "ai-draft-shows-its-basis",
        "script":  "tools/validate_ai_draft_shows_its_basis.py",
        "args":    [],
        "label":   "T80: an AI draft shows its basis — T80's review oracle (2026-08-26). fmea-populator reads a year of corrective logbook entries, clusters them by root cause, and writes suggested failure modes to rcm_fmea_modes with source='ai_logbook' and approved_at NULL. An engineer then approves them, and APPROVAL is what makes a mode count in v_fmea_truth and drive maintenance strategy. FOUND: the row announced itself honestly - a 'from AI logbook scan' pill and a confidence badge, provenance done properly - and said NOTHING about what it read. The engineer was asked to approve a failure mode with no way to check the claim, on a screen whose output shapes how a machine gets maintained. The function held the exact cluster and simply never recorded it; the `notes` column existed and sat unused. Confirmed in the fixture: a live ai_logbook row ('Lip seal leaking', 0.552 confidence) with notes NULL. Same lesson AH15 learned about citations - the one thing a citation is FOR is letting a reader verify. Now stamped as the window and root cause a person can filter the logbook by ('AI draft from 6 corrective logbook entries (root cause: seal wear) between ... Review against those entries before approving.'), not raw uuids, which nobody can check by reading. ★IT CHECKS BOTH HALVES (the AI6 pair): a basis written but never rendered helps no one, and a render with nothing to show is a blank line. Teeth: reverting either side fails it; reverting both reports both. Verified live end to end - the grounding line and confidence badge render on the row, 0 page errors, probe row removed and re-counted to 0. ★AND ONLY FOR DRAFTS AWAITING A HUMAN: a mode an engineer wrote needs no such note; the point is not provenance theatre, it is that whoever is asked to APPROVE can check.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "voice-refuses-ambiguous-asset",
        "script":  "tools/validate_voice_refuses_ambiguous_asset.py",
        "args":    [],
        "label":   "T79: voice refuses an ambiguous asset — T79's commit oracle (2026-08-26). The voice router turns \"I replaced the seal on Pump 2\" into a logbook.create intent and resolves the machine; when more than one matches it returns asset_resolution ambiguous:true with the candidates. FOUND: ambiguity was RENDERED and never ENFORCED. The intent card turned the asset pill red and appended '(multiple matches)' - and Confirm still wrote the entry to ar.primary, whichever candidate ranked first. A worker's repair record landed on the wrong machine, and the only warning was a colour; on a plant floor that is maintenance history attached to the wrong equipment, the one thing the history exists to get right. ★AND THE GUARD FOR THIS ALREADY EXISTED AND WAS DEAD: _preflightAction was defined, exported in the test-helper block, and certified by validate_ai_companion_integration_audit's preflight check - which asserted its four blocker STRINGS appeared in the file. They did, inside a function nothing ever called. Its writeVerbs were slot-style ('log_entry') while the router emits 'logbook.create', so even a call would have matched nothing. Presence is not wiring: that audit now requires a real call site plus the router's own kinds, and fails against pre-fix HEAD on both counts. THE ASSERTION, driven against the shipped file in a browser: a write kind with an ambiguous resolution is refused with blocker 'ambiguous_asset', the SAME kind with one clear match is NOT refused for ambiguity, and the refusal names the candidates and says nothing was saved. ★DIRECTION TWO IS THE POINT - a guard that refused everything would satisfy direction one while making voice logging useless. Teeth: pre-fix the probe reads 'ambiguous -> allowed'.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "no-eaten-escapes",
        "script":  "tools/validate_no_eaten_escapes.py",
        "args":    [],
        "label":   "Platform: no eaten escapes — a control character in source is a regex that cannot fire (2026-08-26). Written through nested quoting layers (a shell heredoc, a nested python -c, an editor round-trip), a \\b can arrive on disk as a literal BACKSPACE byte. The file parses, the regex compiles, and the alternative it guards can never match, because no real string contains a backspace. Nothing errors; the check quietly stops checking. MEASURED, five instances in one session: shift-brain.html (x2) and report-sender.html carried /\\b429\\b|rate.?limit|too many requests/i with both boundaries turned into control bytes, so a bare '429' or 'Request failed with status 429' was NOT recognised as rate limiting - proven against the shipped file, '429' -> false before and true after, on the very pages that tell a person why their report or shift plan did not run; tools/prove_route_numbers.mjs lost its suffixed-unit branch and over-reported naked numbers; and two gates written the same day had partly-dead matchers. An earlier session found 28 at once, including a \\b429\\b fix that was itself dead and passed only because the text happened to say 'Too Many Requests'. ★A SNIPPET IS NOT THE ARTIFACT - that session proved its boundary in a separate snippet and shipped a dead regex; this gate reads BYTES off disk, because a console renders 0x08 as nothing, which is exactly how the bug hides (it printed a repaired-looking /429|.../ while still holding the control character). ★WHY BYTES AND NOT A REGEX: every other detector here could be a victim of the bug it hunts; a byte count has no matcher to break. ★SCOPE: repo-wide, 15,089 files - validate_page_ui_provers.py has the same check scoped to provers, and all three shipped instances sat outside it, which is the too-narrow-scope failure this class keeps producing.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "scheduled-claim-has-a-schedule",
        "script":  "tools/validate_scheduled_claim_has_a_schedule.py",
        "args":    [],
        "label":   "T63/T66: a cadence promise needs a scheduler — cron.job is the oracle (2026-08-26). A page telling a reader something happens 'daily' or 'monthly' is making a claim about a SCHEDULER, settled against the live database rather than against prose. FOUND on integrations.html: 'New SAP work orders then flow in daily' and 'WorkHive polls your CMMS API daily' - measured, 26 cron jobs and not one touching cmms-sync, which runs only when someone presses Sync now. Worse than copy: the Sync Schedule control DEFAULTED to 'Daily', saved sync_freq, and plant-connections rendered that value in its status table - three surfaces agreeing on a cadence nothing runs, while nothing anywhere reads sync_freq. What was REAL stayed (the webhook a CMMS posts to, and the push-back cmms-push-completion fires as each job closes). ★THEN THE SAME DEFECT ON A SECOND PAGE, which is why the scope moved: ph-intelligence.html claimed a monthly rhythm in EIGHT places - 'Published monthly', 'refreshes monthly', 'Reports are generated monthly', and worst 'next monthly refresh due soon', a promise about a future event nothing would deliver - with no cron touching intelligence-report either. Scoping a gate to where a bug was NOTICED rather than where the CLASS lives is the failure this session kept finding in other people's checks. ★AND A SAMPLE IS NOT A CENSUS: the first pass on that page read a slice of visible text, caught four claims and declared it clean; a grep for the word found four more. ★ITS OWN VOCABULARY NEEDED FIXING TWICE - it flagged <option> labels (offering a choice is not promising it runs) and then its own honest disclaimer, so selects and denials are excluded; and it miscounted 26 cron jobs as 51 by splitting multi-line commands. ★SKIPS WITHOUT ITS ORACLE. Teeth: reverting either page alone flags that page's claims while the other stays clean.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "new-claims-are-opt-in",
        "script":  "tools/validate_new_claims_are_opt_in.py",
        "args":    [],
        "label":   "T84: new claims are opt-in — T84's resume-polish oracle (2026-08-26). resume.html's review sheet is a good pattern: the AI only SUGGESTS, and nothing reaches the document until the worker approves it. But two different acts arrived with the same default. REWRITE - polishing a bullet the worker wrote - is shown as 'Was: <their text>' and pre-ticked; their content, improved, with the change visible, which is fine. ADD - the 'suggested highlights' from tailor_to_jd - is PUSHED into their experience, was also pre-ticked, and carries no 'Was:' because there is no before. Those are NET-NEW CLAIMS ABOUT THE WORKER, generated with the JOB AD in context: exactly the pressure that produces a line they cannot back up in an interview, on the document they hand an employer. FOUND: both checked:true, so the default path (Tailor, then Apply) added up to five such claims without one deliberate act of accepting any. The prompt DOES instruct the model to keep highlights truthful and grounded in stated skills - worth having, and kept - but a prompt is the only thing enforcing it, and pre-ticking removed the human check meant to be the backstop. New claims now start unchecked, and the tailored summary gained its 'Was:' so its diff is visible like the polish path's. ★IT READS THE APPLY FUNCTION, NOT THE LABEL: addition versus edit is decided by what it does to the document (.push vs assignment), which no wording can disguise. ★AND IT DOES NOT BAN PRE-TICKING - making every suggestion opt-in would push workers toward tapping Apply without reading, the very behaviour this prevents; only new claims about a person carry the burden. ★ITS OWN MATCHER WAS BLIND FIRST: it bounded the apply body with [^;]{0,400} and every real apply is several statements long, so it matched nothing and PASSED against the world it was built to catch - caught only by teeth-proving, which is why it is not registered vacuous.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "rating-admits-refusal",
        "script":  "tools/validate_rating_admits_refusal.py",
        "args":    [],
        "label":   "T88: a rating admits refusal — T88's feedback-loop oracle (2026-08-26). Every companion reply carries thumbs up/down. The tap used to lock both buttons and paint the chosen one green or red IMMEDIATELY, then fire the insert and discard whatever came back. ★AND THE MOST LIKELY FAILURE DOES NOT THROW: supabase-js returns { error } on an RLS refusal rather than raising, so the try/catch around that insert never fired for it - the code's own comment records that anon callers 'silently skip'. A worker tapped thumbs-down, watched it turn red, and nothing was stored. That is worse than having no thumbs at all: it teaches them their feedback is read when it is not, on the surface whose entire purpose is to collect it. MEASURED with the insert answered 403 the way a real RLS denial answers: _recordReplyRating returned undefined and the UI had already committed. It now returns false, the buttons come back, and the worker is told to tap again; on success it returns true and only then does the button take its accepted colour. ★BOTH DIRECTIONS, because a function that always reported failure would satisfy the first half while silently discarding every real rating - refused -> false, allowed -> true, with the row counted in the database. ★NOT COSMETIC: ai-quality aggregates these thumbs, and 3+ negatives in 7 days flags ai_quality_escalation for supervisor outreach, so silently dropped ratings quietly bias all of it. Probe rows marked WH-T88-PROBE and deleted, with a re-count proving cleanup.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "temporal-answer-names-its-window",
        "script":  "tools/validate_temporal_answer_names_its_window.py",
        "args":    [],
        "label":   "T91: a temporal answer names its window — T91's audit oracle (2026-08-26). temporal-rag-orchestrator answers 'what changed since last month?' by decomposing a range into periods and folding per-period analyses into one comparison. Its FOLD_SYSTEM rule 7 tells the model to open with the resolved absolute range, because 'last week' is ambiguous - trailing 7 days versus calendar week, and the PHT boundary - and the numbers cannot be audited without it. FOUND: that rule was the ONLY thing enforcing it. The handler returned fold.answer verbatim and the resolved range never appeared in the response at all, so a model that skips rule 7 (models do) produces an answer whose numbers belong to a window nobody can name - two_windows_one_metric arriving through an LLM instead of a query, on figures a supervisor may carry into a meeting. ★THE REPO ALREADY HAD THE ANSWER: its WAT split says compute the hard facts in code and let the model write prose from them, exactly how the resume summary synthesis is built. The periods carry their own resolved boundaries, so the range is known before the model is called. It is now stated in code - prepended only when the model did not already do it, so a compliant answer is never labelled twice - and returned structurally as `covering`, so a consumer never parses prose to learn which window it is reading. Helpers were exercised against the artifact: omitted -> prepended, complied -> not duplicated, no periods -> unchanged. ★A PROMPT RULE IS NOT AN ENFORCEMENT - this gate exists because rule 7 was already written, and correct, and insufficient. Teeth: against pre-fix HEAD all three checks fail.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "narration-rail-holds-every-figure",
        "script":  "tools/validate_narration_rail_holds_every_figure.py",
        "args":    [],
        "label":   "T83: the narration rail holds EVERY figure — engineering-calc-agent's spoken-headline oracle (2026-08-26). The calc is computed deterministically; a model then writes the report narrative plus a spoken `narration` that must 'quote the single headline design value verbatim'. A rail already guarded that, commented 'AI-1 grounding rail: silence the spoken line if it quotes an unverifiable figure'. ★IT CHECKED THE OPPOSITE OF ITS OWN COMMENT: `nums.some(...)` passed as soon as ONE number matched a computed result, so 'Provide a 61 kW unit at 45.2 kW bus' passed on the 45.2 while the 61 was invented - and the invented number is the one SPOKEN as the headline, on a calculation an engineer signs. Measured against pre-fix HEAD that narration returned true; it now returns false, and pure fabrication is still caught, so this is a strict tightening rather than a rewrite. ★`every` ALONE WOULD HAVE BEEN ITS OWN FAILURE - a rail that mutes honest narrations teaches people to ignore it. Narrations legitimately carry figures that are not results: the INPUTS they were computed from, small counts ('2 units'), and standard designations ('IEC 62305-3', 'PEC 2017'). Those are exempted by name; everything else must match a computed value within tolerance, and at least one must be a real RESULT so that merely echoing the inputs does not count as quoting the answer. Seven cases were exercised against the shipped function, one per exemption. Teeth: all five checks fail against pre-fix HEAD.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "missing-brief-says-so",
        "script":  "tools/validate_missing_brief_says_so.py",
        "args":    [],
        "label":   "T81: a missing brief says so — T81's AMC freshness oracle (2026-08-26). The 6am PHT cron writes one amc_briefings row per shift_date and alert-hub reads only TODAY's. That scoping is right and stays: yesterday's brief must never pose as this morning's. ★BUT WHEN THE CRON MISSED, THE CARD VANISHED - renderAmcCard(null) set display:none, so a supervisor could not tell 'no brief ran today' from 'this feature does not exist' or 'the page is still loading'. Worse, a skeleton is rendered in that card on first load, so the sequence was a loading state resolving into NOTHING: an answer erasing itself, on a surface whose whole promise is a brief every morning. On a missed morning the absence IS the news. It now stays visible, says no brief was generated for this shift, names the 6am cadence so the reader knows what was missed, points at the most recent brief the page already fetched (marked as not a substitute), and blanks the figure tiles so no number stands under a 'not generated' header. BOTH DIRECTIONS: with today's row absent the card speaks; with the real row it still renders the brief and does not claim absence - the second half matters, since a card that always cried 'no brief' would satisfy the first while destroying the feature. ★NO DATA IS MUTATED: only the today-scoped read (shift_date=eq.) is intercepted, so the historic query answers for real, which is what lets the empty state name the most recent brief. Teeth: against pre-fix HEAD the card reads shown=false.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "converted-money-states-its-rate",
        "script":  "tools/validate_converted_money_states_its_rate.py",
        "args":    [],
        "label":   "T89: converted money states its rate — the owner's AI-cost oracle (2026-08-26). ai-quality's cost card is the owner's answer to 'what is this costing me'. Every peso figure on it - the hero, the per-call average, and the MODEST / TYPICAL / HEAVY verdict thresholds - came from one hardcoded constant, PHP_PER_USD = 56, which was neither dated nor shown. PHP/USD has moved between roughly 50 and 59 in recent years, so a drifting rate silently shifts both the number and the JUDGEMENT about it, while the card reads like a measurement. The figure is not so much wrong as unfalsifiable: an owner cannot tell whether ₱2,100 means 'heavy use' or 'the rate is stale'. The rate is now stated beside the figure, the way a source chip states a window. ★IT DOES NOT DEMAND A LIVE RATE - a fixed rate is a reasonable choice for a cost estimate; presenting the result as if nothing was assumed is not. Checked and found CORRECT rather than manufactured into a defect: the conversion itself is sound (php = totalCost * PHP_PER_USD, and the thresholds compare pesos to pesos), and the details table's USD figure is labelled as USD. Teeth: against pre-fix HEAD the rate is undisclosed and the gate names the line.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "quota-warns-before-the-wall",
        "script":  "tools/validate_quota_warns_before_the_wall.py",
        "args":    [],
        "label":   "T89: the quota warns before the wall — a count is not a warning (2026-08-26). _shared/rate-limit.ts returns `remaining` on EVERY allowed call, so the platform always knows how close a hive is to its hourly cap. NOTHING CONSUMED IT: there is no threshold anywhere in the codebase, and the only signal a worker ever got was the 429 AFTER the wall - mid-task, with the work half done. asset-hub was the single surface rendering the number at all, and it rendered it flat ('12 AI calls remaining this hour'), which reads the same at 12 as at 1. whQuotaNotice() now turns the number the server already sends into a warning at 5 or fewer and names the reset, so 'wait' has a 'how long'. ★THE UNKNOWN CASE WAS A BUG IN THE FIRST VERSION, caught by exercising the shipped helper rather than trusting it: Number(null) is 0, so a quota the server did not report was announced as 'No AI calls left this hour' - absence rendered as a value, alarming and false. An absent reading is not a reading of zero; it is now silent. ★IT DOES NOT REQUIRE EVERY AI SURFACE TO SHOW A QUOTA - most never receive `remaining`, and demanding that plumbing everywhere would make this a migration rather than a gate; it holds the surfaces that DO show it to showing it usefully. ★AND ITS OWN MATCHER WAS BLIND FIRST: it looked for 'whQuotaNotice(' while utils.js defines it as 'whQuotaNotice = function', so it reported the helper missing while it sat ten lines above - it would have failed forever on a correct file. Teeth: pre-fix it flags both the bare render and the absent helper.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "verdict-needs-a-denominator",
        "script":  "tools/validate_verdict_needs_a_denominator.py",
        "args":    [],
        "label":   "T65: a verdict needs a denominator — ai-quality's trust-card oracle (2026-08-26). The Worker Trust card is where an owner decides whether the AI is worth paying for. It turned thumbs into a percentage and then into a VERDICT: HEALTHY in green at 70%+, WATCH in amber, LOW TRUST in red below 50%. FOUND: no sample floor. With a SINGLE rating the card read '100% · HEALTHY' in green, or '0% · LOW TRUST' in red - a confident judgement about the whole AI layer from one tap. The zero case was already handled honestly ('NO DATA') and the raw counts already sat beside the figure, so the gap was narrow and specific: a percentage needs a denominator before it can carry a verdict, and below about five ratings a single tap moves it twenty points or more. The number is still shown - hiding it would be its own dishonesty - but it is not dressed as a verdict until it can support one, and the card says what would make it meaningful. Exercised against the shipped branch: 1 and 4 ratings read TOO FEW RATINGS in grey, 5+ resolve to a real tag, and a genuine 2-up/8-down still lands as LOW TRUST in red. ★THE POINT IS NOT TO HIDE BAD NEWS - the gate also fails if the real verdicts disappear, since a card that never judges is its own failure. ★SAME DISCIPLINE THE RELIABILITY WORKBENCH APPLIES to a Weibull fit with too few failures: say the sample is too small and what would fix it, rather than fitting a curve to three points.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "search-button-is-not-dead",
        "script":  "tools/validate_search_button_is_not_dead.py",
        "args":    [],
        "label":   "T78: the search button is not dead — the spine's dead-control oracle (2026-08-26). nav-hub.js is on every page and lazy-loads search-overlay.js asynchronously, so window.WHSearch is briefly absent on EVERY page load and permanently absent if that request fails - a cache miss, a bad deploy, a flaky plant connection. ★THE HANDLER HAD ONLY THE HAPPY BRANCH: `if (window.WHSearch) { open() }` and nothing else, so a press with the script missing did nothing whatsoever. MEASURED with search-overlay.js answered 404: the overlay never opened, NOTHING was said, and there were zero page errors - the exact shape of a control that looks alive and is not, on the one element every page carries. It now retries the load once (the usual cause is a request that had not landed yet) and then says why it cannot open. A search that will not start is a small failure; a button that swallows the press teaches the worker the platform is broken and gives them nothing to do about it. BOTH DIRECTIONS: healthy opens the overlay and does not cry unavailable; script-dead does not open and says so - the first half is what stops the fix from being 'always claim it is broken'. Driven at 390, because the hub is the phone's only navigation. Teeth: against pre-fix HEAD the dead-script case reads saysUnavailable=false.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "filter-vocabulary-is-complete",
        "script":  "tools/validate_filter_vocabulary_is_complete.py",
        "args":    [],
        "label":   "T64: the filter vocabulary is complete — audit-log's compliance oracle (2026-08-26). The feed reads the newest 500 rows, which is right for a scrolling feed. The filter dropdowns were then built from THAT array, so the choices offered were whatever happened to fall inside the cap. MEASURED on the fixture hive: 3,402 audit rows, 4 distinct actors and 22 distinct actions overall, but only 3 actors and 18 actions inside the newest 500. A supervisor could not filter to one of the four people who acted, or to 4 of the 22 action types, and nothing said the list was partial. On a compliance surface 'no option for that person' reads as 'that person did nothing' - the opposite of what an audit trail is for. ★SAME CLASS THIS FILE HAD ALREADY FIXED FOR THE CSV EXPORT, one function away, which is the tell: the cap was removed where someone had looked and left where nobody had. The options now come from their own complete pass, paged past the cap with the same hive scope, reading only three short columns, and falling back to the capped set if that pass fails - a short list beats no list. THE ASSERTION: the option lists match the DISTINCT values in the DATABASE, which is the oracle, so it cannot drift into checking the page against itself. ★THE FIX'S OWN FIRST VERSION FAILED LIVE: it declared its cache with `let` further down the file than the async path that reads it - a temporal dead zone - so the page threw ReferenceError on first paint and every dropdown came back EMPTY. No syntax check sees that, which is why this gate is live rather than static. Teeth: against pre-fix HEAD the page offers 3 actors and 18 actions against the database's 4 and 22.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "freshness-stamp-tracks-content",
        "script":  "tools/validate_freshness_stamp_tracks_content.py",
        "args":    [],
        "label":   "T39: a freshness stamp tracks its content — alert-hub's background-upgrade oracle (2026-08-26). The AMC card serves the stored 6am brief instantly, then calls analytics-orchestrator as a background UPGRADE. renderAmcCard stamps the meta 'stored brief, generated <time>', which is exactly right while the stored copy is showing. ★THE UPGRADE REPLACED THE SUMMARY AND LEFT THE STAMP: on success the pane held a freshly composed action brief under a line saying it came from this morning's stored row. Not a stale number - a stale CLAIM ABOUT a number, harder to catch because everything on screen looks internally consistent, and it lands on a supervisor deciding what to do with their shift. ★THE FAILURE PATH WAS ALREADY HONEST and is deliberately untouched: it keeps the stored summary AND the stored stamp, which agree - a silent keep is right for a background upgrade, since the reader still has a brief. BOTH DIRECTIONS are driven, because a fix that stamped 'live' unconditionally would satisfy the first while making the second lie. Found while censusing T39's real question: 11 of 12 AI-invoking surfaces already distinguish a quota refusal from a network failure - this was the twelfth, and its answer turned out to be a stamp rather than a message. Teeth: against pre-fix HEAD a successful upgrade still reads 'stored brief'.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "engineer-lens-is-one-lens",
        "script":  "tools/validate_engineer_lens_is_one_lens.py",
        "args":    [],
        "label":   "T52: the engineer lens is one lens — asset-hub / nav-hub coherence oracle (2026-08-26). There is no engineer ROLE on this platform: hive_members.role is CHECK-constrained to worker | supervisor. 'Engineer' exists TWICE as a lens - nav-hub's localStorage 'wh_nav_mode' (which filters TOOLS) and asset-hub's 'wh_asset_view' (which decides PANELS) - independent keys sharing one user-facing word. TWO DEFECTS, both user-visible: (1) someone who picked the Engineer lens in the hub still landed in asset-hub's WORKER view, because an unset wh_asset_view reads as false - they declared themselves once and had to declare it again, with nothing saying a second switch existed; (2) _syncAssetView ran from exactly two places (the toggle's click handler and loadDetailFmea), so on a fresh load the button kept its markup default and read 'Show Reliability Workbench' - i.e. OFF - even for someone whose stored choice was engineer, who would then press it to enable what was already enabled and disable it. ★THE FIX IS A DEFAULT, NOT A MERGE: the scopes genuinely differ, so only the UNSET case derives from the hub lens and an explicit local toggle still wins. Four cases are driven for exactly that reason - a fix that merely mirrored the hub would pass the first two and destroy the local choice. ★AND THE ORACLE TOOK THREE TRIES, which is the transferable part: asking the helper directly failed pre-fix with 'noFn' (a fact about the patch, not the user); reading the reliability CARD failed the other way, since it lives in the detail pane and is hidden at load regardless of lens; the honest signal is the toggle's aria-expanded, set straight from the lens and present in the pre-fix world so the comparison is real. Teeth: pre-fix, cases 1 and 4 both fail.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "engineer-lens-covers-its-lane",
        "script":  "tools/validate_engineer_lens_covers_its_lane.py",
        "args":    [],
        "label":   "T52: what the Engineer lens SELECTS — the TOOLS-registry content oracle (2026-08-27). The sibling gate asks whether the two switches called Engineer agree with each other; this one answers the question T52's own note left open, whether the nav-hub engineer filter selects the right tools. ★THE INVARIANT WITH TEETH: no tool may be tagged `engineer` WITHOUT also being tagged `supervisor`. That is not tidiness. There is no engineer ROLE here — hive_members.role is CHECK-constrained to worker | supervisor — so `roles: ['engineer']` alone puts a tool in NO role's default spine: reachable only by someone who had already gone into the hub and switched the lens by hand, and invisible to the supervisors who ARE the engineers on this platform. nav-hub.js says so at the line that added supervisor to Eng. Design — 'no separate engineer auth exists (the engineer persona IS supervisors in practice, T52); a supervisor needing a calc could not find Eng. Design here' — and the same reasoning applies to every future tool, with nothing holding it. This is the retired-page class (a live control reachable only through a door most people never open), caught at the registry rather than after someone loses a tool. ★AND THE LANE: T52 established the design-and-reliability set (Eng. Design, Analytics, Asset Hub, Project Manager, Project Report, PH Intelligence); each is asserted to keep its engineer TAG so a tool cannot quietly leave the lens while the lens still calls itself Engineer. Tagging, not visibility: isVisibleInMode also drops every hidden:true tool from EVERY mode, and two of the six are hidden Phase-B pages reached from a parent button, so a visibility check would fail them forever for a reason unrelated to engineers. The lane is a FLOOR, not a ceiling — extra engineer-visible tools are allowed, because whether Reports belongs is a judgement the registry records with a reason, not one a validator should overturn. Teeth (--selftest, 5 cases): an engineer-only tool and a lane member leaving the lens both go RED; a universal tool and an absent tool stay green.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "old-draft-says-its-age",
        "script":  "tools/validate_old_draft_says_its_age.py",
        "args":    [],
        "label":   "T57: an old draft says its age — the dormant-return oracle (2026-08-26). whAutoSaveDraft is X2 interruption resilience, and it is careful work: it refuses a draft belonging to another worker (the shared-device leak), detects an untouched SELECT by its default rather than by emptiness (the 'Critical' urgency that silently reverted to 'Normal'), and says where drafts live. ★BUT IT CARRIED NO AGE: a draft typed three months ago restored into the form exactly like one typed three minutes ago - silently, into fields the worker is about to submit. On a logbook entry that means filing a stale reading against today's shift with nothing having said the text was old. Interruption resilience is measured in minutes and hours; a draft that survives a WEEK has stopped being an interruption and become a leftover. ★IT SAYS THE AGE RATHER THAN DISCARDING THE TEXT - the work is still the worker's to keep or clear, and throwing a note away because it is old would be the opposite failure, since X2 exists precisely so typed work is never lost. FOUR AGES ARE DRIVEN, because a notice that always fired would be noise and one that never fired would be the bug: 0d and 6d restore silently, 8d and 90d restore and say how old they are. ★THE PROBE MUST SIGN IN - the owner check compares against wh_last_worker, and unauthenticated the page redirects to index where the owner reads null, so the draft is correctly refused and nothing restores; an earlier run reported restored:false four times and looked like a broken fix. Teeth: against pre-fix HEAD the 90-day draft restores with saysAge=false.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "maturity-says-yes-too",
        "script":  "tools/validate_maturity_says_yes_too.py",
        "args":    [],
        "label":   "T187: the maturity ladder says yes too — the month-three oracle (2026-08-26). maturity-gate told a hive what it could not have yet ('unlocks at Stair 2', 'reach Stair 3') on every gated surface, and said NOTHING when the hive crossed one. A plant that spent three months building logbook history earned Stair 2 in SILENCE: the refusal simply stopped appearing, which nobody notices, because noticing an absence is not how people work. The entire premise of the ladder is that patience is rewarded, and the reward was never announced - a one-directional system that only ever refuses teaches people it is a wall. It now says so once, on the first gated surface loaded after the crossing. ★DETECTED CENTRALLY in checkMaturityGate, because every gated page already calls it and only the FIRST after a crossing should speak - per-page detection would have congratulated the same climb on every surface visited. ★KEYED BY HIVE, so a multi-hive contractor never gets one hive's progress announced as another's. ★RISE ONLY, deliberately: readiness DECAYS if logging stops, and telling someone unprompted that they have been demoted, on whatever page they happened to open, is a different and crueller feature - the refusal messages already explain what is missing at the next gate. ★AND FIRST SIGHT IS SILENT: with no previous reading there is no crossing, only a baseline, and without that every worker's first page load would congratulate them. Exercised against the shipped helper (first sight silent, 1->2 announces, repeat silent, 2->3 announces, 3->1 silent) and verified live: a gated page still locks, the baseline is written per hive, 0 page errors. Teeth: against pre-fix HEAD there is no announcement at all.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "invite-code-round-trip",
        "script":  "tools/validate_invite_code_round_trip.py",
        "args":    [],
        "label":   "T6/T7/T185: the invite-code round trip — the briefing-room exchange (2026-08-26). The platform's whole onboarding rests on one exchange: a supervisor creates a hive and reads a six-character code aloud; a worker types it on their phone and is in. It was walked on prod as a smoke test and GATED BY NOTHING - T6 sat at 90% and T7 at 85% with no gate registered against either, so the most load-bearing flow on the platform was protected by nobody. ★TWO BROWSER CONTEXTS, because this is a two-person exchange and a single-context test cannot fail the way the real thing fails: the supervisor's browser creates the hive, and a SEPARATE context signed in as a DIFFERENT real account joins with the code, with nothing passing between them but the six characters - exactly the channel the real flow uses. THE ASSERTIONS: the code is six readable characters; a different account joins and lands in the SAME hive, confirmed in the DATABASE rather than by reading a success banner (a banner is what a broken join would also show); and a WRONG code is refused out loud without joining anything. ★DIRECTION THREE RUNS FIRST, deliberately - a join that accepted anything would sail through the other two, and checking the wrong code before the right one means a permissive join cannot hide behind a later success. ★AND IT CLEANS UP: the probe hive and its memberships are removed and re-counted; two consecutive runs left 0 probe hives, an unchanged membership count on the joining account, and 0 orphaned memberships. This is also the first slice of T185's day-one choreography, which is the same exchange at six-context scale.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "no-raw-enum",
        "script":  "tools/validate_no_raw_enum.py",
        "args":    [],
        "label":   "CE: no raw enum reaches a person (2026-08-26). `in_progress`, `rate_limit_exceeded`, `cancelled_by_provider` are how a COLUMN stores a state, not how a person says it - and when one reaches the screen the reader translates the schema in their head. The states that leak are usually the ones that matter: a refusal, a failure, a half-finished job. ★THE SHAPE IS THE TELL, which makes this decidable rather than a matter of taste: a short visible text leaf matching lowercase_with_underscores is a raw token with near-certainty, because nobody writes English that way. ★WHY THIS GATE EXISTS AT ALL: the PROVER did not run. tools/prove_no_raw_enum.mjs was built, works, and was invoked by NOTHING - an audit of 106 prove_*.mjs harnesses found 3 that no file in the repo mentions, and this was the most valuable. It passes 22 pages with 0 failures and 0 ungraded, so the property was already true; what was missing was anything to keep it that way, and a working oracle nobody runs is indistinguishable from one never written. ★AND THE AUDIT THAT FOUND IT OVER-REPORTED FIRST: scanning only the gate registry and validate_*.py wrappers called 15 provers orphaned; widening to every .py/.mjs/.js/.md/.json/.sh/.yml cut it to 3 - the same correction the double-submit census needed when 118 flagged handlers turned out to be 4. ★A ZERO DENOMINATOR IS NOT A PASS: if the prover grades 0 pages the gate FAILS, because that means its matcher or roster broke rather than the platform being clean.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "longest-truncates",
        "script":  "tools/validate_longest_truncates.py",
        "args":    [],
        "label":   "CF: the longest realistic value truncates (2026-08-26). Real plants name things badly - a part called 'Bearing, spherical roller, 22320 E1 XL C3, SKF, for kiln drive gearbox non-drive end' is not a stress test, it is Tuesday, and the card holding it was laid out around a nine-character example. THE ORACLE: the longest realistic title truncates VISIBLY rather than overflowing its card or pushing the price out of view, measured by INJECTING the long value and watching the layout rather than reasoning about CSS - the only way to catch a container that grows silently until the thing beside it is off-screen. ★WHY THIS GATE EXISTS: the prover ran nowhere. An audit of 106 prove_*.mjs harnesses found 3 that no file in the repo mentions; this was one, and it works - 18 pages pass, 0 fail. The property was already held and nothing was keeping it that way. ★UNGRADED IS NOT PASS, and the prover already knew it: 4 pages report UNGRADED because no rendered row title existed to lengthen, and it says so rather than counting them clean. The wrapper carries that through and also FAILS on a zero tally, because nothing-measured means the roster or selector broke, not that the layout is sound.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "join-names-the-namesake",
        "script":  "tools/validate_join_names_the_namesake.py",
        "args":    [],
        "label":   "T185: two people who share a name both get into the hive (2026-08-26). A plant pilots WorkHive and five workers are handed ONE invite code at the morning briefing; two of them are named Juan Dela Cruz, which in a Philippine plant is not a contrived edge case, it is Tuesday. The first joins. The second - a different human being, a different auth identity, the correct code - hit `duplicate key value violates unique constraint hive_members_hive_id_worker_name_key`, and hive.html rendered it VERBATIM via 'Could not join: ' + joinErr.message. Three failures at once: he cannot join his team, he is shown database internals, and he is offered no way forward, on the pilot's first morning. ★THE CONSTRAINT IS RIGHT AND STAYS: worker_name is the attribution key across many tables, so two identical names inside one hive would make every by-name rollup ambiguous. The defect was never the constraint - it was that the function let the constraint speak to the user. ★TWO CASES RAISE THE IDENTICAL 23505 AND MUST NOT GET THE SAME ANSWER, which is why this gate runs a REAL race rather than reading the source: a genuine namesake must be refused BY NAME (HIVE_NAME_TAKEN) so the page can offer a distinguishing one, while the SAME user double-tapping Join must be told 'you are in' - two concurrent calls both read no membership under READ COMMITTED and both insert, so the idempotent path is BYPASSED by the race and only the exception handler can tell them apart by re-reading who won. Measured: pre-fix, the double-tap surfaced the raw violation too. ★THE RACE IS MADE DETERMINISTIC by holding an UNCOMMITTED insert in one session while the RPC runs in another, so it blocks on the unique index and takes the handler for real. ★AND THE REMEDY MUST BE WALKABLE: the join form carries only a code input, so naming the cause without offering a name field is a dead end - the gate asserts the branch, the revealed input, and that the chosen name is persisted to wh_last_worker, since otherwise the roster would show the new name while every later write attributed to the old.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "control-within-viewport",
        "script":  "tools/prove_control_within_viewport.mjs",
        "args":    ["--gate"],
        "report":  "control_within_viewport_report.json",
        "label":   "CF: a tappable control must not sit past the floor viewport's edge (T113, 2026-08-27). MEASURED: '← Back to Basic Worker' ran +45px past a 390 viewport on inventory and +35px on logbook, its parent computing overflow:visible — part of a real control simply off-screen on the smallest phone the platform supports. ★WHY NOTHING CAUGHT IT: the page never SCROLLS horizontally. The navbar is a fixed-height flex row and the document's scrollWidth never grows, so every check asking 'does this page scroll sideways at 390?' — the usual Z2 / WCAG 1.4.10 shape — reads perfectly clean while a button hangs over the edge. Scroll is a SYMPTOM of overflow, not a definition of it, and that gap is what this gate closes. ★AND THE ROOT IS RARELY THE OVERFLOWING ELEMENT: shortening the button changed nothing (it was still at 438 in a 390 box at 101px wide). The row was over-full because a SIBLING would not shrink — a flex item defaults to min-width:auto and refuses to go below its content width — so min-w-0 on the group plus truncate on its long label fixed both pages with the full label intact. The oracle ignores fixed/absolute decoration and anything inside a scrolling or clipping ancestor, so it reports only what a thumb actually cannot reach. Resurrection-proved: removing min-w-0 turns it RED (exit 1, naming the control at +228px), restoring it GREEN. ★THE FLOOR IS 320, NOT 390 (corrected 2026-08-27): the first cut of this gate guarded 390 and called it the floor, but the platform's declared floor is 320 (T113 / WCAG 1.4.10) — 390 is merely the common phone. A gate on the wider box cannot see what only breaks in the narrower one, and moving it found TWO more offenders the same day: community's post action row gives a supervisor viewing their OWN post FIVE icon buttons instead of four, and the fifth — Delete, which also carries a deliberate 22px Z3 gap so a destructive control is never adjacent to a routine one — ran 15px off-screen, so the delete control was unreachable on exactly the posts its owner is most likely to manage; and analytics' 'Stage 3' badge sat 10px past, because .hdr-right is deliberately flex-shrink:0 (its EN/FIL buttons owe 44px on both axes) so the row had nowhere to give. Both fixed by wrapping, which preserves the Z3 separation and the 44px minimums instead of trading them away. --width narrows the run and writes its own report; 320 and 390 both green.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "cross-tab-identity-propagates",
        "script":  "tools/validate_cross_tab_identity_propagates.py",
        "args":    [],
        "label":   "CF: an identity change in ONE tab must reach the others (T148, 2026-08-27). Two events change what EVERY open tab is allowed to show, and both happen in a single tab. SIGN-OUT already propagated and did it well - measured with two real tabs, clearing identity in tab A sent tab B to index.html?signin=1&reason=othertab&return=<page>: it noticed, NAMED the reason, and kept the destination. HIVE SWITCH propagated NOT AT ALL: storage said Manila Electronics Assembly while the other tab still rendered Baguio Textile Mills - the cross-tenant confusion T51 exists to prevent, and worse than an ordinary stale number because the person JUST switched and believes they are looking at the new plant. ★THE HANDLER TELLS RATHER THAN RELOADS, and the gate enforces that as a FORBIDDEN clause: auto-reloading somebody's other tab would discard a half-typed entry to fix a DISPLAY problem, on a platform whose whole posture on interruption is that typed work survives. A reload the person CHOOSES (a 44px button) is correct; a reload done to them is not. The notice names BOTH hives so the reader knows which figures are in front of them. ★AND THE NAME MUST BE READ LATE: a switch writes several keys and storage events arrive one per key in write order, so reading wh_hive_name the instant the ID event landed read the name the switch had not replaced yet - the first cut said 'You switched to Baguio... still showing Baguio', naming one hive twice, which reads like a bug in the switch rather than a stale tab. Deferred one tick, with a neutral 'another hive' fallback. Teeth 8/8, one per clause plus the forbidden unprompted reload; the negatives remove EVERY occurrence of a pattern after one of them passed vacuously by replacing only the first.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "selection-is-exposed",
        "script":  "tools/validate_selection_is_exposed.py",
        "label":   "T125: which control is SELECTED must reach the accessibility tree, not only the pixels (2026-08-27). `.active` paints a violet background - invisible to a screen reader and to a forced-colors user. 20 pages appeared to use role=tab; 18 set aria-selected, asset-hub set NONE (3 reliability tabs announcing tab without ever saying which was current, no tabpanels, no arrow keys), and the 20th was this gate's own grep matching a COMMENT. Fixed by completing the widget; analytics.html aria-pressed toggles are the other legitimate answer and are honoured. The aria-controls gap on 17 pages is REPORTED every run, not failed - real owed work, neither enforced nor deleted.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "shared-button-invites-the-click",
        "script":  "tools/validate_shared_button_invites_the_click.py",
        "label":   "T179 affordance parity (2026-08-27): 601 live interactive elements across 12 signed-in pages, and exactly ONE rendered without cursor:pointer - marketplace's .btn-ghost 'Preview as buyer', accepting a click it never invited. The class is used on 16 pages; tokens.css owned its 44px tap floor but not its cursor, and 15 pages happened to declare one locally, hiding the incomplete contract. Fixed centrally rather than in marketplace, because patching the instance leaves the trap armed for the next page. This gate holds the CONTRACT: the shared class invites the click when enabled and withdraws it when disabled.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "your-own-message-is-visible",
        "script":  "tools/validate_your_own_message_is_visible.py",
        "label":   "T96 (2026-08-27): a person must see the message THEY sent, answered or not. Measured with two signed-in buyers on ONE code path - Romeo Beltran (replied) saw his thread, Pablo Aguilar (pending) saw NOTHING: no message, no state, no sign he had ever asked. One clause did it, .not(reply_text,is,null), so a thread appeared only once the other party got round to answering. An invisible sent message is indistinguishable from one that never sent, so the buyer cannot tell whether to follow up while the platform holds the record the whole time. Holds four things: the read is not filtered to answered-only; the pending state is RENDERED not merely fetched; the pending line carries a DATE (how long it has been IS the question); and the panel stays scoped to buyer_name, since one showing OTHER people's inquiries would satisfy the rest and be a privacy defect. Selftest 6/6. Its own first run FAILED on the fixed file by matching the fix COMMENT, which quotes the clause it removed - the gate now strips comments.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "reload-mid-flow",
        "script":  "tools/prove_reload.mjs",
        "args":    [],
        "label":   "The CF `reload` oracle, measured by actually reloading mid-flow: a half-filled sheet either survives intact or is GONE, never restored into a state the person did not leave it in. 42 targets, 12 graded (all GONE, an acceptable answer), 29 not-applicable with the reason stated (a sheet with no text field has no half-fill to measure), 1 reach gap. ★REGISTERED 2026-08-28 FROM THE ORPHAN SWEEP. It was on disk, runnable, and invoked by NOTHING - neither listed as a board script nor called by any registered one. Thirteen provers were in that state; ten ran clean, and six of those EXIT NON-ZERO on failure, so they are real gates rather than recorders. The four that only record are deliberately NOT registered: a gate that cannot go red is a permanent false green, which is worse than no gate at all. ★AND THIS ONE WAS ORPHANED BECAUSE IT CRASHED: one dead context ended the whole run, so it reported five passes and a stack trace. Repaired at BOTH layers - reviving the context alone just moved the crash two targets along, because the browser process itself was dying.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "public-feed-walk",
        "script":  "tools/prove_public_feed.mjs",
        "args":    [],
        "label":   "The anon public-feed surface walked end to end - 11 pass / 0 fail / 0 ungraded. ★REGISTERED 2026-08-28 FROM THE ORPHAN SWEEP. It was on disk, runnable, and invoked by NOTHING - neither listed as a board script nor called by any registered one. Thirteen provers were in that state; ten ran clean, and six of those EXIT NON-ZERO on failure, so they are real gates rather than recorders. The four that only record are deliberately NOT registered: a gate that cannot go red is a permanent false green, which is worse than no gate at all. ",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "session-expiry-read",
        "script":  "tools/prove_session_expiry_read.mjs",
        "args":    [],
        "label":   "The READ half of the session_expiry oracle: what a page shows when the token died under a read rather than a write - 8 pass / 0 fail / 0 ungraded. The write half (prove_session_expiry) still TIMES OUT at 240s and is left unregistered until that is diagnosed, rather than wired in as a slow amber. ★REGISTERED 2026-08-28 FROM THE ORPHAN SWEEP. It was on disk, runnable, and invoked by NOTHING - neither listed as a board script nor called by any registered one. Thirteen provers were in that state; ten ran clean, and six of those EXIT NON-ZERO on failure, so they are real gates rather than recorders. The four that only record are deliberately NOT registered: a gate that cannot go red is a permanent false green, which is worse than no gate at all. ",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "analytics-report-walk",
        "script":  "tools/prove_analytics_report.mjs",
        "args":    [],
        "label":   "The analytics-report generation surface walked live. ★REGISTERED 2026-08-28 FROM THE ORPHAN SWEEP. It was on disk, runnable, and invoked by NOTHING - neither listed as a board script nor called by any registered one. Thirteen provers were in that state; ten ran clean, and six of those EXIT NON-ZERO on failure, so they are real gates rather than recorders. The four that only record are deliberately NOT registered: a gate that cannot go red is a permanent false green, which is worse than no gate at all. ",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "assistant-walk",
        "script":  "tools/prove_assistant.mjs",
        "args":    [],
        "label":   "The AI assistant surface walked live. ★REGISTERED 2026-08-28 FROM THE ORPHAN SWEEP. It was on disk, runnable, and invoked by NOTHING - neither listed as a board script nor called by any registered one. Thirteen provers were in that state; ten ran clean, and six of those EXIT NON-ZERO on failure, so they are real gates rather than recorders. The four that only record are deliberately NOT registered: a gate that cannot go red is a permanent false green, which is worse than no gate at all. ",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "cross-surface-agreement",
        "script":  "tools/prove_cross_surface.mjs",
        "args":    [],
        "label":   "The same datum read on two surfaces must agree - the cross_surface_agreement oracle. ★REGISTERED 2026-08-28 FROM THE ORPHAN SWEEP. It was on disk, runnable, and invoked by NOTHING - neither listed as a board script nor called by any registered one. Thirteen provers were in that state; ten ran clean, and six of those EXIT NON-ZERO on failure, so they are real gates rather than recorders. The four that only record are deliberately NOT registered: a gate that cannot go red is a permanent false green, which is worse than no gate at all. ",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "cta-activation",
        "script":  "tools/prove_cta_activation.mjs",
        "args":    [],
        "label":   "T1.2 (built 2026-08; REGISTERED 2026-08-28): every visible primary CTA, CLICKED, with an observable consequence. ★THIS PROVER EXISTED FOR WEEKS AND WAS NEVER ON THE BOARD - built precisely because 790 green gates coexisted with a dead 'Get Early Access' bar on the landing page, since every CTA oracle here asserts EXISTENCE (the element is present, named, 44px) and none asserted CONSEQUENCE (tapping it changes the world). It was referenced only inside ANOTHER gate's label text, which reads like registration and runs nothing. ★AND IT WAS UNREGISTERABLE WHILE IT LIED: run as shipped it reported a dead click on 'Open the AI Assistant', and the CTA works. Cause: the seeder serves each page at BOTH /learn/<slug>/ and /workhive/learn/<slug>/, 200 either way with identical text, but rewrites root-absolute hrefs only under the PREFIXED path - so at the unprefixed path the link pointed at /assistant.html, which 404s, and the prover recorded a working control as dead. A prover that cries wolf about the one class it exists to catch is a prover nobody registers, which is exactly what happened. Now walks ${ORIGIN}${PATH_BASE}/, verified by hand-clicking the control under the prefix (it navigates and lands on the assistant page). 29 probes across index, public-feed, marketplace and the learn/tools exemplars at 390 and 1280; 0 dead against a 0 baseline. Demands navigation | modal | scroll-delta | focus-move | DOM-mutation, and records a still-inert control as inert-stuck rather than skipping it.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-shown-count-is-not-the-page-size",
        "script":  "tools/validate_a_shown_count_is_not_the_page_size.py",
        "label":   "T15/T129 (2026-08-28): a number on the glass counts the WORLD, not the fetch. logbook's machine-history panel read .limit(5) and titled itself `data.length + past repairs on + machineId` - a sentence about the array wearing the clothes of a sentence about the plant. MEASURED: 94 machines in the fixture hold more than five fault_knowledge rows and M-003 holds 77, so the panel announced 5 past repairs on a machine with 77 - and this is the panel a technician consults BEFORE opening equipment. Under-reporting a machine's history to the person deciding how careful to be is the wrong direction to be wrong in. Fixed by fetching the true total (count:'exact', head:true - no rows shipped) and rendering '47 past repairs on M-001 · showing the latest 5', verified live; if the count read fails it falls back to describing the LIST ('Latest 5 repairs') rather than a number it cannot support. ★THE CLASS WAS ALREADY KNOWN HERE, which is why the gate holds the SHAPE: asset-hub hit the identical bug (AH16 - '500 reading(s) in the last 30 days' for an asset with 3,174, under-reporting 6x while every mean and sigma above it came from the same truncated slice) and marketplace solves it a third way by rendering an em-dash when the read was partial. Three surfaces, one property; logbook was the instance nobody had walked. ★DELIBERATELY SCOPED to the two sites where the cap is REACHABLE - founder-console (500 campaigns) and marketplace-admin (100 disputes) share the shape but hold 3 and 0 rows, and gating arithmetic that cannot happen yet is noise. Selftest 5/5; its exact-count negative was vacuous first, because logbook holds THREE such reads and the mutation edited an unrelated query 1,600 lines away.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-calc-count-is-true",
        "script":  "tools/validate_the_calc_count_is_true.py",
        "label":   "T151/T161 (2026-08-28): a public claim about how many calculators we have must match how many we have. Two surfaces were wrong: engineering-design.html's meta said '46 calculation types ... and Structural', and assistant.html's GROUNDING - the text the AI answers from - said '46 ... Structural, Machine Design, Vertical Transportation'. Counted from CALC_TYPES_UI, the file's own declared sole source of truth: 55 types across SIX disciplines (HVAC & Cooling 10, Mechanical 4, Electrical 14, Plumbing 10, Fire Protection 5, Machine Design 12), confirmed live in the browser and against the project's own substrate note. ★THE UNDERCOUNT IS THE FORGIVABLE HALF - the damaging half is the DISCIPLINE list: both named Structural and the assistant also named Vertical Transportation, neither of which exists here. An SEO snippet promising a structural calculator sends a searcher to a page that has none; the assistant's grounding is worse, because the AI states it confidently in conversation to someone who then goes looking. A wrong NUMBER is a stale fact; a wrong CAPABILITY is a promise the product cannot keep. ★THE GATE COUNTS THE REGISTRY RATHER THAN COMPARING TWO STRINGS, because the drift happened exactly as it always does - a human typed a number that was right at the time, and anything hand-maintained against a growing registry is a future defect with a date on it. ★AND THE /learn '60 free calculators' CLAIM IS DELIBERATELY OUT OF SCOPE: it counts the 60 PUBLIC /tools pages, a different population, and folding two populations into one gate is how a CORRECT claim gets 'fixed' into a wrong one. Selftest 6/6.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-pasted-identifier-still-matches",
        "script":  "tools/validate_a_pasted_identifier_still_matches.py",
        "label":   "T123 (2026-08-28): whitespace must never decide whether the system works. People paste - a part number from a Viber message, a username from an email - and every pasted value can carry a leading or trailing space the person cannot see. Where the value is used for IDENTITY or MATCHING, that invisible character decides the outcome: a space on si-username/su-username and sign-in fails with correct credentials; a space in the inventory SEARCH and a part that exists returns nothing, which reads as 'we do not stock it' to someone standing at the shelf; a space in part_number/part_name on WRITE is baked into the record, so the row never matches its own search again; a space on a recipient email and the report bounces. All six are trimmed today and this holds them there. ★THE PROPERTY IS DELIBERATELY NOT 'TRIM EVERYTHING': dayplanner's note and logbook's knowledge field are textareas where a person may indent lines, so leading whitespace is CONTENT, and a gate demanding .trim() on every text field would enforce an opinion about prose and eventually be switched off. ★THE SCAN NEEDED CAREFUL READING, WHICH IS WHY THE SUBJECT LIST IS EXPLICIT: a search for untrimmed values reaching a payload flagged inventory's q and cat, both of which are the remembered-filter SNAPSHOT - restored into the input and re-trimmed at the point of use - so judging by the assignment rather than the use would have filed two defects against correct code. ★AND THE SELFTEST MUTATES COPIES, NEVER DISK: its first version rewrote the real product files and restored them in a finally block, which is one crash or one concurrent board read away from a corrupted source or a voided board. Selftest 7/7, each field mutated independently.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-return-promise-is-kept-safely",
        "script":  "tools/validate_the_return_promise_is_kept_safely.py",
        "label":   "T4 (2026-08-28): a wall that promises to send you back must send you back, and only somewhere safe. An anon or hive-less visitor tapping into Community is bounced to the hive gate, and that bounce used to DROP the destination - so a person who set out for one place arrived somewhere else with no way back and nothing explaining why. Now community stamps hive.html?return=community.html and hive.html renders a 'Continue to Community' banner honouring it. TWO HALVES GATED TOGETHER because they are ONE promise in two files: a stamp nobody reads is a query string, a banner with nothing to read is dead markup, and a fix to either side alone breaks the promise with nothing erroring. ★THE THIRD CLAUSE MATTERS MOST - IT IS A SECURITY PROPERTY HIDING INSIDE A USABILITY FEATURE: ?return= is user-controllable, and the naive implementation (location.href = raw) is an OPEN REDIRECT, a link to our own trusted domain that lands the person on an attacker's page wearing our reputation. The guard must be a strict same-origin FILENAME allow-list, never a blocklist - one rejecting 'http://' and '//' still passes '/\\evil.com' and a dozen encodings. Nothing about the feature LOOKS security-sensitive, which is exactly why its shape is locked. Fourth: the label is built with textContent, so a crafted filename cannot inject markup even if the allow-list is ever loosened. Selftest 6/6.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-removed-image-actually-leaves",
        "script":  "tools/validate_a_removed_image_actually_leaves.py",
        "label":   "T134 (2026-08-28): a listing's photo leaves the public bucket when the listing does - and when it is REPLACED. Deleting a listing removed the row and left the JPEG at a permanent public URL, so 'this cannot be undone' was true of the row and quietly false of the photo: anyone holding the link still had it after the seller believed they had removed the listing. ★THE FIX-EVERY-PATH PASS FOUND THE BIGGER SOURCE - deleting is rare, REPLACING an image on edit is common, and it orphaned the old object exactly the same way, far more often. A gate holding only the delete path would have locked the half that almost never runs while the frequent one kept leaking, which is worse than no gate because the board would report the class closed. Holds both sites and both of the edit path's guards: remove only when the URL actually CHANGED (re-saving an unchanged listing must not delete its own live image) and only when the old object is OURS by bucket-marker (a seller may paste an external URL, and we must not reach into someone else's host). Both cleanups stay BEST-EFFORT and wrapped, running after the write so a storage hiccup can never cost the seller the edit or delete they asked for - the inverse of the coupling T12 had to break. Selftest 5/5. ★THE OWNERSHIP NEGATIVE WAS VACUOUS FIRST: a whole-file search for indexOf(_marker) is satisfied by the DELETE path's own copy, so removing the EDIT path's guard left the file matching and the case passed while detecting nothing - two near-identical blocks on one page is precisely the shape that makes a loose check vacuous, and it is now scoped to the edit block.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-policy-matches-what-ships",
        "script":  "tools/validate_the_policy_matches_what_ships.py",
        "label":   "T165 (2026-08-28): the privacy policy must describe the analytics that actually ship. It said 'we do not run third-party analytics... when we add web analytics in the future, this page will be updated' while GA4 (wh-ga4.js) was already live on 10 public pages. ★A STALE SENTENCE ON A LEGAL SURFACE IS A DIFFERENT KIND OF DEFECT: everywhere else drift means a number is wrong or a label is old, but here it means the document a regulator, an enterprise buyer or a worker reads to decide whether to trust us was making a FALSE STATEMENT about what we collect - false in the direction that flatters us, which is the direction nobody audits, and invisible because prose never looks broken. Gated as PARITY in both directions so it cannot rot from either side: if wh-ga4.js loads, the policy must NAME Google Analytics and must not carry the future-tense denial; it must state the IP anonymization the code actually runs (naming GA4 without it under-claims our own protection, which is still a mismatch); and if GA4 were ever removed, a policy still describing it over-claims what is collected - the same drift in reverse. Comment-stripped, and that is not a formality here: the fix left an HTML comment quoting the old denial verbatim so the correction stays legible, and a detector reading comments would find 'when we add web analytics in the future' in the FIXED file and convict the page for documenting its own repair - the single most reliable false positive in this whole gate family, hit three times on other subjects this session. Selftest 6/6, including a non-vacuity check that the GA4 page roster is not empty.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-note-survives-the-reply",
        "script":  "tools/validate_the_note_survives_the_reply.py",
        "label":   "T12 (2026-08-28): a worker's note must outlive the AI that was going to answer it. voice-journal is where someone captures a thought mid-shift; the companion's reply is the nice part, the NOTE is the part that matters. ★THE ORIGINAL DESIGN LOST THE NOTE TO A QUOTA ERROR: the write happened server-side inside the voice-journal-agent call, so a 429 from the gateway meant the row was never written - the page was HONEST about it ('your note was not saved') and the note was still gone. On a shared free tier that is Friday afternoon, which is exactly when a worker is capturing something they will not remember later. Three clauses: (1) the page has its OWN EYES - noteLanded() re-reads for the note instead of inferring its fate from the call's outcome, because an error on the reply says nothing about whether the row exists and a page that guesses will tell a worker their words are gone when they are safe, or the reverse, which is worse; (2) it says the true sentence on BOTH paths - typed and spoken are separate catch blocks, so a fix landing in one produces a page that tells the truth depending on how you happened to enter the note; (3) the microphone failure NAMES ITS OWN CAUSE - one catch blamed 'permission denied' for every getUserMedia failure, so a mic-less desktop sent the worker to check a permission they never refused, and branching on err.name (NotAllowed/Security vs NotFound/Overconstrained) is the difference between a remedy and a wild goose chase, with every branch still naming the typed fallback. Selftest 5/5, with the two reply paths mutated independently.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-ack-scope-matches-its-promise",
        "script":  "tools/validate_the_ack_scope_matches_its_promise.py",
        "label":   "T144 (2026-08-28): acking a plant alert hides it for the WHOLE CREW, and the page must say so. alert-hub's dismissal is HIVE state - the upsert is keyed onConflict: 'hive_id,alert_key', NOT on the actor - so one person marking an alert handled removes it from every teammate's feed. That is deliberate: a plant alert is about a machine, not a personal inbox, and it should stop nagging the crew once somebody has dealt with it. ★IT IS ALSO THE ONE FAILURE MODE THIS PAGE CANNOT AFFORD IF IT HAPPENS SILENTLY - a worker tidying what they believe is their own view can blind the entire crew to a live plant condition - so the scope and the SENTENCE are held as ONE property: the write is hive-keyed; the row still records actor (crew-wide AND anonymous would be far worse than either alone); and the toast says both, 'hidden for your whole hive now, recorded under your name'. The old copy said only 'Alert marked handled', which is true and tells the person nothing about who else just stopped seeing it: a scope this wide is defensible when stated and indefensible when discovered. Also gates convergence - setInterval(loadAll, 60000) so a second tab is not stale forever, and a visibilitychange pause so a phone in a pocket is not polling a plant database with no reader. Selftest 6/6. ★ITS ATTRIBUTION NEGATIVE WAS VACUOUS TWICE: a whole-file search for actor: WORKER_NAME also matched an UNRELATED upsert and two comments, so removing it from every dismissal still passed; and the mutation that fixed that deleted the upsert prefix too, tripping 'no upsert found' while the case was labelled 'losing attribution' - detecting something real and proving nothing about the clause it named. The mutation is now a named helper that removes ONLY the actor field, verified to leave the statement intact.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-busy-button-says-working",
        "script":  "tools/validate_a_busy_button_says_working.py",
        "label":   "T41 (2026-08-28): a locked control announces WORK, not unavailability. button-lock.js is the platform's single-flight guard - withButtonLock disables a committing control while its work runs, so a rage-tap on a slow plant connection produces one effect instead of five - and it set disabled plus an is-loading class and nothing else. ★DISABLED AND BUSY ARE DIFFERENT SENTENCES: `disabled` alone announces 'unavailable', the same thing a control says when it is not applicable or you may not use it at all. A sighted user never hears that sentence - they see the spinner and know the press landed. A screen-reader user hears only 'unavailable', reasonably concludes the tap did NOT register, and presses again, on exactly the controls where a second press costs the most. aria-busy is the state that means in-progress. THE PAIRING IS THE OTHER HALF: it must be REMOVED in the release beside disabled, because a button left announcing itself busy forever after an exception is worse than one that never announced it - the control is now permanently lying. ★AND IT MUST HOLD IN BOTH PAIRS: this file has TWO lock/release paths (withButtonLock, which awaits an async fn, and lockButtonDuring, which returns a release closure), and fixing only one produces a shared helper that is right HALF THE TIME - harder to trust than one consistently wrong, since three pages adopt it today and every future adopter inherits whichever path it happens to call. Selftest 6/6, with each pair mutated INDEPENDENTLY so a fix landing in only one path is caught.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-photo-cannot-orphan-the-entry",
        "script":  "tools/validate_the_photo_cannot_orphan_the_entry.py",
        "label":   "T9 (2026-08-28): a repair entry and its photo land together or not at all. The logbook is the platform's core write - a worker on a plant floor, one-handed, recording what they just fixed - and attaching a photo is where that write goes wrong on most systems, because a photo normally means a SECOND write (upload the file, then insert the row) and two writes have two ways to half-succeed: an orphaned file with no entry, or an entry claiming evidence it does not have. ★NEITHER IS REACHABLE HERE, BY CONSTRUCTION: logbook.html contains NO storage.from(...).upload() at all - the photo is compressed client-side and carried INLINE as a data URL in logbook.photo (a text column) inside the SAME insert as the entry, so there is no second write to fail. The decoupling T12 had to ARGUE for on voice-journal is free here. That is precisely why it needs a gate rather than a comment: adding an upload to this page would read as an IMPROVEMENT in review ('move photos to storage, keep rows small') and would silently reintroduce a failure class the page currently cannot have. ★THE COUPLING RUNS THE OTHER WAY AND IS THE REAL RISK - one row carries the typed work AND the image, so a big photo makes a big row and a failed insert takes the worker's words with it. Both halves gated: an oversize file is refused BY NAME before compression ('Photo too large (max 20 MB).') so the worker learns the limit instead of watching a save fail, and the image is compressed rather than sent raw. Fourth clause: when the AI vision path fails it must say the entry is UNCHANGED, because an analysis failure is not a save failure and a worker who cannot tell them apart retypes work that was never lost. Selftest 5/5, comment-stripped - this page documents its single-write design thoroughly, which is exactly what would let a prose-reading detector pass a file that had lost it.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-deferral-is-not-a-completion",
        "script":  "tools/validate_a_deferral_is_not_a_completion.py",
        "label":   "T10 (2026-08-28): a task put off is not a task done. A worker who cannot do a PM today DEFERS it; one who does it COMPLETES it, and the gap between those is where a compliance percentage becomes a lie - if a deferral sets last_completed_at, next_due_date rolls forward, the item drops off the overdue list, and the plant's number rises for work nobody did. Nothing errors; the number just quietly stops meaning maintenance. ★THE ENFORCEMENT IS ONE CLAUSE IN ONE VIEW, WHICH IS EXACTLY WHY IT NEEDS A GATE: v_pm_scope_items_truth's LATERAL join carries AND (pc.status = 'done'), so every consumer inherits the rule instead of re-implementing it - right design, and also a single dropped AND that would look like a harmless simplification in a diff. Three independent checks, because each can be true while another is false: STRUCTURAL (the view still carries the filter), EMPIRICAL (of the 5 scope items whose completions are ONLY deferrals, 0 carry a last_completed_at - structure can be right while a backfill or manual UPDATE made the data wrong), and LEXICAL (pm-scheduler's history still maps status to 'Completed'/'Deferred'). ★TWO OF MY OWN CUTS CONVICTED CORRECT CODE FIRST. I put analytics.html in the render list on the strength of this trajectory's own prose; analytics does NOT render PM status - its matches are comments about deferred CHART DRAWS and a deferred redirect. And I hunted raw-enum leaks with a regex, which flagged pm-scheduler's `status: isDefer ? 'skipped' : 'done'` - the DB WRITE. The enum SHOULD appear in code that talks to the database; what must never appear is the enum on the GLASS, so the check now reads the display ternary rather than pattern-matching a string with every right to be there. Fails CLOSED when the DB is unreachable. Teeth 2/2: rebuilds the view from its OWN definition with the predicate stripped, inside a rolled-back transaction.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "presence-unavailable-is-not-absent",
        "script":  "tools/validate_presence_unavailable_is_not_absent.py",
        "label":   "T141 (2026-08-28): a fact about the NETWORK must not be rendered as a claim about PEOPLE. hive.html's presence bar answers 'who is here right now'; when the realtime link failed its fallback said 'Members offline' and painted every member with a solid offline dot - a positive claim about people made entirely from a fact about the websocket. Presence unavailable is NOT everyone-absent: the teammate you decide not to call may be standing at the machine. ★THE COST IS A DECISION, NOT A PIXEL - nobody reads a presence bar for fun, they read it to decide whether to walk over, phone someone, or handle it alone, and a bar that says 'offline' when it means 'I cannot see' sends that decision the wrong way while looking perfectly healthy. Same shape as a failed read rendering as an empty list, and it needs its own gate for the same reason: the honest state and the alarming state are both quiet. Four clauses - the fallback names the LINK; it asserts nothing about members being offline; the dot renders UNKNOWN (hollow fill with an outline) rather than the solid away-dot, because the dot is what people read at a glance; and both strings carry EN+FIL. ★THE FOURTH CLAUSE KEEPS THE OTHERS HONEST: renderPresence()'s genuine 'No one online yet' must stay DISTINCT, since that is TRUE when the link is UP and nobody is there - collapsing the two would trade this defect for its mirror image, reporting a real empty room as a network fault. Two notices, two truth conditions. Selftest 6/6, comment-stripped. ★ITS OWN FIRST RUN PRODUCED TWO CONFIDENT FALSE FINDINGS about correct code: find('function renderPresence') matches renderPresenceFallback, which is defined FIRST in the file, so the gate audited the fallback against the live path's rules. The lookup now requires the open paren - the same prefix-matching class as 'end' inside 'trend', caught only by the clean-file selftest case.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-secret-stays-masked",
        "script":  "tools/validate_a_secret_stays_masked.py",
        "label":   "T170 (2026-08-28): a password arrives masked, and revealing it ANNOUNCES that it is revealed. Two halves, each useless alone. MASKED BY DEFAULT: every password field ships type=password, and the shared whPrompt honours inputType:'password' end to end - which had a real gap, since _mount read inputType but whPrompt built its own options object and never forwarded it, so a caller asking for a masked prompt silently got a plain text box and a secret was typed in the clear on a shared plant tablet. The caller was correct; the plumbing dropped it with nothing looking wrong. REVEAL MUST SPEAK: a show/hide toggle is a usability necessity because people mistype passwords on phones, but a control that silently changes whether a secret is visible is a shoulder-surfing hazard for exactly the person who cannot check by looking - so it must flip BOTH aria-label (what pressing it does next) and aria-pressed (what state it is in now). ★AND THE GATE ANCHORS ON THE ACCESSIBLE NAME RATHER THAN THE CODE SHAPE FOR A MEASURED REASON: a search for controls flipping an input type found ZERO, twice, because the pattern expected a quoted literal and togglePwd assigns a TERNARY. The three aria-labels reading 'Show password' were the only evidence the toggles existed - the accessible name was what the code-shape search could not see. Selftest 8/8.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "at-cap-fits",
        "script":  "tools/prove_at_cap_fits.mjs",
        "args":    ["--gate"],
        "label":   "T130 (2026-08-28): the longest LEGAL value must not break the form that accepts it. A maxlength tells a person how much they may type and says nothing about whether the layout survives them typing it - a field can advertise 200 characters and blow its container at 180, with nobody knowing until a worker with a long part number meets it on a phone. ★A RESTING SWEEP CANNOT SEE THIS, which is why the prover drives flows open: filling capped fields at rest reached 10 of 66, because the other 56 live inside modals, sheets, composers and wizard steps that do not exist until something is clicked - inventory, logbook, project-manager, community and pm-scheduler each reported ZERO capped fields, not for lack of caps but for lack of an opener. Reporting '66 fields, 0 break' off that run would have been a skipped partition reading as a covered one. The value is a single unbroken run of 'W' on purpose: a cap-length SENTENCE wraps and hides the overflow, while a cap-length part number, serial or URL - what people actually paste - does not. ★TWO OF MY OWN CUTS WENT GREEN OVER NOTHING BEFORE THIS WAS RIGHT. First, measuring geometry without visibility produced 18 'defects' that were one CLOSED feedback drawer (visibility:hidden at x=754 against a 390 viewport) counted once per page. Then, guessing the surface selector as '#part-form, #add-part-modal, form' when the real container is #part-modal reached 0 of inventory's 4 fields and reported ok - a prover that opens the right form and measures the wrong box is indistinguishable from a clean run. So each flow now declares how many fields it EXPECTS and under-reach is a FAILURE, not a quiet zero. 18 capped fields across 7 flows at 390, 0 breaking. Teeth 7/7: every flow with fields goes red when its container is widened, each verified clean=0 first. Read-only - fills, measures, restores; never submits.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-concurrency-guard-can-fire",
        "script":  "tools/validate_the_concurrency_guard_can_fire.py",
        "label":   "T138 (2026-08-28): a guarded table must move its updated_at SERVER-side, not by caller discipline. Ten pages guard an edit by re-sending the row's last-seen updated_at as a filter, so a stale writer matches no row and is told. The stamp can move two ways: the CALLER sends updated_at in its payload (what inventory.html has always done, and it works), or a TRIGGER sets it. This gate ratchets the second. ★IT EXISTS BECAUSE I FIRST REPORTED THE FIRST ONE AS A DEFECT AND WAS WRONG: inventory_items had no trigger, so I probed with `UPDATE ... SET qty_on_hand = ...`, saw the stamp frozen and writer B overwrite writer A, and filed a guard that could never fire. The page does not send that statement - it sends `SET qty_on_hand = ..., updated_at = <now>`. Re-raced with two PRODUCT-SHAPED writers and the trigger dropped: A rows=1, B rows=0, A survived. The guard had always worked; I had measured a shape the product never sends, and the frozen stamp was an artifact of my statement rather than a property of the table. THE HONEST PROPERTY, which is what is gated: caller-supplied stamps work but are a promise every future edit path must keep, and the failure is SILENT - omit the field and the concurrency check quietly stops, with nothing erroring. A trigger cannot be forgotten, and it also makes the stamp server-authoritative instead of trusting new Date() on a device whose clock may be wrong. A red here means 'this table relies on caller discipline', NEVER 'this guard is broken'. 9/9 green, fails CLOSED when the DB is unreachable, teeth 2/2 by dropping the trigger inside a rolled-back transaction.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "the-interrupted-action-completes",
        "script":  "tools/validate_the_interrupted_action_completes.py",
        "label":   "T5 (2026-08-28): the action a person tapped BEFORE the wall must finish after it. An anon visitor taps Save on a listing; the platform answers with a sign-in door, correctly, and what decides whether the door was worth walking through is what happens on the far side. Returning them to the PAGE is the easy half and was already done; returning them to the ACTION is this one - toggleWatchlist's anon branch stashes wh_pending_action, loadWatchlist replays it once the person has a name. Proven live 2026-08-25 and then left with NO GATE, which is why this exists: the replay is four lines inside a loader, invisible to every other oracle, and its failure is SILENT - nothing errors, the listing is simply not saved, and only the person who tapped knows. Holds four clauses, each of which a plausible edit removes: the intent is stashed (in sessionStorage, so it dies with the tab - one that outlived the session would fire days later against a listing they no longer want); it is actually REPLAYED (a stash nobody reads does nothing); it is SINGLE-SHOT, the key consumed BEFORE the call so a throwing replay cannot loop; and it is IDEMPOTENT. ★THE LAST CLAUSE IS THE ONE WORTH GATING: the other three fail visibly the first time anyone walks the flow, but this one fires only when state raced ahead - saved in another tab, saved another way - and since toggleWatchlist is a TOGGLE, replaying over an already-saved listing UN-SAVES the very thing the person asked for. Its symptom reads as 'the platform lost my save', not as a replay bug. Selftest 7/7, comment-stripped because the fix's own comment describes all four clauses and a prose-reading detector would pass a file whose CODE had lost them.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "read-only-lane",
        "script":  "tools/prove_read_only_lane.mjs",
        "args":    ["--gate"],
        "label":   "T53 (2026-08-28): a person who only READS is never forced to write. Not every account operates the plant - a manager, an owner, a client on a site visit signs in to LOOK, reads, and leaves. That lane had no gate, because every existing journey asserts that an ACTION succeeds, and an action-shaped oracle cannot see this persona's defect: being stopped before reading anything at all. ★THE TRAJECTORY SPLITS AND ONLY HALF IS GATEABLE: whether the exec deserves a different LANDING is a design call and stays Ian's; whether a reader is FORCED INTO A WRITE is observable on arrival and needs no decision. Walks six exec-shaped surfaces signed in, settles, and requires no blocking modal, no body-scroll lock, no sign-in/pick-a-hive wall, no focus stolen into a text input (on a phone that opens a keyboard at someone who came to read), and no page error. ★THE SIXTH CLAUSE IS THE LOAD-BEARING ONE, because the other five are all ABSENCES and a BLANK PAGE satisfies every one of them - an all-absence oracle grades a dead page as a perfect reading experience. So it also requires the reader actually got words, counting VISIBLE TEXT rather than figures: T53's own walk twice misread a healthy surface as thin, once by counting characters mid-render and once by counting figures on analytics-report, whose correct state is an instruction with no figures at all. The 220-char floor is deliberately low for exactly that page. Read-only by construction - never clicks, types or submits. 6/6 green at 1280. Teeth 5/5 via --teeth: each clause injected on a real signed-in page and asserted to fire, each checked non-vacuous (the clean page reported 0 first), because a gate locking already-correct behaviour has never been seen to go red and is otherwise a green light of unknown wiring.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "a-dismissal-belongs-to-a-person",
        "script":  "tools/validate_a_dismissal_belongs_to_a_person.py",
        "label":   "T121 (2026-08-28): a dismissal is a person's decision, not the browser's. The shared station tablet is this platform's normal deployment - one device, a crew signing in and out through a shift - and three dismissal flags stored the literal string '1', which belongs to the BROWSER: wh_onboarding_dismissed (index's first-run Getting Started ladder), wh_cmms_guide_dismissed (integrations' setup guide), and wh_guide_link_dismissed_<page> (learn-link's 'New to this page?' chip, which rides EVERY page). Worker A taps Dismiss; worker B signs in on the same tablet and is never introduced to anything. ★THIS LEAK RUNS THE OPPOSITE DIRECTION FROM ITS SIBLINGS, WHICH IS WHY IT SURVIVED THEM: the draft, filter and companion-history fixes all stopped A's CONTENT reaching B, so they read as privacy work and this did not - nothing of A's is exposed. What crosses is A's DECISION, and the cost lands on B as an ABSENCE: no card, no chip, nothing on screen to notice or report. The onboarding ladder made it sharper still - its three step-checks were ALREADY person-aware (workerName, jobs, pmDoneToday), so the card knew B had done nothing; it just never got to run, because the flag returned before the check. Correct logic behind a device-wide gate. Fixed centrally (whStateOwner/whIsDismissed/whSetDismissed in utils.js, replacing a FOURTH hand-rolled owner read) and at all three sites; a legacy '1' is nobody's, so the guide returns once - for HELP that is the safe direction, since re-showing a card costs a tap and hiding it costs the introduction. sessionStorage flags are exempt: they die with the tab. Live-proven at 390 (A dismissed -> B sees the guide -> A still dismissed -> legacy '1' belongs to no one), probe restoring the real owner it captured first. Selftest 9/9, including two non-vacuity checks: the first versions of both negatives swapped only the helper CALL, leaving the typeof guard inside the exemption window, so they passed while detecting nothing.",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "animation-never-hides-content",
        "script":  "tools/validate_animation_never_hides_content.py",
        "args":    [],
        "label":   "CF: an optional API's absence must never hide CONTENT (T119, 2026-08-27). MEASURED by removing the capability an older engine lacks: index.html styled `.reveal { opacity: 0 }` and made those elements visible only when an IntersectionObserver callback added `.visible`, so on an engine without the API the constructor THREW, the rest of that script block never ran, and all 14 .reveal elements stayed invisible permanently - the landing page rendered 525 characters of text where every other page rendered 1146+. ★NOTHING WOULD HAVE REPORTED IT: no failed request, no error state, no blank shell - the page 'loaded fine' and simply never faded its content in, the same shape as a skeleton that never resolves. And the source SAID it was safe; the comment above the observer read 'all browsers', which is an assumption wearing the clothes of a check. ★THE GATE REQUIRES BOTH HALVES, because a guard alone is not enough: skipping the observer while leaving opacity:0 in place would pass a naive 'is it guarded' test and still ship a blank page. So every construction must have a feature check BEFORE it - positionally, since a check that runs after the throw protects nothing - AND the hidden class must have a reveal-everything fallback. Fixed at all five call sites, each failing OPEN: the main reveal shows everything, the stagger reuses its EXISTING reducedMotion branch rather than inventing a second path, the before/after rows are shown outright, and the sticky-CTA and nav-highlight observers (which only ADD behaviour) return early - the nav one was the loudest, constructing an observer PER SECTION. Verified both ways: without the API index renders 1061 chars and nothing throws; with it, .reveal still gains .visible on scroll. Teeth 4/4.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "dead-end-has-a-door",
        "script":  "tools/validate_dead_end_has_a_door.py",
        "args":    [],
        "label":   "CF: a read-failure state must offer a way OUT, not just a sentence (T193, 2026-08-27). whListError is the shared read-failure panel behind ~24 pages, so what it renders IS the platform's error state, and it must carry all three doors, each answering a question a stuck person actually asks: RETRY ('try again' - the answer when the failure was transient), STATUS ('is it just me?' - a link to status.html, the answer when it was not), and THE ESCALATION DOOR ('then who do I tell?' - into the feedback queue with page, error sentence and time PRE-ATTACHED, so a report describes the failure instead of saying 'it doesn't work'). Lose the third and someone who has retried, checked status and found the platform healthy is simply stuck. ★WHY IT GATES THE HELPER AND NOT A PAGE CENSUS: a first cut counted pages that call whListError and also ship the feedback widget, and reported ALL 24 as missing it - because no page ships it. nav-hub.js injects wh-feedback-fab.js centrally. The mechanism was fine and the census was measuring a per-page script tag that does not exist by design; absence of a per-page marker is not absence of the capability, so the property is held where it lives, in the one function that renders the state. ★THE DOOR IS CONDITIONAL ON PURPOSE - rendered only when window.WHFeedback.open exists, so it can never be a door onto nothing - which makes TIMING the real risk, since the fab is injected asynchronously and a read failing faster than it loads would build a panel with no door. Measured live under aborted reads: WHFeedback ready at 1200ms, before any panel built, and a panel rendered on demand carries retry + status + door at a 44px gloved-hand target. Teeth 4/4, one per affordance removed in turn.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "typed-work-is-drafted",
        "script":  "tools/validate_typed_work_is_drafted.py",
        "args":    [],
        "label":   "CF: a committing surface that holds TYPED work must be able to give it back (T38, 2026-08-27). A session dies, a phone is backgrounded, a tab is killed. On a page someone only READ, that costs a reload; on a page where they TYPED, it costs their words - and this platform's whole promise on a plant floor is that the work you did is the work that is recorded. So a page that both writes rows and offers a textarea must carry a draft mechanism. ★THE GATE'S OWN FIRST CENSUS WAS WRONG, WHICH IS WHY THE RULE IS SHAPED THIS WAY: checking for `whAutoSaveDraft` alone reported logbook.html - the flagship committing surface of the entire platform - as UNPROTECTED, when it has carried its own DRAFT_KEY / saveDraft / restoreDraft trio for months. Absence of the shared HELPER is not absence of the GUARD; a gate that recognises one implementation measures a naming convention, not the property, and would have sent someone to 'fix' a page that was already correct. Both forms count and a page is a finding only when NEITHER is present. MEASURED: 12 committing surfaces with typed work - 11 on the shared helper, logbook on its own, assistant exempt by reason (a chat turn is SENT immediately, so there is no compose-then-commit gap to lose) - and resume.html had NOTHING. Its #jd-input holds a job description the person PASTED from elsewhere, exactly the work that is expensive to reproduce: a session dying between the paste and the Tailor call sent them back to find the posting again. Fixed with the shared helper (which also stamps owner and age, so a shared plant tablet never hands one worker's paste to the next) and PROVEN LIVE - the paste survives a full reload byte-for-byte, restored into an empty field only. Teeth 4/4, including the one that matters: a page whose protection is page-local rather than shared must PASS.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "earning-moment-is-announced",
        "script":  "tools/validate_earning_moment_is_announced.py",
        "args":    [],
        "label":   "CF: every success path of an XP-earning action must name the award (T17, 2026-08-27). MEASURED: a pm_completions insert pays XP through trg_pm_achievement, and submitCompletion() has TWO terminal success paths - one when the worker also mirrors the work to the logbook, one when they do not. The first announced ' · +N XP'; the second was SILENT. Same action, same trigger, same award, two different receipts, decided by an unrelated toggle ('also log this to the logbook') that has nothing to do with earning. That is the sibling-drift shape this codebase keeps meeting: a fix lands on the path someone walked and not the one beside it. ★SO THE RULE IS NOT 'THE PAGE MENTIONS XP SOMEWHERE' - it is that every success branch routes through ONE helper, because two inline copies would satisfy a mention-count check and drift again. ★AND THE RECEIPT READS THE LEDGER, NEVER RECOMPUTES IT: the trigger's rule lives in SQL (20 XP, +20 more for a detailed entry) and a JavaScript copy would be a second source of truth for a number shown to a worker as fact, so the helper reads achievement_xp_log back by source_id. Verified live that it is RLS-scoped: a row the signed-in worker owns returns ' · +60 XP', another worker's returns '' - so the receipt can neither leak nor overstate - and a failed read returns '' too, making it quieter but never wrong. The dedup-upgrade path is exempt BY REASON, not by silence: it turns a deferral into a completion with an UPDATE, and the trigger fires on INSERT, so no award is paid and claiming one would be inventing it. ★ITS OWN MATCHER WAS WRONG FIRST: a regex window stopping at the first ')' landed INSIDE the message text ('...Analytics (SMRP).') and reported two already-fixed paths as silent - a matcher that stops at a bracket the COPY happens to contain is measuring punctuation, not code - so it now scans balanced parens. Teeth 4/4: silent sibling, a helper that invents a number, a helper that counts reversed awards, and the clean surface.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "dialogs-fit-the-floor",
        "script":  "tools/prove_dialogs_fit_the_floor.mjs",
        "args":    ["--gate"],
        "report":  "dialogs_fit_the_floor_report.json",
        "label":   "CF: every dialog in the bank must fit the floor viewport, controls included (T113, 2026-08-27). ★ITS DENOMINATOR IS THE PLATFORM'S, NOT MINE: flow-fits-the-floor walks four flows I chose by hand, and a gate whose subject list I wrote can only find what I thought to look at. This one consumes tools/dialog_targets.mjs - already the ONE source of truth for how each V2/V3 dialog opens, every entry's path READ FROM SOURCE with its `ref` line - so any dialog added there is covered here for free. 42 subjects, 33 measured, 0 unreachable controls at 320x844. ★MEASURE THE SHEET, NOT THE SCRIM: marketplace's openSheet(name) opens BOTH `#overlay-<name>` and `#sheet-<name>` - the overlay is the backdrop and the SHEET carries the content - so measuring the overlay found it permanently empty even after a real listing click, and the gate was one step from filing 'tapping a listing opens a blank full-screen sheet' against a marketplace that is fine. Seven commerce dialogs went from unmeasurable to measured (21, 6, 6, 5, 4 and 14 controls), taking coverage 26 -> 33. Reaching them also took two corrections the gate reported honestly rather than passing over: listing cards bind by addEventListener on .listing-card[data-id], not an inline onclick, and openDetailSheet lives in the page's closure so page.evaluate cannot call it - the fix is to CLICK the card, which is the real user action anyway. The content opener lives in THIS file rather than dialog_targets.mjs on purpose: that registry is shared with the modal-exit and dialog-layout provers, and changing an opener under them is the one-shared-fix-expires-unrelated-claims shape. ★AND THE SUBJECT TEST IS THE WHOLE GATE: the registry holds fixed overlays AND in-flow panels/tabs/lists, and bottom-checking the in-flow ones flagged SIX 'unreachable' controls (asset-hub's FMEA panel, its Weibull panel, engineering-design's history tab, analytics' summary, alert-hub's AMC card, voice-journal's history list) that a reader reaches by simply scrolling the page. Every one would have been a false finding banked against a page that is fine. The property is UNREACHABILITY, not off-screen-ness: the bottom axis applies only where the surface is fixed AND neither it nor an ancestor scrolls, because a dialog is centred and does not scroll, so a control past its fold is a button that cannot be pressed - whereas page-flow content past the fold is what scrolling is for. ★A SURFACE WITH NO CONTROLS IS UNMEASURED, NOT PASSED: twelve of the first run's 'ok' rows held zero controls and proved nothing, so they are now reported as skipped rather than padding the denominator. Preconditions that fail are UNGRADED, per the registry's own rule. Opens and measures; never submits. Proactive guard, teeth are synthetic negatives 6/6, including the two that matter most - a FIXED sheet IS bottom-checked and an IN-FLOW surface is NOT.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "printed-truncation-is-disclosed",
        "script":  "tools/validate_printed_truncation_is_disclosed.py",
        "args":    [],
        "label":   "CF: a printed artifact that shortens free text must SAY so (T49, 2026-08-27). export-reads-its-own-set holds the ROW axis - a CSV must not inherit the render buffer's cap, so no row goes missing. This holds the CELL axis on the same kind of artifact. MEASURED: project-report.html rendered every daily-progress log through `.slice(0, 200)` with nothing appended, and that page exists to be PRINTED and handed to a stakeholder. On paper there is no hover, no expand, no scroll - a 350-character blocker arrived as a complete-looking 200-character sentence, usually cut mid-word, and the reader had no way to know anything had been removed. Blockers is the field that carries WHY a project is late. ★THE RULE IS NOT 'NEVER TRUNCATE' - a printed column is real and text has to fit - IT IS 'TRUNCATE OUT LOUD': the cap stays, and a conditional marker plus a one-line table note now disclose it, appearing ONLY when the cap actually bites so a complete report carries no false alarm. Verified live on WO-2026-001: 6/6 rows render, zero marks and no note (no fixture value exceeds 200), while clip() on a synthetic 500-char value keeps exactly 200 and marks it, and a 50-char value passes through untouched. ★AND THE MARKER MUST REACH PAPER: a disclosure hidden by @media print discloses nothing to the person holding the printout, so that is asserted too. Proactive guard, teeth are synthetic negatives, 5/5: bare prose slice, unconditional helper, marker hidden in print, helper with no marker, and the real surface clean. One of those negatives initially replaced a string that did not exist in the source, so it tested the UNCHANGED file and passed vacuously - fixed to strip every marker token including the title attribute.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "flow-fits-the-floor",
        "script":  "tools/prove_flow_fits_the_floor.mjs",
        "args":    ["--gate"],
        "report":  "flow_fits_the_floor_report.json",
        "label":   "CF: a flow's OPENED state must fit the floor viewport, not just the page at rest (T113, 2026-08-27). ★THE GAP: control-within-viewport walks 16 pages AT REST, but the controls that decide whether work gets RECORDED do not exist at rest - the PM completion sheet, the deferral control, the add-task sheet are built or revealed mid-flow, and each is a different layout from the page hosting it. The resting sweep can be green over every page while the Save button of the sheet a worker must press sits off the edge: the same class T113 already found twice, one interaction deeper than any resting check can reach. Walks T10 (complete a PM, and its honest alternative - record a DEFERRAL) and T13 (the clock-in handover read) at 320. ★IT ARRIVES THE WAY A WORKER DOES: alert-hub hands pm-scheduler an ?asset=, the detail opens, and a REAL scope item's mark-done control is clicked. An earlier cut called openSheet() by hand and measured an EMPTY sheet - the same seven controls whatever the task, because markDone() is what fills in the item text, asset name, frequency badge and findings fields. That would have proved the sheet's chrome fits, not the sheet. ★AND A FLOW THAT NEVER OPENED IS REPORTED AS UNREACHED, NEVER AS CLEAN: the first run passed page.evaluate three arguments where it takes one, and every flow errored - counted as an instrument failure, which is why the run did not read as three tidy surfaces. Read-only: it opens and closes, never saves. ★AND FOR AN OVERLAY THE EDGE THAT MATTERS IS THE BOTTOM (T17 added 2026-08-27): the level-up card is a CENTRED FIXED overlay, so a card taller than a 320x844 phone puts its only dismiss button below the fold, and because the overlay does not scroll there is no gesture that reaches it - the reward moment becomes a trap. Driven through showLevelUpModal() with a REAL achievement id so the card carries its badge, heading and tier line, since an empty overlay is a different height. The bottom axis is checked only where a flow asks for it, because an ordinary PAGE is meant to extend past the fold - that is what scrolling is for - and flagging that everywhere would drown the finding. PROACTIVE guard (the property holds today), so its teeth are synthetic negatives, one per independent failure mode, all six verified: an off-screen control, a required control that is absent, a surface that does not exist, the real sheet clean at the floor, an off-BOTTOM control caught when the flow asks, and the bottom NOT checked when it does not - that last one proves the flag actually gates the check rather than being ignored. A width negative alone is NOT sufficient - at --width 200 the handover went RED while both PM sheets still passed.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "flow-speaks-filipino",
        "script":  "tools/prove_flow_speaks_filipino.mjs",
        "args":    ["--gate"],
        "report":  "flow_speaks_filipino_report.json",
        "label":   "CF: a flow's MESSAGES must speak the language the page is set to (T45, 2026-08-27). MEASURED: with wh_lang=fil, all three core field flows refused in English - logbook 'Please select an asset before saving.', pm-scheduler 'Please enter a task description.', voice-journal 'Write a note first, then send it.' ★WHY 100% FIL COVERAGE MISSED IT: the platform has TWO translation mechanisms and they cover different halves of the screen. data-i + whI18nApply swap STATIC markup at load - 442/442 here, which is where 'FIL is 100%' comes from - but every message the FLOW produces is born AFTER that pass, from showToast(...), which takes a raw string. The only translator that reaches those is _t(en, fil) at the call site. So the resting page was fully Filipino and the WORKING page was not, and no load-time DOM sweep can ever see it, because at load the message does not yet exist. ★THE EN CONTROL IS WHAT MAKES IT A MEASUREMENT: each step runs twice, fil and en, and only an identical multi-word message counts as untranslated - separating 'never translated' from 'same word in both languages'. ★AND AN EMPTY READING IS THE INSTRUMENT, NEVER A PASS: three earlier cuts of this prover scored a resting toast placeholder ('Entry saved.' is markup, not a message), a click that threw 'not visible', and a closed <details> whose hidden children still report layout rects - so the gate now refuses to grade any step it did not demonstrably reach. The steps are REFUSALS on empty required fields, so the walk writes no rows and needs no cleanup, and a refusal is the message a worker can least afford to not understand. Fixed by wrapping 14 call sites across the three flows, incl. a second inline refusal path for the same string. Resurrection-proved: the pre-fix logbook turns it RED (exit 1), the fix GREEN.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "bounded-list-offers-the-rest",
        "script":  "tools/validate_bounded_list_offers_the_rest.py",
        "args":    [],
        "label":   "CF: a capped list must admit the rest exists (T129, 2026-08-27). A long list has to be capped - the DOM cannot carry two thousand rows and stay usable - so the danger is never the cap itself, it is a SILENT one: rows 1-20 of 548 rendered with nothing on screen admitting the other 528. That reads as 'this is everything', and a supervisor planning against it plans against a fifth of their own history. MEASURED at two volumes, which is what this holds in place: with the hive at 941 rows (314 for the signed-in worker) and again at 1641 (548), rows painted stayed 20 -> 20, DOM nodes 1458 -> 1396, first rows 1125ms -> 1221ms. A 75% growth in the reader's own history cost 96ms and NO dom weight, because the slice is constant - and the load-more wrap was offered rather than hidden, so the remainder was disclosed both times. THE PROPERTY IS THREE-PART because any one alone is not enough: the render must SLICE by a display count (or the page grows with the data), something must INCREASE that count (or the cap is a wall, not a page), and the control must be revealed exactly when more rows exist - toggle('hidden', count >= total). That third part is what makes the cap honest; without it a list can slice, own a hidden button, and still tell the reader nothing. Covers logbook and inventory, the two surfaces long enough to need it; teeth are a synthetic negative per part.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "journey-na-is-earned",
        "script":  "tools/validate_journey_na_is_earned.py",
        "args":    [],
        "label":   "CF: a journey cell may only be N/A for a reason the walk actually observed (2026-08-27). An N/A is the cheapest verdict on the board - it removes a cell from the denominator without anyone having to see a failure - so it is the one that must be earned. MEASURED against journey_report.json, this found two ways it was not: (1) a handoff declared 'not measured' while several of its own candidates WERE in the roster - the chooser returned a single best candidate and discarded the rest, so a walkable hand-off was excluded because the FIRST pick happened to be off-roster (hive named 5 such, skillmatrix 2, community 1); (2) an abandon cell blaming its opener for 'never becoming clickable' when the persona had been REDIRECTED AWAY and never reached the page at all - the oracle could not tell 'the control is stuck' from 'we were never there', and both read as the same N/A. Both are fixed in prove_journey.mjs (the chooser now ranks every candidate and prefers a roster one; abandon distinguishes redirected / not-in-DOM / hidden-or-disabled / present-and-stuck) and this gate holds the line: it re-reads the live report every run, so a future N/A that names a reason the walk did not observe turns it red.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "source-chip-names-a-real-relation",
        "script":  "tools/validate_source_chip_names_a_real_relation.py",
        "args":    [],
        "label":   "CF: a provenance chip must name a relation that EXISTS (2026-08-27). MEASURED: hive.html's readiness chip named `v_maturity_truth` and there is no such relation - its own comment states the promise it was breaking, 'every readiness number on the page is reproducible by querying this view', so a supervisor who followed the chip to run that ad-hoc query got `relation \"v_maturity_truth\" does not exist`. The numbers were correct; the TRACE dead-ended, which is the only thing a provenance chip is for. The real view is v_hive_readiness_truth (verified to carry composite_score, current_stair and the five dimension columns the card renders). A second chip cited 'at_risk' as a source when it is a TIER LABEL, not a relation. ★WHY prove_source_chip.mjs COULD NOT SEE EITHER: it asks the sharper question - does the chip name a relation the page actually REQUESTED? - from captured PostgREST traffic; but hive.html renders readiness through the compute_hive_readiness RPC, so the chip carries a `source-chip-allow` marker. That exemption is legitimate for the network check and was never meant to exempt the chip from being TRUE, yet nothing else tested the name, so it was checked by nothing at all. An exemption must waive the check it was written for, never the question of whether the claim holds. Scope is deliberately narrow so it cannot cry wolf: chip sources are free prose, so only identifier-shaped tokens are tested, and tables, views, functions AND column names all count as real (the first cut flagged 'qty_on_hand'/'min_qty'/'at_risk'/'get_hive_dashboard' - columns and an RPC - which would have trained readers to ignore it).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "records-linked-is-true",
        "script":  "tools/validate_records_linked_is_true.py",
        "args":    [],
        "label":   "CF: the signup notice may only claim what was actually linked (2026-08-26). After creating an account index.html could announce 'Your existing records as <name> are now linked to your account' - and it decided that by COUNTING v_worker_truth rows matching the display name. That view exposes worker_profiles.display_name and is security_invoker, so under RLS a brand-new user can see exactly ONE row: the profile the very same flow inserted moments earlier. ★MEASURED on a fresh identity with no hive: c2_count = 1, visible_rows = 1 - so the count was always >= 1 and EVERY new signup was told their existing records had been linked. For someone who had none that is false; for a namesake it claims another person's work history, which is the same identity-by-name trap that let a second Juan Dela Cruz be refused a hive. ★THE CHECK AND THE ACTION READ DIFFERENT ROWS: the reassurance was computed from PROFILE rows while the actual linking is done by resolveActiveHiveContext(), which reads MEMBERSHIP. The notice now rests on that function's return value - non-null exactly when a membership was found and linked - and NAMES the hive it landed in, so the claim says what it rests on. ★THE GATE RUNS THE DB HALF LIVE rather than only reading the page, because the page assertion alone would not explain itself: it re-seeds a membership-less identity and re-measures the self-match, so if that count ever stops being 1 the gate says the PREMISE changed instead of passing quietly.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "hive-seating-is-by-identity",
        "script":  "tools/validate_hive_seating_is_by_identity.py",
        "args":    [],
        "label":   "I-pillar: which hive a session is seated in is decided by AUTH, not by a name (2026-08-26). resolveActiveHiveContext() in index.html chooses the tenant a signed-in session lands in, and it selected memberships with .eq('worker_name', displayName) - where worker_name in v_worker_truth is worker_profiles.display_name. So the query asked 'which memberships belong to this NAME' and depended entirely on the profiles_read_own RLS policy (auth_uid = auth.uid()) to turn that into 'which memberships are MINE'. ★CORRECT, AND FOR THE WRONG REASON - which is the whole point: measured, a caller sees exactly ONE profile row, so the name filter could only ever match themselves and there was no live defect. But the safety lived in the POLICY, not in the query, and widening profile reads even slightly - a co-member roster is an ordinary thing to want - would have let a NAMESAKE's memberships resolve here and seated a person in a hive they do not belong to. The view already joins membership on hm.auth_uid = wp.auth_uid, so the name filter bought nothing the identity does not buy more safely. Verified no behaviour change on a real multi-hive account: both filters return the same two memberships. ★THIS IS THE THIRD FACE OF ONE DEFECT, all found the same day: a hive join that refused a second Juan Dela Cruz by unique index, a signup notice that counted its own freshly-written profile row, and a tenant seat chosen by name. A name is not an identity. ★BOTH HALVES ARE ASSERTED because either can rot: the page must filter by identity AND worker_profiles SELECT must still be self-scoped - if that policy widens, the gate says so rather than passing quietly, since that widening is precisely what would have made the old code dangerous. An unknown identity must mean SOLO MODE, never a name guess. Teeth: reverting the filter to worker_name goes RED on both assertions.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "invite-code-is-unguessable",
        "script":  "tools/validate_invite_code_is_unguessable.py",
        "args":    [],
        "label":   "T185/security: the one credential that admits a stranger to a plant (2026-08-26). A hive's 6-character invite code is the whole of what stands between an outsider and a plant's maintenance records - hand it over and you are in - and it was generated with Math.random(), a plain PRNG whose internal state is recoverable from a handful of observed outputs, making later codes from the same page predictable. There was no compatibility excuse: createHive already REQUIRES crypto.randomUUID() (a null id aborts the create), so the page depended on modern crypto two lines away. ★THE CENSUS IS WHY THIS GATE IS NARROW AND NOT A BAN: 31 Math.random() uses across the production pages and exactly ONE was a credential - the rest are element ids, upload filenames, retry jitter and an analytics session id that already prefers randomUUID, all cases where collision resistance matters and unpredictability does not. Banning Math.random outright would have raised 30 false findings and taught everyone to ignore the gate. ★IT ALSO GUARDS THE MODULO, the part a future editor breaks silently: mapping random bytes with `b % len` is unbiased ONLY when len divides 256. The alphabet is 32 characters (O/0/I/1 omitted so a code survives being read aloud in a noisy plant) and 256 % 32 = 0, so each character gets exactly 8 byte values; add one character for readability and the first 256 % len characters quietly become likelier. The gate COMPUTES that rather than trusting it - teeth-proven by a 30-character alphabet, which it caught as favouring the first 16. ★COLLISIONS WERE ALREADY SAFE and were checked before being claimed: hives carries UNIQUE (invite_code), so a repeat is a clean create failure routed through whWriteError ('Nothing was made; try again'), never a cross-tenant join into the wrong plant.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "password-change-reachable",
        "script":  "tools/validate_password_change_reachable.py",
        "args":    [],
        "label":   "T59: a signed-in worker can find how to change their password (2026-08-26). MEASURED, and the answer was that the surface does not exist: there is no settings, account or profile page anywhere in the roster, and the ONLY password write on the platform runs inside the PASSWORD_RECOVERY auth event - so changing a password meant leaving the app, finding 'Forgot your password?' on the LANDING page, and waiting for an email. Nothing anywhere said so, leaving a security-conscious user hunting for a settings page that was never built. ★THE ROADMAP'S BAR IS 'findable and walkable, OR honestly stated absent' AND IT WAS NEITHER - which is why this was fixable without waiting on the product decision. It does NOT build the account surface (active sessions, sign-out-everywhere, 2FA remain Ian's call); it surfaces the flow that ALREADY WORKS, in the one place this platform keeps account actions - beside Sign Out on the ops-home header - reusing window.forgotPassword, whose copy already handles the case that matters most here: the many field workers with no email on file are told to ask their supervisor. ★THE GATE IS LIVE BECAUSE A STATIC CHECK CANNOT ANSWER THIS: a button whose handler throws, or which renders behind a hidden panel, reads identically in source to one that works. It signs in at 390, asserts the control is VISIBLE, meets the 44px touch floor a gloved hand needs, and that CLICKING it produces a prompt with an input and copy explaining what will happen. ★FIL PARITY IS PART OF THE FIX, not an afterthought - the new [data-i] key carries 'Palitan ang Password', verified live by flipping the toggle, because a data-i the swapper does not know silently stays English for every Filipino user. Teeth: removing the control reports that a signed-in worker has NO way to change their password.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "return-promise-survives-reauth",
        "script":  "tools/validate_return_promise_survives_reauth.py",
        "args":    [],
        "label":   "T8: re-authenticating costs one sign-in, not your place (2026-08-26). When a token expires, session-timeout.js sends the worker to index.html?signin=1&return=<where they were>, and index.html validates that value before honouring it - an open-redirect guard, and the right instinct. But the guard's character class did not include characters the platform's OWN pages write into their URLs. ★MEASURED BY ROUND-TRIPPING THE REAL CHAIN (page writes filter -> session-timeout captures -> index re-reads): URLSearchParams.set() encodes a space as '+', and audit-log.html mirrors its filters into the URL including `actor`, which is a worker's NAME - so `audit-log.html?actor=Leandro+Marquez` and `audit-log.html?q=pump+seal` both failed the guard and were dropped IN SILENCE, landing the supervisor on the dashboard. That is T28's exact scenario, someone mid-investigation into who changed what, losing their place to a token refresh with nothing on screen explaining why. ★WHY WIDENING TO + , : IS SAFE, ARGUED NOT ASSUMED: the guard's job is to refuse a scheme, a protocol-relative //, and a foreign absolute path, and its ANCHOR already requires the value to begin with <name>.html or /workhive/ - so those three can only appear AFTER the ? or #, where a colon cannot read as a scheme and a plus is just a space. The gate proves both halves rather than trusting the argument: 8 real destinations survive AND 10 attack shapes (javascript:, //evil.com, https://, ../../, backslash-UNC, index.html@evil.com) are still refused. ★IT EXTRACTS THE REGEX FROM index.html instead of restating it, because a gate holding its own copy proves only that the copy is fine - and it FAILS IF THE THREE GUARDS DISAGREE (sign-in, signup, entry bridge), since a fix reaching one path and not the others is how this class survives. Teeth proven on both: the pre-fix class resurrects the two dropped destinations, and reverting a SINGLE guard is caught as disagreement.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "signout-says-why",
        "script":  "tools/validate_signout_says_why.py",
        "args":    [],
        "label":   "T8: the sign-in screen says WHY the worker is looking at it (2026-08-26). A worker mid-task looks up and finds a sign-in modal with no explanation, which is indistinguishable from being logged out at random - and the question in their head goes unanswered: did I lose what I was doing? ★ONE FUNCTION, THREE EVENTS, THREE DIFFERENT TRUTHS: session-timeout.js's clearIdentityHard() runs for an automatic idle timeout, for the worker deliberately pressing Sign out, AND (via the storage listener) for a sign-out in another tab. Telling someone who just pressed Sign out that their session 'timed out' is false, so the reason is now a parameter, the deliberate path passes none, and the gate asserts that separation - collapsing it is the easy mistake, and passing clearIdentityHard straight to addEventListener would make the click EVENT the reason. ★THE REAL ASSERTION IS THE CLAIM'S PRECONDITION, NOT THE WORDING: the notice tells an idle-timed-out worker 'your drafts are still here', which is true ONLY because clearIdentityHard wipes identity keys alone (wh_last_worker + the hive keys) and never touches whAutoSaveDraft storage. Add a draft key to that wipe list and the copy silently becomes a lie no test would catch - so the gate READS the wipe list and fails on anything draft-shaped. A claim has to rest on something; this is the something. ★AND THE COPY IS A WHITELIST, NOT INTERPOLATION: ?reason= comes from the URL, so it is looked up in a fixed table and written with textContent, and an unknown value renders NOTHING - verified live across four cases (idle and othertab show; a deliberate sign-in and a nonsense reason stay silent). role=status so a screen-reader user is told too. Teeth proven on all three.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "collision-is-not-try-again",
        "script":  "tools/validate_collision_is_not_try_again.py",
        "args":    [],
        "label":   "CE: a permanent collision must not be answered with 'try again' (2026-08-26). whWriteError is the write-error taxonomy, and it already understood this principle for DELIBERATE refusals - it passes 23514/42501/P0001 through in the guard's own words, with a note in the source explaining why: a seller told 'Save failed. Try again.' instead of 'Listing needs PHP50 credits held and you have 0 available' will retry forever, because retrying is precisely what cannot work. ★23505 HAD THE SAME PROBLEM AND WAS NOT ON THE LIST. A unique violation is permanent - the name, tag, code or username is TAKEN - so it fell to the caller's fallback. Measured before fixing, by calling the real function: a duplicate asset tag and a raced username BOTH returned 'The save did not go through. Nothing changed; try again.' It reaches real surfaces on day one: asset_nodes is UNIQUE (hive_id, tag), so two P-101s collide while a plant registers its equipment, and worker_profiles is UNIQUE (username). Now says the value is already taken and to change it - an action that CAN work. ★THE SENTENCE STAYS GENERIC ON PURPOSE: the only clue to WHICH field collided is the constraint name, a schema word no reader should see, and the gate asserts that no constraint name, 'duplicate key' or error code reaches the sentence. ★IT EXERCISES THE FUNCTION RATHER THAN GREPPING FOR THE BRANCH - a grep proves a line exists; only calling whWriteError with real error objects proves what a person is TOLD, and the same call guards the two things that must not change (deliberate refusals still speak for themselves; unrelated errors still fall through). ★AND ITS FIRST TEETH TEST WAS A FALSE RED: Node 24 makes `navigator` getter-only, so the shim threw and the prover exited 1 for a reason unrelated to the code under test. Fixed with defineProperty and re-teethed honestly - a RED you did not cause is not proof of anything.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "day-one-parallel-join",
        "script":  "tools/validate_day_one_parallel_join.py",
        "args":    [],
        "label":   "T185 slice 1: five workers, one invite code, the same minute (2026-08-26). The scenario the trajectory is named for - a supervisor reads ONE code out at the morning briefing and the crew types it in at once. It is the first thing that has to work on a pilot's first day, because if two of the five silently fail to join, the pilot is over before anyone logs a repair. ★DISTINCT FROM join-names-the-namesake, which proves the RPC's behaviour for a SINGLE caller (named refusal, idempotent double-tap). This puts N DISTINCT identities on the same hive row and the same unique index CONCURRENTLY - spawned, not awaited in turn, so the contention is real rather than staged - and asserts the three things that can go wrong: nobody is silently lost, nobody lands TWICE, and the one who shares a teammate's name is refused BY NAME rather than by the raw index. Measured: 5 workers, 4 joined exactly once each, 1 HIVE_NAME_TAKEN, 4 rows for 4 identities. ★IT CHECKS THE DATABASE, NOT THE RETURN VALUES - what each session reported is only half the story; the row count and the DISTINCT identity count are what would expose a double-join under a race. ★AND IT CLEANS UP AND RE-COUNTS, because a prober that litters a shared database is a defect of its own; every row it makes carries a WH-T185-PARALLEL marker and residue is asserted at 0. ★ITS FIRST RUN REPORTED THE PLATFORM BROKEN AND THE PLATFORM WAS RIGHT: the fixture gave 'the namesake' its own distinct name, so 5 distinct names correctly produced 5 joins and the collision assertion fired against nothing. An assertion is only as good as the situation it actually sets up. Teeth: against the pre-fix RPC worker 2 hits the raw unique index under the same concurrency.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "hive-at-scale",
        "script":  "tools/validate_hive_at_scale.py",
        "args":    [],
        "label":   "T61 slice 1: the people surfaces at 20 members (2026-08-26). Every seeded hive on this platform is small - 8 members in the largest - so the roster, the standings and the assignee pickers have only ever been read at a size where nothing can go wrong. A roster of 8 fits on a phone; 20 is where ordering, truncation and find-a-person begin to matter, and where a count that disagrees with its list stops being invisible. ★THE FIXTURE IS ON-DEMAND, NOT STANDING, and that is the design decision worth keeping: a permanent 20-member hive would move the numbers every OTHER gate reads - member counts, standings, per-hive rollups - and a fixture that quietly changes someone else's denominator is worse than no fixture at all. It grows a marked hive, measures, removes it, and re-counts to prove the removal. ★IT ASSERTS AGREEMENT, NOT TASTE: the roster read returns EVERY member (a silent cap at 10 or 20 makes a 20-person crew look like a small team and nothing on screen says so - the row-cap class); the count equals the list; the ordering is TOTAL, name plus a tiebreak, so two members cannot swap places between refreshes; and no duplicate names slipped in, which at 20 generated members is a real test of the per-hive unique index rather than a formality. ★THE NAMES ARE FILIPINO AND DELIBERATELY COLLIDE-PRONE (shared given names, mixed lengths) so the ordering assertion means something - 'Worker 7' twenty times would sort perfectly and prove nothing. ★AND IT READS THE GLASS, NOT ONLY THE ROWS: rows agreeing is necessary and not sufficient, because the failure this trajectory is about is a COUNT that says 20 beside a LIST that shows 10 - which no database assertion can see. A second phase tops a real hive up to 20, signs in, expands the roster, and compares the rendered row count to the number printed next to it and to the database, then removes ONLY the marked fixtures so the hive's own members are never touched. Measured: 20 planted / 20 returned / 0 duplicates / order stable / residue 0, and live: 20 rendered, count '· 20', database 20. Teeth proven three ways - a silent cap, a non-total order, and a capped RENDER, which fires both live assertions at once ('rendered 10 of 20' and 'the number and the thing it counts disagree on the same screen').",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "export-reads-its-own-set",
        "script":  "tools/validate_export_reads_its_own_set.py",
        "args":    [],
        "label":   "T129/T49: the file that leaves the building must be complete (2026-08-26). A logbook export is what an auditor reads, and it is the one artifact whose incompleteness the reader CANNOT see: a short LIST shows a Load more button, but a CSV carries no total, no window and no hint of omission. ★THE RECORDED INCIDENT: the team-branch export handed over `_teamEntries` - only what the feed had fetched, one 30-row page plus whatever Load more added - while the page said '517 entries'. A supervisor exporting a filtered team view for a DOLE/ISO trail got a file that looked whole. The mine branch had been fixed in an earlier arc; the team branch was the one nobody had walked. ★THREE ASSERTIONS: every exporter reads its OWN set by PAGING (a loop with .range) rather than reusing the render buffer, since an export sourced from a screen buffer inherits that buffer's cap; the paging carries a TOTAL order (sort key AND tiebreak), because a non-total order over a paged read can repeat one row and drop another - a file of the right LENGTH and the wrong CONTENTS, worse than a short one; and the property is NON-VACUOUS, checked against the database (largest hive 1,700 rows against a 30-row page) so it cannot pass on a table too small to fail. ★IT NAMES THE BUFFER IT FORBIDS rather than pattern-matching 'looks like an export', because the bug was one identifier. ★AND ITS FIRST VERSION MADE TWO FALSE ACCUSATIONS: a fixed 4000-character window ran past the function's end, through the next one and into the click handler, so it reported BOTH exporters as reusing the feed buffer when neither does - one hit was a COMMENT describing the old bug, the other the handler's ANNOUNCED fallback (which is correct behaviour: it exports the loaded window and says so in a toast). Bodies are now delimited by matching braces. A window sized to today's code is wrong the day someone documents something. Teeth proven on both the buffer reuse and a missing tiebreak.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "print-is-readable-on-paper",
        "script":  "tools/validate_print_is_readable_on_paper.py",
        "args":    [],
        "label":   "T124: the printed page must not be the dark theme (2026-08-26). Plants run on paper - a PM schedule goes on the noticeboard, an audit trail into a folder for DOLE - and this platform is DARK. Printed without rules that is either a black sheet that empties a cartridge or, because most browsers drop backgrounds, pale grey text nobody can read. Neither failure appears anywhere ON SCREEN, which is exactly why it needs a gate: print CSS is invisible until it has already wasted someone's trip to the printer. MEASURED: the three pages a plant demonstrably prints already do this properly - white sheet, #111 text, the reset applied to DESCENDANTS (each card and chip carries its own dark background, so a rule on body alone leaves them), the hub/FAB/toasts/dialogs/buttons hidden, and page-break-inside on cards and table rows. This is a RATCHET over work already done, and the held set may not SHRINK - deleting the rules is not a way to pass. ★ITS FIRST VERSION MANUFACTURED FIVE FINDINGS AND ALL FIVE WERE WRONG, which is the part worth keeping: (1) it treated every @media print block as a claim to be printable, but asset-hub's block says in its own comment that it exists so the rule is 'reachable from document.styleSheets' for a DIFFERENT gate, and the report pages already render as light document panels where a dark-to-light reset would be redundant; (2) it read only the FIRST print block, and analytics-report carries SIX - the first being a one-line rule about a field's min-height - so five pages were graded on whichever rule sorted earliest; (3) its skip-list matched substrings against the ABSOLUTE path, and this project lives under '.../Self-learning Road-Map/...', which contains 'learn' - so it excluded EVERY page and reported '0 printable pages examined' while still failing. Three instrument errors before one true reading. ★AND ITS PASS LINE OVERSTATED until corrected: it claimed all 8 pages met the contract when only 3 are held. Teeth proven on a weakened rule AND on deletion.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "xp-belongs-to-one-person",
        "script":  "tools/validate_xp_belongs_to_one_person.py",
        "args":    [],
        "label":   "T17/T51: an XP ledger keyed by NAME merges two people who share one (2026-08-26). achievement_xp_log carries worker_name, achievement_id, xp_earned, source_action, source_id, earned_at, reversed_at - and NO auth_uid, NO hive_id - while its RLS reads `worker_name IN (SELECT auth_worker_names())`, so a row belongs to whoever answers to that name. ★THAT IS A HAZARD RATHER THAN A CHOICE because worker_name is unique PER HIVE, not globally: two different people in two different hives may both be Juan Dela Cruz, which in a Philippine plant is ordinary, and this table cannot tell them apart. Their totals merge, their LEVELS are computed from each other's work, and because the RLS predicate IS the name, each can READ the other's ledger - source_action and source_id describing work done in a hive they have never belonged to. ★MEASURED: it is NOT happening - 982 rows across 20 names, zero names held by two auth_uids - and that is precisely why the gate exists NOW. While every name still resolves to one person a backfill onto auth_uid is unambiguous; once two namesakes accumulate history the ledgers cannot be untangled, because nothing recorded which of them earned what. ★IT DETECTS THE TRIGGER AND DOES NOT PRETEND TO FIX IT: the remedy is an auth_uid column, backfilled while it is still possible, with the four read sites (achievements x2, logbook, pm-scheduler) and the RLS predicate moved onto it - a schema change to a ledger carrying reversal semantics, which is Ian's call rather than something to smuggle in. This fails the moment the condition appears, so the window to fix it cleanly cannot close unnoticed, and it PASSES ITSELF OUT once auth_uid exists rather than policing a dead condition. Teeth proven by creating a cross-hive namesake, which it named exactly.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "rejection-reaches-the-submitter",
        "script":  "tools/validate_rejection_reaches_the_submitter.py",
        "args":    [],
        "label":   "T20: a refusal must reach the person who was refused (2026-08-26). A supervisor clearing the approval queue rejects an asset, a part, a change order; the item leaves the queue and their job is done - but the WORKER who submitted it is the one who has to act, and if all they ever see is 'Rejected' they resubmit the same thing, give up, or go and ask in person. A refusal that names no cause is a loop the platform opened and did not close. ★BOTH HALVES ARE ASSERTED because either alone is useless: every path writing status 'rejected' records a REASON in the SAME update (a reason captured later, or optionally, is a reason usually missing), and every surface that knows about the rejected state RENDERS it back (a column written and never read is the write-only class, and the submitter is the only person it was written for). ★IT PAIRS WRITES TO RENDERS BY TABLE, NOT BY PAGE, because the halves deliberately live apart: the supervisor rejects from hive.html and the worker reads the reason on inventory.html - a check demanding both in one file would fail the working design. MEASURED: all three paths already comply (hive's queue for asset_nodes AND inventory_items, asset-hub's own reject, project-manager's change orders), rendered as 'Why: ...', 'Why rejected: ... - fix it and resubmit via Edit', and 'Rejected: ...' - and asset-hub even says 'No reason recorded.' for rows predating the column rather than showing an empty box. Nothing was broken; nothing was guarding it, and a fourth reject path added later would have no reason to know the contract exists. ★ITS OWN FIRST RUN WRONGLY ACCUSED asset-hub, by matching a capitalised 'Rejected' caption when that page branches on `status === 'rejected'` inside a template - the state is a VALUE there, not a caption. Teeth proven on both halves.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "asset-pm-handoff-carries-context",
        "script":  "tools/validate_asset_pm_handoff_carries_context.py",
        "args":    [],
        "label":   "T21: scheduling a PM must not lose the asset (2026-08-26). A supervisor approves a new asset and the next thing they do is give it a maintenance program. If the link carries the asset that is one tap; if it does not, they arrive at a list of everything and have to re-find the asset they were just looking at - which on a phone, in a plant, is where the task gets abandoned. ★BOTH ENDS ARE ASSERTED, because a parameter nobody reads is a promise only the sender believes it is keeping - the declared-but-never-wired class: every pm-scheduler link rendered with an ASSET in hand carries its identity (?tag / ?asset / ?asset_id), AND pm-scheduler actually consumes them (it reads all four: asset_id, asset, pm_id, tag). ★EMPTY-STATE LINKS ARE EXEMPT AND IT IS NOT A LOOPHOLE: asset-hub's two bare links both sit under 'No approved assets in this hive yet', where there is no asset to name and demanding a parameter would be demanding a lie - and the exemption is decided by the surrounding TEXT, not by an allowlist a later edit could quietly join. MEASURED: 2 links carry the asset, 2 are bare in genuine empty states. ★ITS FIRST RUN ACCUSED A COMMENT - 'Canonical PM frequencies (match pm-scheduler.html's FREQ registry)' - because it matched the bare filename anywhere; a mention is not a navigation, so it now matches only href= and .href= assignments. Teeth proven by dropping the asset from a real link.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "flagged-post-tells-its-author",
        "script":  "tools/validate_flagged_post_tells_its_author.py",
        "args":    [],
        "label":   "T24: a reported post must not vanish on the person who wrote it (2026-08-26). When a teammate reports a post the platform hides it from the hive until a supervisor rules, and the easy way to build that is ONE predicate - hide every flagged post from every worker - which is wrong for exactly one person: the AUTHOR, who wrote something, watched it disappear, and was told nothing. They re-post it or conclude the app is broken, and either way the moderation nobody has done yet has already cost their trust. ★THREE PARTIES, THREE OBLIGATIONS: the author keeps seeing their OWN flagged post (the feed query must EXEMPT their rows, not merely filter flagged=false); the author is TOLD in words what happened, who decides, and that teammates cannot see it meanwhile (a post that reappears unexplained is barely better than one that vanished); and the SUPERVISOR sees the REPORTER'S REASON on the card, because judging a flag without knowing why it was raised means opening the audit log first, and a queue that costs a tab is a queue that waits. ★IT CHECKS THE QUERY PREDICATE, NOT JUST THE BADGE, and that is the half a page-level check would miss entirely: if the row is filtered out server-side the badge is unreachable, so the render can be perfectly correct while the author still never sees their post. MEASURED: all three hold today - the author exemption, the UNDER REVIEW state carrying its explanation, and the reporter's reason inline for the supervisor. Teeth proven on the predicate and on the notice.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "absent-money-is-not-zero",
        "script":  "tools/validate_absent_money_is_not_zero.py",
        "args":    [],
        "label":   "T94: a peso amount that did not arrive must not render as zero (2026-08-26). '₱0.00' is a confident statement that something moved nothing, and when the number is actually ABSENT - a read that failed, a view column reported nullable, a join that missed - printing zero trades a VISIBLE defect (₱NaN) for an INVISIBLE one. Recorded incidents: a filed top-up of ₱300 rendered as ₱0.00, and a ledger line of unknown value as '+₱0', which says the entry moved nothing - the most misleading thing a ledger can say. ★THE GUARANTEE IS CENTRAL, so the gate checks the centre first: whFmtPeso returns a GAP for null/undefined/'' and for anything non-finite, while a REAL zero still prints ₱0 - a person can act on 'we do not know' and cannot act on a wrong number. ★AND IT CHECKS THE BYPASS, because a helper is only as good as its use: `'₱' + Number(x || 0)` is the exact resurrection, since Number(null) is 0 AND finite. That is permitted ONLY as the else-branch of a `typeof whFmtPeso === 'function'` fallback, for a page whose utils.js did not load. ★IT MATTERS THOUGH THE BASE TABLES ARE NOT NULL: Postgres does not propagate NOT NULL through a VIEW, so every v_*_truth column reports nullable and these reads go through views - the door is already open and the helper is what stands in it. ★FOUR DELIMITERS BEFORE ONE THAT WORKED, all kept in the source: a 260-char lookback found an UNRELATED helper's typeof check two lines up and exempted a real bypass (this clause's teeth test passed GREEN); the nearest brace cut the expression at the `{` inside `{ decimals: 2 }` and accused three legitimate fallbacks; a statement walk starting one line UP walked past the bypass's own `const` into the helper above and inherited its check - green again. Only a walk beginning at the hit's OWN line is right. Teeth proven on both clauses.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "watchlist-says-what-it-does",
        "script":  "tools/validate_watchlist_says_what_it_does.py",
        "args":    [],
        "label":   "T95: a bookmark must not imply a subscription (2026-08-26). Tapping a star on a listing looks like 'tell me when this changes'. Here it is a BOOKMARK - nothing consumes marketplace_watchlist to send price or stock alerts - and a buyer who believes otherwise STOPS CHECKING the listing and misses the very change they were watching for. The feature would do harm precisely by seeming to work. ★THE PLATFORM ALREADY HANDLES THIS WELL, which is why this is a PARITY check and not a defect report: marketplace.html says outright \"WorkHive doesn't send watchlist alerts yet.\" Stating an absence plainly is the honest answer to a capability you have not built. ★AND IT IS ASSERTED IN BOTH DIRECTIONS, which most claim-checks are not: while nothing consumes the watchlist the disclosure must be PRESENT (deleting the sentence silently restores the implied promise), and once something DOES consume it the disclosure must GO - a stale 'doesn't send alerts yet' under a feature that now sends them is the same defect pointing the other way, and it teaches people to ignore the one place the product tells them the truth. A claim and a capability drift apart in either direction; a gate watching only one of them is half a gate. ★A CONSUMER MEANS AN EDGE FUNCTION OR CRON, not a page reading its own list to draw stars - the distinction is what stops the gate flipping on the rendering code. Teeth proven BOTH ways: deleting the sentence, and adding a function that touches the table.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "search-term-survives-punctuation",
        "script":  "tools/validate_search_term_survives_punctuation.py",
        "args":    [],
        "label":   "T99: a comma in a search box must not 400 the read (2026-08-26). MEASURED against the live REST endpoint: or=(machine.ilike.*bearing, spherical*,...) returns HTTP 400, 'failed to parse logic tree'. PostgREST's or=(a,b) grammar splits on top-level commas, so an unquoted comma inside a value ends the condition early and the ENTIRE filter is rejected; parentheses do the same. ★THE TERM THAT BREAKS IT IS THE ORDINARY ONE: 'Bearing, spherical roller, 22320 E1 XL C3, SKF' is how a real part is named - it is this platform's OWN example in the longest-truncates gate - and typing it into the logbook feed, the logbook EXPORT, community, or the global search overlay produced a 400 the page renders as a FAILED READ. A legitimate search looked like an outage, which is worse than no results: the worker concludes the system is down and stops looking. ★TWO DIFFERENT JOBS, AND ONLY ONE WAS BEING DONE. Escaping % and _ stops a wildcard changing WHICH rows match; removing the delimiters stops the filter failing to parse at all. FOUR sites did the first and not the second. marketplace.html was the exception - it strips them - which is why the same phrase worked there and nowhere else, and why nobody had noticed. ★FIXED CENTRALLY with whSafeSearchTerm (delimiters removed, wildcards escaped, capped), applied at all four sites. Delimiters are REMOVED rather than escaped because PostgREST offers no escape for them inside an unquoted value, and a space keeps word boundaries so the phrase still matches. VERIFIED END TO END: the same read that returned 400 now returns 200 with an empty array - an honest no-match instead of an error. ★AND THE SAME BUG WAS IN THE EDGE FUNCTIONS, where it is WORSE because it is SILENT. agentic-rag-loop's logbook lane sanitises a natural-language QUESTION - where commas are not an edge case but the common case - and read the result as `const { data: logs } = await q`, DESTRUCTURING THE ERROR AWAY. supabase-js RETURNS {data:null,error} rather than throwing, so the rejected filter never reached the surrounding catch and `logs || []` turned a 400 into an ordinary empty result: the AI answered with less grounding and nothing anywhere said why. Its comment reads 'Escape LIKE wildcards in user input (per data-engineer skill)' - the author followed the skill exactly, and the skill covered wildcards but not delimiters. visual-defect-capture passed an asset TAG into an .or() with no escaping at all. Both fixed, and the error is now logged instead of discarded. The gate covers the edge functions too, including the `const { data } = await q` shape that makes the failure invisible. Teeth proven on four clauses: the helper weakened, a page interpolating a raw term, an edge fn dropping the delimiter strip, and an edge fn discarding the error again.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "journal-says-what-survives",
        "script":  "tools/validate_journal_says_what_survives.py",
        "args":    [],
        "label":   "T77/T164: the most private surface must say what you can take back (2026-08-27). A worker speaks or types something personal into the voice journal, and two facts govern whether they should - neither guessable. THE NOTE IS PERMANENT: ai-gateway persists the row inside the same call that generates the reply, so it exists before the worker has seen anything, and renderHistory's only per-entry action is 'Speak again' - no edit, no per-note delete. AND THERE IS SOMEWHERE TO GO: the privacy policy's Data Rights Request route, which validate_erasure_path_intact already proves the database honours. ★THE IN-APP ERASURE IS VOICE-ONLY AND IS DELIBERATELY NOT ADVERTISED AS THE REMEDY - the gate FAILS if the page starts offering it. voice-handler's _isErasureRequest does detect 'delete my voice history', but it is reached from _onStopRecording alone, so only after a voice recording; this page's typed fallback exists, in its own words, for 'a loud plant floor, a denied mic, or a worker who cannot speak', and pointing those people at a spoken phrase offers a remedy that excludes precisely whoever most needs it. ★AND IT ASSERTS THE DISCLOSURE APPEARS ONCE, because the first attempt at this fix was DUPLICATION: the permanence was ALREADY stated on the primary surface, and a new paragraph restating it landed inside a collapsed <details> nobody opens - a second home for information that already had one. Reverted; the fix became a single clause completing the sentence that was already there. Teeth proven on the missing route and on the duplicate.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "draft-survives-reauth",
        "script":  "tools/validate_draft_survives_reauth.py",
        "args":    [],
        "label":   "T38: typed work must outlive a session expiry (2026-08-27). A worker half-fills a logbook entry, the token dies, they sign in again - and whether their typing is still there depends on a three-link chain no single file makes visible, with every link in a different place: whAutoSaveDraft persists the text; session-timeout's clearIdentityHard wipes the IDENTITY keys and must not touch draft keys; and the draft is restored only to the worker who typed it, by an OWNER CHECK that reads wh_last_worker - the very key the expiry just deleted. ★SO THE DRAFT SURVIVES ONLY BECAUSE RE-AUTH PUTS THAT KEY BACK. Sign-in rewrites wh_last_worker, so by the time the worker returns the owner check matches again and the text reappears. Re-key that check to something sign-in does NOT restore and every draft on the platform is silently discarded after an expiry: the form comes back empty and nothing explains it. That is the assertion - the key the owner check reads must be one the sign-in path writes. ★THE OWNER CHECK IS NOT THE BUG, it is the shared-tablet protection: worker A's draft must not appear in worker B's form. Removing it would 'fix' the expiry case by leaking someone's typing to the next person on the device, which is worse. ★AND THE ASSERTION HAD TO BE 'AT LEAST ONE LINK', NOT 'EVERY LINK': the check is a fallback chain (wh_last_worker || wh_worker_name) and utils.js records that the alias reads are dead - 'never written anywhere in the codebase (get-without-set)'. Demanding every link be restored flagged that dead alias and called a working chain broken. Teeth proven on both clauses: sign-in ceasing to restore the key, and an expiry that starts wiping a draft key.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "marketplace-warns-before-payment",
        "script":  "tools/validate_marketplace_warns_before_payment.py",
        "args":    [],
        "label":   "T101: 100% of the money risk sits at one step (2026-08-27). This is a CONTACT-ONLY marketplace - no escrow, no held funds, no reversal - so a buyer contacts a stranger and pays them off-platform, and every peso of risk lives at that single moment. RA 11967, the PH Internet Transactions Act, requires consumer education on the red flags in internet transactions, which makes this a legal floor as well as a decent one. ★STATING A FACT IS NOT WARNING: 'payment is off-platform' is true and tells a buyer nothing about what to DO. The page carries an actual warning at the decision point - inspect the item, meet at the seller's business address or a public place, avoid paying in full up front to a new seller - and the sentence that sets expectations correctly: WorkHive never holds your payment, so we cannot reverse it. ★THE OPPOSITE FAILURE IS WORSE THAN SILENCE, so the gate also refuses escrow / buyer-protection / money-back / guaranteed-refund language on the marketplace surfaces: a buyer who believes there is protection behaves as though a loss is recoverable. It strips COMMENTS before matching, so prose ABOUT a promise is not read as the promise. ★AND IT HOLDS TWO SUBTLE CLAIMS THE PAGE ALREADY GOT RIGHT: 'credits back' must carry that credits are spend-only and cannot be withdrawn, because the bare words read like money returning; and the trust bar must say 'ID-Verified badge per seller' rather than 'ID-Verified Sellers', which as a bare heading reads as a property of the RESULTS - the same false claim the source chip carried when 3 of 9 listings actually had a verified seller. Teeth proven on the removed reversal sentence and on an introduced protection promise.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "status-survives-the-outage",
        "script":  "tools/validate_status_survives_the_outage.py",
        "args":    [],
        "label":   "T71: the page that reports the outage must load during it (2026-08-27). whReadError sends a person to status.html when a read fails on good internet, because 'is it me or them?' is the one question a failure state can actually answer - and that pointer is only honest if the status page works when the platform does not. ★TWO WAYS A STATUS PAGE LIES, and both are asserted: (1) IT DEPENDS ON WHAT IT REPORTS - a page that needs the database cannot report a database outage; it fails exactly when someone finally needs it, and the reader concludes the whole product is gone. So status.html must carry no Supabase client, no getDb, no table read. (2) IT IS HARDCODED GREEN - 'All systems operational' baked into the markup is a trust signal with no living producer: it stays green THROUGH the outage and is worse than no page at all, because it actively contradicts what the person is experiencing. ★WHAT IT DOES INSTEAD IS THE RIGHT SHAPE, and the measurement confirms it: no database reference anywhere, and it pings each edge function's public /health endpoint, rendering one card per service, ambering when a round-trip exceeds the interactive SLO, with a Refresh to re-check. Live per service, independent of the database - which is precisely why it can still answer when the database is the thing that is down. ★IT ALSO HOLDS THE POINTER ITSELF: if utils.js stops sending a failed read to status.html, every failure state goes back to answering 'is it me or them?' with silence. Teeth proven on both lies - a Supabase client introduced, and a hardcoded green.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "audit-is-reachable-from-a-record",
        "script":  "tools/validate_audit_is_reachable_from_a_record.py",
        "args":    [],
        "label":   "T28: an audit nobody can reach is write-only (2026-08-27). 'Who changed this?' is asked while looking at the RECORD - an asset whose details are wrong, a reported post, a decision someone disputes. If the only way in is to know the page exists and open it cold, the trail is technically complete and practically useless: the supervisor gives up or goes to SQL, and the compliance story the platform tells about itself is one nobody can walk. ★A LINK THAT DROPS THE RECORD IS BARELY BETTER - arriving at an unfiltered log of every power action while holding the id in your head is the same scavenger hunt with extra steps. So BOTH ends are asserted: the links must carry the subject (asset-hub sends ?q=<tag>, community sends ?action=report_post) AND audit-log must CONSUME those parameters, or the context is written into the URL and thrown away - a promise only the sender believes, which is the declared-but-never-wired class. MEASURED: two context-carrying links, audit-log consumes all four of q/actor/action/target. ★THE HIVE MENU ENTRY DOES NOT SATISFY THIS: audit-log is a hidden page, so a menu item makes it reachable only to someone who already knows to look; the record-level affordances are what make it findable at the moment the question is actually asked, which is why bare links are reported but do not count toward the requirement. Teeth proven on both halves - links stripped of their record, and the audit page ceasing to read what it is sent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "removal-tells-the-worker",
        "script":  "tools/validate_removal_tells_the_worker.py",
        "args":    [],
        "label":   "T25: being removed from a hive must not arrive as an empty screen (2026-08-27). A supervisor kicks someone; the supervisor's side is a confirm and a roster that shrinks. The WORKER's side is the one that matters - they open the app and their hive is simply gone, and without a word the honest readings available to them are 'the app broke' and 'my work was deleted', one of which is frightening enough to end their use of the product. ★THREE THINGS A REMOVED PERSON NEEDS, all asserted: WHAT HAPPENED (removed from the named hive, BY A SUPERVISOR - without an actor it reads as a malfunction), WHAT SURVIVED ('your personal records are still yours', which is the fear underneath, and silence about it is read as loss), and WHAT NOW (join another hive or create one - otherwise the notice tells someone their place is gone and leaves them there). ★AND IT MUST BE HELD LONG ENOUGH TO READ, which is the part usually missed and is asserted as part of the disclosure rather than as styling: the page's toast hides at a fixed 3s and this notice overrides it to 12s. Three seconds is fine for 'saved'; it is not enough for two sentences about your identity and your work, and a message nobody finishes reading was not delivered - the zero-millisecond-refusal lesson wearing a longer coat. Teeth proven on the hold (dropped to 2.5s) and on the records reassurance (removed).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "md-twins-current",
        "script":  "tools/validate_md_twins.py",
        "args":    [],
        "label":   "T3/T154: the markdown twin must say what the page says (2026-08-27). Every public page ships a clean twin at <page>.md - the llms.txt convention, served deliberately (_headers: 'Markdown twins ... every page also exists at <page>.md') so answer engines read structured text instead of scraping markup. That makes the twin a PUBLISHING CHANNEL, and a stale one is worse than none: an agent fetching it gets a confident, well-formed, WRONG version of the page. ★MEASURED 2026-08-27: 107 of 119 twins had DRIFTED, including privacy-policy and terms-of-service - an answer engine could quote retired legal text. The sharpest instance was T3's recorded defect surviving in the machine channel after being fixed in the human one: 60 calculator twins still said 'free, no sign-up needed' on a link INTO engineering-design.html, which redirects to sign-in, while the HTML had already been corrected to 'A free account takes about 30 seconds'. Zero served pages carried the claim; sixty twins did. ★THE VALIDATOR ALREADY EXISTED AND WORKED - tools/validate_md_twins.py, with a source_sha per twin and the right reasoning in its own docstring - and NOTHING RAN IT: it appeared in no gate registration, the same orphaned-prover shape as the three unreferenced provers found earlier this session. A working oracle nobody runs is indistinguishable from one never written. Fixed by rebuilding (118 rebuilt, 119/119 current, 0 twins still carrying the claim) and by REGISTERING the validator, so the next HTML edit that forgets the twin fails here instead of shipping a confident wrong answer to a machine. FIX ON FAIL: python tools/build_md_twins.py",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "money-trust-confirmed",
        "script":  "tools/validate_money_and_trust_confirmed.py",
        "args":    [],
        "label":   "T67: money and trust writes confirm — T67's owner-cockpit oracle (2026-08-26). Most admin verbs are reversible; two are not. Verifying a service-credit top-up MINTS credits into a provider's wallet, and once spent this console cannot pull them back — the mirror action refuses a top-up somebody actually PAID for. Setting kyb_verified / cert_verified is the platform VOUCHING for a stranger to a buyer who is about to send them money; it is the same claim trust-claim-backed keeps honest at the render end, and this keeps it honest at the GRANT end. ★THE FAILURE MODE IS NOT A CHANGE OF MIND, IT IS THE WRONG ROW: both live in per-row button lists in a queue worked at speed, so a misclick does not undo the last action — it applies a real one to the wrong person. FOUND: founder-console carried six writes and ZERO whConfirm; svcTopupDecide minted credits on one click while toasting 'Credits minted to the provider's wallet', and the verify-kyb/verify-cert branch granted the badge asking nothing. platform-actions held the same three (an optimistic status lock stopped a DOUBLE press there, which cannot stop a wrong-ROW press). All six now name the consequence, and the trust ones name WHO. Verified live: the dialog carries the real text and cancelling sends ZERO writes. Teeth: 6 of 6 unconfirmed against pre-fix HEAD, 0 of 6 after. ★NARROW ON PURPOSE — it does not demand a confirm on every admin verb; approving a listing is reversible, and a dialog there is friction that trains people to click through. ★AND THE GATE'S OWN UNIT WAS WRONG FIRST: it took the NEAREST enclosing braces and called platform-actions unconfirmed with the confirm four lines above, just outside the `try` — guarding before the try is the CORRECT place, since a cancel there leaves the button untouched. It now accepts a confirm that precedes the write in ANY enclosing scope, up to the handler.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "cascade-confirm-parity",
        "script":  "tools/validate_cascade_confirm_parity.py",
        "args":    [],
        "label":   "T139: cascade-confirm parity — T139's blast-radius oracle (2026-08-26). A delete confirm is a CLAIM about consequences, and the FK graph is the truth. When a table has ON DELETE CASCADE children, deleting one row silently destroys rows on other surfaces — an asset's Weibull fits, a project's change orders, a PM plan's completion history. If the confirm does not say so, the person approves a consequence they were never shown. WHAT IT DOES 1. Reads the LIVE FK graph (psql): every table with ON DELETE CASCADE children. 2. Finds every client delete site: `.from('<table>')` … `.delete()` in the HTML. 3. Looks back for the nearest whConfirm/confirm text guarding that site. 4. FAILs a site whose table cascades and whose confirm names NO consequence — no child-count, no 'removes/deletes/cannot be undone' clause, or no confirm at all. It checks that a consequence is STATED, not that the wording is perfect: prose grading produces false reds, while 'cascades silently, says nothing' is objective and is the class that hurts. Forward-only against tools/cascade_confirm_baseline.json. SKIPs when psql is unreachable — the FK graph is the oracle, and a gate without its oracle must say SKIP, never PASS. ★INSTRUMENT FIX 2026-08-26 (T50): this gate was VACUOUS. Its consequence pattern included `removes?` and `deletes?`, so 'Remove this asset from the registry?' - four words naming only the ACTION - matched on 'Remove' and passed, and nearly every destructive confirm is phrased that way, so it reported 0 while asking nothing. A consequence is what happens BESIDES the thing you just asked for; the verb is the ask. Demonstrated by resurrection: against the pre-fix wording the old pattern found 0 and the tightened one finds the logbook asset delete, whose row CASCADES seven children (asset_edges, asset_embeddings, pf_intervals, rcm_fmea_modes, sensor_readings, sensor_topic_map, weibull_fits) and detaches five more - reachable by a supervisor on an APPROVED asset, so a plant's reliability history could go behind a four-word question. Confirm rewritten to name both halves (what dies, what survives); tightening revealed no other site, so it cost no backlog.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "error-taxonomy-ratchet",
        "script":  "tools/validate_error_taxonomy_ratchet.py",
        "args":    [],
        "label":   "T176: error-taxonomy ratchet — T176's 'new errors are born compliant' gate (2026-08-26). The platform has three taxonomy helpers — whReadError / whWriteError / whAiError — that turn a raw failure into a sentence naming the CAUSE, the state of the user's WORK, and the NEXT STEP. Adoption was driven page by page across several arcs. The risk now is regression by accretion: a new `showToast('Something went wrong')` costs nothing to write and quietly re-opens the class. WHAT THIS COUNTS: user-facing failure strings handed to showToast / setStatus / textContent / innerHTML inside (or right after) a catch or an `if (error)` branch, where NO taxonomy helper appears on the same line. That is the RAW-APPEND class. Forward-only ratchet against tools/error_taxonomy_baseline.json — the count may fall (and the baseline auto-lowers), never rise. It deliberately does NOT try to judge sentence quality: a lint that guesses at prose produces false reds, while a count of raw appends is objective and is exactly the regression signal. ★ZERO IS NOT THE GOAL. Audited 2026-08-26 by reading the remainder one by one: much of what is left is well-written raw sentences that already name cause, work state and next step ('Wrong username or password.', 'A logbook entry can only be corrected by its author', 'Someone else changed this feedback. Your note was not saved') — they simply do not route through a helper. Rewriting those to lower the number would make the copy WORSE to move a metric. Convert what ECHOES A DRIVER MESSAGE or says nothing useful; leave sentences that already do the job.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "feedback-round-trip",
        "script":  "tools/validate_feedback_round_trip.py",
        "args":    [],
        "label":   "T157: feedback-round-trip - T157: the feedback loop actually goes somewhere (2026-08-26). 'Email us with corrections' and a feedback button are TRUST CLAIMS. A widget that thanks somebody and stores nothing is the worst version of this: the person believes they were heard, nobody was told, and the silence reads as being ignored rather than as a bug. Measured from the public /about/ page ANONYMOUSLY - the hardest case, because an anon insert must pass RLS with no session behind it. It does: platform_feedback carries a `feedback anon submit` policy admitting inserts only as status 'new', not public, with no admin_note - so a stranger can report a problem and cannot forge a resolved one. Six assertions, and the REFUSAL half is the point. A loop that works when every field is filled is easy; the failure that costs trust is a submit that quietly does nothing. So: a complete submission lands as status 'new'; an incomplete one (no kind chosen) is refused BEFORE any network call, names what is missing in a VISIBLE element, and does not clear what was typed. *** TWO FALSE READINGS ON THE WAY, NEITHER BANKED. *** The first run clicked send without choosing a kind, got no row, and could have been written up as 'public feedback never reaches the table' - it reached nothing because the widget correctly refused and said so in an element the probe was not reading. The second reused the panel from that refusal, and WHFeedback.open(prefill) deliberately fills only EMPTY fields (it must never clobber typed text), so the row landed under the OLD subject and the probe again read zero. The product was right both times; the probe was reading the wrong element, then reusing dirty state.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "truth-view-read-isolation",
        "script":  "tools/validate_truth_view_read_isolation.py",
        "args":    [],
        "label":   "Cross-hive READ isolation across ALL truth views (LIVE, rolled-back - batch generalization of the security_invoker read-leak class, mig 001). As a real authenticated member of hive A, reads EVERY hive-scoped v_*_truth view filtered to hive B and asserts 0 rows - so a future view shipped without security_invoker (or a base-table SELECT-RLS regression) leaks a foreign tenant's data and FAILs here. 31 hive-private truth views covered (covering every page's reads); the 4 cross-hive-PUBLIC-by-design views are excluded (marketplace listings/sellers, public community posts, public reputation). Actors + a data-rich foreign hive picked dynamically = reseed-robust. Skips if DB / a two-hive fixture is absent.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "no-client-counter-write",
        "script":  "tools/validate_no_client_counter_write.py",
        "args":    [],
        "label":   "P6 lost-update regression gate (STATIC): asserts NO page HTML writes a value-integrity counter (qty_on_hand/xp_total/total_sales/rating_avg/votes/points/balance/...) via client .update()/.upsert() with a client-computed absolute value - every such mutation must go through an atomic server RPC (inventory_deduct/inventory_restock FOR-UPDATE, increment_*, grade_*) that serialises the read-modify-write under a row lock. Locks the class fixed by mig 20260713000008; the 2026-07-14 live sweep found 0 client counter-writes and this keeps it at 0. Static (no DB) so it runs in --fast and never flakes.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "substrate-freshness",
        "script":  "tools/validate_substrate_freshness.py",
        "args":    [],
        "label":   "Platform Knowledge Substrate freshness (PKS Layer-2 anti-regression - PLATFORM_KNOWLEDGE_SUBSTRATE_ROADMAP.md). The substrate/ chunk index (tools/build_substrate.py: metadata-prefixed .md chunks over pages / edge-fns / table-RLS / DEFINER-RPCs / skills / docs) lets a task RETRIEVE the relevant slice (a ~1KB page map) instead of a Workflow fan-out re-reading the whole platform (logbook.html 301KB, etc.) every run - the token-sustainability fix (Ian 2026-07-13: fan-out burned 0.5-3M tokens/run). This gate anchors every chunk to its source by a source_sha (file-content hash for page/edge-fn/skill/doc; live DB introspection for table-rls/rpc) and FAILs if any chunk DRIFTED (source changed but the chunk was not rebuilt) - treats the substrate as code, the doc-drift CI model. FIX on FAIL: python tools/build_substrate.py. DB-dependent chunks skip cleanly if docker absent; file-based always checked; fast (file hashing + one introspection pass).",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "memory-cache-coverage",
        "script":  "tools/memory_cache.py",
        "args":    ["--check"],
        "label":   "PKS P3 memory retrieval cache - coverage + budget (PLATFORM_KNOWLEDGE_SUBSTRATE_ROADMAP L3). The SQLite FTS5+TF-IDF cache (Memento memory.db - project_memento_local_memory_cache P0-P10, already built) delivers the prompt-matched memory slice (<2.5K tokens) per prompt; so MEMORY.md is kept a slim DOCTRINE-CORE + recent refs, with older reference/project pointers RETRIEVAL-ONLY (surfaced on demand, not loaded whole). This gate asserts (a) every durable reference/project memory file on disk is INDEXED in memory.db (FTS5-retrievable - so a pointer-light MEMORY.md loses nothing), and (b) MEMORY.md is under the 24.4KB native hard load cap (FAIL over cap = entries silently truncating). FIX: re-run the Memento indexer (coverage gap) or `python tools/memory_cache.py --slim --apply` (over budget). Ends the whole-MEMORY.md-load budget pressure that climbed to the cap this session (Ian 2026-07-13).",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "night-crawler-freshness",
        "script":  "tools/validate_night_crawler_freshness.py",
        "args":    [],
        "label":   "Night Crawler external-substrate freshness (NIGHT_CRAWLER — the on-demand web crawler tools/night_crawler.py that distills external sources into substrate/external/*.md so the agent RETRIEVES the ~2KB distilled chunk instead of re-crawling + re-understanding raw HTML every session = the token-waste fix, the automation of the PKS L4 reference-distillation tier). Unlike the INTERNAL substrate whose source_sha re-derives from local files, an external chunk's source lives on the public web and drifts SILENTLY; this gate reports every external chunk whose age exceeds its own ttl_days (default 30) so staleness is surfaced, then hands the fix: python tools/night_crawler.py --refresh-stale. NON-BLOCKING BY DESIGN — external staleness is expected and must never break a commit, so the gate ALWAYS exits 0 (reports, never fails). SKIPs cleanly if substrate/external/ is unseeded.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "night-crawler-selftest",
        "script":  "tools/night_crawler.py",
        "args":    ["--selftest"],
        "label":   "Night Crawler distill quality guard self-test (tools/night_crawler.py --selftest — deterministic, no network/AI, instant). The crawler's distiller now EVALUATES its output before bagging it (distill_quality, an Evaluator-Optimizer step): a thin / link-inflated / all-provenance / boilerplate distill is REFUSED not written, and a soft-404 / bot-wall error page is skipped BEFORE distilling (is_error_page) — so a nav/link-index or dead-URL page can't pollute substrate/external/. Born 2026-07-17 from the 12-Factor README distilling to license mush + 2 NN/g chunks the old harvest distilled from 404 pages. This gate runs the guard's fixtures (link_density + distill_quality + is_error_page: a good chunk passes; mush / 404 / all-provenance / NO_DURABLE_CONTENT fail) so a regression in the quality heuristics breaks the build. FIX on FAIL: the guard logic in tools/night_crawler.py changed — restore the fixtures' invariants.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "rubric-parity",
        "script":  "tools/validate_rubric_parity.py",
        "args":    [],
        "label":   "UFAI rubric SSOT parity (UR-P0 lock, 2026-07-21) — the prose ruler (substrate/reference/ufai-ux-rubric.md) and the code lens (survey_ufai_rubric.js, which tags each dim via M('A1')/J('D2')/NA('C2')) must AGREE: every dim the doc declares is encoded, and vice-versa, EXCEPT the cross-page S2/S3 owned by family_rubric_sweep; AND no header may claim a stale count. Born from the measured drift where ONE ruler had THREE counts (doc header 49, code header 44, body 63). Add a class to the prose without encoding it (or leave a header count stale) and this FAILs. The teeth of the UFAI_RUBRIC_CENTRALIZATION_ROADMAP §0 lock. FIX on FAIL: encode/document the missing dim, or reconcile the header count (a stale header IS the drift). Self-test: --self-test (deterministic, no files).",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "test-hive-fixtures",
        "script":  "tools/validate_test_hive_fixtures.py",
        "args":    [],
        "label":   "Stale test-hive fixture detector (2026-07-21) — a reseed re-mints hive UUIDs, so ANY pinned hive UUID in a harness/gate rots: the signed-in user isn't a member → RLS 0-rows / 403 not_a_member → the surface is SKIPPED or scanned EMPTY → the gate VACUOUSLY PASSES (silently disables itself). Proven: 3 generations of rot (9b4eaeac → 636cf7e8 → deleted; Lucena c9def338 → 4eec150e); asset-hub's Arc-W 'focal regression' was this class. Scans tools/** + root for hive-UUID pins on code lines, live-checks each against the hives table (SKIPs cleanly if DB down), exempts documented fallbacks + synthetic probe ids (>=6-char runs, e.g. isolation spoof PKs), and forward-ratchets the STALE count (baseline test_hive_fixture_baseline.json — may only FALL; started 54). THE FIX per hit: resolve at runtime — Python tools/lib/test_identity.py; JS live_page_journeys signIn()/h.hive. Never pin a newer UUID (that was 'the fix' twice; it rotted twice). Self-test: --self-test.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "accessor-load-order",
        "script":  "tools/validate_accessor_load_order.py",
        "args":    [],
        "label":   "Accessor-before-utils.js load-order (2026-07-22) — a utils.js-defined accessor (whWorker/whHiveId/…) called UNGUARDED in an inline script ABOVE the <script src=utils.js> tag throws ReferenceError at document-order execution. Born from the storage-SSOT sweep shipping dayplanner.html with `whWorker()` at L459 while utils loaded at L1710 → init IIFE threw → the page stuck on 'Loading day plan…' for EVERY user (invisible to console-only probes — it fires pageerror). Requires early calls to be `(typeof whWorker==='function'?…:fallback)`-guarded. Deterministic, offline, forward-only ratchet (baseline 0). Self-test: --self-test. FIX on FAIL: typeof-guard the early call + re-read the accessor at init after utils loads.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "client-write-grants",
        "script":  "tools/validate_client_write_grants.py",
        "args":    [],
        "label":   "Client-write GRANT parity (2026-07-22) — the LOCK for the 42501 'permission denied for table' class. For every base table a client page WRITES via .from('T').insert|upsert|update|delete, asserts `authenticated` (or PUBLIC) holds the matching privilege: insert→INSERT, update→UPDATE, delete→DELETE, upsert→INSERT+UPDATE+SELECT (ON CONFLICT read + PostgREST RETURNING). Born from Arc-K MS3: marketplace_sellers granted I/U/D but NOT SELECT → every seller's messenger/cert upsert 42501'd platform-wide (masked because reads go via a truth VIEW). 42501 is a GRANT-layer denial evaluated BEFORE RLS, so this can't be caught by policy checks. Teeth-proven both ways (revoke SELECT → flags the exact table+fix; restore → 0). Forward-only ratchet (baseline 0); skips cleanly if the DB is down. Complements validate_migration_grant_regression (the OVER-grant direction). FIX on FAIL: GRANT the named privilege, or add a `grant-check-allow` marker if the write is intentionally unreachable. Self-test: --self-test.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "redundant-widgets",
        "script":  "tools/validate_redundant_widgets.py",
        "args":    [],
        "label":   "Redundant status-chrome + duplicate-action widgets (2026-07-22) — the LOCK for the redundant-widget consolidation (Ian: 'redundant displays on every page … online and live pill … a bottom updated x min ago … a + widget but there are already a function of that'). Check A (deterministic): whFreshnessFooter must STAY a no-op — the bottom-right 'Updated X ago' footer DUPLICATED each page's own source chip (.wh-source-chip 'Live · refreshed…'), so it was neutered at its utils.js SSOT (removed the display on all ~18 adopters + the ~25 guarded call-sites in one edit); re-stamping it via whFreshnessChip() re-introduces the duplicate freshness platform-wide. Check B (forward ratchet, baseline 4): a create/primary action wired to 2+ STATIC buttons on one page (e.g. pm-scheduler's Add-asset '+' FAB + the 'Add Asset' bottom-nav tab, both goAddAsset(), both visible) is a redundant widget — route it to ONE labeled entry, not a second floating FAB. State-branch pairs (empty-vs-populated) stay in the baseline. Also silenced: the 'Live' realtime pill (hive+community) + the nav-hub 'Online' pill now hide when healthy, surface only on degrade. Self-test: --self-test. FIX on FAIL: keep the footer a no-op; remove the duplicate create-action button.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "rubric-coverage",
        "script":  "tools/rubric_coverage.py",
        "args":    ["--check"],
        "label":   "UFAI rubric coverage board (UR-P4, 2026-07-21) — aggregates the 61 single-page dims (family_rubric_scoreboard.json) + the 2 cross-page dims S2/S3 (component_consistency_corpus.json) into ONE 63-dim board (rubric_coverage.json), asserting every dim in ufai-rubric-spec.json has a measurement source. A new rubric dim with no source FAILs. Self-test: --self-test.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "journey-ux-dims",
        "script":  "tools/validate_journey_ux_dims.py",
        "args":    [],
        "label":   "UFAI experience-in-motion source-grep dims (2026-07-22, PDDA_UX_PAINPOINT_JOURNEY_ROADMAP) — the 3 journey dims the runtime __RUBRIC lens can't cleanly see, measured from page SOURCE + forward-ratcheted: J3 consequence (every destructive handler routes through the shared whConfirm OR is soft-delete-undoable — NN/g 'undo > confirmation'), G5 system-memory (a filterable page persists the user's filter/view/sort choice to localStorage), S4 behavioral-consistency (destructive actions use the ONE shared whConfirm, no raw window.confirm). Emits journey_ux_dims_report.json (the coverage-gate source for J3/G5/S4). Baselines in journey_ux_dims_baseline.json ratchet forward. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "ai-surface-quota",
        "script":  "tools/validate_ai_surface_quota.py",
        "args":    ["--check"],
        "label":   "D12 per-SURFACE AI cost/quota adoption (2026-07-23, the D-ledger's 'per-surface oracle unbuilt' cell, now built). `ai_rate_limits` is keyed by hive_id ALONE - one hourly+daily budget shared by EVERY AI surface - so a single runaway surface (looping companion, batch brief, retry storm) can drain the hive's whole AI allowance and starve assistant/voice/RAG/report-gen. This is a fairness+DoS bound, not just cost. The per-surface limiter ALREADY EXISTS (`_shared/rate-limit.ts` checkRouteRateLimit -> hive_route_quotas.hourly_cap + hive_route_calls counter keyed by (hive,route,hour), with an `enforce` flag so a surface can onboard in LOG-ONLY mode first) - so this is an ADOPTION gap, the METHOD-LAW shape (one unadopted central component, not N bespoke fixes). Currently 2/19 = 10.5% (ai-gateway, platform-gateway). Forward-only ratchet: a NEW rate-limited AI surface wired to the global cap alone drops the % and FAILs. Measurement only - changes no enforcement. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "ufai-deep-u",
        "script":  "tools/validate_ufai_deep_u.py",
        "args":    [],
        "label":   "UFAI U-pillar deep-verification lock (2026-07-23, PDDA §11 comprehensive deepwalk) — the live per-page deep-probe found the coarse A-Z lens (Z3 = 24px WCAG floor) was blind to the deeper UFAI U2 field standard (44px gloved-hand tap goal) + the Z2d responsive-image floor. The fix is a set of SHARED rules in tokens.css (the ONE file every page loads): input.wh-input/select.wh-select 44px, .btn-secondary/.btn-ghost 44px, img{max-width:100%}. This static gate asserts those shared rules STAY — remove one and every form control / secondary button / image on every page silently regresses below the field standard. Forward-only. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "file-upload-safety",
        "script":  "tools/validate_file_upload_safety.py",
        "args":    [],
        "label":   "File-upload safety — P12 upload-safety scanner (bug-hunt denominator v2, 2026-07-17). VERIFIED the platform has NO server-side file storage (zero storage.from().upload()); files are read client-side (resume->AI-extract, logbook photo->data-URI) and discarded, so classic unrestricted-upload/path-traversal (CWE-434/22) are low/N/A. The REAL residual on every <input type=file> surface is client-side: an accept= type allowlist AND a file.size cap (a huge file OOMs FileReader/canvas/the AI extractor = DoS). This gate asserts BOTH per surface; heuristic v1 (regex, so 'guarded'=references a byte-magnitude file.size, may display-not-enforce — refine to parse an actual cap comparison). Current: 9 surfaces, 2 gaps (integrations.html, inventory.html). ADVISORY / non-blocking (always exits 0) — surfaces the gap while per-page size-cap fixes land, then ratchets guarded-only. Self-test: --selftest (deterministic).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "oc-updated-at-backed",
        "script":  "tools/validate_oc_updated_at_backed.py",
        "args":    [],
        "label":   "Optimistic-concurrency backing (LIVE) — every client `updated_at` write must be backed by a real column (bug-hunt roadmap P6, 2026-07-17). Scans client pages for `.from('T')...update({...updated_at...})` and asserts table T has an updated_at column in the DB. A write to a column-less table = a DEAD optimistic-concurrency guard + a phantom-column write (lost-update race + a likely 400 on the edit) — static analysis sees 'OC present' and misses it; only the live DB reveals the missing column. Born from pm_assets (pm-scheduler asset-edit OC was a dead no-op) + immediately caught the sibling marketplace_disputes (dispute-resolution admin flow). FIX: ADD COLUMN updated_at timestamptz DEFAULT now() + reuse the canonical touch_updated_at() trigger. Skips cleanly if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "attribution-pinned",
        "script":  "tools/validate_attribution_pinned.py",
        "args":    [],
        "label":   "Attribution-forge lock (LIVE) — every hive-scoped ACTION-attribution column (actor, approved_by, acknowledged_by, resolved_by, reviewed_by, assigned_by, submitted_by, dismissed_by, closed_by, rejected_by, completed_by, sent_by) must be pinned to the caller's hive_members identity by a bind trigger (bug-hunt roadmap P3/P5, 2026-07-18). RLS gates the ROLE (member/supervisor of the hive) but NOT the NAME — without a pin a caller stamps ANOTHER person's name on an approval/ack/assignment (intra-hive impersonation of an accountability record). Found live: alert_dismissals.actor + anomaly_signals.acknowledged_by/resolved_by (mig 000014) + 10 more approved_by/acknowledged_by/reviewed_by/assigned_by (mig 000015), all forge-probe verified. Same class as the worker_name sweep (migs 010/011) but different column names. Locks those pins against a dropped trigger AND catches any NEW unpinned attribution column. Skips if docker/DB unreachable. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "ai-hive-context",
        "script":  "tools/validate_ai_hive_context.py",
        "args":    [],
        "label":   "AI-context hive-resolution consistency (static) — a hive-id variable must never be ASSIGNED from the LEGACY `wh_hive_id` alone; the canonical resolution is `wh_active_hive_id || wh_hive_id || <default>` (bug-hunt roadmap P5, 2026-07-19). Live bug it locks: assistant.html's AI-gateway + semantic-RAG calls used `getItem('wh_hive_id') || null` while every data read used the active-first form — so in the modern flow (wh_active_hive_id set, wh_hive_id null) the assistant sent NULL hive context and answered WITHOUT the team knowledge base. Legit legacy reads (a `legId` backfill, the notif-key string concat) don't match the bug shape and are exempt. Forward-only, fast, no stack. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "approval-lock",
        "script":  "tools/validate_approval_lock.py",
        "args":    [],
        "label":   "Approval optimistic-lock class (static) — every client approve-write that stamps `approved_at: new Date()` must carry an optimistic lock (`.eq('status','pending')` OR `.is('approved_at',null)`) in the SAME chained update (bug-hunt roadmap P6/P7, 2026-07-19). Without it a concurrent approve / double-click / stale card silently OVERWRITES the first supervisor's approval attribution (approved_by/approved_at) = a last-write-wins accountability leak. Live-proven on rcm_fmea_modes (writerB re-approve overwrote 'Supervisor A'); the sweep it locks found 3 gaps — asset-hub approveFmeaMode/approveStrategy + alert-hub actOnAmcBrief (amc_briefings) + project-manager cancelCO — all fixed to mirror approveAssetNode/approveCO. Clears/restores (approved_at:null) are exempt. Forward-only, fast, no stack. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "readonly-p6-no-edit",
        "script":  "tools/validate_readonly_p6_no_edit.py",
        "args":    [],
        "label":   "Read-only P6 lock (static) — 11 pages VERIFIED to have no client edit surface (no .update/.upsert on a shared row) were scored P6=100 covered-by-nature (no concurrent-edit race possible). This gate asserts they STAY edit-free; if one gains a .update/.upsert its covered-by-nature basis is void → FAIL, forcing a real P6 hunt + re-score (bug-hunt roadmap, 2026-07-17). Forward-only, fast. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "anomaly-status-forward",
        "script":  "tools/validate_anomaly_status_forward.py",
        "args":    [],
        "label":   "Anomaly status forward-only machine (LIVE) — anomaly_signals must keep the BEFORE UPDATE OF status trigger that makes resolved/expired TERMINAL (bug-hunt alert-hub P6, 2026-07-17). Without it, a stale/concurrent Acknowledge regresses a resolved alert back to acknowledged (lost-update on the state machine; live-verified). Asserts trigger tg_anomaly_signals_forward_status + fn anomaly_signals_forward_only_status exist and still guard terminal states. Skips if docker/DB unreachable. FIX: re-apply mig 20260717000007. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "i18n-coverage",
        "script":  "tools/validate_i18n_coverage.py",
        "args":    [],
        "label":   "i18n coverage — P11 EN/FIL adoption of the shared data-i/_t localization system (bug-hunt denominator v2, 2026-07-17). Counts i18n markers (data-i= + _t( + whT() per USER-FACING page and classifies adoption (covered>=25 / partial / thin / none); internal-admin/dev/utility surfaces exempt. Current: 31 user-facing pages, 25 i18n-adopted (81%), 6 gaps (resume.html=0 worst). The P5 lever-ladder fix is adopting the SHARED system on the gaps, not per-page translation. Heuristic v1 (marker COUNT is an adoption proxy, not exact translated/total %). ADVISORY / non-blocking (always exits 0), ratchets. Self-test: --selftest.",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "intelligence-jsonb-shape",
        "script":  "tools/validate_intelligence_jsonb_shape.py",
        "args":    [],
        "label":   "Intelligence-layer JSONB shape (LIVE: asserts every jsonb column the Asset/Alert/Shift pages read as an array/object is actually stored as that jsonb type, never a double-encoded JSON *string* scalar. A seeder/producer doing `json.dumps(list)` into a jsonb column yields jsonb_typeof='string', which the consumers' `Array.isArray(x)?x:[]` guard silently reads as EMPTY — the staging card showed 0 parts while the rationale said '3 parts', and alert-hub printed '0 parts recommended … 3 parts appear'. Found+fixed 2026-07-12 [Asset/Alert/Shift PDDA F1]: seeders parts_staging.py + shift_plans.py did json.dumps; this LIVE type-shape gate caught a SECOND instance [shift_plans.payload 3/5 rows string] that static key-drift analysis cannot see. Covers parts_staging_recommendations.parts, anomaly_signals.top_reasons, shift_plans.payload, asset_risk_scores.top_factors. Skips per-column if empty/absent, whole gate if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "intelligence-write-isolation",
        "script":  "tools/validate_intelligence_write_isolation.py",
        "args":    [],
        "label":   "Intelligence-layer write isolation (LIVE two-tenant, rolled-back: simulates a real authenticated member and asserts a member CANNOT fabricate [INSERT] nor overwrite [UPDATE] an asset_risk_scores row — the nightly-batch-owned risk cache feeding asset-hub/alert-hub/shift-brain/analytics — nor insert an asset_node into a foreign hive, while their own-hive risk READ still works. Locks F11 [live-exploited 2026-07-12, Asset/Alert/Shift PDDA]: asset_risk_scores was a FOR ALL policy open to any active member, so a worker could inject a phantom 'critical' or bury a real one as 'low'; now service-role-only writes [matching sensor_readings/anomaly_signals]. + F10 defense-in-depth: asset_nodes_write USING owner-branch now hive-gated. Migration 20260712000013. Actors picked dynamically = reseed-robust. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "growth-write-isolation",
        "script":  "tools/validate_growth_write_isolation.py",
        "args":    [],
        "label":   "Growth-layer write isolation (LIVE, rolled-back: simulates a real authenticated member and asserts a member CANNOT self-mint a skill_badge [competence + 250 XP forgery] nor write a forged skill_exam_attempt nor write a skill_profile attributed to another worker [BOLA], that grade_skill_exam() exists + is SECURITY DEFINER [the only server-graded earn path], and that own-badge READ still works. Locks K1 [live-exploited 2026-07-12, Dayplanner/Growth PDDA]: skill_badges/skill_exam_attempts were client-writable + the exam was client-scored, so a worker could console-mint any badge; now client writes are locked + the exam is graded server-side vs the write-locked skill_exam_keys. + K6: skill_profiles WITH CHECK now pins auth_uid. Migrations 20260712000015/16. Actors picked dynamically = reseed-robust. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "reliability-kpi-faithfulness",
        "script":  "tools/validate_reliability_kpi_faithfulness.py",
        "args":    [],
        "label":   "Reliability-KPI faithfulness (LIVE: precomputed `asset_risk_scores.mtbf_days` must mirror the live canonical `get_mtbf_by_machine` engine — a divergence is allowed ONLY when a logbook event postdates the score's generated_at [bounded staleness the next cron folds in]. A divergence with NO newer source = a methodology fork / silent wrong reliability number = the temporal seesaw [ARC DI §10.5]. Measured live 2026-07-08: 90 machines, 1 bounded-stale, 0 unexplained. + a structural guard that batch-risk-scoring still sources mtbf from the canonical RPC. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "benchmark-rollup-faithfulness",
        "script":  "tools/validate_benchmark_rollup_faithfulness.py",
        "args":    [],
        "label":   "Cross-hive benchmark rollup faithfulness (LIVE: every `network_benchmarks` cross-tenant rollup must == the EXACT aggregate of the current per-hive `hive_benchmarks` in its segment [avg=mean, p25/p75=percentile_disc, sample_hives=distinct-hive count], sample_hives>=3 for privacy, no orphan rollup with no inputs — locks the cross-tenant seesaw [ARC DI §10.5]: a per-hive change that doesn't propagate benchmarks a hive against a stale/forked peer number. Measured live 2026-07-08: 5 rollups, 0 unfaithful/privacy/orphan; teeth-proven [a per-hive mutation was DETECTED]. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "embedding-no-stale-duplicates",
        "script":  "tools/validate_embedding_no_stale_duplicates.py",
        "args":    [],
        "label":   "Embedding re-embed-on-edit (LIVE: each logbook source entry must carry exactly ONE fault_knowledge embedding — a logbook edit-in-place re-calls embed-entry, which used to `.insert()` a SECOND embedding [no unique key on logbook_id], leaving a STALE duplicate in the RAG index [semantic search could return pre-edit text; ARC DI §10.5 embedding seesaw]. Migration 20260708000002 adds a UNIQUE index on fault_knowledge(logbook_id) + embed-entry now UPSERTs on it [re-embed REPLACES]. fix-to-ZERO. Skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "edge-observed-coverage",
        "script":  "tools/validate_edge_observed_coverage.py",
        "args":    [],
        "label":   "Edge observability coverage (fix-to-ZERO: every AI edge fn in ai_seams_catalog.ai_fns has serveObserved in its index.ts — the Arc-T net that lands a wh_traces error row on any unhandled throw must COVER every AI surface; a new AI fn shipped without the wrapper is an observability hole and blocks CI. Static fast half; the LIVE fire-proof is observability_fault_walk.py. Binds deep-walk D21 for the AI sub-grid).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "frontend-floor-cells",
        "script":  "tools/validate_frontend_floor_cells.py",
        "args":    [],
        "label":   "Frontend floor cells (fix-to-ZERO ratchet over the live-mined F-lens in frontend_ufai_results.json: F1 consoleErrors==0 [D17 SMOKE — page loads clean] + F6 loading/empty/error present [D15 honest degraded states], on every production page the sweep covers. Fast half of the two-tool pattern [mine_frontend_ufai_surfaces.mjs probes, this ratchets]; a page that starts throwing a console error or drops a degraded state blocks CI. SKIPs if the artifact is absent).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "deepwalk-flywheel",
        "script":  "tools/deepwalk_flywheel.py",
        "args":    [],
        "label":   "PLATFORM deep-walk FLYWHEEL v2 (Ian 2026-07-08): the WHOLE-platform quality ruler — GLOB-discovers the grid each cycle (40 pages × 13 oracle dims + 33 AI edge fns × 8 AI dims = ~784 cells → deepwalk_grid.json), EVIDENCE-BINDS every cell to an on-disk validator via its `# DEEPWALK-CELL: <surface> <dim>` self-tag (auto-joins a new validator with zero edits), derives ✅ (locked gate green) / 🟡 (oracle exists, unratcheted) / ⬜ (no oracle = the frontier), runs the DISCOVERED gate-floor (every tagged+registered-blocking validator), and names the lowest-coverage cell as the next --drive target. MEASURED %, not eyeballed; folds the DI §10.2 grid (100%) in as one sub-report. Exit 1 only if a locked gate regresses; coverage <100 is reported, not failed. Baseline 2026-07-08: 36.3% (page 28.7% · AI 50.8%).",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "warn",
    },
    {
        "id":      "rpc-write-integrity",
        "script":  "tools/validate_rpc_write_integrity.py",
        "args":    [],
        "label":   "RPC write-integrity (LIVE: every public plpgsql function's INSERT covers its target's NOT NULL columns + only writes tables that EXIST — locks two silent-100%-fatal classes found live 2026-07-07: NOT-NULL-omission [store_memory_turn] and stale-table-reference [delete_worker_data still UPDATEd the dropped `assets`]; both made a GDPR erasure / companion memory fail on every call behind a swallowed error; skips cleanly if the local DB is down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "cron-health",
        "script":  "tools/validate_cron_health.py",
        "args":    [],
        "label":   "Cron health (LIVE: no active pg_cron job's latest run failed with a CODE error — locks the unattended-silent-failure class found live 2026-07-07, where the soft-delete retention cron [hard_delete_expired_soft_deletes] errored 'column deleted_at does not exist' on 28 straight runs and nothing surfaced; excludes transient startup-timeouts + the local-only app.supabase_functions_url GUC; skips if pg_cron/DB absent)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "private-memory-isolation",
        "script":  "tools/validate_private_memory_isolation.py",
        "args":    [],
        "label":   "Private-memory isolation (LIVE: the per-worker AI-companion conversation tables [agent_memory/voice_journal_entries/dialog_state] must have OWNER-only SELECT policies — no hive_members read branch — locks the privacy leak found+proven-live 2026-07-07 where agent_memory_read let any hive member read another worker's private companion turns; dim-3; skips if DB down)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "leave-audit-ordering",
        "script":  "tools/validate_leave_audit_ordering.py",
        "args":    [],
        "label":   "Leave-audit ordering (hive.html writeAuditLog is awaitable + performLeave AWAITS the member_left audit BEFORE the hive_members self-delete — locks the race found live 2026-07-07 where fire-and-forget audit + awaited delete silently lost the audit trail once the delete committed first and RLS [hive_id IN user_hive_ids()] rejected the late insert; dim-6 destructive-action audit-trail)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "axe-live-authed",
        "script":  "tools/validate_axe_live.py",
        "args":    [],
        "label":   "Axe a11y — AUTHENTICATED write surfaces (LIVE: password-grants a seeded supervisor + scans the 9 Tier-1 write pages [hive/inventory/logbook/pm-scheduler/skillmatrix/community/dayplanner/marketplace/project-manager] through axe-core WCAG 2.2 AA @390px — the auth-gated pages the static axe_scan.js SKIPS, i.e. the highest-a11y-risk forms/modals/destructive surfaces. Integrity-at-zero: baseline 0, any NEW violation FAILs; proven 0 live 2026-07-07 [dim-8]. Skips cleanly if node/local-stack absent)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "assistant-recall",
        "script":  "tools/validate_assistant_recall.py",
        "args":    [],
        "label":   "Assistant multi-turn recall (ai-orchestrator's 0-agents 'not enough data' deflection must stay MEMORY-AWARE — guarded by memoryBlock + RECALL_RE — so a 'what did I just tell you?' recall question answers from the working buffer instead of deflecting; locks the CL5 defect found+fixed+live-verified 2026-07-08 where the Work Assistant had ZERO multi-turn recall while voice-journal recalled fine; dim-13)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "persona-echo-live",
        "script":  "tools/validate_persona_echo_live.py",
        "args":    [],
        "label":   "CL9 persona-echo LIVE (the floating companion's persona SELECTION must reach the backend: ai-gateway agent=voice-journal with context.persona=hezekiah|zaniah must echo data.persona back as the same value. Locks the fix for the live-caught bug where companion-launcher read an undefined getCurrentPersona + wrong key 'wh_persona' → the widget ALWAYS sent 'zaniah' while the avatar showed the real pick. Live-tier: GoTrue-auths a seeded worker, skips cleanly if the local stack is down; teeth-proven. 2026-07-08; dim-13)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "faithfulness-rail",
        "script":  "tools/validate_faithfulness_rail.py",
        "args":    [],
        "label":   "CL10 faithfulness rails (the assistant/chat brain is read-only advisory; two live-caught fabrication classes must stay guarded before an answer ships: (1) ACTION fabrication — 'Updated maintenance record'/'Log entry added' claims of writes it can't do [verified 0 logbook rows / 0 followups], stripped by _shared/action_provenance.ts; (2) ungrounded-KPI — token-accurate grounding + the no-provenance 'Your split is X%' hedge. Asserts both rails imported, token-accurate, committed-tested, and wired before the synthesis return; teeth-proven. Found+fixed+live-verified 2026-07-08; dim-13)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "clickable-keyboard-a11y",
        "script":  "tools/validate_clickable_keyboard_a11y.py",
        "args":    [],
        "label":   "Clickable keyboard a11y (dim-8 RESOLVED, not ratcheted: a runtime polyfill in utils.js [whClickableKbdA11y] makes every mouse-only clickable div/span/li keyboard-operable — focusable + role=button + Enter/Space activation, incl. dynamic renders. Gate asserts the polyfill is present + every active clickable-bearing page loads utils.js/fixes inline. Found+FIXED live 2026-07-07 platform-wide, not baselined)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "arc-u-focus-trap",
        "script":  "tools/validate_arc_u_focus_trap.py",
        "args":    [],
        "label":   "Arc U modal focus-trap + focus-restore (WCAG 2.1.2 No Keyboard Trap + 2.4.3 Focus Order — axe is STATIC and cannot see a focus trap. LIVE headless probe [tools/arc_u_focus_trap_probe.mjs, reuses FB2 programmatic sign-in, not the thrash-prone MCP browser] opens the marketplace Post modal [representative of all 9 sheets wired via wireSheetA11y->whModalA11y], Tab-walks 40x -> asserts focus-escapes=0 + Escape closes + focus returns to the opener #fab-post. Locks whModalA11y against regression. Verified clean 2026-07-11 [Arc U]. Skips cleanly if node/local-stack absent)",
        "group":   "Platform",
        "skip_if_fast": True,
        "severity": "fail",
    },
    {
        "id":      "plain-language",
        "script":  "tools/validate_plain_language.py",
        "args":    [],
        "label":   "Plain language (no consumer-tech jargon [KYB/IDOR/RLS], internal terms, or removed-payment vestige [escrow/2307/GMV] in user-facing static copy — audience is EVERY Filipino worker, not just engineers; Ian 2026-07-06. Industrial acronyms MTBF/OEE/RCM are WARN-only, expand on first use; marketplace marketing articles rewritten free/contact-only)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "supervisor-approval-backstop",
        "script":  "tools/validate_supervisor_approval_backstop.py",
        "args":    [],
        "label":   "Supervisor-approval backstop (approval-gated tables asset_nodes/rcm_fmea_modes/rcm_strategies carry the tg_guard_approval trigger so a worker cannot self-approve or delete signed-off work via direct PostgREST — locks the UI-only-gate privilege-escalation class found + exploited live 2026-07-07; dim-2)",
        "group":   "Platform",
        "skip_if_fast": False,
        "severity": "fail",
    },
    {
        "id":      "deeplink-param-contracts",
        "script":  "validate_deeplink_param_contracts.py",
        "args":    [],
        "label":   "Deep-Link Param Contracts (forward-only: no NEW emitted ?param lacks a .get() reader in its destination — catches the dead-param class from the Phase-6b edge walk)",
        "group":   "Platform",
        "report":  "deeplink_param_contracts_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "kpi-source-registry",
        "script":  "validate_kpi_source_registry.py",
        "args":    [],
        "label":   "KPI Source Registry (one metric = one official derivation; consumers must read it and never re-derive a documented wrong way — catches the F4 26-vs-4 class)",
        "group":   "Platform",
        "report":  "kpi_source_registry_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "tester-coverage",
        "script":  "validate_tester_coverage.py",
        "args":    [],
        "label":   "Tester Coverage Validator (every live tool page is in PUBLIC_PAGES + 4 flow PAGES lists)",
        "group":   "Platform",
        "report":  "tester_coverage_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "schema-coverage",
        "script":  "validate_schema_coverage.py",
        "args":    [],
        "label":   "Schema Coverage Validator (auto-derived from migrations, table+column existence)",
        "group":   "Platform",
        "report":  "schema_coverage_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "reset-coverage",
        "script":  "validate_reset_coverage.py",
        "args":    [],
        "label":   "Reset Coverage Validator (every migration table is in reset.py)",
        "group":   "Platform",
        "report":  "reset_coverage_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "edge-config",
        "script":  "validate_edge_config.py",
        "args":    [],
        "label":   "Edge Function Config Validator (config.toml coverage)",
        "group":   "Platform",
        "report":  "edge_config_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "cross-page",
        "script":  "validate_cross_page.py",
        "args":    [],
        "label":   "Cross-Page Flow Validator",
        "group":   "Platform",
        "report":  "cross_page_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "dom-refs",
        "script":  "validate_dom_refs.py",
        "args":    [],
        "label":   "DOM Reference Integrity Validator (bare getElementById on missing elements)",
        "group":   "Platform",
        "report":  "dom_refs_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "asset-brain",
        "script":  "validate_asset_brain.py",
        "args":    [],
        "label":   "Asset Brain Foundation Validator (schema, RLS, realtime publication)",
        "group":   "Platform",
        "report":  "asset_brain_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "canonical-sources",
        "script":  "validate_canonical_sources.py",
        "args":    [],
        "label":   "Canonical Sources Registry Validator (truth-scattering fix foundation + L2 drift detection)",
        "group":   "Platform",
        "report":  "canonical_sources_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "kpi-chip-coverage",
        "script":  "validate_kpi_chip_coverage.py",
        "args":    [],
        "label":   "KPI Chip Coverage Validator (pages reading v_*_truth must render renderSourceChip)",
        "group":   "Platform",
        "report":  "kpi_chip_coverage_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        "id":      "calm-canonical-audit",
        "script":  "tools/audit_calm_dashboard_canonical.py",
        "args":    [],
        "label":   "Calm Dashboard Canonical-Wiring Audit (per-tile classify: canonical/drift/gap/allowed)",
        "group":   "Platform",
        "report":  "calm_canonical_audit_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        # Arc P (Pareto Page Revamp) content gate. Forward-only ratchet on the ONE
        # signal Ian named: DISPLAYED defensive-copy count = 0 platform-wide ("kill
        # 'we won't fake this'"). Scans HTML visible text + maturity-gate-consumer
        # inline JS + shared DOM-renderer .js, stripping comments / JSON-LD / AI
        # system prompts first (the R0 3x-over-count lesson). Baseline auto-tightens
        # DOWN (Rule B); FAILS only on a NEW defensive phrase. Wave 0 drives 10 -> 0.
        "id":      "pareto-content",
        "script":  "validate_pareto_content.py",
        "args":    ["--gate"],
        "label":   "Pareto Content Gate (Arc P: displayed defensive-copy ratchet -> 0; per-page P1/P3 metrics)",
        "group":   "Platform",
        "report":  "pareto_content_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        # Arc P P4 lens: NO EM DASH in displayed copy (the standing no-em-dash rule).
        # Prose em dashes hide in dynamically-rendered strings the empty-state walks miss
        # (found 11 on ph-intelligence/ai-quality only after walking them populated).
        # Forward-only ratchet (Rule B): FAILS only when the displayed em-dash count RISES
        # above the frozen baseline; auto-tightens DOWN as pages are cleaned. Scans HTML
        # visible text + title/aria/placeholder attrs + inline JS display strings + content
        # .js, excluding comments / non-display harness JS. Baseline seeded at the honest
        # current count; the sweep drives it toward 0 over sessions.
        "id":      "no-em-dash",
        "script":  "validate_no_em_dash.py",
        "args":    ["--gate"],
        "label":   "No-Em-Dash Gate (Arc P: displayed em-dash ratchet, forward-only toward 0)",
        "group":   "Platform",
        "report":  "no_em_dash_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        # L-1.5 platform-wide canonical-drift miner. Closes the seam between
        # validate_canonical_sources (which grandfathers via KNOWN_DRIFT /
        # HTML_OWNERS) and audit_calm_dashboard_canonical (which only scans
        # Calm-opted-in pages). Caught the "21 PM overdue on hive vs 0 on
        # pm-scheduler" mismatch on 2026-05-20: pm-scheduler was grandfathered
        # owner of pm_scope_items AND reimplemented FREQ_DAYS/calcNextDue
        # locally. The miner tags pages that render hero KPIs and read raw
        # tables that have a v_*_truth view as TIER A — drift here produces
        # the "two pages, two numbers" symptom users notice.
        "id":      "canonical-drift-platform-miner",
        "script":  os.path.join("tools", "mine_canonical_drift_platform.py"),
        "args":    [],
        "label":   "Canonical Drift — Platform-Wide Miner (L-1.5: TIER A = KPI page + canonical drift; produces baseline)",
        "group":   "Platform",
        "report":  "canonical_drift_platform_report.json",
        "skip_if_fast": False,
        "severity": "blocker",
    },
    {
        # L0 forward-only ratchet over the L-1.5 miner. FAILs when:
        #   - a page that wasn't TIER A becomes TIER A
        #   - a TIER A page's drift count regresses upward
        #   - local truth-math is introduced where baseline had none
        # Baseline lives in canonical_drift_baseline.json and only moves
        # downward (via --update-baseline after a deliberate reduction).
        "id":      "user-facing-kpi-canonical",
        "script":  "validate_user_facing_kpi_canonical.py",
        "args":    [],
        "label":   "User-Facing KPI Canonical Gate (L0: forward-only ratchet over L-1.5 TIER A footprint)",
        "group":   "Platform",
        "report":  "user_facing_kpi_canonical_report.json",
        "skip_if_fast": False,
    },
    {
        # Cross-migration table-collision auditor. Catches the agent_memory
        # class of bug where two migrations both `CREATE TABLE IF NOT EXISTS X`
        # with incompatible column sets. The static counterpart to the runtime
        # sentinel that surfaced this on 2026-05-20 (5 collisions found across
        # 102 migrations, all documented with `-- table-collision-allow:` markers).
        # Exits 1 on any unallowed collision.
        "id":      "table-collision-audit",
        "script":  os.path.join("tools", "audit_table_collision.py"),
        "args":    [],
        "label":   "Cross-Migration Table-Collision Auditor (catches CREATE TABLE IF NOT EXISTS with incompatible column sets across migrations)",
        "group":   "Platform",
        "report":  "table_collision_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "phantom-captures",
        "script":  "tools/audit_phantom_captures.py",
        "args":    [],
        "label":   "Phantom Capture Auditor (reverse-lineage: every <input>/<select> must have >=1 downstream consumer)",
        "group":   "Platform",
        "report":  "phantom_captures_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "phantom-columns",
        "script":  "tools/audit_phantom_columns.py",
        "args":    [],
        "label":   "Phantom Column Auditor (schema-bloat: every column in registry must have >=1 consumer)",
        "group":   "Platform",
        "report":  "phantom_columns_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "tier-contracts",
        "script":  "tools/audit_tier_contracts.py",
        "args":    [],
        "label":   "Tier Contract Auditor (Fuel/Engine/Brain/Glue registry health + chain integrity)",
        "group":   "Platform",
        "report":  "tier_contracts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "standards-alignment",
        "script":  "tools/audit_standards_alignment.py",
        "args":    [],
        "label":   "Standards Alignment Auditor (Tier S — formula required_inputs supersets cited standard OR honestly declared partial_variant)",
        "group":   "Platform",
        "report":  "standards_alignment_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ai-prompt-standards",
        "script":  "tools/audit_ai_prompt_standards.py",
        "args":    [],
        "label":   "AI Prompt Standards Audit (Tier B — edge fn prompts mentioning a metric must cite its canonical standard)",
        "group":   "Platform",
        "report":  "ai_prompt_standards_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "displayed-values",
        "script":  "tools/audit_displayed_values.py",
        "args":    [],
        "label":   "Displayed Values Audit (Tier S coverage — every value rendered to users should map to a formula contract OR be classified as raw display)",
        "group":   "Platform",
        "report":  "displayed_values_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "correctness-scoreboard",
        "script":  "tools/build_correctness_scoreboard.py",
        "args":    ["--check"],
        "label":   "CORRECTNESS anti-drift scoreboard — the value-at-the-glass sibling of bughunt-scoreboard. Regenerates CORRECTNESS_SCOREBOARD.md mapping every contracted user-facing metric to the QA WHAT-axis (a canonical-source · b calculation · c cross-surface parity · d db-truth · e provenance) and its covering gate; FAILs if a contracted metric loses source/formula/static-parity coverage. Reads displayed_values_report.json (so runs after the displayed-values audit refreshes). 2026-07-20: 10 metrics · 10 COVERED · 0 GAP · 6 multi-page. Residual tracked build = RUNTIME cross-surface parity (a live probe that a multi-page hive KPI renders the same value on each page)",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        "id":      "partial-label-honesty",
        "script":  "tools/audit_partial_label_honesty.py",
        "args":    [],
        "label":   "Partial-Label Honesty Audit (Tier S rendering — every page displaying a partial-variant metric must render the honesty marker near the value)",
        "group":   "Platform",
        "report":  "partial_label_honesty_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "home-stack-coverage",
        "script":  "validate_home_stack_coverage.py",
        "args":    [],
        "label":   "Home Stack Coverage Validator (primary-nav cardinality + hidden tools have deep-links)",
        "group":   "Platform",
        "report":  "home_stack_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "js-syntax-sanity",
        "script":  "validate_js_syntax_sanity.py",
        "args":    [],
        "label":   "JS Syntax Sanity (no `await` inside non-async function/IIFE in inline scripts)",
        "group":   "Platform",
        "report":  "js_syntax_sanity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "silo-monitor",
        "script":  "validate_silo_monitor.py",
        "args":    [],
        "label":   "Silo Monitor (4-layer: drift + orphans + unregistered hotspots + cross-system matrix)",
        "group":   "Platform",
        "report":  "silo_monitor_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "canonical-anchor",
        "script":  "validate_canonical_anchor.py",
        "args":    [],
        "label":   "Canonical Anchor Gate (8-layer forward-anchor: fuel/engine/Tier A/Tier C/formula/standard/dashboard/capture)",
        "group":   "Platform",
        "report":  "canonical_anchor_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — meta-validator: every consumer-scanning audit must cover
        # supabase/functions/_shared/**/*.ts + subdirectory HTML, or the next
        # contributor will reintroduce the false-positive phantom-column class
        # caught 2026-05-20 (memory: feedback_audit_scanner_scope.md).
        "id":      "audit-scanner-scope",
        "script":  "validate_audit_scanner_scope.py",
        "args":    [],
        "label":   "Audit Scanner Scope (meta-validator: every consumer-scanning audit covers _shared + subdir HTML)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — catches semantic drift INSIDE the same canonical signal:
        # surfaces that read a v_*_truth column but locally re-derive its math
        # (qty_on_hand <= reorder_point, FREQ_DAYS, Date.now() - last_completed,
        # 'nodata' fallback for never-serviced items). Three real bugs caught
        # on first run: pm-scheduler.html, index.html low-stock, logbook.html.
        "id":      "truth-view-signal-trust",
        "script":  "validate_truth_view_signal_trust.py",
        "args":    [],
        "label":   "Truth-View Signal-Trust (no local re-derivation alongside v_*_truth reads; forward-only ratchet)",
        "group":   "Platform",
        "report":  "truth_view_signal_trust_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — catches KPI tiles declared in HTML with default values
        # ("0", "—") that no JS setter ever populates. Caught the marketplace
        # `mk-total-hero` / skillmatrix `sm-*-hero` class on first pass (turned
        # out wired via setCard dispatch; baseline 0 locks the cleaned state).
        "id":      "orphan-kpi-tiles",
        "script":  "validate_orphan_kpi_tiles.py",
        "args":    [],
        "label":   "Orphan KPI Tiles (every default-value tile must have a JS setter; forward-only ratchet)",
        "group":   "Platform",
        "report":  "orphan_kpi_tiles_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — catches the home-tile undercount class: a page does
        # `db.from(v_*_truth)...limit(N)` then renders `arr.length` as a
        # KPI count. The number caps at N silently when actual data exceeds.
        # Caught the index.html Open Jobs / Risk Alerts bug (limit(5)) and
        # marketplace-seller.html badges (limit(50)).
        "id":      "kpi-count-query-safety",
        "script":  "validate_kpi_count_query_safety.py",
        "args":    [],
        "label":   "KPI Count-Query Safety (no .limit(N) + .length as canonical KPI count; forward-only ratchet)",
        "group":   "Platform",
        "report":  "kpi_count_query_safety_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every renderSourceChip({ source: 'v_X_truth + ...' })
        # must reference views the page actually reads. Catches stale chip
        # declarations that lie about lineage after a refactor (page stopped
        # reading view X, but the chip still claims it). Baseline tightened
        # from 7 to 0 the same day by fixing hive chips + allow-markering
        # RPC/wrapper-backed chips.
        "id":      "source-chip-truth",
        "script":  "validate_source_chip_truth.py",
        "args":    [],
        "label":   "Source-Chip Truth (every renderSourceChip view is actually .from()-read on the page; forward-only ratchet)",
        "group":   "Platform",
        "report":  "source_chip_truth_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-06-14 (STREAMLINE §13/§14 E1) — keeps developer jargon OFF the glass.
        # Ian screenshotted dashboard captions leaking v_*_truth view names, RPC/edge-fn
        # names, code idents (_pmOverdueCount, hideZeroStat()), *.md doc refs and raw SQL
        # predicates (qty_on_hand <= min_qty). The provenance chip is good; its TEXT was
        # authored in engineer voice. Scans user-VISIBLE strings only — chip
        # freshness/window/notes + HTML explainer blocks — NOT the source: field (that
        # stays canonical for source-chip-truth and is translated via WH_SOURCE_LABELS in
        # utils.js) and NOT JS/HTML comments or <code>/<pre>. Forward-only ratchet.
        "id":      "user-facing-jargon",
        "script":  "validate_user_facing_jargon.py",
        "args":    [],
        "label":   "User-Facing Jargon (no v_*_truth / RPC / code-ident / *.md / SQL on the glass; chip source: exempt; forward-only ratchet)",
        "group":   "Platform",
        "report":  "user_facing_jargon_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-06-14 (STREAMLINE §14 E4) — the design palette/type/spacing from
        # designer SKILL.md was raw hex inline on every page (a wrong orange
        # #e8920a once drifted into parts-tracker/assistant). E4 promotes it to
        # CSS custom properties in components.css :root. This gate: L1 the token
        # block must declare every canonical value (palette can't drift/delete),
        # L2 the drift hex #e8920a is banned on the glass, L3 raw-brand-hex
        # inline usage ratchets forward-only (use var(--wh-*)).
        "id":      "design-tokens",
        "script":  "validate_design_tokens.py",
        "args":    [],
        "label":   "Design Tokens (components.css :root canonical palette intact + no #e8920a drift + raw-brand-hex forward-only ratchet)",
        "group":   "Platform",
        "report":  "design_tokens_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-07-16 (FULLSTACK_COMPONENT_LIBRARY_ROADMAP §2.3 F-P2) — FAMILY_UFAI
        # §10 proved dims fail exactly where canonical components sit unadopted
        # (whListSkeleton, .wh-disclose, whFmt*…). This ratchet recomputes adoption
        # LIVE per design_component_registry.json row over the 32 family pages and
        # fails any drop below the component_adoption_baseline.json floor;
        # rises auto-tighten. Also fails registry/floor orphans + inline
        # redefinitions of canonical utils.js functions.
        "id":      "component-adoption",
        "script":  "validate_component_adoption.py",
        "args":    [],
        "label":   "Component Adoption (canonical design-library adoption per registry row; forward-only floors, auto-tighten; no inline redefinitions)",
        "group":   "Platform",
        "report":  "component_adoption_gate_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-07-17 (FULLSTACK_COMPONENT_LIBRARY_ROADMAP Layer A, A-P2) — the Layer-A
        # sibling of component-adoption: canonical _shared/ module adoption per
        # api_component_registry.json row, recomputed live over every edge function's
        # index.ts. A function dropping its cors/envelope/rate-limit/tenant-context
        # import fails; rises auto-tighten. Exemptions documented in the registry
        # (voice-model-call orphan; fixed-endpoint outbound fns).
        "id":      "api-adoption",
        "script":  "validate_api_adoption.py",
        "args":    [],
        "label":   "API Adoption (canonical _shared/ module adoption per edge function; forward-only floors, auto-tighten)",
        "group":   "Platform",
        "report":  "api_adoption_gate_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-07-17 (FULLSTACK_COMPONENT_LIBRARY_ROADMAP Layer D, D-P2) — canonical DB
        # PATTERN adoption over the substrate's live-DB-derived chunks: RLS on tenant
        # tables, hive-membership scoping, auth_uid ownership policies, security_invoker
        # views. A view/table dropping its pattern fails; rises auto-tighten. Source
        # chunks are freshness-gated by validate_substrate_freshness.py.
        "id":      "db-adoption",
        "script":  "validate_db_adoption.py",
        "args":    [],
        "label":   "DB Adoption (canonical RLS/policy/invoker pattern adoption per table/view; forward-only floors, auto-tighten)",
        "group":   "Platform",
        "report":  "db_adoption_gate_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-07-17 (FULLSTACK_COMPONENT_LIBRARY_ROADMAP Layer AU, AU-P2) — the client
        # session/identity floor: restoreIdentityFromSession + session-settled reads
        # (the cold-load-401 class) per family page. A page dropping either regresses.
        "id":      "au-adoption",
        "script":  "validate_au_adoption.py",
        "args":    [],
        "label":   "AU Adoption (client auth floor: identity restore + session-settled reads per page; forward-only floors)",
        "group":   "Platform",
        "report":  "au_adoption_gate_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-07-17 (FULLSTACK_COMPONENT_LIBRARY_ROADMAP Layer AV) — the offline canonical
        # set (banner/queue/connectivity/session-timeout/device-fingerprint) ships as ONE
        # unit on every non-exempt family page (29/29 at wave completion). A page dropping
        # any of the five, or carrying a partial set, fails.
        "id":      "av-adoption",
        "script":  "validate_av_adoption.py",
        "args":    [],
        "label":   "AV Adoption (offline canonical set per page: full 5-script unit; forward-only floor + no-partial rule)",
        "group":   "Platform",
        "report":  "av_adoption_gate_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — catches enum-column filter case/spelling drift across
        # surfaces. Two pages filtering `.eq('status', 'Open')` vs
        # `.eq('status', 'open')` return different result sets from the SAME
        # column. Tracks `(column, lowercase(value))` and fails when 2+
        # files use distinct case variants. Allow with `// filter-case-allow`.
        "id":      "filter-case-consistency",
        "script":  "validate_filter_case_consistency.py",
        "args":    [],
        "label":   "Filter Case Consistency (same enum-column filter must use consistent case across files; forward-only ratchet)",
        "group":   "Platform",
        "report":  "filter_case_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — catches realtime subscription drift. `hive.html` had
        # been subscribing to `postgres_changes` on the DEAD `assets` table
        # for months — supervisors never got realtime "asset pending approval"
        # notifications because workers actually submit to `asset_nodes`.
        # Fixed in same commit. Validator subscribes-table must match (or be
        # an underlying-table of) the page's .from() reads.
        "id":      "realtime-subscription",
        "script":  "validate_realtime_subscription_consistency.py",
        "args":    [],
        "label":   "Realtime Subscription Consistency (every postgres_changes table must be read by the page; forward-only ratchet)",
        "group":   "Platform",
        "report":  "realtime_subscription_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — catches dead-link CTAs (href / location.href targets
        # to a .html file that doesn't exist on disk). User clicks → 404.
        # Caught voice-journal.html → login.html on first run (the canonical
        # signin redirect is index.html?signin=1; login.html never existed).
        "id":      "link-target-existence",
        "script":  "validate_link_target_existence.py",
        "args":    [],
        "label":   "Link Target Existence (every <a href>/location.href to a .html target must exist on disk; T1 2026-08-24: roster DERIVED from the served root (42 pages, was a stale hardcoded 30) AND #anchors resolve — in-page and cross-page fragments must name a declared id, the exact regex hole the dead sticky-#join CTA lived in; forward-only ratchet)",
        "group":   "Platform",
        "report":  "link_target_existence_report.json",
        "skip_if_fast": False,
    },
    {
        # T155 (2026-08-28) — the sibling above resolves .html targets and #anchors across the app
        # roster, and cannot see the shape the PUBLIC FUNNEL is built from: /learn/<slug>/ and
        # /tools/<slug>/ are DIRECTORY urls served by an inner index.html, and a href ending in
        # "/" never matches a .html suffix test. The 113 public pages were also link SOURCES in no
        # gate at all. Proven with teeth before registering: three dead links injected into a learn
        # page (directory url, relative directory, .html file) were all caught, and removing them
        # returned the count to zero — so the zero this holds is a measurement, not a silence.
        "id":      "internal-links-resolve",
        "script":  "tools/prove_internal_links_resolve.py",
        "args":    [],
        "label":   "Internal Links Resolve (every internal href on all 155 pages — 42 app + 53 learn + 60 calculators — must land on a file OR a directory that serves an index.html; the directory-url shape the .html-suffix gate is blind to; markup only, scripts/styles/comments stripped)",
        "group":   "Platform",
        "report":  "internal_link_census_report.json",
        "skip_if_fast": False,
    },
    {
        # T155 (2026-08-28) — the notice a lost visitor sees. Its first version ended "use the
        # navigation or search below" and index.html has NO search box: the one message whose job
        # is helping someone lost pointed at something that does not exist. It now carries a real
        # search handing off to the learn hub's ?q= filter, so this asserts the whole promise —
        # fires on foreign paths, silent on real ones, and the search REACHES guides. Teeth proven
        # both ways: breaking the hub's ?q= reddens 2 assertions, breaking the notice's pre-fill
        # reddens 3. Serves its own subject (the seeder hard-404s, so netlify's rewrite — the exact
        # condition the notice detects — cannot be reproduced against it).
        "id":      "404-notice-helps",
        "script":  "tools/prove_404_notice_helps.mjs",
        "args":    [],
        "label":   "404 Notice Helps (a dead path names itself, pre-fills the slug the visitor asked for, and one Enter reaches matching guides; no-match says so; real pages stay silent)",
        "group":   "Platform",
        "skip_if_fast": True,
    },
    {
        # T1 (2026-08-24) — conversion-visibility family. Born from a dead primary CTA under a
        # 790-gate green board: every prior CTA oracle asserted EXISTENCE, none CONSEQUENCE.
        "id":      "scenario-matrix",
        "script":  "tools/validate_scenario_matrix.py",
        "args":    [],
        "label":   "Scenario Matrix (T1.2 diversity guarantee: 72 device x auth x entry x intent cells — structure complete, no axis value living only as declared_na, every NA reasoned, every covered cell evidenced; silence is never coverage)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "cv-public-surface",
        "script":  "tools/public_surface_gate.py",
        "args":    [],
        "label":   "Public Surface (T1.3: the 114 learn/tools funnel pages join a UX denominator — both auth doors present, zero waitlist CTAs, 'no sign-up needed' claims scoped to the embedded tool, every site-relative link resolves; instance-cheap static lint, template-deep = prove_cta_activation exemplars)",
        "group":   "Platform",
        "report":  "public_surface_registry.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cv-funnel",
        "script":  "tools/validate_conversion_bank.py",
        "args":    [],
        "label":   "Conversion Funnel Bank (T1.4: cross-page rows keyed scenario-cell x CV oracle; verdicts NOT stored — re-derived every run from cta_activation_report + K1 journey results + the public-surface registry, so a row cannot stay green after its prover stops agreeing; evidence older than 45d goes OWED)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "deploy-root-hygiene",
        "script":  "tools/validate_deploy_root_hygiene.py",
        "args":    [],
        "label":   "Deploy Root Hygiene (T1.4: publish='.' serves every root file — no *-test/*.backup dev copies in the root (index.backup.html was publicly reachable), _fixtures/ force-404'd and ordered before the catch-all)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "page-roster",
        "script":  "tools/validate_page_roster.py",
        "args":    [],
        "label":   "Page Roster Parity (T1.4: ONE roster (substrate/reference/page_roster.json), every consumer's scope checked inside it — sw.js SHELL_FILES (a precached 404 fails the whole SW install), nav-hub TOOLS, the 22 banks, the link gate's derived scope; six instruments once drifted six ways)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Ian 2026-08-24: "put completion percentage each trajectories so that we have anti-drift
        # discipline on the header of the roadmap." The header scoreboard is GENERATED from
        # trajectory_registry.json; this gate fails on drift, dishonest pct, or an unbacked lock.
        "id":      "trajectory-registry",
        "script":  "tools/validate_trajectory_registry.py",
        "args":    [],
        "label":   "Trajectory Registry + Header Scoreboard (500-arc program SSOT: ids complete T1-T500, pct honest per status (any other value needs an in-flight status + written basis), the roadmap's header scoreboard equals a fresh regeneration, scenario-matrix v1+v2 refs resolve, a 'locked' trajectory names only REGISTERED gates)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-08-31 — REGISTRATION is not EXECUTION and execution is not a PASS. The first full board
        # exposed two rows LOCKED at 100% (T11, T176) whose gate was red, locked on a fast board that
        # SKIPPED their gate. The trajectory-registry gate above holds a 'locked' row to NAMING a
        # registered gate; this one holds it to that gate reading PASS on the last FULL board — a hard
        # fail on a false 100%, and it refuses (UNVERIFIABLE) rather than passing on a fast board.
        "id":      "locks-are-verified",
        "script":  "tools/validate_locks_are_verified.py",
        "args":    [],
        "label":   "Locks Are Verified (a 'locked' 100% trajectory's named gate must read PASS on the last FULL board, not merely be registered; refuses UNVERIFIABLE on a fast board; 'locking' WIP with a red gate is surfaced, not failed)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every payload.new.X / payload.old.X in a
        # postgres_changes callback must be a real column of the subscribed
        # table. Sibling to realtime-subscription (which checks table).
        "id":      "realtime-payload-columns",
        "script":  "validate_realtime_payload_columns.py",
        "args":    [],
        "label":   "Realtime Payload Columns (payload.new/old.X must be a real column on the subscribed table; forward-only ratchet)",
        "group":   "Platform",
        "report":  "realtime_payload_columns_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every functions.invoke('NAME') must point at a real
        # edge fn directory. Caught silent-404 risk when fn renamed.
        "id":      "edge-function-invoke",
        "script":  "validate_edge_function_invoke.py",
        "args":    [],
        "label":   "Edge Function Invoke (every functions.invoke('X') target must exist; forward-only ratchet)",
        "group":   "Platform",
        "report":  "edge_function_invoke_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every db.rpc('X', { p_y: ... }) must use real arg
        # keys for the RPC's signature. Caught registry bug where the
        # CREATE FUNCTION regex truncated args at vector(384) — fixed there
        # so this validator can do its job cleanly.
        "id":      "rpc-argument-consistency",
        "script":  "validate_rpc_argument_consistency.py",
        "args":    [],
        "label":   "RPC Argument Consistency (every db.rpc() name + arg keys exist; forward-only ratchet)",
        "group":   "Platform",
        "report":  "rpc_argument_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every <img src>, <link href>, <script src>, css url()
        # pointing at a local asset must resolve to a file. Catches broken
        # image icons and 404'd CSS/JS includes after a rename.
        "id":      "image-asset-existence",
        "script":  "validate_image_asset_existence.py",
        "args":    [],
        "label":   "Image / Asset Existence (every local asset ref must resolve to a file; forward-only ratchet)",
        "group":   "Platform",
        "report":  "image_asset_existence_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every path in sw.js's SHELL_FILES = [...] array
        # must exist. Otherwise SW precache install fails silently.
        "id":      "service-worker-shell",
        "script":  "validate_service_worker_shell.py",
        "args":    [],
        "label":   "Service Worker SHELL_FILES (every precache path must exist; forward-only ratchet)",
        "group":   "Platform",
        "report":  "service_worker_shell_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every column in .select/.eq/.in/.gt/.gte/.lt/.lte/
        # .order/.is on a known table must be a real column. Catches schema-
        # rename drift where a column was removed/renamed but consumer code
        # still references the old name. Includes a registry-miner fix for
        # multi-clause ALTER TABLE ADD COLUMN.
        "id":      "query-column-existence",
        "script":  "validate_query_column_existence.py",
        "args":    [],
        "label":   "Query Column Existence (every .select/.eq/.in column must exist on the table; forward-only ratchet)",
        "group":   "Platform",
        "report":  "query_column_existence_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-28 — sibling of query-column-existence: closes its blind spot
        # (`cols = table_cols.get(table); if cols is None: continue` silently
        # skips refs to objects that don't exist AT ALL). Every .from('T')/
        # /rest/v1/T must be a real table/view; every .rpc('fn')/rest/v1/rpc/fn
        # a real function. Found via the MCP cockpit flywheel (Playwright 404 +
        # Postgres cross-verify). Allow with `// obj-exist-allow: <reason>`.
        "id":      "supabase-object-existence",
        "script":  "validate_supabase_object_existence.py",
        "args":    [],
        "label":   "Supabase Object Existence (every .from/.rpc/REST object must exist in the canonical registry; forward-only ratchet)",
        "group":   "Platform",
        "report":  "supabase_object_existence_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-28 — GH hardening bridge from the cockpit flywheel: every NAMED
        # relative import under supabase/functions/** must resolve to a real
        # export. Catches the boot-break class (Deno "module does not provide an
        # export named X") that passes every static gate and only 503s at
        # `functions serve`. Caught cors.ts/corsHeaders + pdf-ingest/embedText.
        "id":      "edge-import-exports",
        "script":  "validate_edge_import_exports.py",
        "args":    [],
        "label":   "Edge Import/Export Resolution (every named relative import resolves to a real export; forward-only ratchet)",
        "group":   "Platform",
        "report":  "edge_import_exports_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-30 — memory-stack flywheel Turn 1 (layer 02 Episodic). Keeps the
        # durable agent_episodic_memory layer (Phase 7) wired into the live
        # gateway path: recallEpisodic before forward + persistEpisodic after,
        # via the single-source-of-truth _shared/episodic-memory.ts. Guards
        # against a refactor silently re-orphaning the episodic layer (it was
        # dead substrate — table + CRUD fn existed, gateway never called them).
        "id":      "episodic-memory-wiring",
        "script":  "validate_episodic_memory_wiring.py",
        "args":    [],
        "label":   "Episodic Memory Wiring (agent_episodic_memory recall+persist stays wired into ai-gateway; forward-only ratchet)",
        "group":   "Platform",
        "report":  "episodic_memory_wiring_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-30 — memory-stack flywheel Turn 2 (layer 07 Shared Memory).
        # Keeps the verified-state / conflict-resolution surface over
        # unified_events (v_asset_state_truth, resolved by source trust
        # precedence then recency) wired through _shared/verified-state.ts into
        # the gateway. Guards "one truth, every agent aligned" from regression.
        "id":      "verified-state-wiring",
        "script":  "validate_verified_state_wiring.py",
        "args":    [],
        "label":   "Verified-State Wiring (v_asset_state_truth conflict resolution stays wired into ai-gateway; forward-only ratchet)",
        "group":   "Platform",
        "report":  "verified_state_wiring_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every getElementById('X')/querySelector('#X') must
        # match an HTML element with id=X. Sibling of orphan-kpi-tiles
        # (which finds the opposite: HTML elements with no JS setter).
        "id":      "getelementbyid-orphan-setter",
        "script":  "validate_getelementbyid_orphan_setter.py",
        "args":    [],
        "label":   "getElementById Orphan Setter (every JS id lookup must have a matching <id> in HTML; forward-only ratchet)",
        "group":   "Platform",
        "report":  "getelementbyid_orphan_setter_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — surfaces hardcoded `N * 86400000` day windows where
        # the same context keyword (overdue/completed/recent/...) is used
        # with DIFFERENT N values across files. Catches "different numbers
        # because different windows" silent drift.
        "id":      "time-window-consistency",
        "script":  "validate_time_window_consistency.py",
        "args":    [],
        "label":   "Time-Window Consistency (same context keyword must use same N*day window across files; forward-only ratchet)",
        "group":   "Platform",
        "report":  "time_window_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — role-string permission-gate consistency. Every
        # `role === 'X'` literal must use a canonical role name; off-canon
        # strings are typo / privilege-drift candidates.
        "id":      "role-string-consistency",
        "script":  "validate_role_string_consistency.py",
        "args":    [],
        "label":   "Role String Consistency (every role === '...' literal must use a canonical role name; forward-only ratchet)",
        "group":   "Platform",
        "report":  "role_string_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — inline onclick/onchange/... handlers must reference
        # a defined function. Caught engineering-design.html → syncEGSoilInput()
        # which was a stale handler (function never existed). Fixed by removing
        # the dead onchange attribute.
        "id":      "inline-onclick-handler",
        "script":  "validate_inline_onclick_handler.py",
        "args":    [],
        "label":   "Inline Handler Existence (every onclick/onchange/... fn must be defined; forward-only ratchet)",
        "group":   "Platform",
        "report":  "inline_onclick_handler_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-21 paydown — bug-class L0 ratchets added during flywheel
        # turns 31-35 that landed on disk but were never wired into the
        # platform-check runner (caught by auto-discovery
        # validator_registered check).
        "id":      "empty-catch",
        "script":  "validate_empty_catch.py",
        "args":    [],
        "label":   "Empty Catch Block (try/catch{} that silently swallows errors; forward-only ratchet)",
        "group":   "Platform",
        "report":  "empty_catch_report.json",
        "skip_if_fast": False,
    },
    {
        # Locks the 2026-07-23 wayfinding-pill CLS flicker: wayfinding.js sets body.paddingTop
        # POST-PAINT on bare pages (via nav-hub), so under contention a DIFFERENT bare page shifts
        # ~64px = ~0.1 CLS each sweep (the intermittent rotating I1 the dynamic sweep can't reliably
        # catch). Every page that WILL get the pill (not-home + loads nav-hub + no in-layout back
        # affordance + has <main>/<h1>) must ship the static `body{padding-top:calc(64px+env(...))}`
        # reserve so the pill band is present at first paint (wayfinding's `if(band>cur)` then skips).
        "id":      "pill-reserve",
        "script":  "validate_pill_reserve.py",
        "args":    [],
        "label":   "Wayfinding Pill CLS-Reserve (every pill-page ships the static back-pill band reserve; absolute, baseline 0)",
        "group":   "Platform",
        "report":  "pill_reserve_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "settimeout-string",
        "script":  "validate_settimeout_string.py",
        "args":    [],
        "label":   "setTimeout/setInterval String Arg (string-form is eval-equivalent; forward-only ratchet)",
        "group":   "Platform",
        "report":  "settimeout_string_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "document-write",
        "script":  "validate_document_write.py",
        "args":    [],
        "label":   "document.write Usage (forbidden API; forward-only ratchet)",
        "group":   "Platform",
        "report":  "document_write_report.json",
        "skip_if_fast": False,
    },
    {
        # Deterministic half of the Holistic/Cross-Page Critic (Grounded MCP Sweep
        # Phase 4.7): redundancy is a RELATIONSHIP between files, invisible to the
        # per-element critic. Wraps jscpd (clone detector); forward-only ratchet on
        # DUPLICATED-LINES (S12 2026-06-14: switched from clone-COUNT, which S8's page
        # fusions made misleading — count fell while % rose; predictive.html now excluded).
        # Baseline clone_debt_baseline.json = 4742 lines / 25.92%. Degrades to SKIP if jscpd
        # absent (npm i -D jscpd to activate). Blocks NEW copy-paste; collapsing ratchets it down.
        "id":      "clone-debt",
        "script":  "validate_clone_debt.py",
        "args":    [],
        "label":   "Clone Debt (jscpd cross-page duplication; forward-only ratchet — redundancy critic)",
        "group":   "Platform",
        "report":  "clone_debt_baseline.json",
        "skip_if_fast": False,
    },
    {
        "id":      "tabindex-positive",
        "script":  "validate_tabindex_positive.py",
        "args":    [],
        "label":   "Positive tabindex (a11y anti-pattern: tabindex >= 1 breaks tab order; forward-only ratchet)",
        "group":   "Platform",
        "report":  "tabindex_positive_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "viewport-user-scalable",
        "script":  "validate_viewport_user_scalable.py",
        "args":    [],
        "label":   "Viewport user-scalable=no (a11y anti-pattern: blocks pinch-zoom; forward-only ratchet)",
        "group":   "Platform",
        "report":  "viewport_user_scalable_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — innerHTML = `...${interp}...` template literals must
        # run interpolations through escHtml/sanitize/e() escaper. XSS class
        # guard with forward-only ratchet.
        "id":      "innerhtml-eschtml",
        "script":  "validate_innerhtml_eschtml.py",
        "args":    [],
        "label":   "innerHTML escHtml Audit (interpolating template literals must escape; XSS guard, forward-only ratchet)",
        "group":   "Platform",
        "report":  "innerhtml_eschtml_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — Deno.env.get('X') / process.env.X references in edge
        # fns + python-api must be documented in .env.example/README. Catches
        # secret-rename drift that would crash edge fns at runtime.
        "id":      "env-variable-existence",
        "script":  "validate_env_variable_existence.py",
        "args":    [],
        "label":   "Env Variable Existence (every env reference must be in .env.example/README; forward-only ratchet)",
        "group":   "Platform",
        "report":  "env_variable_existence_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — interactive elements (button, input) must have an
        # accessible name (aria-label / aria-labelledby / title / associated
        # label / visible text). Accessibility class with forward-only ratchet.
        "id":      "aria-label-coverage",
        "script":  "validate_aria_label_coverage.py",
        "args":    [],
        "label":   "ARIA Label Coverage (every interactive element has an accessible name; forward-only ratchet)",
        "group":   "Platform",
        "report":  "aria_label_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every db.channel(...) must have matching db.removeChannel()
        # OR a beforeunload/pagehide cleanup listener. Without cleanup, navigating
        # between pages leaks websocket channels; free-tier hits its 200-channel
        # ceiling and silently stops delivering events.
        "id":      "realtime-channel-cleanup",
        "script":  "validate_realtime_channel_cleanup.py",
        "args":    [],
        "label":   "Realtime Channel Cleanup (every db.channel() has cleanup; forward-only ratchet)",
        "group":   "Platform",
        "report":  "realtime_channel_cleanup_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every page.locator('#X') in tests/journey-*.spec.ts
        # must point at an id that exists on the target HTML page. Catches
        # tests that flake silently after a page rename.
        "id":      "playwright-selector-existence",
        "script":  "validate_playwright_selector_existence.py",
        "args":    [],
        "label":   "Playwright Selector Existence (every locator('#X') id must exist on target page; forward-only ratchet)",
        "group":   "Platform",
        "report":  "playwright_selector_existence_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — localStorage / sessionStorage keys must be both set
        # AND read somewhere in the codebase. Catches cache-key drift where
        # writer + reader disagree on the key name.
        "id":      "localstorage-key-consistency",
        "script":  "validate_localstorage_key_consistency.py",
        "args":    [],
        "label":   "localStorage Key Consistency (every key must be set AND read; forward-only ratchet)",
        "group":   "Platform",
        "report":  "localstorage_key_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 — every classList.add/remove/toggle('X') must have a
        # matching `.X` CSS rule somewhere (style block or CSS file). Catches
        # JS-only classes where the CSS was deleted but the toggle stayed.
        "id":      "css-class-existence",
        "script":  "validate_css_class_existence.py",
        "args":    [],
        "label":   "CSS Class Existence (every classList.* class must have a CSS rule; forward-only ratchet)",
        "group":   "Platform",
        "report":  "css_class_existence_report.json",
        "skip_if_fast": False,
    },
    {"id":"pg-cron-target","script":"validate_pg_cron_target_existence.py","args":[],
     "label":"pg_cron Target Existence (jobs reference real tables + RPCs; forward-only ratchet)",
     "group":"Platform","report":"pg_cron_target_existence_report.json","skip_if_fast":False},
    {"id":"trigger-function","script":"validate_trigger_function_existence.py","args":[],
     "label":"Trigger Function Existence (CREATE TRIGGER target functions exist; forward-only ratchet)",
     "group":"Platform","report":"trigger_function_existence_report.json","skip_if_fast":False},
    {"id":"meta-description","script":"validate_meta_description_coverage.py","args":[],
     "label":"Meta Description Coverage (every page has description + og:title + og:image + canonical; forward-only ratchet)",
     "group":"Platform","report":"meta_description_coverage_report.json","skip_if_fast":False},
    {"id":"sitemap-page-existence","script":"validate_sitemap_page_existence.py","args":[],
     "label":"Sitemap Page Existence (every sitemap.xml URL resolves to a file; forward-only ratchet)",
     "group":"Platform","report":"sitemap_page_existence_report.json","skip_if_fast":False},
    {"id":"unbounded-query","script":"validate_unbounded_query.py","args":[],
     "label":"Unbounded Query Detection (every .from() chain has .limit/.single/.range/.eq-on-id; forward-only ratchet)",
     "group":"Platform","report":"unbounded_query_report.json","skip_if_fast":False},
    {"id":"heading-hierarchy","script":"validate_heading_hierarchy.py","args":[],
     "label":"Heading Hierarchy (no skipped levels, no multiple h1; forward-only ratchet)",
     "group":"Platform","report":"heading_hierarchy_report.json","skip_if_fast":False},
    {"id":"canonical-url","script":"validate_canonical_url_consistency.py","args":[],
     "label":"Canonical URL Consistency (<link rel=canonical> points at the page; forward-only ratchet)",
     "group":"Platform","report":"canonical_url_consistency_report.json","skip_if_fast":False},
    {"id":"event-listener-cleanup","script":"validate_event_listener_cleanup.py","args":[],
     "label":"Event Listener Cleanup (pages with 10+ addEventListener need removes; forward-only ratchet)",
     "group":"Platform","report":"event_listener_cleanup_report.json","skip_if_fast":False},
    # 2026-05-20 Flywheel 5-turn sweep: 5 new bug-class L0 ratchets.
    {"id":"external-link-rel","script":"validate_external_link_rel.py","args":[],
     "label":"External Link rel=noopener (every <a target=_blank> sets rel=noopener/noreferrer; forward-only ratchet)",
     "group":"Platform","report":"external_link_rel_report.json","skip_if_fast":False},
    {"id":"button-type-in-form","script":"validate_button_type_in_form.py","args":[],
     "label":"Button Type in Form (every <button> inside <form> declares type=button/submit/reset; forward-only ratchet)",
     "group":"Platform","report":"button_type_in_form_report.json","skip_if_fast":False},
    {"id":"security-definer-search-path","script":"validate_security_definer_search_path.py","args":[],
     "label":"SECURITY DEFINER search_path (every definer fn pins search_path; covers ALTER FUNCTION hardening; forward-only ratchet)",
     "group":"Platform","report":"security_definer_search_path_report.json","skip_if_fast":False},
    {"id":"duplicate-script-tags","script":"validate_duplicate_script_tags.py","args":[],
     "label":"Duplicate <script>/<link> Tags (no per-page duplicates of script src or stylesheet href; forward-only ratchet)",
     "group":"Platform","report":"duplicate_script_tags_report.json","skip_if_fast":False},
    {"id":"native-dialog-calls","script":"validate_native_dialog_calls.py","args":[],
     "label":"Native alert/confirm/prompt (production code must use the platform toast/modal stack; forward-only ratchet)",
     "group":"Platform","report":"native_dialog_calls_report.json","skip_if_fast":False},
    # Flywheel turns 6-10 (2026-05-20): 5 more bug-class L0 ratchets.
    {"id":"duplicate-html-id","script":"validate_duplicate_html_id.py","args":[],
     "label":"Duplicate HTML id (every static id is unique per document; forward-only ratchet)",
     "group":"Platform","report":"duplicate_html_id_report.json","skip_if_fast":False},
    {"id":"img-alt-coverage","script":"validate_img_alt_coverage.py","args":[],
     "label":"<img> alt Coverage (every <img> declares an alt attribute; forward-only ratchet)",
     "group":"Platform","report":"img_alt_coverage_report.json","skip_if_fast":False},
    {"id":"json-parse-safety","script":"validate_json_parse_safety.py","args":[],
     "label":"JSON.parse Safety (every JSON.parse() is inside try/catch; forward-only ratchet)",
     "group":"Platform","report":"json_parse_safety_report.json","skip_if_fast":False},
    {"id":"fetch-error-handling","script":"validate_fetch_error_handling.py","args":[],
     "label":"fetch() Error Handling (every fetch() is in try/catch or chained to .catch; forward-only ratchet)",
     "group":"Platform","report":"fetch_error_handling_report.json","skip_if_fast":False},
    {"id":"edge-status-body","script":"validate_edge_status_body_consistency.py","args":[],
     "label":"Edge Status/Body Consistency (HTTP status matches body ok/error semantics; forward-only ratchet)",
     "group":"Platform","report":"edge_status_body_consistency_report.json","skip_if_fast":False},
    # Flywheel turns 11-15 (2026-05-21): 5 more bug-class L0 ratchets.
    {"id":"edge-unpinned-imports","script":"validate_edge_unpinned_imports.py","args":[],
     "label":"Edge Unpinned Imports (every remote import pins @version; supply-chain hardening; forward-only ratchet)",
     "group":"Platform","report":"edge_unpinned_imports_report.json","skip_if_fast":False},
    {"id":"timer-cleanup","script":"validate_timer_cleanup.py","args":[],
     "label":"Timer Cleanup (setInterval has clearInterval; high-count setTimeout has clearTimeout; forward-only ratchet)",
     "group":"Platform","report":"timer_cleanup_report.json","skip_if_fast":False},
    {"id":"css-id-existence","script":"validate_css_id_existence.py","args":[],
     "label":"CSS id Existence (every CSS #id selector matches a declared id; dead-rule guard; forward-only ratchet)",
     "group":"Platform","report":"css_id_existence_report.json","skip_if_fast":False},
    {"id":"add-column-default","script":"validate_add_column_default.py","args":[],
     "label":"ADD COLUMN DEFAULT (every ADD COLUMN NOT NULL has a DEFAULT; backfill safety; forward-only ratchet)",
     "group":"Platform","report":"add_column_default_report.json","skip_if_fast":False},
    {"id":"form-submission-target","script":"validate_form_submission_target.py","args":[],
     "label":"<form> Submission Target (every form has action OR onsubmit OR addEventListener('submit'); forward-only ratchet)",
     "group":"Platform","report":"form_submission_target_report.json","skip_if_fast":False},
    # Flywheel turns 16-20 (2026-05-21): 5 more bug-class L0 ratchets.
    {"id":"edge-options-preflight","script":"validate_edge_options_preflight.py","args":[],
     "label":"Edge OPTIONS Preflight (body-consuming edge fn handles CORS preflight; forward-only ratchet)",
     "group":"Platform","report":"edge_options_preflight_report.json","skip_if_fast":False},
    {"id":"password-input-form","script":"validate_password_input_form.py","args":[],
     "label":"<input type=password> Form Wrapper (password inputs wrapped in <form> for autofill+save; forward-only ratchet)",
     "group":"Platform","report":"password_input_form_report.json","skip_if_fast":False},
    {"id":"fk-on-delete","script":"validate_fk_on_delete.py","args":[],
     "label":"FK ON DELETE (every REFERENCES declares explicit ON DELETE behavior; covers ALTER ADD CONSTRAINT supersede; forward-only ratchet)",
     "group":"Platform","report":"fk_on_delete_report.json","skip_if_fast":False},
    {"id":"edge-body-size-guard","script":"validate_edge_body_size_guard.py","args":[],
     "label":"Edge Body Size Guard (req.json() in try/catch or has Content-Length/sizeLimit check; DoS hardening; forward-only ratchet)",
     "group":"Platform","report":"edge_body_size_guard_report.json","skip_if_fast":False},
    {"id":"select-placeholder","script":"validate_select_placeholder.py","args":[],
     "label":"<select> Placeholder (every <select> has explicit selected/value=''/disabled-placeholder first option; forward-only ratchet)",
     "group":"Platform","report":"select_placeholder_report.json","skip_if_fast":False},
    # Flywheel turns 21-25 (2026-05-21): 5 more bug-class L0 ratchets.
    {"id":"rls-open-policy","script":"validate_rls_open_policy.py","args":[],
     "label":"RLS Open Policy (CREATE POLICY USING(true)/WITH CHECK(true) flagged; covers DROP POLICY supersede; forward-only ratchet)",
     "group":"Platform","report":"rls_open_policy_report.json","skip_if_fast":False},
    {"id":"console-log-drift","script":"validate_console_log_drift.py","args":[],
     "label":"console.log Production Drift (no console.log outside catch/DEBUG-guard in production code; forward-only ratchet)",
     "group":"Platform","report":"console_log_drift_report.json","skip_if_fast":False},
    {"id":"javascript-href","script":"validate_javascript_href.py","args":[],
     "label":"<a href='javascript:'> Anti-Pattern (use <button> for actions; reserve <a href> for navigation; forward-only ratchet)",
     "group":"Platform","report":"javascript_href_report.json","skip_if_fast":False},
    {"id":"view-select-star","script":"validate_view_select_star.py","args":[],
     "label":"CREATE VIEW SELECT * (every view projects explicit columns; preserves canonical-registry coverage; forward-only ratchet)",
     "group":"Platform","report":"view_select_star_report.json","skip_if_fast":False},
    {"id":"meta-refresh","script":"validate_meta_refresh.py","args":[],
     "label":"<meta http-equiv=refresh> (no auto-redirect anti-pattern; use JS or 30x; forward-only ratchet)",
     "group":"Platform","report":"meta_refresh_report.json","skip_if_fast":False},
    # Flywheel turns 26-30 (2026-05-21): 5 more bug-class L0 ratchets.
    {"id":"like-escape","script":"validate_like_escape.py","args":[],
     "label":"SQL LIKE Escape (.ilike/.like templates escape % and _; tracks file-wide escape-helper vars; forward-only ratchet)",
     "group":"Platform","report":"like_escape_report.json","skip_if_fast":False},
    {"id":"icon-button-label","script":"validate_icon_button_label.py","args":[],
     "label":"Icon-Only Button aria-label (svg-only <button> has aria-label/title/sr-only; forward-only ratchet)",
     "group":"Platform","report":"icon_button_label_report.json","skip_if_fast":False},
    {"id":"edge-response-content-type","script":"validate_edge_response_content_type.py","args":[],
     "label":"Edge Response Content-Type (every new Response(JSON.stringify) sets application/json; comment-stripped; forward-only ratchet)",
     "group":"Platform","report":"edge_response_content_type_report.json","skip_if_fast":False},
    {"id":"drop-if-exists","script":"validate_drop_if_exists.py","args":[],
     "label":"DROP IF EXISTS Idempotency (every DROP TABLE/VIEW/FUNCTION/POLICY/INDEX/TRIGGER/TYPE includes IF EXISTS; forward-only ratchet)",
     "group":"Platform","report":"drop_if_exists_report.json","skip_if_fast":False},
    {"id":"table-accessible-name","script":"validate_table_accessible_name.py","args":[],
     "label":"<table> Accessible Name (every table has caption/aria-label/role=presentation; forward-only ratchet)",
     "group":"Platform","report":"table_accessible_name_report.json","skip_if_fast":False},
    {
        # 2026-05-20 — Flywheel orchestrator: one turn per Mega Gate run.
        # Walks L-1 -> L-1.5 -> L0 -> L2 -> L13, diffs against the previous
        # turn's snapshot, surfaces RATCHETS (baselines tightened) +
        # REGRESSIONS (baselines loosened). Reporting-only — exit 0 always.
        "id":      "flywheel-turn",
        "script":  os.path.join("tools", "flywheel_orchestrator.py"),
        "args":    [],
        "label":   "Flywheel Turn (walks every Mega Gate layer; ratchet/regression diff vs prior turn)",
        "group":   "Platform",
        "report":  "flywheel_state.json",
        "skip_if_fast": False,
    },
    {
        "id":      "persona-contract",
        "script":  "validate_persona_contract.py",
        "args":    [],
        "label":   "Persona Contract Validator (8-layer: modules + server + client + gateway + hive + migrations + key parity + Step D differentiation)",
        "group":   "Platform",
        "report":  "persona_contract_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-19 Companion Streamline Step C/D hardening: prevents any
        # production JS file from re-introducing the legacy Cloudflare Worker
        # fetch pattern. Added after voice-handler.js was caught still calling
        # the dead worker, which silently triggered the "Sorry, I'm offline"
        # fallback in Zaniah's (then "Rosa") voice command UI.
        "id":      "legacy-worker-decommission",
        "script":  "validate_legacy_worker_decommission.py",
        "args":    [],
        "label":   "Legacy Worker Decommission Validator (no production JS calls workhive-assistant.workers.dev)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-19 Hardening Loop for the Zaniah-offline incident (formerly "Rosa", f5a8d99):
        # locks in ANON_OK_AGENTS Set + auth-gate skip + persistence guard +
        # AGENT_ROUTES registration so the voice-journal anon path can't
        # silently regress to 401 -> "Sorry, I'm offline" again.
        "id":      "gateway-anon-voice-journal",
        "script":  "validate_gateway_anon_voice_journal.py",
        "args":    [],
        "label":   "ai-gateway Anon Voice-Journal Contract (4-layer: ANON_OK_AGENTS set + auth-gate skip + authUid persistence guard + AGENT_ROUTES entry)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "voice-canonical-anchor",
        "script":  "validate_voice_canonical_anchor.py",
        "args":    [],
        "label":   "Voice Canonical Anchor Validator (4-layer: classifier + fetch + wiring + DATA block in prompt)",
        "group":   "Platform",
        "report":  "voice_canonical_anchor_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-routing-unification",
        "script":  "validate_voice_routing_unification.py",
        "args":    [],
        "label":   "Voice Routing Unification (Phase 0: router output passing)",
        "group":   "Platform",
        "report":  "voice_routing_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-phase1",
        "script":  "validate_voice_companion_phase1.py",
        "args":    [],
        "label":   "Voice Companion Phase 1 (multi-agent orchestrator)",
        "group":   "Platform",
        "report":  "voice_phase1_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-phase1-5",
        "script":  "validate_voice_companion_phase1_5.py",
        "args":    [],
        "label":   "Voice Companion Phase 1.5 (semantic RAG with pgvector)",
        "group":   "Platform",
        "report":  "voice_phase1_5_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-phase2",
        "script":  "validate_voice_companion_phase2.py",
        "args":    [],
        "label":   "Voice Companion Phase 2 (multi-model A/B testing)",
        "group":   "Platform",
        "report":  "voice_phase2_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-phase3",
        "script":  "validate_voice_companion_phase3.py",
        "args":    [],
        "label":   "Voice Companion Phase 3 (error recovery + anon memory)",
        "group":   "Platform",
        "report":  "voice_phase3_report.json",
        "skip_if_fast": False,
    },
    # `companion-page-coverage` and `companion-source-coverage` were registered TWICE here, with a second
    # identical pair further down (same id, same script, same args). Both copies of each ran every suite, so
    # three scripts were executing twice for no added assertion and the registry's own gate count was
    # overstated by three. The duplicates removed are these two, not the later pair, because the later
    # `companion-page-coverage` carries a `report` path this one set to None — deleting the copy that emits
    # the report would have quietly dropped an artifact. See the id-uniqueness assertion in main(), added so
    # this cannot recur silently: nothing was checking that a gate id appears once.
    {
        "id":      "memory-integrity",
        "script":  "validate_memory_integrity.py",
        "args":    [],
        "label":   "Memory Integrity (Phase 2: session memory, turn tracking, dedup)",
        "group":   "Platform",
        "report":  "memory_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "dialog-flow",
        "script":  "validate_dialog_flow.py",
        "args":    [],
        "label":   "Dialog Flow (Phase 4: intent refinement, clarification, slot-filling)",
        "group":   "Platform",
        "report":  "dialog_flow_report.json",
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 Hardening Loop for the "Yes, the details → topic-switch
        # UI" bug class. Locks _isFollowupAffirmation + the call-site bypass
        # + _shouldClarify symmetry guard so short PH/English affirmations
        # ('yes', 'sige', 'oo', 'the details') resume the prior topic
        # instead of tripping the clarification UI. Paired with the L2
        # sentinels in tests/journey-voice-journal.spec.ts.
        "id":      "dialog-affirmation-bypass",
        "script":  "validate_dialog_affirmation_bypass.py",
        "args":    [],
        "label":   "Dialog Affirmation Bypass (5-layer: regex + vocabulary + word-cap + callsite bypass + shouldClarify symmetry)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 Sister hardening loop covering the NEGATIVE side +
        # noise + clarification-loop ceiling. Locks _isFollowupNegation
        # ('no', 'cancel', 'wala', 'hindi'), _isNoisyTranscript (empty /
        # 1-2 char / lone filler), and _clarifyStreak (caps consecutive
        # clarifications at 2 then switches shape + resets). Paired with
        # 3 L2 sentinels in tests/journey-voice-journal.spec.ts.
        "id":      "dialog-followup-handlers",
        "script":  "validate_dialog_followup_handlers.py",
        "args":    [],
        "label":   "Dialog Follow-up Handlers (6-layer: negation + vocabulary + noise + state-clear + upstream + clarify-streak ceiling)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 Flywheel turn #3 of the AI Companion dialog-quality
        # stack. Locks the multi-turn CONTINUITY surface inside
        # _buildVoiceSystemPrompt: PRIOR TOPIC HANDLE block (pronoun
        # resolution — 'it' / 'that' / 'yan' / 'yun' resolved to the
        # prior intent) + natural-language SLOT ENUMERATION ('You already
        # know: asset tag = P-203'). Without these the LLM has no
        # deterministic anchor for short follow-ups and the worker feels
        # the companion 'forgot' them. Paired with L2 sentinels:
        # dialog-prior-topic-handle + dialog-slot-enumeration + the
        # case-invariance probes against the affirmation/negation regex.
        "id":      "dialog-continuity",
        "script":  "validate_dialog_continuity.py",
        "args":    [],
        "label":   "Dialog Continuity (5-layer: prompt builder + DIALOG STATE block + PRIOR TOPIC HANDLE + slot enumeration + PH/English pronoun vocabulary)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 Flywheel turn #4 of the AI Companion dialog-quality
        # stack. Two concerns share the validator:
        #   Phase 4.7 — clarification-recovery routing: bare "logbook" /
        #               "PM" / "analytics" replies after the streak-ceiling
        #               prompt route directly to that intent so the loop
        #               actually breaks.
        #   Phase 4.8 — crisis-line safety override: persona.ts MUST keep
        #               its 'self-harm' + 'helpline' clause positioned in
        #               the conversational reply rules block. Voice
        #               journals are a high-trust surface — this clause
        #               can NEVER be silently optimised away.
        "id":      "dialog-recovery-safety",
        "script":  "validate_dialog_recovery_safety.py",
        "args":    [],
        "label":   "Dialog Recovery + Safety (5-layer: recovery helper + recovery vocabulary + clarification_pending guard + crisis line present + crisis line positioned)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 Flywheel turns #5-#14 (10-turn dialog-quality
        # expansion bundle). Each layer locks one bug class:
        #   T5  persona-switch utterance       T10 first-turn greeting
        #   T6  stale dialog-state guard       T11 code-switch anchor
        #   T7  topic-interruption signal      T12 sensitive-topic redirect
        #   T8  thanks / ack handler           T13 worker-name personalization
        #   T9  asset-context auto-priming     T14 repeat-that handler
        "id":      "dialog-quality-extended",
        "script":  "validate_dialog_quality_extended.py",
        "args":    [],
        "label":   "Dialog Quality Extended (10-layer: turns #5-#14 — persona-switch + stale-guard + topic-interrupt + thanks + asset-prime + greeting + code-switch + sensitive-topic + worker-name + repeat)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-20 Flywheel turns #15-#24 — different DIMENSION from
        # the dialog-state stack. TRUST (hallucination, citation),
        # AUDIO QUALITY (interrupt, acronym pronunciation),
        # OBSERVABILITY (TTS latency, rate-limit, cost-cap, fallback
        # UX), CROSS-SURFACE COHERENCE (assistant.html pulls voice-
        # journal entries), and lifecycle (conversation-end ack).
        "id":      "ai-companion-trust-observability",
        "script":  "validate_ai_companion_trust_observability.py",
        "args":    [],
        "label":   "AI Companion Trust + Observability (10-layer: turns #15-#24 — hallucination guard + citation + audio interrupt + TTS latency + rate-limit + fallback UX + acronym SSML + assistant journal pull + cost-cap + end ack)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21 Flywheel turns #25-#34 — CONTEXT AWARENESS +
        # INTELLIGENCE layer. Shift, repeated-issue, standards lookup,
        # voice shortcuts, quality thumbs, worker discipline, goodbye,
        # confidence calibration, long-session pacing, alerts override.
        "id":      "ai-companion-intelligence",
        "script":  "validate_ai_companion_intelligence.py",
        "args":    [],
        "label":   "AI Companion Intelligence (10-layer: turns #25-#34 — shift + repeated-issue + standards + shortcuts + thumbs + discipline + goodbye + confidence + pacing + alerts override)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21 Flywheel turns #35-#44 — COLLABORATION + WELLBEING.
        # Action confirmation, wellbeing nudge, encouragement, skill gap,
        # shift handover, batch action, explainability, co-worker
        # mention, fatigue signal, transcript export.
        "id":      "ai-companion-collaboration",
        "script":  "validate_ai_companion_collaboration.py",
        "args":    [],
        "label":   "AI Companion Collaboration + Wellbeing (10-layer: turns #35-#44 — action confirm + wellbeing + encouragement + skill gap + handover + batch + explainability + co-worker mention + fatigue + transcript export)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21 Flywheel turns #45-#54 — RESILIENCE + MEMORY +
        # TRUST OPS. Offline tracker, 10-min reply cache, feedback
        # escalation, custom plant terminology resolver, conversation
        # branching stack, multi-modal photo intent, avatar emotion
        # state, cross-hive anonymised benchmark, summary-on-demand,
        # identity drift tracker.
        "id":      "ai-companion-resilience",
        "script":  "validate_ai_companion_resilience.py",
        "args":    [],
        "label":   "AI Companion Resilience + Memory (10-layer: turns #45-#54 — offline + cache + escalation + terminology + branching + photo intent + avatar state + benchmark + summary + identity drift)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21 Flywheel turns #55-#64 — WORKFLOW + PERSONALIZATION.
        # Proactive companion turn (open accepts alert payload), maturity-
        # stair gating, per-slot expiry windows, action replay, language
        # opt-in, brevity preference, timer follow-up, URL-context pre-
        # fill, mic quality meter, multi-step action queue.
        "id":      "ai-companion-workflow",
        "script":  "validate_ai_companion_workflow.py",
        "args":    [],
        "label":   "AI Companion Workflow + Personalization (10-layer: turns #55-#64 — proactive + maturity + slot expiry + action replay + language + brevity + timer + URL prefill + mic quality + action queue)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion seventh 10-turn flywheel batch
        # (turns #65-#74). ORCHESTRATION + INTEGRATION layer: PDF
        # export detector → Report Sender, per-device pronunciation
        # library, voice-execute safety lock (default OFF), persona
        # portrait animation, cross-hive benchmark RPC wiring, daily
        # digest mode, push notification readiness, multi-worker
        # concurrency lock, accent / voice-signature adaptation,
        # streaming SSE response indicator.
        "id":      "ai-companion-orchestration",
        "script":  "validate_ai_companion_orchestration.py",
        "args":    [],
        "label":   "AI Companion Orchestration + Integration (10-layer: turns #65-#74 — pdf export + pronunciation + voice-execute lock + avatar animation + cross-hive RPC + digest + push + session lock + accent + streaming)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion eighth 10-turn flywheel batch
        # (turns #75-#84). TRUST DEPLOYMENT layer — production
        # safety + collaboration: toxicity guard, question shape
        # classifier, freshness disclosure, rate-limit cooldown,
        # conversation share link, readback request, scope
        # disclosure, multi-turn correction, confidence label
        # tier, crisis escalation extension (self-harm + workplace
        # violence).
        "id":      "ai-companion-trust-deployment",
        "script":  "validate_ai_companion_trust_deployment.py",
        "args":    [],
        "label":   "AI Companion Trust Deployment (10-layer: turns #75-#84 — toxicity + question shape + freshness + rate-limit + share + readback + scope + correction + confidence label + crisis extension)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion ninth 10-turn flywheel batch
        # (turns #85-#94). INPUT NORMALIZATION + ONBOARDING layer.
        "id":      "ai-companion-input-normalization",
        "script":  "validate_ai_companion_input_normalization.py",
        "args":    [],
        "label":   "AI Companion Input Normalization + Onboarding (10-layer: turns #85-#94 — precision rule + asset-tag normalization + time-range + ack style + forbidden topics + mic env + pin + help + KPI translation + new-worker welcome)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion tenth 10-turn flywheel batch
        # (turns #95-#104). INTEGRATION + AUDIT layer.
        "id":      "ai-companion-integration-audit",
        "script":  "validate_ai_companion_integration_audit.py",
        "args":    [],
        "label":   "AI Companion Integration + Audit (10-layer: turns #95-#104 — audit log + quiet hours + preflight + idle cleanup + error analytics + session tag + deep link + grammar guess + phrase pool + shift-end)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion eleventh 10-turn flywheel batch
        # (turns #105-#114). PROACTIVE ASSISTANCE + LEARNING layer.
        "id":      "ai-companion-learning",
        "script":  "validate_ai_companion_learning.py",
        "args":    [],
        "label":   "AI Companion Proactive Assistance + Learning (10-layer: turns #105-#114 — PM sync drift + skill-level adaptation + cross-asset pattern + intent history + sentiment over time + asset warm-up + symptom normalizer + shift boundary + knowledge gap + mentor handoff)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion twelfth 10-turn flywheel batch
        # (turns #115-#124). COMPLIANCE + DATA GOVERNANCE.
        "id":      "ai-companion-compliance",
        "script":  "validate_ai_companion_compliance.py",
        "args":    [],
        "label":   "AI Companion Compliance + Data Governance (10-layer: turns #115-#124 — PII scrubber + consent capture + retention + right-to-erasure + audit CSV + suspicious activity + AI disclosure + locale-aware dates + monthly cost cap + voice drift advisory)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion thirteenth 10-turn flywheel batch
        # (turns #125-#134). MULTI-MODAL + ACCESSIBILITY.
        "id":      "ai-companion-accessibility",
        "script":  "validate_ai_companion_accessibility.py",
        "args":    [],
        "label":   "AI Companion Multi-Modal + Accessibility (10-layer: turns #125-#134 — camera capture + file attachment + reduced motion + aria-live + keyboard nav + CB-safe palette + large text + haptic + voice-only mode + live captions)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion fourteenth 10-turn flywheel batch
        # (turns #135-#144). OPERATIONAL EXCELLENCE.
        "id":      "ai-companion-operational",
        "script":  "validate_ai_companion_operational.py",
        "args":    [],
        "label":   "AI Companion Operational Excellence (10-layer: turns #135-#144 — health ping + self-test + feature flags + browser support + network adapt + memory pressure + clock drift + background pause + crash recovery + presence heartbeat)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion fifteenth 10-turn flywheel batch
        # (turns #145-#154). TEAM COORDINATION + CROSS-WORKER.
        "id":      "ai-companion-team-coordination",
        "script":  "validate_ai_companion_team_coordination.py",
        "args":    [],
        "label":   "AI Companion Team Coordination (10-layer: turns #145-#154 — active sessions + handoff + shared notes + concurrency alert + watchlist + broadcast + resolution + cross-shift continuity + buddy mode + mention notifications)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion sixteenth 10-turn flywheel batch
        # (turns #155-#164). EXTERNAL INTEGRATION.
        "id":      "ai-companion-external-integration",
        "script":  "validate_ai_companion_external_integration.py",
        "args":    [],
        "label":   "AI Companion External Integration (10-layer: turns #155-#164 — SAP PM webhook + Maximo poll + OPC-UA tag + MQTT topic + Slack + email digest + Teams card + ICS calendar + signature compare + outbound retry queue)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion seventeenth 10-turn flywheel batch
        # (turns #165-#174). ADVANCED ANALYTICS.
        "id":      "ai-companion-analytics",
        "script":  "validate_ai_companion_analytics.py",
        "args":    [],
        "label":   "AI Companion Advanced Analytics (10-layer: turns #165-#174 — 3σ anomaly + Weibull MTBF + Pareto + linear trend + seasonal peak + trimmed mean + z-score + correlation + Weibull CDF + availability)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion eighteenth 10-turn flywheel batch
        # (turns #175-#184). SAFETY + PERMIT-TO-WORK.
        "id":      "ai-companion-safety",
        "script":  "validate_ai_companion_safety.py",
        "args":    [],
        "label":   "AI Companion Safety + Permit-to-Work (10-layer: turns #175-#184 — LOTO + hot work + confined space + PPE matrix + near-miss + JSA + gas test + incident + energy isolation + permit expiry)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 1 of AGENTIC_RAG_ROADMAP.md.
        # 5-stage self-correcting agentic RAG loop (Router → Retriever → Grader →
        # Generator → Checker), free-tier multi-provider chain only, per-stage
        # cost-logged, trace-persisted to agentic_rag_traces.
        "id":      "agentic-rag-loop",
        "script":  "validate_agentic_rag_loop.py",
        "args":    [],
        "label":   "Agentic RAG Loop Phase 1 (18-layer: edge fn + 5 stages + hive scoping + FREE-TIER-ONLY + callAI + rate limit + retry cap + grader threshold + question cap + trace + cost log + migration + 4-place sync + em-dash safety + JSON mode)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 2 of AGENTIC_RAG_ROADMAP.md.
        # Hierarchical period summaries (Daily → Weekly → Monthly → Quarterly →
        # Yearly) pre-digested in canonical_period_summaries. Agentic RAG
        # Retriever reads these instead of raw logbook for time-bound queries.
        "id":      "hierarchical-summaries",
        "script":  "validate_hierarchical_summaries.py",
        "args":    [],
        "label":   "Hierarchical Period Summaries Phase 2 (16-layer: migration + 5 levels + RLS + aggregator + Breakdown/Corrective filter + FREE-TIER-ONLY + callAI + hive scoping + row cap + empty short-circuit + upsert + 4-place sync + em-dash safety + cost log)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 4 of AGENTIC_RAG_ROADMAP.md.
        # Tiered Model Router — adds taskProfile parameter to callAI; reorders
        # the free-tier multi-provider chain per task. Drops TPM pressure ~40%
        # by routing cheap tasks (grader, checker, intent) to the 8B model
        # instead of 17B Scout. All 11 expected task profiles covered.
        "id":      "model-router",
        "script":  "validate_model_router.py",
        "args":    [],
        "label":   "Tiered Model Router Phase 4 (9-layer: TASK_PROFILES + 11 profiles + free-tier values + reorderChain + callAI signature + reorderChain usage + Phase 1 stages wired + Phase 2 digest wired + no paid models)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 8 of AGENTIC_RAG_ROADMAP.md.
        # Supervisor-facing observability dashboard reading agentic_rag_traces.
        "id":      "agentic-rag-observability",
        "script":  "validate_agentic_rag_observability.py",
        "args":    [],
        "label":   "Agentic RAG Observability Phase 8 (10-layer: page exists + calm-dashboard meta + utils + hive gate + hive-scoped query + narrow select + 4 render fns + escHtml + bounded fetch + window filter)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 3 of AGENTIC_RAG_ROADMAP.md.
        # Supervisor-worker temporal RAG orchestrator: decomposes time-bound
        # questions across N parallel sub-agents on canonical_period_summaries,
        # then folds. Bounded concurrency keeps Groq TPM contention safe.
        "id":      "temporal-orchestrator",
        "script":  "validate_temporal_orchestrator.py",
        "args":    [],
        "label":   "Temporal RAG Orchestrator Phase 3 (17-layer: edge fn + decompose + 3 granularities + auto-heuristic + MAX_PERIODS + MAX_PARALLEL + runBounded + reads Phase 2 + 2x callAI + sub/fold taskProfiles + FREE-TIER + hive scoping + rate limit before fan-out + 4-place sync + cost log + em-dash safety)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 7 of AGENTIC_RAG_ROADMAP.md.
        # agent_episodic_memory + agent-memory-store edge fn. Durable facts
        # (factual/procedural/episodic/semantic) extracted by Phase 1 Checker
        # at run end, recalled at run start. LRU eviction with importance
        # weighting. Distinct from agent_memory (which is conversation-turn).
        "id":      "agent-episodic-memory",
        "script":  "validate_agent_memory_store.py",
        "args":    [],
        "label":   "Agent Episodic Memory Phase 7 (12-layer: migration + 4 types + RLS + edge fn + recall+store ops + caps + content cap + importance×log rank + batch cap + hive scoping + 4-place sync + no raw fetch)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 5 of AGENTIC_RAG_ROADMAP.md (scaffolding).
        # unified_events + data-fabric-normalizer edge fn. Canonical event
        # schema that normalizes SAP/Maximo/OPC-UA/MQTT/CMMS/voice/photo/
        # sensor/email/manual sources into one shape for cross-source RAG.
        "id":      "data-fabric",
        "script":  "validate_data_fabric.py",
        "args":    [],
        "label":   "Data Fabric Normalizer Phase 5 (9-layer scaffolding: migration + 10 sources + RLS + edge fn + 3 adapters + SHA-256 dedup + hive scoping + duplicate handling + no LLM + 4-place sync)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: Phase 6 of AGENTIC_RAG_ROADMAP.md.
        # 2026-05-31 (memory-stack Turn 3): cold-archive-query is now WIRED -
        # reads per-hive Parquet snapshots in-process via hyparquet, returns 200
        # ok:true on read paths (the 503 scaffold is gone). Paired exporter is
        # tools/cold_archive_exporter.py. See COLD_ARCHIVE_SCALEUP_ROADMAP.md.
        "id":      "cold-archive",
        "script":  "validate_cold_archive.py",
        "args":    [],
        "label":   "Cold Lakehouse Archive Phase 6 (10-layer contract: edge fn + 4 supported tables + 200 ok:true hyparquet read + storage list + hive scoping + Python exporter + --commit dry-run default + ARCHIVE_AGE_MONTHS=18 + no auto-delete safety + 4-place sync)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-31 (memory-stack Turn 3): asserts the cold-tier read path is
        # genuinely wired with hyparquet (not a stub) - _shared/cold-archive.ts
        # helpers + parquetReadObjects + storage.download + TABLE_FILE map +
        # MAX_QUARTERS/LIMIT_CAP bounds + ok:true contract. Must pass at 0.
        # Sibling to episodic-memory-wiring / verified-state-wiring.
        "id":      "cold-archive-wiring",
        "script":  "validate_cold_archive_wiring.py",
        "args":    [],
        "label":   "Cold Archive Wiring (Hierarchical layer: hyparquet Parquet read stays wired into cold-archive-query - _shared helpers + parquetReadObjects + bounds + ok:true)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-31 (memory-stack Turn 4): the Semantic layer (layer 03) per-hive
        # entity extractor is genuinely wired - reads v_logbook_truth, extracts
        # typed S-P-O triples via the free-tier chain, embeds them, and idempotently
        # upserts into knowledge_graph_facts (source_type 'ai_extraction') against
        # uq_kgf_triple_source. Fills the store voice-handler._fetchKGContext reads.
        # Must pass at 0. Sibling to episodic/verified-state/cold-archive wiring.
        "id":      "semantic-fact-extractor-wiring",
        "script":  "validate_semantic_fact_extractor_wiring.py",
        "args":    [],
        "label":   "Semantic Fact Extractor Wiring (Semantic layer: logbook -> KG triples -> embed -> idempotent upsert into knowledge_graph_facts; _shared/semantic-facts.ts helpers + dedupe migration + 4-place sync)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-31 (memory-stack Turn 5): the Procedural layer (layer 04) skill
        # library + matcher is wired end to end - persistEpisodic embeds procedural
        # memories, match_procedural_memories (RPC + idx_aem_embedding) retrieves
        # them by cosine, _shared/skill-library.ts wraps it, and ai-gateway injects
        # the top proven procedures for fix-oriented agents. Must pass at 0. Sibling
        # to episodic/verified-state/cold-archive/semantic-fact wiring.
        "id":      "skill-library-wiring",
        "script":  "validate_skill_library_wiring.py",
        "args":    [],
        "label":   "Skill Library Wiring (Procedural layer: embed procedural memories + match_procedural_memories cosine RPC + _shared/skill-library.ts matcher + ai-gateway injection for fix agents)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-31 (memory-stack Turn 6): the Prospective layer (layer 06)
        # deferred follow-up queue is wired end to end - agent_followups store
        # (non-open RLS), _shared/followups.ts enqueues (capped) + recalls only
        # DUE items (marking them surfaced), and ai-gateway both surfaces due
        # follow-ups into context and enqueues new ones from the specialist
        # envelope. Must pass at 0. Final sibling in the memory-stack wiring set.
        "id":      "followup-queue-wiring",
        "script":  "validate_followup_queue_wiring.py",
        "args":    [],
        "label":   "Follow-up Queue Wiring (Prospective layer: agent_followups store + _shared/followups.ts enqueue/recall-due/surface + ai-gateway surfacing + envelope-driven enqueue for task agents)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: RAG Flywheel processor + multi-turn loop orchestrator.
        # tools/rag_flywheel_processor.py + run_rag_flywheel_loop.py drive
        # synthetic walks; this validator ratchets the contract surface
        # (canonical tile tags, KPI seeds, walk template branches, lane D).
        "id":      "rag-flywheel",
        "script":  "validate_rag_flywheel.py",
        "args":    [],
        "label":   "RAG Flywheel (processor + loop orchestrator + canonical tile tags + KPI seeds + walk template branches + lane D retriever)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-22: RAG Flywheel hard locks (immutable contracts that the
        # loop must never regress: 5s inter-tile throttle, domain prefix on
        # rag_tile inserts, rule-9 view-name self-check, positive-framing
        # seeds, live-query cold_archive demotion).
        "id":      "rag-flywheel-locks",
        "script":  "validate_rag_flywheel_locks.py",
        "args":    [],
        "label":   "RAG Flywheel Locks (inter-tile throttle + domain prefix + view-name self-check + positive-framing seeds + cold_archive demotion)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion nineteenth 10-turn flywheel batch
        # (turns #185-#194). KNOWLEDGE GRAPH.
        "id":      "ai-companion-knowledge-graph",
        "script":  "validate_ai_companion_knowledge_graph.py",
        "args":    [],
        "label":   "AI Companion Knowledge Graph (10-layer: turns #185-#194 — entity + relation + triple + RAG block + FNV hash + chunking + citation + query rewrite + reasoning trace + KB version)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion twentieth 10-turn flywheel batch
        # (turns #195-#204). ENERGY + SUSTAINABILITY.
        "id":      "ai-companion-sustainability",
        "script":  "validate_ai_companion_sustainability.py",
        "args":    [],
        "label":   "AI Companion Energy + Sustainability (10-layer: turns #195-#204 — EnPI + PH carbon factor + peak demand + 5σ energy anomaly + standby waste + water + air leak + motor efficiency + sustainability bundle + energy query)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # 2026-05-21: AI Companion twenty-first 10-turn flywheel batch
        # (turns #205-#214). MULTI-LANGUAGE NLU.
        "id":      "ai-companion-multilang",
        "script":  "validate_ai_companion_multilang.py",
        "args":    [],
        "label":   "AI Companion Multi-Language NLU (10-layer: turns #205-#214 — Cebuano + Ilonggo + Tagalog imperative + code-switch ratio + politeness register + PH time phrases + number words + filler strip + stop words + slang dict)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "proactive-alerts",
        "script":  "validate_proactive_alerts.py",
        "args":    [],
        "label":   "Proactive Alerts (Phase 5: KPI spikes, risk escalation, overdue PM)",
        "group":   "Platform",
        "report":  "proactive_alerts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-alert-formatting",
        "script":  "validate_voice_alert_formatting.py",
        "args":    [],
        "label":   "Voice Alert Formatting (Phase 5: alerts render with descriptions, not IDs)",
        "group":   "Platform",
        "report":  "voice_alert_formatting_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "offline-resilience",
        "script":  "validate_offline_resilience_phase6.py",
        "args":    [],
        "label":   "Offline Resilience (Phase 6: snapshot caching, response queue)",
        "group":   "Platform",
        "report":  "offline_resilience_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "tts-quality",
        "script":  "validate_tts_quality_phase7.py",
        "args":    [],
        "label":   "TTS Quality Metrics (Phase 7: latency logging, cache)",
        "group":   "Platform",
        "report":  "tts_quality_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "avatar-state",
        "script":  "validate_avatar_state_phase10.py",
        "args":    [],
        "label":   "Avatar State Management (Phase 10: emotion tracking, animations)",
        "group":   "Platform",
        "report":  "avatar_state_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "multilingual-support",
        "script":  "validate_multilingual_phase11.py",
        "args":    [],
        "label":   "Multilingual Support (Phase 11: term translation, language prefs)",
        "group":   "Platform",
        "report":  "multilingual_support_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "voice-data-flow",
        "script":  "validate_voice_data_flow.py",
        "args":    [],
        "label":   "Voice Data Flow Audit (Phase 3/5/8: KB RAG, proactive alerts, analytics)",
        "group":   "Platform",
        "report":  "voice_data_flow_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "rag-integrity",
        "script":  "validate_rag_integrity.py",
        "args":    [],
        "label":   "RAG Integrity (Phase 1.5: semantic search, KB chunks, embeddings)",
        "group":   "Platform",
        "report":  "rag_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "analytics-integrity",
        "script":  "validate_analytics_integrity.py",
        "args":    [],
        "label":   "Analytics Integrity (Phase 8: conversation quality metrics, health view)",
        "group":   "Platform",
        "report":  "analytics_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "team-coordination",
        "script":  "validate_team_coordination.py",
        "args":    [],
        "label":   "Team Coordination (Phase 9: cross-hive alerts, best practices sharing)",
        "group":   "Platform",
        "report":  "team_coordination_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "tier-c-contracts",
        "script":  "validate_tier_c_contracts.py",
        "args":    [],
        "label":   "Tier C Contract Regression Validator (good/bad fixtures per agent contract)",
        "group":   "Platform",
        "report":  "tier_c_contracts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "capture-contracts",
        "script":  "validate_capture_contracts.py",
        "args":    [],
        "label":   "Tier F Capture Contract Regression Validator (good/bad payload fixtures per input surface)",
        "group":   "Platform",
        "report":  "capture_contracts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "formula-invocation",
        "script":  "validate_formula_invocation.py",
        "args":    [],
        "label":   "Formula Invocation Drift (Tier D-f refinement: same formula called with different period_days across consumers)",
        "group":   "Platform",
        "report":  "formula_invocation_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "capability-dedup",
        "script":  "validate_capability_dedup.py",
        "args":    [],
        "label":   "Tier G / Layer 9 Capability Catalog & Dedup (every user-facing function pinned to one primary surface)",
        "group":   "Platform",
        "report":  "capability_dedup_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "playwright-coverage",
        "script":  "validate_playwright_coverage.py",
        "args":    [],
        "label":   "Playwright Coverage (every LIVE_TOOL_PAGE has tests/<page>.spec.ts with a real goto)",
        "group":   "Platform",
        "report":  "playwright_coverage_report.json",
        "skip_if_fast": False,  # static check, ~50ms
    },
    {
        "id":      "seed-consumer-contract",
        "script":  "validate_seed_consumer_contract.py",
        "args":    [],
        "label":   "Seed -> Consumer Contract (TZ-aware date columns + JSONB key contract for AMC-like blobs)",
        "group":   "Platform",
        "report":  "seed_consumer_contract_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ux-contract",
        "script":  "validate_ux_contract.py",
        "args":    [],
        "label":   "WorkHive UX Contract (input labels [ratchet] + destructive confirm + page title + role-gate)",
        "group":   "Platform",
        "report":  "ux_contract_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "supabase-singleton",
        "script":  "validate_supabase_singleton.py",
        "args":    [],
        "label":   "Supabase Client Singleton (at-most-one createClient per page; shared JS uses singleton)",
        "group":   "Platform",
        "report":  "supabase_singleton_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "playwright-smoke",
        "script":  "validate_playwright_smoke.py",
        "args":    [],
        "label":   "Playwright UI Smoke Suite (real browser, silent-failure regression locks per page)",
        "group":   "Platform",
        "report":  "playwright_smoke_report.json",
        "skip_if_fast": True,   # ~3 min runtime; opt-in via full guardian
    },
    {
        "id":      "write-path-monitor",
        "script":  "validate_write_path_monitor.py",
        "args":    [],
        "label":   "Write Path Monitor (4-layer: shape drift + orphan RPCs + write hotspots + single-layer writers)",
        "group":   "Platform",
        "report":  "write_path_monitor_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "schema-phantom",
        "script":  "validate_schema_phantom.py",
        "args":    [],
        "label":   "Schema Phantom Column Detector (4-layer: phantom reads + dead columns + alias drift + layer hotspots)",
        "group":   "Platform",
        "report":  "schema_phantom_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "embed-integrity",
        "script":  "validate_embed_integrity.py",
        "args":    [],
        "label":   "PostgREST Embed Integrity (4-layer: phantom target + phantom embed column + missing FK + embed distribution)",
        "group":   "Platform",
        "report":  "embed_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "kg-scope-split",
        "script":  "validate_kg_scope_split.py",
        "args":    [],
        "label":   "KG Facts Scope Split (4-layer: platform table + RPC + voice-handler fan-out + no broadcast pattern)",
        "group":   "Platform",
        "report":  "kg_scope_split_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "service-role-exposure",
        "script":  "validate_service_role_exposure.py",
        "args":    [],
        "label":   "Service-Role Key Exposure (5-layer: service_role identifier + JWT in client + secret env + anon-key inventory + JWT in the live DB catalog)",
        "group":   "Platform",
        "report":  "service_role_exposure_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "migration-immutability",
        "script":  "validate_migration_immutability.py",
        "args":    [],
        "label":   "Migration Immutability (4-layer: edited-after-first-commit + filename convention + whitespace-only + recency)",
        "group":   "Platform",
        "report":  "migration_immutability_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "rls-symmetry",
        "script":  "validate_rls_symmetry.py",
        "args":    [],
        "label":   "RLS Policy Symmetry (4-layer: write-without-read + read-without-create + update gap + CRUD matrix)",
        "group":   "Platform",
        "report":  "rls_symmetry_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "optimistic-concurrency",
        "script":  "validate_optimistic_concurrency.py",
        "args":    [],
        "label":   "Optimistic Concurrency (4-layer: content-without-guard + no-defence-available + writer matrix + adoption count)",
        "group":   "Platform",
        "report":  "optimistic_concurrency_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "pii-egress",
        "script":  "validate_pii_egress.py",
        "args":    [],
        "label":   "PII Egress to Third Parties (4-layer: direct-fetch+PII + AI-prompt+PII + host distribution + PII reach)",
        "group":   "Platform",
        "report":  "pii_egress_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "index-coverage",
        "script":  "validate_index_coverage.py",
        "args":    [],
        "label":   "Index Coverage (4-layer: high-freq unindexed + med-freq unindexed + coverage matrix + tables-with-only-PK)",
        "group":   "Platform",
        "report":  "index_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cors-wildcard",
        "script":  "validate_cors_wildcard.py",
        "args":    [],
        "label":   "CORS Wildcard Audit (4-layer: hardcoded-* + wildcard-on-data + strategy distribution + echo-without-allowlist)",
        "group":   "Platform",
        "report":  "cors_wildcard_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "abort-timeout",
        "script":  "validate_abort_timeout.py",
        "args":    [],
        "label":   "AbortSignal Timeout Coverage (4-layer: external-no-signal + loop-no-timeout + timeout distribution + no-fetch fns)",
        "group":   "Platform",
        "report":  "abort_timeout_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cold-start-memoization",
        "script":  "validate_cold_start_memoization.py",
        "args":    [],
        "label":   "Cold-Start Memoization (4-layer: createClient-in-handler + multiple-calls + adoption + budget)",
        "group":   "Platform",
        "report":  "cold_start_memoization_report.json",
        "skip_if_fast": False,
    },
    {
        # Layer -1 Convention Mining -- discovers emergent edge-fn patterns
        # and flags outliers. Always exits 0 (informational). Promotion
        # candidates are surfaced in edge_pattern_mining_report.md for
        # human review; real rules then graduate to their own validators.
        "id":      "edge-pattern-mining",
        "script":  os.path.join("tools", "mine_edge_patterns.py"),
        "args":    [],
        "label":   "Edge-Fn Pattern Miner (L-1 Convention Mining -- informational, surfaces drift)",
        "group":   "Platform",
        "report":  "edge_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # Layer -1 Convention Mining -- HTML page cluster. Companion to the
        # edge-fn miner; proves L-1 generalises across heterogeneous file
        # types. Static-HTML scan only: cannot see runtime-injected scripts.
        "id":      "html-pattern-mining",
        "script":  os.path.join("tools", "mine_html_patterns.py"),
        "args":    [],
        "label":   "HTML Page Pattern Miner (L-1 Convention Mining -- informational, surfaces drift)",
        "group":   "Platform",
        "report":  "html_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1 cluster A: JS shared modules (utils, nav-hub, wh-*, etc.).
        # Tighter threshold (>=85% / <=3 outliers) because the cluster is
        # small (24 files).
        "id":      "js-module-pattern-mining",
        "script":  os.path.join("tools", "mine_js_module_patterns.py"),
        "args":    [],
        "label":   "JS Shared Module Pattern Miner (L-1 Convention Mining -- informational)",
        "group":   "Platform",
        "report":  "js_module_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1 cluster B: SQL migrations. Largest cluster (~145 files).
        # Lesson #14 applied: SQL-specific comment-strip (-- + /* */).
        "id":      "migration-pattern-mining",
        "script":  os.path.join("tools", "mine_migration_patterns.py"),
        "args":    [],
        "label":   "SQL Migration Pattern Miner (L-1 Convention Mining -- informational)",
        "group":   "Platform",
        "report":  "migration_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1 cluster C: Python tools (tools/*.py).
        "id":      "python-tool-pattern-mining",
        "script":  os.path.join("tools", "mine_python_tool_patterns.py"),
        "args":    [],
        "label":   "Python Tool Pattern Miner (L-1 Convention Mining -- informational)",
        "group":   "Platform",
        "report":  "python_tool_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1 cluster D: Validators themselves (meta). Homogeneous cluster
        # -> threshold raised to >=90%. Most useful for surfacing validators
        # that diverge from the established skeleton (missing cp1252 guard,
        # missing format_result, missing main guard).
        "id":      "validator-pattern-mining",
        "script":  os.path.join("tools", "mine_validator_patterns.py"),
        "args":    [],
        "label":   "Validator Pattern Miner [META] (L-1 Convention Mining -- informational)",
        "group":   "Platform",
        "report":  "validator_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1 cluster E: Seeders (test-data-seeder/seeders/*.py).
        "id":      "seeder-pattern-mining",
        "script":  os.path.join("tools", "mine_seeder_patterns.py"),
        "args":    [],
        "label":   "Seeder Pattern Miner (L-1 Convention Mining -- informational)",
        "group":   "Platform",
        "report":  "seeder_pattern_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1.5 Skill-Rule Mining -- documented rules from SKILL.md files
        # enforced via skill_rules_manifest.json. Surfaces violations of
        # rules the user has written but never had automatically checked.
        # First run on 2026-05-18 found 8 critical + 6 high-severity
        # violations across 23 rules from 5 skills.
        "id":      "skill-rule-mining",
        "script":  os.path.join("tools", "mine_skill_rules.py"),
        "args":    [],
        "label":   "Skill-Rule Miner [L-1.5] (documented rules from SKILL.md -- informational)",
        "group":   "Platform",
        "report":  "skill_rules_mining_report.json",
        "skip_if_fast": True,
    },
    {
        # L-1 Foundation: Canonical Source Registry. Walks migrations +
        # HTML surfaces + edge fns to build an inventory of every table,
        # RPC, view, and who reads/writes each. Output is the
        # "what's already on the platform" reference -- consulted before
        # proposing any new surface/column/RPC. Also surfaces duplicate
        # signals: surface-pair table-overlap, near-duplicate columns,
        # dead tables, phantom tables. Informational; never blocks.
        "id":      "canonical-registry",
        "script":  os.path.join("tools", "mine_canonical_registry.py"),
        "args":    [],
        "label":   "Canonical Source Registry [L-1 Foundation] (tables/RPCs/views/surfaces inventory + duplicate signals)",
        "group":   "Platform",
        "report":  "canonical_registry.json",
        "skip_if_fast": True,
    },
    {
        # L-1 Layer 2: Canonical Overlap Validator. Reads the registry +
        # an allowlist (canonical_overlap_allowlist.json) and FAILs the
        # gate on (a) phantom tables -- refs in code, no migration def --
        # and (b) NEW surface-pair overlaps not in the allowlist. New
        # duplicate-creation gets blocked; legit role-views must be
        # explicitly documented. Adding to the allowlist is the
        # "this is intentional" switch.
        "id":      "canonical-overlap",
        "script":  "validate_canonical_overlap.py",
        "args":    [],
        "label":   "Canonical Overlap (L-1 Layer 2 -- blocks phantom tables + undocumented surface overlaps)",
        "group":   "Platform",
        "report":  "canonical_overlap_report.json",
        "skip_if_fast": False,
    },
    {
        # First validator graduated from L-1 Convention Mining (86%
        # conformance candidate from mine_html_patterns.py, now an
        # enforced Layer 0 rule). escHtml() XSS defence + Supabase
        # singleton both live in utils.js -- pages that skip it are
        # either pure brochure (allowlisted) or accidental XSS holes.
        "id":      "loads-utils-js",
        "script":  "validate_loads_utils_js.py",
        "args":    [],
        "label":   "Loads-Utils-JS (3-layer: required + allowlist-freshness + census)",
        "group":   "Platform",
        "report":  "loads_utils_js_report.json",
        "skip_if_fast": False,
    },
    {
        # Second validator graduated from L-1 Convention Mining
        # (mine_validator_patterns.py surfaced 33 outliers at 82%
        # conformance; all patched on 2026-05-18, now 100%). Locks the
        # rule so a new validator authored without the cp1252 stdout
        # guard fails Mega Gate immediately rather than crashing on
        # Windows when its output hits a Unicode character.
        "id":      "validator-cp1252-guard",
        "script":  "validate_validator_cp1252_guard.py",
        "args":    [],
        "label":   "Validator cp1252-Guard (3-layer: required + allowlist + placement)",
        "group":   "Platform",
        "report":  "validator_cp1252_guard_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cascade-behavior",
        "script":  "validate_cascade_behavior.py",
        "args":    [],
        "label":   "Cascade Behavior (4-layer: no-on-delete-clause + explicit-no-action + distribution + orphan-risk)",
        "group":   "Platform",
        "report":  "cascade_behavior_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "hardcoded-secrets",
        "script":  "validate_hardcoded_secrets.py",
        "args":    [],
        "label":   "Hardcoded Secret Detector (4-layer: provider tokens + generic assignments + provider distribution + allowlist inventory)",
        "group":   "Platform",
        "report":  "hardcoded_secrets_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "loading-state",
        "script":  "validate_loading_state.py",
        "args":    [],
        "label":   "Loading State Coverage (4-layer: async-no-loading + submit-no-preventDefault + mechanism distribution + async density)",
        "group":   "Platform",
        "report":  "loading_state_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "jsonb-drift",
        "script":  "validate_jsonb_drift.py",
        "args":    [],
        "label":   "JSONB Schema Drift (4-layer: unread JSONB + reader-without-writer + key inventory + column census)",
        "group":   "Platform",
        "report":  "jsonb_drift_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "validator-self-coverage",
        "script":  "validate_validator_self_coverage.py",
        "args":    [],
        "label":   "Validator Self-Coverage Meta-Gate (4-layer: missing script + unregistered + report mismatch + census)",
        "group":   "Platform",
        "report":  "validator_self_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        # P3 of SELF_IMPROVING_GATE_ROADMAP.md — the gate's freshness sense.
        # Catches the #1 rot vector (a validator asserting a literal/shape the
        # code already moved past) CHEAPLY at G-1, before a full-gate run trips
        # it opaquely. L1 (FAIL): author-declared FRESHNESS_ANCHORS must still
        # match their target file. L2 (INFO): ledger-cross-referenced decay
        # census (never-fired validator whose code-under-test out-paces it).
        "id":      "validator-freshness",
        "script":  "validate_validator_freshness.py",
        "args":    [],
        "label":   "Validator Freshness / Decay Meta-Gate (P3: declared anchors still match target + never-fired-stale-target census)",
        "group":   "Platform",
        "report":  "validator_freshness_report.json",
        "skip_if_fast": False,
    },
    {
        # C5 of SELF_IMPROVING_GATE_ROADMAP.md — version/baseline AI assets
        # like migrations. C2's eval gate scores against artifacts the gate
        # itself doesn't gate today (golden fixtures, judge prompt, model
        # chain, persona block) — a silent edit there invalidates every
        # baseline downstream. This validator FAILs if any manifest asset's
        # hash moved without its declared version bumping. Policy lives in
        # tools/ai_asset_baseline.py; this wraps `verify`.
        "id":      "ai-asset-versioning",
        "script":  "validate_ai_asset_versioning.py",
        "args":    [],
        "label":   "AI Asset Versioning (C5: prompts/eval-sets/model-chain/judge versioned + hash-locked like migrations)",
        "group":   "Platform",
        "report":  "ai_asset_baseline_report.json",
        "skip_if_fast": False,
    },
    {
        # C3 Phase 1 of SELF_IMPROVING_GATE_ROADMAP.md — promote the C2 AI
        # eval regression gate (`tools/ai_eval_gate.py gate`) to a G0
        # validator. Degrade-to-SKIP by design: no committed golden baseline
        # OR no fresh results = exit 0 with an explanatory message; only a
        # locked-test regression beyond tolerance returns exit 1. Mirrors
        # how P1's ledger and P6's split shipped standalone first. C3 Phase
        # 2 (clock + prod off `ai_quality_log` via Grafana/Sentry) is the
        # production extension — separate, needs deploy.
        "id":      "ai-eval-regression",
        "script":  "validate_ai_eval_regression.py",
        "args":    [],
        "label":   "AI Eval Regression Gate (C3 Phase 1: score locked-test split vs frozen golden; degrade-to-SKIP without data)",
        "group":   "Platform",
        "report":  "ai_eval_baseline.json",
        "skip_if_fast": False,
    },
    {
        # Phase 8 §8.3 of AI_SURFACE_MAP.md — the per-COMPANION-DIMENSION regression gate
        # (agent/rag/memory/persona), sibling to ai-eval-regression (which gates the frozen
        # functionality/safety axis). Wraps `tools/ai_eval_gate.py companion_gate()`: for each
        # registry-`active` dim with a frozen baseline in companion_dim_baselines.json, score the
        # dim's latest results on the LOCKED-TEST split and exit 1 only if a *blocking* dim
        # regressed beyond tolerance. Degrade-to-SKIP (exit 0) without a baseline or fresh
        # results. Agent shipped active+forward-only (blocking=false) at n=2 locked-test;
        # flips blocking=true once the golden set is expanded. RAG/Memory/Persona join as built.
        "id":      "companion-dim-gate",
        "script":  "validate_companion_dim_gate.py",
        "args":    [],
        "label":   "Companion Per-Dimension Regression Gate (Phase 8 §8.3: agent/rag/memory/persona locked-test; degrade-to-SKIP without data)",
        "group":   "Platform",
        "report":  "companion_dim_baselines.json",
        "skip_if_fast": False,
    },
    {
        # §0.7 Grounding Doctrine / G-Accept — the STANDING HELD-OUT diverse gate. Ian's invariant:
        # "millions of questions can be right yet a real user still fails." The TEMPLATED fabrication
        # families overfit; the `--diverse` bank (novel/adversarial phrasings, run ONCE, mechanical
        # DB-truth grader in companion_fabrication_sweep.py) keeps the fabrication FLOOR honest. This
        # gate institutionalizes it: it reads the latest diverse board and FAILS only on a genuine
        # REGRESSION beyond the oscillation-ceiling threshold (the rate OSCILLATES ~0-7% run-to-run
        # vs the rotating free-tier model — so it is threshold-not-zero by design), and DEGRADES-TO-
        # SKIP (exit 0) without a fresh/valid board so it never blocks a commit on missing live infra.
        # Produce fresh data with `python validate_companion_diverse_gate.py --run` (needs the local
        # stack + free-tier keys); a scheduled job runs that for a true standing loop. Threshold +
        # forward-only ratchet live in companion_diverse_baseline.json.
        "id":      "companion-diverse-gate",
        "script":  "validate_companion_diverse_gate.py",
        "args":    [],
        "label":   "Companion Held-Out Diverse Gate (§0.7: novel-phrasing fabrication floor; threshold-not-zero; degrade-to-SKIP without a fresh board)",
        "group":   "Platform",
        "report":  "companion_diverse_baseline.json",
        "skip_if_fast": False,
    },
    {
        # C4 Phase 1 of SELF_IMPROVING_GATE_ROADMAP.md — catalog the AI seams
        # (saas→ai / ai→ai / ai→tenant / ai→quota) and ratchet forward-only
        # on the inventory. "Per-domain green ≠ system green" — a seam bug
        # passes both domain gates individually. This Phase 1 establishes
        # the inventory; Phase 2a will wire per-seam contract tests; Phase
        # 2b's meta-gate consumes the catalog to decide blast radius.
        "id":      "ai-seams-inventory",
        "script":  "validate_ai_seams_inventory.py",
        "args":    [],
        "label":   "AI Seams Inventory (C4 Phase 1: catalog SaaS→AI / AI→tenant / AI→quota boundaries + forward-only ratchet)",
        "group":   "Platform",
        "report":  "ai_seams_catalog.json",
        "skip_if_fast": False,
    },
    {
        # C4 Phase 2a of SELF_IMPROVING_GATE_ROADMAP.md — forward-only ratchet
        # on the count of AI seams that lack a wire-format contract test.
        # Each entry wired into ai_seam_contracts.json (seam_id -> test path)
        # pays the baseline down by 1; the baseline floor only drops, never
        # rises silently. Today's floor = 118 (all uncovered); ratchets to 0
        # as Phase 2a payoff work writes contract tests.
        "id":      "ai-seam-coverage",
        "script":  "validate_ai_seam_coverage.py",
        "args":    [],
        "label":   "AI Seam Contract-Test Coverage (C4 Phase 2a: forward-only on uncovered seam count; floor auto-lowers as tests get wired)",
        "group":   "Platform",
        "report":  "ai_seam_coverage_baseline.json",
        "skip_if_fast": False,
    },
    {
        # C4 Phase 2c of SELF_IMPROVING_GATE_ROADMAP.md — observation-only
        # G0 promotion of the meta-gate. The composition policy in
        # tools/meta_gate.py is strictly MORE PERMISSIVE than the monolithic
        # 354-validator gate (it converts FAILs to warn via blast radius +
        # seam-sharpening, never adds blocks), so promoting it as a hard
        # blocker would be a no-op for ship/block. What this promotion DOES
        # add: every gate run writes a per-domain composition reasoning
        # line to meta_gate_decisions.jsonl. That's the macro-loop input
        # P2's promotion engine will later mine — same pattern as P1's
        # efficacy ledger (ship the observer first; make it a driver later).
        "id":      "meta-gate",
        "script":  "validate_meta_gate.py",
        "args":    [],
        "label":   "Meta-Gate Recorder (C4 Phase 2c: observation-mode promotion; writes per-domain decision to meta_gate_decisions.jsonl per gate run)",
        "group":   "Platform",
        "report":  "meta_gate_decisions.jsonl",
        "skip_if_fast": False,
    },
    {
        # Meta-check for the Self-Improving Gate's C-track stack. The 7 C-track
        # phases shipped over 6 in-session turns produced 9 artifacts + 5
        # validators. Once in place, the next risk is silent erosion (a
        # baseline gets deleted, a catalog rename buries the validator that
        # reads it, etc.). This validator asserts every C-track artifact +
        # validator + registry entry + roadmap label is intact + well-formed.
        # FAILs loud the moment any piece goes missing.
        "id":      "c-track-self-coverage",
        "script":  "validate_c_track_self_coverage.py",
        "args":    [],
        "label":   "C-track Self-Coverage (meta: 9 artifacts + 5 validators + registry + roadmap labels of the Self-Improving Gate C-track)",
        "group":   "Platform",
        "report":  "c_track_self_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        # Standing Rule D for the Grounded MCP Sweep: every page marked done in
        # GROUNDED_SWEEP_ROADMAP.md must keep its crystallized live lock (a
        # journey-*.spec.ts assertion named in grounded_sweep_locks.json). FAILs
        # the moment a done page has no lock, or a lock's spec/marker vanishes —
        # so the sweep's guards can't silently erode. Makes the sweep a
        # self-policed part of the self-improving gate, not an adjacent ritual.
        "id":      "grounded-sweep",
        "script":  "validate_grounded_sweep.py",
        "args":    [],
        "label":   "Grounded MCP Sweep Self-Coverage (meta: every done page in the roadmap keeps its crystallized journey lock)",
        "group":   "Platform",
        "report":  "grounded_sweep_report.json",
        "skip_if_fast": False,
    },
    {
        # Step 7 capstone (Companion Stack Battery): the AI sibling of the UFAI
        # sweep. Keeps companion_battery.js (window.__CSB) + the durable
        # journey-companion-comprehensive.spec.ts wired, and forward-only
        # ratchets the rubric's max_major_defects (the capstone's invariant: every
        # HARD grounded proof — model_chain / agent_memory row / cited[] / no-leak
        # — passes). Degrade-to-SKIP if the rubric isn't established yet.
        "id":      "companion-stack",
        "script":  "validate_companion_stack.py",
        "args":    [],
        "label":   "Companion Stack capstone self-coverage (Agent/Memory/RAG/Safety; forward-only on Major grounded defects)",
        "group":   "Platform",
        "report":  "companion_stack_report.json",
        "skip_if_fast": False,
    },
    {
        # Grounded Sweep critique C7, promoted to a gate rule: forward-only DEBT
        # ratchet on hand-rolled modal a11y (role="dialog"+aria-modal). Baselines
        # today's debt (modal_a11y_baseline.json) so it doesn't break the gate,
        # FAILs only when a NEW non-a11y modal ships, and ratchets down as C7
        # retrofits land. Turns a systemic critique into an enforced, tracked metric.
        "id":      "modal-a11y",
        "script":  "validate_modal_a11y.py",
        "args":    [],
        "label":   "Modal A11y Debt Ratchet (no NEW hand-rolled modal without role=dialog+aria-modal; critique C7)",
        "group":   "Platform",
        "report":  "modal_a11y_report.json",
        "skip_if_fast": False,
    },
    {
        # Grounded Sweep (link-destination class): no committed resource attribute
        # (<script src>, <link href>, <img src>, srcset, or <a href>) may hardcode
        # the dev-only /workhive/ prefix. The local URL bridge only rewrites <a href>
        # at runtime, so a committed /workhive/ in any src/link 404s in production.
        # Caught the learn + legal feedback-FAB leak (41 pages dead in prod).
        "id":      "prod-path-leak",
        "script":  "validate_prod_path_leak.py",
        "args":    [],
        "label":   "Prod path leak (no committed /workhive/ resource paths — they 404 in production)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Grounded Sweep critique W3 (status-enum-constants): deterministic guard
        # that utils.js window.WH_STATUS_ENUMS never silently diverges from the
        # canonical capture-contract enum in supabase/migrations. Prevents the class
        # of bug where a hand-typed status literal (e.g. 'closed' vs the real
        # pending/in_progress/done/blocked/skipped) miscounts a KPI — the dayplanner
        # overdue bug. Source-vs-source compare, no page scan, zero false positives.
        "id":      "status-enum-drift",
        "script":  "validate_status_enum_drift.py",
        "args":    [],
        "label":   "Status-Enum Drift Guard (WH_STATUS_ENUMS == canonical DB enum; critique W3)",
        "group":   "Platform",
        "report":  "status_enum_drift_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "provider-bypass",
        "script":  "validate_provider_bypass.py",
        "args":    [],
        "label":   "Direct Provider Bypass (4-layer: client provider + edge bypass + SDK drift + distribution)",
        "group":   "Platform",
        "report":  "provider_bypass_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cache-invalidation",
        "script":  "validate_cache_invalidation.py",
        "args":    [],
        "label":   "Cache Invalidation (4-layer: shell missing + shell drift + version history + shell inventory)",
        "group":   "Platform",
        "report":  "cache_invalidation_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "realtime-filter",
        "script":  "validate_realtime_filter.py",
        "args":    [],
        "label":   "Cross-Hive Realtime Filter Coverage (4-layer: missing filter + channel naming + scoped distribution + density)",
        "group":   "Platform",
        "report":  "realtime_filter_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "optimistic-reconciliation",
        "script":  "validate_optimistic_reconciliation.py",
        "args":    [],
        "label":   "Optimistic Update Reconciliation (4-layer: no error path + catch w/o rollback + pattern density + handler distribution)",
        "group":   "Platform",
        "report":  "optimistic_reconciliation_report.json",
        "skip_if_fast": False,
    },
    # REMOVED: a second `memory-integrity` registration pointing at the same script, labelled "4-layer:
    # schema + RLS + index + retention". That label was also WRONG — validate_memory_integrity.py checks the
    # agent_memory schema, per-tab session_id tracking, >90%-similarity dedup and the memory window passed to
    # the LLM. Neither RLS, nor indexes, nor retention. So the duplicate was both redundant work and a false
    # description of what the gate proves, which is the more expensive half: a reader trusting that label
    # would believe retention was covered when nothing asserts it
    # ([[feedback_the_prose_was_stale_the_registry_was_right]], inverted — here the prose was the registry).
    {
        "id":      "gateway-routing",
        "script":  "validate_gateway_routing.py",
        "args":    [],
        "label":   "AI Gateway Routing (4-layer: gateway present + routes exist + canonical coverage + inventory)",
        "group":   "Platform",
        "report":  "gateway_routing_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "agent-handoff-contract",
        "script":  "validate_agent_handoff_contract.py",
        "args":    [],
        "label":   "Agent Handoff Contract (4-layer: handoff keys + specialist awareness + worker_name trust + inventory)",
        "group":   "Platform",
        "report":  "agent_handoff_contract_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "function-security",
        "script":  "validate_function_security.py",
        "args":    [],
        "label":   "SQL Function Security Posture (4-layer: DEFINER+search_path + trigger explicit + matrix + aggregate)",
        "group":   "Platform",
        "report":  "function_security_report.json",
        "skip_if_fast": False,
    },
    {
        # Companion to function-security: that checks the search_path-shadowing
        # class; this checks the TENANT-isolation class. A SECURITY DEFINER fn
        # taking a hive_id bypasses RLS, so if it's reachable by anon/auth and
        # has no in-function membership gate it's a cross-hive IDOR vector
        # (invisible to RLS-policy validators). FAIL-at-0: gated OR service_role-only.
        "id":      "definer-membership-gate",
        "script":  "validate_definer_membership_gate.py",
        "args":    [],
        "label":   "SECURITY DEFINER Hive-Membership Gate (every DEFINER hive-fn gated OR service_role-only)",
        "group":   "Platform",
        "report":  "definer_membership_gate_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "admin-gates",
        "script":  "validate_admin_gates.py",
        "args":    [],
        "label":   "Admin Gate Enforcement (founder-console, marketplace-admin must verify admin)",
        "group":   "Platform",
        "report":  "admin_gates_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "html-id-unique",
        "script":  "validate_html_id_unique.py",
        "args":    [],
        "label":   "HTML ID Uniqueness (4-layer: dup-within-file + cross-page drift + density + reserved-name)",
        "group":   "Platform",
        "report":  "html_id_unique_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "trigger-reentrancy",
        "script":  "validate_trigger_reentrancy.py",
        "args":    [],
        "label":   "Trigger Reentrancy Safety (4-layer: self-write guard + indirect loop + inventory + depth adoption)",
        "group":   "Platform",
        "report":  "trigger_reentrancy_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "migration-order",
        "script":  "validate_migration_order.py",
        "args":    [],
        "label":   "Schema Migration Order Safety (4-layer: table order + column order + function order + dependency matrix)",
        "group":   "Platform",
        "report":  "migration_order_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "auth-boundary",
        "script":  "validate_auth_boundary.py",
        "args":    [],
        "label":   "Auth Boundary Coverage (4-layer: HTML identity + edge auth + identity distribution + anon writes)",
        "group":   "Platform",
        "report":  "auth_boundary_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ai-eval-coverage",
        "script":  "validate_ai_eval_coverage.py",
        "args":    [],
        "label":   "AI Evaluation Coverage (4-layer: registry present + fixture coverage + eval cron + quality log)",
        "group":   "Platform",
        "report":  "ai_eval_coverage_report.json",
        "skip_if_fast": False,
    },
    {"id": "bundle-bloat", "script": "validate_bundle_bloat.py", "args": [],
     "label": "Edge Function Bundle Bloat (4-layer: LOC + imports + distribution + dynamic adoption)",
     "group": "Platform", "report": "bundle_bloat_report.json", "skip_if_fast": False},
    {"id": "sw-offline", "script": "validate_sw_offline.py", "args": [],
     "label": "Service Worker Offline Coverage (4-layer: critical-in-shell + offline fallback + resilience + register)",
     "group": "Platform", "report": "sw_offline_report.json", "skip_if_fast": False},
    {"id": "module-scope-state", "script": "validate_module_scope_state.py", "args": [],
     "label": "Module-Scope Mutable State (4-layer: unbounded growth + eviction adoption + inventory + clean fns)",
     "group": "Platform", "report": "module_scope_state_report.json", "skip_if_fast": False},
    {"id": "date-arithmetic", "script": "validate_date_arithmetic.py", "args": [],
     "label": "Date Arithmetic Safety (4-layer: space-date + parse-vs-ISO + ms literals + TZ-naive helpers)",
     "group": "Platform", "report": "date_arithmetic_report.json", "skip_if_fast": False},
    {"id": "cron-functional", "script": "validate_cron_functional.py", "args": [],
     "label": "Cron Job Functional Coverage (4-layer: target exists + config entry + AI gate + density)",
     "group": "Platform", "report": "cron_functional_report.json", "skip_if_fast": False},
    {"id": "jsonb-index", "script": "validate_jsonb_index.py", "args": [],
     "label": "JSONB Index Drift (4-layer: missing GIN + arrow freq + inventory + op distribution)",
     "group": "Platform", "report": "jsonb_index_report.json", "skip_if_fast": False},
    {"id": "test-page-drift", "script": "validate_test_page_drift.py", "args": [],
     "label": "Test Page Drift (4-layer: smaller + larger + orphans + inventory)",
     "group": "Platform", "report": "test_page_drift_report.json", "skip_if_fast": False},
    {"id": "embedding-coverage", "script": "validate_embedding_coverage.py", "args": [],
     "label": "Embedding Coverage & Freshness (4-layer: refresh pipeline + vector index + source coverage + dim inventory)",
     "group": "Platform", "report": "embedding_coverage_report.json", "skip_if_fast": False},
    {"id": "ai-cost-observability", "script": "validate_ai_cost_observability.py", "args": [],
     "label": "AI Cost Observability (4-layer: ledger + callAI logs + dashboard + invocations)",
     "group": "Platform", "report": "ai_cost_observability_report.json", "skip_if_fast": False},
    {"id": "hive-quota", "script": "validate_hive_quota.py", "args": [],
     "label": "Per-Hive Resource Quota (4-layer: quota table + trigger coverage + inventory + adoption)",
     "group": "Platform", "report": "hive_quota_report.json", "skip_if_fast": False},
    {"id": "logbook-quota", "script": "tools/validate_logbook_quota.py", "args": [],
     "label": "Q0 Logbook Quota Pilot (per-day rate-limit trigger + server text caps + friendly UX + photo size assert; the Q2-replication template)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "quota-coverage", "script": "tools/validate_quota_coverage.py", "args": [],
     "label": "Q2/Q5 Quota Coverage Ratchet (every high-write table has a per-day cap on a REAL timestamp column; FAILs if a new surface ships uncapped)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "text-cap-coverage", "script": "tools/validate_text_cap_coverage.py", "args": [],
     "label": "Q3 Text+Upload Cap Ratchet (server-side text-cap trigger per high-write table + upload size/duration caps; no unbounded user input)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "quota-board", "script": "tools/quota_board.py", "args": ["--check"],
     "label": "Q5 Unified Quota Board (aggregates all quota dimensions into one measured board; FAILs if any bound is red)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "quota-page-audit", "script": "tools/quota_page_audit.py", "args": ["--check"],
     "label": "Per-Page Quota Audit (EVERY production feature page's write tables are capped or documented-excluded; FAILs on any uncapped page write)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "global-ai-budget", "script": "tools/validate_global_ai_budget.py", "args": [],
     "label": "Q6 Global LLM Budget Guard (the org-shared-pool layer above per-tenant caps: atomic row-locked consume RPC + daily circuit-breaker + per-minute burst smoother that sheds background/passes interactive + fail-open + wired at the gateway; FAILs if any regress)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "cumulative-quota-enforce", "script": "tools/validate_cumulative_quota_enforcement.py", "args": [],
     "label": "Q1 Cumulative Quota Enforcement (hive_quotas.enforce_blocking flipped ON + generous abuse-ceiling caps backfilled + new-hive auto-seed + all 5 cumulative triggers ATTACHED + status-fixed RAISE 54000; catches the trigger-drift + 'warn'-status bug classes)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "embedding-retention", "script": "tools/validate_embedding_retention.py", "args": [],
     "label": "Q5-b Embedding/Growth Retention (embedding_cache LRU auto-prune cron + prune fn; canonical big tables via the safe DRY-RUN-default, double-gated cold_archive_prune step-3 whose safety gate provably never deletes without a verified Parquet snapshot)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "realtime-channel-cap", "script": "tools/validate_realtime_channel_cap.py", "args": [],
     "label": "Q5 Realtime Cap + Graceful-429 (whRealtimeSubscribe bounds channels PER CLIENT against the verified 200-concurrent free-tier wall + degrades overflow/offline to visibility-aware polling + frees the slot on stop; scope-aware 429 UX maps each gateway body (burst/platform/daily/hourly) to the right hint; C5+C6 run Node behavioural tests as real teeth)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "inline-image-guard", "script": "tools/validate_inline_image_guard.py", "args": [],
     "label": "Q5-a Inline Image Detector-Guard (server-side base64 photo size cap on logbook+inventory_items backstopping the <=700KB client compression + photo_attach_stats() telemetry; the right-sized move since measured attach-rate is 0% — full Storage-offload stays deferred until the rate climbs)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "ai-daily-ceiling", "script": "tools/validate_ai_daily_ceiling.py", "args": [],
     "label": "Q4 AI Daily Ceiling (per-day AI window on ai_rate_limits+ai_user_rate_limits denies scope='day' at the daily cap; the deno-gated live-runtime 429 is proven via a Node local-substitute that drives the REAL checkAIRateLimit/checkSoloRateLimit decision — deploy Ian-gated)",
     "group": "Platform", "report": None, "skip_if_fast": False},
    {"id": "data-retention", "script": "validate_data_retention.py", "args": [],
     "label": "Data Retention / Right-to-Erasure (4-layer: delete path + helper + PII inventory + retention)",
     "group": "Platform", "report": "data_retention_report.json", "skip_if_fast": False},
    {"id": "ai-safety", "script": "validate_ai_safety.py", "args": [],
     "label": "AI Input Bounds / Safety (4-layer: field slices + any slice + slice constants + input inventory)",
     "group": "Platform", "report": "ai_safety_report.json", "skip_if_fast": False},
    {"id": "ai-alignment", "script": "validate_ai_alignment.py", "args": [],
     "label": "AI Alignment / Provenance (4-layer: source stamp + provenance cols + dashboard filter + inventory)",
     "group": "Platform", "report": "ai_alignment_report.json", "skip_if_fast": False},
    {"id": "ai-payload-hygiene", "script": "validate_ai_payload_hygiene.py", "args": [],
     "label": "AI Payload Hygiene (4-layer: no select-star + module prompts + limit bounds + payload inventory)",
     "group": "Platform", "report": "ai_payload_hygiene_report.json", "skip_if_fast": False},
    {"id": "pgvector-consistency", "script": "validate_pgvector_consistency.py", "args": [],
     "label": "pgvector Consistency (4-layer: dim match + hive filter + embedding RLS + dim distribution)",
     "group": "Platform", "report": "pgvector_consistency_report.json", "skip_if_fast": False},
    {"id": "pdf-pipeline", "script": "validate_pdf_pipeline.py", "args": [],
     "label": "PDF Pipeline / Knowledge Ingestion (4-layer: jobs table + runner fn + coverage + inventory)",
     "group": "Platform", "report": "pdf_pipeline_report.json", "skip_if_fast": False},
    {"id": "rag-completeness", "script": "validate_rag_completeness.py", "args": [],
     "label": "RAG Completeness (4-layer: rerank helper + budget helper + rerank adoption + inventory)",
     "group": "Platform", "report": "rag_completeness_report.json", "skip_if_fast": False},
    {"id": "gateway-coverage", "script": "validate_gateway_coverage.py", "args": [],
     "label": "Platform Gateway Coverage (4-layer: gateway present + routes exist + coverage + inventory)",
     "group": "Platform", "report": "gateway_coverage_report.json", "skip_if_fast": False},
    {"id": "gateway-audit", "script": "validate_gateway_audit.py", "args": [],
     "label": "Platform Gateway Audit Completeness (4-layer: schema + writes + RLS + retention)",
     "group": "Platform", "report": "gateway_audit_report.json", "skip_if_fast": False},
    {"id": "gateway-tenancy", "script": "validate_gateway_tenancy.py", "args": [],
     "label": "Gateway Tenancy Verification (Pillar I: client hive_id must be membership-verified; ratchet 34->0)",
     "group": "Platform", "report": "gateway_tenancy_report.json", "skip_if_fast": False},
    {"id": "policy-hive-binding", "script": "validate_policy_hive_binding.py", "args": [],
     "label": "Gateway Policy Hive-Binding (Pillar P: anon-capable fns must rate-limit on the verified tenant, never a raw client hive_id)",
     "group": "Platform", "report": "policy_hive_binding_report.json", "skip_if_fast": False},
    {
        "id":      "ai-pattern-compliance",
        "script":  "validate_ai_pattern_compliance.py",
        "args":    [],
        "label":   "AI Pattern Compliance (4-layer: rate-gate-first + fallback chain + JSON mode + cost concentration)",
        "group":   "Platform",
        "report":  "ai_pattern_compliance_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "state-machine-integrity",
        "script":  "validate_state_machine_integrity.py",
        "args":    [],
        "label":   "State Machine Integrity (4-layer: invalid writes + unreachable states + unconstrained columns + writer matrix)",
        "group":   "Platform",
        "report":  "state_machine_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "audit-log-coverage",
        "script":  "validate_audit_log_coverage.py",
        "args":    [],
        "label":   "Audit Log Coverage (4-layer: unaudited writers + dead audit columns + critical-table coverage + writer matrix)",
        "group":   "Platform",
        "report":  "audit_log_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cron-schedule-integrity",
        "script":  "validate_cron_schedule_integrity.py",
        "args":    [],
        "label":   "Cron Schedule Integrity (4-layer: function existence + scheduled-agents routing + config drift + schedule sanity)",
        "group":   "Platform",
        "report":  "cron_schedule_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "rls-readiness",
        "script":  "validate_rls_readiness.py",
        "args":    [],
        "label":   "RLS Readiness Audit (4-layer: lockout traps + dead policies + permissive USING(true) catalog + verb completeness)",
        "group":   "Platform",
        "report":  "rls_readiness_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "auth-migration-readiness",
        "script":  "validate_auth_migration_readiness.py",
        "args":    [],
        "label":   "Auth Migration Readiness (Phase A audit: sibling coverage + auth_uid columns + identity gate strength)",
        "group":   "Platform",
        "report":  "auth_migration_readiness_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "edge-caller-contract",
        "script":  "validate_edge_caller_contract.py",
        "args":    [],
        "label":   "Edge Function Caller Contract (4-layer: function existence + required field coverage + phantom fields + orphan functions)",
        "group":   "Platform",
        "report":  "edge_caller_contract_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "env-secret-coverage",
        "script":  "validate_env_secret_coverage.py",
        "args":    [],
        "label":   "Env Secret Coverage (4-layer: declared coverage + required-vs-optional + orphan keys + hardcoded secret detection)",
        "group":   "Platform",
        "report":  "env_secret_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "realtime-payload-contract",
        "script":  "validate_realtime_payload_contract.py",
        "args":    [],
        "label":   "Realtime Payload Consumer Contract (4-layer: subscribed table + payload columns + filter columns + channel name uniqueness)",
        "group":   "Platform",
        "report":  "realtime_payload_contract_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "edge-response-contract",
        "script":  "validate_edge_response_contract.py",
        "args":    [],
        "label":   "Edge Function Response Contract (4-layer: function returns + caller field validity + introspection coverage + error-only detection)",
        "group":   "Platform",
        "report":  "edge_response_contract_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "realtime-cleanup",
        "script":  "validate_realtime_cleanup.py",
        "args":    [],
        "label":   "Realtime Subscription Cleanup (4-layer: cleanup pairing + lifecycle wiring + const-decl warning + asymmetry metric)",
        "group":   "Platform",
        "report":  "realtime_cleanup_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "reliability-workbench",
        "script":  "validate_reliability_workbench.py",
        "args":    [],
        "label":   "Reliability Workbench Validator (FMEA + RCM + Weibull + P-F schema, RLS, canonical registration)",
        "group":   "Platform",
        "report":  "reliability_workbench_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "maturity-gating",
        "script":  "validate_maturity_gating.py",
        "args":    [],
        "label":   "Maturity Gating Validator (Phase 0.5: gated pages load maturity-gate.js + call checkMaturityGate + render honest empty state)",
        "group":   "Platform",
        "report":  "maturity_gating_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "amc",
        "script":  "validate_amc.py",
        "args":    [],
        "label":   "AMC Validator (Phase 1.9: amc_briefings migration + cost log + realtime + alert-hub subscription + canonical anchor)",
        "group":   "Platform",
        "report":  "amc_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "visual-defect",
        "script":  "validate_visual_defect.py",
        "args":    [],
        "label":   "Visual Defect Capture Validator (Phase 1.9: callAIMultimodal + rate-limit + MIME whitelist + fire-and-forget embed + cost log)",
        "group":   "Platform",
        "report":  "visual_defect_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "sensor-pipeline",
        "script":  "validate_sensor_pipeline.py",
        "args":    [],
        "label":   "Sensor Pipeline Validator (Phase 1.9: sensor_readings schema + realtime + asset-hub subscription + anomaly module)",
        "group":   "Platform",
        "report":  "sensor_pipeline_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "achievements",
        "script":  "validate_achievements.py",
        "args":    [],
        "label":   "Achievements Validator (Phase 1.9: badge_key + catalog-not-in-reset + worker_achievements realtime + ON CONFLICT shape)",
        "group":   "Platform",
        "report":  "achievements_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "dayplanner",
        "script":  "validate_dayplanner.py",
        "args":    [],
        "label":   "Day Planner Validator (Phase 1.9: DILO/WILO/MILO/YILO tabs + schedule_items + nav-hub linkage + auth-aware)",
        "group":   "Platform",
        "report":  "dayplanner_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "resilience",
        "script":  "validate_resilience.py",
        "args":    [],
        "label":   "Resilience Validator (Phase 1.10 reframe: offline queue + network-loss UI + fetchWithTimeout + shared-device sign-out)",
        "group":   "Platform",
        "report":  "resilience_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "adoption-observability",
        "script":  "validate_adoption_observability.py",
        "args":    [],
        "label":   "Adoption Observability Validator (Phase 3.6: hive_adoption_score migration + supervisor card + onboarding stepper + intent capture + canonical anchors)",
        "group":   "Platform",
        "report":  "adoption_observability_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "revenue-surfaces",
        "script":  "validate_revenue_surfaces.py",
        "args":    [],
        "label":   "Revenue Surfaces Validator (Phase 4: AI Quality Stair 2 gate + Anomaly Engine 2.0 Stair 3 gate + Knowledge Pipeline tile + canonical anchors)",
        "group":   "Platform",
        "report":  "revenue_surfaces_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "enterprise-unlock",
        "script":  "validate_enterprise_unlock.py",
        "args":    [],
        "label":   "Enterprise Unlock Validator (Phase 5: retention + soft-delete cron + PDPA export + auth_session_events + MFA scaffold + SSO scaffold + Plant Connections Console)",
        "group":   "Platform",
        "report":  "enterprise_unlock_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "industry-defining",
        "script":  "validate_industry_defining.py",
        "args":    [],
        "label":   "Industry-Defining Validator (Phase 6: knowledge graph + drone inspections + standards registry + federated opt-in + insurance bridge view + MaaS consulting engagements)",
        "group":   "Platform",
        "report":  "industry_defining_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "audit-trail-coverage",
        "script":  "validate_audit_trail_coverage.py",
        "args":    [],
        "label":   "Audit Trail Coverage (2-layer: lifecycle status updates write to hive_audit_log + every action name has ACTION_ICON entry)",
        "group":   "Platform",
        "report":  "audit_trail_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "realtime-publication",
        "script":  "validate_realtime_publication.py",
        "args":    [],
        "label":   "Realtime Publication Coverage Validator (subscribed tables in supabase_realtime)",
        "group":   "Platform",
        "report":  "realtime_publication_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "hive-state-consistency",
        "script":  "validate_hive_state_consistency.py",
        "args":    [],
        "label":   "Hive-State LocalStorage Consistency Validator (branch-symmetry on hive.html)",
        "group":   "Platform",
        "report":  "hive_state_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "arc-x-cognitive",
        "script":  "validate_arc_x_cognitive.py",
        "args":    [],
        "label":   "Arc X Cognitive-Load HARD Gate (L1 real-login hive resolution / Issue #1)",
        "group":   "Platform",
        "report":  "arc_x_cognitive_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "arc-x-cfamily",
        "script":  "tools/validate_arc_x_cfamily.py",
        "args":    [],
        "label":   "Arc X Family C scanner (C2 placeholder-as-label + C1 recall-entity floor at 0)",
        "group":   "Platform",
        "report":  "arc_x_cfamily_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "arc-x-befamily",
        "script":  "tools/validate_arc_x_befamily.py",
        "args":    [],
        "label":   "Arc X Family B+E scanner (B2 information-scent floor at 0; B3/E3 candidate lists)",
        "group":   "Platform",
        "report":  "arc_x_befamily_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "soft-delete",
        "script":  "validate_soft_delete.py",
        "args":    [],
        "label":   "Soft-Delete Read-Path Validator (.is(deleted_at, null) on every SELECT)",
        "group":   "Platform",
        "report":  "soft_delete_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "schema-drift",
        "script":  "validate_schema_drift.py",
        "args":    [],
        "label":   "Schema Drift Validator (HTML SELECT columns exist in EXPECTED_SCHEMA)",
        "group":   "Platform",
        "report":  "schema_drift_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "logbook-consistency",
        "script":  "validate_logbook_consistency.py",
        "args":    [],
        "label":   "Logbook Consistency (closed_at set, Open no closed_at, parts txn parity)",
        "group":   "Data Quality",
        "report":  "logbook_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "pattern-alerts",
        "script":  "validate_pattern_alerts.py",
        "args":    [],
        "label":   "Pattern Alerts Quality (no <think> leak, valid rule_ids, non-empty text)",
        "group":   "Data Quality",
        "report":  "pattern_alerts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "inventory-integrity",
        "script":  "validate_inventory_integrity.py",
        "args":    [],
        "label":   "Inventory Integrity (no negative qty, valid txn types, qty_after accuracy)",
        "group":   "Data Quality",
        "report":  "inventory_integrity_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "hive",
        "script":  "validate_hive.py",
        "args":    [],
        "label":   "Hive Validator",
        "group":   "Platform",
        "report":  "hive_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "logbook",
        "script":  "validate_logbook.py",
        "args":    [],
        "label":   "Logbook Validator",
        "group":   "Platform",
        "report":  "logbook_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "inventory",
        "script":  "validate_inventory.py",
        "args":    [],
        "label":   "Inventory Validator",
        "group":   "Platform",
        "report":  "inventory_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "marketplace",
        "script":  "validate_marketplace.py",
        "args":    [],
        "label":   "Marketplace Validator (4-layer: schema + edge functions + UI gates + money flow)",
        "group":   "Platform",
        "report":  "marketplace_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "pm",
        "script":  "validate_pm.py",
        "args":    [],
        "label":   "PM Scheduler Validator",
        "group":   "Platform",
        "report":  "pm_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "skillmatrix",
        "script":  "validate_skillmatrix.py",
        "args":    [],
        "label":   "Skill Matrix Validator",
        "group":   "Platform",
        "report":  "skillmatrix_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "report-sender",
        "script":  "validate_report_sender.py",
        "args":    [],
        "label":   "Report Sender Validator (32 checks: structure + UI + logic + PWA)",
        "group":   "Platform",
        "report":  "report_sender_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "community",
        "script":  "validate_community.py",
        "args":    [],
        "label":   "Community Validator (24 checks: XSS + isolation + access + realtime + standards + feature schema completeness)",
        "group":   "Platform",
        "report":  "community_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "assistant",
        "script":  "validate_assistant.py",
        "args":    [],
        "label":   "Assistant Validator",
        "group":   "Platform",
        "report":  "assistant_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ai-context",
        "script":  "validate_ai_context.py",
        "args":    [],
        "label":   "AI Context Quality Validator",
        "group":   "Platform",
        "report":  "ai_context_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "input-guards",
        "script":  "validate_input_guards.py",
        "args":    [],
        "label":   "Input Guards Validator",
        "group":   "Platform",
        "report":  "input_guards_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "schema",
        "script":  "validate_schema.py",
        "args":    [],
        "label":   "Schema Consistency Validator",
        "group":   "Platform",
        "report":  "schema_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "observability",
        "script":  "validate_observability.py",
        "args":    [],
        "label":   "Observability Validator",
        "group":   "Platform",
        "report":  "observability_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "governance",
        "script":  "validate_governance.py",
        "args":    [],
        "label":   "Data Governance Validator",
        "group":   "Platform",
        "report":  "governance_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "pwa",
        "script":  "validate_pwa.py",
        "args":    [],
        "label":   "PWA Integrity Validator",
        "group":   "Platform",
        "report":  "pwa_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "accessibility",
        "script":  "validate_accessibility.py",
        "args":    [],
        "label":   "Accessibility Baseline Validator",
        "group":   "Platform",
        "report":  "accessibility_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "tenant-boundary",
        "script":  "validate_tenant_boundary.py",
        "args":    [],
        "label":   "Tenant Boundary Escape Validator (5-layer, +nullable auth_uid RLS trap)",
        "group":   "Platform",
        "report":  "tenant_boundary_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ai-regression",
        "script":  "validate_ai_regression.py",
        "args":    [],
        "label":   "AI Prompt Regression Validator (4-layer: consistency + content + parity + Tier-S citation)",
        "group":   "Platform",
        "report":  "ai_regression_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "timers",
        "script":  "validate_timers.py",
        "args":    [],
        "label":   "Timer and Scheduled Job Hygiene",
        "group":   "Platform",
        "report":  "timers_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "xss",
        "script":  "validate_xss.py",
        "args":    [],
        "label":   "XSS / escHtml Coverage Validator",
        "group":   "Platform",
        "report":  "xss_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "seo",
        "script":  "validate_seo.py",
        "args":    [],
        "label":   "SEO and Page Metadata Validator",
        "group":   "Platform",
        "report":  "seo_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "mobile",
        "script":  "validate_mobile.py",
        "args":    [],
        # Checks: viewport-fit=cover, input font-size >=16px, safe-area-inset-bottom,
        # touch targets >=44px, will-change:filter mobile override (iOS GPU crash guard),
        # body{animation} prefers-reduced-motion override (blank page guard)
        "label":   "Mobile UX Compliance Validator",
        "group":   "Platform",
        "report":  "mobile_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "nav-registry",
        "script":  "validate_nav_registry.py",
        "args":    [],
        "label":   "Nav Hub Registry Validator",
        "group":   "Platform",
        "report":  "nav_registry_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "performance",
        "script":  "validate_performance.py",
        "args":    [],
        # Checks: unbounded queries, select('*') on wide tables, N+1 loops,
        # sequential awaits, body{animation} animationend safety guard (blank page guard)
        "label":   "Performance Anti-Pattern Validator",
        "group":   "Platform",
        "report":  "performance_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "catalog-scope",
        "script":  "validate_catalog_scope.py",
        "args":    [],
        "label":   "Catalog Approval Status Validator",
        "group":   "Platform",
        "report":  "catalog_scope_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "drawings",
        "script":  "validate_drawings.py",
        "args":    [],
        "label":   "Drawing Standards Compliance Validator",
        "group":   "Platform",
        "report":  "drawings_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "diagram_inputs",
        "script":  "validate_diagram_inputs.py",
        "args":    [],
        "label":   "Diagram Inputs Contract Validator (inp.xxx vs collectInputs keys)",
        "group":   "Platform",
        "report":  "diagram_inputs_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "compliance",
        "script":  "validate_compliance.py",
        "args":    [],
        "label":   "Enterprise Compliance Baseline Validator",
        "group":   "Platform",
        "report":  "compliance_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "notifications",
        "script":  "validate_notifications.py",
        "args":    [],
        "label":   "Notification and Alert Health Validator",
        "group":   "Platform",
        "report":  "notifications_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "predictive",
        "script":  "validate_predictive.py",
        "args":    [],
        "label":   "Predictive Analytics Data Quality Validator",
        "group":   "Platform",
        "report":  "predictive_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "vector-schema",
        "script":  "validate_vector_schema.py",
        "args":    [],
        "label":   "Vector Knowledge Base Schema Validator",
        "group":   "Platform",
        "report":  "vector_schema_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "groq-fallback",
        "script":  "validate_groq_fallback.py",
        "args":    [],
        "label":   "AI Provider Chain Validator",
        "group":   "Platform",
        "report":  "groq_fallback_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "edge-contracts",
        "script":  "validate_edge_contracts.py",
        "args":    [],
        "label":   "Edge Function API Contract Validator",
        "group":   "Platform",
        "report":  "edge_contracts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ops-snapshot-agents",
        "script":  "validate_ops_snapshot_agents.py",
        "args":    [],
        "label":   "Ops-Snapshot Agent Coverage (every factual-answer agent is grounded, not just the companion)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "ai-attribution",
        "script":  "validate_ai_attribution.py",
        "args":    [],
        "label":   "AI Output Attribution Validator",
        "group":   "Platform",
        "report":  "ai_attribution_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "context-window",
        "script":  "validate_context_window.py",
        "args":    [],
        "label":   "Context Window Management Validator",
        "group":   "Platform",
        "report":  "context_window_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "knowledge-freshness",
        "script":  "validate_knowledge_freshness.py",
        "args":    [],
        "label":   "Knowledge Base Freshness Validator",
        "group":   "Platform",
        "report":  "knowledge_freshness_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "sso-readiness",
        "script":  "validate_sso_readiness.py",
        "args":    [],
        "label":   "SSO Readiness Validator",
        "group":   "Platform",
        "report":  "sso_readiness_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "idempotency",
        "script":  "validate_idempotency.py",
        "args":    [],
        "label":   "Webhook and Integration Idempotency Validator (5-layer, +UPDATE col exists, +backfill timing)",
        "group":   "Platform",
        "report":  "idempotency_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "integration-security",
        "script":  "validate_integration_security.py",
        "args":    [],
        "label":   "Integration Security Baseline Validator (3-layer, +cors dynamic, +deploy coverage)",
        "group":   "Platform",
        "report":  "integration_security_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cmms-contracts",
        "script":  "validate_cmms_contracts.py",
        "args":    [],
        "label":   "CMMS Contracts Validator (STATUS_MAP parity, DB column targets, shared imports)",
        "group":   "Platform",
        "report":  "cmms_contracts_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "cmms-reconciliation",
        "script":  "validate_cmms_reconciliation.py",
        "args":    [],
        "label":   "CMMS Reconciliation Validator (external_sync vs table counts, audit coverage, quality scores)",
        "group":   "Platform",
        "report":  "cmms_reconciliation_report.json",
        "skip_if_fast": True,   # requires live Supabase
    },
    {
        "id":      "digital-twin",
        "script":  "validate_digital_twin.py",
        "args":    [],
        "label":   "Digital Twin Schema Readiness Validator",
        "group":   "Platform",
        "report":  "digital_twin_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "iot-protocols",
        "script":  "validate_iot_protocols.py",
        "args":    [],
        "label":   "IoT and MQTT Protocol Safety Validator",
        "group":   "Platform",
        "report":  "iot_protocols_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "content-quality",
        "script":  "validate_content_quality.py",
        "args":    [],
        "label":   "Content Quality Validator (embed guard, schema drift, label quality)",
        "group":   "Platform",
        "report":  "content_quality_report.json",
        "skip_if_fast": False,
    },
    {
        # Content Grounding Gate (the 3rd sibling) — folded into the DEFAULT run so
        # outward content<->platform drift is caught on every routine gate run, not
        # only when content_dev.py is invoked by hand (D2 self-running, 2026-06-29).
        # The loop was a SENSOR that went 17 days stale; this makes it self-running.
        # Ratcheted (forward-only): PASS on pre-existing drift, FAIL on NEW drift.
        # 12 checks incl. derive-and-render surface_render_drift + llms_article_completeness.
        # Headless (~2s, offline; no server/DB/model). skip_if_fast like its heavy siblings.
        "id":      "content-grounding",
        "script":  "tools/content_grounding_gate.py",
        "args":    [],
        "label":   "Content Grounding Gate (12-check outward content drift: feature/count/link/capability/surface-render/llms-completeness; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "content_grounding_report.json",
        "skip_if_fast": True,
    },
    {
        # Article<->Page taxonomy consistency (2026-07-05): a feature /learn/ article's
        # stated taxonomy-noun counts (disciplines/calculators/templates/tabs/...) must
        # match its OWN linked feature page. Catches the class content_grounding's
        # capability_drift misses: the eng-calculators article claimed "6 disciplines /
        # 30+ calculators" vs the page's 6 real disciplines + 53 calcs; skill-matrix says
        # "4 of 6 disciplines" vs the page's "5 disciplines". Prose-only, deterministic,
        # offline (~1s) → runs even in --fast. Forward-only ratchet.
        "id":      "article-taxonomy",
        "script":  "tools/validate_article_taxonomy.py",
        "args":    [],
        "label":   "Content Grounding: Article<->Page Taxonomy Consistency (forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "article_taxonomy.json",
        "skip_if_fast": False,
    },
    {
        # SEO Technical Gate (SEO/AEO/GEO Arc P1) — the on-page technical levers
        # validate_seo.py lacks: exactly-one-H1, <img> alt, JSON-LD VALIDITY (parses,
        # not just present), and no NEW retired FAQ/HowTo rich-result schema (Google
        # retired both). Surface list is CATALOG-DERIVED, so a new /learn article is
        # covered automatically (validate_seo.py's hand-list had drifted 28-vs-38).
        # Ratcheted forward-only; PASS on pre-existing, FAIL on NEW drift. ~1s offline.
        "id":      "seo-technical",
        "script":  "tools/seo_technical_gate.py",
        "args":    [],
        "label":   "SEO Technical Gate (P1: one-H1 + img-alt + JSON-LD validity + no-new-retired-schema; catalog-derived surfaces; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "seo_technical_report.json",
        "skip_if_fast": True,
    },
    {
        # Extractability Gate (SEO/AEO/GEO Arc P2) — the on-page AEO/GEO levers
        # WorkHive fully controls (Princeton/SIGKDD GEO triad): a crisp answer-first
        # opener + >=1 statistic + >=1 cited source per /learn article. Catalog-derived,
        # ratcheted forward-only (a new article can't ship without the levers). ~1s offline.
        "id":      "extractability",
        "script":  "tools/extractability_gate.py",
        "args":    [],
        "label":   "Extractability Gate (P2: answer-first + statistic + cited-source per article; Princeton GEO triad; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "extractability_report.json",
        "skip_if_fast": True,
    },
    {
        # Landing Extractability Gate (SEO/AEO/GEO Arc P2.5) — the layer the article
        # gates missed: index.html is the ONLY index,follow page, yet its richest
        # content (the 28-tool catalog) + most internal tool links live ONLY inside a
        # JS stageData popup (innerHTML on click), invisible to AI crawlers (don't run
        # JS) and to Googlebot (never fires the click). Measures the SCRIPT-STRIPPED DOM
        # — what a crawler actually sees (display:none in-DOM links DO count). Catalog-/
        # stageData-derived, ratcheted forward-only. Detection-only. ~1s offline.
        "id":      "landing-extractability",
        "script":  "tools/landing_extractability_gate.py",
        "args":    [],
        "label":   "Landing Extractability Gate (P2.5: catalog tool-page links + popup copy/links crawlable + featureList; script-stripped DOM; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "landing_extractability_report.json",
        "skip_if_fast": True,
    },
    {
        # Programmatic calculator-page gate (SEO_AEO_GEO_STRATEGY_V2.md, Pillar 3) — the
        # 58 engineering calculators are the largest programmatic-SEO surface WorkHive
        # owns, but they live ONLY in a noindex client-side JS app (engineering-design.js),
        # invisible to AI crawlers that fetch-but-don't-execute JS
        # (external-ai-crawlers-fetch-but-do-not-execute-javascript-). This gate proves
        # every generated /tools/<calc>-calculator/ STATIC page is crawlable (numeric
        # worked result in raw HTML, not JS-injected), answer-first (a .answer-first stat
        # right after the H1), schema-valid (SoftwareApplication + FAQPage + HowTo),
        # non-orphan (>=2 internal /learn|/tools links), and non-thin. Scans
        # seo_assets/calc_pages_staging/ (pre-ship) + any shipped tools/*-calculator/.
        # PASSES when no pages exist yet (nothing to gate). Static, offline (~1s).
        # Self-test: `python tools/validate_calc_pages.py --self-test`.
        "id":      "calc-pages",
        "script":  "tools/validate_calc_pages.py",
        "args":    [],
        "label":   "Programmatic Calculator-Page Gate (Pillar 3: crawlable + answer-first + SoftwareApplication/FAQPage schema + non-orphan; static HTML for AI crawlers)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # AIO-readiness gate (SEO_AEO_GEO_V3 §5) — the instrument for the pillar V3 split
        # out of GEO. Three of AIO's four levers were already built; the fourth,
        # MULTI-SOURCE CREDIBILITY, is the one our own pages cannot supply, and nothing
        # measured it. At first run every cluster pillar NAMED standards bodies in prose
        # (SMRP, ISO, DOE, NFPA) and LINKED zero of them — and an AI Overview assembles
        # agreeing INDEPENDENT sources, so a cluster whose every link points back to
        # workhiveph.com reads as one unverified source. Hence the bar is LINKED, not
        # named: >=2 independent external domains per pillar, plus schema completeness and
        # a resolvable Organization entity. `entity_resolvable` is the one component no
        # local work can move (it needs real profile URLs in index.html's sameAs), which is
        # why it is reported separately rather than folded into a single score. Ratcheted
        # forward-only — the honest initial state was mostly failing, and a permanently-red
        # gate teaches everyone to ignore it. Static, offline (~0.1s).
        # Self-test: `python tools/aio_readiness_gate.py --self-test`.
        # Calculator-claim consistency (SEO_AEO_GEO_V3 §5, AIO multi-source credibility).
        # Nothing checked whether the numbers WorkHive states ABOUT ITSELF are true, so
        # they drifted while every other gate stayed green: the public surface claimed
        # 51, 53, 58 AND 60 calculators across 6, 7 and 8 disciplines — four sizes for one
        # product — with the calculator guide contradicting ITSELF (60 in four places, 53
        # in three) and its visible "Updated 17 May" disagreeing with its own dateModified
        # of 5 August. That is a GEO defect, not a typo: an answer engine quotes a specific
        # figure, and the AIO pillar is scored on multi-source credibility. A site that
        # cannot corroborate its own headline number gives an engine no way to pick the
        # right one, so the safe move for the engine is to cite somebody else.
        # TWO SSOTs, kept separate because they count DIFFERENT THINGS: the workbench
        # registry (CALC_TYPES_UI, 55/6) and the standalone pages (CALC_DATA, 60/8). A page
        # may quote either, so the gate does not demand one number — it demands that every
        # number quoted matches a DERIVED one. Word forms count too ("Six disciplines"),
        # because the drift hid in both forms; and a discipline claim only counts when
        # calculator language sits within 120 chars, so skill-matrix's "5 disciplines"
        # (about skills) is not swept in. Static, offline (~1s).
        # Self-test: `python tools/calc_claim_consistency_gate.py --self-test`.
        "id":      "calc-claim-consistency",
        "script":  "tools/calc_claim_consistency_gate.py",
        "args":    [],
        "label":   "Calculator-Claim Consistency Gate (V3 §5: every self-reported calculator/discipline count matches a derived SSOT; visible Updated == schema dateModified; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "calc_claim_consistency_report.json",
        "skip_if_fast": True,
    },
    {
        # AIO scores MULTI-SOURCE CREDIBILITY, and aio-readiness enforces that a source be
        # LINKED rather than merely named. Nothing checked that the link still RESOLVES. A
        # citation pointing at a 404 is worse than no citation: it is a claim a reader can
        # disprove in one click, on exactly the pages built to look authoritative.
        #
        # NOT a naive status check, and that distinction is the whole design. First
        # measurement returned 5 of 14 authority URLs as 403 -- dole.gov.ph, erc.gov.ph,
        # iso.org, and both officialgazette.gov.ph laws. Reporting those as broken would
        # have been a FABRICATED finding: re-checked through a real browser every one
        # returned a page titled "Just a moment...", which is Cloudflare's bot challenge,
        # and iec.ch returned CloudFront's block page. They are alive for a human and
        # closed to a robot. So 404/410 counts as DEAD; 403/429/503 carrying a challenge
        # marker is SHIELDED and reported but never failed; a network error is never DEAD,
        # because that may be our connection rather than their site.
        #
        # ADVISORY (exit 0 unless a link is provably dead AND new). A gate that reaches the
        # network flakes under load, and a check that reddens for something the author
        # cannot fix is one the team learns to ignore -- this repo has both precedents.
        # Current state: 15 citations, 9 alive, 6 shielded, 0 dead.
        # Self-test: `python tools/authority_link_gate.py --self-test`.
        "id":      "authority-links",
        "script":  "tools/authority_link_gate.py",
        "args":    [],
        "label":   "Authority-link resolution (V3 5, AIO: every outbound citation still resolves; "
                   "404/410 = dead and ratcheted, bot-challenge 403 = shielded and reported, never failed)",
        "group":   "AI Validation",
        "report":  "authority_link_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "aio-readiness",
        "script":  "tools/aio_readiness_gate.py",
        "args":    [],
        "label":   "AIO-Readiness Gate (V3 §5: per-pillar schema + >=2 LINKED independent sources + resolvable entity; multi-source credibility for AI Overviews/Copilot; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "aio_readiness_report.json",
        "skip_if_fast": True,
    },
    {
        # CTA gate (SEO_AEO_GEO_V3 §6, SXO) — the last post-click lever with no instrument.
        # Winning the citation is the expensive half; a visitor who arrives from an AI
        # answer, reads to the end and finds nowhere to go is the cheapest loss on the
        # board. Nothing noticed that 19 of 53 /learn articles ended with no next action.
        # What counts is an IN-BODY link to an ACTION surface (a WorkHive tool or the join
        # flow). Deliberately excluded: links to other articles (a good next read is not a
        # next action) and header/footer chrome — every page carries those, so counting
        # them would score 100% and measure nothing, which is the failure mode this gate
        # exists to avoid. Ratcheted forward-only. Static, offline (~0.8s).
        # Self-test: `python tools/cta_gate.py --self-test`.
        "id":      "cta",
        "script":  "tools/cta_gate.py",
        "args":    [],
        "label":   "CTA Coverage Gate (V3 §6 SXO: every public content page offers a next ACTION; in-body tool/join links only, chrome and cross-article links excluded; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "cta_report.json",
        "skip_if_fast": True,
    },
    {
        # Content-surface mobile gate (SEO_AEO_GEO_V3 §6, SXO) — validate_mobile.py targets
        # the APP pages and always has; its checks (safe-area insets, overscroll
        # containment, animation cascade) are about a stateful JS surface and
        # false-positive on static articles, so extending it was the wrong move. This is
        # the content-surface equivalent: viewport, responsiveness, >=16px body prose, and
        # no fixed layout width outside a media query — the four properties that decide
        # whether a phone visitor arriving from a search result can read the page at all.
        # Three false-positive classes are pinned by its self-test after measuring produced
        # each one: `\bwidth` matching inside `max-width` (27 phantom defects, every one an
        # @media breakpoint), 11px on an eyebrow LABEL read as unreadable body text, and
        # judging Tailwind responsiveness by @media (27/114 on a site built from one shared
        # responsive template). Ratcheted forward-only. Static, offline (~0.7s).
        # Self-test: `python tools/content_mobile_gate.py --self-test`.
        "id":      "content-mobile",
        "script":  "tools/content_mobile_gate.py",
        "args":    [],
        "label":   "Content-Surface Mobile Gate (V3 §6 SXO: viewport + responsive + >=16px prose + no fixed width across /learn and /tools; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "content_mobile_report.json",
        "skip_if_fast": True,
    },
    {
        # Content-freshness census (SEO_AEO_GEO_STRATEGY_V2 §4.3) — ADVISORY, always exit 0.
        # Perplexity cites content updated within the last 30 days 82% of the time, and the
        # refresh cadence was prescribed with nothing measuring it, so it silently did not
        # happen: at first run 43 of 53 articles were 31-90 days old and only 10 were inside
        # the window. Staleness tiers differ by what the page is for (pillar/comparison 30d,
        # article 90d, calculator 365d — a formula and a standard do not move).
        # DELIBERATELY NOT A FAILING CHECK: a blocking gate here would push toward bumping
        # `dateModified` without editing the content, which is a lie to the crawler, is
        # trivially detectable by diffing against the previous crawl, and burns exactly the
        # trust the freshness signal measures. It ranks the refresh queue so a REAL refresh
        # lands where it pays most. Static, offline (~0.3s).
        # Self-test: `python tools/content_freshness_gate.py --self-test`.
        "id":      "content-freshness",
        "script":  "tools/content_freshness_gate.py",
        "args":    [],
        "label":   "Content-Freshness Census (V2 §4.3: staleness per page against tiered targets, ranked refresh queue; ADVISORY — never fails, so it cannot incentivise a fake date bump)",
        "group":   "AI Validation",
        "report":  "content_freshness_report.json",
        "skip_if_fast": True,
    },
    {
        # Landing + Home-Dashboard Deep Arc gate (LANDING_DASHBOARD_DEEP_ARC.md) —
        # locks the confirmed front-door defects the arc fixed so they cannot
        # regress: (G6/F4) og:title + twitter:title must equal <title> (the arc
        # found the social cards silently dropped "Filipino"); (AI3) no fabricated
        # "NN% precision/accuracy" metric in the marketing copy (found "98%
        # precision" on AI worker-matching with zero source behind
        # v_worker_assignment_truth); (F4) every landing subdir carries a
        # twitter:card (feedback/ shipped without one). Static, offline (~1s) →
        # runs even in --fast. Forward-only ratchet.
        "id":      "landing-deep",
        "script":  "tools/validate_landing.py",
        "args":    [],
        "label":   "Landing Deep-Arc Gate (title-token consistency + no-fabricated-metric + subdir twitter:card; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "landing_validation.json",
        "skip_if_fast": False,
    },
    {
        # CSP Hardening ratchet (CSP_HARDENING_ARC.md, Landing arc I6a). Ian chose the
        # STRICT + nonce path (a multi-phase build: server nonce + inline-handler removal +
        # Tailwind-CDN->built-CSS + strict CSP). Until it lands, this FREEZES the inline-
        # handler + un-nonced-script debt on index.html (baseline 46 handlers / 12 scripts):
        # the counts may only SHRINK, never grow (a new onclick= makes the eventual strict
        # CSP strictly harder). Also reports the measured distance to strict-CSP-done. Flip
        # STRICT_ENFORCED=True in the script when the build completes. Static, offline (~1s).
        "id":      "csp-ratchet",
        "script":  "tools/validate_csp.py",
        "args":    [],
        "label":   "CSP Hardening Ratchet (index.html inline-handler/un-nonced-script debt frozen; forward-only toward strict CSP)",
        "group":   "AI Validation",
        "report":  "csp_baseline.json",
        "skip_if_fast": False,
    },
    {
        # Orphan / click-depth (SEO/AEO/GEO Arc, Crawl-index cell) — every public surface
        # must have >=1 crawlable inbound <a href> (no orphans) AND be reachable from
        # index.html in <=3 clicks. Builds the internal-link graph from the SCRIPT-STRIPPED
        # DOM (crawler view) over the catalog-derived public surfaces; BFS from the homepage.
        # Pure-code, ratcheted forward-only. ~1s offline.
        "id":      "orphan-depth",
        "script":  "tools/orphan_depth_gate.py",
        "args":    [],
        "label":   "Orphan / Click-Depth Gate (P1: 0 orphaned public pages + every page <=3 clicks from index.html; crawl-graph over script-stripped DOM; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "orphan_depth_report.json",
        "skip_if_fast": True,
    },
    {
        # Core Web Vitals (SEO/AEO/GEO Arc, P3 CWV cell) — the FAST half of a two-tool scorer:
        # tools/cwv_probe.mjs drives headless mobile Chromium across the public surfaces (warm-
        # median LCP/INP/CLS, written to cwv_measurements.json), and THIS gate ratchets the
        # measurements (LCP<=2500/INP<=200/CLS<=0.1 + a coverage check). Sub-second (reads JSON);
        # the heavy browser sweep is on-demand. Forward-only ratchet. ~0.1s offline.
        "id":      "cwv",
        "script":  "tools/cwv_gate.py",
        "args":    [],
        "label":   "Core Web Vitals Gate (P3: warm-median mobile LCP/INP/CLS over public surfaces vs 2026 thresholds + coverage; reads cwv_measurements.json; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "cwv_report.json",
        "skip_if_fast": True,
    },
    {
        # Platform Name Alignment (Platform Alignment Arc) — the PAGES are the naming
        # ground truth; this flags every feature whose catalog name or landing-popup
        # name has drifted from the page's own <title>. (Root: the catalog name field
        # is hand-authored intel that drifted — schema_featurelist passes against the
        # stale catalog while the displayed names are stale-vs-reality.) Ratcheted
        # forward-only; burns down via the page-truth re-anchor + surface sync. ~1s.
        "id":      "name-alignment",
        "script":  "tools/platform_name_alignment.py",
        "args":    [],
        "label":   "Platform Name Alignment (page <title> = authority; catalog/popup name drift; forward-only ratchet)",
        "group":   "AI Validation",
        "report":  "platform_name_alignment_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "data-governance-kb",
        "script":  "validate_data_governance.py",
        "args":    [],
        "label":   "Data Governance Validator (ownership, metadata, write path, versioning)",
        "group":   "Platform",
        "report":  "data_governance_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ai-data-pipeline",
        "script":  "validate_ai_data_pipeline.py",
        "args":    [],
        "label":   "AI Data Pipeline Validator (stale data, silos, latency, observability)",
        "group":   "Platform",
        "report":  "ai_data_pipeline_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "data-quality",
        "script":  "validate_data_quality.py",
        "args":    [],
        "label":   "Data Quality Validator (duplicates, incomplete, bias, inconsistent formats)",
        "group":   "Platform",
        "report":  "data_quality_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "analytics",
        "script":  "validate_analytics.py",
        "args":    [],
        "label":   "Analytics Engine Validator (4-layer: HTML + Edge + Python + AST)",
        "group":   "Platform",
        "report":  "analytics_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "project-manager",
        "script":  "validate_project_manager.py",
        "args":    [],
        "label":   "Project Manager Validator (4-layer: HTML + Edge + Python + Smoke)",
        "group":   "Platform",
        "report":  "project_manager_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ml-layer",
        "script":  "validate_ml.py",
        "args":    [],
        "label":   "ML Layer Validator (5-layer: features + API + artifacts + edge fns + UI)",
        "group":   "Platform",
        "report":  "ml_report.json",
        "skip_if_fast": False,
    },
    # ── Analytics Live Integration Test ──────────────────────────────────────
    {
        "id":      "analytics-live",
        "script":  "validate_analytics_live.py",
        "args":    [],
        "label":   "Analytics Live Test (L4 — deployed endpoint, all 4 phases)",
        "group":   "Platform",
        "report":  "analytics_live_report.json",
        "skip_if_fast": True,   # skip with --fast
    },
    # ── Engineering Calc Integration Test (Layer 3) ───────────────────────────
    {
        "id":      "calc-integration",
        "script":  "validate_integration.py",
        "args":    [],
        "label":   "Calc Integration Test (L3 — live edge function)",
        "group":   "Engineering Calculator",
        "report":  None,
        "skip_if_fast": True,   # skip with --fast
    },
    {
        "id":      "playwright-staleness",
        "script":  "validate_playwright_staleness.py",
        "args":    [],
        "label":   "Playwright Staleness Gate (L13 — walkthrough coverage + finding closure + chip assertions)",
        "group":   "Platform",
        "report":  "playwright_staleness_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "companion-page-coverage",
        "script":  "validate_companion_page_coverage.py",
        "args":    [],
        "label":   "Companion Launcher Page Coverage (L0 — every nav-hub page has companion-launcher.js)",
        "group":   "Platform",
        "report":  "companion_page_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "companion-source-coverage",
        "script":  "validate_companion_source_coverage.py",
        "args":    [],
        "label":   "Companion Source Coverage (L0 — the Sources Gateway: every v_*_truth view triaged in companion_source_registry.json)",
        "group":   "Platform",
        "skip_if_fast": False,
    },
    {
        "id":      "voice_kb_context",
        "script":  "tools/validate_voice_kb_context.py",
        "args":    [],
        "label":   "AI Self-Improvement: Voice Kb Context",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "voice_alert_order",
        "script":  "tools/validate_voice_alert_order.py",
        "args":    [],
        "label":   "AI Self-Improvement: Voice Alert Order",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "voice_response_latency",
        "script":  "tools/validate_voice_response_latency.py",
        "args":    [],
        "label":   "AI Self-Improvement: Voice Response Latency",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "response_format_validation",
        "script":  "tools/validate_response_format_validation.py",
        "args":    [],
        "label":   "AI Self-Improvement: Response Format Validation",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "data_completeness",
        "script":  "tools/validate_data_completeness.py",
        "args":    [],
        "label":   "AI Self-Improvement: Data Completeness",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "calc_formula_accuracy",
        "script":  "tools/validate_calc_formula_accuracy.py",
        "args":    [],
        "label":   "AI Self-Improvement: Calc Formula Accuracy",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "calc_live_value",
        "script":  "tools/validate_calc_live_value.py",
        "args":    [],
        "label":   "Arc Q: Calc LIVE Value-at-the-Glass (63/63 types — running API serves the standard-correct number)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,   # needs the python-api container up (live HTTP)
    },
    {
        "id":      "engines_live_value",
        "script":  "tools/validate_engines_live_value.py",
        "args":    [],
        "label":   "Arc Q: Engines LIVE Value-at-the-Glass (analytics MTBF/OEE/MTTR + reliability P-F + projects EVM/CPM — running API serves standard-correct)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,   # needs the python-api container up (live HTTP)
    },
    {
        "id":      "calc_api_serializable",
        "script":  "tools/validate_calc_api_serializable.py",
        "args":    [],
        "label":   "Calc API JSON-Serializability (numpy-500 / silent-TS-fallback class)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "engdesign_registry",
        "script":  "tools/validate_engdesign_registry.py",
        "args":    [],
        "label":   "Deep-arc P1: Engineering-Design registry SSOT (counts derive from CALC_TYPES_UI; no drift/orphans)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "engdesign_write_isolation",
        "script":  "tools/validate_engdesign_write_isolation.py",
        "args":    [],
        "label":   "Deep-arc P2/I-2: engineering_calcs WRITE-side isolation (foreign auth_uid insert blocked, self allowed) — LIVE",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,   # needs the local supabase_db_workhive container
    },
    {
        "id":      "engdesign_a11y",
        "script":  "tools/validate_engdesign_a11y.py",
        "args":    [],
        "label":   "Deep-arc P3: Engineering-Design report a11y (contrast U-1/U-4, 44px U-6, label/announce passes U-2/U-3/U-8)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "engdesign_xss",
        "script":  "tools/validate_engdesign_xss.py",
        "args":    [],
        "label":   "Deep-arc P4: Engineering-Design XSS/output-encoding (no row-into-onclick I-7, no raw narrative I-6, validateBeforeSave I-5)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "engdesign_ai",
        "script":  "tools/validate_engdesign_ai.py",
        "args":    [],
        "label":   "Deep-arc P5: Engineering-Design AI egress integrity (timeout AI-4, honest copy AI-5, disclosure AI-7, friendly errors AI-8, narration grounding AI-1)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "engdesign_ai_citations",
        "script":  "tools/validate_engdesign_ai_citations.py",
        "args":    [],
        "label":   "Deep-arc P5/AI-6: Engineering-Design AI citation grounding (fabricated-standard detector w/ teeth; live narratives cite only real standard families)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,   # live tier needs the edge runtime up; hermetic teeth run regardless
    },
    {
        "id":      "engdesign_silent_zero",
        "script":  "tools/validate_engdesign_silent_zero.py",
        "args":    [],
        "label":   "Deep-arc P7/F-5: Engineering-Design silent-zero ratchet (result `|| 0` alias-chains can't grow past baseline)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "engdesign_units",
        "script":  "tools/validate_engdesign_units.py",
        "args":    [],
        "label":   "Deep-arc P6/A-6: Engineering-Design scoped SI/IP units toggle (engine normalized to SI; universal/ambiguous units excluded)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "analytics_correctness",
        "script":  "tools/validate_analytics_correctness.py",
        "args":    [],
        "label":   "AI Self-Improvement: Analytics Engine Value Accuracy",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Anti-drift lock for ANALYTICS_UFAI_ROADMAP.md's 97.2% scoreboard: re-asserts the
        # code-side invariant behind every dim driven to 100% (CLS reserves, 44px targets,
        # tabular nums, reduced-motion, i18n wiring, h2 headings, Pareto encoding, and the
        # ISO8601 date parse that stopped a silent 99.4% row loss). A doc drifts; a gate does not.
        "id":      "analytics_ufai_scoreboard",
        "script":  "tools/validate_analytics_ufai_scoreboard.py",
        "args":    [],
        "label":   "UFAI: Analytics Engine scoreboard invariants (anti-drift lock)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "projects_correctness",
        "script":  "tools/validate_projects_correctness.py",
        "args":    [],
        "label":   "AI Self-Improvement: Project Manager EVM + CPM Value Accuracy",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "reliability_correctness",
        "script":  "tools/validate_reliability_correctness.py",
        "args":    [],
        "label":   "AI Self-Improvement: Reliability P-F Interval Value Accuracy",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc F (Python Compute API UFAI) B1 keystone — edge↔python shared-secret gate.
        "id":      "python_api_auth",
        "script":  "tools/validate_python_api_auth.py",
        "args":    ["--self-test"],
        "label":   "Arc F: Python API auth gate (edge↔python shared secret)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc F B3 — whole-API supply-chain gate (generalizes validate_ml_deps + pip-audit CVE scan).
        "id":      "python_api_deps",
        "script":  "tools/validate_python_api_deps.py",
        "args":    [],
        "label":   "Arc F: Python API supply-chain (hard-import declaration + CVE scan)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc H H2 — voice-router DETERMINISTIC routing/tool-selection oracle. Hermetic value-oracle
        # (no seeder/model/DB): runs tests/voice-router-determinism.spec.ts against the REAL exported
        # core (kind allowlist, confidence clamp [0,1], slot-fill guard, asset disambiguation). The
        # edge fn imports the same _shared/voice-router-core.ts the test exercises — one source, zero drift.
        "id":      "voice_router_oracle",
        "script":  "validate_voice_router_oracle.py",
        "args":    [],
        "label":   "Arc H: Voice-router determinism oracle (routing/tool-selection value-correctness)",
        "group":   "AI Validation",
        "report":  "voice_router_oracle_report.json",
        "skip_if_fast": False,
    },
    {
        # Arc H H2/F — voice-router slot-fill guard LIVE end-to-end (real JWT+LLM invoke of voice-action-router):
        # no confident asset-required intent without a resolved asset (the A3 junk-write protection). $0 free-tier;
        # needs the seeder + edge runtime, so skip in --fast.
        "id":      "voice_router_live",
        "script":  "validate_voice_router_live.py",
        "args":    [],
        "label":   "Arc H: Voice-router LIVE slot-fill guard (no junk-write; runtime path)",
        "group":   "AI Validation",
        "report":  "voice_router_live_report.json",
        "skip_if_fast": True,
    },
    {
        # Arc G G1 — SECURITY DEFINER tenant-gate (DEFINER bypasses RLS; FORCE-RLS=0). Needs the
        # live DB (docker exec psql), so skip in --fast; the readiness gate requires it in full mode.
        "id":      "definer_tenant_gate",
        "script":  "tools/validate_definer_tenant_gate.py",
        "args":    [],
        "label":   "Arc G: DEFINER tenant-gate (no un-gated cross-tenant DEFINER mutator)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc G G2 — legacy always-true RLS policy detector (auth-migration enforcement gap).
        # Forward-only DOWN ratchet: regression (a NEW exposed table) fails; needs live DB.
        "id":      "rls_permissive_bypass",
        "script":  "tools/validate_rls_no_permissive_bypass.py",
        "args":    [],
        "label":   "Arc G: RLS no-permissive-bypass (legacy USING(true) tenant-isolation gap)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc G G4 — VIEW security_invoker (read-path twin of the DEFINER IDOR). A non-security_invoker
        # view runs as its BYPASSRLS owner and leaks every hive's rows. Needs live DB; skip in --fast.
        "id":      "view_security_invoker",
        "script":  "tools/validate_view_security_invoker.py",
        "args":    [],
        "label":   "Arc G: view security_invoker (no view bypasses base-table RLS = cross-tenant read)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc G G2 — LIVE per-table two-tenant RLS isolation (a member sees 0 of another hive's rows).
        # Needs the live DB; by-design cross-hive tables (community/feedback/admin-analytics) are curated.
        "id":      "rls_tenant_isolation",
        "script":  "tools/validate_rls_tenant_isolation.py",
        "args":    [],
        "label":   "Arc G: RLS tenant isolation (live two-tenant; member sees 0 cross-hive rows)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc G G1 — non-RLS hive-scoped table detector (RLS entirely disabled). DOWN ratchet @0;
        # a NEW non-RLS hive table regresses. Needs live DB.
        "id":      "rls_coverage",
        "script":  "tools/validate_rls_coverage.py",
        "args":    [],
        "label":   "Arc G: RLS coverage (no hive OR personal auth_uid table ships with RLS disabled)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc G G4 — v_*_truth views must be security_invoker so base-table RLS applies to the READ path
        # (an owner-running truth view bypasses RLS = cross-tenant read). Baseline 0; needs live DB.
        "id":      "truth_view_security_invoker",
        "script":  "tools/validate_truth_view_security_invoker.py",
        "args":    [],
        "label":   "Arc G: truth-view security_invoker (read path respects RLS, no view RLS-bypass)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc G G3 (U/F) — RPC return-shape consumer contract: no app RPC returns bare record/SETOF record
        # without OUT/TABLE params (opaque to PostgREST/callers). Baseline 0 opaque; needs live DB.
        "id":      "rpc_return_shape",
        "script":  "tools/validate_rpc_return_shape.py",
        "args":    [],
        "label":   "Arc G: RPC return-shape (no opaque record returns; introspectable consumer contract)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc H H1 — AI retrieval tenant-isolation (OWASP LLM08): a user-callable SECURITY DEFINER retrieval
        # RPC that filters by a client hive_id/auth_uid must self-gate membership (DEFINER bypasses RLS).
        # Baseline 0; also catches the PUBLIC-default grant blind spot. Needs live DB.
        "id":      "ai_retrieval_isolation",
        "script":  "tools/validate_ai_retrieval_isolation.py",
        "args":    [],
        "label":   "Arc H: AI retrieval isolation (no cross-tenant DEFINER read/vector IDOR; LLM08)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc J keystone — Realtime subscription isolation (read-path twin of the DEFINER/view IDOR).
        # The publication is the live broadcast set; channel-name + client `filter` are NOT boundaries —
        # the table's SELECT RLS policy is. A published table with RLS-off or an always-true policy is a
        # cross-tenant LIVE stream. Forward-only DOWN ratchet; needs live DB; skip in --fast.
        "id":      "realtime_subscription_isolation",
        "script":  "tools/validate_realtime_subscription_isolation.py",
        "args":    [],
        "label":   "Arc J: realtime subscription isolation (no realtime-published table streams to anon)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc J/J7 — auth-migration completion: an unauthenticated (anon) client must read 0 rows from
        # every core hive table (DB enforcement proof, positive twin of rls_no_permissive_bypass) AND every
        # production hive-read page must establish a session. Forward-only; needs live DB; skip in --fast.
        "id":      "anon_key_retirement",
        "script":  "tools/validate_anon_key_retirement.py",
        "args":    [],
        "label":   "Arc J/J7: anon-key retirement (anon reads 0 hive rows; pages session-gated)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc H H7/I — every frontend-direct LLM edge fn must rate-limit (OWASP LLM10 Unbounded
        # Consumption). Static gate; gateway-fronted fns exempt (rate-limited upstream). Baseline 0.
        "id":      "ai_rate_limit_coverage",
        "script":  "tools/validate_ai_rate_limit_coverage.py",
        "args":    [],
        "label":   "Arc H: AI rate-limit coverage (no unbounded frontend-direct LLM call; LLM10)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (Z, OWASP A10) — no unguarded user/tenant-controlled outbound fetch. The shared
        # _shared/ssrf-guard.ts (safeFetch) must wrap every tenant-URL fetch (cmms-sync /
        # cmms-push-completion endpoint_url, equipment-label-ocr image_url). Static; teeth.
        "id":      "ssrf_egress",
        "script":  "tools/validate_ssrf_egress.py",
        "args":    [],
        "label":   "Arc R: SSRF egress (tenant-controlled fetch routed through ssrf-guard; A10)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (Z, OWASP A01) — every verify_jwt=false edge fn that calls an LLM must enforce its
        # OWN guard (auth OR rate-limit). Catches the open-LLM-proxy class (voice-model-call /
        # ai-eval-runner / voice-embeddings) the rate-limit-coverage gate missed. Static; teeth.
        "id":      "public_fn_authz",
        "script":  "tools/validate_public_fn_authz.py",
        "args":    [],
        "label":   "Arc R: public-fn authZ (no verify_jwt=false LLM fn without auth/rate-limit; A01)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (Z, OWASP A01) — a security lock must not silently RE-OPEN. Once a migration explicitly
        # revokes anon/authenticated EXECUTE on a fn (a deliberate app-role lock-out), no LATER migration
        # may re-grant it to public/anon/authenticated without a `-- regrant-approved` marker. Catches the
        # exact C2.1 regression (20260624 episodic_supersedes re-granted match_procedural_memories that
        # 20260620 had locked → cross-tenant retrieval IDOR). Static/offline; teeth (self-test).
        "id":      "migration_grant_regression",
        "script":  "tools/validate_migration_grant_regression.py",
        "args":    [],
        "label":   "Arc R: migration grant-regression (a revoked lock not silently re-granted; A01)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (Z, OWASP A01/BFLA) — a verify_jwt=false edge fn that WRITES on the service-role client
        # must enforce an auth/cron/signature gate. Sibling of public_fn_authz (LLM-proxy subset) for the
        # WRITE surface. Caught pdf-ingest = anon-triggerable all-hives drainer (R2, 2026-07-01). Static; teeth.
        "id":      "public_fn_write_authz",
        "script":  "tools/validate_public_fn_write_authz.py",
        "args":    [],
        "label":   "Arc R: public-fn write authZ (no anon-triggerable service-role writer; A01/BFLA)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (X, OWASP A03) — no UNESCAPED worker-writable DB free-text field interpolated into
        # an HTML template (the stored-XSS class validate_xss's presence check misses). Baseline 0; teeth.
        "id":      "dom_xss_fields",
        "script":  "tools/validate_dom_xss_fields.py",
        "args":    [],
        "label":   "Arc R: DOM-XSS DB-field escaping (DB free-text in HTML must be escHtml; A03)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Hive Board Deep Arc (PDDA 2026-07-10) — locks this arc's structural fixes:
        # no write to the DROPPED `assets` table, the #supervisor-summary CLS reserve is
        # role-scoped (no worker void), and the RLS-hardening migration is present.
        "id":      "hive_board",
        "script":  "tools/validate_hive_board.py",
        "args":    [],
        "label":   "Hive Board: asset write on asset_nodes + role-scoped reserve + RLS migration",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (P, OWASP LLM10/A03) — user text entering an LLM prompt must be length-capped
        # (ai-orchestrator/scheduled-agents/walkthrough-analyzer/voice-model-call). Deterministic
        # static gate (never flakes — unlike an LLM-grading gate); locks the caps against regression.
        "id":      "ai_input_caps",
        "script":  "tools/validate_ai_input_caps.py",
        "args":    [],
        "label":   "Arc R: AI input caps (user text length-capped before LLM; LLM10)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Memory hygiene (2026-06-24) — the auto-memory MEMORY.md index must stay under the session
        # LOAD CAP so older memories don't silently truncate at session start. Detect layer of the
        # Prevent(one-liner rule)→Detect(this)→Fix(compact_memory_index.py --apply)→Govern(doctrine)
        # discipline. WARN over the soft budget (still loads); FAIL only over the hard load cap.
        "id":      "memory_index_budget",
        "script":  "tools/compact_memory_index.py",
        "args":    ["--check"],
        "label":   "Memory hygiene: MEMORY.md index under the session load cap (auto-memory)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "gate_milestone_ratchet",
        "script":  "tools/gate_milestone_ledger.py",
        "args":    ["--check"],
        "label":   "GATE MILESTONE RATCHET (Ian, 2026-08-17: 'save for all the platform gates if we achieve "
                   "considerable percentage green, like 80% and 100%'). platform_baseline.json is only "
                   "written when EVERYTHING passes, so the entire climb was unrecorded — there was no way "
                   "to say when the suite reached 80%, nor to notice it slipping back under. This records "
                   "each crossing of 80/90/95/100 with its git SHA, its counts, and the NAMES of every "
                   "failing and skipped gate, then holds it forward-only. ★THE MILESTONE IS CLAIMED ON THE "
                   "STRICT DENOMINATOR (pass / ALL registered), never on pass/ran: the live run reads 572 "
                   "pass · 8 fail · 158 skip of 738, which is 77.5% strict but 98.6% excluding skips — two "
                   "defensible percentages twenty-one points apart from ONE run, where 21% of the suite "
                   "never executed. The lenient figure is recorded beside it, labelled, because it also "
                   "IMPROVES when a gate starts skipping (DB down, tool missing, no baseline): the suite "
                   "goes quieter and the score goes up. Two refusals, both self-tested: a new milestone is "
                   "REFUSED when total_registered SHRANK since the last one (a higher fraction over a "
                   "smaller set is a deletion laundered into an achievement — the exact shape of "
                   "[[feedback_short_denominator_is_a_false_100]] and "
                   "[[feedback_coverage_improved_by_deleting_obligations]]), and --check FAILS both when "
                   "strict green falls below an earned rung AND when the registry shrinks beneath the size "
                   "that rung was earned at, since a percentage-only ratchet would applaud dropping 100 "
                   "gates. Skipped gates are ENUMERATED with reasons, never averaged away "
                   "([[feedback_a_skipped_partition_reads_as_a_covered_one]]). Reads platform_health.json "
                   "that this runner already wrote — it never re-runs the suite. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  "gate_milestone_ledger.json",
    },
    {
        # Marketplace Deepwalk EXPANSION arc (2026-07-24) — the ANTI-DRIFT COMPASS with teeth.
        # The arc grows TWO denominators: journeys (20 x 5 phases G/W/O/H/R, each requiring >=2
        # personas AND >=2 states) and new MK dimension classes (10 x 6 stages). This gate is the
        # FORWARD-ONLY RATCHET: the measured % may only RISE. A dropped journey, a downgraded phase,
        # or a deleted MK class FAILs the build — so the roadmap cannot silently rot into a stale
        # doc (feedback_roadmap_percent_is_the_anti_drift_compass + measured_percent_not_qualitative).
        # Offline + deterministic (pure JSON maths, no DB/network) -> runs in --fast. Self-test:
        # `python tools/marketplace_deepwalk_scoreboard.py --selftest`. Ratchet up with --accept.
        # RENAMED from `marketplace_deepwalk_ratchet`, which this entry SHARED with a different gate
        # (`validate_marketplace_deepwalk.py`, ~230 lines below). Two different scripts under one id collide
        # in the results dict, so whichever ran second overwrote the first — and `gate_efficacy_ledger.json`
        # proves which way it went: its lone `marketplace_deepwalk_ratchet` entry carries the OTHER gate's
        # label, meaning THIS gate's verdict was being discarded from the ledger and could have gone red
        # without registering. Found by the id-uniqueness assertion added to main() in the same pass that
        # removed three exact-duplicate registrations. The other entry keeps the original id so its ledger
        # history stays correctly attached; this one takes a name that says what it is.
        "id":      "marketplace_deepwalk_scoreboard",
        "script":  "tools/marketplace_deepwalk_scoreboard.py",
        "args":    ["--check"],
        "label":   "Marketplace deepwalk expansion %-board (forward-only ratchet: journeys x phases + MK dimension classes may only rise)",
        "group":   "AI Validation",
        "report":  "MARKETPLACE_DEEPWALK_SCOREBOARD.md",
    },
    {
        # The ⑥ R-resolve LOCK for the classes closed by the Marketplace Deepwalk EXPANSION arc
        # (2026-07-24). Each check encodes a live-found defect so a regression FAILs the build instead
        # of waiting for the next deepwalk to rediscover it:
        #   MK2 — all THREE moderation surfaces (platform-actions live; marketplace-admin +
        #         founder-console retired-but-preserved) must capture moderation_reason on reject.
        #         Fixing one and missing another is the J20 divergence this arc exists to catch.
        #   MK7 — no `marketplace.html#listing-<id>` dead deep links (the page only reads ?listing=).
        #   MK3 — the anon-granted public seller RPC must project no messenger_username/hive_id/auth_uid.
        #   MK1 — a star rating must be guarded by a real review count (no unattributable score).
        # Static/offline (file parsing only) -> runs in --fast, never flakes. Self-test: --selftest.
        # MK8 marketplace safety: the ONLY gate modelling adversarial USER BEHAVIOUR rather than
        # adversarial input. This is a contact-only marketplace with no escrow, so a buyer's whole
        # risk sits at the off-platform meet-and-pay step. Three live signals: duplicate/spam
        # listings, price anomalies vs the category median (with a peer-count floor so a thin
        # category is never judged), and copy pressuring payment before inspection - the scam-shaped
        # ask that contradicts the safety guidance the contact step now shows. Integrity-at-zero
        # forward-only. Reads the live DB and SKIPS cleanly when it is down. Self-test: --selftest.
        "id":      "marketplace_fraud_signals",
        "script":  "tools/validate_marketplace_fraud_signals.py",
        "args":    [],
        "label":   "Marketplace fraud signals (MK8: duplicate/spam listings, price anomalies, off-platform payment pressure)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "marketplace_deepwalk_classes",
        "script":  "tools/validate_marketplace_deepwalk_classes.py",
        "args":    [],
        "label":   "Marketplace deepwalk class locks (MK1 attributable trust signals / MK2 moderation reason across all 3 surfaces / MK3 public-RPC PII / MK4 watchlist sold-surface / MK7 no dead deep links / MK8 contact-step safety / MK9 computed response SLA / MK10 ranking disclosure)",
        "group":   "AI Validation",
        "report":  None,
    },
    {
        "id":      "reachable_capability",
        "script":  "tools/validate_reachable_capability.py",
        "args":    [],
        "label":   "MK13 - reachable capability: a page that LISTS table T and shows a forward-looking empty state (\"they will appear here\") must have some client path that can INSERT into T, or the empty state is promising a capability the product does not have. Born from the walk: both admin consoles said \"No open disputes\" for a queue no buyer can ever file into, and the same detector then found marketplace_reviews is SELECT-only everywhere - there is no review-submission UI, so a seller cannot currently earn a rating in-product. Views and reviewed server-fed tables (trigger/edge-fn/cron) are allowlisted, since unwritable-from-the-client is legitimate for those. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        # The SIBLING of reachable_capability, and it exists because that one could not see this.
        # MK13 asks whether an EMPTY STATE promises a capability nothing can produce. It cannot see a
        # REAL control that a full-screen retirement overlay has made unreachable — founder-console.html
        # hid the GCash top-up verify queue, the single control that mints every credit, for ~2 weeks
        # while 5 specs stayed green by calling its handler by hand and ~35 static validators kept
        # passing over its deliberately-preserved markup. Money could not enter the system.
        "id":      "retired_page_sole_control",
        "script":  "tools/validate_retired_page_sole_control.py",
        "args":    [],
        "label":   "Retired-page sole control (retiring a PAGE must not silently delete a capability: "
                   "every write/RPC on an overlay-retired page must also be reachable from a live one; "
                   "forward-only, 2 known strandings printed loudly rather than allowlisted)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # THE BANK'S OWN HONESTY GATE. The live-MCP registry read 343 green / 0 owed and Ian did not
        # believe it; he was right. A green row carried no typed evidence, never expired, and a
        # STRUCTURAL probe ("the page renders") was allowed to satisfy a BEHAVIOURAL oracle ("the
        # number matches its source"). The credits-back chip settled the argument: the surface
        # rendered perfectly while the one number a buyer needs had vanished from every listing.
        # Under this gate the same bank reads 124 green / 240 owed. That is the real starting line.
        "id":      "live_mcp_bank",
        "script":  "tools/validate_live_mcp_bank.py",
        "args":    [],
        "label":   "Live-MCP bank honesty (every non-owed scenario carries typed evidence — kind/ref/"
                   "asserts — and that evidence EXPIRES when a file it depends on changes; forward-only "
                   "ratchet on green, with stale excluded so drift surfaces instead of being absorbed)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # THE EXPIRY RULE ABOVE IS ONLY AS GOOD AS ITS DIGEST, AND THE DIGEST WAS COUNTING PROSE.
        # This bank collapsed four times (752 green -> 34, then 342, ~365, ~320 rows), each read at
        # the time as "shared-library edits are expensive, batch them". They were not. fn_digests v3
        # segments top-level code by counting brackets PER LINE, INSIDE COMMENTS INCLUDED, and this
        # repo's house style names calls in prose. One line -- "// every client routes through getDb(
        # and _timeoutFetch" -- carries an unbalanced paren, glues every following line into a single
        # statement, and vanishes 881 of 881 utils.js keys. 93% of what v3 hashed as that file's
        # "top-level statements" was prose. Writing a SENTENCE ABOUT the code counted as changing it.
        #
        # v4 strips comments (string-aware; and <!-- --> for .html, where the larger half of the bank
        # actually lives) as a NEW VERSION, never a redefinition -- fn_digests_still_hold dispatches
        # on the version each row recorded, because the last unversioned change here expired 661 rows
        # in one run and the ratchet correctly refused it.
        #
        # This test exists because the fix has a converse that is easy to lose and fatal to miss: a
        # v4 that merely stopped expiring would mint FALSE GREENS, which is strictly worse than the
        # over-sensitivity it replaces. So it asserts BOTH directions -- prose expires nothing, real
        # code and real markup edits still expire -- plus version isolation, plus that a narrowed
        # stamp still retains every top-level key (filtering by exercised FUNCTIONS alone silently
        # deleted them once, and a row recording no top-level statement cannot notice a top-level
        # change). It mutates utils.js and marketplace.html and restores them BYTE-for-byte: the
        # ad-hoc probe that found the defect used text mode and converted 4,024 CRLF endings to LF,
        # manufacturing an 18-row phantom regression. Static, offline (~4s).
        "id":      "fn-digest-contract",
        "script":  "tools/test_fn_digest_versions.py",
        "args":    [],
        "label":   "Digest contract (v3 prose defect stays fixed; v4 expires real code AND real markup; "
                   "v3 recordings never reinterpreted; narrowing keeps top-level keys; mutated sources "
                   "restored byte-for-byte)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # ORDER TOTALITY, ASKED OF THE SCHEMA RATHER THAN THE PAGE'S GOOD INTENTIONS. A paginated read
        # whose ORDER BY does not uniquely determine sequence may return the same row on two pages or
        # skip it, because Postgres breaks ties as it pleases between two queries. The codebase already
        # carries the fix in places ("MR4 tiebreaker: the sort above is not total on its own") — this
        # keeps it true everywhere, and keeps it true against the CATALOG: v_pm_compliance_truth,
        # v_pm_scope_items_truth and v_inventory_items_truth are VIEWS with no single-column unique
        # index, so `.order('id')` on them is not self-evidently a tiebreaker.
        # It reports four verdicts, and the middle two are why it is trustworthy: `total` (the last
        # order column is unique per pg_index), `total-in-practice` (no unique tiebreaker but zero ties
        # measured today), `capped-unordered` (a lone `.limit()` row cap — NOT pagination, so no row can
        # double; worth naming, not a defect) and `NON-TOTAL` (`.range(` with ties, or with no order at
        # all). Its first version reported 11 of 11 logbook reads NON-TOTAL; both causes were the lens
        # (a char window that swallowed the next query's `.limit(`, and treating a cap as pagination),
        # which is why the categories are separated now. Runs on psql via docker exec; skips if the DB
        # is down, and needs no browser — the local-substitute path when the Playwright MCP is absent.
        "id":      "order_totality",
        "script":  "tools/prove_order_totality.py",
        "args":    ["--gate"],
        "label":   "Order totality (every paginated read ends in a tiebreaker the CATALOG confirms is "
                   "unique; a lone .limit() cap is reported, not failed)",
        "group":   "Platform",
        "report":  "order_totality_report.json",
        "skip_if_fast": True,
    },
    {
        # THE OTHER HALF OF THE SAME QUESTION. Order totality asks whether the ORDER pins row sequence;
        # this asks whether two executions of the identical read actually agree, byte for byte, over the
        # full ordered projection. The two runs are SEPARATE psql connections on purpose: two CTEs in one
        # statement would let Postgres compute the scan once and compare it with itself, which passes by
        # construction — the same vacuity as an injection that intercepts zero requests.
        # AND IT CARRIES A NON-VACUITY CONTROL, because two identical SELECTs against a database nobody
        # is writing to match almost by definition, so a bare "idempotent" is a pass that cost nothing.
        # The digest must be shown to MOVE: one row vs two rows of the same relation must hash
        # differently, or the reading is REFUSED as control-failed instead of reported. Getting that
        # control right took two corrections — perturbing the read's own `.limit()` compares a
        # `.limit(1)` read with itself, and a cap ABOVE the relation's size (hive_members has 18 rows
        # behind `.limit(500)`) makes limit and limit-1 identical, which produced three false
        # control-failures while the digest was demonstrably sensitive.
        # SCOPE, stated so a pass is not over-read: this measures the DATABASE's answer, not PostgREST's
        # serialisation of it. envelope_shape and status_body_agreement need the REST gateway and stay
        # owed rather than being inferred from here.
        "id":      "read_idempotency",
        "script":  "tools/prove_read_idempotency.py",
        "args":    ["--gate"],
        "label":   "Read idempotency (the same ordered read twice, over two separate connections, "
                   "returns the same bytes — with a proven-sensitive digest, not a vacuous match)",
        "group":   "Platform",
        "report":  "read_idempotency_report.json",
        "skip_if_fast": True,
    },
    {
        # DOES A QUANTITY CARRY ITS UNIT ACROSS THE BOUNDARY? The defect class is a knob set to 10
        # meaning 1000% — a number crossing a seam with its scale left to convention, so two callers can
        # read one column under two conventions and nothing errors. Asks two questions of every column in
        # the money/credits/percent/duration families: is the unit DECLARED (a CHECK bounding the range, a
        # column COMMENT, a sibling unit/uom/currency column, or a name that carries the unit itself), and
        # do the stored values AGREE WITH ONE SCALE — the second asked in SQL, because a percent column
        # holding both (0,1] and >1 values is storing fractions and whole percents in one place, which is
        # the defect rather than a smell.
        # ITS FIRST RUN FLAGGED 21 COLUMNS AND MEANT 7, and the corrections are why it is trustworthy now:
        # a bare `total` matched total_tokens / total_chunks / xp_total (counts, not money), a bare `min`
        # matched min_chars and reward_min_per_listing (a character count and a MINIMUM, not minutes), and
        # a suffix-only self-declaring test called hours_worked / duration_ms / day_count undeclared when
        # the unit is right there in the name. Reading the output against the schema is what took 21 to 7.
        # EXPOSURE IS RANKED rather than binary, because "undeclared" means something different for an
        # empty column than a populated one: a populated one is a LIVE ambiguity, an empty one is latent
        # and the first writer's guess becomes every later reader's inheritance.
        "id":      "units_at_boundary",
        "script":  "tools/prove_units_at_boundary.py",
        "args":    ["--gate"],
        "label":   "Units at the boundary (every money/credit/percent/duration column declares its unit "
                   "by CHECK, comment, sibling column or name — and no column mixes two scales)",
        "group":   "Platform",
        "report":  "units_at_boundary_report.json",
        "skip_if_fast": True,
    },
    {
        # DOES NULL MEAN THE SAME THING ON BOTH SIDES? The oracle names its own defect: NULL meant "no
        # cap" in the DB and arrived as a cap of ZERO in the client. Both halves are measured here.
        # DB SIDE: every view output column whose whole value is a COALESCE, located by a BALANCED-PAREN
        # scan (a character window is defeated by COALESCE(a, COALESCE(b, 0)), exactly as one defeated the
        # order-totality prover). CLIENT SIDE: every `x.col ?? 0` / `|| ''` site that is genuinely a
        # NULLABLE column of a relation THAT PAGE READS — narrowed that far because the 22 pages contain
        # 1,557 matching sites and almost none are this defect (`opts.icon || ''` is a render option,
        # `error.message || ''` is an exception, `target.value || ''` is the DOM).
        # THE VERDICT TURNS ON A PAIR, NOT ON THE PRESENCE OF A COALESCE, because most are correct:
        # COALESCE(count(x), 0) is not a defect — for a count, "no rows" and "zero" are THE SAME FACT.
        # What fails is a substitution a reader cannot distinguish from a real value: avg/min/max
        # defaulted to 0, a timestamp defaulted to now() (absence becomes the FRESHEST POSSIBLE instant,
        # and it moves on every read), a non-zero constant standing in for a missing measurement, or a
        # nullable column defaulted to a value that is legal for its type.
        # THE TEXT IS ONLY A LOCATOR — the DB gives the verdict. count(*) minus count(col) over the view
        # must be 0, or the reading was wrong about that column being on the output path and it is dropped
        # rather than reported (one was). AND A LATENT RISK IS NOT A LIVE DEFECT: the source is counted in
        # SQL, over the WHOLE fallback chain. The first run reported a live collapse on
        # COALESCE(knowledge, problem, action, '') from 43 NULL `knowledge` rows when the default needs
        # all three NULL and that count is zero — one argument tested where three were needed.
        # NON-VACUITY CONTROL: it must find a view column that really does emit a NULL, or a broken query
        # would report "nothing coalesced anywhere" and agree with itself forever.
        # DOES A MULTI-TABLE WRITE LAND WHOLE, OR CAN IT LAND HALF? Two halves, because the oracle has
        # two. STRUCTURE: a page writing two tables through two awaited client calls has no transaction
        # around them, so a 500 or a dead network between them leaves the first applied and the second
        # missing with nothing erroring afterwards; a page that writes one table and lets a TRIGGER or an
        # RPC write the second is atomic by construction. That linkage is READ OUT of pg_trigger and
        # pg_proc (each body scanned for INSERT/UPDATE/DELETE targets), never guessed. LEDGER: structure
        # says a half-write is POSSIBLE, five declared conservation invariants say whether one HAS
        # happened — and only that half is asserted.
        # THE INVARIANTS ARE DECLARED WITH THEIR REASONING, because one wrong entry silently converts a
        # violation into a pass — and two of them exist to record comparisons that MISLEAD.
        # sum(inventory_transactions.qty_change) differs from inventory_items.qty_on_hand on 65 of 81
        # items and that is NOT a partial write (opening balances were seeded without an opening
        # transaction); the invariant that holds is the ledger's own running balance, newest qty_after ==
        # current qty_on_hand, 81 of 81. And reconciling community_xp against achievement_xp_log reports
        # 10 of 10 workers mismatched by up to thousands — a CATEGORY ERROR, they are different ledgers.
        # THIS GATE'S MEASUREMENT FOUND A REAL DEFECT: reply XP was paid by
        # `PERFORM increment_community_xp(...)` with no award row and no reversal, farmable via the REST
        # delete path, feeding a COMMERCIAL trust signal. Closed by 20260812000060 and locked by 7 new
        # checks in validate_community_xp_ledger.py.
        # NON-VACUITY: the linkage detector must SEE community_posts -> community_post_xp_awards, or a
        # detector matching nothing would call every pair on the platform client-sequenced.
        # SCOPE: the pairing is PAGE-level, not ACTION-level, so the client-sequenced count is an upper
        # bound on structural exposure and is REPORTED rather than failed.
        # DOES EVERY FIELD NAME STILL EXIST ON THE RELATION IT IS USED AGAINST? A rename on the DB side
        # that the client still reads by the old name fails differently depending on where the stale name
        # sits, and the worst case is the silent one: in a .select() list or a filter PostgREST 400s the
        # whole request, so ONE stale name blanks an entire panel; in an insert/update object the write
        # 400s and a capture silently fails; but after select('*'), `row.old_name` raises NOTHING — it is
        # undefined, and combined with the `?? 0` / `|| ''` defaults this platform uses it RENDERS AS A
        # REAL VALUE. Names are resolved against the LIVE CATALOG, never a hand-kept list, because a
        # hand-kept list is a second source of truth that drifts from the schema it describes. PostgREST
        # aliases are resolved to the real column (`id:project_id` names project_id).
        # ITS FIRST RUN PRODUCED ONE FINDING AND IT WAS FALSE, which is why it reads code and not prose:
        # it flagged v_project_truth.id, matching an `.order('id')` written inside project-report.html's
        # seven-line comment explaining why that ordering totalises a DIFFERENT query. The
        # paginated-order-totality gate had already been bitten by this exact file, so this imports that
        # gate's line-preserving comment stripper instead of hand-rolling a second one — and stripping
        # RAISED the count 1606 -> 1611, because fixing the chain windows attributed more real filters.
        # NON-VACUITY: a column that cannot exist is injected and must be caught, or a regex that
        # silently matched nothing would report "0 stale" over 0 checked and look perfectly clean.
        # SCOPE: concatenated select lists, embedded resources, and bare row.property reads after
        # select('*') are counted as SKIPPED rather than silently dropped; the last is the known gap.
        # IS WHAT THE CLIENT WRITES WHAT THE DATABASE HANDS BACK? On this platform the interesting answer
        # is not network corruption — it is that 87 BEFORE triggers DELIBERATELY rewrite NEW.<column> on
        # the way in, across 54 relations, so a submitted value can differ from the stored one with no
        # error. The gate therefore asks whether every rewrite is one we can NAME: attribution-rebind (35,
        # a SECURITY feature — a spoofed worker_name/auth_uid is replaced with the caller's own, which
        # also means any surface optimistically echoing the form is showing a value the DB refused),
        # silent-truncation (33), timestamp-stamp (14), status-coercion (2), matching (1),
        # derived-from-row (1), default-fill (1). An UNEXPLAINED rewrite fails.
        # THE LIVE HALF, because a possible truncation and an actual one are different facts: all 106
        # left(NEW.col,N) caps are measured and NONE holds a value at its cap (widest: sow_text 984/4000).
        # CORRECTED TWICE BEFORE ANY ROW WAS BANKED, neither time by widening until green. A name-only
        # classifier called 5 legitimate rewrites UNEXPLAINED, so classification now reads the ASSIGNED
        # EXPRESSION (does it mention now(), is it built from the caller's own NEW columns, is it guarded
        # by a comparison against the column default). Then apply_hive_broadcast_radius matched
        # derived-from-row because service_knob(NEW.hive_id,...) mentions a NEW column — but that label
        # asserts "nothing external overrode their intent" and a hive knob IS external, so default-fill is
        # now tested first. A label that OVERSTATES what was proven is the same defect as a wrong verdict.
        # A FINDING THAT WAS NOT ONE, recorded so it is not re-derived: root_cause is capped at 200 while
        # logbook.html's maxlength=2000 covers only problem/action/knowledge — but f-root-cause is a
        # SELECT, so the cap is unreachable, and the longest stored value is 20 chars.
        # NON-VACUITY: the census must SEE logbook.auth_uid rebound by bind_logbook_submitter. It earned
        # its place immediately — a broken edit dropped the trigger body from the query, the census
        # returned 0, and the control refused to let that report as a clean bill of health.
        # WHAT THE HTTP GATEWAY ACTUALLY RETURNS — and these three oracles sat OWED for sessions on a
        # premise that lived in our own tooling. Two provers documented that they "need the REST gateway,
        # which is not reachable from this shell (kong publishes no host port)", and one copied that
        # sentence into banked evidence. FALSE: docker ps shows 0.0.0.0:54321->8000/tcp and one curl with
        # the publishable key answers 200. A ceiling written into a docstring stops reading like a claim
        # and starts reading like documentation, which is exactly why it survived.
        # Real GETs against the live local gateway, 40 relations, three personas — anon with the
        # publishable key, an AUTHENTICATED member with a minted JWT, and no apikey at all. GET only, so
        # nothing is written. Result: ZERO envelope defects and ZERO status/body disagreements — the error
        # body always carries {code,message,details,hint}, an unknown column is 400 (not 500), a
        # single-object read of 0 rows is 406/PGRST116, and Prefer: count=exact returns Content-Range.
        # THE FINDING IS ABOUT THE CONTRACT, NOT A BUG: on 26 of 40 relations a REFUSAL IS
        # INDISTINGUISHABLE FROM ABSENCE — anon and a member both get 200 and a JSON array, differing only
        # in row count, and a request with NO apikey also gets 200 []. That is PostgREST+RLS by
        # construction (row filtering is not request refusal), so it is reported rather than failed — but
        # it is an obligation every surface inherits: decide refusal-vs-absence from SESSION STATE, or an
        # expired session renders as an empty plant. Some relations answer 42501 instead, which is the
        # behaviour a client can actually act on.
        # THREE NON-VACUITY CONTROLS, all required to pass: an unknown column must 400; community_posts
        # must return rows to anon (it is the public feed, so a 200 [] elsewhere is RLS not a dead key);
        # and authenticated must out-read anon somewhere, without which the headline finding would be
        # vacuously true.
        # ITS OWN FIRST RUN REPORTED TWO DEFECTS THAT WERE BOTH THE PROBE'S FAULT: it queried ?id=eq.<uuid>
        # on community_xp, which is keyed (worker_name, hive_id) and has no id column, and it treated
        # projects' 42501 as a shape violation when a coded refusal is a correct envelope.
        "id":      "http_envelope",
        "script":  "tools/prove_http_envelope.py",
        "args":    ["--gate"],
        "label":   "HTTP envelope at the live gateway (error bodies carry all four keys, status agrees "
                   "with body; refusal-vs-absence measured across anon/member/no-key)",
        "group":   "Platform",
        "report":  "http_envelope_report.json",
        "skip_if_fast": True,
    },
    {
        # THE FOUR BROWSER PROVERS. Everything else in this block reads source or the database; these
        # four need a signed-in page, and each exists because the cheap version of its measurement was
        # WRONG in a way that read green (or read as a defect that was not there).
        # viewport_overflow: the browser's scale factor is measured and divided out — the MCP browser
        # runs at 1.5x and an unscaled read invented 142 of 398 "offenders"; a width it cannot verify
        # is refused rather than reported. It also signs in and ASSERTS THE LANDED URL, because an
        # earlier run measured the sign-in screen for 18 of 22 pages and called it the page.
        "id":      "cj_viewport_overflow",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "viewport_overflow", "--gate"],
        "label":   "CJ ui-layout live (no unclipped horizontal overflow at 390/641/1280 on any of the "
                   "22 production pages; every effective tap target >= 44px, with skipped candidates "
                   "counted WITH their reason — a labelled radio is activated by its label, so "
                   "measuring the 16px box instead of the target fabricated 7 defects)",
        "group":   "Platform",
        "report":  "viewport_overflow_report.json",
        "skip_if_fast": True,
    },
    {
        # Arms its recorder in addInitScript BEFORE first paint. Arming at readyState 'interactive'
        # lost the first ~650ms and banked "no loading state" for components that had one.
        # CL a11y-states, promoted from hand-walks by A16.C4 (rule of two): focus_visible /
        # reduced_motion / icon_only_name / no_raw_enum had been live-MCP-walked at least twice and
        # re-staled on every shared-file edit (~240 rows). The prover measures the four families
        # per page with the recorded instrument traps honoured (trusted Tab because programmatic
        # focus() never matches :focus-visible; focused-vs-unfocused DELTA because a standing
        # box-shadow is not a focus ring; getAnimations() under emulateMedia so the RESULT is
        # measured; CLARITY_PROBE reused for icon-only so there is one classifier, not two).
        "id":      "cl_a11y_states",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "a11y_states", "--gate"],
        "label":   "CL a11y states (focus visible as a style DELTA on sampled trusted-Tab stops; "
                   "reduced-motion honoured as a measured RESULT; icon-only controls carry an "
                   "accessible name; no raw snake_case enum reaches a person)",
        "group":   "Platform",
        "report":  "a11y_states_report.json",
        "skip_if_fast": True,
    },
    # ── SEVEN WALK INSTRUMENTS PROMOTED TO GATES (2026-08-21, the owed-100 drive). Each had been
    # run as a standalone walker and its bank rows re-staled on every shared edit; registration is
    # what lets those rows convert to gate-backed one-way green (A15) and re-earn by RUNNING the
    # gate. retry_path / count_matches_source / effect_visible were given real --gate exit codes at
    # promotion — before that each always exited 0, a gate that could not fail.
    {"id": "cb_did_it_land",     "script": "tools/prove_did_it_land.mjs",     "args": ["--gate"],
     "label": "CB did-it-land (after a slow action the person can tell whether it landed; every mutating request intercepted in-page)",
     "group": "Platform", "report": "did_it_land_report.json", "skip_if_fast": True},
    {"id": "cb_retry_path",      "script": "tools/prove_retry_path.mjs",      "args": ["--gate"],
     "label": "CB retry-path (a failure offers a retry that actually re-attempts and succeeds when the cause is gone)",
     "group": "Platform", "report": "retry_path_report.json", "skip_if_fast": True},
    {"id": "cf_count_matches_source", "script": "tools/prove_count_matches_source.mjs", "args": ["--gate"],
     "label": "CF count-matches-source (a rendered count equals its canonical source read live)",
     "group": "Platform", "report": "count_matches_source_report.json", "skip_if_fast": True},
    {"id": "cb_wrong_then_fix",  "script": "tools/prove_wrong_then_fix.mjs",  "args": ["--gate"],
     "label": "CB wrong-then-fix (an incomplete submit is refused with the reason, nothing is written, and the person's input is KEPT)",
     "group": "Platform", "report": "wrong_then_fix_report.json", "skip_if_fast": True},
    {"id": "cf_effect_visible",  "script": "tools/prove_effect_visible.mjs",  "args": ["--gate"],
     "label": "CF effect-visible (a driven write lands in the DB and shows on the surface; cleanup verified)",
     "group": "Platform", "report": "effect_visible_report.json", "skip_if_fast": True},
    {"id": "cb_what_happens_next", "script": "tools/prove_what_happens_next.mjs", "args": ["--gate"],
     "label": "CB what-happens-next (a confirmation says WHAT happens next and WHEN, not just that something fired)",
     "group": "Platform", "report": "what_happens_next_report.json", "skip_if_fast": True},
    {"id": "cb_double_fire",     "script": "tools/prove_double_fire.mjs",     "args": ["--gate"],
     "label": "CB double-fire (a double-activated control sends ONE write; every mutating request counted and held in-page)",
     "group": "Platform", "report": "double_fire_report.json", "skip_if_fast": True},
    # batch-2 promotions, same drive: exit codes added at promotion (all five always exited 0).
    {"id": "cd_fallback_engaged", "script": "tools/prove_fallback_engaged.mjs", "args": ["--gate"],
     "label": "CD fallback-engaged (with the edge stubbed 500 the page's declared fallback actually engages; no write escapes)",
     "group": "Platform", "report": "fallback_engaged_report.json", "skip_if_fast": True},
    {"id": "cm_number_explained", "script": "tools/prove_number_explained.mjs", "args": ["--gate"],
     "label": "CM number-explained (a derived figure can be explained without leaving the surface)",
     "group": "Platform", "report": "number_explained_report.json", "skip_if_fast": True},
    {"id": "cm_one_vocabulary",  "script": "tools/prove_one_vocabulary.mjs",  "args": ["--gate"],
     "label": "CM one-vocabulary (a concept keeps ONE name across surfaces; MIXED concepts fail)",
     "group": "Platform", "report": "one_vocabulary_report.json", "skip_if_fast": True},
    {"id": "cb_abandon_resume",  "script": "tools/prove_abandon_resume.mjs",  "args": ["--gate"],
     "label": "CB abandon-resume (abandoning midway leaves nothing half-applied; HALF-WRITTEN fails)",
     "group": "Platform", "report": "abandon_resume_report.json", "skip_if_fast": True},
    {"id": "cf_source_chip",     "script": "tools/prove_source_chip.mjs",     "args": ["--gate"],
     "label": "CF source-chip (a chip naming a feed the page never read is a false provenance claim)",
     "group": "Platform", "report": "source_chip_report.json", "skip_if_fast": True},
    {"id": "cm_cost_before_commit", "script": "tools/prove_cost_before_commit.mjs", "args": ["--gate"],
     "label": "CM cost-before-commit (a credit-costing action states its cost BEFORE the person commits)",
     "group": "Platform", "report": "cost_before_commit_report.json", "skip_if_fast": True},
    {"id": "cf_units_visible",   "script": "tools/prove_units_visible.mjs",   "args": ["--gate"],
     "label": "CF units-visible (every examined quantity in each opened view carries its unit on screen)",
     "group": "Platform", "report": "units_visible_report.json", "skip_if_fast": True},
    {"id": "cj_zoom200",         "script": "tools/prove_zoom200.mjs",         "args": ["--gate"],
     "label": "CJ zoom-200 (at 200% zoom content reflows without clipping, sideways scroll, or unreachable controls)",
     "group": "Platform", "report": "zoom200_report.json", "skip_if_fast": True},
    {"id": "co_back_nav",        "script": "tools/prove_back_nav.mjs",        "args": ["--gate"],
     "label": "CO back-nav (browser Back lands where wayfinding says, with and without a referrer)",
     "group": "Platform", "report": "back_nav_report.json", "skip_if_fast": True},
    {"id": "cg_offline_views", "script": "tools/prove_offline_refusal.mjs", "args": ["--case", "contact"],
     "label": "CG offline-views representative case (the full sweep is the per-case loop; each run "
              "updates its cell in offline_refusal_report.json - refused BEFORE firing and SAID so, "
              "with navigator.onLine overridden AND the network cut)",
     "group": "Platform", "report": "offline_refusal_report.json", "skip_if_fast": True},
    {"id": "cm_fallback_views", "script": "tools/prove_fallback_views.mjs", "args": [],
     "label": "CM fallback-views (fallback_engaged per (page,view): when a dialog's OWN edge-fn "
              "primary is answered 500, content still renders AND the view names a stored source "
              "or announces the failure - judged per-sentence against the healthy-open baseline; "
              "views measured to invoke no edge function are not-applicable)",
     "group": "Platform", "report": "fallback_views_report.json", "skip_if_fast": True},
    {"id": "cm_retry_views", "script": "tools/prove_retry_views.mjs", "args": [],
     "label": "CM retry-views (retry_path per (page,view): a dialog whose reads fail paints the "
              "failure AND offers a retry inside the view that actually re-attempts and recovers "
              "once the cause clears; no-read and memory-rendered views are measured "
              "not-applicable)",
     "group": "Platform", "report": "retry_views_report.json", "skip_if_fast": True},
    {"id": "cm_backnav_views", "script": "tools/prove_backnav_views.mjs", "args": [],
     "label": "CM backnav-views (browser Back out of each open dialog leaves no orphaned overlay "
              "and no locked body - judged for history-state sheets (close in place) and plain "
              "sheets (leave cleanly) alike)",
     "group": "Platform", "report": "backnav_views_report.json", "skip_if_fast": True},
    {"id": "cm_number_views", "script": "tools/prove_number_views.mjs", "args": [],
     "label": "CM number-views (number_explained per (page,view): every derived figure in an opened "
              "dialog carries naming context in reach; raw counts explain themselves and figure-less "
              "views are measured not-applicable; the vocab half reports snake_case-on-the-glass as "
              "findings only)",
     "group": "Platform", "report": "number_views_report.json", "skip_if_fast": True},
    {"id": "cm_rate_views", "script": "tools/prove_rate_views.mjs", "args": [],
     "label": "CM rate-views (rate_limit_legible per (page,view): a 429 with Retry-After injected "
              "AFTER the view opens is answered by naming the WAIT, never a bare retry; both "
              "induction paths tried; zero reads = measured not-applicable)",
     "group": "Platform", "report": "rate_views_report.json", "skip_if_fast": True},
    {"id": "cm_cost_views", "script": "tools/prove_cost_views.mjs", "args": [],
     "label": "CM cost-views (what_does_it_cost per (page,view): a view with a commit control states "
              "cost/hold/reward BEFORE the press; a view with none is measured not-applicable by "
              "enumeration; commit-without-vocabulary cells queue for the declared FLOWS list, "
              "never failed on a guess)",
     "group": "Platform", "report": "cost_views_report.json", "skip_if_fast": True},
    {"id": "svc_pane_populated", "script": "tools/prove_services_pane.mjs", "args": [],
     "label": "market_svc populated (the services pane's every visible number attributed to its own "
              "label and matched to the security_invoker view the page reads, under the caller's own "
              "claims, across five personas; the tab driven by CLICK with aria-selected confirmed; "
              "an em-dash badge is a failed read, never 0)",
     "group": "Platform", "report": "services_pane_report.json", "skip_if_fast": True},
    {
        "id":      "ck_component_states",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "component_states", "--gate"],
        "label":   "CK ui-state live (no component left stuck in a loading or skeleton state — a stuck "
                   "skeleton is invisible to every other gate: 200 OK, rows returned, promise "
                   "resolved, axe clean, shimmer forever. A positive control fires per page, and "
                   "stuck candidates are re-checked after +9s so SLOW is not reported as STUCK)",
        "group":   "Platform",
        "report":  "component_states_report.json",
        "skip_if_fast": True,
    },
    {
        # The non-vacuity control is the point: a window wide enough to find real labels is also wide
        # enough to find SOME text near anything, so a bare 4242 is injected and MUST come back
        # unlabelled. It caught two pages mid-render, which is the control refusing a half-painted page.
        "id":      "cm_number_labelled",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "number_labelled", "--gate"],
        "label":   "CM ux-comprehension live (every number a page renders carries a label naming what "
                   "it is, proven with an injected bare-number control per page so the zero is "
                   "load-bearing rather than free; ordinals and asset codes excluded WITH reasons)",
        "group":   "Platform",
        "report":  "number_labelled_report.json",
        "skip_if_fast": True,
    },
    {
        # getComputedStyle CANNOT answer this — headless resolves env(safe-area-inset-*) to 0px, so a
        # computed read cannot distinguish "declared" from "absent". The AUTHORED css is read over CDP.
        # SEVEN corrections; the last two are the ones that matter. Matching the inset DIRECTION to the
        # pinned edge stopped a false GREEN on index (a padding-BOTTOM rule passing a TOP-pinned nav),
        # and refusing the ancestor credit for a viewport-pinned box stopped two more: `position:fixed`
        # is never displaced by ancestor padding, and `position:sticky` is covered at rest but pins to
        # viewport top 0 the moment the page scrolls — which is exactly when a person is reading it.
        "id":      "cj_safe_area",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "safe_area", "--gate"],
        "label":   "CJ safe-area live (every piece of edge-pinned chrome declares the safe-area inset "
                   "for the edge IT is pinned to, read from the authored CSS over CDP; the "
                   "DECLARATION is proven, rendering on a notched device is not, and that limit is "
                   "stated on every row rather than implied)",
        "group":   "Platform",
        "report":  "safe_area_report.json",
        "skip_if_fast": True,
    },
    {
        # NOT a presence check. A back control that exists and lands somewhere unrelated is worse than
        # none, because discovering that costs a navigation. wayfinding.js defines the destination in a
        # fixed order (?from= slug -> same-origin referrer -> PARENT[path] -> index.html), so each page
        # is driven TWICE: once arriving from a referrer, once with none, because those are different
        # promises. The referrer is chosen to be neither the page nor its mapped parent — using `hive`
        # for everything made both branches expect the same destination on the three pages whose parent
        # IS hive, where a component that ignored the referrer entirely would have passed both.
        "id":      "co_back_out",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "back_out", "--gate"],
        "label":   "CO ux-recovery live (every non-home page offers a reachable in-app way OUT that "
                   "lands where the platform's own wayfinding contract says it should, proven on both "
                   "the referrer branch and the parent-map branch; the mirrored PARENT map is read "
                   "back out of wayfinding.js each run, so a drifted copy fails loudly instead of "
                   "grading every page against the wrong destination)",
        "group":   "Platform",
        "report":  "back_out_report.json",
        "skip_if_fast": True,
    },
    {
        # The failure this platform has shipped three times in three different costumes: a read that
        # could NOT have succeeded renders as though it had. Two moderation badges asserting "0 waiting"
        # on a failed read; a 42501 telling a signed-IN buyer their session had expired; skillmatrix
        # offering first-run onboarding after a failed read, overwriting the discipline the worker had
        # already picked. Every time the page was confident and wrong.
        # NOT graded as "does it redirect to sign-in" — several of these surfaces are legitimately
        # readable signed-out, so a redirect is only ONE honest answer. Three are accepted and NAMED
        # (redirect/prompt, empty-with-a-reason, genuinely-public-and-identical); what fails is the
        # fourth behaviour, hive-scoped numbers or unexplained zeros standing in for data that isn't
        # there. The session is killed in localStorage (supabase-js persists `sb-<ref>-auth-token`
        # there, NOT in a cookie, so clearCookies would leave the page signed in and measure nothing),
        # every `sb-*` key is swept rather than one hardcoded project ref, and the signed-in reading is
        # taken FIRST as a control — a probe that cannot prove it changed anything cannot report that
        # the change was handled well.
        "id":      "co_session_died",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "session_died", "--gate"],
        "label":   "CO ux-recovery live (a dead session is never presented as signed-in data: every "
                   "page either redirects/prompts, states why it is empty, or is genuinely public and "
                   "identical — never confident zeros standing in for a read that could not succeed)",
        "group":   "Platform",
        "report":  "session_died_report.json",
        "skip_if_fast": True,
    },
    {
        # ADOPTION half of CO back_out for the modal views, and static on purpose. Driving 17 modals live
        # needs each page's OPENER, and the roadmap records what guessing those costs (a generic label
        # regex matched "Load more posts" instead of a composer). The way out of a modal is not per-page
        # anyway — it is one shared helper, whModalA11y, which wires Escape + focus-restore; so this asks
        # the question that IS answerable from source: is each dialog view registered with it.
        # THREE OUTCOMES, NOT TWO, and that distinction is the whole value. Its first run reported SIX
        # pages with "no registered keyboard way out" — including inventory's #part-modal, whose
        # registration at inventory.html:585 I had read minutes earlier. The loop regex captured the
        # forEach body as a LAZY `([\s\S]{0,400}?)\)`, which stopped at the first `)` — the one in
        # `document.getElementById(id)` — so the capture ended before `whModalA11y(el)`. Fixed, 6 became 3.
        # Then the remaining 3 turned out to hand-roll the behaviour rather than lack it
        # (community.html:2822 states it outright, resume.html:2088 has its own handler), so calling them
        # failures was a second over-claim. Now: no way out at all = FAIL; registered with the helper =
        # PASS; hand-rolled = UNGRADED with the divergence recorded, because a page-level Escape handler
        # is evidence the page implements this, not proof it covers THAT modal. Result: 0 failing, 10
        # modals on the helper, 5 hand-rolled across community/index/resume — a real CENTRALIZE-FIRST
        # divergence this gate now holds at its current size. The BEHAVIOUR half stays with
        # arc_u_focus_trap_probe.mjs; adoption alone is never banked as a green
        # (feedback_banner_adoption_is_not_write_refusal).
        "id":      "co_modal_escape_adoption",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "modal_escape", "--gate"],
        "label":   "CO ux-recovery, modal-exit ADOPTION (every V2/V3 dialog view named in "
                   "page_bank_anatomy has a keyboard way out; registration with the shared whModalA11y "
                   "helper PASSES, a hand-rolled Escape handler is UNGRADED with the divergence "
                   "recorded for centralization, and only a dialog with neither FAILS)",
        "group":   "Platform",
        "report":  "modal_escape_adoption_report.json",
        "skip_if_fast": True,
    },
    {
        # BEHAVIOUR half, and it found a real WCAG 2.4.3 defect that adoption alone certified as fine.
        # resume's #resume-manager closed on Escape but left focus on BODY, with the opener still in the
        # DOM, visible and focusable. Instrumenting HTMLElement.prototype.focus showed the restore firing
        # at #rm-current-title — an element INSIDE the just-hidden sheet — so focus fell to the body.
        # ROOT CAUSE was a DUPLICATE focus manager, not a missing one: #resume-manager is
        # `class="sheet-overlay"`, and utils.js:2800-2830 (whSheetA11y) auto-wires every
        # .sheet-overlay/.modal-overlay through whModalA11y at load and on mutation. resume ALSO kept its
        # own trapFocus/releaseFocusTrap pair, whose single global `_lastFocused` slot captured
        # activeElement AFTER the shared helper had already moved focus into the sheet. On Escape both
        # restored, and the page's stale in-sheet element won. Removing the page's duplicate trap for its
        # two auto-wired sheets fixed it — verified focus=true (#btn-resumes) — while #preview-overlay
        # KEEPS the hand-rolled trap, because its class is `.preview-overlay`, which whSheetA11y does not
        # sweep. Exactly the CENTRALIZE-FIRST failure: a hand-rolled copy left in place after the shared
        # wirer arrived, where each mechanism alone would have been correct.
        # Two assertion strengths, deliberately: Escape-closes is proven for every target; focus-restore
        # ONLY where a real opener ELEMENT is clicked, because whModalA11y restores to whatever was active
        # at open time — open a dialog by calling the page's function and that is <body>, so asserting
        # restore there would measure the probe's own starting state.
        "id":      "co_modal_escape_live",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "modal_escape_live", "--gate"],
        "label":   "CO ux-recovery, modal-exit BEHAVIOUR live (Escape closes each V2/V3 dialog and focus "
                   "returns to the control that opened it; every target's open path is a source ref, "
                   "never a label guess, and focus-restore is asserted only where a real opener element "
                   "was clicked)",
        "group":   "Platform",
        "report":  "modal_escape_live_report.json",
        "skip_if_fast": True,
    },
    {
        # CJ measured INSIDE the opened V2/V3 dialogs — rows that sat owed not because the oracle was hard
        # but because nobody could open those views. Resolving all 15 dialog open paths for the CO back_out
        # work removed the blocker; the table is shared via tools/dialog_targets.mjs so the two provers
        # cannot drift. Scoped to the DIALOG element, never document.body, or a sheet overlaying the page
        # would re-measure V1 and bank it as V2.
        # THE TAP-TARGET RULE IS PORTED VERBATIM FROM prove_viewport_overflow.mjs. The first cut omitted
        # four of V1's exclusions (sr-only, disabled, the actionable check, the WCAG 2.5.5 inline-link
        # exception) and REPLACED a control's rect with its label instead of UNIONING them — a stricter
        # ruler at V2 than V1 manufactures a difference between the views and calls it a defect. With V1's
        # exact rule the count came back identical, which is what makes the findings real.
        # RESULT: 13 of 15 dialogs graded (resume's #review-sheet has no read-only path in; hive's
        # #handover-sheet cannot be reached at all), ZERO unclipped overflow in all 13, and 10 of 13 clean
        # on tap targets. Three fail and they share a shape: 36px chips (inventory .asset-tag,
        # report-sender .label-pill), 40px tabs and an 11.7x20 close button (index #signin-modal).
        # CM IS MEASURED HERE AND DELIBERATELY ABSTAINS: its non-vacuity control FAILS inside a dialog,
        # because a dialog's whole subtree is usually under the 400-char ancestor-text cap so the top
        # ancestor "labels" every number. The control refusing to let a free zero be banked is the control
        # working; CJ still reports because it is geometric.
        "id":      "cj_dialog_layout",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "dialog_layout", "--gate"],
        "label":   "CJ ui-layout inside the V2/V3 DIALOGS (each opened by its own source-read path: no "
                   "unclipped horizontal overflow at 390 and every effective tap target >= 44px, on the "
                   "same ruler that banked 750/750 at page level; CM abstains per dialog because its "
                   "control cannot discriminate at that scope, so those rows stay owed)",
        "group":   "Platform",
        "report":  "dialog_layout_report.json",
        "skip_if_fast": True,
    },
    {
        # CL inside the opened V2/V3 dialogs — three of its five oracles, and the two omissions are
        # deliberate: contrast_wcag/contrast_apca are NOT computable from the CSSOM here (uncomposited
        # alpha reads a ratio of 1.00 and a gradient defeats a single-colour sample), so those rows stay
        # OWED rather than banked from a number known to be wrong.
        # FOUR INSTRUMENT CORRECTIONS, every one of which would have shipped false findings:
        #  1. focus was driven with el.focus(), and `:focus-visible` deliberately does NOT match a
        #     PROGRAMMATIC focus in Chromium. It reported resume 0/3, achievements 0/1, inventory 7/25 —
        #     while index passed 9/9 only because index styles plain `:focus`. Now focus is driven by TAB,
        #     which is what a keyboard user does.
        #  2. the baseline was captured while the dialog had ALREADY autofocused its first control, so that
        #     control's "unfocused" style was its focused style and it read as having no indicator. Now any
        #     focus inside the dialog is blurred before the baseline. (This surfaced only after the
        #     duplicate focus trap was removed from resume — the fix exposed it.)
        #  3. reduced_motion flagged every transition, including Tailwind's transition-colors, and called
        #     index's close button and both auth tabs defects. WCAG 2.3.3 is about MOTION; a 150ms colour
        #     change is not motion. Only transform/offset/dimension transitions and keyframe animations
        #     count now, and spinners are exempt and counted — an indeterminate indicator that stops
        #     animating stops meaning anything.
        #  4. innerText is not used for names (it returned '' for visible controls in an earlier arc and
        #     inflated the unnamed count 0 -> 12); textContent plus a glyph test instead.
        # A FRESH CONTEXT PER DIALOG, because reduced-motion is emulated per context and a leaked
        # emulation would measure every later dialog under the wrong media state.
        "id":      "cl_dialog_a11y",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "dialog_a11y", "--gate"],
        "label":   "CL ui-visual inside the V2/V3 DIALOGS (every control has an accessible name; focus is "
                   "visible, verified CAUSALLY by tabbing and comparing each control against its own "
                   "unfocused baseline rather than trusting a stylesheet rule; nothing moves under "
                   "prefers-reduced-motion, with spinners exempt and counted. Contrast is NOT claimed — "
                   "it is not computable from the CSSOM here, so those rows stay owed)",
        "group":   "Platform",
        "report":  "dialog_a11y_report.json",
        "skip_if_fast": True,
    },
    {
        # CO session_died one level IN: with the session killed, what happens to the DIALOG / TAB / SECTION?
        # Reuses tools/dialog_targets.mjs, so these are the same views the layout and a11y provers measure.
        # UNREACHABLE COUNTS AS A PASS, and recognising that is the point — it is the answer most of this
        # platform gives (10 of 22 pages redirect to index on a dead session) and it is the correct one: with
        # no session there is no view, so there is nothing to mislead anyone with. What FAILS is a view that
        # opens and still shows values that outlived the session, or bare zeros with no explanation.
        # THE SIGNED-IN READING IS THE CONTROL: a view that cannot be opened even WITH a session is UNGRADED,
        # never passed, or "unreachable" would be indistinguishable from "unreachable BECAUSE the session is
        # gone" — which is the entire question.
        # TWO CORRECTIONS, both caught pre-bank. It reported achievements' #levelup-overlay as leaking the
        # value "2" — which is the newLevel argument the probe itself passes to showLevelUpModal, i.e. its own
        # input rendered back at it; that target is now flagged syntheticContent and this oracle abstains on
        # it. And it judged only the VIEW's text for an explanation, reporting analytics V2/V3 as unexplained
        # while the PAGE around them had changed to a signed-out state — a section may be legitimately empty
        # if the page says why, so the page-level signal now counts too.
        "id":      "co_dialog_session_died",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "dialog_session_died", "--gate"],
        "label":   "CO ux-recovery at V2/V3 (with the stored session removed, no dialog, tab or section "
                   "opens and presents figures that outlived it; redirect-before-open and "
                   "empty-with-a-stated-reason both count as honest, and a view unopenable even WITH a "
                   "session is ungraded rather than passed)",
        "group":   "Platform",
        "report":  "dialog_session_died_report.json",
        "skip_if_fast": True,
    },
    {
        # CO back_out for the TAB and SECTION views, and it is a genuinely different question from the modal
        # one rather than a workaround. A dialog's way out is Escape (co_modal_escape_live owns that). A tab
        # or section has NO Escape — pressing it there should do nothing — so that prover skips them, which
        # left these rows owed. What matters for a tab is whether the PAGE-LEVEL way out survives the view
        # being open: a tab that quietly breaks the back affordance strands someone as effectively as a modal
        # with no close button. The view is confirmed OPEN before the affordance is looked for, so a failure
        # is a property of the view and not of the page at rest.
        # Contract is wayfinding.js's own, and the mirrored PARENT map is re-read from that file every run so
        # a drifted copy fails loudly instead of grading against the wrong destination. The referrer is chosen
        # to be neither the page nor its mapped parent, so a tier-1 affordance can only pass by honouring the
        # referrer rather than falling through to the parent map; a tier-2 (the page's own in-layout link) is
        # NOT referrer-aware and that weaker guarantee is recorded on the row rather than failed.
        # ONE CORRECTION, carried over rather than rediscovered: index is HOME, and wayfinding.js skips
        # IS_HOME by design, so its landing was reported as "NO way out is visible" — true, and the specified
        # behaviour. It is declared-na, matching the V1 row already banked for the same reason; failing it
        # would have contradicted a green row on the same page.
        "id":      "co_dialog_back_out",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "dialog_back_out", "--gate"],
        "label":   "CO ux-recovery for TAB/SECTION views (with each view OPEN, the page-level way out is "
                   "still visible and still lands where wayfinding.js's contract says; home is declared-na "
                   "because IS_HOME deliberately has no back affordance)",
        "group":   "Platform",
        "report":  "dialog_back_out_report.json",
        "skip_if_fast": True,
    },
    {
        # CM why_refused — induces a REAL PostgREST row-level-security refusal (403 + 42501 "permission denied
        # for table") on every read via a window.fetch patch, then asks whether the page tells the person WHY.
        # THE GOOD NEWS FIRST: zero of 16 graded pages blamed IDENTITY for the refusal. That inversion is a bug
        # this platform HAS shipped (a 42501 rendered as "your session has expired", sending a signed-IN person
        # to re-authenticate for something authentication cannot fix), and utils.js:1607 whIsAuthFailure() is
        # why it is now absent: it returns FALSE for 403 with the comment "authenticated, and refused. Not a
        # session problem". That discrimination is holding.
        # THE FINDING IS ONE MISSING SURFACE, NOT 15 PAGE BUGS: 13 of 16 pages said NOTHING when every read was
        # refused, 2 showed only a generic error, 1 passed. On logbook "516 entries 30 machines 6 open" became
        # "- entries - machines - open" with the body text identical to the character (1107 before and after)
        # and no word about why. The platform has a central WRITE-failure message and the correct 401-vs-403
        # discriminator; the READ path has no equivalent. One adoption, not fifteen edits.
        # THE INJECTION PROVES ITSELF, because the first version silently did not run: a regex written inside a
        # template literal collapsed to one terminating at its second character, so the init script was a
        # SyntaxError, every read succeeded, and the prover reported "the reads were all refused and the page
        # said nothing" while nothing had been refused. Now every intercepted call increments a counter (8-42
        # per page on the recorded run) and a zero-hit page is UNGRADED rather than judged.
        # CN ux-journey, the family that had 330 rows and not one walk. Extends the discipline in
        # tests/ux-journeys.spec.ts (which covers only the 4 marketplace surfaces) to the 22 product pages:
        # every step asserts the TRANSITION rather than that a control was clickable, and a journey that
        # cannot be CONSTRUCTED fails instead of skipping. Personas are not invented here — each page's own
        # page_bank_anatomy/<page>.json `journey_personas` supplies the label, and a label this harness
        # cannot establish FAILS rather than quietly defaulting to a signed-in worker.
        # The tooth: skillmatrix once asked a worker to re-pick a discipline they had already chosen,
        # because the READ had failed and onboarding was the fallback — which a naive "did they reach
        # something actionable?" check scores as a PASS while it is about to overwrite their real setup.
        # So an onboarding screen counts as value only if the page's own reads SUCCEEDED, counted from the
        # network rather than guessed.
        # CM reward_explained. The reward pattern is a REGEX LITERAL handed over as .source, never a
        # hand-escaped string: '\d' inside a JS single-quoted string collapses to 'd', which silently
        # matched nothing and reported every page as having no rewards at all. The same collapse once made
        # a whole injection script inert. The element must also BE the figure, not prose mentioning one —
        # "4 of 12 domains touched at Level 1+" matches on "Level 1" and is a sentence, not a reward.
        # CC failure-injection for the 20 product pages. It found and closed a real gap on its first run:
        # the transport notice hung off `.then(res => …)`, so a REJECTED or ABORTED fetch — which never
        # produces a res — skipped it entirely. logbook answered a dead network with zero page errors,
        # zero console errors and not one word on screen. utils.js now notices transport failures in a
        # `.catch` that RE-THROWS (the wrapper's own note forbids swallowing the error callers surface).
        # Two instrument traps are baked in: the patch is installed via addInitScript so supabase-js
        # constructs its client around it (a post-load override silently no-ops), and the hang stub
        # honours the AbortController — without that it suppressed the very timeout under test.
        "id":      "cc_failure_injection",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "failure_injection", "--gate"],
        "label":   "CC failure-injection (500 / 401 / timeout / offline: a failed read renders a FAILURE, "
                   "never an emptiness, a stuck skeleton, or silence; injection is hit-counted so a "
                   "page where it never fired is ungraded rather than judged)",
        "group":   "Platform",
        "report":  "failure_injection_report.json",
        "skip_if_fast": True,
    },
    {
        # ★ WHY A GATE AND NOT A BANK ROW. PB-logbook-105 asserts "LOTO status is read from the
        # loto_applied column, never re-derived from a free-text regex" — a claim about DERIVATION,
        # which none of the bank's evidence kinds (live-walk / gate / psql / declared-na) lets a source
        # reading satisfy. Writing the check makes the claim re-provable on every run instead of
        # resting on one afternoon's grep.
        # WHY IT EARNS A GATE AT ALL: lock-out/tag-out is the record that a machine was isolated before
        # someone put their hands in it. `entry.loto_applied` is a boolean a worker set — auditable and
        # defensible to DOLE. `/loto/i.test(entry.action)` is an INFERENCE that reads "applied" for
        # "LOTO not required" and "not applied" whenever someone isolated the machine and did not write
        # the word. Both directions are dangerous; the second invites a supervisor to "correct" a
        # record that was already right.
        # Verified both ways before registering: clean exit 0; a planted
        # `/loto|lock ?out/i.test(entry.action)` caught at its exact line with exit 1.
        "id":      "safety_field_provenance",
        "script":  "tools/validate_safety_field_provenance.py",
        "args":    [],
        "label":   "Safety field provenance (LOTO / permit come from their COLUMN, never inferred from "
                   "free text — an isolation record guessed from prose is true when the word appears "
                   "and false when a worker isolated the machine without writing it)",
        "group":   "Platform",
        "report":  "",
        "skip_if_fast": False,
    },
    {
        # CI is the family that asks an ENGINEERING question rather than a software one: is this the
        # ISO 14224 definition of MTBF, does this OEE state which factor is missing, does this reorder
        # point name the threshold it crossed. A page can be flawless software and still be wrong here,
        # and the person it misleads is a planner making a decision about a plant.
        # ★ REGISTERED RED, DELIBERATELY, and that is the point of registering it. It currently fails on
        # 8 measured findings (analytics states no PM tolerance window and no rolling-vs-calendar;
        # pm-scheduler names neither its 90-day compliance window nor a tolerance; asset-hub gives no
        # 1-10 RPN range or critical band and no sensor age; hive's readiness cannot be fully
        # decomposed; inventory suggests a reorder with no lead time — that last one needs a schema
        # column, so it is Ian's call, not a patch). Precedent: co_modal_escape_live is red by design on
        # the hive handover finding. A gate that goes green only once real defects are fixed is the
        # forward-only ratchet working; hiding it until then would be the coverage-by-deletion move this
        # bank exists to prevent.
        # Every pattern it uses was HARVESTED FROM A LIVE RENDER, it records the exact string it matched
        # so a verdict can be audited without re-running it, and `--selftest` plants a satisfying AND a
        # violating text per check so one that cannot fire is caught before it banks anything.
        "id":      "cm_quota_legible",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "quota_legible", "--gate"],
        "label":   "CM what_does_it_cost, QUOTA half — a rate limit must be reported AS a rate limit. "
                   "Model quota is invisible until you hit it, so the constraint is INDUCED: every "
                   "AI/orchestrator invoke is answered 429 and the surface is read. LOCKS A CLASS FOUND "
                   "THREE TIMES AND FIXED (2026-08-17): assistant collapsed every gateway failure into a "
                   "single null and said 'Assistant timed out. Please retry on a faster connection'; "
                   "shift-brain matched 429 with a network-error regex, so a rate limit read as 'Shift "
                   "planner is offline locally. Run npx supabase functions serve' — a developer command "
                   "shown to a plant supervisor; report-sender carried a COMMENT asserting invoke() parses "
                   "the body into `data` on a non-2xx, which it does not in this client version, so the "
                   "toast fell through to 'All reports failed, check connection and try again'. One shape: "
                   "a broad failure classifier swallowing a specific actionable cause, and all three sent "
                   "someone chasing bandwidth while the connection was fine and the service was busy. "
                   "Messages are sampled ACROSS the window (they are ~1s toasts) and controls are clicked "
                   "in-page (shift-brain's has offsetParent null), because both shortcuts previously "
                   "produced a false 'the surface said nothing'. A page whose action draws no invoke is "
                   "UNGRADED, never failed — no quota spent means nothing to be legible about.",
        "group":   "Platform",
        "report":  "quota_legible_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "ci_domain_truth",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "domain_truth", "--gate"],
        "label":   "CI domain-truth (a metric names its standard AND its basis, a partial figure is "
                   "labelled partial, a reorder names its threshold, a composite states its bands — "
                   "RED by design on 8 measured findings until they are fixed)",
        "group":   "Platform",
        "report":  "domain_truth_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "cm_reward_explained",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "reward_explained", "--gate"],
        "label":   "CM ux-comprehension (a reward figure — XP, level, tier — states its criteria in its "
                   "own card: what earns it or the threshold it sits at; a page with no reward on screen "
                   "is not applicable and stays owed rather than being failed)",
        "group":   "Platform",
        "report":  "reward_explained_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "cn_journey",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "journey", "--gate"],
        "label":   "CN ux-journey (a first-timer reaches value without a dead end and a returning person "
                   "is not made to redo setup, walked as each page's own grounded persona; onboarding "
                   "shown over a failed read is a failure, not value)",
        "group":   "Platform",
        "report":  "journey_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "cm_why_refused",
        "script":  "tools/validate_page_ui_provers.py",
        "args":    ["--prover", "why_refused", "--gate"],
        "label":   "CM ux-comprehension (a 42501/403 read refusal must be explained by NAMING permission: not "
                   "blamed on identity, not left as a bare generic error, not silent. The injection counts "
                   "its own hits and a zero-hit page is ungraded; a page whose ordinary copy already matches "
                   "the vocabulary is ungraded too, so the signal has to APPEAR)",
        "group":   "Platform",
        "report":  "why_refused_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "values_survive_write",
        "script":  "tools/prove_values_survive_the_write.py",
        "args":    ["--gate"],
        "label":   "Values survive the write (every BEFORE-trigger rewrite of a submitted value belongs "
                   "to a named class, and no capped column has truncated stored text)",
        "group":   "Platform",
        "report":  "values_survive_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "field_names_survive",
        "script":  "tools/prove_field_names_survive.py",
        "args":    ["--gate"],
        "label":   "Field names survive the seam (every literal column name a page uses still exists on "
                   "the relation it is used against, resolved against the live catalog)",
        "group":   "Platform",
        "report":  "field_names_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "write_atomicity",
        "script":  "tools/prove_write_atomicity.py",
        "args":    ["--gate"],
        "label":   "Write atomicity (no declared ledger invariant shows a half-applied write; "
                   "client-sequenced write pairs reported as structural exposure)",
        "group":   "Platform",
        "report":  "write_atomicity_report.json",
        "skip_if_fast": True,
    },
    {
        "id":      "null_semantics",
        "script":  "tools/prove_null_semantics.py",
        "args":    ["--gate"],
        "label":   "NULL semantics across the seam (no view column and no client `?? 0` substitutes a "
                   "value a reader could mistake for a real one, for a NULL that is present today)",
        "group":   "Platform",
        "report":  "null_semantics_report.json",
        "skip_if_fast": True,
    },
    {
        # THE FIRST DEFECT THE PAGE BANKS FOUND, AND THE GATE THAT KEEPS IT DEAD. Walking
        # PB-community-057 on 2026-08-05 measured three cycles of (post a category='safety' post ->
        # soft-delete it) moving one worker's community_xp from 185 to 260 with ZERO posts left visible
        # on any feed. community_xp was a running TOTAL with no ledger, so no award could be attributed
        # to the post that earned it and nothing could reverse one — the one-sided-write class, in
        # reputation instead of pesos. It reached further than gamification: community_xp feeds the hive
        # board, achievements XP parity, and the community->commerce reputation seam
        # (get_community_reputation / v_marketplace_sellers_truth), so farmed XP inflated a COMMERCIAL
        # trust signal.
        # The load-bearing check here is LIVE, not structural: it re-runs the original farm in a
        # rolled-back transaction and fails if the total moves. A structural "table exists, triggers
        # attached" check would stay green through a logic change that silently stopped reversing,
        # which is the exact false-green shape this whole bank exists to end. Proven by fault
        # injection: disabling trg_community_post_visibility_xp turns it RED.
        # A SKELETON MAY NOT OUTLIVE ITS LOAD. Found 2026-08-05 walking PB-logbook-126: the logbook
        # entry feed showed a permanent shimmer, still there at 14s, on the platform's primary capture
        # surface. NOTHING existing could have caught it — the request returned 200, 200 rows were
        # loaded, loadEntries resolved in 58ms, the console was silent at warning level and above, axe
        # reported zero violations and every static validator passed. The page simply looked slow,
        # forever. Root cause was a contract violation: loadEntries() paints the skeleton whenever the
        # list is empty, while its own comment says "Mine-mode only"; in team mode renderEntries clears
        # the list and shows a search prompt, then the init-time personal load re-painted a placeholder
        # no later render would clear.
        # RESOLVED 2026-08-06. This note used to call the headless auth a known limitation; it was not
        # one. The probe must sign in to reach the surfaces that paint list skeletons, and it failed with
        # "name resolution failed" — which reads like DNS and points at the wrong layer entirely. Sign-in
        # goes through the `login` EDGE FUNCTION; the supabase_edge_runtime_workhive container was
        # stopped, and Kong words an unresolvable upstream exactly that way. A `docker start` took this
        # gate from SKIP to PASS on 8 of 8 surfaces, each with a real signed-in render of 1,145-4,707
        # chars. Before believing a DNS or credential story, check that the container is up.
        # The non-vacuity floor (MIN_RENDERED_CHARS) stays and stays load-bearing — its first run PASSED
        # three pages at an identical 407 chars, the signed-out shell, where "no skeleton" was true only
        # because there was no page.
        "id":      "skeleton_resolves",
        "script":  "tools/validate_skeleton_resolves.py",
        "args":    ["--settle", "9"],
        "label":   "Loading skeletons resolve (no shimmer may outlive its load; a stuck placeholder is "
                   "invisible to console, axe and every static check — logbook's team-mode orphan)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # LATE CONTENT MAY NOT PUSH THE PAGE, AND A RESERVATION MAY NOT ROT IN SILENCE. Found 2026-08-06
        # walking PB-project-manager: CLS 0.108 against the 0.1 budget, caused not by a missing
        # reservation but by two that had gone SHORT — #pm-mgr-source-chip reserved 54px and rendered 62,
        # #pm-verdict reserved 99 and rendered 102. Eleven px of drift across two hand-picked numbers
        # nobody re-measured after the copy changed.
        # WHY 11px COST 0.108: CLS is impact-fraction x distance-fraction, and both reservations sit near
        # the top of #list-view, so everything below them moved together — verdict, stat row, action
        # card, tab strip, filter row, about 95% of the viewport. A small miss at the TOP of a page is
        # not a small defect. Fixing the two numbers took it to 0.0066.
        # WHY A GATE AND NOT A CODE REVIEW: nine surfaces each hand-pick a pixel for the SAME component
        # (the empty <p> renderSourceChip() fills) and they disagree by 6x — 27, 30, 40, 54, 54, 62, 76,
        # 161. Each was right when measured and each rots silently when the copy, font or breakpoint
        # moves. Nothing recomputed them and nothing noticed.
        "id":      "cls_reservations",
        "script":  "tools/validate_cls_reservations.py",
        "args":    ["--settle", "9"],
        "label":   "Late-filled blocks don't push the page (CLS <= 0.1 per surface; reports which "
                   "min-height reservation has gone SHORT, because layout-shift sources name the blocks "
                   "that MOVED, never the one that grew)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "community_xp_ledger",
        "script":  "tools/validate_community_xp_ledger.py",
        "args":    [],
        "label":   "Community XP is attributable, reversible and unfarmable (award ledger keyed "
                   "(post_id, reason); reversal hangs on the deleted_at TRANSITION because the product "
                   "soft-deletes — an ON DELETE trigger would never fire on a real person's delete; "
                   "live teeth re-run the farm and require zero drift)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # THE INSTRUMENT BEHIND 136 OF THAT BANK'S ROWS. Those rows assert things no page can show you
        # — the ledger conserves credits, every grant has a caller-aware policy behind it, a DEFINER
        # helper is not callable by a stranger — and until now each was walked BY HAND, which proves
        # only that it held on the afternoon someone looked. Making each one a QUERY found three real
        # holes on its first honest run: voice transcripts readable by anon (mig 50), a nightly sweep
        # any visitor could fire — it returned 3, three real records expired on demand (mig 51), and
        # the platform's own AI spend counters world-readable (mig 52).
        #
        # It is also why the bank's rows now declare `supabase/migrations` instead of whichever page
        # was open during the walk: a DB claim must expire when a MIGRATION lands, and under the old
        # declaration mig 50 would have expired nothing (gate rule R7).
        # BOLA / BFLA / JWT-not-body / tenant boundary, asked of the SERVER with a real session.
        # It probes as a verified NON-ADMIN, because the standing test account (pabloaguilar) is in
        # marketplace_platform_admins — probing as him showed a "critical" listing takeover that was
        # simply moderation working, since the policy reads
        #   USING ((seller_name IN (SELECT auth_worker_names())) OR is_marketplace_admin())
        # Every write runs inside a rolled-back transaction: the first version of this probe proved a
        # read boundary by writing, and left a live listing titled "TAKEN OVER BY A STRANGER" with no
        # recoverable original.
        "id":      "identity_boundaries",
        "script":  "tools/verify_identity_boundaries.py",
        "args":    [],
        "label":   "Identity boundaries (another person's draft is invisible; their listing is "
                   "unwritable; a forged seller_name in the body is refused; an admin-gated function "
                   "is refused at the server — probed as a verified non-admin, all writes rolled back)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,          # needs the local database and gateway up
    },
    {
        # THE MONEY LIFECYCLE. Registered the day a commission was found debiting the ledger and never
        # retiring the credits, so `issued_credits` published a supply 360.00 larger than the credits
        # that existed. That defect lived on the DB side of every screen, which is why it survived
        # every hand-walk: the page showed a balance, and the claim was about what the ledger did.
        # Every probe that writes runs inside a single psql call ending in rollback.
        "id":      "money_lifecycle",
        "script":  "tools/verify_money_lifecycle.py",
        "args":    [],
        "label":   "The money lifecycle asked of the database (a verified top-up mints exactly once "
                   "and an unverified one mints nothing; a verification cannot be undone by UPDATE; "
                   "commission is paired with retirement; credits cover at most the knob's share of a "
                   "purchase and a spend AT the cap is still accepted; a job earns or spends, never "
                   "both; credits cannot be moved between accounts and there is no cash-out function; "
                   "no account is overdrawn; an auto-confirmed payment never also names a human)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,          # needs the local database up
    },
    {
        "id":      "layer_invariants",
        "script":  "tools/verify_layer_invariants.py",
        "args":    [],
        "label":   "Layer + seam invariants asserted against the live system (ledger conserves against "
                   "the treasury; every grant has a caller-aware policy; DEFINER cron helpers are not "
                   "user-callable; realtime publishes nothing RLS-free; NULL keeps its meaning across "
                   "the client seam; pg_cron is not allowed more jobs than there are workers)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,          # needs the local database up
    },
    {
        # The READ-side sibling of client_write_grants, and it exists because three gates each asked
        # a neighbouring question and left this one uncovered. credit_treasury — one row holding the
        # platform's whole money position — was granted SELECT to anon AND authenticated under
        # `using (id = 1)`, a predicate that picks a row without ever checking a person. rls_coverage
        # classifies by TENANT COLUMN and this table has none; the rls_open_policy flywheel check
        # greps migration text for `USING (true)` and this is not that string; write_grants asks who
        # can WRITE. Nobody wrote. The leak was a read, and every gate passed.
        "id":      "public_read_surface",
        "script":  "tools/validate_public_read_surface.py",
        "args":    [],
        "label":   "Public read surface (every table a client can SELECT must either consult the "
                   "caller, inherit scoping through a subquery, deny by default, or be DECLARED "
                   "public with a reason; reads the LIVE catalogue, not migration text)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "post_action_coherence",
        "script":  "tools/validate_post_action_coherence.py",
        "args":    [],
        "label":   "MK12 · post-action coherence — a handler that writes and then tells the user it SUCCEEDED must also re-derive the surfaces it just invalidated, or the page contradicts its own receipt. Born from the walk: saving a marketplace search toasted \"Search saved.\" while the counter beside it still read 0 until a full reload, because the handler refreshed the sheet but never called updateSavedSearchBadge(), which already existed. Deliberately tolerant, since a gate that cries wolf on correct code gets ignored: an OPTIMISTIC refresh made BEFORE the write counts (the watchlist toggle updates the heart, then writes, then confirms — better than refreshing after), a degraded/queued notice (\"saved offline, will sync\") is not a commit receipt, and a handler that navigates away or is realtime-backed is exempt. All three shapes were live findings pinned as self-test cases. Forward-only, currently 0. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "error_remedy_actionable",
        "script":  "tools/validate_error_remedy_actionable.py",
        "args":    [],
        "label":   "MK11 · error-remedy actionability — a catch around a client WRITE (insert/upsert/update/delete/rpc/functions.invoke) that tells the user to \"try again\" must FIRST branch on the auth/permission case, because a 42501/401 means the session is gone and retrying reproduces the failure exactly. Born from the walk, not a reading list: the 2026-07-24 deepwalk hit this shape four independent times (buyer inquiry, platform-actions, founder-console, saved-search save) — on the inquiry step the user had already typed a phone number before being told to retry something that could never work. Standard: nngroup.com/articles/error-message-guidelines (\"context and potential remedies\", \"instructions on how to resolve\"). The branch is centralized as window.whIsAuthFailure / whWriteError in utils.js, and the detector accepts either the helper or an inline signature test, so adopting the helper never reads as a regression. Forward-only; 12 -> 2 on the seeding sweep. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "hive_deepwalk_ratchet",
        "script":  "validate_hive_deepwalk.py",
        "args":    [],
        "label":   "Hive deepwalk ANTI-DRIFT ratchet (HIVE_DEEPWALK_EXPANSION_ROADMAP.md §4) — the measured %-board (14 journeys x 5 phases G/W/O/H/R, HK classes x 6 stages) held as a forward-only FLOOR, so a cell that was walked/observed/resolved/locked cannot silently revert. Computed from hive_deepwalk_state.json, never asserted in prose. Carries the shallow_W guard: a journey cannot claim W=done without >=2 personas AND >=2 states recorded, because without it 'walked' degrades into 'opened the page once as myself' — the exact shallowness this arc exists to kill (it caught over-crediting twice on the marketplace board). `--accept` ratchets the floor up after real progress. Static/fast. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
    },
    {
        "id":      "corrective_definition_parity",
        "script":  "tools/validate_corrective_definition_parity.py",
        "args":    [],
        "label":   "LG2 · corrective-definition parity — one derived semantic must not carry three definitions. 'Corrective' is expressed three ways: v_logbook_truth.is_corrective is a REGEX, v_asset_truth.last_failure_at and the five analytics RPCs (get_mtbf / get_mttr / failure_frequency / downtime_pareto / repeat_failures) use the EXACT string 'Breakdown / Corrective', and trigger-ml-retrain uses ILIKE. They agree today only because every corrective row happens to use the exact string (1,146 = 1,146 = 1,146) — luck of vocabulary, not a guarantee. Add one entry typed 'Emergency Breakdown' and is_corrective counts it while last_failure_at and EVERY MTBF/MTTR/failure-frequency KPI silently miss it, so the asset reads as MORE reliable than it is, which is the direction that gets someone hurt. Collapsing the three into one definition means editing a view, five RPCs and an edge function that all feed KPIs a plant would act on, so this takes the middle disposition instead: the duplicate representations are allowed, and divergence is made impossible-then-DETECTABLE as a fix-to-ZERO ratchet caught at CI on the first divergent row. Live-tier, skips cleanly without docker. Teeth proven by inserting a divergent value (FAIL, names the offending vocabulary) and removing it (PASS). Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "offline_queue_confirm",
        "script":  "tools/validate_offline_queue_confirm.py",
        "args":    [],
        "label":   "LG3 · offline-queue drain confirmation — a queued write that changed 0 ROWS must never be reported as synced. A PostgREST update/delete matching nothing is not an error (supabase-js returns error:null, status 204), so every drain's `if (!error)` success test treated 'wrote nowhere' as 'written': it deleted the queued item and toasted success. Walked live 2026-07-28 (LB7): a tech edits an entry offline, the entry is deleted server-side, and on reconnect the app says '1 offline change synced.' while the text is destroyed. Zero rows is the NORMAL outcome under RLS — logbook's UPDATE policy is owner-scoped on auth_uid while SELECT is hive-scoped, so a worker can see entries the database will never let them edit. The shared helper carries the same shape to 6 more surfaces (community_posts, schedule_items, inventory_items, pm_completions, rcm_fmea_modes, skill_profiles). Invariant: inside a drain, an update/delete whose success path removes the queued item must .select(...) the affected rows AND gate the removal on receiving one. Scoped to drain functions on purpose — a .update() elsewhere has a human watching it. Teeth proven by reverting: FAILs on the real pre-fix code at all 3 sites. Static/fast. Self-test: --selftest (pins the pre-fix and the looks-right-but-ignores-the-rows shapes as must-fail fixtures).",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "pm_ontime_vs_compliance",
        "script":  "tools/validate_pm_ontime_vs_compliance.py",
        "args":    [],
        "label":   "PMK1 · on-time vs compliance — a PM completed LATE still counts as compliant, so the headline can flatter the program to the person least able to check it. Walked live 2026-07-28 as two personas in two hives: the page reads '0 of 31 on track now · 88% PM compliance (SMRP)' for a supervisor with 20 overdue, and '3 of 30 on track · 86%' for a worker with 24 overdue. Measured at the DB, which explains how both are true at once: 331 of 1,224 consecutive-completion intervals (27.0%) ran past frequency_days, 14.5% past 1.5x, and get_pm_compliance_smrp counts every one. Currently compliance 86.3% vs on-time 73.0% — a 13.3-point gap. Deliberately does NOT redefine the RPC: it implements a NAMED standard (SMRP 2.1.1) with verified analytics parity, so changing it silently would break a documented contract and move a number a plant acts on. Instead it holds the GAP as a forward-only ratchet, so the day delivery drifts further from on-time the build fails before anyone reads a healthy percentage and relaxes. Standard harvested on a genuine bag miss (the external corpus held 144 chunks and none on maintenance): substrate/external/external-pm-schedule-compliance-metric.md, which states the formula, the 90% world-class benchmark and the explicit late-PM blind spot. Live-tier, skips cleanly without docker. Teeth proven by tightening the ceiling. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "source_chip_freshness",
        "script":  "tools/validate_source_chip_freshness.py",
        "args":    [],
        "label":   "PM5 · a saved copy must not claim to be live — the source chip is the platform's provenance UI, so reporting freshness is its only job and overstating it is worse than having no chip. Walked 2026-07-28: analytics.html renders analytics_snapshots (the first view of the PHT day computes and saves; every later view replays the saved copy and only Refresh recomputes — a deliberate, good feature) while the chip hardcoded 'Live recomputation each refresh' on EVERY load. Measured: the KPI numbers were on screen at 0ms, up to ~24h old, under a chip promising they were live. For this arc's journey that means a technician completes a PM, opens analytics, sees compliance unmoved, and nothing on the page says the figure predates their work. The existing 'Source-chip truth' dimension in canonical_status.py checks that a chip names views the page actually READS and says nothing about the freshness CLAIM, which is how this shipped green. Asserts: any page that reads a *_snapshots / *_cache table must COMPUTE its freshness string rather than hardcode a liveness claim; pages with no cache path may keep a literal, because for them it is true. Static/fast. Teeth proven by restoring the hardcoded claim (exit 1). Self-test pins the pre-fix shape as must-detect, the computed fix as must-not-flag, and a cache-free page as must-not-flag.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "pm_frequency_mapping",
        "script":  "tools/validate_pm_frequency_mapping.py",
        "args":    [],
        "label":   "PM08 · one interval-to-frequency mapping, never rarer than asked — a PM's frequency is a WORD in the database and the schedule is DERIVED from that word by a CASE in v_pm_scope_items_truth, so every writer converting an interval in DAYS into that word is deciding the real schedule. Walked 2026-07-28: integrations.html (the CMMS/CSV import a plant uses to onboard its existing program) carried a local map with NO Daily bucket, taking the first bucket >= days over [7,30,90,180,365] — measured, a 1-day interval became 'Weekly' (7x rarer than the source system asked), 14 became 'Monthly' (2.1x), 45 became 'Quarterly' (2x). Every drift ran the SAME direction: less often than specified, which is the direction that leaves equipment un-inspected. asset-hub.html rounded to the NEAREST bucket instead, so a 300-day RCM interval became 'Annual' (365). Fixed with ONE shared mapping (whFreqFromDays/whFreqDays in utils.js) that snaps DOWN — rounding to a shorter interval costs labour, rounding to a longer one is a safety decision. Asserts three things: PARITY (utils.js day-values equal the view's frequency_days CASE label-for-label, including the biweekly/fortnightly synonyms the UI's canonFreq resolves to 7 while the view says 14 — latent today, since no such rows exist, and pinned here so it stays impossible-then-detectable like the logbook arc's 'corrective' vocabulary); SAFE DIRECTION (no interval 1..400 maps to a longer period); NO PRIVATE COPIES (neither writer reintroduces its own bucket list). Parity is live-tier and skips cleanly without docker; the rest is static. Teeth proven by restoring the private map: exit 1. Self-test: --selftest pins the bucket-list shape as must-match and a non-frequency numeric array as must-not.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "workflow_states_present",
        "script":  "tools/validate_workflow_states_present.py",
        "args":    [],
        "label":   "AHK3 - a workflow state with no rows is a state nobody has walked, and the seeder is part of the test surface. The Asset Hub arc hit this THREE times for three different reasons: asset_nodes had 95 rows ALL status='approved' with 0 auth_uid (assets.py computed the author, asset_brain.py dropped it in the hand-off and hardcoded the status) - which is why the PDDA arc's F21 sat undiagnosed and AH3's authority hole hid behind the same gap; rcm_strategies was 172 approved / 0 unapproved (reliability.py set approved_at unconditionally while the fmea rows twelve lines above already branched), so pushStrategyToPm's REFUSAL had never once run; and weibull_fits had 91 fits and 0 'insufficient_data', so the state that tells a planner NOT to act on a number had never been rendered. Holds SEVEN states open (asset pending/rejected/authored/cold-start, strategy unapproved/pushed, weibull insufficient_data) - the cold-start one added by AH1, where 0 of 79 approved assets lacked a Weibull fit because reliability.py fitted every node unconditionally, so the state every newly-commissioned machine and every new customer is in had never been rendered. Demands REPRESENTATION, not a ratio - the point is that the next walk has something to walk. Teeth proven by emptying one state: exit 1. Live-tier, skips without docker. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "schedule_health_falsifiable",
        "script":  "tools/validate_schedule_health_falsifiable.py",
        "args":    [],
        "label":   "PJK2 - a number that decides what may slip, or what a CLIENT is told, must be falsifiable. Half of what this gate holds is that the ENGINE IS GOOD, asserted so nobody 'simplifies' it away: prescriptive.py cites PMBOK 7 §6.5.2.2 + AACE 24R-03 and uses networkx.dag_longest_path() rather than hand-rolled graph code (its own docstring records that the PREVIOUS hand-rolled version silently miscomputed cycles), and diagnostic.py cites PMBOK 7 + AACE RP 80R-13, states every EVM formula, attributes the green/amber/red bands and returns {available:False, reason:...} rather than guessing. THE DEFECTS IT LOCKS OUT: (1) TWO IMPLEMENTATIONS - clientRollup re-computed EVM in the browser, uncited, and had already diverged by rounding pct_complete to an integer where the engine keeps the float, moving EV by up to 0.5% of BAC (about PHP 9,250 on a PHP 1,850,000 project) and being the number a supervisor saw whenever the edge fn had not answered; deleted, not patched, because a cost figure that silently disagrees with the cited engine is worse than none. (2) AN ABSENCE RENDERED AS A VERDICT - renderCpm defaulted to an empty critical path when the rollup was unavailable, so every task drew as NON-critical with ZERO slack, a positive and self-contradictory answer since zero slack is normally what MAKES a task critical; renderBudget likewise told a supervisor to 'add a budget, start date and end date' when all three already existed. Also asserts the EVM contract is registered partial_variant with a partial_reason admitting AC is an hours x rate PROXY (no cost ledger; the rate defaults to PHP 200/hr and measured, no caller passes the override). NOTE: this gate STRIPS COMMENTS before hunting the deleted implementation - its own first run failed because the comment RECORDING the removal quotes the removed code, the third comment-vs-code scanner bug found in one session. Teeth proven by reinstating the magic rate in code: FAIL.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "project_authority",
        "script":  "tools/validate_project_authority.py",
        "args":    [],
        "label":   "PJK3 - authority is enforced where the DATA is, not in the renderer. project-manager.html carried three claims only its renderer believed, each probed live as an ordinary worker and each true of the screen and false of the database: renderBudget printed 'Budget visibility is restricted to supervisors' while projects_hive_rw granted every member SELECT (a worker read CAP-2026-001's PHP 1,850,000 off the table, and budget_php was in the MAIN list select for everyone so the figure reached the browser before any pane decided to show it); the Delete button had no role check and neither did the DB, so a worker soft-deleted a shutdown project they did not own while hive_audit_log held ZERO project rows; and ackLog wrote acknowledged_by = WORKER_NAME with no role check, so any member could acknowledge any progress report including their own. A UI-only gate is not a control, it is a label. This gate asserts each is now enforced in the database. IT CHECKS MECHANISMS, NOT NAMES - the budget one is enforced by COLUMN PRIVILEGES and a column-level REVOKE is a NO-OP while a table-wide GRANT stands, which is exactly how the first attempt failed silently with information_schema still listing authenticated on budget_php while the page looked fixed. So it asks the CATALOG 'does authenticated hold SELECT on budget_php' rather than grepping a migration for the word REVOKE. Also holds: budget_php absent from v_project_truth (security_invoker, so re-adding it re-opens the read AND breaks every select('*') caller), the supervisor-only RPC still exists, and the three guard triggers are attached and NOT disabled (tgenabled <> 'D' - a disabled trigger looks present and enforces nothing). Teeth proven by re-granting the column: FAIL with the no-op warning inline.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "change_order_immutability",
        "script":  "tools/validate_change_order_immutability.py",
        "args":    [],
        "label":   "PJK1 - a raised change order is a CONTRACT, not a draft. project_change_orders carries cost_impact_php and schedule_impact_days, so approving one commits money. The approval ACT was already guarded (wh_guard_supervisor_approval refuses a non-supervisor approving, rejecting, or editing an APPROVED order - I asserted otherwise from the policy layer and the walk refuted me), but nothing protected the CONTENT or EXISTENCE of a request awaiting review. Proven live as an ordinary worker: rewrote ANOTHER worker's pending order from PHP 500,000 to PHP 9,999,999 and replaced its scope text, with the row still reading requested_by = Wilfredo Malabanan; the same worker could DELETE a pending order outright. That is the worst place for it to be open, because the supervisor approves WHAT THEY ARE SHOWN and what they are shown was editable by anyone in the hive right up to the click - and with no auth_uid on the table and no audit trigger on projects, nothing recorded that the figure changed or who changed it. The guard is IMMUTABILITY rather than a role check, chosen after measuring the legitimate callers: exactly THREE update paths exist (approveCO/rejectCO/cancelCO), all setting status plus approver fields, with NO edit-CO path anywhere and NO delete path in any page or edge fn - so the terms were already write-once in practice and making the database agree breaks zero callers. Holds: the function and trigger exist and the trigger still covers UPDATE **and DELETE** (losing DELETE silently restores the make-it-disappear path); every commercial term is still named in the check (a term dropped from that list becomes quietly editable again, which is exactly the failure found); status/approved_by/approved_at/rejection_reason are NOT frozen, because freezing them would break the lifecycle the table exists for; and LIVE that the trigger is attached and ENABLED (a disabled trigger looks present and enforces nothing). Teeth proven by unfreezing cost_impact_php: FAIL on exactly that term.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "project_tenancy",
        "script":  "tools/validate_project_tenancy.py",
        "args":    [],
        "label":   "PJK4 - a project child row's PARENT must live in its own hive. EARNED BY PROBING, not assumed from the AHK4 analogy: reads were measured clean first (as a member of one hive, projects/project_items/project_change_orders all return 0 rows for another hive - recorded as a real result), then the write side was probed with MY hive_id and ANOTHER hive's project_id and all three were ACCEPTED - project_items, project_progress_logs, and a project_change_order attached to a project the writer cannot even see. Every policy is a single PERMISSIVE FOR ALL testing only hive_id IN user_hive_ids(); nothing joined `projects`. AN INSTRUMENT NOTE: the first probes failed with 23514 (a status enum I got wrong) and 23502 (a NOT NULL I omitted), neither of which is 42501, and taking either as 'isolation holds' would have recorded a false clean - the same trap the AH13 walk hit. SEVERITY MEASURED AND HONESTLY CONTAINED: v_project_truth's rollup subqueries (item_count, estimated_total_hours, approved_change_orders, approved_co_cost_php, link_count) carry NO hive filter, and what stops an injected row reaching the VICTIM's numbers is that the view is security_invoker, so those subqueries run as the READER and RLS hides the foreign row - proven by injecting an item with 999 estimated hours and watching the victim's rollup stay at 7 items / 33.00 est hrs. FIXED ANYWAY because that containment rests entirely on security_invoker staying ON, and earlier the SAME DAY a routine CREATE OR REPLACE silently cleared that option from v_sensor_truth (CREATE VIEW does not carry reloptions forward) - one such slip on v_project_truth turns every unfiltered rollup into a cross-tenant read. Mig ...025 adds five RESTRICTIVE parent-hive policies; 0 existing rows violated them, so nothing that exists was rejected. Holds each policy's existence, RESTRICTIVE-ness (a PERMISSIVE rewrite ORs and would WIDEN while looking like a guard) and that it still JOINS `projects` to compare hive_id, plus a live cross-boundary row scan since WITH CHECK never applies retroactively. Teeth proven by dropping a policy: FAIL on exactly that table.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "reliability_tenancy",
        "script":  "tools/validate_reliability_tenancy.py",
        "args":    [],
        "label":   "AHK4 - reliability data is hive-private, PARENT included. FMEA modes, RCM strategies, Weibull fits and P-F intervals are competitive plant knowledge: what breaks, how often, and what a plant does about it. The AH13 walk found reads correctly isolated (0 rows across all four tables read as a member of another hive - recorded as a real result, not assumed) and WRITES carrying the PM13 gap: the WITH CHECK validated that hive_id is one the caller belongs to and said NOTHING about whether the PARENT lived there. Probed live, both accepted - an FMEA mode with hive_id=MINE and asset_id=ANOTHER hive's node, and a strategy with hive_id=MINE and fmea_mode_id=THEIRS. An instrument error nearly recorded that walk as clean: the first probe came back BLOCKED with 23514 and it looked like isolation holding, but 23514 is check_violation, not 42501 - the decision-enum CHECK rejecting an invalid value of the probe's OWN making, with the tenancy question still untested. Verify WHAT blocked a write, never merely THAT something did. Migration ...017 closed it with four RESTRICTIVE policies; this gate keeps them closed. It asserts each policy EXISTS, is RESTRICTIVE (a PERMISSIVE rewrite ORs with the others and WIDENS access while looking like a guard - a gate checking only 'a policy by this name exists' would pass that happily), and still JOINS the parent table to compare its hive rather than the row's own. Plus a live scan for rows that already cross a boundary, because WITH CHECK never applies retroactively - a guard added over existing violations would silently keep them. Teeth proven three ways: policy dropped -> FAIL, rewritten PERMISSIVE -> FAIL, weakened to own-hive-only -> FAIL, each with the right diagnosis.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "asset_identity_spine",
        "script":  "tools/validate_asset_identity_spine.py",
        "args":    [],
        "label":   "AH11 - a rename must carry the WHOLE identity, and only inside ONE hive. An asset's identity is stored as free text in five tables and sync_pm_asset_identity propagated a rename into exactly one: measured live, renaming GEN-002 moved 2 pm_assets rows and left 116 others pointing at a tag that no longer existed (103 logbook.machine, 12 fault_knowledge.machine, 1 asset_risk_scores). Every one of those columns holds the TAG even where the column is NAMED asset_name, so a display-name change is harmless and a TAG change is what breaks them. fault_knowledge has NO uuid column at all, so a rename severed the AI's learned fault corpus permanently; asset_risk_scores is worse still, because the asset page then renders 'No risk score yet for this asset... a score will appear here' - honest about a cold start, a lie about a rename. THE SECOND DEFECT WAS INTRODUCED BY THE FIX and is the reason this gate is per-statement: the first draft scoped its UPDATEs to 'any hive the caller belongs to', and tags are unique per HIVE not per platform, so renaming Lucena's GEN-002 also rewrote Manila Electronics Assembly's 60 logbook rows, 6 fault_knowledge rows, its risk score, a parts recommendation and a pm_asset. A cross-tenant write caused by the repair; found on the revert leg, repaired to 0 residual. Holds: every identity column is still updated by the RPC (a NEW table denormalising a tag goes red here and nowhere else), and EACH UPDATE carries BOTH hive_id = v_hive AND the caller's membership - v_hive alone would trust a tag from any tenant, membership alone IS the cross-tenant bug. Also that the merge guard exists and excludes the node being renamed (without that it refuses every rename, since callers update asset_nodes first), and that the caller passes the node id and surfaces the refusal. Teeth: the FIRST version of this gate counted '= v_hive' occurrences globally and PASSED with the bug reintroduced - it now slices the migration per UPDATE, and deleting one predicate fails exactly that statement. Live tier reports orphan tags (a row naming a tag no asset in its hive carries) - 77 pre-existing, 72 of them the logbook rows that have no asset_node_id to fall back on.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "fmea_priority_order",
        "script":  "tools/validate_fmea_priority_order.py",
        "args":    [],
        "label":   "AH7 - a severity-9 failure mode must never be buried by RPN. The RPN arithmetic was already sound (rpn is a GENERATED ALWAYS column S x O x D, inputs CHECK-bounded 1..10, 0 of 300 rows disagreeing) - the defect was what the page DID with it: every FMEA surface ordered by rpn DESC alone, and because RPN multiplies severity by how RARE and how DETECTABLE a failure is, a rare easily-caught SAFETY failure scores below a frequent nuisance. Measured: all 17 modes with severity >= 9 sat below the median RPN, and 'Cable insulation breakdown' (S9, safety, RPN 108) ranked BELOW 'Coupling misalignment beyond 0.05 mm' (S6, production, RPN 180) on every asset page and every printed report - a report whose own header cites AIAG-VDA 2019, the standard that replaced RPN precisely because of this. canonical/standards.json had already recorded the caveat; the UI sorted by RPN anyway. Fixed with an Action Priority band (severity >= 9 or RPN >= 200 -> H; severity >= 7 or RPN >= 100 -> M) that only ever RAISES priority, so nothing urgent today is quietly demoted, registered as a PARTIAL formula variant because it is the severity escalation both IEC 60812:2018 and AIAG-VDA agree on and NOT the full ~1000-cell AP lookup - inventing that table's contents would defeat the point of an auditable contract. Holds: the helpers exist, BOTH the screen and the printed report order through the same function (they must not disagree about which failure matters most), the report still explains why it is not in RPN order, and LIVE that rpn is still generated and S/O/D still bounded 1..10 - deliberately NOT 'sort by our own rule then assert our own rule', which can never fail. Teeth proven twice: reverting the report to rpn order -> FAIL, dropping the severity CHECK -> FAIL. Self-test pins the exact real pair above.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "refusal_reason_persists",
        "script":  "tools/validate_refusal_reason_persists.py",
        "args":    [],
        "label":   "AHK2 - when a reliability calculation REFUSES, the reason must outlive the click. python-api/reliability/weibull.py declines below MIN_FAILURES_FOR_FIT=4 and returns beta/eta NULL with an actionable diagnostic ('Need at least 4 corrective events in the lookback window (have 2). Log more breakdowns or widen since_days before refitting.') - that sentence is the entire product of the refusal, telling a planner why there is no number and how to get one. But weibull_fits had NO diagnostic column, so persistFit never wrote it, v_weibull_truth could not expose it, and loadLatestWeibull selected a column set that could not contain it: fit.diagnostic was undefined and the box was cleared. Visible for seconds after Compute, BLANK on every page load after. A planner opening the asset the next day saw beta '--', eta '--', a pill reading 'insufficient data', and an empty bordered box - the F18 dead-card shape landing on the most important sentence in the reliability workbench. Fixed by 20260728000014 plus the fitter writing it and the page selecting it on the LOAD path; verified live end-to-end. Holds the whole round-trip (column, view exposure, the view still returning the LATEST fit per asset via its DISTINCT ON, the fitter persisting, the page selecting, and every refused fit actually carrying a reason) because breaking ANY link restores the silent blank. Teeth proven twice: reverting the page select -> exit 1, dropping the column -> exit 1. Live-tier, skips without docker. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "approval_authority",
        "script":  "tools/validate_approval_authority.py",
        "args":    [],
        "label":   "AHK1 - approving and REJECTING are the same authority. wh_guard_supervisor_approval correctly refused a non-supervisor who APPROVED, but its UPDATE clause treated a status change as privileged only when 'approved' was on one side, so pending -> rejected slipped through on ALL SIX tables the trigger guards (asset_nodes, inventory_items, logbook, project_change_orders, rcm_fmea_modes, rcm_strategies). Walked live 2026-07-28 once the Asset Hub governance fixture existed to walk at all: a WORKER set their own pending asset to 'rejected' and wrote rejection_reason = 'Rejected by the supervisor - not fit for the register.' - 1 row, no error. Reachable rather than theoretical: asset-hub's reject is caught only incidentally because it also writes approved_by/approved_at, while hive.html's rejectItem() updates { status:'rejected' } ALONE behind a client-side role check. WHY THE REASON FIELD MATTERS: rejection_reason is the REVIEWER'S VOICE - the queue renders it back to the submitter as 'Why: ...' - so a submitter who can write it authors a verdict in a supervisor's name, and the next supervisor cannot tell. Fixed by 20260728000013 (reviewer_states + a rejection_reason clause); safe to tighten because both legitimate reject paths are already supervisor-only in their UI and service_role is exempt. Asserts the guard STATICALLY (both outcomes + the reason field still privileged) and LIVE in a rolled-back probe (worker self-reject blocked, reason-edit blocked, supervisor still able to reject). Teeth proven against the REAL pre-fix guard: 3 of 4 checks fail. Skips cleanly without docker or a worker-authored pending row. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        "id":      "project_manager_deepwalk_ratchet",
        "script":  "validate_project_manager_deepwalk.py",
        "args":    [],
        "label":   "Project Manager deepwalk expansion - forward-only %-board over 18 journeys x 5 phases + the PJK dimension classes, with the shallow-W guard (a journey may not be credited W=done on fewer than 2 personas AND 2 states). Opened 2026-07-28 after the Asset Hub arc closed at 100%; project-manager was chosen on measured evidence - the WRITE-HEAVIEST surface on the platform (21 client writes across 7 tables: 9 insert / 10 update / 2 delete), 68 interactive elements, 96 top-level functions, and the page where the platform handles MONEY and COMMITMENTS (a change order is a contract amendment, an Earned Value status is a health claim to a client, a critical path decides which work may slip). SIX GROUND FINDINGS the board exists to drive out, all measured before any journey: (G1) six of seven project tables have NO auth_uid column AT ALL - only `projects` has one - so approving a change order is attributable to nobody; (G2) `projects` appears nowhere in hive_audit_log.target_type while assets/logbook/inventory_items/marketplace/worker_profiles all do, so the write-heaviest domain has no tamper-evident trail; (G3) every child table validates only its OWN hive_id and no policy joins `projects`, which is the PM13/AHK4 child-parent gap un-probed; (G4) one PERMISSIVE FOR ALL policy per table means any active member can approve/reject/cancel a change order, and project_roles is the ONLY table with a supervisor predicate; (G5) project_change_orders = 0 rows and project_roles = 0 rows, so the entire CO approve/reject path and the role flow have never had data (the AHK3 class); (G6) renderBudget hides the budget from non-supervisors in the RENDERER while projects_hive_rw grants every member read, so the number is one db.from('projects') away. PJK4 is in the denominator from day one as a CANDIDATE precisely because the Asset Hub board read 100% with its fourth class untracked. Fails on a REGRESSION below the accepted baseline. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "asset_hub_deepwalk_ratchet",
        "script":  "validate_asset_hub_deepwalk.py",
        "args":    [],
        "label":   "Asset Hub deepwalk expansion - forward-only %-board over 18 journeys x 5 phases + the AHK dimension classes, with the shallow-W guard (a journey may not be credited W=done on fewer than 2 personas AND 2 states). Opened 2026-07-28 after the PM Scheduler arc closed at 100%; Asset Hub was chosen on measured evidence - the widest truth-view surface on the platform (11), 11 DB writes, the asset IDENTITY spine every other page reads, and TWO defects the PM arc found in it independently (an RCM interval rounded to a rarer frequency; a lazy pm_assets create with no role gate). THE GROUND FINDING the board exists to drive out: the page's whole governance workflow is UNWALKABLE - asset_nodes has 95 rows, all seeder-created with auth_uid NULL, all status='approved', ZERO pending, ZERO rejected, zero authored by any worker or supervisor, and rcm_strategies is 172 approved / 0 unapproved. That is why the PDDA arc's F21 (a worker's Pending-assets tile always reads 0) was never diagnosed: there has never been a pending asset to see. Plus 0 audit triggers across asset_nodes / rcm_strategies / rcm_fmea_modes (567 rows). Fails on a REGRESSION below the accepted baseline, so a walked cell cannot silently revert. Self-test: --selftest (pins the scoring and the shallow-W refusal).",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        "id":      "pm_deepwalk_ratchet",
        "script":  "validate_pm_deepwalk.py",
        "args":    [],
        "label":   "PM Scheduler deepwalk ANTI-DRIFT ratchet (PM_DEEPWALK_EXPANSION_ROADMAP.md §4) — the measured %-board (18 journeys x 5 phases G/W/O/H/R, PMK classes x 6 stages) held as a forward-only FLOOR, so a cell that was walked/observed/resolved/locked cannot silently revert. Computed from pm_deepwalk_state.json, never asserted in prose. Carries the shallow_W guard: a journey cannot claim W=done without >=2 personas AND >=2 states, which matters more here than anywhere yet — the PDDA arc NAMED the supervisor-plans/tech-completes split as its heavyweight and then walked it as one signed-in user, and this arc's keystone (27% of PM intervals ran late while the compliance RPC reports 85-87%) is invisible to any single-persona glance. `--accept` ratchets after real progress. Static/fast. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
    },
    {
        "id":      "logbook_deepwalk_ratchet",
        "script":  "validate_logbook_deepwalk.py",
        "args":    [],
        "label":   "Logbook deepwalk ANTI-DRIFT ratchet (LOGBOOK_DEEPWALK_EXPANSION_ROADMAP.md §4) — the measured %-board (20 journeys x 5 phases G/W/O/H/R, LG classes x 6 stages) held as a forward-only FLOOR, so a cell that was walked/observed/resolved/locked cannot silently revert. Computed from logbook_deepwalk_state.json, never asserted in prose. Carries the shallow_W guard: a journey cannot claim W=done without >=2 personas AND >=2 states recorded — the logbook is the platform's central WRITE surface (v_logbook_truth feeds ~11 pages), so a one-persona 'walk' here is exactly the shallowness that let the offline queue, the edit-others boundary and amendment propagation go unwalked for an entire prior arc. `--accept` ratchets the floor up after real progress. Static/fast. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
    },
    {
        "id":      "marketplace_deepwalk_ratchet",
        "script":  "validate_marketplace_deepwalk.py",
        "args":    [],
        "label":   "Marketplace deepwalk ANTI-DRIFT ratchet — the measured %-board (20 journeys x 5 phases, 10 MK classes x 6 stages) held as a forward-only FLOOR: a cell that was walked/observed/resolved/locked cannot silently revert. Lives at the repo root under this exact name because the flywheel orchestrator classifies a baseline rise by re-running `validate_<name>.py`; without it, every legitimate rise in the locked-cell count fell into the conservative 'unknown' bucket and was scored as ROT rather than as the adoption floor moving up (77 -> 84 -> 92 in one session). Delegates to tools/marketplace_deepwalk_scoreboard.py, which fails only when the board drops BELOW its accepted baseline; `--accept` ratchets.",
        "group":   "AI Validation",
        "report":  None,
    },
    {
        "id":      "marketplace_partsflow_continuity",
        "script":  "tools/validate_marketplace_partsflow_continuity.py",
        "args":    [],
        "label":   "Marketplace parts-flow continuity (MK6) — locks the inventory <-> marketplace ROUND TRIP that the 2026-07-24 deepwalk walked clean in both directions: (1) inventory emits a `from_inventory` Sell deep link AND marketplace READS it to prefill (a link nobody reads is a dead end); (2) buyer search matches `part_number`, the strong join key, because inventory is material-centric while listings are equipment-centric so their categories diverge and category alone can never match a part; (3) the listing detail routes back into inventory's own ledger-consistent receive path (`receive=1`); (4) LIVE check that `source_inventory_item_id` stays BASE-only and is never projected through v_marketplace_listings_truth, since it is one tenant's internal inventory id and would leak tenant topology to buyers. A clean walk is a real result but not a durable one — this is what makes it stay true. MK6.4 needs the DB and skips cleanly without it; the rest are static. Self-test: --selftest.",
        "group":   "AI Validation",
        "report":  None,
    },
    {
        "id":      "hidden_class_defined",
        "script":  "tools/validate_hidden_class_defined.py",
        "args":    [],
        "label":   "`.hidden` is actually DEFINED for every page that uses it — a page hiding an element with class=\"hidden\" must load a stylesheet carrying a GENERAL `.hidden` rule, or the element ships permanently VISIBLE. Found live 2026-07-24 (deepwalk J9, first-time-seller dashboard): a dead \"Load More\" button under an empty inquiry list, because marketplace-seller.html links only tokens.css and the JS toggled a class nothing defined. Six live pages were in that state (asset-hub / audit-log / community / marketplace-admin / marketplace-seller / shift-brain). Easy to ship because wh-tw.css DOES define it (so the pattern works on most pages), because components.css's rule is SCOPED to `.action-card .ac-cta.hidden` and grep makes it look covered, and because an ID base style like `#toast{display:flex}` outranks the class anyway. The gate resolves it the way the browser does: only a selector that is exactly `.hidden` counts. Design prototypes (*-test.html) and *.backup*.html are exempt (not shipped, not in the SW shell). Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        # HIVE DEEPWALK · HK1 detector — a role marker must not outlive the role.
        "id":      "role_marker_revocation",
        "script":  "tools/validate_role_marker_revocation.py",
        "args":    [],
        "label":   "A role-derived class on a PERSISTENT ROOT (<html>/<body>) must be removable — if a page adds one, it must also remove it on the path where the role is lost. Found live 2026-07-27 (hive deepwalk H8, remove-member): hive.html painted `html.is-supervisor` early from the CACHED role, deliberately, so supervisor-only chrome would not flash in after the async membership check (a CLS optimisation) — and nothing ever cleared it. Deleting a member's row left her browser carrying a supervisor marker on <html> with no supervisor membership behind it. Harmless ONLY because the board never renders for a removed member; the moment any supervisor-only rule keys off that class on a view a removed member CAN reach, it misapplies. Root elements are the whole point: unlike a card or a button they survive every view switch and re-render, so the removal has to be written, never inherited from a re-render. Verified as an instrument, not assumed — run against the pre-fix hive.html it FAILS on `is-supervisor`, against the fix it passes. Deliberately narrow so a PASS means something: only documentElement/body targets, only role-vocabulary names (so button-lock's `is-loading` is out of scope), and toggle() counts as a removal since it is add-and-remove by construction. This is HK1's SECOND assertion only; the first (a hidden element must be EMPTY, not display:none over real data) is a DOM property the live deepwalk checks, and folding it in here would let a static PASS imply a live guarantee it never made. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        # HIVE DEEPWALK · HK3 — one adopter per page for the canonical role.
        "id":      "role_adoption_centralized",
        "script":  "tools/validate_role_adoption_centralized.py",
        "args":    [],
        "label":   "A page may adopt the canonical role (`wh_hive_role`) from exactly ONE place. Adopting a role is not one assignment — it is the module variable, the stored hint, AND any paint derived from it, which must move together. Found live 2026-07-27 (hive deepwalk H8b/H8d/H3): hive.html had SEVEN role-adoption sites and only TWO repainted `html.is-supervisor`, whose rules are FUNCTIONAL not cosmetic (they set `#my-work-card{display:none}` and hide `#pm-overdue-alert` / `#stock-alert`). Each unreconciled site was its own door onto the same defect and THREE were confirmed live before being fixed: DEMOTION (role synced down, marker stayed, so a demoted worker lost their own work card and their overdue-PM and stock alerts while the page looked normal), SWITCH (a switch never reloads — it calls initBoard() in place — so the page-load fix never ran), and JOIN (joining a hive where you are a worker while holding supervisor paint from another). The lesson is not 'remember to repaint' but that a value with invariants attached needs ONE adopter, so the invariant is written once instead of remembered seven times; hive.html now routes everything through applyHiveRole(). Verified as an instrument rather than assumed: against pre-centralization hive.html it FAILS with 7 sites (lines 1807/1911/1980/2119/2256/2270/5234), against the fix it passes with 1. Reads are deliberately not counted — counting them would fail every page on the platform — and scan/test harnesses that plant a role to set up a scenario are exempt. Static/fast. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
    },
    {
        # HIVE DEEPWALK · H8c — the client role reader must understand every role the DB stores.
        "id":      "role_vocabulary_coverage",
        "script":  "tools/validate_role_vocabulary_coverage.py",
        "args":    [],
        "label":   "The canonical client RBAC reader (wh-roles.js) must UNDERSTAND every role value hive_members actually stores. Found live 2026-07-27 (hive deepwalk H8c): the reader reads the AUTH key wh_hive_role (which holds what hive_members.role holds) while its capability map mirrors nav-hub's `roles` arrays, written in DISPLAY vocabulary (field|supervisor|engineer). The two were never reconciled. hive_members.role stores 'worker', 'worker' was absent from ROLES, so whRole() returned '' — and an empty role is DELIBERATELY permissive (solo mode / new install, so a lone tech is not locked out of their own tools). Two reasonable decisions combined into an unreasonable outcome: every real worker (12 of 16 seeded memberships) silently satisfied can('approve'), can('manage_hive') and can('audit_log'). Confirmed in the browser before fixing, and fixed by mirroring nav-hub's own worker->field mapping rather than inventing a second vocabulary. It shipped harmless ONLY by accident — nothing calls can() yet — while every comment in the codebase tells the next change to use it, so a latent trap stayed one call away. Compares against LIVE distinct roles on purpose: the failure was a mismatch between what the DB WRITES and what the client UNDERSTANDS, so reading only source would compare the client against itself. Verified as an instrument: against the pre-fix reader it FAILS on 'worker', against the fix it passes, and a hypothetical future role ('contractor') is caught by the selftest. Skips clean when the DB is down. Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # HIVE DEEPWALK · HK2 gate — a shipped capability must be reachable by the data that EXISTS.
        "id":      "fixture_capability_coverage",
        "script":  "tools/validate_fixture_capability_coverage.py",
        "args":    [],
        "label":   "A shipped capability must be EXERCISABLE by the data that actually exists, not merely by seeder code that could create it. Found 2026-07-27 (hive deepwalk): the hive switcher shipped, worked, and was UNWALKABLE BY CONSTRUCTION — seeded data was strictly one worker to one hive, so the switch button (rendered only when the list exceeds 1) never appeared and the switch path had never once been exercised. The moment a fixture existed, the first walk found a real defect behind it: the member list refreshed ONLY when the user had no active hive, so a membership granted after sign-in stayed invisible until sign-out. The class then proved itself again, which is why this gate queries LIVE ROWS rather than reading the seeder: walking H8 (remove a member) DELETED the platform's only multi-hive membership, the seeder still contained the code to create one, and the capability was silently unwalkable again — a seeder-reading gate would have said PASS while the fixture was gone. Prevents the most expensive false confidence there is: a green suite over a feature nobody can enter, which looks exactly like coverage. A failed query counts as a shortfall, never a silent pass, since \"I could not tell\" must not read as \"covered\". Skips clean when the local DB is down (fixture check, not an uptime check). Self-test: --selftest.",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc R (S, OWASP A02) — no real secret in a TRACKED .env.* file (the hardcoded-secrets
        # scanner only opens *.html/js/ts/py, so a committed dotfile credential is invisible). Teeth.
        "id":      "committed_env_secret",
        "script":  "tools/validate_committed_env_secret.py",
        "args":    [],
        "label":   "Arc R: committed .env secret (no credential in a tracked dotfile; A02)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (S, OWASP A08) — Subresource Integrity on every version-pinned third-party CDN script
        # (CDN compromise => arbitrary JS on the authed page). Floating tags = pin-first backlog. Teeth.
        "id":      "sri_cdn_scripts",
        "script":  "tools/validate_sri.py",
        "args":    [],
        "label":   "Arc R: SRI on pinned CDN scripts (supply-chain; A08)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R (S, OWASP A09 meta) — the SAST posture map must cover the FULL OWASP Top-10 (A07/A09/A10
        # were absent => a false "every category covered" claim). Asserts sast_scan.OWASP completeness. Teeth.
        "id":      "sast_owasp_complete",
        "script":  "tools/validate_sast_owasp_complete.py",
        "args":    [],
        "label":   "Arc R: SAST OWASP-map completeness (full Top-10 mapped; meta)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc R aggregate board — the 4-lens (X/Z/S/P) ratcheted security posture, OWASP-mapped.
        # Runs the underlying validators as subprocesses (slow) — skip in --fast. Floors X100/Z100/S95/P90.
        "id":      "security_adversarial_sweep",
        "script":  "tools/security_adversarial_sweep.py",
        "args":    [],
        "label":   "Arc R: security/adversarial sweep (4 lenses, OWASP Top-10, ratcheted)",
        "group":   "AI Validation",
        "report":  "security_adversarial_results.json",
        "skip_if_fast": True,
    },
    # ── Arc S — Resilience / DR / Chaos (4 lenses F/R/C/D; floors F90/R95/C100/D85) ──
    # 13 file-static gates run in --fast; the aggregate board (skip_if_fast) re-runs all
    # 15 incl. the 2 DB-runtime cells and enforces the floors + the forward-only ratchet.
    {"id": "dedup_constraints",      "script": "validate_dedup_constraints.py",   "args": [], "label": "Arc S/C: dedup UNIQUE constraints (exactly-once on retries)", "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "optimistic_lock",        "script": "validate_optimistic_lock.py",     "args": [], "label": "Arc S/C: optimistic-lock compare-and-set (no lost-update)",     "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "optimistic_ui",          "script": "validate_optimistic_ui.py",       "args": [], "label": "Arc S/C: optimistic-UI rollback (no phantom-saved row)",        "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "atomic_writes",          "script": "validate_atomic_writes.py",       "args": [], "label": "Arc S/C: atomic multi-step writes (no partial-write corruption)", "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "dependency_timeout",     "script": "validate_dependency_timeout.py",  "args": [], "label": "Arc S/F: dependency timeout (no infinite hang)",                "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "ai_alldown_degrade",     "script": "validate_ai_alldown_degrade.py",  "args": [], "label": "Arc S/F: AI all-down degrade (no silent empty)",                "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "cdn_resilience",         "script": "validate_cdn_resilience.py",      "args": [], "label": "Arc S/F: CDN resilience (no silent dead lib)",                  "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "circuit_breaker",        "script": "validate_circuit_breaker.py",     "args": [], "label": "Arc S/F: external circuit-breaker (Resend/CMMS, no hammering)",  "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "offline_resilience",     "script": "validate_offline_resilience.py",  "args": [], "label": "Arc S/D: offline write queue (no lost field write)",            "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "offline_queue_retry",    "script": "validate_offline_queue_retry.py", "args": [], "label": "Arc S/D: offline-queue retry/backoff/dead-letter",              "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "precache_coverage",      "script": "validate_precache_coverage.py",   "args": [], "label": "Arc S/D: precache + offline navigation fallback (no blank tab)", "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "degraded_mode",          "script": "validate_degraded_mode.py",       "args": [], "label": "Arc S/D: backend-degraded detection (not just navigator.onLine)", "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    {"id": "dr_claims",              "script": "validate_dr_claims.py",           "args": [], "label": "Arc S/R: DR claims backed (no false-sense recovery doc)",       "group": "Resilience / DR", "report": None, "skip_if_fast": False},
    # The 2 DB-runtime cells are ALSO aggregated by resilience_dr_sweep below, but are registered
    # individually here so auto-discovery sees every root validate_*.py registered. DB-runtime -> skip in --fast.
    {"id": "data_backup",            "script": "validate_data_backup.py",         "args": [], "label": "Arc S/R: logical dump + restore drill + documented restore path (DB-runtime)", "group": "Resilience / DR", "report": None, "skip_if_fast": True},
    {"id": "dataloss_detection",     "script": "validate_dataloss_detection.py",  "args": [], "label": "Arc S/R: rowcount-snapshot data-loss monitor live (DB-runtime)",          "group": "Resilience / DR", "report": None, "skip_if_fast": True},
    {
        # Arc S aggregate board — the 4-lens (F/R/C/D) ratcheted resilience posture. Re-runs all 15
        # validators incl. the 2 DB-runtime cells (data_backup restore drill + dataloss monitor) as
        # subprocesses (slow) — skip in --fast. Floors F90/R95/C100/D85; baseline resilience_dr_baseline.json.
        "id":      "resilience_dr_sweep",
        "script":  "tools/resilience_dr_sweep.py",
        "args":    [],
        "label":   "Arc S: resilience / DR sweep (4 lenses F/R/C/D, ratcheted)",
        "group":   "Resilience / DR",
        "report":  "resilience_dr_results.json",
        "skip_if_fast": True,
    },
    {
        # Memory-System M1.1 — the agent's own memory.db (~90MB Memento SQLite/FTS5) had ZERO backup
        # and corrupts on interrupted hook writes. This drill proves the full backup->restore round-trip
        # deterministically every run: online-backup the live DB to scratch, reopen, PRAGMA integrity_check,
        # assert chunk count ~matches + schema version, run a REAL FTS5 BM25 query on the restored copy
        # (proves the index, not just the bytes), drop scratch. ~2s DB-runtime op -> skip in --fast.
        "id":      "memory_db_backup",
        "script":  "tools/memory_db_backup.py",
        "args":    ["--drill", "--vacuum-drill"],
        "label":   "Memory M1.1+M1.2: memory.db backup/restore round-trip + VACUUM shrink drill",
        "group":   "Memory System",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # AI6 - agentic write accountability (dimension-expansion loop 21). AI1-AI5 grade the AI's
        # ANSWER; this grades its ACT. 14 AI edge fns WRITE, and 6 write into tables a human later
        # reads as fact. Found live: visual-defect-capture stamped model-generated diagnosis into
        # fault_knowledge under the signed-in WORKER'S NAME, and that table feeds RAG + intelligence
        # reports - so AI output re-entered the knowledge base as human field experience. Denominator
        # is curated by evidence (telemetry tables excluded; self-declaring ai_* tables excluded).
        # Forward-only ratchet on ai_write_provenance_baseline.json. Static grep -> runs in --fast.
        "id":      "ai_write_provenance",
        "script":  "tools/validate_ai_write_provenance.py",
        "args":    [],
        "label":   "AI6: agentic write accountability (AI writes into human-read domain tables declare machine authorship)",
        "group":   "AI Validation",
        "report":  "ai_write_provenance_report.json",
    },
    {
        # Memory-System M2.1 — retrieval recall@k gate. Retriever tuning knobs (IMPORTANCE,
        # half-lives, MIN_FINAL_SCORE, transcript boost, RRF k) were eyeballed with no guard.
        # 25 grounded (query -> canonical memory) golden pairs run against the LIVE retriever;
        # gate on fixed health floors (R@3>=0.60, R@5>=0.80, MRR>=0.40). A tuning change that
        # silently drops recall now FAILs. ~7s of retrieval -> skip in --fast.
        "id":      "memory_recall_eval",
        "script":  "tools/memory_recall_eval.py",
        "args":    [],
        "label":   "Memory M2.1: retriever recall@k eval (25 golden pairs, ratcheted to health floors)",
        "group":   "Memory System",
        "report":  "memory_recall_baseline.json",
        "skip_if_fast": True,
    },
    {
        # Memory-System M3.1 — write-side quality gate for curated memory topic files. The indexer
        # derives a chunk's TYPE (-> importance + decay = ranking) from frontmatter then a filename
        # prefix; a file with neither valid type silently indexes as 'unknown' (degraded recall
        # forever), and empty name/description starve the title/FTS signal. Imports the REAL indexer
        # (parse_frontmatter + detect_type) so the lint can't drift. Static (no DB) -> runs in --fast.
        "id":      "memory_write_quality",
        "script":  "tools/memory_write_quality.py",
        "args":    ["--quiet"],
        "label":   "Memory M3.1: topic-file write-quality lint (type/name/description, no silent type=unknown)",
        "group":   "Memory System",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Memory-System M2.2 — health-regression gate. The retriever's health metrics (silent_rate,
        # p95 latency, file-grounded %, index size) were dashboard-only; this wraps the SAME
        # build_payload() in thresholds so a regression FAILs automatically. Honors the warming-up
        # honesty flag (defers activity thresholds when retrievals_today < 10). Fast DB read.
        "id":      "memory_health_gate",
        "script":  "tools/memory_health_gate.py",
        "args":    [],
        "label":   "Memory M2.2: retriever health-regression gate (silent_rate / median+p95 latency / grounding thresholds)",
        "group":   "Memory System",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Memory-System M3.2 — supersedes down-rank mechanism integrity. A memory with a
        # `supersedes: <slug>` field replaces the older one; the retriever down-ranks the
        # superseded chunk so an obsolete decision can't co-surface with its reversal. The
        # self-test proves the whole chain (frontmatter->map->loader->down-rank) so a future
        # retriever refactor that breaks the penalty FAILs. Static + deterministic -> runs in --fast.
        "id":      "memory_supersedes",
        "script":  "tools/memory_supersedes.py",
        "args":    ["--self-test"],
        "label":   "Memory M3.2: supersedes down-rank mechanism (superseded memory ranks below its replacement)",
        "group":   "Memory System",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Memory-System M4.1 — transcript retention/pruning mechanism. The DB grows unbounded
        # (transcripts ~31% of chunks; vector_query full-scans every row). The scheduled prune
        # (backup-guarded, weekly, 60d retention, wired into SessionStart) evicts ONLY old
        # never-retrieved transcripts. The self-test proves on a real-schema scratch DB that it
        # prunes ONLY old never-retrieved transcripts and keeps retrieved/fresh/curated intact.
        "id":      "memory_prune_transcripts",
        "script":  "tools/memory_prune_transcripts.py",
        "args":    ["--self-test"],
        "label":   "Memory M4.1: transcript prune mechanism (evicts only old never-retrieved, keeps the rest)",
        "group":   "Memory System",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Companion-Memory C1.1 — the per-dimension memory regression gate (ai_eval_gate.companion_gate,
        # G0 via validate_companion_dim_gate.py) is only as strong as its locked-test power. This self-test
        # PINS that the gate actually BLOCKS (exit 1) a degraded memory locked-test run and PASSES a clean
        # one — the C1.1 acceptance bar — deterministically over the REAL locked-test membership, with zero
        # live-LLM calls (so it can't flake). Degrades-to-SKIP if the locked-test memory set is too small
        # to construct an honest blocking scenario. Static + deterministic -> runs in --fast.
        "id":      "companion_memory_gate_teeth",
        "script":  "tools/companion_gate_teeth.py",
        "args":    [],
        "label":   "Companion Memory C1.1: memory gate has teeth (degraded locked-test FAILs, clean passes)",
        "group":   "Companion Memory",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Companion-Memory C2.1 — store-level supersedes down-rank (SAFETY). When an episodic
        # memory is corrected/replaced, the obsolete row must be down-ranked at retrieval so an
        # outdated procedure cannot co-surface as current. Static-checks the migration superseded_by
        # column + RPC ×0.4 penalty + the guarded recallEpisodic down-rank, then proves it LIVE in a
        # rolled-back transaction (zero pollution): match_procedural_memories returns both procedures,
        # then only the replacement after one is superseded. Degrades-to-SKIP if the local DB is down.
        "id":      "companion_memory_supersedes",
        "script":  "tools/companion_supersedes.py",
        "args":    [],
        "label":   "Companion Memory C2.1: supersedes down-rank (obsolete procedure ranks below its replacement)",
        "group":   "Companion Memory",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Companion-Memory C3.1 — backup + restore DRILL of the durable companion memory tables
        # (agent_episodic_memory + agent_memory). pg_dump -> restore into a scratch schema -> assert
        # rowcount round-trips AND a seeded canary fact is still RECALLABLE from the restored copy
        # (rowcount alone doesn't prove the memory survives queryably). Runs against the LOCAL docker
        # DB only; zero net pollution (canary removed in finally). Degrades-to-SKIP if the DB is down.
        # Heavier (pg_dump/restore) so it runs in the full suite, not --fast (the DR-drill convention).
        "id":      "companion_memory_backup_drill",
        "script":  "tools/companion_memory_backup.py",
        "args":    ["--drill"],
        "label":   "Companion Memory C3.1: backup + restore drill (rowcount round-trip + recall survives restore)",
        "group":   "Companion Memory",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Companion-Memory C3.2 — memory health-regression gate (M2.2 pattern). Thresholds the
        # groundable store-health signals: structural (episodic + agent_memory non-empty) + integrity
        # (procedural null-embedding rate — un-embedded procedures are invisible to
        # match_procedural_memories forever). Warming-up clause defers the rate on a tiny sample;
        # surfaces (informational) the response_time_ms/intent_confidence instrumentation gap that
        # blocks latency/silent_rate gating. Degrades-to-SKIP if the local DB is down.
        "id":      "companion_memory_health_gate",
        "script":  "tools/companion_memory_health_gate.py",
        "args":    [],
        "label":   "Companion Memory C3.2: memory health gate (structural + procedural-embedding integrity)",
        "group":   "Companion Memory",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Companion-Memory C2.3 — re-embed retry for null procedural memories. A procedural memory
        # stored with embedding=NULL is invisible to match_procedural_memories forever (the
        # "invisible-forever" skill-library bug). The self-test proves the mechanism in a rolled-back
        # txn (null -> invisible -> re-embed -> searchable); the operational --backfill re-embeds live
        # nulls via embedding_helper (degrades-to-SKIP without a provider). Deterministic self-test.
        "id":      "companion_memory_reembed",
        "script":  "tools/companion_reembed_procedural.py",
        "args":    ["--self-test"],
        "label":   "Companion Memory C2.3: re-embed-retry (null procedural becomes searchable after re-embed)",
        "group":   "Companion Memory",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Companion-Memory C2.2 — write-side semantic dedup. persistEpisodic now MERGES a near-duplicate
        # procedural memory (bump use_count, keep higher importance) instead of inserting a paraphrase
        # (store-hygiene: the skill library doesn't bloat with restatements). Self-test proves it in a
        # rolled-back txn: a near-dup embedding is detected + merged (1 row, use_count bumped) while an
        # orthogonal one is NOT (a distinct procedure still inserts). Deterministic; degrades-to-SKIP if DB down.
        "id":      "companion_memory_dedup",
        "script":  "tools/companion_dedup.py",
        "args":    [],
        "label":   "Companion Memory C2.2: write-side semantic dedup (near-dup procedural merges, distinct inserts)",
        "group":   "Companion Memory",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc I I1/I — account-enumeration resistance (OWASP ASVS V2.2). Login must use a uniform
        # invalid-credential response (no user-exists tell); signup username-availability must route
        # through the rate-limitable check_username_available RPC (by-design carve-out). Static; baseline 0.
        "id":      "signup_enumeration_safety",
        "script":  "tools/validate_signup_enumeration_safety.py",
        "args":    [],
        "label":   "Arc I: account-enumeration resistance (login uniform-response + signup RPC carve-out; ASVS V2.2)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc I I7/I — signup bot-protection wiring (OWASP ASVS V2.1 anti-automation). Asserts the
        # configure-to-enable Turnstile in-page integration stays intact (container + loader + token
        # hand-off, inert when unconfigured). Live bot-block = Supabase dashboard enrollment (attributed).
        "id":      "signup_bot_protection",
        "script":  "tools/validate_signup_bot_protection.py",
        "args":    [],
        "label":   "Arc I: signup bot-protection wiring (Turnstile configure-to-enable; ASVS V2.1)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc I I8/I — GDPR/PDPA account offboarding: soft-deactivate + anonymize. The deactivate_my_account()
        # RPC must be self-scoped (auth.uid, no IDOR param), anonymize PII, PRESERVE operational records,
        # revoke hive access, and be hardened (search_path + PUBLIC-revoke). Static; baseline 0.
        "id":      "account_deactivation",
        "script":  "tools/validate_account_deactivation.py",
        "args":    [],
        "label":   "Arc I: account offboarding (self-scoped anonymize, preserve records; GDPR/PDPA)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc I I7/A — server-side brute-force lockout: the `login` edge proxy gates by a real
        # failed-attempt counter (5 tries → 423) so a correct password can't bypass a lock. Needs live DB.
        "id":      "login_proxy_lockout",
        "script":  "tools/validate_login_proxy_lockout.py",
        "args":    [],
        "label":   "Arc I: login brute-force lockout (server-side proxy; correct pw can't bypass a lock)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc I I3/I — password recovery: supervisor-assisted (no-email field workers) + email fallback.
        # The supervisor-reset is scoped (active supervisor → same-hive WORKER only, not a peer supervisor,
        # no cross-hive) + audit-logged. Needs live DB/runtime for the behavioral half; skip in --fast.
        "id":      "password_recovery",
        "script":  "tools/validate_password_recovery.py",
        "args":    [],
        "label":   "Arc I: password recovery (supervisor-assisted scoped + email fallback)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc H H4/I — OWASP LLM05 output-handling: EXECUTE companion renderMarkdown on XSS payloads,
        # assert escaping (no live tag survives). Hermetic (node), so runs in --fast.
        "id":      "companion_output_escaping",
        "script":  "tools/validate_companion_output_escaping.py",
        "args":    [],
        "label":   "Arc H: companion output escaping (LLM05 — untrusted LLM output can't XSS)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Analytics Engine arc I3/I6 — EXECUTE send-report-email buildEmailHtml on XSS payloads
        # injected into hiveName / r.type / r.summary, assert escaping (no raw tag survives).
        # Hermetic (node), so runs in --fast.
        "id":      "report_email_escaping",
        "script":  "tools/validate_report_email_escaping.py",
        "args":    [],
        "label":   "Analytics I3/I6: send-report-email HTML-injection (every email sink escaped)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Analytics Engine arc AI2/F5 — assert the AI action-plan synthesis reads only REAL
        # 4-phase output keys (no stale key → ungrounded prompt → fabrication). Static, --fast-safe.
        "id":      "analytics_synthesis_grounding",
        "script":  "tools/validate_analytics_synthesis_grounding.py",
        "args":    [],
        "label":   "Analytics AI2/F5: action-plan synthesis reads real phase keys (no ungrounded AI)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Analytics Engine arc page invariants — chart CLS reserve (A4), ISO-55001 honest
        # composite label (AI3), no rendered em-dash (A6). Static, --fast-safe.
        "id":      "analytics_page",
        "script":  "tools/validate_analytics_page.py",
        "args":    [],
        "label":   "Analytics A4/AI3/A6: page invariants (chart CLS reserve, honest label, no em-dash)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Analytics arc F1b — the canonical get_oee_by_machine RPC must derive Quality from
        # good_units/total_units (not quality_pct-only), or report/asset-hub OEE diverges from
        # the analytics page. Static (reads the migration SQL) → --fast-safe.
        "id":      "oee_quality_derivation",
        "script":  "tools/validate_oee_quality_derivation.py",
        "args":    [],
        "label":   "Analytics F1b: canonical OEE quality = good/total (report matches page)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Analytics arc F1d — canonical PM compliance overall_pct = WEIGHTED (SMRP 2.1.1
        # Σcompleted/Σscheduled), not the unweighted mean that contradicts the N-of-M count.
        # Static (migration SQL + descriptive.py) → --fast-safe.
        "id":      "pm_compliance_weighted",
        "script":  "tools/validate_pm_compliance_weighted.py",
        "args":    [],
        "label":   "Analytics F1d: PM compliance is SMRP-weighted (hero matches its count)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Arc I — LIVE data-layer auth proofs via docker psql (synthetic-email isolation, non-active
        # member exclusion, server-derived tenancy, auth-migration backfill trigger). Needs the local
        # DB up, so skip in --fast (mirrors the rls_tenant_isolation / definer_tenant_gate live gates).
        "id":      "auth_live_db",
        "script":  "tools/validate_auth_live_db.py",
        "args":    [],
        "label":   "Arc I: live data-layer auth proofs (synthetic-email/non-active/server-tenancy/backfill)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc I I3/A — GoTrue enforces credential strength server-side (live probe → 422 weak_password).
        # Needs the local stack up, so skip in --fast.
        "id":      "auth_live_gotrue",
        "script":  "tools/validate_auth_live_gotrue.py",
        "args":    [],
        "label":   "Arc I: live credential-strength (GoTrue rejects weak password, 422)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc I I2/A — shared-device idle expiry, LIVE (Playwright drives the real session-timeout.js via the
        # WH_IDLE_TIMEOUT_OVERRIDE clock-seam): idle→prompt→Continue→hard-clear+redirect. Needs the seeder; skip in --fast.
        "id":      "auth_idle_timeout_live",
        "script":  "validate_auth_idle_timeout_live.py",
        "args":    [],
        "label":   "Arc I: live idle-timeout (shared-device idle→prompt→hard-clear sequence)",
        "group":   "AI Validation",
        "report":  "auth_idle_timeout_live_report.json",
        "skip_if_fast": True,
    },
    {
        # Arc I I4/U — role-gated UI render LIVE (journey-permissions): supervisor-only blocks render per role,
        # worker/anon gated+redirected. Needs the seeder + live stack; skip in --fast.
        "id":      "auth_role_render_live",
        "script":  "validate_auth_role_render_live.py",
        "args":    [],
        "label":   "Arc I: live role-gated render (RBAC at the render layer; supervisor-only visibility)",
        "group":   "AI Validation",
        "report":  "auth_role_render_live_report.json",
        "skip_if_fast": True,
    },
    {
        # Arc I I7/F — AI rate-limit ENFORCED live (LLM10): boundary test drives the counter to the limit →
        # real AI edge invoke returns 429. Self-resetting. Needs seeder + edge + docker psql; skip in --fast.
        "id":      "auth_rate_limit_live",
        "script":  "validate_auth_rate_limit_live.py",
        "args":    [],
        "label":   "Arc I: live AI rate-limit enforcement (counter at limit → 429; LLM10)",
        "group":   "AI Validation",
        "report":  "auth_rate_limit_live_report.json",
        "skip_if_fast": True,
    },
    {
        # Arc I I4/A — function-level role guard LIVE: a worker-role JWT → 403 on the supervisor-only
        # export-hive-data edge fn (RBAC at the function layer, not UI-only). Needs seeder+edge; skip in --fast.
        "id":      "auth_role_guard_live",
        "script":  "validate_auth_role_guard_live.py",
        "args":    [],
        "label":   "Arc I: live function-level role guard (worker → 403 on supervisor-only fn)",
        "group":   "AI Validation",
        "report":  "auth_role_guard_live_report.json",
        "skip_if_fast": True,
    },
    {
        # CMMS Integrations PDDA I1/I2/I3 — live authz gate. Real supervisor+worker JWTs vs the
        # served edge + PostgREST: a supervisor CANNOT sync a FOREIGN hive's config_id (BOLA),
        # a worker CANNOT read the plaintext auth_token or repoint a config's endpoint_url, and
        # the supervisor's OWN-config path still works (no over-block). Needs seeder+edge; skip --fast.
        "id":      "integration_configs_authz_live",
        "script":  "tools/validate_integration_configs_authz_live.py",
        "args":    [],
        "label":   "CMMS: live integration_configs authz (config_id BOLA closed; worker token-read/write blocked)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # CMMS Integrations PDDA F1004/I6/F1-b — live webhook hardening gate. A validly-signed
        # webhook with an OLD/FAR-FUTURE timestamp is REJECTED (replay window ±300s), a malformed
        # body → 400 (not 500), wrong-sig → 401, fresh → 200. Complements the F5 idempotency gate.
        # Needs edge + docker psql; skip --fast.
        "id":      "cmms_webhook_security_live",
        "script":  "tools/validate_cmms_webhook_security_live.py",
        "args":    [],
        "label":   "CMMS: live webhook security (replay window + malformed 400 + wrong-sig 401 + fresh 200)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # CMMS Integrations PDDA F1/F2 — live entity-sync gate. The webhook's asset.updated /
        # inventory.updated / pm.overdue handlers were silent no-ops; this drives real signed
        # webhooks and asserts the DB effect (external_sync asset/inventory/pm_schedule rows +
        # inventory_items, idempotent qty update). Self-contained, reseed-resilient. Skip --fast.
        "id":      "cmms_entity_sync_live",
        "script":  "tools/verify_cmms_entity_sync_live.py",
        "args":    [],
        "label":   "CMMS: live entity sync (asset/inventory/pm.overdue webhook handlers land in DB, idempotent)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": True,
    },
    {
        # Arc H H8/I — prompt-injection posture (LLM01): no AI fn interpolates untrusted input INTO the
        # system prompt (role separation). Static; baseline 0. The probabilistic jailbreak residual is
        # the live fabrication/Family-E sweep, not this gate.
        "id":      "ai_prompt_injection",
        "script":  "tools/validate_ai_prompt_injection.py",
        "args":    [],
        "label":   "Arc H: AI prompt-injection posture (untrusted input out of system prompt; LLM01)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # D25 PII-egress floor: the shared _shared/redactPII.ts scrubber (which every AI fn's
        # answer routes through via the ai-gateway) must not regress its ISO-timestamp carve-out —
        # a loose PHONE_RE once ate the YYYY-MM-DD head of ISO datetimes, leaking `<phone>T..`
        # into asset-brain answers. Deterministic ($0, no deno/DB/model): re-runs the carve-out
        # from the .ts regex sources — ISO survives, real phones/emails still redact. Tagged
        # `# DEEPWALK-CELL: ai:* D25` → locks the D25 egress cell across every AI fn.
        "id":      "redact_iso",
        "script":  "tools/validate_redact_iso.py",
        "args":    [],
        "label":   "Arc H: PII-egress redaction ISO carve-out (D25, shared redactPII.ts, deterministic)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # K2 multi-turn PII egress (2026-07-12, live-caught): the current-turn message is fully
        # redacted, but an email/phone a worker stated in a PRIOR turn is carried raw in the
        # agent_memory turn_text + semantic journal recall — the forwarded memory_block AND the
        # summariser transcript were scrubbed only by redactKnownNames (NAMES), so those emails/
        # phones reached the model provider raw (proven: pii.leak.test@plant.ph survived twice).
        # Fix = redactMemoryText (names + email/phone via scrubExceptISO) wired into BOTH egress
        # sites. Deterministic ($0, no deno/DB/model): structural (both sites use redactMemoryText,
        # neither regresses to bare redactKnownNames; the helper still scrubs email/phone) +
        # behavioral (re-runs the scrub from the .ts regex sources: email/phone die, ISO survives).
        # Tagged `# DEEPWALK-CELL: ai:* D25` → locks the multi-turn half of the D25 egress cell.
        "id":      "memory_pii_redaction",
        "script":  "tools/validate_memory_pii_redaction.py",
        "args":    [],
        "label":   "Arc H: multi-turn PII egress rail (K2, memory_block + summariser, redactMemoryText)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # K1 gateway-bypass floor (2026-07-12, live-caught): assistant.html's Step-2 fallback
        # POSTed the full system prompt + chat history + KB context to a PUBLIC Cloudflare Worker
        # (workhive-assistant.*.workers.dev) — no auth / no PII redaction / no rate-limit / no
        # memory — whenever the slow orchestrator path timed out. Fixed by routing the fallback
        # through ai-gateway 'voice-journal'. This gate locks the whole class forward-only:
        # scans every root *.html for a browser network call to a BANNED external model endpoint
        # (*.workers.dev / api.groq/openai/anthropic / openrouter / gemini / mistral / cerebras /
        # /chat/completions / /v1/messages). Ext-1 "gateway-route EVERY AI call". Deterministic.
        # Tagged `# DEEPWALK-CELL: ai:* D26` → locks the AI-gateway-reuse cell across every page.
        "id":      "no_ai_gateway_bypass",
        "script":  "tools/validate_no_ai_gateway_bypass.py",
        "args":    [],
        "label":   "Arc H: no browser page bypasses ai-gateway with a direct external-model call (K1, Ext-1)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # K4 companion-context PII floor (2026-07-12): companion-launcher only transmits a page's
        # setContext summary to the LLM when piiSafe:true. The gateway's context redaction scrubs
        # email/phone but NOT a name in prose — so a piiSafe summary interpolating owner_name/
        # display_name/etc. ships a person's name to the model. project-manager/report shipped that
        # shape (flag omitted → summary dropped → grounding dead); K4 set piiSafe:true + stripped
        # the names + added an asset-hub asset context. This gate locks the invariant forward-only:
        # a piiSafe:true summary interpolating a *_name person field FAILs. Deterministic ($0).
        # Tagged `# DEEPWALK-CELL: ai:* D25` → the client half of the D25 egress cell.
        "id":      "setcontext_pii_safe",
        "script":  "tools/validate_setcontext_pii_safe.py",
        "args":    [],
        "label":   "Arc H: piiSafe companion context carries no person-name (K4, page_context egress)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # X-FIND ASR-confidence clarify-gate (2026-07-12, live-caught): a garbled voice transcript
        # (Cebuano 40% fidelity) was sent to the companion, which CONFABULATED a grounded answer on the
        # garble. Fix = a 4-layer additive chain surfacing the indigenous ASR's avg_logprob so the
        # voice-journal page CLARIFIES ("say it again") below a floor instead of grounding a mis-heard
        # question. This gate asserts every link (asr_server avg_logprob -> audio-chain passthrough ->
        # voice-transcribe low_confidence -> voice-journal.html early-return gate) so the signal can't
        # silently stop flowing. Deterministic ($0). Tagged `# DEEPWALK-CELL: ai:* D27`.
        "id":      "asr_confidence_gate",
        "script":  "tools/validate_asr_confidence_gate.py",
        "args":    [],
        "label":   "Arc H: ASR-confidence clarify-gate intact (X-FIND, garbled voice does not confabulate)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # V-axis V6 SOVEREIGNTY ratchet (NATIVE_AI_ROADMAP.md #6): every AI capability that touches a hot
        # path must keep BOTH a LOCAL-FIRST env-gated slot (so a plant runs inference in-house + its data
        # never leaves the plant) AND a FALLBACK (so no single external provider — or a local outage — can
        # take the feature down). Asserts it across all 4: Embeddings (BGE_EMBED_URL), ASR (WH_ASR_URL),
        # TTS (WH_TTS_URL, built this session), LLM (WH_LLM_URL, built this session). A future edit that
        # drops a local slot or a fallback FAILs. Deterministic ($0). Tagged `# DEEPWALK-CELL: ai:* D28`.
        "id":      "indigenous_stack",
        "script":  "tools/validate_indigenous_stack.py",
        "args":    [],
        "label":   "Arc H/V: indigenous stack keeps local-first + fallback (data sovereignty, no hard external dep)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # AI Companion arc (2026-07-13): voice_family_probe caught the companion leaking its
        # persona/chain-of-thought scaffold verbatim ("We need to respond as Zaniah, strategist...
        # the worker says:...") — an UNTAGGED reasoning leak the <think>-tag strip missed. Case 3
        # in _shared/ai-chain.ts stripReasoningBlocks (+ both Python mirrors) returns "" so callAI
        # falls to the next model. This gate is the teeth: strips the leak corpus, spares real
        # answers, and asserts the TS + both Python mirrors keep the Case-3 signature (no drift).
        "id":      "reasoning_scaffold_strip",
        "script":  "validate_reasoning_scaffold_strip.py",
        "args":    [],
        "label":   "AI Companion: reasoning/persona-scaffold leak strip (untagged CoT never reaches the reply)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # D13 action-faithfulness (CL10 fabrication class): the advisory read-only brain must not
        # claim a COMPLETED system write it didn't make ("Log entry added", "Updated maintenance
        # record"). Asserts the shared rail (_shared/action_provenance.ts) is intact, the ai-gateway
        # centralizes it over EVERY advisory route (a new uncovered advisory route FAILs), and every
        # generative AI fn resolves to a fabrication guard. Deterministic ($0, no deno/DB/model).
        # Tagged `# DEEPWALK-CELL: ai:* D13` → locks the D13 fabrication cell across every AI fn.
        "id":      "ai_fabrication_contract",
        "script":  "tools/validate_ai_fabrication_contract.py",
        "args":    [],
        "label":   "Arc H: AI action-faithfulness rail centralized (D13, no fabricated completed-write)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Deepwalk D18 — destructive-safety: delete/reset flows are gated by the SHARED whConfirm
        # modal (a real cancellable gate, loaded platform-wide, guarding 26 destructive call-sites).
        # A regression that strips confirms below the floor FAILs. Deterministic ($0, static).
        "id":      "destructive_safety",
        "script":  "tools/validate_destructive_safety.py",
        "args":    [],
        "label":   "Deepwalk D18: destructive-safety (delete/reset confirm-gated via shared whConfirm)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Deepwalk D19 — idle-session robustness: the singleton Supabase client auto-refreshes +
        # persists the session and refreshes on tab-wake (visibilitychange) so an idle tab never
        # fires a stale-token 401; no surface opts out. Deterministic ($0, static).
        "id":      "session_resilience",
        "script":  "tools/validate_session_resilience.py",
        "args":    [],
        "label":   "Deepwalk D19: idle-session robustness (autoRefreshToken + wake-refresh, no stale 401)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Deepwalk D20 — client resilience: every client request is AbortController-timeout-bounded
        # (no infinite spinner on a hung backend) + the offline-banner/connectivity widgets exist.
        # Deterministic ($0, static).
        "id":      "client_resilience",
        "script":  "tools/validate_client_resilience.py",
        "args":    [],
        "label":   "Deepwalk D20: client resilience (timeout-bounded fetch + offline/connectivity UX)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # Deepwalk content-surface fold — the 45 static /learn articles' presentation floor across
        # D4 (a11y: lang/single-h1/heading-order-honoring-aria-level/img-alt), D5 (responsive
        # viewport), D7 (no dynamic-HTML sink on a static page), D17 (structurally whole). Caught +
        # fixed a real h2→h4 heading skip on the platform-overview guide. Deterministic ($0, static).
        "id":      "content_page_hygiene",
        "script":  "tools/validate_content_page_hygiene.py",
        "args":    [],
        "label":   "Deepwalk content fold: /learn article presentation floor (D4/D5/D7/D17)",
        "group":   "Platform",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "artifact_alignment",
        "script":  "tools/validate_artifact_alignment.py",
        "args":    [],
        "label":   "AI Self-Improvement: Artifact-Alignment Correctness (§13.13)",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        # §13.13 A4 LIVE tier: diagram dimension labels == calc results (needs python-api
        # :8000, so skip in --fast; the readiness gate already requires it in full mode).
        "id":      "diagram_value_alignment",
        "script":  "tools/validate_diagram_value_alignment.py",
        "args":    [],
        "label":   "AI Self-Improvement: Diagram-Value Alignment (§13.13 A4, live)",
        "group":   "AI Validation",
        "report":  "diagram_value_alignment.json",
        "skip_if_fast": True,
    },
    {
        # §13.15 A6: STATIC full-coverage grounding field-contract — every BOM/SOW agent's
        # results.<field> reads must resolve to a real calc output key (forward-only baseline;
        # a NEW drift cell FAILs). Needs python-api :8000 for the live calc key-sets → skip_if_fast.
        "id":      "grounding_contract",
        "script":  "tools/validate_grounding_contract.py",
        "args":    [],
        "label":   "AI Self-Improvement: Grounding Field-Contract (§13.15 A6, forward-only)",
        "group":   "AI Validation",
        "report":  "grounding_contract.json",
        "skip_if_fast": True,
    },
    {
        # §13.13 live grounding tier: the LLM-generated BOM/SOW must cite the calc's
        # primary sized values (catches a hallucinated/dropped quantity). Needs
        # python-api + edge + a (free-tier) model key → skip in --fast.
        "id":      "bom_sow_grounding",
        "script":  "tools/validate_bom_sow_grounding.py",
        "args":    [],
        "label":   "AI Self-Improvement: BOM/SOW Grounding-Consistency (§13.13, live LLM)",
        "group":   "AI Validation",
        "report":  "bom_sow_grounding.json",
        "skip_if_fast": True,
    },
    {
        # §13.16 A7.1: page AI-narrative must cite only TRUE platform numbers (no fabricated
        # metric in rendered prose) — the whole-platform analogue of the companion gate.
        # Invokes a narrative edge fn live (auth + free-tier LLM) → prose #s ⊆ grounding-set.
        # Needs edge + auth + a model key → skip in --fast.
        "id":      "narrative_grounding",
        "script":  "tools/validate_narrative_grounding.py",
        "args":    [],
        "label":   "AI Self-Improvement: Narrative Grounding (§13.16 A7.1, live LLM)",
        "group":   "AI Validation",
        "report":  "narrative_grounding.json",
        "skip_if_fast": True,
    },
    {
        # Arc H proof→LIVE battery: invokes each AI surface end-to-end (user JWT + live LLM + real DB)
        # and asserts a DETERMINISTIC invariant on the live response (fallback fallover via W4 fault-inject,
        # PII-egress redaction, eval-runner verdict, TS↔py chain parity, Whisper transcription round-trip,
        # companion anti-fabrication rail). Drives the Arc-H live-subset to ~100%. Needs the serving edge
        # runtime + compute-API + a model key → skip in --fast (returns 0 / records 0 live when env is down).
        "id":      "ai_live_invoke",
        "script":  "tools/validate_ai_live_invoke.py",
        "args":    [],
        "label":   "Arc H: AI Live-Invoke battery (proof→LIVE, live LLM + edge runtime)",
        "group":   "AI Validation",
        "report":  "ai_live_invoke_results.json",
        "skip_if_fast": True,
    },
    {
        # §13.16 A7.2: CSV export-value contract — each exported column maps to a real source
        # field (a renamed/missing column exports BLANK). Static + docker psql (no LLM/browser).
        "id":      "export_value_contract",
        "script":  "tools/validate_export_value_contract.py",
        "args":    [],
        "label":   "AI Self-Improvement: Export-Value Contract (§13.16 A7.2, CSV)",
        "group":   "AI Validation",
        "report":  "export_value_contract.json",
        "skip_if_fast": True,
    },
    # ── Sentinel Architecture (Layer 0 -> Layer 2 bridge) ────────────────────
    # Runs the sentinel pipeline: coverage map (per-check), gap proposer,
    # pattern consistency, depth, freshness. Produces sentinel_health.json
    # for the Platform Guardian to surface alongside validator results.
    # See SENTINEL_ARCHITECTURE.md and feedback_hardening_loop for the
    # two-bridge model. Cheap (~1-2s deterministic) so always runs.
    {
        "id":      "sentinel-review",
        "script":  "run_sentinel_review.py",
        "args":    [],
        "label":   "Sentinel Review (L0->L2 bridge: coverage + pattern + depth + freshness)",
        "group":   "Sentinel",
        "report":  "sentinel_health.json",
        "skip_if_fast": False,
    },
    {
        "id":      "sentinel-baseline",
        "script":  "validate_sentinel_baseline.py",
        "args":    [],
        "label":   "Sentinel Baseline Ratchet (forward-only behavioral coverage; locks at first run)",
        "group":   "Sentinel",
        "report":  "sentinel_baseline_report.json",
        "skip_if_fast": False,
    },
    # ── SEO / Marketing Closed Loop (2026-05-17) ──────────────────────────────
    # 8 validators that enforce every promise the SEO + marketing work made:
    # no em-dashes, contact consistency, GA4 coverage, audience block per article,
    # tool-aligned CTA per article, sitemap sync, llms.txt sync, AI chain mirror.
    # Without these, today's wins regress silently. Each runs in <1 sec, no DB.
    {
        "id":      "em-dash",
        "script":  "validate_em_dash.py",
        "args":    [],
        "label":   "Em-Dash Validator (no em or en dashes in public body text)",
        "group":   "SEO Closed Loop",
        "report":  "em_dash_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "feedback-widget",
        "script":  "validate_feedback_widget.py",
        "args":    [],
        "label":   "Feedback Widget Validator (3-layer: script wiring + form integrity + schema RLS/rate-limit/resolved_at)",
        "group":   "Platform Feedback",
        "report":  "feedback_widget_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "contact-consistency",
        "script":  "validate_contact_consistency.py",
        "args":    [],
        "label":   "Contact Consistency Validator (3-layer: no stale hello@/ian.beronio37@ + canonical admin@ present)",
        "group":   "SEO Closed Loop",
        "report":  "contact_consistency_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ga4-coverage",
        "script":  "validate_ga4_coverage.py",
        "args":    [],
        "label":   "GA4 Coverage Validator (4-layer: GA4 block + wh-ga4.js load + canonical ID + custom-events file)",
        "group":   "SEO Closed Loop",
        "report":  "ga4_coverage_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "audience-block",
        "script":  "validate_audience_block.py",
        "args":    [],
        "label":   "Audience Block Validator (3-layer: every /learn/ article has Who-this-is-for + 4+ roles + beyond-technicians)",
        "group":   "SEO Closed Loop",
        "report":  "audience_block_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "tool-aligned-cta",
        "script":  "validate_tool_aligned_cta.py",
        "args":    [],
        "label":   "Tool-Aligned CTA Validator (4-layer: every /learn/ article anchors to a /<tool>.html, names the tool, avoids a join-only CTA, and links the tool its REGISTRY ENTRY DECLARES). ★L4 ADDED 2026-08-28 AFTER THE FIRST THREE PASSED OVER NINE MIS-DECLARED ARTICLES. L1 asks whether an article links SOME tool and L2 whether it names A tool; neither compares the CTA to the article's own declared tool_path in wh_pages.LEARN_ARTICLES. Nine disagreed - an article titled 'Maintenance Project Planning' declared /hive.html, a CMMS-integration article declared /hive.html, an AI-quality article declared /analytics-report.html - and because learn-link.js keys the page-guide chip off tool_path, SIX pages showed no help at all while their guides sat on unrelated pages, project-manager (the platform's most complex page) among them. Adding the one comparison found all nine and took guide coverage from 19 to 25 of 32 pages with NO content written. L4 exempts a non-tool declared target, because the platform overview declares /index.html and links all 26 tools - flagging it would be the check misreading its own denominator.",
        "group":   "SEO Closed Loop",
        "report":  "tool_aligned_cta_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "sitemap-sync",
        "script":  "validate_sitemap_sync.py",
        "args":    [],
        "label":   "Sitemap Sync Validator (3-layer: sitemap URLs <-> filesystem in sync + metadata complete)",
        "group":   "SEO Closed Loop",
        "report":  "sitemap_sync_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "llms-sync",
        "script":  "validate_llms_sync.py",
        "args":    [],
        "label":   "llms.txt Sync Validator (4-layer: every article in llms.txt + no stale slugs + sections + contact)",
        "group":   "SEO Closed Loop",
        "report":  "llms_sync_report.json",
        "skip_if_fast": False,
    },
    {
        "id":      "ai-chain-mirror",
        "script":  "validate_ai_chain_mirror.py",
        "args":    [],
        "label":   "AI Chain Mirror Validator (4-layer: Python ai_chain.py mirrors TS _shared/ai-chain.ts PROVIDER_CHAIN)",
        "group":   "SEO Closed Loop",
        "report":  "ai_chain_mirror_report.json",
        "skip_if_fast": False,
    },

    # ── P1 Roadmap (turns 5-7, re-registered after revert 2026-05-27) ────────
    {"id": "truth-view-contract",          "script": "validate_truth_view_contract.py",          "args": [], "label": "Truth-View Contract (every v_*_truth declares _source_count/_freshness_ts/_canonical_version)",                       "group": "P1 Roadmap", "report": "truth_view_contract_report.json",          "skip_if_fast": False, "severity": "blocker",    "parallel_safe": True},
    {"id": "envelope-conformance",         "script": "validate_envelope_conformance.py",         "args": [], "label": "Envelope Conformance (every edge fn imports _shared/envelope.ts OR is exempt)",                                       "group": "P1 Roadmap", "report": "envelope_conformance_report.json",         "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "health-endpoint",              "script": "validate_health_endpoint.py",              "args": [], "label": "Health Endpoint (every load-bearing edge fn handles /health)",                                                        "group": "P1 Roadmap", "report": "health_endpoint_report.json",              "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "render-budget",                "script": "validate_render_budget.py",                "args": [], "label": "Render Budget (per-page HTML + inline JS + external script ratchet)",                                                "group": "P1 Roadmap", "report": "render_budget_report.json",                "skip_if_fast": False, "severity": "warn",       "parallel_safe": True},
    # PJ18: a stray comment block above <!DOCTYPE> put project-manager.html into QUIRKS MODE
    # (measured document.compatMode === 'BackCompat') and rendered the comment as page text.
    # node --check reads only <script> bodies and the byte-count gates were happy, so nothing
    # caught it — a browser recovers silently, which is what makes it dangerous.
    {"id": "doctype-first",                "script": "validate_doctype_first.py",                "args": [], "label": "Doctype First (nothing may precede <!DOCTYPE html> — content there forces quirks mode)",                        "group": "P1 Roadmap", "report": None,                                       "skip_if_fast": False, "severity": "blocker",    "parallel_safe": True},
    # PJ18: migration 029 retyped a parameter, so CREATE OR REPLACE made a SECOND overload instead
    # of replacing. PostgREST then refused BOTH call shapes with PGRST203 and project creation was
    # dead — in production. psql resolves overloads by literal type, so every psql check passed.
    {"id": "rpc-overloads",                "script": "validate_rpc_overloads.py",                "args": [], "label": "RPC Overloads (no PostgREST-exposed function may have two signatures — PGRST203 kills the endpoint)", "group": "P1 Roadmap", "report": None,                                       "skip_if_fast": True,  "severity": "blocker",    "parallel_safe": True},
    # PJ15: project-manager embedded scope items and lessons into project_knowledge since Phase 6.5
    # and NOTHING read that table — no db function, no edge function, no page. The page paid an
    # embedding round-trip on every save into an index no answer could ever reach.
    {"id": "embedding-index-read",         "script": "validate_embedding_index_is_read.py",      "args": [], "label": "Embedding Index Is Read (every table with an embedding column has a reader, not just a writer)", "group": "P1 Roadmap", "report": None,                                       "skip_if_fast": True,  "severity": "regression", "parallel_safe": True},
    # PJ2/PJ6/PJ12/PJ14 ratchet: four guards this arc earned by live probe had NOTHING asserting
    # they still existed. Each check is a PROPERTY, not an existence test — a function can exist
    # with its guard clause deleted. Self-test: --selftest.
    {"id": "project-guard-spine",          "script": os.path.join("tools", "validate_project_guard_spine.py"), "args": [], "label": "Project Guard Spine (code-reuse / budget-write / progress-record / attribution-pin / delete-audit / lessons guards still hold)", "group": "P1 Roadmap", "report": None,                        "skip_if_fast": True,  "severity": "blocker",    "parallel_safe": True},
    {"id": "migration-immutability-strict","script": "validate_migration_immutability_strict.py","args": [], "label": "Migration Immutability Strict (sha256 every migration; FAIL on edit-after-first-observation)",                        "group": "P1 Roadmap", "report": "migration_immutability_strict_report.json","skip_if_fast": False, "severity": "blocker",    "parallel_safe": True},
    {"id": "substrate-manifest",           "script": os.path.join("tools", "build_substrate_manifest.py"), "args": [], "label": "Substrate Manifest (L-1.5: aggregate all 13 pattern miners + drift detectors into one view)",          "group": "P1 Roadmap", "report": "substrate_manifest.json",                  "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "fullstack-gate-coverage",      "script": os.path.join("tools", "audit_fullstack_gate_coverage.py"), "args": [], "label": "Full-Stack × Gate Coverage Meta-Gate (every artefact named in the study's 13×6 matrix must exist)", "group": "P1 Roadmap", "report": "fullstack_gate_coverage_report.json",      "skip_if_fast": False, "severity": "blocker",    "parallel_safe": True},
    {"id": "rate-limit-adoption",          "script": "validate_rate_limit_adoption.py",          "args": [], "label": "Rate-Limit Adoption (every callAI fn calls checkAIRateLimit/checkUserRateLimit/checkRouteRateLimit)",                "group": "P1 Roadmap", "report": "rate_limit_adoption_report.json",          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "llm-cache-adoption",           "script": "validate_llm_cache_adoption.py",           "args": [], "label": "LLM Cache Adoption (count of fns using cached() from _shared/cache.ts; floor ratchet)",                              "group": "P1 Roadmap", "report": "llm_cache_adoption_report.json",           "skip_if_fast": False, "severity": "warn",       "parallel_safe": True},
    {"id": "structured-log-adoption",      "script": "validate_structured_log_adoption.py",      "args": [], "label": "Structured Log Adoption (count of fns importing + calling log.* from _shared/logger.ts; floor ratchet)",            "group": "P1 Roadmap", "report": "structured_log_adoption_report.json",      "skip_if_fast": False, "severity": "warn",       "parallel_safe": True},
    {"id": "edge-error-capture",           "script": "validate_edge_error_capture.py",           "args": [], "label": "Edge Error-Capture Adoption (Arc T/T2 keystone: every edge fn routes through serveObserved so unhandled throws aggregate to wh_traces via trackError; L1 wrapper-integrity + L2 forward-only adoption floor; live fault-inject-proven)", "group": "P1 Roadmap", "report": "edge_error_capture_report.json",          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "observability-fault-walk",     "script": os.path.join("tools", "observability_fault_walk.py"), "args": [], "label": "Observability Fault-Inject Walk (Arc T/T2 LIVE proof: injects an unhandled throw via the auth-gated chaos hook -> asserts a wh_traces error row lands with the same trace_id + a non-leaky 500 envelope + anon cannot inject; SKIPs cleanly if local Supabase is down)", "group": "P1 Roadmap", "report": "observability_fault_walk_report.json", "skip_if_fast": True,  "severity": "regression", "parallel_safe": False},
    {"id": "slo-rollup",                   "script": "validate_slo_rollup.py",                   "args": [], "label": "SLO Error-Budget Rollup Gate (Arc T/T3: v_wh_traces_slo view + slo_error_budget() RPC exist AND compute correctly - per-route error counts excl. 401/403/429 policy rejections, windowed 28d/6h/1h, burn = error_rate / 1% SLO budget; proven by a seeded rolled-back self-test; SKIPs if local DB down)", "group": "P1 Roadmap", "report": "slo_rollup_report.json", "skip_if_fast": True,  "severity": "regression", "parallel_safe": False},
    {"id": "grafana-slo-dashboard",        "script": "validate_grafana_slo_dashboard.py",        "args": [], "label": "Grafana SLO Provisioning Gate (Arc T/T5+T4: the repo-provisioned golden-signal dashboard workhive-slo-arct - 6 panels over wh_traces/v_wh_traces_slo on the supabase_local datasource - AND the alert routing (rule wh_slo_edge_errors + contact point + policy) exist, parse, and stay wired; static, no running Grafana needed)", "group": "P1 Roadmap", "report": "grafana_slo_dashboard_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "grafana-reader-reads",         "script": "validate_grafana_reader_reads.py",         "args": [], "label": "Grafana-Reader Read-Path Gate (Operator-Console->Grafana: LIVE-queries every dashboard-dependency table AS grafana_reader and fails if a read errors or is RLS-partially-blinded - catches the silent-blindness class that dark'd the SLO alert + founder-console observe tables; SKIPs if local DB down)", "group": "P1 Roadmap", "report": "grafana_reader_reads_report.json", "skip_if_fast": True,  "severity": "regression", "parallel_safe": False},
    {"id": "reproducible-build-pin",       "script": "validate_reproducible_build_pin.py",       "args": [], "label": "Reproducible Build Pin (L1 .tool-versions + L2 package-lock + L3 engines.node agreement)",                        "group": "P1 Roadmap", "report": "reproducible_build_pin_report.json",       "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "live-page-journeys",           "script": "validate_live_page_journeys.py",           "args": [], "label": "Arc K Live-Page Journeys Ratchet (live-as-a-user JTBDs never regress non-live + deterministic floor never grows; reads live_page_journeys_results.json vs baseline)", "group": "Arc K", "report": "live_page_journeys_check_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "arc-v-effortless",             "script": "validate_arc_v_effort.py",                "args": [], "label": "Arc V EFFORTLESS 3-Lens Ratchet (E: total click-hops CEILING - any new friction fails + excess-click debt never grows; L: cognitive-load floor never grows - Miller >7-choice/>40 walls/competing CTAs; F: Doherty flow floor never grows - slow-and-silent actions + dead-ends; reads arc_v_results.json vs baseline)", "group": "Arc V", "report": "arc_v_effort_check_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "arc-v-capstone",               "script": "validate_arc_v_capstone.py",              "args": [], "label": "Arc V EFFORTLESS Family-Capstone Ratchet (cross-page JOBS stay effortless: continuity never breaks - no hop bounces to sign-in / loses hive context; cumulative hop-cost never exceeds ideal; reads arc_v_capstone_results.json vs baseline)", "group": "Arc V", "report": "arc_v_capstone_check_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "arc-w-visual",                 "script": "validate_arc_w_visual.py",                "args": [], "label": "Arc W VISUAL UI/UX 9-Lens Ratchet (D depth/H focal/W whitespace/G grouping/C consistency/V dashboard/T color-type/M-S motion-state/I icon - every per-lens violation floor is a forward-only CEILING so visual quality can't rot; + M/S control-state CSS-rule floor :active/:focus-visible can't drop; reads arc_w_results.json vs baseline)", "group": "Arc W", "report": "arc_w_visual_check_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "arc-y-intuition",              "script": "validate_arc_y_intuition.py",             "args": [], "label": "Arc Y INTUITION GRADIENT Ratchet (per-page 5-lens novice floor never drops: L1 jargon-without-gloss can't grow / L3 first-paint overwhelm ceiling / L4 displayed-value-vs-DB-truth can't go silent / L5 in-app BACK can't disappear / L6 dead-links+unlabeled can't grow; 3-persona gradient measured by intuition_gradient_harness.mjs; reads intuition_gradient_report.json vs intuition_gradient_baseline.json)", "group": "Arc Y", "report": "arc_y_intuition_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "fb2-browser-ci-persona",       "script": "validate_browser_ci_persona.py",          "args": [], "label": "FB2 Browser-CI Multi-Persona Live Floor Ratchet (every page walked HEADLESS as field-tech/supervisor/new-worker/admin = role x viewport x hive; the persona-delta live floor - per-persona runtime/security/serious-a11y breakage a single-identity sweep can't see - never falls + every persona's sign-in holds; reads browser_ci_persona_board.json vs baseline; full re-drive = node tools/browser_ci_persona_walk.mjs --accept)", "group": "Forward-Build", "report": "browser_ci_persona_check_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "fb4-grounding-eval",           "script": os.path.join("tools", "fb4_grounding_eval.py"), "args": ["--accept"], "label": "FB4 Live-LLM Grounding/Fabrication Eval (invokes the served LLM edge fns with DIVERSE ASKER PERSONAS - earnest/edge-case/adversarial-injection/Tagalog - and grades answer-named asset-tags subset-of the hive DB truth = no fabrication; adversarial must not leak a secret; Tagalog must still get a grounded answer; free-tier $0 single invokes; forward-only pass ratchet vs fb4_grounding_baseline.json)", "group": "Forward-Build", "report": "fb4_grounding_results.json", "skip_if_fast": True, "severity": "regression", "parallel_safe": False},
    {"id": "mine-rls-policies",            "script": os.path.join("tools", "mine_rls_policies.py"), "args": [], "label": "RLS Policy Substrate Miner (L-1.5: USING(true) / WITH CHECK(true) / missing TO clause)",                     "group": "P1 Roadmap", "report": "rls_policy_mining_report.json",            "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "mine-cache-name-drift",        "script": os.path.join("tools", "mine_cache_name_drift.py"), "args": [], "label": "Cache-Name Drift Miner (L-1: SHELL_FILEs committed after sw.js — bump CACHE_NAME warning)",            "group": "P1 Roadmap", "report": "cache_name_drift_report.json",             "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    # `report` corrected from `rls_policy_mining_report.json` to this gate's OWN artifact. It only READS the
    # mining report (validate_rls_strict.py:39 FAILs if that file is absent and tells you to run the miner
    # first) and writes `rls_strict_baseline.json` itself. Declaring the producer's file made two gates claim
    # one artifact and described the data flow backwards — caught by the registry check added below in main().
    {"id": "rls-strict",                   "script": "validate_rls_strict.py",                   "args": [], "label": "RLS Strict Baseline (L0 ratchet over mine_rls_policies: USING(true) + WITH CHECK(true) frozen at baseline)",         "group": "P1 Roadmap", "report": "rls_strict_baseline.json",                 "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "envelope-return-shape",        "script": "validate_envelope_return_shape.py",        "args": [], "label": "Envelope Return-Shape Adoption (true adoption: fns that actually call ok(ctx, ...); floor ratchet)",                 "group": "P1 Roadmap", "report": "envelope_return_shape_report.json",        "skip_if_fast": False, "severity": "warn",       "parallel_safe": True},
    {"id": "mine-capacity-signals",        "script": os.path.join("tools", "mine_capacity_signals.py"), "args": [], "label": "Capacity-Signals Miner (LB G-1.5: realtime channel/subscribe/teardown + unbounded select shape)",          "group": "Maturity P1", "report": "capacity_signals_report.json",            "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "connection-surface-discovery", "script": "validate_connection_surface_discovery.py", "args": [], "label": "Connection-Surface Discovery (LB G-1: every subscribing surface registered + budgeted)",                    "group": "Maturity P1", "report": "connection_surface_discovery_report.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "connection-pool-saturation",   "script": "validate_connection_pool_saturation.py",   "args": [], "label": "Connection-Pool Saturation Ratchet (LB GH: leak surfaces frozen at 0 + surface count + alarm declared)",   "group": "Maturity P1", "report": "connection_pool_saturation_report.json",   "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "load-resilience",              "script": "validate_load_resilience.py",              "args": [], "label": "Load-Resilience Sentinel (LB GS: load_probe + LOAD-SLO + DEGRADED-MODE + 429/503 graceful degrade)",       "group": "Maturity P1", "report": "load_resilience_report.json",              "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "mine-health-surface",          "script": os.path.join("tools", "mine_health_surface.py"), "args": [], "label": "Health-Surface Miner (AV G-1.5: /health coverage shape across all edge fns)",                            "group": "Maturity P1", "report": "health_surface_report.json",               "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "health-surface-discovery",     "script": "validate_health_surface_discovery.py",     "args": [], "label": "Health-Surface Discovery (AV G-1: count of fns without /health frozen at baseline — new health-less fn FAILs)", "group": "Maturity P1", "report": "health_surface_discovery_report.json",     "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "game-day-readiness",           "script": "validate_game_day_readiness.py",           "args": [], "label": "Game-Day Readiness (AV GH: game_day + verify_backups + RTO/RPO + rollback runbook + SLO all present)",       "group": "Maturity P1", "report": "game_day_readiness_report.json",           "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "mine-cache-signals",           "script": os.path.join("tools", "mine_cache_signals.py"), "args": [], "label": "Cache-Signals Miner (CA G-1.5: CDN _headers + LLM cached() adopters + SW shell precache shape)",                "group": "Maturity P2", "report": "cache_signals_report.json",                "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "mine-rate-limit-signals",      "script": os.path.join("tools", "mine_rate_limit_signals.py"), "args": [], "label": "Rate-Limit-Signals Miner (RL G-1.5: per-fn rate-limit primitives + verifiedHiveId bucketing-key shape)",  "group": "Maturity P2", "report": "rate_limit_signals_report.json",          "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "cache-hit-rate",               "script": "validate_cache_hit_rate.py",               "args": [], "label": "Cache Efficiency Ratchet (CA GH: CDN Cache-Control rules present + LLM cached() adopter floor)",                     "group": "Maturity P2", "report": "cache_hit_rate_report.json",               "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "rate-limit-fairness",          "script": "validate_rate_limit_fairness.py",          "args": [], "label": "Rate-Limit Fairness Sentinel (RL GS: no fn buckets on a spoofable client hive_id; latent ratchet; keystone fair)", "group": "Maturity P2", "report": "rate_limit_fairness_report.json",          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "mine-deploy-signals",          "script": os.path.join("tools", "mine_deploy_signals.py"), "args": [], "label": "Deploy-Signals Miner (H G-1.5: edge fn registration/deploy coverage + rollback/pre-deploy presence)",          "group": "Maturity P3", "report": "deploy_signals_report.json",               "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "mine-ci-signals",              "script": os.path.join("tools", "mine_ci_signals.py"), "args": [], "label": "CI-Signals Miner (CI G-1.5: workflow count/triggers + gate-running + .tool-versions pin)",                     "group": "Maturity P3", "report": "ci_signals_report.json",                   "skip_if_fast": False, "severity": "info",       "parallel_safe": True},
    {"id": "deploy-safety",                "script": "validate_deploy_safety.py",                "args": [], "label": "Deploy-Safety Sentinel (H GS: rollback runbook + pre-deploy gate + undeployed-fn ratchet)",                  "group": "Maturity P3", "report": "deploy_safety_report.json",                "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "ci-gate-sentinel",             "script": "validate_ci_gate_sentinel.py",             "args": [], "label": "CI-Gate Sentinel (CI GS: local ci_gate + a workflow runs the gate + reproducible pin + wired trigger)",     "group": "Maturity P3", "report": "ci_gate_sentinel_report.json",             "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "log-surface-discovery",        "script": "validate_log_surface_discovery.py",        "args": [], "label": "Log-Surface Discovery (L G-1: count of fns logging raw console.* without logger.ts frozen at baseline)",     "group": "Maturity P3", "report": "log_surface_discovery_report.json",        "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "log-correlation",              "script": "validate_log_correlation_sentinel.py",     "args": [], "label": "Log-Correlation Sentinel (L GS: structured logger + trace_id correlation + JSON + trace-store aggregation)",  "group": "Maturity P3", "report": "log_correlation_report.json",              "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "openapi-sync",                 "script": "validate_openapi_sync.py",                 "args": [], "label": "OpenAPI Sync (A capability: openapi.json covers every edge fn in ALL_FUNCTIONS, no ghost routes; re-run tools/gen_openapi.py on drift)", "group": "Maturity P4", "report": "openapi_sync_report.json",                 "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "sast-scan",                    "script": os.path.join("tools", "sast_scan.py"), "args": [], "label": "SAST Posture (S capability: every OWASP Top-10 category has an automated scanner; aggregates the 12 security validators)", "group": "Maturity P4", "report": "sast_report.json",                          "skip_if_fast": True,  "severity": "regression", "parallel_safe": True},
    {"id": "layer-depth",                  "script": os.path.join("tools", "measure_layer_depth.py"), "args": [], "label": "Layer sub-discipline COVERAGE (A7.4 §14.5: 99-item rubric checklist across 13 layers; forward-only ratchet — a layer losing validator/tool evidence FAILs. Measures presence-of-mechanism = upper bound on true-scope depth, NOT depth)", "group": "Maturity P4", "report": "layer_depth.json",        "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "gateway-bypass",               "script": os.path.join("tools", "validate_gateway_bypass.py"), "args": [], "label": "Gateway-axis MEASURED (G2 §14.6: derives each layer's PEP-chokepoint grade from REAL bypass reports — gateway_coverage/tenancy/policy-hive-binding/canonical-sources/kpi-canonical; forward-only on bypass count. 7/13 measured; data-gateway bypass = G1 target → 0)", "group": "Maturity P4", "report": "gateway_bypass.json",  "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "gateway-gate-depth",           "script": os.path.join("tools", "measure_gateway_gate.py"), "args": [], "label": "HONEST per-layer depth (§14.6: Gateway PEP × Gate ratchet × Prod-real, 13 layers; forward-only composite ratchet. Stricter than the 13x6 matrix + coverage — requires a PREVENTION chokepoint + prod-real, not detection/local-substitute. Overall 64.1%; Gateway axis 7.5/13 = the frontier, now 7 grades instrument-backed via gateway-bypass)", "group": "Maturity P4", "report": "gateway_gate_depth.json", "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "memento-catalog-citations",    "script": os.path.join("tools", "memento_catalog_citation_validator.py"), "args": [], "label": "Memento Pattern-Catalog Citation Rot (reference_pattern_catalog.md citations all resolve on disk or via the index)", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "pm-logbook-lineage",           "script": os.path.join("tools", "validate_pm_logbook_lineage.py"), "args": [], "label": "T32: PM<->Logbook lineage ON GLASS both directions - the 'via PM Scheduler' chip keyed on the pm- id prefix (a mirrored entry announces its origin) + the 'Logged via Logbook' provenance note on the PM completion. Locks both markers against a silent drop (declared-but-never-wired class).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "listing-cost-transparency",    "script": os.path.join("tools", "validate_listing_cost_transparency.py"), "args": [], "label": "T34: the listing composer tells the WHOLE cost truth at the commit point - the 10% credit reservation held at publish AND that it returns if the listing does not sell - so a hold is never read as a fee nor hidden behind a bare 'no platform fees / free' claim (the cost-transparency break T34 fixed).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "destructive-proportional-confirm", "script": os.path.join("tools", "validate_destructive_proportional_confirm.py"), "args": [], "label": "T50: every destructive action is rostered + confirmed (build_destructive_control_registry roster current), no blank confirm, and the confirm roster is forward-only (removing a confirmation reddens). Proportionality of prose is the hand-audit; this counts the objective floor.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "no-dead-feature-remnants",     "script": os.path.join("tools", "validate_no_dead_feature_remnants.py"), "args": [], "label": "T60: a retired feature leaves no orphaned function behind - a top-level page fn defined but referenced NOWHERE is dead weight or a half-removed feature's door; forward-only ratchet against a frozen baseline (conservative: on*/framework idioms spared).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "status-incidents-derived",     "script": os.path.join("tools", "validate_status_incidents.py"), "args": [], "label": "T71: the status page's incidents are DERIVED, true, and DB-independent - well-formed, open<->red equals the currently-FAIL/WARN validators in platform_health, resolved windows sane (an unrecovered historical incident closes at its last bad run), and the committed file equals a fresh derivation from the signals.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "picker-arrow-keys",            "script": os.path.join("tools", "validate_picker_arrow_keys.py"), "args": [], "label": "T48: the logbook asset-picker is a keyboard combobox, not a Tab-only list - ArrowDown/ArrowUp move focus through the options (the keyboard-a11y gap T48 measured and fixed: ArrowDown+Enter selected nothing).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "project-report-error-honesty", "script": os.path.join("tools", "validate_project_report_error_honesty.py"), "args": [], "label": "T73: the project-report page tells the truth about why a project is unseen - 42501 (access, a refused-but-identified caller) is distinguished from absence, the access branch names the boundary, and the copy never falsely claims the project 'no longer exists' to a cross-hive reader for whom RLS merely filters it.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "realtime-degradation-honest",  "script": os.path.join("tools", "validate_realtime_degradation_honest.py"), "args": [], "label": "T143: when realtime degrades on the hive board, setConn('offline') paints the honest offline state AND renderPresenceFallback() re-reads the roster from the DB on the same path - a dead presence socket cannot read as a live-but-quiet hive (the silence-a-reader-misreads-as-fact class).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "seller-tier-distinct-counterparties", "script": os.path.join("tools", "validate_seller_tier_distinct_counterparties.py"), "args": [], "label": "T98: a seller's reputation tier is COUNT(DISTINCT COALESCE(buyer_auth_uid, normalised_contact, name)) across sold listings - distinct COUNTERPARTIES, not rows - so a repeat buyer counts once and a tier cannot be inflated by repeat sales (SKIPs if DB unreachable).", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "lowstock-marketplace-handoff", "script": os.path.join("tools", "validate_lowstock_marketplace_handoff.py"), "args": [], "label": "T30: the low-stock->marketplace buy hop carries context - 'Find on Marketplace' navigates to marketplace.html?section=parts&q=<part> with the part number seeded (encodeURIComponent), not a bare marketplace link that drops the reader into an empty search.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "weibull-small-n-caveat",       "script": os.path.join("tools", "validate_weibull_small_n_caveat.py"), "args": [], "label": "T26: a Weibull fit on few failures (n_failures < 15) is shown with a confidence caveat, not a decisive two-decimal verdict - so a low-n beta that flips wear-out<->infant on one more failure is never defended as certain (the qualifier-next-to-the-figure class).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "state-scope-registry",         "script": os.path.join("tools", "validate_state_scope_registry.py"), "args": [], "label": "T120: the account-vs-device state split is documented + correctly classified - identity/memberships/records at ACCOUNT level, filters/drafts/offline-queues/ACTIVE HIVE at DEVICE level (picking a hive on the phone must not change it on the PC); shape + the two easily-wrong classifications locked.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "pwa-sw-central-register",       "script": os.path.join("tools", "validate_pwa_sw_central_register.py"), "args": [], "label": "T44: the service worker registers centrally from the shell (nav-hub.js, every page) at a DERIVED root (whSwRoot: /workhive/ local, / prod) - so an installed PWA has its offline shell everywhere and the path does not 404 in prod (was one supervisor-page-only register hardcoded to the local prefix).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "hive-value-summary",           "script": os.path.join("tools", "validate_hive_value_summary.py"), "args": [], "label": "T188 (data layer): the whole-platform value summary v_hive_value_summary is a security_invoker view computing the three honest renewal counts from existing tables - pms_kept (pm_completions done, not skipped), faults_resolved (logbook Closed), knowledge_written (fault_knowledge). SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "hive-value-card",              "script": os.path.join("tools", "validate_hive_value_card.py"), "args": [], "label": "T188 (UI layer): the hive board surfaces the value summary - #value-summary-card renders pms/faults/knowledge wired to v_hive_value_summary via loadValueSummary() (loaded on board init), with an honest empty state (a brand-new hive sees encouragement, not three zeros framed as value).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "profile-no-self-privilege",    "script": os.path.join("tools", "validate_profile_no_self_privilege.py"), "args": [], "label": "T365 (mass-assignment): a client cannot self-grant a privileged field on its own record - LIVE probe proves a seller self-stamping service_providers.verified is REFUSED (guard_service_provider_writes) and a worker self-promoting hive_members.role to supervisor changes 0 rows (RLS supervisor-only UPDATE). SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "webhook-signature-verified",   "script": os.path.join("tools", "validate_webhook_signature_verified.py"), "args": [], "label": "T378 (webhook forgery): gcash-receipt-inbound (which can MINT credits) fails closed with no GCASH_INBOUND_SECRET, verifies an HMAC of (ts.body) with a constant-time compare, refuses a mismatch with 401, and enforces a replay window - a forged receipt cannot mint credits.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "export-supervisor-only",       "script": os.path.join("tools", "validate_export_supervisor_only.py"), "args": [], "label": "T380 (low-role exfil): export-hive-data (a whole-hive PDPA dump) requires an ACTIVE SUPERVISOR via checkSupervisor(), refuses others with 403, and runs that auth check BEFORE the export_hive_data RPC (call-site ordering) - a worker/anon cannot exfiltrate the hive.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "stale-membership-no-access",    "script": os.path.join("tools", "validate_stale_membership_no_access.py"), "args": [], "label": "T374 (stale-membership escalation): the tenancy helpers user_hive_ids / user_supervisor_hive_ids filter hive_members to status='active' (scoped to auth.uid()), and user_can_access_hive delegates through them - a removed/suspended member regains NO access. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "storage-object-rls",           "script": os.path.join("tools", "validate_storage_object_rls.py"), "args": [], "label": "T376 (signed-URL scope): RLS is enabled on storage.objects, the destructive DELETE is owner/admin-scoped (owner=auth.uid() OR admin), and no policy grants anon writes - a signed URL / guessed path cannot abuse scope beyond public-by-design buckets. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "no-interpolated-filter-injection", "script": os.path.join("tools", "validate_no_interpolated_filter_injection.py"), "args": [], "label": "T368 (PostgREST filter injection): every edge fn that interpolates user input into a .or/.filter logic filter first strips the [,()] breakout chars + escapes %/_ (agentic-rag-loop, visual-defect-capture) - a comma in a search term cannot inject a second condition.", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "no-get-mutation",              "script": os.path.join("tools", "validate_no_get_mutation.py"), "args": [], "label": "T377 (CSRF on state-changing GET): no edge fn mutates on an HTTP GET (no fn both branches on method==='GET' and does .insert/.update/.delete); state changes are POST with a bearer JWT (no ambient-cookie CSRF surface).", "group": "Platform",   "report": None,                                          "skip_if_fast": False, "severity": "regression", "parallel_safe": True},
    {"id": "audit-log-append-only",        "script": os.path.join("tools", "validate_audit_log_append_only.py"), "args": [], "label": "T384 (audit tampering/gap): hive_audit_log has RLS on + INSERT(member) + SELECT(supervisor) policies and NO UPDATE/DELETE/ALL policy - RLS default-denies any tamper/delete, so the audit trail cannot be rewritten or gapped. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "no-future-timestamps",         "script": os.path.join("tools", "validate_no_future_timestamps.py"), "args": [], "label": "T437 (backfill future timestamp): every public table with a created_at holds only past/near-now values (135 tables checked) - no backfill or clock bug left a future-dated row that would poison time-ordered reads / recent-activity windows / MTBF-OEE. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "game-day-capstone", "script": os.path.join("tools", "validate_game_day_capstone.py"), "args": [], "label": "T200 (the full-platform game day, the programs proof): the game day composes as the UNION of the five degradation lanes (cc_failure_injection, offline-queued, cg_offline_views, auto-read-retry, media-fails-alone) under a DECLARED precedence (degradation_precedence.md) with a shift-completes north star; reddens if any lane is missing/red or the precedence is undeclared.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "year-walk-capstone", "script": os.path.join("tools", "validate_year_walk_capstone.py"), "args": [], "label": "T196 (the complete story: one hive, one year): the year-compression material exists (grow_hive_fixture + age_hive_fixture + seed_5y_synthetic_history) AND every cross-year invariant is gated (ledger/FK/soft-delete/cache-freshness/membership/RLS/shift-window/deferral/shutdown-skip) - the one-hive-one-year story composes and holds.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "worker-drift-visible", "script": os.path.join("tools", "validate_worker_drift_visible.py"), "args": [], "label": "T186 (week-one habit / drift): a worker drifting away is VISIBLE to a supervisor while there is time to help - hive.html Team Pulse reads v_worker_assignment_truth (last_job_at + last_pm_at, row-per-member so never-engaged workers count too) and renders #pulse-quiet-workers naming workers quiet 7+ days, with an honest-degraded path (unread != all-active).", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "pull-help-coverage", "script": os.path.join("tools", "validate_pull_help_coverage.py"), "args": [], "label": "T174 (the help system: pull-help everywhere): every core worker-facing tool page has a pull-help guide entry (learn_links.json) and every guide slug resolves to a real /learn/ article - no uncovered core page, no dangling guide link.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "pm-shutdown-skip-honesty", "script": os.path.join("tools", "validate_pm_shutdown_skip_honesty.py"), "args": [], "label": "T189 (seasonal shutdown: the plant sleeps): a shutdown does not read as a false compliance cliff - a PM that could not run is recorded skipped (CHECK-constrained, distinct from done/overdue), counted in v_pm_compliance_truth, and surfaced on asset-hub tile, so a supervisor can explain the dip. Paired with a-deferral-is-not-a-completion.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "announcement-reach-moderation", "script": os.path.join("tools", "validate_announcement_reach_moderation.py"), "args": [], "label": "T149 (broadcast semantics: hive announcements): a moderated post does not keep announcing itself - the nav-hub badge (an announcement only standing reach) counts community posts but excludes soft-deleted ones (.is deleted_at null; RLS does not for members), and the announcement category is supervisor-guarded.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "power-user-accelerator", "script": os.path.join("tools", "validate_power_user_accelerator.py"), "args": [], "label": "T194 (the power user ceiling): the one real accelerator - a Ctrl/Cmd+K global-search palette - genuinely exists and is reachable platform-wide (nav-hub.js binds Ctrl/Cmd+K, lazy-loads search-overlay, included on 30+ pages), not a dead binding.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "resume-review-sheet", "script": os.path.join("tools", "validate_resume_review_sheet.py"), "args": [], "label": "T33 (resume upload->extract->review->export): the extracted resume is a per-item REVIEW SHEET - accept/edit/reject affordances + per-item checkboxes - so the worker curates the machine guess, not take-or-leave a finished CV.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "shift-brain-no-manual-edit", "script": os.path.join("tools", "validate_shift_brain_no_manual_edit.py"), "args": [], "label": "T76 (shift-brain generation & regeneration): the brief is a generated read-only artifact with NO manual-edit surface (no input/textarea/contenteditable/edit control), so regeneration cannot silently discard hand-tuning. Ratchet: adding an edit control reddens until edit-then-regen honesty is handled.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "provider-availability-driven", "script": os.path.join("tools", "validate_provider_availability_driven.py"), "args": [], "label": "T102 (service-provider availability honesty): availability is DRIVEN end-to-end - accept_service_request SETS service_providers.availability on job-take and sync_provider_availability KEEPS it in step - so a provider returns to the supply pool automatically, never stuck write-once (the 43%-of-supply-vanished bug).", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "dispute-machinery", "script": os.path.join("tools", "validate_dispute_machinery.py"), "args": [], "label": "T97 (dispute resolution end-to-end): the three-role lane is real - a marketplace_disputes table (first-class record), apply_dispute_adjustment (controlled credit consequence), and a CHECK-enforced dispute lifecycle - not UI theatre.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "analytics-cache-freshness", "script": os.path.join("tools", "validate_analytics_cache_freshness.py"), "args": [], "label": "T428 (stale cache after a write): the analytics/benchmark cache is freshness-tracked - producers stamp computed_at on every compute, and readers filter to a bounded recency window (.gte computed_at now-Ndays) + take newest-first, so a stale out-of-window row is excluded, never served.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "shift-window-server-validated", "script": os.path.join("tools", "validate_shift_window_server_validated.py"), "args": [], "label": "T423 (timezone-crossing shift at midnight): the shift window is a SERVER-side enum VALID_WINDOWS {06-14,14-22,22-06} with the midnight-crossing 22-06 first-class, and an incoming shift_window is rejected unless it is a member - the night shift is filed by the server definition, never the device clock.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "founder-console-honest", "script": os.path.join("tools", "validate_founder_console_honest.py"), "args": [], "label": "T488 (founder console shows honest platform state): founder-gated (isPlatformAdmin), grounded in the v_*_truth canonical views + platform_health across many live DB reads, with no fabricated (Math.random/hardcoded) displayed metric - measured state, not invented.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "promo-poster-valid", "script": os.path.join("tools", "validate_promo_poster.py"), "args": [], "label": "T489 (promo-poster generator produces a valid asset): promo-poster.html has the headline + all 5 product pillars, every local img src resolves to an existing file, and all svg are balanced - a complete, renderable, shareable asset.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "symbol-gallery-complete", "script": os.path.join("tools", "validate_symbol_gallery.py"), "args": [], "label": "T490 (symbol gallery complete & accurate): every symbol getSymbolList() returns maps to one of the gallery rendered disciplines (electrical/mechanical/pid/lps) - none silently dropped - and every one draws a real SVG element. node-audited against the gallery own DISCIPLINE_ORDER so the two cannot diverge; 28 symbols, 0 gaps.", "group": "Platform", "report": None, "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "money-columns-numeric",     "script": os.path.join("tools", "validate_money_columns_numeric.py"), "args": [], "label": "T424 (float precision in cost/hours rollup): no money-context column (amount/price/peso/payout/credit/fee/cost...) is a binary float - every peso is numeric/integer, so a SUM cannot drift by a centavo. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "soft-delete-not-aggregated",    "script": os.path.join("tools", "validate_soft_delete_not_aggregated.py"), "args": [], "label": "T432 (soft-delete leaks into aggregate): every view that counts/sums a soft-deletable table (community_posts, projects) filters deleted_at IS NULL - a deleted row cannot inflate a reputation/total. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {"id": "hive-fk-integrity",            "script": os.path.join("tools", "validate_hive_fk_integrity.py"), "args": [], "label": "T422 (orphaned child after parent delete): every public hive_id table is FK-enforced against hives with a deliberate disposition (CASCADE or SET NULL - never a blocking NO ACTION), uuid-typed, zero dangling refs. Locks the 2026-09-01 finding: 33 tables had NO FK and 1,860 live orphans existed (analytics_events 1,834); fixed by mig 20260901000001. SKIPs if DB unreachable.", "group": "Platform",   "report": None,                                          "skip_if_fast": True, "severity": "regression", "parallel_safe": True},
    {
        "id":      "visual_defect_confidence",
        "script":  "tools/validate_visual_defect_confidence.py",
        "args":    [],
        "label":   "AI Self-Improvement: Visual Defect Confidence",
        "group":   "AI Validation",
        "report":  None,
        "skip_if_fast": False,
    },
    {
        "id":      "perf-scale",
        "script":  "validate_perf_scale.py",
        "args":    [],
        "label":   "Arc L Performance & Scale ratchet (forward-only: S/E/R/B pass-counts >= locked baseline; floors S90/E85/R85/B95)",
        "group":   "Arc L",
        "report":  None,
        "skip_if_fast": False,
        "severity": "regression",
        "parallel_safe": True,
    },
]

PYTHON_API_URL  = "https://engineering-calc-api.onrender.com/calculate"
SUPABASE_URL    = "https://hzyvnjtisfgbksicrouu.supabase.co/functions/v1/engineering-calc-agent"


# ── Run one validator ─────────────────────────────────────────────────────────
VALIDATOR_TIMEOUT_SECONDS = 1200  # per-validator hard cap; hung child gets SIGTERM
# 2026-05-24: bumped 300 -> 600 -> 1200. Phantom capture + phantom column
# auditors do O(captures x blobs) and O(columns x blobs) cross-products,
# which grow superlinearly with the codebase. Under gate concurrency they
# slow markedly: phantom_captures hit 347.9s (was 300+ killed), phantom_columns
# hit 600.1s (was killed at 600s ceiling). Standalone each finishes in 4-5 min.
# Bumping the cap to 1200s gives 2x headroom for future growth without losing
# the hung-child safety net. If a validator legitimately needs longer, that's
# the signal to refactor it (index by tail-N bytes / parallelize tables).


def _kill_process_tree(pid):
    """Kill the child AND every descendant.

    subprocess.run(timeout=) kills only the DIRECT child. A validator driving Playwright leaves
    node/chromium GRANDCHILDREN alive, and they inherited the stdout pipe -- so the kill is
    followed by a communicate() that blocks forever waiting for an EOF a live browser will never
    send. The suite then hangs with no timeout left to save it.

    Measured 2026-08-20, three times in ONE run: the suite process sat at 0.00 CPU-sec/25s while a
    single node worker idled at ~1%, and each time killing node by hand let the suite advance
    within seconds. The old comment claimed "hung child gets SIGTERM" -- there is no SIGTERM on
    Windows, and it would not have reached the grandchildren in any case.
    """
    if os.name == "nt":
        try:
            subprocess.run(["taskkill", "/F", "/T", "/PID", str(pid)],
                           capture_output=True, text=True, timeout=60)
        except Exception:
            pass
    else:
        import signal as _signal
        try:
            os.killpg(os.getpgid(pid), _signal.SIGKILL)
        except Exception:
            pass

def run_validator(v):
    if not os.path.exists(v["script"]):
        return {"status": "ERROR", "reason": f"Script not found: {v['script']}", "output": ""}

    # interpreter by extension: the seven walk-instruments promoted to gates on 2026-08-21 are
    # node .mjs scripts registered directly (no python wrapper); running them under PYTHON would
    # SyntaxError instantly and read as a broken gate rather than a wrong interpreter.
    interp = "node" if v["script"].endswith((".mjs", ".js")) else PYTHON
    # ★`args` IS OPTIONAL, and treating it as required killed the whole board (2026-08-31).
    # Nineteen registered gates carry only a script and a label - selection-is-exposed,
    # shared-button-invites-the-click, the-return-promise-is-kept-safely and sixteen more, all
    # added in the working tree and none of them wrong: a gate that takes no arguments has no
    # reason to write "args": []. This line raised KeyError on the FIRST one it reached, after
    # 66 minutes of successful gate runs, so the crash landed at the end of a long board and
    # looked like a late-stage failure rather than a registration schema mismatch. The board
    # then wrote no platform_health.json at all, which is why every restamp downstream refused:
    # the reports existed but the run never closed.
    cmd = [interp, v["script"]] + list(v.get("args") or [])
    t0  = time.time()
    popen_kw = {}
    if os.name != "nt":
        popen_kw["start_new_session"] = True   # so the whole group can be signalled
    try:
        proc = subprocess.Popen(
            cmd, stdout=subprocess.PIPE, stderr=subprocess.PIPE,
            text=True, encoding="utf-8", errors="replace", **popen_kw)
    except Exception as ex:
        return {"status": "ERROR", "reason": str(ex), "output": "", "elapsed": 0}
    try:
        out, err = proc.communicate(timeout=VALIDATOR_TIMEOUT_SECONDS)
        elapsed = round(time.time() - t0, 1)
        stdout  = (out or "") + (err or "")
        status  = "PASS" if proc.returncode == 0 else "FAIL"
        return {"status": status, "output": stdout, "elapsed": elapsed}
    except subprocess.TimeoutExpired:
        # THE TREE, not just the child. Draining only becomes possible once the
        # grandchildren holding the inherited pipe are gone (see _kill_process_tree).
        _kill_process_tree(proc.pid)
        try:
            out, err = proc.communicate(timeout=60)
        except Exception:
            out, err = "", ""
        elapsed = round(time.time() - t0, 1)
        partial = (out or "") + (err or "")
        return {
            "status":  "FAIL",
            "reason":  f"hung; process TREE killed after {VALIDATOR_TIMEOUT_SECONDS}s",
            "output":  partial + f"{chr(10)}[TIMEOUT — process TREE killed after {VALIDATOR_TIMEOUT_SECONDS}s]",
            "elapsed": elapsed,
        }
    except Exception as ex:
        _kill_process_tree(proc.pid)
        return {"status": "ERROR", "reason": str(ex), "output": "", "elapsed": 0}


# ── Readiness Gate ─────────────────────────────────────────────────────────────
def check_git_clean():
    try:
        r = subprocess.run(
            ["git", "status", "--short"],
            capture_output=True, text=True
        )
        lines = [l for l in r.stdout.strip().splitlines()
                 if not l.startswith("??")]  # ignore untracked
        return len(lines) == 0, lines[:5]
    except Exception as ex:
        return None, [str(ex)]


def check_api(url, payload, label):
    try:
        data = json.dumps(payload).encode()
        req  = urllib.request.Request(
            url, data=data,
            headers={"Content-Type": "application/json"}, method="POST",
        )
        with urllib.request.urlopen(req, timeout=30) as r:
            return r.status == 200, f"HTTP {r.status}"
    except Exception as ex:
        return False, str(ex)[:80]


def run_readiness_gate():
    gate = {}

    # Git clean
    clean, dirty = check_git_clean()
    if clean is None:
        gate["git"] = {"status": "WARN", "detail": "git not available"}
    elif clean:
        gate["git"] = {"status": "PASS", "detail": "working tree clean"}
    else:
        gate["git"] = {"status": "WARN", "detail": f"{len(dirty)} uncommitted file(s)", "files": dirty}

    if not FAST:
        # Python API live
        ok, detail = check_api(
            PYTHON_API_URL,
            {"calc_type": "Pump Sizing (TDH)", "inputs": {"flow_rate": 10, "static_head": 20}},
            "Python API"
        )
        gate["python_api"] = {"status": "PASS" if ok else "FAIL", "detail": detail}

        # Supabase edge function live
        try:
            req = urllib.request.Request(
                SUPABASE_URL, method="OPTIONS",
                headers={"Origin": "https://workhiveph.com",
                         "Access-Control-Request-Method": "POST"}
            )
            with urllib.request.urlopen(req, timeout=20) as r:
                ok2 = r.status == 200
        except Exception as ex:
            ok2 = False
            detail = str(ex)[:80]
        gate["supabase"] = {"status": "PASS" if ok2 else "WARN", "detail": "OPTIONS 200" if ok2 else detail}
    else:
        gate["python_api"] = {"status": "SKIP", "detail": "--fast mode"}
        gate["supabase"]   = {"status": "SKIP", "detail": "--fast mode"}

    return gate


# ── Baseline comparison ───────────────────────────────────────────────────────
def load_baseline():
    if not os.path.exists(BASELINE_FILE):
        return None
    try:
        with open(BASELINE_FILE) as f:
            return json.load(f)
    except Exception:
        return None


def save_baseline(results, gate):
    baseline = {
        "timestamp":  datetime.datetime.now(datetime.timezone.utc).isoformat(),
        "validators": {v["id"]: r["status"] for v, r in results},
        "readiness":  {k: v["status"] for k, v in gate.items()},
    }
    with open(BASELINE_FILE, "w") as f:
        json.dump(baseline, f, indent=2)


def detect_regressions(results, baseline):
    if not baseline:
        return []
    regressions = []
    for v, r in results:
        prev = baseline.get("validators", {}).get(v["id"])
        curr = r["status"]
        if prev in ("PASS",) and curr == "FAIL":
            regressions.append({
                "id": v["id"], "label": v["label"],
                "was": prev, "now": curr,
            })
    return regressions


# ── Print helpers ─────────────────────────────────────────────────────────────
def status_icon(s):
    return {
        "PASS":  green("PASS"),
        "FAIL":  red("FAIL"),
        "WARN":  yellow("WARN"),
        "SKIP":  cyan("SKIP"),
        "ERROR": red("ERR "),
    }.get(s, s)


def divider(char="=", width=72):
    print(char * width)


# ── Main ──────────────────────────────────────────────────────────────────────
def main():
    start_time = time.time()
    now_str    = datetime.datetime.now().strftime("%Y-%m-%d %H:%M")

    divider()
    print(bold("  WorkHive Platform Guardian"))
    print(f"  {now_str}  |  {'FAST mode (skip live API)' if FAST else 'Full mode'}  |  Python {sys.version.split()[0]}")
    divider()

    # THE REGISTRY CHECKS ITSELF FIRST. Nothing asserted that a gate `id` appears once, and three had been
    # registered TWICE (`memory-integrity`, `companion-page-coverage`, `companion-source-coverage`): each ran
    # its script twice every suite for no extra assertion, the gate count was overstated by three, and the
    # duplicate `memory-integrity` label falsely claimed RLS/index/retention coverage that no check performs.
    # An id is the key that results, baselines and ratchets are stored under, so two gates sharing one can
    # overwrite each other's floor — the failure would show up as a ratchet that mysteriously stopped holding.
    # Printed and fatal, because a registry that cannot be trusted makes every number below it unreliable.
    _seen, _dupes = set(), []
    for _v in VALIDATORS:
        _id = _v.get("id")
        (_dupes.append(_id) if _id in _seen else _seen.add(_id))
    if _dupes:
        print(red(f"\n  REGISTRY ERROR — duplicate gate id(s): {', '.join(sorted(set(_dupes)))}"))
        print("  An id is the key results/baselines/ratchets are stored under; two entries sharing one can")
        print("  silently overwrite each other. Give each gate a unique id, or delete the redundant entry.")
        return 1

    # Two more properties in the SAME class as the duplicate id, both cheap and both previously unchecked.
    #
    # A script that does not resolve currently fails only WHEN THE GATE RUNS, which in --fast mode a
    # skip_if_fast gate never does: a typo'd path could sit green for weeks. Checked up front instead.
    #
    # A `report` filename shared by two gates means the second overwrites the first's artifact. The one
    # instance found was a MISLABEL rather than a clash — `rls-strict` declared the report that
    # `mine-rls-policies` produces, because it READS that file and writes its own baseline. Same lesson as
    # the id collision: the field means "the artifact this gate emits", and a consumer claiming a producer's
    # output makes the registry describe a data flow that does not exist.
    _bad_script = [(v.get("id"), v.get("script")) for v in VALIDATORS
                   if v.get("script") and not os.path.exists(v["script"])]
    if _bad_script:
        print(red(f"\n  REGISTRY ERROR — {len(_bad_script)} gate(s) point at a script that does not exist:"))
        for _i, _s in _bad_script[:6]:
            print(f"    {_i} -> {_s}")
        print("  A missing script only fails when the gate RUNS, and a skip_if_fast gate never runs in")
        print("  --fast mode — so this would sit green until a full run happened to reach it.")
        return 1

    _rep = {}
    for _v in VALIDATORS:
        if _v.get("report"):
            _rep.setdefault(_v["report"], []).append(_v.get("id"))
    _rep_dupes = {k: ids for k, ids in _rep.items() if len(ids) > 1}
    if _rep_dupes:
        print(red("\n  REGISTRY ERROR — a report artifact is claimed by more than one gate:"))
        for _k, _ids in sorted(_rep_dupes.items()):
            print(f"    {_k} <- {', '.join(_ids)}")
        print("  Whichever gate runs second overwrites the first's artifact. If one gate only READS the")
        print("  file, it should not declare it as its report — that field is what the gate EMITS.")
        return 1

    # A declared `report` that the gate never writes is a FICTIONAL pointer. It reads as "this gate leaves an
    # artifact you can inspect", and following it finds nothing — which is how the `rls-strict` entry misled me
    # into thinking it produced the RLS mining report when it only READS it. Measured rather than assumed: 25
    # of the 394 gates that declare a report have a script containing no report-writing code at all, and most
    # of those files do not exist on disk. That is pre-existing metadata debt, not a regression, so it is a
    # FORWARD-ONLY ceiling: the count is printed every run and only a RISE fails. Cleaning it up lowers the
    # ceiling; nothing forces 25 fixes today to land one new gate.
    _FICTIONAL_REPORT_CEILING = 25
    _fiction = []
    for _v in VALIDATORS:
        _rep, _scr = _v.get("report"), _v.get("script")
        if not _rep or not _scr or not os.path.exists(_scr):
            continue
        try:
            _src = open(_scr, encoding="utf-8", errors="replace").read()
        except OSError:
            continue
        if os.path.basename(_rep) not in _src:
            _fiction.append(f"{_v.get('id')}->{os.path.basename(_rep)}")
    if len(_fiction) > _FICTIONAL_REPORT_CEILING:
        print(red(f"\n  REGISTRY ERROR — {len(_fiction)} gates declare a `report` their script never writes "
                  f"(ceiling {_FICTIONAL_REPORT_CEILING}). New offenders:"))
        for _f in _fiction[_FICTIONAL_REPORT_CEILING:]:
            print(f"    {_f}")
        print("  A report nobody writes is a pointer to nothing. Either write the artifact or drop the field.")
        return 1
    print(f"  {len(VALIDATORS)} gates registered · ids unique · scripts resolve · reports unclaimed twice · "
          f"{len(_fiction)}/{_FICTIONAL_REPORT_CEILING} unwritten report declarations (forward-only)")

    baseline = load_baseline()
    if baseline:
        base_time = baseline.get("timestamp", "?")[:16].replace("T", " ")
        print(f"\n  Baseline loaded from {base_time}")
    else:
        print("\n  No baseline found — this run will create one if all pass.")

    # ── GATE ONLY MODE ────────────────────────────────────────────────────────
    if GATE:
        print("\n" + cyan("  READINESS GATE ONLY") + "\n")
        gate = run_readiness_gate()
        for key, v in gate.items():
            print(f"  {status_icon(v['status'])}  {key:20s}  {v['detail']}")
        divider()
        all_ok = all(v["status"] in ("PASS", "WARN", "SKIP") for v in gate.values())
        print(f"\n  {'READY' if all_ok else 'BLOCKED'}\n")
        return 0 if all_ok else 3

    # ── LOOP 1: RUN ALL VALIDATORS ────────────────────────────────────────────
    results    = []
    group_seen = set()

    for v in VALIDATORS:
        if FAST and v.get("skip_if_fast"):
            results.append((v, {"status": "SKIP", "output": "--fast", "elapsed": 0}))
            continue

        if v["group"] not in group_seen:
            group_seen.add(v["group"])
            print(f"\n  {cyan(v['group'].upper())}")
            print("  " + "-" * 68)

        print(f"  {'RUN ':4s}  {v['label']:52s}", end="", flush=True)
        t0 = time.time()
        r  = run_validator(v)
        elapsed = r.get("elapsed", round(time.time() - t0, 1))
        print(f"  {status_icon(r['status'])}  {elapsed:4.1f}s")
        results.append((v, r))

    # ── LOOP 0: REGRESSION DETECTION ─────────────────────────────────────────
    regressions = detect_regressions(results, baseline)

    # ── LOOP 3: READINESS GATE ────────────────────────────────────────────────
    print(f"\n  {cyan('READINESS GATE')}")
    print("  " + "-" * 68)
    gate = run_readiness_gate()
    for key, v in gate.items():
        label = {"git": "Git working tree", "python_api": "Python API (Render)", "supabase": "Supabase edge function"}
        print(f"  {status_icon(v['status'])}  {label.get(key, key):38s}  {v['detail']}")

    # ── LOOP 4: IMPROVEMENT BACKLOG SUMMARY ───────────────────────────────────
    try:
        import json as _json
        if os.path.exists("improvement_backlog.json"):
            with open("improvement_backlog.json", encoding="utf-8") as _f:
                _bl = _json.load(_f)
            _high  = sum(1 for i in _bl if i.get("priority") == "HIGH"   and i.get("score", 0) >= 30)
            _eb    = sum(1 for i in _bl if i.get("business_value") == "enterprise_blocker" and i.get("score", 0) >= 30)
            if _high > 0:
                print(f"  {yellow('WARN')}  {'Improvement backlog':38s}  {_high} HIGH item(s)  {_eb} enterprise blocker(s) — run python improve.py")
    except Exception:
        pass

    # ── SUMMARY ───────────────────────────────────────────────────────────────
    pass_count = sum(1 for _, r in results if r["status"] == "PASS")
    fail_count = sum(1 for _, r in results if r["status"] == "FAIL")
    skip_count = sum(1 for _, r in results if r["status"] == "SKIP")
    warn_count = sum(1 for _, r in results if r["status"] == "WARN")
    total_time = round(time.time() - start_time, 1)

    gate_blocked = any(v["status"] == "FAIL" for v in gate.values())

    print(f"\n  {'FAILURES' if fail_count else 'ALL PASS'}")
    print("  " + "-" * 68)
    for v, r in results:
        if r["status"] == "FAIL":
            print(f"  {red('FAIL')}  {v['label']}")
            # Show first few lines of output
            for line in r["output"].strip().splitlines():
                if "FAIL" in line or "CRITICAL" in line or "missing" in line.lower():
                    print(f"         {line.strip()[:70]}")
                    break

    if regressions:
        print(f"\n  {red('REGRESSIONS DETECTED')} (was PASS, now FAIL):")
        for reg in regressions:
            print(f"  {red('REG')}  {reg['label']}")

    divider()
    status_line = (
        red("BLOCKED — fix failures before deploying")
        if fail_count or regressions
        else yellow("READY (review WARNs)")
        if warn_count or gate_blocked
        else green("READY — safe to deploy")
    )
    print(f"\n  {bold(status_line)}")
    print(f"  {pass_count} PASS  {fail_count} FAIL  {warn_count} WARN  {skip_count} SKIP  |  {total_time}s total\n")

    # ── WRITE platform_health.json ────────────────────────────────────────────
    # Preserve improvement_backlog from previous improve.py run
    # (run_platform_checks.py rewrites health but must not wipe the backlog)
    preserved_backlog = None
    if os.path.exists(HEALTH_FILE):
        try:
            with open(HEALTH_FILE, encoding="utf-8") as _hf:
                _prev = json.load(_hf)
            preserved_backlog = _prev.get("improvement_backlog")
        except Exception:
            pass
    # Also read directly from improvement_backlog.json if health doesn't have it
    if not preserved_backlog and os.path.exists("improvement_backlog.json"):
        try:
            with open("improvement_backlog.json", encoding="utf-8") as _bf:
                _bl = json.load(_bf)
            _h  = sum(1 for i in _bl if i.get("priority") == "HIGH"   and i.get("score", 0) >= 30)
            _m  = sum(1 for i in _bl if i.get("priority") == "MEDIUM" and i.get("score", 0) >= 20)
            _lo = sum(1 for i in _bl if i.get("priority") == "LOW")
            _eb = sum(1 for i in _bl if i.get("business_value") == "enterprise_blocker" and i.get("score", 0) >= 30)
            _lu = _bl[-1].get("checked_at", "") if _bl else ""
            preserved_backlog = {
                "total": len(_bl), "high": _h, "medium": _m, "low": _lo,
                "enterprise_blockers": _eb, "last_updated": _lu,
            }
        except Exception:
            pass

    health = {
        "timestamp":    datetime.datetime.now(datetime.timezone.utc).isoformat(),
        "mode":         "fast" if FAST else "full",
        "overall":      "FAIL" if (fail_count or regressions) else "WARN" if warn_count else "PASS",
        "summary":      {"pass": pass_count, "fail": fail_count, "warn": warn_count, "skip": skip_count},
        "duration_s":   total_time,
        "validators":   [
            {
                "id":      v["id"],
                "label":   v["label"],
                "group":   v["group"],
                "status":  r["status"],
                "elapsed": r.get("elapsed", 0),
                "report":  v.get("report"),   # optional — runtime-discovered validators (calc-suite auto-discovery) omit it
            }
            for v, r in results
        ],
        "regressions":  regressions,
        "readiness":    gate,
        "baseline_ref": baseline.get("timestamp", None) if baseline else None,
    }
    if preserved_backlog:
        health["improvement_backlog"] = preserved_backlog

    with open(HEALTH_FILE, "w") as f:
        json.dump(health, f, indent=2)
    print(f"  Saved {HEALTH_FILE}")

    # Save baseline only when everything passes
    if fail_count == 0 and not regressions:
        save_baseline(results, gate)
        print(f"  Saved {BASELINE_FILE} (new clean baseline)\n")

    # ── EXIT CODE ─────────────────────────────────────────────────────────────
    # ── AUTO-FIX (optional) ───────────────────────────────────────────────────
    if AUTOFIX and fail_count:
        print(f"\n  {cyan('AUTO-FIX')}\n  {'—' * 68}")
        af_result = subprocess.run(
            [PYTHON, "autofix.py"],
            capture_output=True, text=True,
            encoding="utf-8", errors="replace"
        )
        af_out = (af_result.stdout or "") + (af_result.stderr or "")
        for line in af_out.strip().splitlines():
            if any(w in line for w in ["FIXED", "SKIP", "ERROR", "fixed", "error"]):
                print(f"  {line.strip()[:70]}")
        af_fixed = sum(1 for l in af_out.splitlines() if "FIXED" in l)
        if af_fixed:
            print(f"\n  {af_fixed} auto-fix(es) applied — re-run to verify:")
            print(f"  python run_platform_checks.py --fast\n")

    if regressions:
        return 2
    if fail_count:
        return 1
    return 0


if __name__ == "__main__":
    sys.exit(main())
