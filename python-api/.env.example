# Copy this to .env and fill in values before running locally.
# In Railway/Render, set these as environment variables in the dashboard.

# No secrets required for Phase 0.
# Future phases may add:
# COOLPROP_BACKEND=HEOS   # optional CoolProp backend override
