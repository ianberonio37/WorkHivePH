{
  "_README": "Set a candidate key (e.g. 'rule:edge:imports_cors_shared' or 'sentinel:rls_open_policy') to {\"status\": \"approved|rejected|snoozed\", \"note\": \"...\"} to drop it from promotion_queue.md. The orchestrator never overwrites this file.",
  "_SWEEP_DISPOSITION_2026-06-07": "24 Grounded-MCP-Sweep critiques dispositioned by owner. They collapse into 6 root-cause work items (W1 tap-target base rule, W2 modal-a11y retrofit, W3 status-enum guard, W4 index a11y, W5 WHShell seam, W6 judgment). 23 approved / 1 rejected. Owner steers: (Q1) tap-target rule = 44px EVERYWHERE, no exceptions incl. filter-chips; (Q2) #5 onboarding = align verdict+CTA+ladder on ONE first action.",
  "sweep:platform-wide:interactive-min-height-rule": {
    "status": "approved",
    "note": "W1 PRIMARY. Establish one .wh-tappable base (min-height:44px on button/[role=button]/tab/chip/pill/icon-btn) + extend validate_mobile to compute PER-ELEMENT box size (current check_touch_targets is inline-style-only = BLIND SPOT #1/#2, structurally can't see class/padding-sized controls). Owner: 44px everywhere, NO exception list. Closes the whole class instead of chasing it per-page."
  },
  "sweep:platform:companion-overlay-tap-targets": {
    "status": "approved",
    "note": "W1. Shared companion launcher/overlay (wh-ai-trigger 22px, mic/send 14px, close 9px) sub-44 on every page -> fix once in shell component to >=44px."
  },
  "sweep:platform:global-search-44": {
    "status": "approved",
    "note": "W1. #wh-hub-global-search 42px -> 44px in shell header."
  },
  "sweep:platform-wide:nav-hub-mode-btn-tap": {
    "status": "approved",
    "note": "W1. .wh-hub-mode-btn 83x26 ships in nav-hub.js -> sub-44 on every page; bump in shell."
  },
  "sweep:index:onboarding-ladder-44": {
    "status": "approved",
    "note": "W1. #oh-onboarding ladder items 42px -> 44px (extend mobile rule)."
  },
  "sweep:dayplanner:btn-icon-tap": {
    "status": "approved",
    "note": "W1. .btn-icon (sidebar collapse, prev/next date) 28-30px -> 44px; primary date nav below floor."
  },
  "sweep:dayplanner:view-tab-tap": {
    "status": "approved",
    "note": "W1. .view-tab (DILO/WILO/MILO/YILO) 94x31 -> 44px; primary switcher."
  },
  "sweep:shift-brain:shift-pill-tap": {
    "status": "approved",
    "note": "W1. .shift-pill (06-14/14-22/22-06) 140x37 -> 44px; primary shift selector."
  },
  "sweep:shift-brain:back-btn-tap": {
    "status": "approved",
    "note": "W1. .back-btn 36x36 -> 44x44."
  },
  "sweep:logbook:voice-btn-consistency": {
    "status": "approved",
    "note": "W1. Bake 44px into the shared .wh-tappable class so a dropped min-h utility can't shrink a header action (the Voice-Journal failure mode). = the base-rule work."
  },
  "sweep:resume:dedupe-toggle-font": {
    "status": "approved",
    "note": "W1 (+a11y). promote-dedupe hit-area -> 44px via base rule; ALSO bump the 13.3px label to >=14px. Lone sub-spec control on an otherwise clean page."
  },
  "sweep:inventory:min-height-unset-antipattern": {
    "status": "approved",
    "note": "W1. inline style=min-height:unset on .btn-* defeats the 44px default (inline beats media query, bites phones). Row icon buttons -> 44px hitbox; the per-element validate_mobile upgrade catches this inline-unset smell that the class check misses."
  },
  "sweep:voicejournal:filter-chip-density": {
    "status": "approved",
    "note": "W1. Owner Q1 = 44px EVERYWHERE: voice-journal .filter-chip 36px -> 44px (overrides the prior 'deliberately compact' framing; one rule, no per-page drift)."
  },
  "sweep:pm-scheduler:filter-chip-tap": {
    "status": "approved",
    "note": "W1. Owner Q1 = 44px EVERYWHERE: pm-scheduler status filter-chips 32px -> 44px. Resolves the chip-drift the critic flagged between this page and voice-journal."
  },
  "sweep:platform:modal-a11y-helper": {
    "status": "approved",
    "note": "W2 PRIMARY. Build shared whModalA11y(modalEl,{onClose,labelledBy}) -> role=dialog+aria-modal+accessible-name+ESC-close+Tab-trap+focus-restore. Retrofit existing hand-rolled modals (logbook 8, index inline -> migrate). Ratchets validate_modal_a11y.py C7 debt baseline (13) down as modals are migrated."
  },
  "sweep:platform:inline-styled-modals-evade-ratchet": {
    "status": "approved",
    "note": "W2. validate_modal_a11y.py keys on class='fixed inset-0' so inline style=position:fixed;inset:0 modals evade the C7 ratchet (pm-scheduler 3 modals w/ ZERO keydown handlers, dayplanner .modal-overlay). Widen validator to also match inline fixed/inset overlays, THEN retrofit with whModalA11y."
  },
  "sweep:pm-scheduler:edit-modal-a11y": {
    "status": "approved",
    "note": "W2. #pm-edit-modal role=null/aria-modal=null/no ESC/no trap -> apply whModalA11y."
  },
  "sweep:dayplanner:modal-a11y": {
    "status": "approved",
    "note": "W2. #modal/#modal-box role=null/aria-modal=null -> apply whModalA11y."
  },
  "sweep:platform:status-enum-constants": {
    "status": "approved",
    "note": "W3. The dayplanner overdue bug (filter checked item_status!='closed' but schedule_items enum is done/in_progress/pending -> counted DONE as overdue, 6 vs 3 DB) was invisible to every static gate. Centralize per-table status enums in a shared JS constant + a validator that greps filter string-literals against the actual DB enum so a typo'd status can't silently miscount a KPI. High value (real bug already shipped+fixed; this is the systemic guard)."
  },
  "sweep:index:pwd-reveal-keyboard": {
    "status": "approved",
    "note": "W4. Show/hide-password toggles (index.html:2509/2546/2555) are tabindex=-1 -> keyboard users can't reveal. WCAG 2.1.1. Fix: drop -1 (keep last in field tab order) OR labelled 'Show password' checkbox."
  },
  "sweep:index:auth-tabs-aria": {
    "status": "approved",
    "note": "W4. #tab-signin/#tab-signup are plain buttons -> add role=tablist/tab/tabpanel + aria-selected (WAI-ARIA Tabs pattern). Dialog already has role+aria-modal from the Wave-0 fix."
  },
  "sweep:index:whshell-seam": {
    "status": "approved",
    "note": "W5 (low priority, TASTE). Expose read-only window.WHShell={mode(),identity(),role()} for testability + cross-page consistency with sibling resume.html's window.WHResume."
  },
  "sweep:index:first-action-alignment": {
    "status": "approved",
    "note": "W6. Owner Q2 = APPROVE. Align verdict + primary CTA + onboarding ladder on ONE activating first action for a zero-data novice (Hick's law). Suggested canonical action: 'Log your first job' (data-creating activation); owner sets the final canonical action."
  },
  "sweep:index:landing-cta-density": {
    "status": "rejected",
    "note": "W6 won't-fix. The critic itself says 57 CTAs on a logged-out marketing landing is 'likely FINE' (Jakob's law for marketing pages) and recommended no change without a conversion goal. Logged for honesty; reopen only if a conversion goal is set."
  },
  "_UFAI_WAVE0_1_DISPOSITION_2026-06-07": "6 critiques from the C-battery re-sweep (Wave 0 index + Wave 1 logbook/inventory/pm-scheduler), dispositioned by the agent under owner's 'go for it' delegation. 5 approved / 0 rejected; 1 (overdue-KPI) approved-with-root-cause for an owner architecture call.",
  "sweep:platform:header-logo-tap-aria": {
    "status": "approved",
    "note": "Fix PER-PAGE during each wave's sweep, NOT a blanket tool — the header markup varies too much (about/feedback wrap logo+brand-text in a flex <a> already >=44; community/hive ship a bare 28px <img>; some logos aren't links). A regex tool would mis-edit. Already fixed inline on index/logbook/inventory + pm-scheduler back-link; the battery catches it on every remaining page. Skip *-test.html."
  },
  "sweep:pm-scheduler:overdue-kpi-vs-dashboard": {
    "status": "approved",
    "note": "ROOT-CAUSED (needs an OWNER architecture/labeling decision, NOT an auto-fix). 'PM Overdue' is sourced from TWO different canonical views: home dashboard pm_overdue_count = count(v_pm_compliance_truth WHERE is_due) = PER-ASSET (26); pm-scheduler #stat-overdue = count(v_pm_scope_items_truth WHERE is_overdue) = PER-SCOPE-ITEM. Different granularity under the SAME label -> latent cross-surface drift (the code itself documents a prior 21-vs-0 incident, only partially fixed). SOP Phase-4.7 mandate: collapse a KPI computed on >1 surface to ONE shared source, OR relabel so users see 'N assets need PM' vs 'N tasks overdue'. Owner picks unify-vs-relabel."
  },
  "sweep:pm-scheduler:fab-obscured-conn-chip": {
    "status": "approved",
    "note": "Real Major (FAB 52x52 -> ~52x8 usable, obscured by the global #wh-conn-chip). Cross-element/shell positioning — fix in a focused batch: page FABs must sit ABOVE the global connection chip (consistent bottom offset) OR the conn-chip must not occupy the FAB corner. Likely affects every bottom-right-FAB page, so verify across pages before shipping; deferred from the per-page inline fixes for that reason."
  },
  "sweep:platform:cls-list-popin": {
    "status": "approved",
    "note": "Approved, batch with sweep:index-dashboard:cls-tile-popin. Reserve space (min-height/skeleton) for async KPI tiles + list rows so the page doesn't reflow on data arrival (measured CLS 0.08-0.13 on dashboard + inventory). Medium effort, not blocking; do as one skeleton pass across data pages. Re-confirm vs a production-like load (local Flask inflates CLS)."
  },
  "sweep:index-dashboard:cls-tile-popin": {
    "status": "approved",
    "note": "Same class as cls-list-popin — batch together. Home-dashboard KPI tiles + signature-alert banner pop in without reserved space."
  },
  "sweep:platform:page-db-seam": {
    "status": "approved",
    "note": "Low-priority TASTE. Expose a read-only window.WH<Page>={db,get} (or surface the memoized window._whSupabaseClient) on data pages so grounded sweeps can read canonical parity in-browser like index(window.db)/resume(WHResume). Until then, parity falls back to cross-surface text match + the journey-hive-isolation spec. Deferred."
  },
  "sweep:ia:relabel:pending-approval": {
    "status": "approved",
    "note": "RELABEL not consolidate: asset-hub \"Pending approval\"=assets vs inventory=parts (same-named != same-derivation). Disambiguate labels (asset-hub->\"Assets pending approval\", inventory->\"Parts pending approval\"). Small clear win on a CONFUSING novice path (Phase-3). Frontend-only, no data change."
  },
  "sweep:ia:theme:late-overdue": {
    "status": "deferred",
    "note": "Overdue theme (dayplanner/pm-scheduler/project-manager), CONFUSING novice path. DEFER until Journey-D overdue-continuity runs LIVE: if the count truly drifts (per-asset v_pm_compliance_truth vs per-scope-item v_pm_scope_items_truth) escalate to CONSOLIDATE to one canonical source; if it agrees, downgrade to a label/nav tweak. Need the live evidence before touching it."
  },
  "sweep:ia:theme:due-soon-upcoming": {
    "status": "deferred",
    "note": "Due-soon theme, same Journey-D dependency as late-overdue. DEFER pending live continuity result."
  },
  "sweep:ia:theme:risk-hot-critical": {
    "status": "deferred",
    "note": "Risk shown 3 ways (predictive/asset-hub/alert-hub) + canonical predictive is HIDDEN in nav (Phase-3 canonical-unreachable). Highest-value IA item but bigger than a tile tweak: DEFER pending (a) Journey-D risk-continuity (one number vs 3 legitimate lenses?) and (b) a product decision on predictive nav visibility."
  },
  "sweep:ia:theme:healthy-on-track": {
    "status": "rejected",
    "note": "Phase-3 cross-subject downgrade: pm-scheduler \"On track\"=PM compliance != skillmatrix \"On target workers\"=skills != predictive \"Healthy assets\"=asset health. Different jobs sharing a word, not real redundancy. Not a true cluster."
  },
  "sweep:ia:affordance:asset-hub": {
    "status": "rejected",
    "note": "predictive->asset-hub is a drill-down to the asset detail; index->asset-hub is the home hub. Legitimate contextual links, not redundancy (nav=global reach, body=in-context flow). Keep."
  },
  "sweep:ia:affordance:integrations": {
    "status": "rejected",
    "note": "ph-intelligence/plant-connections->integrations are contextual (configure the connector you are looking at). Legitimate. Keep."
  },
  "sweep:ia:affordance:inventory": {
    "status": "rejected",
    "note": "hive/index->inventory = team-management drill-down + home hub. Legitimate contextual links. Keep."
  },
  "sweep:ia:affordance:ph-intelligence": {
    "status": "rejected",
    "note": "analytics/integrations->ph-intelligence = related-view links. Legitimate. Keep."
  },
  "sweep:ia:affordance:pm-scheduler": {
    "status": "rejected",
    "note": "hive/index->pm-scheduler = team drill-down + home hub. Legitimate. Keep."
  },
  "sweep:ia:affordance:voice-journal": {
    "status": "rejected",
    "note": "index/logbook->voice-journal = capture-flow entry points (voice-journal is hidden in nav, so an in-body link is the INTENDED discovery path). Legitimate. Keep."
  },
  "_SWEEP_DISPOSITION_IA_2026-06-08": "Phase A: disposed the 11 sweep:ia candidates (IA streamlining). 1 approved (relabel) / 3 deferred (drift themes, pending Journey-D live evidence) / 7 rejected (1 cross-subject non-cluster + 6 legitimate contextual cross-links).",
  "csb:companion:memory-buckets": {
    "status": "rejected",
    "note": "DRAFT-REC (owner confirm) accept-by-design: per-agent memory buckets are INTENTIONAL — ai-gateway loads agent_memory keyed by agent_id so each specialist keeps its own context; one shared bucket would bleed Hezekiah's torque notes into Zaniah's strategy. The companion FACE is unified (one launcher); MEMORY is per-agent by design. Documented in companion unification."
  },
  "csb:voice-journal:recall-reliability": {
    "status": "snoozed",
    "note": "DRAFT-REC (owner confirm) snooze/track: persist-then-recall run-to-run variance = the MEM-TMP temporal-recall finding already captured in the Phase-8 memory eval dimension. Not a one-off fix — a GEPA/8.4 optimization target. Re-evaluate after eval-driven tuning, don't hand-patch."
  },
  "sweep:marketplace:cta-density": {
    "status": "snoozed",
    "note": "DRAFT-REC (owner confirm) snooze, bundle with vestigial-stripe-ui: marketplace is a FREE-platform surface pending simplification; 85 CTAs shrink once the seller/payment path is cut. Don't polish a surface being reduced. Re-sweep after the free-marketplace shape is decided."
  },
  "sweep:marketplace:dup-affordance-save-to-watchlist": {
    "status": "rejected",
    "note": "DRAFT-REC (owner confirm) reject = BATTERY FALSE POSITIVE: 13x 'Save to watchlist' = 13 LISTING CARDS each saving its OWN item — the expected per-list-item pattern (KEEP-consistent, Jakob), not IA redundancy. The dup-affordance detector over-counts by counting label instances; it must key on (label x TARGET) and exclude per-card list actions. -> battery refinement (BATTERY_INTELLIGENCE_GAPS R-FP1)."
  },
  "sweep:marketplace:dup-affordance-view": {
    "status": "rejected",
    "note": "DRAFT-REC (owner confirm) reject = same FP class: 13x 'View' = 13 cards, each opens ITS listing. Per-list-item, not redundant. Battery detector fix R-FP1."
  },
  "sweep:marketplace:dup-affordance-close": {
    "status": "rejected",
    "note": "DRAFT-REC (owner confirm) reject = same FP class: 9x 'Close' = per-modal/per-card close buttons, each closes its own surface. Not redundant. Battery detector fix R-FP1."
  },
  "sweep:marketplace:dup-affordance-rb-romeo-beltran": {
    "status": "rejected",
    "note": "DRAFT-REC (owner confirm) reject = same FP class: one seller's avatar/name appears on their 3 listings; per-card, expected. Battery detector fix R-FP1."
  },
  "sweep:shell:fab-occluded-by-conn-chip": {
    "status": "approved",
    "note": "DRAFT-REC (owner confirm) approve [W-shell]. Real Major, cross-cutting (pm-scheduler + community confirmed; ANY bottom-right page .fab collides with the global #wh-conn-chip/#wh-hub cluster). Fix = a shell<->page stacking CONTRACT: global shell exposes a reserved bottom-right zone via a --wh-shell-bottom-inset CSS var; page .fab respects it (or move page FABs bottom-left on mobile). One shell edit closes the whole class. Touches the global shell -> owner-gated deploy."
  },
  "sweep:marketplace:vestigial-stripe-ui": {
    "status": "approved",
    "note": "DRAFT-REC (owner confirm) approve. Remove Connect-Stripe / seller-payout / checkout affordances across marketplace.html + marketplace-seller*.html + marketplace-admin.html — platform is FREE, no Stripe (project-free-platform-no-stripe). FIRST owner-confirm the intended free-marketplace shape (free swap/lend/giveaway board?) so it's remove-vs-convert; then a Frontend pass + re-sweep. Pairs with cta-density snooze."
  },
  "sweep:ia:relabel:overdue-asset-vs-task": {
    "status": "approved",
    "note": "DRAFT-REC (owner confirm) approve as RELABEL (NOT merge, NOT fix-math; both numbers are correct). Canonical differentiated labels ALREADY exist in data-rag-label (pm-scheduler 'Overdue PMs', dayplanner 'Overdue tasks') but the visible .sc-label shows bare 'Overdue' on both -> render the canonical label so a worker stops reading '2 vs 3 Overdue' as a contradiction. Low-risk. Corroborated by sweep:ia:theme:late-overdue + the journey value-drift (2 assets vs 3 tasks)."
  },
  "_DRAFT_RECS_2026-06-08": "10 disposition DRAFTS by agent (status+note prefixed 'DRAFT-REC (owner confirm)'): 4 approve (fab-occlusion, vestigial-stripe, overdue-relabel; +cta-density snooze) / 5 reject (4 marketplace dup-affordance = list-item FP, R-FP1 battery fix; +memory-buckets accept-by-design) / 2 snooze (voice-recall=Phase8 track, cta-density=bundle). Owner flips/ratifies and may drop the DRAFT-REC prefix.",
  "sweep:platform-wide:form-label-association": {
    "status": "approved",
    "note": "DRAFT-REC (owner confirm) approve [W-form] — mirror W1 (one systemic fix, not per-field). Fix the SHARED form-field templates to emit id+for + type-from-field-key + autocomplete; add a linkFieldLabels() helper for static fields; then add __UFAI.formAudit() to the L2 gate so it can't regress. DOWNPAYMENT shipped: resume basicsField (7 fields) labeled+typed+verified live. REMAINING: resume section editors + ~14 pages. High a11y value (every form on the platform is affected)."
  }
}